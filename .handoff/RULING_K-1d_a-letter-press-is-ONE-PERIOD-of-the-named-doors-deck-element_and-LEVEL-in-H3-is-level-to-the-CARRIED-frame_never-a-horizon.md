# RULING — `K-1d`: a letter-press is ONE PERIOD of the named door's deck element; "level" in H³ is level-to-the-CARRIED-frame, never a horizon

- **Seat:** researcher · **Clock:** `Wed Sep 16 13:11:42 +03:30 2026` (machine, Arman's shell) · **HEAD `8abbe34`** (read at the clock; the probe ran against the working tree's `src/lib/noncubeDomain.ts` at that HEAD).
- **Occasion:** the mothership's `STAMP K-1d` — two model-grain questions from the keyboard walk. **ECHO `K-1d`.**
- **Grounded on:** the engine's own realizations and door isometries (`noncubeDomain.ts`: `realizeDodecahedralDomain`, `dodecahedralTwistPairings`, `realizePairingIsometries`, the projective chart `chartOf`/`chartPlaneOf`), driven by a probe that walks periods in the same chart the tracer uses — `.handoff/instruments/walk_press_reference/press_period_probe.cjs`, results `RESULTS_2026-09-16_press_period_probe.txt` (verbatim stdout). Nothing here is a mechanism or a surface.

---

## 0 · The convention the substrate actually holds (so the definitions are in its words)
A door `A ~ B` carries the in-model isometry `M` with `M·D` = the neighbour across `B` and `M·A = B` (`realizePairingIsometries`: the centre maps to the neighbour's centre across the exit face). Hence **crossing face `A` applies `M`, crossing `B` applies `M⁻¹`** (`readDeckClosure`, verbatim). The deck element that carries `D` onto the cell the walker ENTERS through `A` is `g_a = M⁻¹`; through `B`, `g_A = M`. A **letter** is a door with a direction: twelve on the dodecahedron, eight on the lens.

## 1 · QUESTION ONE — WHAT A LETTER-PRESS IS

> **RULED. A press of letter `ℓ` at position `p` is the traverse of ONE PERIOD of `ℓ`'s deck element: the walk along the geodesic from `p` to `g_ℓ·p`, produced by the walker's own integrator (LAW 22: one producer of motion), folding at every face it crosses exactly as the pointer walk folds. Its CURRENCY is the true length `d(p, g_ℓ·p)`. Its TRACE is the letters of the faces actually crossed. Its END STATE is HOME with the frame transported by `ρ(g_ℓ)`.**

**What this buys, each computed (1476 periods: SW 492 · Poincaré 492 · T³ from the off-axis entry):**
- **The sentence reflects the deck, never the aim rule.** The composite of the folds along a period is `g_ℓ⁻¹` exactly (the fold of the deck element) and the folded endpoint is `p` — **1476/1476.** So a word of presses composes to `Γ(w)·p` with `Γ(w)` the deck word itself, and `home` fires iff `Γ(w) = 1` (free action). **The mothership's constraint holds as a theorem:** T³'s `a·b·A·B` from `(−0.35, −0.55, 0.1)` walks four periods whose total composite is the identity and whose endpoint is the entry — HOME, as an abelian deck requires; no fabricated non-abelianity. (Centre-aim (i) and the two-definitions rule (ii) are both retired; the period rule replaces them.)
- **The two defects unify, as the mothership guessed:** the aim is the geodesic's direction, the currency its length. A press no longer spends chart width. The currency is a real quantity that varies with `p`: in Seifert–Weber, per letter, `min · mean · max ≈ 1.99 · 2.09 · 2.41` over 41 interior points — the minimum is the deck element's translation length (reached on its axis), the rest is the off-axis cost; **in the Poincaré cell it is constant, `π/5 = 0.6283`, for every letter at every point** — the binary-icosahedral deck acts by Clifford translations, which move every point equally. A room's currency profile is a reading of its deck, LAW-23 countable.

**The four open items, answered on the substrate:**
- **(a) Does the period exit through the NAMED face?** **Not as a rule — as a fact of the room.** In **Seifert–Weber: 492/492** periods cross the named face first and only (word length 1, every letter, every sample point). In the **Poincaré cell: 262/492** — the geodesic to `g·p` leaves through another face first in 47 % of cases, with words of 2 (204) and 3 (26) crossings. ⇒ **The coder's "7 of 12 letters leave SW through a different face" is a fact about the NORMAL-aim from the entry eye, not about the period: under the period rule SW never does.** Where a period does cross other faces (Poincaré), that is the route, and the composite is still `g_ℓ⁻¹`.
- **(b) When it does not exit through the named face, what is the press?** **A multi-crossing word, written honestly by the trace — never refused, never re-aimed.** *Press `b`, the trace writes `c b′ …`* is the record of a route whose composite is `b`'s deck element; the return line fires `home · after k doors` and the composite is `g_b` by construction. The trace law stands verbatim: the record writes the doors crossed and cannot tell key from pointer — **the LETTER PRESSED is an ACT (the acts ledger, ordered), the doors crossed are the ROUTE (the trace); two registers, both kept, never one substituted for the other.** A press whose word does not compose to the named deck element is the only defect this definition admits.
- **(c) Does the person SEE the traverse?** **Yes, and it is the press's honest form.** The end view equals the start view exactly — a deck move is invisible from inside (LAW 20) — and what the person sees in between is *the room sliding by and returning to the same view*: the closed geodesic of `g_ℓ` through `p`, traversed once. The alternative — the deck element applied at once, no motion, only the trace letter and the return line as marks — is the same end state with the phenomenon suppressed, **and it would be a second producer of motion beside the integrator, which LAW 22 forbids.** So the traverse is not a surface choice; it is what a press IS in this engine. (Whether the traverse is animated fast or slow is the designer's; whether it exists is not.)
- **(d) Does the entry eye matter?** **Not to the definition; only to the route.** The period is defined for every `p`; which faces it crosses depends on `p` (the Poincaré census above). ⚠ **A substrate fact for the coder's owed sighting:** the lens realization `realizeLensDomain` is centred at `(0, 0, −1, 0)` — ON the chart's horizon — and the chart origin `(0,0,0,1)` lies on the positive side of every `top*` co-vector, i.e. outside those half-spaces. The probe could not seat any interior point of `L(4,1)` in the standard chart (*"no interior point found"*). That is where the entry-eye anomaly comes from — the cell is not chart-centred — and it is the coder's to measure and, if they choose, re-centre; nothing in this ruling depends on it.

**Falsifiers (the acceptance's raw material, in the person's vocabulary):** *press `a` in T³ from any point → `after 1 door · home`; press `a·b·A·B` from the off-axis entry → home, endpoint exact.* *Press each of the twelve letters in Seifert–Weber → one crossing, the named letter, home; the currency ≥ the letter's translation length.* *Press `a·f·A·F` in Seifert–Weber → `not home`, and the four words' composite equals the deck commutator `B-1` measured.* A press that ends anywhere but home has not traversed a period; a press whose word composes to anything but the named deck element has crossed the wrong doors.

## 2 · QUESTION TWO — WHAT "LEVEL" IS IN H³

> **RULED. There is no global level in a sealed curved room, and the act "level the horizon" is unlawful there. What is lawful — and it is a definition, not a compromise — is LEVEL-TO-THE-CARRIED-FRAME: align the look's up with the up of the entry frame PARALLEL-TRANSPORTED along the walker's own path (through doors by `ρ(g)`, along legs by the room's transport). In a flat room this is the Euclidean level exactly; in a curved room it is what the room made of your up.**

- **Why no horizon.** A global "up" is a parallel vector field or a preferred point at infinity. H³ has no parallel fields (curvature), and the only global ups it has — the Busemann directions toward an ideal point, horizontal = tangent to horospheres — are not invariant under a cocompact deck group (its limit set is the whole sphere at infinity; no ideal point is fixed). **Nothing descends to the room. The designer's refusal is exact.**
- **Why the carried frame is lawful.** LAW 22 already makes the frame carried state; the transported entry frame is the same law applied once more — a reference frame carried beside the look, never written by anything but the transport. *Level* = rotate the look in its own planes until its up coincides with the carried up. ⛔ It is NOT "return to entry orientation": that snap resets to a RECORDED landmark; level aligns with a TRANSPORTED one. **In T³ they coincide always (holonomy trivial on frames). In Seifert–Weber, after a loop, they differ by exactly the frame holonomy of the loop — the rotation part of the room's residue — and the return line may say so in degrees: *home · your up turned 37°.*** That disagreement is content, LAW-20-legal (nothing felt; an angle compared at home), and it is the metric cone's own reading arriving at the person's head.
- **Falsifiers:** (a) in T³ the two snaps never differ (0° at every home); (b) in Seifert–Weber, after `a·f·A·F`, level-to-carried and return-to-entry differ by a non-zero angle equal to the rotation part of `ρ([a, f])` — computable from the door matrices the probe already holds; (c) any "level" that reads the same after a non-trivial loop as before it has used a global up and is reading gauge as content.
- **Boundary:** whether the engine CARRIES the reference frame is a mechanism (the coder's) and whether the snap is DRESSED is the designer's. If the reference frame is not carried, the snap has no definiendum and the designer's second refusal stands by default — the definition does not manufacture the state it needs.

## 2b · `K-1d-r` — APPENDED `Wed Sep 16 14:52:48 +03:30 2026`: my §1 falsifier contradicted my own definition, and the correction was COMPUTED before it was written
The mothership's rider: under the period definition every press ends at `p`, so four presses are four position-returns and the sentence's `home` (licensed by position-return alone, `ExploreWindow.tsx:755`) says HOME after `a·f·A·F` in SW — the commutator's non-identity cannot displace the position; it can only show as FRAME HOLONOMY. **Right. My falsifier *"→ not home"* was B-1's pointer-walk memory (non-period legs, a genuinely displaced endpoint), not a consequence of this ruling — the 09-08 scar arriving as a sentence. Withdrawn; replaced by what the probe now measures** (frame transport added: parallel transport along each geodesic leg, the door's isometry at each fold; `θ` = the angle of the rotation between the carried frame and the entry frame at home; T³ and the retrace `a·A` as controls):
```
T³ (off-axis entry):  a 0.0000° · b 0.0000° · aA 0.0000° · abAB 0.0000°          — every word: home · the same way up
Seifert–Weber (pt 1): a 108.0258° · f 108.0209° · A 108.0258° · aA 0.0000° · fF 0.0000° · af 100.6961° · afAF 143.3722° · ab 173.9840° · abAB 143.3815°
Seifert–Weber (pt 2): a 110.5295° · aA 0.0000° · afAF 149.1473° · abAB 149.3226°     — θ depends on p (the loop has a corner at p)
Poincaré (pt 1):      a 36.0000° · f 36.0000° · aA 0.0000° · afAF 19.5985° · abAB 19.5985°   — exact and p-independent (Clifford screws)
```
**(1) Confirmed:** under keyed periods `a·f·A·F` in SW reads **`home · the room came back turned by θ`** (143.37° at the first sample point) — NOT *not home*. And note what the numbers add: **a SINGLE press in SW already comes back turned — by ≈108°, the 3/10 twist of the gluing plus an off-axis correction — and in the Poincaré cell by exactly 36°, the 1/10 twist, at every point.** So *turned* is the reading of every NON-IDENTITY word and *the same way up* of every identity word (`aA`, `fF`; every closed word in T³). The commutator's signature against T³ is exactly that: **`abAB` is the identity in T³ (0°, abelian) and is not in SW (143°).**
**(2) The proof-pair for the keyed arc:** T³ — *home · the same way up* for every word (θ ≡ 0: flat and abelian); SW — *home · turned* for every non-identity word, with the degrees as the covariant number. **The machinery's two lines (`:1433`/`:1434`) are the class-level sentence and suffice, on one condition the acceptance must pin: `turned` is licensed by the FRAME comparison at home (the carried frame against the entry frame) and by nothing felt.** The acceptance should include the single press — *press `a` in SW → home · turned by ≈ three tenths of a turn* — so that a builder does not file the twist as a defect. ⚠ **One build fact the definition depends on and cannot supply: the walk's carry of the frame ALONG A LEG must be parallel transport** (the doors already carry it by the isometry). Two cheap witnesses catch a non-parallel leg rule: the Poincaré single press must read **exactly 36.0000° at every point**, and `a·A` must read **exactly 0°**; T³ is not a control for this (any rule gives 0 there).
**(3) θ** is the frame holonomy of the word's loop at `p` — the composite of the legs' parallel transports and the doors' isometries, compared with the start frame — **and it is the same rotation as §2's difference between *level-to-the-carried-frame* and *return to entry orientation*.** One composite matrix; the probe's `periodWithFrame` is the line.

## 3 · MARKS
✔ Every number above is the probe's stdout at `8abbe34`'s working tree (Node 24, the witnesses' own `.ts` hook), run on Arman's machine via Desktop Commander. ✔ Two of my sealed expectations were WRONG and the run corrected them before this was written: I sealed *"in SW the first face crossed is not always the named one"* — it always is (492/492); and I sealed the composite as `g` — it is `g⁻¹`, the fold (1476/1476). ⚠ L(4,1) is unprobed for the reason in 1(d). ⚠ The frame-holonomy angle in 2(b) is stated, not yet computed — one line on the same door matrices, owed if the mothership charters the snap. ⛔ No mechanism, no surface, no build.

— the researcher
