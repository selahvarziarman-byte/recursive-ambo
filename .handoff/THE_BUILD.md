to: the coder
from: the mothership (sixth)
clock (raw, verbatim): `Wed Aug 26 13:15:38 +0330 2026` — mtime is the fact
**STAMP: `B-107`** ⛔ **Echo it. ⚠ QUEUED BEHIND `B-106` — do not start it until `B-106` is filed and reported. It sits here so your lane is never idle, per the new mail convention (`V4`).**

# R1 — THE METRIC RELAXATION. The front Arman aimed (Δ20), and the researcher has SEALED its definitional half.

## 1 · WHAT R1 IS (self-contained)
**The claim, carried on the board for weeks (`THE_CLAIMS_LEDGER.md` row R1):** *the icosahedron never appears.* The chain yields a **diagonalized cuboctahedron** named `pyritohedral-icosahedron` — its 12 vertex positions are **UNRELAXED**. ⇒ **A substrate-truth debt under everything the window draws for pyritohedral seeds, and it GATES two other rows** (R3's n=5 regular-fan gate; DUAL's Σ→4π seal).

## 2 · THE SEAL — the researcher's, ratified whole. Build to it, do not re-derive it.
**`.handoff/SEAL_R1_THE_METRIC_RELAXATION_t_equals_one_over_phi.md`** *(theirs; grounded by two probes they ran)*:
- **TARGET, derived:** the 12 vertices are cyclic perms of **`(0, ±t, ±1)`** on the cube faces. **`t = 1` IS the cuboctahedron** (what we have now); **`t = 1/φ = (√5−1)/2 = 0.6180339887498949`** is the regular icosahedron. **Why:** two edge classes — short `2t`, long `√(t²+(1−t)²+1)` — and **regular ⟺ equal ⟺ `t²+t−1 = 0`.** ⛔ **Unique: any other `t` is icosahedron-SHAPED, not regular.**
- **INVARIANT (the acceptance's core):** every one of the **60 corner angles of the 20 CARRIED faces**, by `acos`, within **ε = 1e-6 rad (5.73e-5°)** of **60°**. ⚠ **ε is a tolerance ON THE ANGLE, never on positions.** Their grounding, so you can check the choice rather than inherit it: true positions read `7.1e-15°`; **`t + 1e-6` reads `9.68e-5°` and FAILS**; sensitivity `dAngle/dt ≈ −51.6°/t`.
- ⛔⛔ **THE TRAP — and it is the clause that keeps the whole thing from being vacuous:** **the angles MUST be measured on the FIXED CARRIED COMBINATORICS, never on a distance-derived graph.** *A "connect the nearest pairs" graph SELF-SELECTS the equal edges (24E/8F) and reads 60° for ANY `t`.* ⇒ **A test that cannot fail, wearing a measurement's clothes.** ★ **Same hazard as `dualView:991`'s position-keyed ordering — a position-keyed structure re-shapes with the positions and hides the defect. KEY ON THE CARRIED COMPLEX.**
- **WHAT MUST NOT MOVE:** the **12V / 30E / 20F combinatorics — byte-identical face and edge ids, only positions differ** · 12 stays 12 · **the lineage: a position CORRECTION that marks itself and RE-BEGETS NOTHING.**
- ⛔ **THE LAW-24 CONTROL, BINDING: the UNRELAXED cuboctahedron FAILS BY 30°.** ⇒ **The witness must show FAIL → PASS. A green that was never red is vacuous.**

## 3 · YOUR PRICE — the part the seal explicitly does NOT cover
⛔ **R1's price carries a RE-DERIVATION ENUMERATION: the COMPLETE list of every artifact carrying owned angles derived from PRE-R1 positions, each with a disposition — re-derived, or marked stale-and-retired.** ⚠ **Not "the fan rebuilds":** by the D6 finding, **no seal can catch a stale stamp over new positions**, so it is enumerated **AT THE POINT OF CHANGE**. **The gate's own words, `THE_CLAIMS_LEDGER.md` §3.** ★ **The two off-path position consumers named there re-enter at this price** (`faithfulInkedModel:134`, `surfaceOperations:293` — era references; **resolve them at HEAD, and if either has moved or died, say so — I would rather learn my citation is stale than have you build around it**).
⚠ **And a caution from my own record this week: those two references come from a row I have not re-measured. Treat them as a STARTING POINT for your enumeration, never as its boundary.**

## 4 · ACCEPTANCE
**At your eye and in the witness:** the control fails by 30° · the relaxed form passes at ε · the combinatorics byte-identical (ids, not counts) · **the enumeration complete with every entry dispositioned** · nothing re-begotten. ⚠ **And say what a PERSON sees, if anything changes for them** — the window draws pyritohedral seeds, so a truer icosahedron may be visible. **If nothing visibly changes, say that too; a substrate-truth cut is allowed to be invisible, but not silently so.**

## 5 · WHAT COMES NEXT, so the shape is legible
**B.4 — the non-cube domain constructor — is rung 3's true head, and the researcher confirmed the dependency: R1 is B.4's PREREQUISITE, because the regular icosahedron and dodecahedron are the seeds a non-cube H³/S³ domain is built from.** ⇒ **Arman's ordering (R1 → B.4 → P5+UNDO) is a real chain, not a preference.** ⛔ **Start none of it here.**

## 6 · THE STANDING ASK
Arman, verbatim (`THE_SOVEREIGN_LEDGER.md` Δ6): ***"i'm tired. it's been 3 months. i need a race to the finish line."*** ⇒ **Build, drive, report. Echo `B-107`. File the report into `.handoff/inbox/mothership/` (Δ21) and copy it into the tracked pair at commit.**

— the mothership (sixth)
