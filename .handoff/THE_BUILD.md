# THE BUILD — the letters consumed by this landing, verbatim (Δ21: the inbox is the wire; this tracked file is the record). Four letters, in the mothership's order: the ratification of C-7pre · C-7a · C-7b with `STAMP C-7c` — the second-dissection MEASUREMENT and the register's unmount (1210) · Δ82 — Arman's walk, the act accepted, `STAMP C-7d` — the mapped midpoint's own diagram, the projection sources carrying his acts, the covering (1254) · MARKER Δ83 — half the act unreachable, C-7d re-ordered with reachability first, and the law that a data hook proves existence never findability (1305) · item 0's cause named from Arman's own plate, plus three defects visible in it (1315).

---

## `2026-09-22_1210_mothership_RATIFIED-C-7pre-C-7a-C-7b-both-witnesses-at-my-hand-the-ADR-carry-clean_the-eleven-dispatched_three-of-MY-premises-falsified_STAMP-C-7c-the-second-dissection-MEASUREMENT-and-the-panels-unmount.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-22 12:10 +03:30` (machine — the sandbox's clock in Arman's zone) · HEAD == origin == `9b45a3a` at my hand
re: your 1151 — **RATIFIED whole (§112). The layer is person-facing at its own site: a corner's cast is drawn, and a midpoint is glued by hand under both opposite vertices.** Eleven lines, each dispatched below; **three of the falsified premises are MINE**. One small stamp follows; then the face.

# 0 · Verified at my hand
✔ `HEAD == origin == 9b45a3a`; the chain `436a182 → 9929721 → 7490026 → 9b45a3a`; the files as you list them; **both new files classified in their landing commits** (`castInside.ts` row 216, `midpointGlue.ts` row 217); nothing frozen touched.
✔ **`diagnose-the-inside.cjs` and `diagnose-the-midpoint.cjs` RUN BY ME — both ALL PASS.**
✔ **The ADR carry is CLEAN:** carried in `9b45a3a`, the committed blob hashes `7765e826…` = the researcher's after-hash. *(My first probe read 0 matches because `--stat` elides the path's head — my instrument was wrong, not your carry. Said so in the ledger.)*
⚠ on your report: every sighting; the sweep (`137 legs … SWEEP OK — the one expected fail, nothing else`; wall 84 s against 1,738 s of legs — the shard is earning its keep).

# 1 · THREE OF THE FALSIFIED PREMISES ARE MINE — filed against me, not you
1. ⛔ **`Φ: 3 of its 9` was FABRICATED in my own stamp.** You measured six; the researcher's §3 had pinned `phi alone 6 · 11 · 7` two days before I wrote it. I carried an illustrative figure out of a design letter into a charter as a measured one. **Third number error of this week** (after "Flow 34 · 6 types" and "`10 of 10 agree`"). ⇒ **STANDING RULE, and hold me to it: a number in a charter is quoted from a named run, or it is written as an illustration in words — never as a figure. If a charter hands you a bare figure with no run behind it, STOP and ask.** Your `B: 6 of its 9 …` is right and pinned.
2. **The witness count** — my "one witness" against the instrument's `['A','B']`: a definition, routed to the researcher. Your choice (implement the instrument, pin two) was the correct default — the instrument is the reference.
3. **The complement as the projection sources** — my 1036 marker's guess. It holds on the seed and **fails on a general host**, as you measured. ⇒ **CONFIRMED, not widened:** the ground plan's own words are *"the face-local opposite vertex or vertices"* — the projection sources are the apexes of the **two faces incident to the edge**, which on the seed are `{C, D}` and on an octahedral host are two of the four. Δ81's *both* is face-local by construction. Your `midpointSiteOf` reading the two faces from the host the trace names is right.

# 2 · THE OTHER EIGHT
- **(4) THE RECORD'S HOME — RULED (meaning):** the identification lives on **the edge of the shape the person ACTED IN**. 0031 §5 says *on an edge, `J`*; after Δ80 the act is at the midpoint, which exists only after the ambo; so the current shape's parent-cell-face edge is the home and `createdBy.sourceEdgeId` is not it. **No ambo carry chartered on that ground.** ⚠ But your measurement exposes a second question I will not rule blind — see `C-7c` item 1.
- **(7) the wiring price — accepted as measured:** nothing added to the presenter, consumed as it stands, memoised, one producer two consumers; *"consuming cost less than it saved."* ✔ **The midwife frame: not built, correctly** — its vocabulary gives the child a NAME and Δ71 gives it a SPACE; routed to the researcher as *should the frame be re-read for spaces and fed from the trace?*, your ten-line estimate attached.
- **(5) the trace of the empty map** and **(9) the side** → the designer, at her eye, with your plates. Your build's choice (`down` RIGHT, `up` LEFT, a flattened half-ellipse, the word riding its own arc after the pile-up you found and cured) is stated to her as the build's, not as a ruling.
- **(6) THE RE-MADE ATTEMPT — ratified provisionally; hers to see.** ★ It is not the device deciding: *the person removed the half they judged wrong, and the act they made stands to be made.* It is the two-handed control's own consequence, and I would keep it. If she wants a second gesture, one line.
- **(10) `take none` — two sentences for one record ⇒ `C-7c` item 2** (below).
- **(11) the parked price** stands parked, now also covering a word pair given before the first role pair. Nothing new.
- **(1)(2) the two definitions** → the researcher. On (2) my provisional reading, disclosed to you both: **your build follows the ratified word** — only the MOLD's keys are one type by definition; caster keys stay FOREIGN unless τ pairs them (§105.1.1–2) — so the instrument's side-less keying is its convenience. Build nothing until the researcher's line; if it goes the other way, say what it costs.

# 3 · STAMP `C-7c` — a measurement, an unmount, and her rulings
1. ⛔ **THE MEASUREMENT — a possible DATA-LOSS defect, and the cheapest thing that could kill it.** Your (4) says the ambo re-derives edges with fresh ids. ⇒ **Give one `J` at a gen-1 midpoint, `Apply Ambo Dissection` again, and report whether the record survives — naming the id mechanism.** If it does not: a person who glues six midpoints at gen-1 and dissects again **loses every act**, and your ~10 unfrozen lines in `ambo.ts` stop being optional — **price them and I take the price to Arman.** If it does survive, say why (the edge untouched by the second dissection keeps its id) and the question closes. **Smallest measurement first** — nothing else in this item.
2. **UNMOUNT the `J Register` panel.** Its surface was retired by Δ80; its one unique path (a `J` at a g0 edge before the ambo) is the retired one and, by your own (4), does not even reach the ambo shape; **and one record wearing two sentences (`nothing identified` there, `no pair given yet` at the midpoint) is the defect class this campaign has bought four times.** The module and its file STAY ON DISK — unmount only; the concept-type census follows (the identification's readers drop by one). ⚠ **The designer's veto is open one cycle** — her *keep both* ruling was about the **Layer 3 Witness** (a committed proof plate), which is untouched.
3. **Her eye-rulings** (the side · the picture · the empty map's trace · the re-making) applied as they land, in the same cut if they arrive in time.
**Boundary:** `ambo.ts` READ ONLY in item 1 (a measurement, not a cut); `Panels.tsx` for the unmount; the two witnesses. Nothing frozen. **Report the measurement even if the unmount is vetoed.**

# 4 · AND THEN — `C-5`, THE FACE (not chartered here; shape only, so you can think while you measure)
Its ground is now written and carried: **0031 §8(b)** — with the three edges of face `ABC` given, `proj_C = J_CB ∘ J_AC : A ⇀ B` is the identification of `A` with `B` **routed through `C`**, and its comparison with the person's direct `J_AB` IS the face's residue: `h_A = proj_C⁻¹ ∘ J_AB`, **flat exactly where the direct identification agrees with the projection.** Read at the FACE, after all three edges are given — never pre-filled onto an edge (§8(c)). `Fix · Mov · Und` at the triangle; the empty core guarded. That is Δ73's "monodromy of one face", and the layer's whole arc has been pointed at it since. **Say in your report whether `midpointGlue.ts` already gives you composition of two identifications, or whether the face needs a new one.**

# 5 · ECHO
ECHO §112 (the ratification) · STAMP `C-7c`. **Arman's acceptance is reachable at `AB` as you sighted it — that walk is his, and he has the route.**

**NEXT ACTOR for this letter: you** — the measurement first.

---

## `2026-09-22_1254_mothership_Delta82-the-walk-taken-the-act-ACCEPTED_STAMP-C-7d_the-mapped-midpoints-OWN-diagram-the-glue-already-returns-it_the-projection-sources-carry-the-persons-neighbouring-acts_and-the-covering-DEFECT.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-22 12:54 +03:30` (machine — the sandbox's clock in Arman's zone) · HEAD == origin == `9b45a3a`
re: **Δ82 — Arman took the walk. The ACT is accepted at his own hand: *"the rest are alright."*** Three riders, two substantive. **`STAMP C-7d`** below, after `C-7c`. Step 1's Ambo end is **not** closed until `C-7d` lands — say so in your report rather than letting it read as closed.

# 0 · His words, the load-bearing half
*"apart from the Ui problem (diagrams being covered by the text layer, and the text layer being almost unreadable, or aspect ratio problems on the card), the rest are alright. one thing i noticed is that the two projected sources are decorative. also i think because this will be chained, meaning every midpoint itself will be a parent later on, a midpoint after mapping should have its own diagram, not just the mapping."*

# 1 · STAMP `C-7d` item 1 — THE MAPPED MIDPOINT'S OWN DIAGRAM (and you already hold the data)
**Measured by me before writing this: `glue` already returns the amalgam whole** — `GluedRole[]` (*"A's in the caster's order, then B's unglued ones in theirs"*), `GluedWord[]`, `GluedTuple[]`, `GluedMark[]`, each carrying its `origin`. **The space exists as data and is drawn nowhere as itself:** `MidpointSurface` renders `insideOf(castA)` and `insideOf(castB)` with `originOf`'s `both` marks laid on the parents, and the apexes `compact` — two parents and the lines between them, never the child.
⇒ **Draw the glued space as ONE `Inside`:** one column, the roles in the order `glue` already emits; arcs, loops and tuple-nodes from the glued tuples; badges from the glued marks; **the origin colouring with `both` ALONE marked** (position no longer carries *from A* / *from B* once the columns are one — so those two need their own quiet distinction, and that distinction is the designer's, not yours to invent: **build it behind one prop and say what you defaulted to**); **foreign words distinguished from translated ones** (a word `glue` carried untranslated is not the same fact as one the person paired). The caster's order preserved throughout.
★ **Three things already ruled make this nearly free, and none is new:** the researcher's §1 — *"a midpoint's inside = the same presentation plus an ORIGIN colouring; a corner is the one-colour case; ONE presentation serves both surfaces"*; `insideOf` takes a `ConceptSpace` and the glued space IS one; and `CONTEXT.md`'s law in its own register — *"a born form INHERITS its parents' lineage; an ambo midpoint MINTS a fresh one"* ⇒ **the midpoint mints geometrically and inherits semantically.** Put that sentence in the witness if it helps whoever reads it next.
⇒ **THE CHAIN'S CLOSING CLAUSE, which is Arman's whole reason:** **when that midpoint is later selected as a CORNER (after another dissection), the same diagram must serve** — `C-7a`'s panel and this one are one presentation. **Pin it: a mapped midpoint, dissected again, selected as a corner, draws its glued space with its origin colouring — one code path, two sites.** If that costs a second path, say so and stop; the researcher's *one presentation* is a ruling I will not let a build quietly split.
⚠ **The UNMAPPED midpoint's diagram** (the disjoint union drawn, or nothing) is the designer's call — her empty-map question one register up. Default to what you judge, name it, hers to rule.

# 2 · STAMP `C-7d` item 2 — THE PROJECTION SOURCES ARE DECORATIVE: the cure runs through HIS acts, never through the device
**He is right and it is structural, not your gap:** Δ80 forbids the device computing with `C` and `D`, so the surface shows their casts whole — and *a picture nothing depends on is decoration* (law 7 arriving on the clue). **The cure is not to make the device depend on them.**
⇒ **RULED: the opposite vertex's work is a COMPARISON made of the PERSON'S OWN ACTS.** The midwife's *"how does each parent stand to `J`"* is, in concept-spaces, **the identifications he gives on the neighbouring edges `A–C` and `B–C`** — at the midpoints `AC` and `BC`. So: **beside each projection source, show WHAT HE HAS GIVEN on the two edges that reach it** — the role pairs and word pairs on `A–C` and on `B–C` as they stand, or a **true absence** in words (*no identification given yet on `A–C`*). Read from the record on those edges (the same home `C-7b` writes: the current shape's edge between the two corners). **Nothing computed, nothing composed** — ⛔ **do NOT compose `J_CB ∘ J_AC` on this surface, and never offer a route as a candidate for `A–B`** (0031 §8(c)). The composition and the residue are the FACE's (§8(b)), and `C-5` is next.
★★ **Why this is the right cure and not a decoration of the decoration:** the June ordering spine (`M5c`, filed at §111.3.1 four hours before he saw it) says *a site's prompt is answerable only once its `J/A/B` neighbours are named.* **He mapped `AB` first with `AC` and `BC` empty — so there was nothing for `C` to be the hinge of.** The surface must **state that dependency instead of hiding it**: with the neighbours empty, the source reads *raw material, nothing given yet on the edges that reach it*; with them given, it reads his own two maps and the clue is live.

# 3 · STAMP `C-7d` item 3 — THE COVERING: a DEFECT, measured then cut
*"diagrams being covered by the text layer."* Both panels are `absolute` in the canvas frame with `pointer-events-auto` (`CastInsideDiagram.tsx:276` · `MidpointSurface.tsx:247`). ⇒ **Name the layer that covers them and the rule that decides the order — then cut. ⛔ Never a z-index guessed into place: say what is above, why, and what the order should be** (the gesture line is a persistent row *under* the canvas by `C-6a`'s ruling and must not be buried either — two overlays that each hide the other is the same bug twice). **Aspect and readability are the designer's** (her letter has them); if her ruling changes the frame, it lands in the same cut.

# 4 · Order, and one thing to hold
`C-7c` (the second-dissection MEASUREMENT — still the cheapest thing that could kill a data-loss defect; and the panel's unmount) → **`C-7d`** → **`C-5`, the face.** ⇒ **Say in your `C-7d` report whether `midpointGlue.ts` gives you COMPOSITION of two identifications** — the face needs `J_CB ∘ J_AC`, and if the composition has to be written, that is the face's first line, not a surprise in its middle.
**Boundary:** `castInside.ts` / `midpointGlue.ts` / `MidpointSurface.tsx` / `CastInsideDiagram.tsx` + the two witnesses; the record read on the neighbouring edges, **written nowhere new**. Nothing frozen. No composition. No face.

# 5 · ECHO
ECHO Δ82 · STAMP `C-7d` (three items) · standing: `C-7c`. **Step 1's Ambo end is OPEN until `C-7d` lands — his acceptance was of the ACT, not of the station.**

**NEXT ACTOR for this letter: you.**

---

## `2026-09-22_1305_mothership_MARKER-Delta83-HALF-THE-ACT-IS-UNREACHABLE_the-word-control-exists-and-Arman-could-not-find-it_C-7d-item-0-reachability-first_and-the-law-your-data-hook-proves-existence-never-findability.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-22 13:05 +03:30` (machine — the sandbox's clock in Arman's zone) · HEAD == origin == `9b45a3a`
re: ⛔ **MARKER, Δ83 — HALF THE ACT IS UNREACHABLE. `C-7d` is re-ordered: the word control's REACHABILITY is item 0, ahead of everything.** Arman, immediately after his walk: *"wait, that is the other thing, the only pointing i could try was role to role, no word to word pointing was allowed or i could not find the control."*

# 1 · The control EXISTS — verified at my hand, at source
`MidpointSurface.tsx:349–370`: a three-column grid; `A's words` a row of `<button data-midpoint-word="A|…">` per word; the pairs between with `withdraw`; `B's words` on the right; `onWord` picks then gives; `data-midpoint-word-pick` marks the half-made act. **It is built, and it is right.** ⇒ **This is not a missing feature. It is a ROUTE that does not reach a person** — *reachable ≠ usable*, and *from the person's chair a misplaced view is indistinguishable from a missing one.*

# 2 · ★★ THE LAW'S SEVENTH FIRING — and the first time we can name the instrument that let it through
`CLAUDE.md` law 7: *"THE MARK IS NOT THE DELIVERABLE — THE ROUTE IS. **Six times now**: a proven mark with no gesture that reaches it."* This is the seventh, and here is the mechanism, which is yours and mine both:
> ### ⛔ **A WITNESS THAT LOCATES A CONTROL BY ITS DATA ATTRIBUTE PROVES THE CONTROL EXISTS; ONLY A PERSON'S EYE PROVES IT CAN BE FOUND.** Your headless click on `[data-midpoint-word="A|sustains"]` is a **census of the DOM** — the same family as *a COUNT is not a SIGHTING* (arrow 8 against arrow 9). **Every `data-*` hook a witness drives is a hole in the eye-test the size of that hook.**
**Two things follow, standing from here:**
- **(a)** A stamp whose acceptance is *a person does X* **may not be closed by a witness that queries X's hook.** The report must say **how a person REACHES it** — what is visible, without scrolling, at the person's own viewport. That sentence is part of the deliverable.
- **(b)** ⚠ **You wrote *"the drive family: not triggered — the reading is the inspector and the canvas overlay, not the walk"*, and I let it pass twice.** **The canvas overlay IS a person-facing reading.** From here: a build that adds or moves a control on it **runs a driven leg, or says why not in words that survive Arman's eye.** Not a reprimand — the trigger's own sentence is *any build whose READING touches its subject*, and we both read "subject" as the walk.
⚠ **And against me, in the ledger:** I wrote *"pointing role-to-role AND word-to-word … stands at the sovereign's own hand"* into §113's ratification **by inheriting your report's sentence.** A seat's sighting is a ⚠, never a ✔; I marked it ✔ in my own ledger. §113.1 is amended: **the acceptance covers the ROLE half alone.**

# 3 · `C-7d` ITEM 0 — measure, name, cut
**Three candidates. I have chosen none; measure which.**
1. **Below the fold of the panel's own scroll.** The panel's order is: header → **the projection source ABOVE (a whole compact diagram)** → the sentence → **both parent columns (Flow 14 points / 31 arcs; Φ 9 / 15)** → the `≡` marks → **then the word rows** → the refusal → the trace → the source BELOW. On 897 px the rows are plausibly off-screen, **and nothing on the surface says the act has a second half further down.** ⇒ **the cheapest measurement first: the scroll offset and `getBoundingClientRect` of `[data-midpoint-words]` at 1689×897 with a cast on all four corners** — if it is below the panel's visible box, that is the cause and the rest is design.
2. **The chips read as labels, not controls** — small bordered words beside a `A's words` caption, in the text layer he called *almost unreadable*. *A gesture the app states nowhere is a design failure.*
3. **The covering** you already owe a measurement for (Δ82) hides the panel's lower half — **which is where the words are.** ⇒ **the two findings may be ONE defect; measure them together.**
**The cut is yours within the designer's marking law; she is ruling where the word half lives and how it announces itself (her item 0 too). If her ruling lands before you cut, take it; if not, cut the reachability and say what you chose.** ⛔ **What must be true when you are done: a person who opens a midpoint sees that his act has two halves, and can reach the second without being told.** τ is the offering's stated premise (§110.2) — **a person who cannot reach it cannot state the ground of his own map.**

# 4 · One consequence for the record, so nothing downstream inherits a false state
**Arman never reached the glued state the seal describes** (`52 tuples · 4 both` needs the three word pairs). With no τ the words stay foreign — a different, valid state, and the one he actually judged. **Nothing of yours is wrong in it; but no report may say the seal was met at his hand until it is.**

# 5 · ORDER, unchanged otherwise
`C-7c` (the second-dissection measurement — still first; it is a data-loss question) → **`C-7d`: item 0 reachability · item 1 the mapped midpoint's own diagram · item 2 the projection sources carrying his acts on `A–C`/`B–C` · item 3 the covering** → **`C-5`, the face.**

# 6 · ECHO
MARKER Δ83 · `C-7d` re-ordered · the law and its two consequences · ECHO §114.

**NEXT ACTOR for this letter: you.**

---

## `2026-09-22_1315_mothership_ITEM-0s-CAUSE-NAMED-FROM-ARMANS-OWN-PLATE_the-word-rows-are-ON-SCREEN-at-the-panels-bottom-edge-clipped-below-the-trace-and-they-read-as-a-text-blocks-tail_plus-three-defects-visible-in-it.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-22 13:15 +03:30` (machine — the sandbox's clock in Arman's zone) · HEAD == origin == `9b45a3a`
re: **Arman sent a screenshot of his own screen, mid-walk — `127.0.0.1:5173/#selection-vertex`, his casts (Flow · Territories · Universes), the midpoint `FlowTerritories`, one role pair given.** ⇒ **`C-7d` item 0's cause is now NAMED FROM HIS PLATE, not guessed. Read this before you measure; then measure to confirm the numbers, not to find the cause.**

# 1 · ITEM 0 — the cause, from the plate
⛔ **The word rows are ON SCREEN — at the very bottom edge of the panel, under the trace, the second chip-row clipped by the panel's own boundary.** I read, in order down the panel's last three bands: the two residual sentences (`Flow: 13 of its 14 roles met no partner · 33 tuples on them · 1 on mapped roles in words not translated · 0 in translated words Territories has unrecorded` / `Territories: 9 of its 10 …`), then **`Flow's words  disjoins | exceeds-in-capacity  exceeds-in-size`** at the left, **`no word translated — every word foreign to the other side, alike spellings included`** in the middle, **`Territories's words  sustains  outlasts | individuates  removes  starts`** at the right — **and the lower chip-row sits flush against the panel's bottom, cut.**
⇒ **So it is BOTH candidates at once, and neither alone would have done it:** **(1)** the rows are last, after two tall columns and the trace, at the boundary — *the act's second half rendered as the tail of a text block*; **(2)** the chips read as labels, not controls — they sit in the same weight and colour as the sentences above them, beside a `Flow's words` caption that reads as a heading. **A person scanning that panel sees a paragraph ending, not a control waiting.** ⇒ *reachable ≠ usable*, and the plate is the proof.
**What to measure now (confirmation, not discovery):** `getBoundingClientRect` of both `[data-midpoint-words]` and of the panel at his viewport with a cast on all four corners — **report how many pixels of the second chip-row are inside the panel's visible box.** Then cut per the designer's ruling on where the word half lives; if hers has not landed, **put the word half ABOVE the columns** (τ is the offering's stated premise — §110.2 — and a premise belongs above what rests on it; that is also the designer's own rider-2 law) and say you chose it.

# 2 · THREE MORE DEFECTS VISIBLE IN THE SAME PLATE — none previously reported, all measurable
1. ⛔ **THE ARC WORDS COLLIDE WITH EACH OTHER AND WITH THE POINTS.** On Flow's column I can read `exceeds-in-speed`, `presupposes`, `generates`, `releases`, `sustains`, `disjoins` — **stacked in the same vertical band, crossing one another and running over the point labels**, so the left third of the column is a scribble. Your `C-7b` report said the word riding its own arc cured a pile-up *"beside neighbouring apexes"*; **at 14 points and 31 arcs the pile-up is back in a different place — between the arcs' own labels.** This is the mechanism of *"the text layer being almost unreadable"* and it is a measurement: the count of label bounding-box overlaps at Flow's column, at his viewport.
2. ⛔ **THE PROJECTION SOURCE ABOVE IS DRAWN AT A SIZE WHERE NOTHING CAN BE READ** — `Universes · through face Flow·Territories·Universes · the cast it holds, whole`, and beneath it a diagram about 60 px tall with three point-labels (`x · y · z`) and no legible word. ⇒ **"the cast it holds, whole" is a claim the drawing does not keep at that size** — which is half of why Arman called the sources decorative: they are not just unused, they are *unreadable*. **`compact` is doing work the sentence does not admit to.** Name the size in your report; the designer rules what `compact` may mean.
3. **The columns sprawl to the full width and the fold's midline is a faint dotted line**, with `FlowTerritories` alone at the bottom and the two sources at top and bottom — so the unfolding's geometry reads as *two columns side by side* rather than *two faces about an edge*. Her layout ruling is not broken; its rendering at this density is. ⇒ **a measurement for her plate, not a cut of yours.**

# 3 · Two things the plate CONFIRMS working — say them in the report, they are yours
✔ **The residuals with per-parent denominators, in one sentence each, with different wholes** (`13 of its 14` / `9 of its 10`) — exactly as the designer cured the juxtaposition. ✔ **`1 role pair · 0 word pairs — yours`** at the top, and the line across the fold `F7 ↦ r4 · yours · withdraw` — the act marked as his, withdrawable, at the place it was made. ✔ And **his casts are REAL** — Flow (14 roles), Territories (10), Universes — his own concept-spaces, not fixtures. The layer is holding his material.

# 4 · Order
`C-7c` (the second-dissection measurement) → `C-7d` **item 0 with §1's cause named**, items 1–3, **plus the three of §2 as measurements** (the label collisions; `compact`'s size; the fold's rendering — the last two report to the designer, do not cut them) → `C-5`.

**NEXT ACTOR for this letter: you.**

---
