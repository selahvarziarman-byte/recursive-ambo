# THE BUILD — always current, self-contained

**STAMP: `B-2026-08-24-C`** ⛔ **Echo this stamp at the top of your report.**
**Written by: mothership · base `a4f7d04` · branch `team-arman`**
*Read `CLAUDE.md` at the repo root first. **If anything below contradicts what you MEASURE, your measurement wins.***

⚠ **Supersedes `-A` and `-B`. R1/R2/R7/§5/§7/§8 are settled and unchanged. ⛔ §6 CHANGED SUBSTANTIALLY — the designer overruled her own no-suffix bound, the arithmetic line is UNBLOCKED with a new shape, and I had MISSED THE WITNESS. Read §6 whole.**

---

## 1 · ✅ `B-2026-08-23-C` RATIFIED — and ⛔ **ONE THING I RATIFIED WAS WRONG. Mine, not yours.**

✔ **The sweep ran FIRST, SHA-pinned, ranked, exclusions declared.** ✔ **D16: `AB₀ · AB₁ · AD₀ · AD₁`, `·A`/`·B` nowhere, rungs reading `—f ← AB₀·AB₁`.** ✔ **Ambo's CELL FACES lead with `AC·CD·BC`; the card has a face register.** ⇒ ★★ **The 08-19 finding is closed.**

★★★ **And the best measurement in the report is the one nobody asked for: on a PRE-MIGRATION band the reach REFUSED the base's id-copy labels, and the letters remained THERE, in the open, on a real file.** ***A guard that is only argued about is a hope; a guard seen refusing is a fact.***

> ### ⛔ **MY ERROR: I wrote `✔ v0 is gone from his screen — an invoked Triangle reads •unnamed·A — born` and marked it ✔. `unnamed·A` is a REGRESSION and I ratified it.**
> **I checked that the DEFECT (`v0`) was gone. I never checked what ARRIVED against the SPEC, which said `unnamed`, plain.** ⇒ **Second instance in four days of the same failure of mine. Not your defect — you shipped a reasonable local fix to a collision; the ruling that should have caught it was mine and it was already written.** §6 cures it.

---

## 2 · ⛔⛔ R1 — RULED. **`leave bounded` CONSUMES the picked pairs.** *(unchanged — build this first)*

**What you measured:** a pair committed, **drawn solid on the domain** so the figure confirms it → press `leave bounded` → the room births **`face-pairs 0 (all preserving)`, orientable `yes`** — **his reversing pick vanished without a sentence**, while `glue — the S² gate judges` takes the same pair correctly.

> ### ⇒ **THE RULING IS ALREADY WRITTEN, ON THE PANEL, ABOVE THE BUTTON:**
> **`pair the ones you choose — the rest stand as walls`** and **`leave bounded — the free rim stands as walls`.**
> ⇒ ⛔ **The copy promises *his pairs PLUS walls*. The code delivers *walls only*.** ***A name that describes INTENT protects code that does not FULFIL it*** — and here the name is a ratified sentence sitting directly above the control.
> ✅ **RULED: `leave bounded` CONSUMES the picked pairs and walls the remainder. Option (b).**

**Why not the other two, so nobody re-opens them:**
- ⛔ **Refuse while a pair stands** — contradicts the **07-18 seal**, which explicitly permits pairing some and holding a body with a boundary. **A cure that violates a ratified seal is not a cure.**
- ⛔ **Speak a drop-sentence** — makes the loss honest and **keeps the loss.** **Nothing needs to be lost here at all.**

> ### ★★★ **THE CAUSE IS THE FIFTH INSTANCE OF ONE THING.** `leave bounded` was written when a domain was either **fully glued or fully open** — true then, stale the moment the **07-18 seal made partial pairing legitimate.** The others: **the `χ===0` predicate · `the identified cube` · `(no body exists)` · the `:625` subtitle.**
> ⇒ ***A seal that widens what the person MAY do must sweep the PREDICATE, the SENTENCE, the TYPE, the WITNESS, the PARENTHESIS — and the BEHAVIOUR.***
> ★★ **The designer's ordering, and carry it into the sweep:** ***a stale SENTENCE misleads; a stale BEHAVIOUR destroys.*** **This is the first of the five that costs him WORK rather than telling him something false. Rank the sweep's finds by that.**

⚠ **ONE TARGETED SWEEP RIDES THIS CUT — not a general one:** **what else assumes a domain is either FULLY GLUED or FULLY OPEN?** ⛔ **Bound it to that predicate.**

**ACCEPTANCE, at the eye:** pick one reversing pair, **press `leave bounded`**, and the room reads **`face-pairs 1`** with **the pair's own mode**, `orientable` reporting the truth of *that* pair, the rest standing as walls. ⛔ **Both directions:** a pair picked ⇒ consumed; **no pair picked ⇒ all walls, exactly as today.**

## 3 · R2 — THE PORT-FACE MENU NEVER GOT THE REACH *(unchanged)*
Your finding: the port/cut-face menu lists **`unnamed · 4 corners` ×12** for **the very faces the card now names.** ⇒ **A fourth reader on the same fact that did not get the resolver.**
⇒ **Wire the composer that exists** (`lineageCornerDisplay` / the D14 rotation) — **the one the card, the menu and the traces already share.** ⛔ **Do not write a fifth path.**

## 4 · R7 — TWO SMALL COPY DEFECTS *(unchanged)*
1. ⛔ **The thicken refusal misfits its operands:** *"the product of two surfaces is a 4-manifold"* fires over a **surface × SEGMENT.** **Say what he actually did.**
2. **`thicken: thicken: …`** — the notice doubles the word.
⚠ **If either needs more than naming the true operands, stop and route it to the designer.** **Fixing a typo is yours; authoring a refusal is hers.**

---

## 5 · ✅ `specimenModel.ts` — **UNBLOCKED. Her words landed. SANCTIONED frozen union.**

⛔ **ITS OWN COMMIT: the edit + the manifest re-seal, nothing else in it.** Re-seal recomputed with the **positive control** (the OLD hash reproducing at the base).

**(a) THE χ NOTE — her exact words, and the layout ruling: the NUMBER stays in the value, the CLAUSE moves to the note.**
> **value: the bare number.**
> **bounded →** `χ — the closed-world check does not apply to a room`
> **unsound →** `χ — a bare count; the S² gate found no manifold for it to describe`

★ **Her ruling on which register these share, answered:** **they share the NOTE register with the twist note** (`w₁ ≠ 0 — non-orientable (the twist)`), **and each names its own subject** — ⇒ **two notes never have to be told apart by POSITION.** *(Your two-notes room already proved both can sit at once without clobbering.)*

**(b) THE QUANTIFIER:** **`face-pairs 1 (all mixed)`** on a ONE-pair reversing room — **`all` hardcoded before the fork, and `mixed` is wrong over a single pair; it should read that pair's own mode.**
★ **A QUANTIFIER asserted before the thing it quantifies is known.**
⚠ **ORDER MATTERS: build R1 FIRST, then measure this on a post-R1 `leave bounded` one-pair room.** ⇒ **R1 opens a SECOND route to the exact state that exposes the bug, and the sentence the person reads is the same sentence in both.**

---

## 6 · ⛔ **NEW — THE BORN-CORNER SUFFIX COMES OFF THE ROWS.** *(the designer ruled; I concur, and the code convicts itself)*

**The site:** `argumentReadingModel.ts:353` — `rootLabelOf.set(id, \`${name}·${letterFor(k, ROOT_LETTERS)}\`); // index, never the name` — and `:443` appends the same to a row label.

**THREE independent convictions, from three instruments:**
1. ⛔ **Hers (legibility):** `unnamed·A / unnamed·B` are **name-shaped and ~90% identical — the eye must read to the last character.** ⇒ **A REGRESSION: `v0` was at least short and wholly distinct.**
2. ⛔ **Mine (doctrine):** `letterFor(k, …)` is a **POSITIONAL INDEX — `v0` in a different alphabet — welded into the NAME'S OWN SLOT.** **BORN ⇒ a TRUE ABSENCE. `unnamed` + an index is not an absence.** The migration removed positional indices from the name slot; this reinstates one.
3. ⛔ **The substrate's own comment:** **`// index, never the name` — written directly on the line that puts the index exactly where the name is.** ***A comment that draws a distinction the code does not draw.***
★ **And it fires ONLY on a name collision** (`rootNameCount > 1`) ⇒ ***a mark emitted only when a resolution FAILS is a symptom wearing a letter, not vocabulary.***

> ### ✅ **RULED: the ROW REGISTER carries NO suffix. Three born corners read `unnamed`, `unnamed`, `unnamed`.**
> **The disambiguation is THE FIGURE — hover or select a row and that corner lights on the shape.** ⛔ **No new gesture** — the same idiom already shipped twice (the map option previewing its twist; the face register's name-on-attention). ★ **The list stops trying to be an identity and becomes a POINTER, which is what it is.**

### ✅ **THE ARITHMETIC LINE — UNBLOCKED, and she RECLASSIFIED it against her own rule**
`ManuscriptView.tsx:4944` renders **`p ← {unnamed, unnamed}`**. I showed it to her as a legibility exception. ⇒ ⛔ **She ruled it is not one: *the line's job is to say p came from TWO DIFFERENT places, and with both terms identical it says the opposite. That is not hard to read — it is WRONG.*** ★ **A FALSE SENTENCE outranks her no-suffix bound, and she said so rather than let her own rule win an argument it should lose.**
⛔ **But the suffix is still not the cure** — `{unnamed·A, unnamed·B}` invents two identities the form does not have.

> ### ✅ **RULED — THE LINE CHANGES SHAPE: it NAMES what it can name and COUNTS what it cannot.**
> | case | the line |
> |---|---|
> | both named | `p ← {AB, CD}` |
> | neither named | **`p ← two unnamed roots`** |
> | one named | **`p ← {AB, one unnamed root}`** |
> ★★ **AND IT IS GENERAL, which answers the requirement without a handle:** a genuine collision of two roots both named `AB` reads **`p ← two roots named AB`.**
> ⇒ ***When two terms in a composed sentence cannot be told apart by their names, the sentence COUNTS them instead of indexing them. A sentence that cannot name its terms distinctly says HOW MANY there are; it does not invent labels to fill the slots.***
> ⚠ **The trap she named: SET NOTATION `{ , }` demands a token per element, so it MANUFACTURES one. The count form has no slot to fill.** ✔ True · ✔ countable against the figure (LAW 23) · ✔ no index in a name's slot.

⇒ **YOUR TASK:** **enumerate every consumer of the suffixed root label and say what each one does** *(a census runs in two directions — name all N)*, then **rows read the plain name, and the composed sentence takes the count form above.** ⛔ **The requirement is general: no positional index in the name's slot, ever.**
**Mechanism is yours.** ⚠ **If a consumer will not take either form, STOP and report it — that is meaning and it comes back to me.**

### ⛔⛔ **AND I MISSED THE WITNESS. Mine — catch it before you cut.**
**`4d32b93` recut `diagnose-argument-card` and PINNED THE SUFFIX:** §11 SEED+INVOKE pins **`unnamed·A..·D`**, and §11 GLUE pins the torus class **`unnamed·A·unnamed·B·unnamed·C·unnamed·D`.**
⇒ ⛔ **This cut FAILS that witness by design. The recut rides the SAME commit.** ★ **My own law, quoted two sections above in this very file — *a ruling that changes what the person READS must sweep the PREDICATE, the SENTENCE, the TYPE, and the WITNESSES* — and I swept three of the four.**
⚠ **Recut the pins from THE RULE, independently — never from the new output.** *Agreement is not correctness, and it is the fakeable one.*

### ⚠ **A THIRD CONSUMER, FOUND IN THAT WITNESS — and my reading is that a RULED GUARD already owns it. MEASURE IT.**
**The torus class name is FOUR suffixed terms concatenated: `unnamed·A·unnamed·B·unnamed·C·unnamed·D`.** ⇒ **Neither a row nor a sentence — the NAME of an identified class, the COMPOSED case (a class from its sources, exactly as a face from its corners).**

> ⚠ **MY READING, INFERRED FROM SOURCE, NOT RUN — your measurement decides it:** **the compose-over-absent guard should already own this.** `ManuscriptView:1059` and `apertureModel:606/612` refuse a composition over an absent source to **`unnamed`**. ⇒ ⛔ **The suffix was BYPASSING that guard: it dressed four ABSENCES as four PRESENCES, so compose-over-absent never fired.** ⇒ **Removing the suffix should RESTORE the guard's domain and the class should read `unnamed` — not `unnamed·unnamed·unnamed·unnamed`.**
> ★ **And the ARITY is not lost: it survives one line over, on the arithmetic line, in her own count form — `… ← four unnamed roots`.** ⇒ **The meaning-trace holds; the name stops carrying what the sentence carries better.**

⇒ **MEASURE IT AND REPORT WHAT YOU SEE.** ✔ **If the guard fires and it reads `unnamed`, say so and pin it.** ⛔ **If it reads `unnamed·unnamed·unnamed·unnamed`, STOP — do not invent a form. That is a meaning question, it comes back to me, and the designer already has the case.** ⚠ **My reading is ⚠, not ✔. Do not inherit it.**

⛔ **AND THE INVITATION CLAUSE IS WITHDRAWN.** I asked for copy making `unnamed` read as an invitation. **She refused, on my own law, and she is right: the naming gesture does not exist, and *an invitation to an absent act is a promise the app cannot keep — the person goes looking for the door.*** ⇒ **`unnamed` stays plain and undecorated. Nothing is owed here. The clause gets written the day the word becomes clickable.**

## 7 · ⛔ **NEW — R9's cure is a STANDING MARK, not a prompt.** *(hers, ruled)*

⛔ **A prompt at the moment of reload is a limit placed AFTER the act it should have prevented — and it costs him the whole session instead of one pick.** ⇒ **MISPLACED.**
> ### ✅ **RULED: a QUIET STANDING MARK that the page holds unsaved work, in the SOURCES panel beside `save the page…` — where the act lives, visible while he still has a choice.**
✔ **Mark the UNSAVED state only** — *there is work here that is not written down* is a positive fact about something he did.
⛔ **Do NOT mark "saved."** **A page with nothing to save is the ORDINARY case, and marking it spends a line teaching him that most states are unremarkable.**
⚠ **A reload guard is acceptable as a BACKSTOP, never as the cure.**
★ **And the asymmetry is worth a plain sentence somewhere he'll meet it: an UNMOUNT survives; a full RELOAD does not.** **That is not obvious, and it is exactly what made this read as data loss.**

## 8 · ⛔ **NEW — R8: widen the TARGET, not the INK.** *(hers, ruled)*

> ### ✅ **RULED: the dim-1 HIT REGION goes to ≥ 24px around the drawn stroke. The INK stays hairline. The look does not change.**
⛔ **Do NOT fill the Loop's hollow.** **A loop is a 1-complex, not a disk — making its interior pickable teaches a false ontology in exchange for a click.**
★★ **Why this is not cosmetic, in her words:** ***a hairline target and a broken door produce the same observation.*** **I could not hit a standing form on 08-19, concluded the capability did not exist, and filed that to three seats.** ⇒ **The failure mode is not a missed click — it is a person concluding the capability is absent.** **A target that produces that conclusion is a defect at full weight.**
✅ **AND THE ONE OPEN FACT IS CLOSED — Arman, asked and answered: *"ring's line."*** ⇒ **His model is CORRECT; he treats the Loop as the 1-complex it is. No ontology question, nothing owed to the designer, nothing dangling on this task.**
> ⇒ ⛔ **Which makes it a HARDER defect, not a softer one: right gesture, right aim, FIVE MISSES.** **The hairline is the whole of it.** ★ **And his answer is a POSITIVE CONTROL** — my own misses confounded three causes (wrong gesture · hairline · absent capability); **his hold two of them fixed and isolate the third.** ⇒ **Build the 24px against that, and if you can, drive one Loop pick at the LINE before and after.**

---

## 9 · NOT YOURS
⚠ **The disambiguator's form** — hers. **You designed nothing, correctly.**
⚠ **The `◻` glyph — ruled by her on my measurement, and it STAYS** *(the card is ONE FLAT SEQUENCE with `† — ◻ ⚠` sigils, not homogeneous blocks; kind is what CHANGES row to row, so the glyph is vocabulary, not noise)*. ✔ **`· 4 corners` stays.**
⚠ **BUT `◻ {N} faces` (the `> 8` elision at `ManuscriptView.tsx:1063`) IS COMING** — she ruled **the glyph innocent and the ROW'S GRAMMAR wrong: an ENTRY and an ELISION are wearing the same shape**, so it must read as *there is more here than I am showing* and never as an item called "12 faces." ⛔ **Not yours yet — she is writing the string.** **Do not touch that row in this build.**

## 10 · ORDER, AND WHAT MUST LAND
**R1 → §6 rows → R2 → R7 → §7 → §8 → §5 (its own commit, last).**
⛔ **R1 is the one that must land** — it is discarding his work every time he presses that button. **A partial delivery that is REPORTED is honest; a full delivery that is narrated is not.**

## 11 · THE STANDING ASK
Arman: ***"i'm tired. it's been 3 months. i need a race to the finish line."*** ⇒ **Build, drive, report. Echo the stamp. Push when it lands.**
