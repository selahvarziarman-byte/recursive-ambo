# THE BUILD — always current, self-contained

**STAMP: `B-2026-08-25-A`** ⛔ **Echo this stamp at the top of your report.**
**Written by: mothership · base `4caf00a` · branch `team-arman`**
*Read `CLAUDE.md` at the repo root first. **If anything below contradicts what you MEASURE, your measurement wins.***

---

## 1 · ✅ `B-2026-08-24-B` + amendment RATIFIED — audited CLAUSE BY CLAUSE, not against the defect

✔ **R1 both directions, with the negative as a positive control** — `face-pairs 1 (reversing) · orientable no` on a pressed pair; **zero picks byte-identical to before.** ★★ **And the sweep found the RESTORE**, which I did not anticipate: **the reload path would have re-dropped every pair.** ⇒ **Two sites, both cut, three look-alikes inspected clean — named all N and said what each does.**
✔ **§6 rows** — three born corners read `unnamed`, and **the figure carries the identity: touching a row lit its corner as a bold dot plus its two edges, through the shipped channel.** ⇒ **The designer's *"the list becomes a POINTER"* is real at the eye, not asserted.**
✔ **R2 · R7 · §8 · §5** — R7's refusal now names what he did; **§8 shipped BOTH of her clauses (≈8px off the hairline selects; the hollow still falls through)**; §5 carries its positive control (`05636d6b…` reproduced) and `face-pairs` reads `0 · 1 (reversing) · 3 (all preserving)` ⇒ **the quantifier only says `all` when it is true of all.**
✔ **§7** — the full cycle measured, **and the backstop then fired in the wild on its own builder.** ★ *A guard seen firing is a fact.*
✔ **The elision row** — all seven clauses at the eye.
★★★ **And §9b killed my story, which is the point of a census: `identify` is LIVE on the selected closed octahedron and opens its trace mode.** ⇒ **My "the reroute is a dead end" was wrong twice over, and you measured it instead of inheriting it.** ★ **The silent-grey state reported as *"did not fire here," never "cannot"* — that is the disclosure discipline exactly.**

> ### ⛔ **AND MY PROTOCOL FAILURE, which cost you nothing but cost the build one ruling — §2.**

---

## 2 · ⛔ ONE RULING DID NOT LAND, AND THE CAUSE IS MINE

**`ManuscriptView.tsx:5021` still renders `${r.ownName ?? 'unnamed'} ← {${r.rootLabels.join(', ')}}`** — the SET form. ✔ **Your hold was executed faithfully and the comment at `argumentReadingModel.ts:350-358` states its own death-condition at the site** (*"HELD for the designer's term-spec; until she rules"*). ⇒ ⛔ **Nothing is wrong with what you did. She HAD ruled — in `-C`, which you never saw as `-C`.**

> ### ★★★ **THE MECHANISM, and it is the third instance: A MID-FLIGHT *APPEND* IS DETECTABLE. A MID-FLIGHT *EDIT OF ALREADY-READ TEXT* IS NOT.**
> **You took the amendment's APPENDED parts (the elision row, §9b) and missed its EDITED part (§6's count form) — because I rewrote a section you had already consumed.** **The STAMP catches a changed DOCUMENT; it cannot catch a changed PARAGRAPH inside one you have read.** *(Task 0 was dropped the same way.)*
> ⇒ ✅ **STANDING, FROM NOW: I DO NOT REWRITE `THE_BUILD.md` WHILE YOU ARE IN FLIGHT.** **A ruling that arrives mid-build goes to `.handoff/inbox/coder/` as ITS OWN FILE with its own marker, and your report echoes each marker separately.** **A new document is unmissable in a way an edited paragraph is not.**

### ✅ SO BUILD IT NOW — the designer's count form (her ruling, verbatim)
⛔ **`p ← {unnamed, unnamed}` is not a legibility defect — it is a FALSE SENTENCE: the line's job is to say *p came from TWO DIFFERENT places*, and identical terms say the opposite.** ⇒ **She overruled her own no-suffix bound for it.**

> | case | the line |
> |---|---|
> | both named | `p ← {AB, CD}` |
> | neither named | **`p ← two unnamed roots`** |
> | one named | **`p ← {AB, one unnamed root}`** |
> ★★ **GENERAL — a real collision of two roots both named `AB` reads `p ← two roots named AB`.**
> ⇒ ***When two terms in a composed sentence cannot be told apart by their names, the sentence COUNTS them instead of indexing them. It says HOW MANY; it does not invent labels to fill slots.***
> ⚠ **Her trap, and the reason the shape must change: SET NOTATION `{ , }` demands a token per element, so it MANUFACTURES one. The count form has no slot to fill.**

⇒ **Consequence you named yourself: the TERM handle at `argumentReadingModel.ts:359` loses its last justification in the `p ← {…}` line.** ⚠ **But you listed THREE term positions — the set line, the composed class label's parts, and relation source readings.** ⛔ **Her ruling reaches the SET LINE. Take the handle out of THAT one and report what the other two then read.** **If either degrades, STOP and report it — do not extend her ruling to a position she has not seen.**
⚠ **The witness pins move again. Recut from THE RULE, never from the new output.**

## 3 · ⛔ **A `reason` MUST BE TOTAL OVER ITS `enabled` — and I want the sweep, not just the fix**

**You measured the gap and were right to report it as latent:** `ManuscriptView.tsx:5326` — `enabled: selected !== null && targetFor(selected) !== null` against `reason: selected === null ? '…' : null`. ⇒ **Two conjuncts, one branch: a form selected but unresolved greys the chip with NOTHING TO SAY.**
✔ **Your "did not fire on any reachable form" is honest and I take it.** ⛔ **But it is a claim about TODAY'S reachable set, and that set changes every build.** ⇒ **A latent silent-refusal arms itself the moment a new form becomes selectable-but-unresolvable, and it is one expression to close.**

> ### ✅ **RULED: cure it — the reason branch must cover BOTH conjuncts.** ⚠ **Wording: if the second case needs more than naming the fact, route it to the designer.**
> ### ⛔ **AND SWEEP IT — this is the high-yield half.** ***Which other chips have an `enabled` predicate with more conjuncts than their `reason` has branches?*** **Every one of those greys out silent in at least one state.**
★★ **The idiom is already ratified and written ten lines below the offender (`ManuscriptChrome:1498`): *"a greyed chip still SPEAKS."*** ⇒ **A rule held by a comment is a rule waiting to be forgotten; a `reason` that is TOTAL over its `enabled` is the same rule held BY CONSTRUCTION.** ⛔ **Report all N. If a chip's silence is correct, say why.**

## 4 · ⚠ **THE OBSERVATION THAT DID NOT REPRODUCE — do NOT chase it yet**
**`collapse`'s "took it" did not reproduce on the lift route.** ⇒ ⛔ **An observation of Arman's that we cannot reproduce is NOT closed, and I will not let a green census close it.** ***When he reports what he sees, find the mechanism — never manufacture one that fits.***
⚠ **I have asked him which route he saw it on** *(his words were "when the CODER brought the octahedron to manuscript" — so it may be a route from YOUR earlier drive, not his own hand)*. ⛔ **Do not spend on it until his answer lands.** ✔ **Your report of the non-reproduction is exactly right and I want more of it: a census that closes a finding it did not reproduce is the failure; naming it open is the discipline.**

## 5 · ⚠ NOT YOURS
**`identify` accepts a closed complex at ENTRY, completion unmeasured** — ✔ **the right finding, correctly routed.** **The researcher has Arman's RP²-by-antipodal-vertices question and this outranks my premise in it.** ⛔ **Do not build toward the quotient.**

## 6 · ORDER
**§2 (the count form + the term census) → §3 (the cure, then the sweep).** ⛔ **§2 is the one that must land** — it is a false sentence on his screen and it has now survived one build because of me.

## 7 · THE STANDING ASK
Arman: ***"i'm tired. it's been 3 months. i need a race to the finish line."*** ⇒ **Build, drive, report. Echo the stamp. Push when it lands.**
