# THE BUILD — the letters consumed by this landing, verbatim (Δ21: the inbox is the wire; this tracked file is the record). One letter: §126 — Δ90 THE RACE (Arman: finish the whole thing; the finish Station C's three sentences at his own eye; the race rules — a stamp carries its own item 0 and proceeds, a designer pass fixes blockers only, each step's acceptance Arman's eye once, the mothership ratifies each landing the turn it arrives), `C-7h` and `C-9` RATIFIED (§126.1), `STAMP C-10` THE LIFT CARRIES (Station C, step 2 — measure and build in one stamp), U1's waves pre-ruled (§126.2), W1 grown by two files (1101). The 1535, 1623 and 1642 letters (§124 · the lock · §125) entered the record with C-7h and C-9's landing, e293e45.

---

## `2026-09-24_1101_mothership_s126_THE-RACE-Delta90_RATIFIED-C-7h-and-C-9_STAMP-C-10-THE-LIFT-CARRIES-measure-and-build-in-one-stamp_U1-waves-pre-ruled_W1.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-24 11:01 +0330` (machine — `date`) · HEAD == origin == `5c9ab76`
re: **⛔⛔ Δ90 — ARMAN WANTS A RACE TO THE FINISH LINE** (*"let's just finish the whole thing. enough is enough … time is very much zero"*, relayed verbatim by the researcher). **The finish: Station C's three sentences at his own eye.** `C-7h` and `C-9` RATIFIED (§126.1). **Yours now: `STAMP C-10` — THE LIFT CARRIES (STEP 2), measure and build in ONE stamp.** U1's waves pre-ruled. W1 grows by two files.

# 0 · THE RACE RULES (§126.6) — they change how you run a stamp
1. **A stamp carries its own item 0 and PROCEEDS without returning to me** — unless a STOP fires: a frozen file off the pre-sanction, a meaning question, a falsified premise. Report once, at the landing.
2. A designer pass fixes BLOCKERS only; everything else is parked.
3. Each step's acceptance is Arman's eye, once.
4. I ratify each landing the turn it arrives, on its essentials.
**PARKED, do not start:** the flatness thread (Δ88–Δ89), Stations A and B, the camera gate, the contrast sweep, τ-persistence, cone-deficit, `M3F`, the fold column, 1400, a gen-3 eye arm, placement polish.

# 1 · RATIFIED — `C-7h` + `C-9` (§126.1)
At my hand: `HEAD == origin == 5c9ab76` · no frozen row across `6fe02bb..5c9ab76` · row 220 at its landing · the lock gone · born-face 13 · born-room 34 · midpoint 58 · freeze 19, your counts, the tree unmoved · **the born-face witness reproduces the researcher's deterministic seals to the digit.** **The induced walk, measured and pinned** — the host's walk, 14 of 14, and the handedness that inverts the researcher's inference: well found. **`midpointGlue.ts` outside the boundary, additively, said — accepted** (a second reader of the record would have been worse). The refusal's grain (every contradicted pair named, 11; the reference's 7 ported as the second implementation) — **the ratified rule, kept.** ⚠ Not sighted: the designer's eye (blockers-only pass now) and Arman's (his STEP-1 walk now).

# 2 · `STAMP C-10` — THE LIFT CARRIES (Station C, STEP 2)
**The person's sentence (the plan):** *he lifts a region he has given transports to, and finds the transports still there on the lifted form.* **The acceptance:** *what the substrate holds, the lift CARRIES — and where it cannot, it MARKS; a born cell's transport is a TRUE ABSENCE, the person's to give, never minted.* **The falsifier:** *a surviving loop whose reading changed with no act.*
**The ground (§126.3, ratified — the researcher's §17, Δ87):** **carry the FAMILY untruncated** — never collapse a vertex to one point (propositional truncation: 33 residues become 1 on the first face), never the playground's set-level refine (it pools two roles of one corner wherever a residue moves a role — 41 of 42). **And RECORD, NOT READING:** carry the INPUTS the concept layer re-derives from — the seed casts, the person's `J` records, the lineage — **never a stored derived space.**
0. **MEASURE FIRST, then proceed** (race rule 1): lift a region the person has worked (`liftSelectionToManuscript` → `liftSubComplex` → `serializeSnapshot`) — a gen-1 corner cell, the gen-1 core, and a gen-2 region with a born pair — and print what the lifted form holds: (a) the seed casts (`vertex.data.cast`); (b) the `J` records (`edge.identification`) — by edge id, or re-keyed?; (c) the lineage (`createdBy.sourceVertexIds`) and whether its referents come with the region; (d) **whether `spaceOf` resolves every lifted vertex to the SAME space as in the Ambo** (by seed content); (e) the face reading on every lifted face whose loop survives, against the Ambo's; (f) what the Manuscript shows of any of it today.
1. **THE CARRY — whatever (0) finds missing:** the lift carries the record the resolver needs — seeds' casts, the person's `J`s on the lineage edges (by their PAIR where ids change), the lineage — **so the ONE resolver re-derives on the lifted form.** Where the lineage reaches outside the region, carry its record as ancestry (the way GAP2C carries the workspace population) or MARK. **The mechanism is yours; the rule is not: carry inputs, re-derive; never fabricate, never truncate, never store a derived space.**
2. **THE MARK:** what the lift cannot carry is a TRUE ABSENCE, said. **A cell BORN after the lift** (by the Manuscript's own acts) **has no transport** — the person's to give, never minted.
3. **THE SURFACE — he must SEE it:** in the Manuscript, a lifted vertex's concept-space inside (reuse `CastInsidePanel` through the resolver) and a lifted face's reading (reuse the face block) — **reuse, not new design**; the designer's rulings already cover both; her eye after, blockers only.
4. **THE WITNESS:** every lifted vertex that held a space in the Ambo resolves the SAME space lifted (roles, words, tuples by seed content) or is MARKED — never a fabricated space; **every surviving loop's reading unchanged (0)**; subdivision composes on a lifted gen-2 region (the two injections along a refined edge compose to its gen-0 `J`); a lifted region with NO casts shows a true absence (the positive control that nothing is minted). **A DRIVE-FAMILY arm:** lift at the eye, select a lifted vertex, see its inside; select a lifted face, see its reading.
**Boundary:** the lift (`liftSubComplex`'s file), the Manuscript's inspector surface, `spaceOf.ts`, the witnesses, the leg. **`snapshot.ts` is FROZEN and on the sanctioned list — if the carry must touch it, STOP and ask, unless Arman has pre-sanctioned it** (I have put that question to him to save you the cycle; look for his word before you stop). Any other frozen file: STOP.
**Acceptance (sealed):** item 0 printed · the same spaces lifted (or marked) · 0 surviving loops changed · subdivision composes · the no-cast control a true absence · the leg's new arm at the eye · sweep OK · `tsc` 0. **Then Arman's STEP-2 acceptance.**

# 3 · U1 — RULED (§126.2)
- The ambiguous head (`RULING_THE_FIRST_FACE_…`, two files): **keep both.**
- **The second wave: APPLY — and for the race every later wave your sweep finds is applied at each record commit and reported in counts.** No per-wave question.
- The too-long ruling file: **the researcher renames it** keeping the head `RULING_THE-INSIDE-MIDPOINT-TRACE-REFUSAL_`; track it at the next record commit after the rename. `core.longpaths` untouched.

# 4 · W1 — carry AS FOUND at your next record commit
The claims ledger (§126) · the front board · the connection-layer plan · **the sovereign ledger (Δ87–Δ90, relayed words — new this turn)** · **ADR 0030 — the researcher's rider** (`sha256-LF b5da0f48…` = `blob b95d8f16` → `sha256-LF a6cea23f…` = `blob 66cb9c3f`; verified at my hand, §126), with its citations' closure (`lift_ground.py → face_residue_reading.py → inside_midpoint_trace.py`) through your sweep. None for content.

# ECHO
ECHO §126 (Δ90, the race; `C-7h` + `C-9` ratified; U1 ruled) · `STAMP C-10` (item 0, then proceed).

---
