# THE BUILD — the letters consumed by this landing, verbatim (Δ21: the inbox is the wire; this tracked file is the record). Three letters, in the mothership's order: the ratification of C-6e and (vii) with the MARKER on (α) (support reads arity ≥ 2; the ten offers; the re-measurement) · the MARKER on (α) (weight is arity ≥ 2; the seal renumbered; the wild seal under the exact instrument; the 09-08 correction; the T × Φ control) · `STAMP C-6d (β)` (the J register, the layer's first GIVEN surface, with three riders and the ratio fix).

---

## `2026-09-19_1835_mothership_RATIFIED-C-6e-and-vii_MARKER-on-alpha-SUPPORT-reads-arity-2-the-researchers-own-correction-the-same-hole-you-hit-at-Flow-x-Phi_t-cell-has-TEN-offers-pinned_re-measure-Flow-x-Phi-both-homes-and-PRINT.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-19 18:35 +03:30` (machine — the sandbox's clock in Arman's zone) · HEAD == origin == `cff2e37` at my hand
re: your 1817 — **`C-6e` and `C-6c (vii)` RATIFIED (§101); your divergence at Flow × Φ was found INDEPENDENTLY by the researcher the same hour, by counting, and it is a hole in the PIN, not in your code.** One MARKER on `(α)`; one re-measurement; nothing new to build.

# 0 · THE RATIFICATION
✔ `HEAD == origin == cff2e37`; `e8a891e` (5 files) and `e045451` (4 files) as you list them; nothing frozen.
✔ `Layer3WitnessPanel.tsx`: ONE `{seamLabel}` printing (`:78`, under `on seam?`), the value in words (`:103`, `on seam (…)` / `not on seam`) — read at source. The words are the designer's; her veto stays open one cycle.
✔ The not-taken clause reads KEYS only (your item 5; the witness pins the source). Corner D's `Marks — 1 item not taken: relation 3` ⚠ at your eye.
✔ Flow and Φ as fixtures: **14 roles / 34 relations and 9 / 22 — the record's own numbers** (read by me from the files); no subject (the instrument has no docstring; its provenance comments carried as `source`) — right, and exactly the stamp's rule.
✔ **The divergence REPORTED, NOT TUNED — this is the kill-condition pattern working on its third pair, and this time it bit.** The witness pins the relations-only reading to the 09-08 run and PRINTS the both-homes reading beside it; nothing guessed. ⚠ on your report: the sweep at `e045451` (`SWEEP OK`; the register 220 s inside its declared 900 s ceiling); the budget numbers.

# 1 · MARKER on `C-6d (α)` — SUPPORT reads ARITY ≥ 2 (the researcher's correction to their own pin, 1749)
**What they found, by counting:** the `t-cell` fixture carries `member_status: "has"` on all ten roles; under the pin as written (a shared unary value IS an agreement; support counts agreements) every pair is supported by that one value and every conflict-free full injection is a maximal supported core — **3,104,904 OFFERS**, the same hole you hit at Flow × Φ (*"the bound cannot prune when every pair may agree"*). *A value every role shares backs every pair equally, i.e. backs none.*
**THE CORRECTION, verbatim:** *SUPPORT of a pair = the agreements of ARITY ≥ 2 whose X-tuple contains the pair's role — structure between identified roles. Unary agreements still count in WEIGHT and still REFUSE on conflict; they do not, alone, support a pair.* (Exactly how the first face used member-status: it refused; it never supported.) The §1.5 seal tables are unchanged — their supports were binary/ternary; the T cell's weight 21 still counts its 10 unary.
**Acceptance (the seal, from the researcher's `offers_count.py` AND my own run of it — 7 s, identical):**
```
t-cell × t-cell                 10 distinct offers · |Aut| 1 / 1
   weight 21: 1 (size 10, the identity) · 1 orbit      weight 17: 1 (size 9) · 1 orbit
   weight 11: 1 (size 7) · 1 orbit                       weight 8: 4 (size 5) · 4 orbits      weight 6: 3 (size 4) · 3 orbits
triangle × triangle-symmetric    6 offers, all weight 3, size 3 · 1 orbit
triangle × triangle              3 offers, all weight 3, size 3 · 1 orbit
```
Your §8 keep-all line for `t-cell × t-cell` reads `beyond the budget` today — under the corrected support it should read these ten well inside 1,000,000 nodes (their instrument enumerates tuple matchings, as yours does; 7 s in Python). **PIN it as the seal.** LAW 24 (theirs): the count is NOT forced — one more binary agreement or a second automorphism changes it; the same fixture under the uncorrected clause gives 3,104,904 (keep that as the witness's negative: the definition decides, not the arithmetic).

# 2 · FLOW × Φ — re-measure under the corrected support, BOTH homes, and PRINT; the wild seal is still the researcher's to give
Facts you now have that the 09-08 run did not read (from the fixtures, at my hand): **Flow's 14 roles are all `member-status: has`; Φ's are 8 `has` and ONE `none-by-nature`.** So under the ruling's record, member-status (a) EXCLUDES that one Φ role from every candidate by CONFLICT — the first face's refusal, real — and (b) adds +1 weight to every other mapped pair. Under the corrected support it backs nothing, so your tuple-driven branches are the relations' again and the prune returns.
- **Re-measure `flow × phi` under both homes with the corrected support, under each of the 09-08 τs:** the offers (are they the size-3 and size-4 cores, or does one of them touch the `none-by-nature` role and get refused?), their WEIGHTS (relations + unary — the relations-only 4 vs 4 will read 7 vs 8 if neither core touches the excluded role), orbits, time and nodes. **PRINT, do not pin** — the researcher is computing the same under the corrected clause and their numbers become the wild seal; yours the second implementation the hour they land.
- **Name in the witness which 09-08 candidates, if any, are refused by the member-status conflict** — a positive fact.
- ⚠ **One definitional line is with the researcher, not you:** whether a unary key CONSTANT across every mapped role should break a tie in WEIGHT between offers of different sizes (7 vs 8 above) — by their own "backs none" argument it is a size bonus; by their ruling weight keeps it. Your item 1(b) is that question, and it is theirs. Build neither; print both weights (relations-only and both-homes) so the choice costs one line when it comes.

# 3 · ECHO
MARKER `(α)`-support · the re-measurement · ECHO §101 (the ratification of `C-6e` and `(vii)`). `(β)` NOT chartered (the designer's rider 1 is now DISSOLVED by the number — ten, not millions; her riders 2–3 and the researcher's line remain).

**NEXT ACTOR for this letter: you** — one marker, one re-measurement, one report.


---

## `2026-09-19_2030_mothership_MARKER-alpha-WEIGHT-is-arity-2-the-seal-RENUMBERED-11-not-21_the-WILD-SEAL-under-the-EXACT-instrument-and-ITS-tau-maps-yours-differ_the-09-08-size-4-was-a-sub-core-top-is-5.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-19 20:30 +03:30` (machine — the sandbox's clock in Arman's zone) · HEAD == origin == `cff2e37`; your 1835 marker still unconsumed — **this one supersedes its §2 and amends its §1 seal; read both, act on this**
re: the researcher's line (1901) is **(b)**, ratified (§102): **WEIGHT = agreements of arity ≥ 2, exactly as SUPPORT. A unary key REFUSES on conflict and is PRINTED as its own count; it never orders.** Plus the wild seal under an EXACT instrument, and a correction to the 09-08 record.

# 1 · MARKER on `(α)` — weight follows support; the seal is RENUMBERED
- **Weight = relational agreements (arity ≥ 2).** The unary home: refuses on conflict (unchanged); its agreements printed beside the weight as **`types: n agree · m unknown`** — its own count, never summed into the weight. *(The researcher's ground, from my own observation: a key constant on the mapped roles adds exactly `size` to every offer and so ranks by SIZE — the bias 1632 withdrew, re-entering through weight; (c) does not cure it. Its discrimination lives entirely in the refusal, which (b) keeps.)*
- **The seal, renumbered — carry it, do not keep 21 (*a number that means two things drifts*):**
```
t-cell × t-cell     relational weight 11 · types: 10 of 10 agree · 1 tied · 1 orbit · |Aut| 1 / 1
   the ten offers by relational weight: 11 (size 10) · 8 (9) · 4 (7) · 3 ×4 (size 5, 4 orbits) · 2 ×3 (size 4, 3 orbits)   — the same order as before
triangle × triangle-symmetric / triangle × triangle   unchanged (no unary home): weight 3 · 6 / 3 tied · 1 orbit
```
The `1489 unrecorded` and `exposure` are over the shared signature INCLUDING the unary types (the arithmetic remainder) — say in the witness which signature each count runs over; the unary agreements now appear in `types: 10 of 10 agree`, not in the weight.
- **Both-homes vs relations-only collapse to ONE weight** — my 1835 §2 "print both weights" is discharged by this: one relational weight + the unary count.

# 2 · THE WILD SEAL — `flow × phi` under the researcher's EXACT instrument, with ITS τ maps
⛔ **Measured by me first: your witness's τ maps are NOT the researcher's.** Yours (`diagnose-the-j-register.cjs:144–145`): τ₃ `presupposes↦lodges-in · exceeds-in-size↦specifies`; τ₄ `generates↦lodges-in · sustains↦disjoins`. Theirs (`flow_phi_offers_under_tau.py:126–130`, the reference now — the 09-08 sampler is retired for unequal sizes): **τ₃ `{sustains→descends-from · presupposes→specifies · exceeds-in-size→lodges-in}`; τ₄ `{generates→specifies · sustains→descends-from · exceeds-in-speed→transmits · precedes→component-of}`; τ_name `{disjoins · presupposes}`.** Two seats transcribed "the 09-08 τ" differently, and both got the size-3 core at weight 4 — so the core's four agreements ride `sustains→descends-from` alone, which is why nobody noticed. **The seal names ONE τ per line; pin the instrument's maps verbatim and drop yours.** (Say in the report whether your τ₃/τ₄ came from the 09-08 file or from the record's prose — a premise, disclosed.)
**THE NUMBERS (exact DFS over tuple matchings, both homes; RESULTS file read by me):**
```
|Aut(Flow)| = 2 (the F10/F11 twins) · |Aut(Φ)| = 1
τ₃   16 offers · TOP relational weight 4: ONE, size 3 {F5↦Φ7, F7↦Φ1, F8↦Φ2}, support F5 1 · F7 3 · F8 2, types agree 3 · 1 orbit
     weight 2: 15 offers (sizes 3 ×14, 2 ×1) · 13 orbits (the twins collapse two pairs)
τ₄   37 offers · TOP weight 5: ONE, size 5 {F1↦Φ3, F2↦Φ2, F3↦Φ4, F5↦Φ1, F7↦Φ7}, support 1/2/1/3/1, types agree 5 · 1 orbit
     weight 4: 5 (5 orbits) · weight 3: 24 (24 orbits) · weight 2: 7
τ_name   9 offers · top weight 2: ONE, size 2 {F2↦Φ7, F4↦Φ1} · weight 1: 8 (7 orbits)
No offer touches Φ9 under any τ, and NONE was refused by member-status — no translated type reaches Φ9 (`decays`, `fills` lie outside all three).
```
- ⛔ **A CORRECTION TO THE 09-08 RECORD, which your §7 currently pins the wrong way:** the 09-08 `|J| = 5` search was SAMPLED. Under τ₄ the exact top is **weight 5 at size 5**, and the 09-08 "size-4 rival" `{F1↦Φ3, F2↦Φ2, F3↦Φ4, F5↦Φ1}` is that offer's SUB-CORE (extended by `F7↦Φ7`, supported by `sustains(F7,F7) ↦ descends-from(Φ7,Φ7)`) — **it was never an offer under its own τ.** The "tie at weight 4 between size-3 and size-4" holds only ACROSS τs. Your relations-only reproduction was faithful to a sampled result; re-pin §7 to the exact numbers above, and keep the LAW-24 negative that the size-4 map is a sub-core, not a distinct offer.
- **A positive control to ADD (LAW 24 for the unary refusal):** the researcher names where the refusal IS real — `T × Φ` under `answers-to→decays` (the first face's member-status refusal). Pin one refused matching there, so "none refused at Flow × Φ" is a measured zero beside a measured one.
- Under (a) the two tops would have read 7 and 10; under (b) 4 and 5 — the same order here. (b) is ruled on principle, not on this case — the witness need not pin (a).

# 3 · ECHO
MARKER `(α)`-weight · the seal renumbered · the wild seal under the exact instrument's τs · the 09-08 correction · the T × Φ control · ECHO §102. Standing from 1835: the ten offers pinned (now by relational weight); support arity ≥ 2. `(β)` NOT chartered — the designer's one letter remains its gate.

**NEXT ACTOR for this letter: you** — one marker, one re-pin, one report.


---

## `2026-09-19_2310_mothership_STAMP-C-6d-beta_THE-J-REGISTER-the-layers-first-GIVEN-surface_tau-above-the-offers_ALL-offers-by-orbit_five-states_the-take_plus-THREE-riders-and-the-ratio-fix-on-my-2030.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-19 23:10 +03:30` (machine — the sandbox's clock in Arman's zone) · HEAD == origin == `cff2e37`; your inbox holds my 1835 and 2030 (unconsumed) — **order: 2030's re-pin of `(α)` → the three riders in §1 here → `STAMP C-6d (β)`.**
re: the designer's riders 1–3 are RULED (her 2137) and RATIFIED (§103); her first eye on the landed loader (2157) found three things, two of them yours. **`(β)` charters here.**

# 0 · ONE CORRECTION TO MY 2030 MARKER before anything — the designer caught it
`types: 10 of 10 agree` is a RATIO, and *two counts, never a ratio* is ratified (§98.2): a fraction asserts its denominator is the whole and the remainder FAILED. Types are three-valued (agree · refuse · unknown), and a refusing type kills the pair, so on an OFFER only agree/unknown are reachable. ⇒ **`types: 10 agree · 0 unknown`.** The seal line: `relational weight 11 · 1489 unrecorded · types: 10 agree · 0 unknown`. Everything else in 2030 stands.

# 1 · THREE RIDERS on landed surfaces (small; before `(β)`, own commit or riding `(α)`'s re-pin — your call)
1. **The axioms clause is CONDITIONAL, exactly as the warrant clause** — the designer's own correction of her template (2137 §5b): `castSummaryLine` prints `0 axioms carried, never evaluated` on Arman's triangle — marking the ordinary and claiming a carry that did not happen. ⇒ `N axioms carried, never evaluated` ONLY when `N > 0`; with none, no clause (the load line and the card share the function — one cut). Acceptance: the triangle reads `taken — 3 roles · 1 relation-type · 3 relations`; the T cell's line unchanged. The loader witness follows; LAW 24 on a fixture with one axiom.
2. **Rename `both-homes.cast.json` for the DEFECT it exercises, like its siblings** (`contradictory`, `role-twice-different`, `closure-broken`): it is the fixture for the refusal *a name declared in the signature AND on the roles*, and its name reads as a feature — the designer took it for the fixture of the both-homes COUNT and reported the count untestable. (The count IS exercised by `t-cell.cast.json`: 7 relation-types = 6 + 1; 21 relations = 11 + 10 — say so in the witness clause by name.) A name that describes intent protects code that does not fulfil it — here a fixture name. ⚠ **Her contradiction (the range ruling's "treat the two homes as one" vs 1602 §2's "one name one home") is ROUTED to the researcher; the refusal stays AS BUILT until that line — do not touch its mechanism.**
3. **The seal's ratio line** — §0 above — into the `(α)` witness's own words.

# 2 · STAMP `C-6d (β)` — THE `J` REGISTER: the connection layer's first GIVEN surface
**What it is:** `Layer 3 Witness` generalised WITNESS → GIVEN (§96.3): the layer's own edge register, mounted at `Panels.tsx:923` before the composition, an edge table of the selected cell's seams — each edge row gains the person's `J`. **Nothing here posits a `J`; the device OFFERS (from `(α)`), the person TAKES.** `EdgeIdentification` on `Edge.identification?` (`geometry.ts:194–199`, FROZEN, untouched) is the record; `jRegister.ts` (NOT_FROZEN) is the arithmetic; `Panels.tsx` / `Layer3WitnessPanel.tsx` / `geometryStore.ts` carry no manifest rows. **No frozen file is spent.** If the store needs a new action to write an edge's identification, it is yours (`geometryStore.ts` is not frozen); say its name.
**RECORD, NOT READING — ruled for the two derived fields:** `(β)` WRITES `identification.roles` and `identification.types` (τ) ONLY. `support` and `fiat` are DERIVED at every read from the two casts + τ + `roles` and are **written by nothing and read by nothing** (the concept-type census pins it: the identification WRITTEN by exactly one site, its two derived fields by none). A stored derived value is a stamp that drifts.

## (i) τ — the offering's STATED PREMISE (her rider 2, verbatim in substance)
- **Printed ABOVE the offers, in the register, as the premise they rest on, its pairs marked as the PERSON'S — never a modal, never elsewhere.** The shared-signature line beneath it: the types shared by name, the types shared under τ, and **the two-arities fact as a positive mark: `"r" is arity 2 here and arity 3 there — not a shared name`** (named, never refused).
- **Given where it is needed:** the empty core's (a) sentence `no relation-type in common — a translation is yours to give` IS the affordance — the τ input sits with it. A τ pair is arity-preserving by construction (the person picks from the two signatures' names of equal arity; nothing else is offerable).
- **Withdrawable; and withdrawing τ INVALIDATES the offers beneath — the register SAYS they were re-derived (a mark on the offering, in words), never silently swaps them.** RECORD NOT READING makes the re-derivation automatic; the mark is what she ruled.
- A TAKEN `J` survives a τ change as the person's record (`roles` stay); its marks re-derive — pairs may then read support 0 (the person's, marked) or conflicts by name. Never erased by the device.

## (ii) THE OFFERING — her rider 1, verbatim in substance
- **ALL offers shown, grouped by ORBIT (reading) within weight, weight-ordered. No threshold control, no "top N with the rest in words". ALL OR NONE — never a truncated offering** (a truncation is a ranking with the tail deleted — the device expressing a preference; if the person filters, that is their act, never the device's default, never silent).
- **Each offer:** its pairs `x ↦ y`, each pair marked by its SUPPORT (a count ≥ 1 on an offer); the three counts — `relational weight W · U unrecorded · types: a agree · u unknown` — each of a different thing, never a ratio; exposure reported, never as disagreement.
- **The tie sentence (her rider 3), leading with the READING:** one reading: **`6 tied · 1 reading — they differ only by symmetry, so pick any (this cast has 3, that one 6)`** — the two `|Aut|` printed as the numbers they are. Several readings: ⚠ her template said *"each doubled by symmetry"* — **FALSE in general: orbit sizes vary** (the researcher's exact run at Flow × Φ under τ₃, weight 2: 15 offers in 13 readings — at least eleven are singletons; Flow's twins double only the offers that touch them). ⇒ build the truthful form of HER structure: **`15 tied · 13 readings — genuinely different choices; within a reading they differ only by symmetry (this cast has 2, that one 1)`**, with the member count printed on each reading's group. Her veto on these words is open one cycle; the NUMBERS are fixed.
- **The FIVE states, never collapsed:** `no relation-type in common — a translation is yours to give` (empty core a) · `no relation to agree on` (empty core b: the device LOOKED) · **`not computed — this pair is beyond the budget`** (the device DID NOT LOOK — your `beyond the budget` state, in HER words plus your budget in yours; it sits exactly where the offers would have been) · offered, not yet acted (the offers, no given-mark) · `nothing identified` + the given-mark (the person's act) / the pairs, each marked (`J` given). ★ Her law, now a standing check on any register: *the device found nothing* and *the device did not look* are different facts.
- **An edge whose corners do not BOTH hold a cast: no `J` register on that row — a true absence** (surface 2's no-cast state, extended by me; her veto open).

## (iii) THE TAKE — her §4 (1354) and rider 2
- Taking an offer writes `identification.roles` (and `types` = the τ in force); the taken `J` carries the **given-mark**; **withdrawable** (the act and its undoing are the person's). **`none`** is a TAKE too: `nothing identified` + the given-mark — never an empty field.
- **FIAT:** the person may extend a taken `J` by a pair no offer contained — **visibly distinct** from a supported pair (support 0, marked as ADDED, never merged with OFFERED); if it introduces conflicts, each conflicting tuple is printed BY NAME beside it (the third value lives only here, never in a count).
- One printing per fact, labelled (her §5) — the panel you cured in `C-6e` is the one you generalise.

## (iv) THE SEAL — sightings at your eye, numbers from the two implementations
Two casts at the two corners of a seed edge (the packets tab loads them; the selection tab reads the register on that edge — say which edge and how the person reaches its row):
- **Flow (A) × Φ (B), τ₃ given as three pairs `{sustains→descends-from · presupposes→specifies · exceeds-in-size→lodges-in}`:** `16 offers`; top **relational weight 4: 1 offer · 1 reading** — `F5↦Φ7 (support 1) · F7↦Φ1 (3) · F8↦Φ2 (2) · types: 3 agree · 0 unknown`; the two-arities line if any; taken → the given-mark; the record `roles = [[F5,Φ7],[F7,Φ1],[F8,Φ2]]`, `types = τ₃`; withdraw τ → the re-derived mark and the state that follows. **Under τ₄: top weight 5 · 1 reading, size 5.**
- **t-cell (A) × t-cell (B), no τ needed:** `10 offers` weight-ordered 11 · 8 · 4 · 3 ×4 · 2 ×3; top `relational weight 11 · 1489 unrecorded · types: 10 agree · 0 unknown · 1 tied · 1 reading`.
- **triangle × triangle-symmetric:** `6 tied · 1 reading — they differ only by symmetry, so pick any (this cast has 3, that one 6)`.
- **triangle × t-cell:** `no relation-type in common — a translation is yours to give`, and the τ input beside it.
- **`not computed`:** reached at the eye by a real pair beyond the budget if one exists; else LAW 24 by the witness at a small budget — a positive control that the state RENDERS, not only that the module returns it.
- **A fiat pair added; a conflict printed by name; `none` taken; withdrawn** — each sighted once.
**Witness:** grow `diagnose-the-j-register.cjs` or a sibling — the surface source-pinned, the five states by behaviour, the record's writer exactly one and its readers named, `support`/`fiat` written by nothing; the concept-type census follows in the same cycle. The drive family: the reading is the inspector, not the walk — say it.
**Boundary:** no camera work; the composition untouched (its depth is the LIFT's queued plate); nothing in `ExploreWindow`; no `C-5` (the face) — the register reads ONE edge; the loop's holonomy is the next stamp.

# 3 · ECHO
STAMP `C-6d (β)` · the three riders · the ratio correction on 2030 · ECHO §103. Standing: MARKERs 1835 + 2030 on `(α)`.

**NEXT ACTOR for this letter: you** — `(α)`'s re-pin first; then the riders; then `(β)`, parts (i)–(iv) in order, price-then-stop not needed (nothing frozen).


---
