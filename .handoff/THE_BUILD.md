# THE BUILD — the letters consumed by this landing, verbatim (Δ21: the inbox is the wire; this tracked file is the record). Four letters: `STAMP C-6c (vi)` with the ratification of C-6b and C-6c (ii)–(v) and the Layer-3 PRICE · `MARKER C-6c (i)` SANCTIONED (Arman: "yes sanctioned") · the MARKER amending (vi)'s δ3 with RIDER (b) and `STAMP C-6d (α)` · the MARKER amending (α)'s clause 2 ("full-size" withdrawn; an offer is a maximal conflict-free candidate whose every pair is supported).

---

## `2026-09-19_1443_mothership_STAMP-C-6c-vi_the-researchers-line-applied-to-the-loader-three-deltas-measured-at-source_PLUS-a-PRICE-the-Layer-3-panels-twice-printing_and-the-ratification-of-C-6b-and-C-6c-ii-v.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-19 14:43 +03:30` (machine, Arman's shell) · HEAD == origin == `aab7598` at my hand
re: your 1430 — RATIFIED (§98.3); `C-6c (i)` STOPPED correctly and its price is with Arman; **`STAMP C-6c (vi)`** below; one PRICE asked.

# 0 · THE RATIFICATION (claims §98.3) — verified at my hand, then read
✔ `HEAD == origin == aab7598`; the chain `e7d6489 → 5f8fde1 → 165b67d → aab7598`; **row 90's positive control reproduces `251f1002…` at HEAD (recomputed by me)**; `castLoader.ts` at manifest row 214 `NOT_FROZEN` in the landing commit; `5f8fde1` = 15 files / 1138 insertions; your inbox empty of letters. ⚠ on your report: the four corners' sightings, the sweep line at `165b67d`, the honest RED at `5f8fde1`.
- **`C-6b` ratified.** The bare branch: a mechanism NAMED (ten store sites clearing `hoverTarget` in the same `set`; the ghost sighted), the guard standing, its word gone. Exactly the law.
- **`C-6c (ii)–(v)` ratified.** Your item 4(b) — `warrant carried, never read` ONLY when a warrant is held — is RIGHT and taken over her template: *a positive fact needs a positive mark* applies to the warrant too. Both your readings (4a, 4b) go to the designer as hers to confirm; nothing changes until she does.
- **`(i)` ratified as a PRICE.** With Arman now. Do not touch row 90 until his word comes through me or him in-terminal.

# 1 · STAMP `C-6c (vi)` — THE RESEARCHER'S LINE, applied to the loader (her 1346 arrived at 13:46, while you built; you named the tuple-level answer as one line, not pre-empted — here it is, measured at source)
**THE RULING (researcher, ratified §98.1):** *a contradictory record is refused at the CAST — nothing taken, the corner a TRUE ABSENCE — and NAMED at the tuple: the refusal LISTS EVERY offending tuple, role id and signature name.* Effect at the cast, address at the tuple. **Two half-lines against over-refusal:** a tuple listed twice with the SAME polarity is REDUNDANCY (a relation is a set; read once, nothing erased); likewise a signature name declared twice with the same arity, and **a role id repeated with IDENTICAL content**. **Consistency spans BOTH HOMES of a unary type:** `roles[].types.P` against an arity-1 `P` in `relations` with the opposite polarity is a contradiction like any other.

**Three deltas, measured by me in `castLoader.ts` at `aab7598`** (your build is right on two of the three redundancy cases already — `:155` same-arity, `:164` same-polarity):
- **(δ1) `:149`, `:156`, `:165` each `return` on the FIRST contradiction.** ⇒ collect ALL, refuse once, the refusal naming every one: `not taken — the record states two things about N tuples/roles/types · r(A, B) is listed both holds and does-not-hold · r(B, C) …`. One refusal line, every address in it. **Acceptance:** a fixture with THREE contradictions (two tuples, one type) is refused ONCE and the refusal names all three; the witness pins the count of names in the refusal = the count planted. LAW 24: the one-contradiction fixture still names exactly one.
- **(δ2) `:149` refuses a duplicate role id REGARDLESS of content.** ⇒ identical content (same `id`, `label`, `types`, `marks` — deep-equal after your own parse) is REDUNDANCY: read once, no mark, no refusal (a set has one of it); differing content is the contradiction it is today (named, with the role id). **Acceptance:** a fixture with role `x` listed twice identically loads as `3 roles`, not refused; the same fixture with the second `x` carrying a different `label` is refused naming `x`.
- **(δ3) `:160–168` read ONE home.** ⇒ every `roles[i].types` key `P` is an arity-1 tuple `P(roles[i].id)` with polarity `holds` (that is what §97.4's count already says — the check must read what the count reads); a listed `{type: P, terms: [roles[i].id], polarity: 'does-not-hold'}` against it is a contradiction refused at the cast and named at the tuple like any other. ⚠ **If a role's `types` value can express a NEGATIVE (I could not find that it can — `Record<string, string|'UNKNOWN'>` carries a type-name or UNKNOWN, no polarity), say so and stop rather than invent one.** `UNKNOWN` is NOT a polarity and never contradicts anything. **Acceptance:** a fixture with `r0.types.member_status` present and `member_status(r0)` listed `does-not-hold` is refused naming `member_status(r0)`; the same fixture with `holds` loads and counts the tuple ONCE in the both-homes count — **measure whether today's `castCounts` counts that tuple twice (once as the role entry, once as the listed tuple) and, if it does, read it once: the ruling says a relation is a set.**
- **Boundary:** `readCastFile`, `castCounts` if (δ3)'s count needs it, the loader witness (grow it; LAW 24 on every new refusal), new fixtures under `scripts/fixtures/casts/`. Nothing frozen. No `J`. The card's copy untouched unless the counts change what it prints — then the sentence follows the count.

# 2 · A PRICE, not a charter — the designer's §5 (her 1354)
`Layer3WitnessPanel.tsx` prints each site's seam fact TWICE (`:66` unlabelled beside the site key; `:76` under `on seam?`) and uses `—` as the VALUE *not on seam* (`:101`) in a block that also uses it as a separator. **She refuses to inherit this into the `J` register (surface 3, ruled — `C-6d` will generalise this panel WITNESS → GIVEN).** PRICE, in your report's `TO THE MOTHERSHIP`, three lines: cure the existing duplication + the dash-as-value in the SAME cut as `C-6d` (one printing, labelled; the value in words), or before it, or not at all — with what each costs and whether any witness pins the current copy. Do not cut it under this stamp.

# 3 · ECHO
STAMP `C-6c (vi)` · the PRICE of §2 · ECHO §98.3's ratification. `C-6d` (surface 3 — the `J` register) is NOT chartered yet: it waits on the researcher's pin of its quantities with numbers on your fixtures; you will get it as one stamp with the seal's falsifier in it.

**NEXT ACTOR for this letter: you** — `(vi)` then the price; Arman's word on `(i)` may arrive while you are in it, and if it does, `(i)` lands ALONE first (edit + re-seal, one commit, nothing else), then `(vi)` on top.


---

## `2026-09-19_1455_mothership_MARKER-C-6c-i-SANCTIONED_Arman-yes-sanctioned_the-subject-matter-field-lands-ALONE-first-edit-plus-re-seal-one-commit_then-the-two-unfrozen-riders_then-vi.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-19 14:55 +03:30` (machine, Arman's shell) · HEAD == origin == `aab7598` at my hand; your `(vi)` stamp (1443) still unconsumed in your inbox
re: **MARKER `C-6c (i)` — SANCTIONED. Arman, verbatim: *"yes sanctioned"* (Δ79, 14:55).** Your price stands as priced; nothing about it changed in the relay.

# 1 · THE ORDER — the marker re-sequences your queue, nothing else
1. **`(i)` LANDS ALONE FIRST.** `subject?: string` on `ConceptSpace` (`src/types/geometry.ts:127–133`, FROZEN row 90) with its comment — the researcher's ground as you priced it: *a concept is a structure TOGETHER WITH its subject matter; load-bearing for identity across isomorphic casts; READ on the card beside the person's label; never the corner's `label`, never the `warrant`.* **The edit + the re-seal of row 90 in ONE commit, nothing else in it.** Positive control FIRST, in the report verbatim: the OLD hash `251f1002…` reproduced at the base (`aab7598`), the NEW hash recomputed `tr -d '\r' < src/types/geometry.ts | sha256sum` and written into the manifest row in the same commit. The concept-type witness (`diagnose-the-concept-type`) is the compiler's census of this field — if it pins the type's shape, it follows in the SAME commit or the sweep goes red on your own family again; say which.
2. **THEN the two unfrozen riders, one commit:** `castLoader.ts` — the file's `subject_matter` / `subject` mapped to the field (a string; anything else carried unread as today; **absent = absent — never fill it**), and it LEAVES the warrant now that it has a home; `Panels.tsx` — one card row beside the person's label, in the caster's register, printed ONLY when held (*a positive fact needs a positive mark*; the triangle has none, the T cell has one). The loader witness grows by one clause with a LAW-24 negative (a file with no subject prints no row). The card's sentence: yours to shape, the two-registers law holds (the person's label · the caster's subject, side by side, never merged).
3. **THEN `(vi)`** as stamped at 1443, on top.
4. **THEN the PRICE** (the Layer-3 panel's twice-printing), three lines, in the report.

# 2 · What this marker does NOT do
No second frozen file. No `J`. No change to the card's other rows. The subject is READ, never CHECKED — it enters no structural check and no refusal (the range of a cast is the type; the subject is content the caster wrote, carried as written).

# 3 · ECHO
MARKER `C-6c (i)` SANCTIONED · STAMP `C-6c (vi)` · the PRICE · ECHO §98.3. **Report all four into `.handoff/inbox/mothership/` as one letter or as they land — your choice; `(i)`'s re-seal I verify at my hand before anything else is ratified.**

**NEXT ACTOR for this letter: you** — `(i)` alone, then the riders, then `(vi)`.


---

## `2026-09-19_1621_mothership_MARKER-vi-delta3-AMENDED-a-name-in-BOTH-homes-is-the-contradiction_RIDER-b-the-malformed-item-CARRIED-beside-its-mark_STAMP-C-6d-alpha-the-J-quantities-as-a-second-implementation.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-19 16:21 +03:30` (machine — the sandbox's clock in Arman's zone; the native shell was down this minute) · HEAD == origin == `aab7598` at my last hand (14:55); your 1443 and 1455 letters still unconsumed
re: **the researcher's pin (1602) is in and RATIFIED (§99). Three things for you: a MARKER amending `(vi)`'s δ3 · a one-line RIDER on `(vi)` · `STAMP C-6d (α)`, queued LAST.** Your order is unchanged at the front: `(i)` alone → the riders → `(vi)` (as amended here) → the price → `(α)`.

# 1 · MARKER on `C-6c (vi)` — δ3 AMENDED, by the researcher's correction of their own 1346
**δ3 as I stamped it (a role's `types.P` against an arity-1 `P` in `relations` with the opposite polarity) is WITHDRAWN — I told you to stop if a role's `types` value could not express a negative, and the researcher has ruled why it cannot: the roles' home is CATEGORICAL.** `K: v` asserts `K(x) = v` and denies every other value; the negative of `windowed: "YES"` is `windowed: "NO"`, never a polarity; `'UNKNOWN'` = unrecorded. **⇒ THE BOTH-HOMES CONTRADICTION NEEDS NO NEGATIVE: a type-name declared in the SIGNATURE that also appears as a key in any role's `types` is itself the contradiction — refused at the cast, named (the class of a signature name with two arities: one name has one home).** The card's both-homes count stays right because the names are disjoint.
**δ3 acceptance, replaced:** a fixture with `member_status` in `signature` (arity 1) AND on a role's `types` is refused naming `member_status` — *the record states two things about one name · "member_status" is declared in the signature and on the roles*; the T cell as landed (unary only on roles) loads unchanged. The both-homes COUNT does not double-count — nothing to measure there now. δ1 and δ2 stand as stamped.

# 2 · RIDER on `C-6c (vi)` — the malformed item is **(b): CARRIED beside its mark**
The researcher's line: a role without an id, a relation without type or terms, a polarity that is neither, an axiom without a sentence are not items OF the structure (taking the structure whole is not a partial take) — **but they are bytes the person wrote, and bytes are carried: on the `warrant`, by index, beside the mark that names them.** Today `:86`, `:119`, `:124`, `:142` mark and do not carry — (c) by a hair. One line each: carry the raw item under `warrant` keyed by its home and index (`roles.3`, `relations.7`, `axioms.1`), the same treatment closure and arity breaks already get. **Acceptance:** a fixture with one malformed relation loads with the mark AND the raw item present on the warrant at its index; the witness pins both, with the LAW-24 negative (a clean fixture carries nothing).

# 3 · STAMP `C-6d (α)` — THE `J` REGISTER'S QUANTITIES, computed on the TYPE, no surface yet
**Why (α) before the surface, and why it is not `C-4AMALGAM` again:** the surface is RULED (§98.2) and the definitions PINNED (§99); the ADR 0031 kill-condition pattern — two independent implementations — is what made its numbers trustworthy, and the register's numbers get the same: **your module is the second implementation of the researcher's `j_register_quantities.py`.** The person's ACT (the take) is (β), chartered when the designer answers three riders the pin raised (the offering at scale; the empty core's two shapes; the tie sentence). Nothing in (α) is person-facing; nothing in it posits a `J`.
**THE DEFINITIONS (researcher, 1602; the ruling file `.handoff/RULING_THE-J-REGISTERS-QUANTITIES-ON-THE-TYPE_…md`; the instrument `.handoff/instruments/connection_layer_reference/j_register_quantities.py` — read it, then implement it INDEPENDENTLY on `ConceptSpace`/`EdgeIdentification`):**
1. **Shared signature by name** = type-names in both casts with the SAME arity + unary keys on roles of both. A name with two arities across the casts is NOT shared — named, never refused. Names translated by the person's τ (`EdgeIdentification.types`, arity-preserving) count as shared under τ.
2. **Candidate** = a partial injection `j` of `X`'s roles into `Y`'s with NO KNOWN CONFLICT (a tuple over `dom j` known on both sides has the same value — that is reflection both ways) and weight > 0. **`J = ∅` is not a candidate.** Offered: full-size candidates weight-ordered; among equal weights the inclusion-maximal.
3. **Agreement** = a tuple over `dom j` in a shared type known on BOTH sides with the SAME value; **`does-not-hold` = `does-not-hold` IS an agreement; a shared unary VALUE is an agreement.** Weight = agreements. Support of a pair = agreements whose `X`-tuple contains it; support 0 = UNSUPPORTED, offered marked.
4. **Exposure** = known on exactly one side; **unrecorded** = `Σ|dom j|^arity − agreements − exposure` over the shared signature. The opposite value is a CONFLICT, which a candidate does not have by definition — two counts on a candidate, never a third; the third exists only on a FIAT-extended `J`, printed by name per conflicting tuple.
5. **`Aut(X)`** = permutations of `X`'s roles preserving the WHOLE record exactly (known ↦ known same value, unrecorded ↦ unrecorded) on the FULL signature, never the shared reduct. Two candidates are ONE READING iff `j' = β∘j∘α⁻¹` (`α ∈ Aut X`, `β ∈ Aut Y`) — a gauge orbit. The count the register will print is ORBITS.
6. **The empty core, two ways, named:** (a) no type shared by name; (b) shared types and every consistent injection weight 0 (a cast with no relations is (b)).
**THE SEAL — the numbers, from the researcher's run AND my own run of the same instrument (sandbox python3, 96 s; every number identical, ✔):**
```
triangle × triangle-symmetric   |Aut| 3 / 6   top weight 3 · 6 tied · 1 orbit   exposure 3 · unrecorded 3   consistent full injections 6
triangle × triangle             |Aut| 3 / 3   top weight 3 · 3 tied · 1 orbit   exposure 0 · unrecorded 6   consistent full injections 6
t-cell × t-cell                 |Aut| 1 / 1   top weight 21 · 1 tied · 1 orbit   exposure 0 · unrecorded 1489   consistent full injections 3,104,904
   (top candidate's per-pair support: r0 6 · r1 6 · r2 4 · r3 2 · r4 2 · r5 2 · r6 3 · r7 2 · r8 4 · r9 2)
triangle × t-cell               EMPTY CORE (a): no relation-type in common
```
**Falsifiers (LAW 24, stated by the researcher, held):** `t-cell × t-cell` *1 tied* fails on a second automorphism; `triangle × triangle` *3 tied* fails on a second relation-type. The symmetric triangle's `|Aut| 6`, *6 tied*, *exposure 3* are FORCED by full symmetry — a control, not evidence. ⚠ **A disagreement between your module and these numbers reopens the definition, not your code first — report it, do not tune to it.**
**Boundary:** a NOT_FROZEN module under `src/lib/` (classified in its commit), pure functions over two `ConceptSpace`s and an optional τ; a witness that runs it on the fixture pairs and pins the seal; **no surface, no store action, no written `J`, nothing in `Panels`/`ExploreWindow`.** RECORD NOT READING: `EdgeIdentification.roles` and `.types` are the INPUTS; `support` and `fiat` are DERIVED — never read from the record, re-derived at every read (whether (β) writes them is (β)'s question).
**THE PRICE INSIDE (α), measured, not assumed:** the reference enumerates `10!` injections for `t-cell × t-cell` — 96 s in Python at my hand. The real casts are Flow (14 roles) and Φ (9). **Your mechanism is yours** (pruning on conflicts as you go is the obvious cut); the acceptance names the time for `t-cell × t-cell` in your witness, and states in words what the module returns when a pair is beyond its budget — a limit found at pick-time costs one pick; found after the person has waited, it costs the act (MISPLACED). ⚠ One case the pin does not reach and I have asked the researcher for: "full-size" when `|X| ≠ |Y|` and when no full injection is conflict-free. Pin it in the witness as UNPINNED (skip, named), not guessed.

# 4 · ECHO
MARKER δ3-amended · RIDER (b) · STAMP `C-6d (α)` · standing: MARKER `(i)` sanctioned · STAMP `(vi)` · the PRICE · ECHO §98.3, §99.

**NEXT ACTOR for this letter: you** — nothing here moves ahead of `(i)`.


---

## `2026-09-19_1641_mothership_MARKER-on-C-6d-alpha_clause-2-AMENDED-full-size-WITHDRAWN_an-OFFER-is-a-maximal-conflict-free-candidate-whose-EVERY-pair-is-SUPPORTED_size-follows-from-support_one-manufactured-falsifier.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-19 16:41 +03:30` (machine — the sandbox's clock in Arman's zone) · HEAD == origin == `aab7598`; your four letters (1443 · 1455 · 1621 · this) unconsumed
re: **MARKER on `STAMP C-6d (α)` — its clause 2 is AMENDED by the researcher's line (1632): "full-size" is WITHDRAWN.** Nothing else in (α) moves; your order is unchanged.

# 1 · CLAUSE 2 of (α), replaced verbatim
**THE LINE (researcher, 1632; ruling §3b; instrument re-run, `all_top_supported` True ×3):** *An OFFER is a maximal conflict-free candidate whose EVERY pair is SUPPORTED — takes part in at least one agreement — extended until no further supported pair can be added without a conflict. Size follows from support, never from `|X|` or `|Y|`.*
- `|X| ≠ |Y|`: nothing changes; no side's roles need all be in `dom j`.
- No conflict-free full injection: nothing changes; fullness was never the criterion.
- Several sizes: yes — ordered by WEIGHT, not size. An offer's pairs each have support ≥ 1 by definition; **an unsupported conflict-free pair is NEVER in an offer** — it is the person's extension (fiat), marked support 0, its conflicts printed by name.
- No injection yields a single agreement: the EMPTY CORE (b).
- The seal's four tables stand unchanged: every top candidate found at full size on the fixtures IS a supported core (checked on the run), and no smaller supported core reaches the top weight (dropping a supported pair drops an agreement).
**So the UNPINNED case in my 1621 is now PINNED — remove that skip from the witness.** Clause 2's earlier sentence ("full-size candidates … inclusion-maximal among equal weights … support 0 = UNSUPPORTED, offered marked") is struck; support 0 is not offered.

# 2 · ONE manufactured falsifier for the new clause (LAW 24 — no fixture in the wild exercises it, so make one)
`X` = the triangle; `Y` = the triangle plus one isolated role `w` (no tuple mentions it). **Pinned:** every offer on `X × Y` has size 3 and `w` is in none of them; the injection `x↦x, y↦y, z↦z` is an offer and `{…, ?↦w}` is not a distinct offer (it is the same offer plus an unsupported rider). The LAW-24 control: `X × X` offers unchanged (3 tied, one orbit). ⚠ The wild case — Flow (14) × Φ (9), weight-4 cores of size 3 and of size 4 side by side, the 09-08 "size-5 candidate" NOT an offer (`edge_candidates_partial_isomorphism.py`) — stays ⚠ on the researcher's report until Flow and Φ exist as `.cast.json`; **transcribing them is NOT in (α) — price it in the report (the T cell cost you what?), do not do it.**

# 3 · ECHO
MARKER (α)-clause-2 · standing: MARKER `(i)` sanctioned · STAMP `(vi)` + MARKER δ3 + RIDER (b) · the PRICE · STAMP `C-6d (α)`.

**NEXT ACTOR for this letter: you** — still `(i)` alone first.


---
