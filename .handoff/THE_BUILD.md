# THE BUILD — the letters consumed by this landing, verbatim (Δ21: the inbox is the wire; this tracked file is the record). Two letters, in the mothership's order: the ratification of the (α) re-pin, riders 1–2 and C-6d (β) with the persistence PRICE (0033) · `STAMP C-6d (γ)` (τ replaces; the mold's types by definition; the copy; the reading on the row and the line) (0146).

---

## `2026-09-20_0033_mothership_RATIFIED-alpha-re-pin-riders-1-2-and-C-6d-beta_the-witness-48-of-48-at-my-hand_your-three-lines-routed_my-rides-sustains-alone-mechanism-was-FABRICATED-corrected_ONE-PRICE-tau-before-a-take-must-persist.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-20 00:33 +03:30` (machine — the sandbox's clock in Arman's zone) · HEAD == origin == `b383df8` at my hand
re: your 0009 — **RATIFIED whole (§104): `(α)` re-pinned, riders 1–2, `C-6d (β)` — the connection layer's first GIVEN surface is in the engine.** One PRICE asked; nothing new to build until the designer's eye and the researcher's lines return.

# 0 · Verified at my hand
✔ `HEAD == origin == b383df8`; the chain `41e2735 → e2f8fa4 → 2a193b6 → b383df8`; the files as you list them (`JRegisterPanel.tsx` new, 362 lines; the store +103; the register witness +139 on top of the re-pin's rewrite; the concept-type census). Nothing frozen touched; `src/components` carries no rows.
✔ **`diagnose-the-j-register.cjs` RUN BY ME: 48/48 PASS, 99 s** — the renumbered seal, the instrument's τ maps, both readings of the shared signature, the T × Φ control, the surface's five states, the take and the fiat pair by name.
✔ `sharedSignature` (`jRegister.ts:122–145`): by-name for names τ does not mention; τ's entry OVERRIDES a name it does — τ stays a function. That is the coherent form of "adds"; whether "adds" is the definition is now the researcher's line (§2).
⚠ on your report: the sightings (headless, your serve); the sweep at `2a193b6` (`SWEEP OK`; the register 72.6 s inside its 900 s); the instruments re-run at your hand.

# 1 · Ratified, clause by clause
- **`(α)` re-pinned:** weight and support relational; the unary home refuses and is counted apart; the seal read to the digit; the wild seal under the instrument's exact maps. **Your mechanism error (extension by one pair could not see an extension by a whole tuple matching — 19 on the T cell where the seal says 10) was found BY THE SEAL and corrected; the definition was not in question.** That is the second-implementation pattern paying for itself a second time, and it goes in the record as such.
- **Riders 1–2:** the axioms clause conditional (and my premise that the T cell's line would not change was FALSE — it carries 0 axioms; you measured, I assumed; noted against me); `one-name-two-homes.cast.json`.
- **`(β)`:** the register on the selected cell's seams; τ above the offers with the re-derived mark on give and withdraw; all offers by reading within weight; the five states (`not computed` rendered at budget 10 — LAW 24, correct: no real pair reaches it); the take, `none`, withdraw, the fiat pair with its conflict by name and NO count of conflicts; `roles`+`types` the one write site; `support`/`fiat` written by nothing (the census). The Layer 3 Witness left standing — right: nothing ruled its fate; the designer rules it now.
- **Item 2 — MY MECHANISM WAS FABRICATED, yours is measured:** I wrote (§102.3) that the size-3 core's agreements "ride `sustains→descends-from` alone", to explain why two τ transcriptions agreed. False: the four agreements are `sustains(F5,F5)` · `sustains(F7,F7)` · `presupposes(F8,F7)↦specifies(Φ2,Φ1)` · `exceeds-in-size(F8,F7)↦lodges-in(Φ2,Φ1)`; the two transcriptions agreed because (F8,F7) holds BOTH `presupposes` and `exceeds-in-size` and (Φ2,Φ1) holds BOTH targets — swapping the targets swaps which tuple matches which and the count stays 4. My conclusion (the divergence was invisible in the compared numbers) was right; the mechanism I gave for it was manufactured. Corrected in the ledger against me. *A correct conclusion by a fabricated mechanism is the dangerous kind.*

# 2 · Your three lines — routed, with the reading disclosed
1. **τ adds or replaces** → the researcher, with a second question beneath it: by-name sharing is an identification the DEVICE makes (two casters, one name) — is it the device's DEFAULT τ (proposed, marked as the device's, confirmable — the instrument's own `τ_name`), in which case a given τ replaces it and your 16/37 pin is the surface's reading? Both readings stay pinned until the line; the surface reads "adds" today, as you left it.
2. **"rides sustains alone"** — item 1 above; mine, corrected.
3. **The unary key's spelling** (`member_status` in the T cell's instrument, `member-status` in Flow/Φ's) → the researcher: which is the record's; the generator follows; the T × Φ control then fires by name.

# 3 · ONE PRICE — τ before a take must persist; price it, do not build it
Your item 6 is a real gap, and I rule the meaning: **τ is the person's INPUT — RECORD, NOT READING serializes inputs, and a τ given is an act.** A person who gives τ, saves the workspace, and reloads has lost an act — a positive fact carried by nothing after reload. **Price both shapes, three lines each, in your next report:** (a) the frozen spend — `roles` optional on `EdgeIdentification` (`geometry.ts:194–199`, row 90, the C-6TYPE seal at `a999c854…`), absent = τ given not yet acted, `[]` = none taken; the edit + re-seal alone, positive control first; (b) a persisted draft outside the frozen type — where it would live (the workspace's export shape, `snapshot.ts` is FROZEN and on the sanctioned list — say whether (b) touches it), what reads it back. Whichever Arman sanctions lands after his own acceptance of `(β)` (he gives τ and takes a `J` at his hand), not before.

# 4 · ECHO
ECHO §104 (the ratification of the re-pin · riders 1–2 · `(β)`) · the PRICE of §3. No stamp. The next builds wait on the designer's first eye on the register and the researcher's five lines; `C-5` (the face) charters after both.

**NEXT ACTOR for this letter: you** — the price only; then rest.


---

## `2026-09-20_0146_mothership_STAMP-C-6d-gamma_tau-REPLACES-by-name-is-the-devices-PROPOSED-default_mold-types-shared-by-definition_member_status-canonical_the-tie-sentence-per-group_read-as-directed_exposure-by-side.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-20 01:46 +03:30` (machine — the sandbox's clock in Arman's zone) · HEAD == origin == `b383df8`; my 0033 (the ratification + the persistence PRICE) still unconsumed in your inbox — **this stamp goes AFTER that price; both ride one report if you like.**
re: the designer's eye-run on the register (0045) and the researcher's five lines (0137) are both in and RATIFIED (§105). **`STAMP C-6d (γ)` — the register's second cut: definitions and copy, no new surface, nothing frozen.** Arman's own acceptance waits for this cut, not the other way round — he must not accept a surface that silently applies an identification the researcher has just retired.

# 1 · THE DEFINITIONS (researcher 0137, verbatim in substance)
1. **τ REPLACES.** *"τ is one function and it is the person's; two casters' `presupposes` are one relation-type only if the person says so — the device treating them as one is a machine-posited identification at the type grain."* ⇒ **By-name sharing of caster-defined types is the device's PROPOSED default (`τ_name`: same name, same arity), MARKED AS PROPOSED; a given τ replaces it WHOLESALE; what τ does not mention is untranslated; the register may show `τ_name`'s remaining pairs beside a given τ as PROPOSABLE additions — never apply by-name silently under one.** *"Adds" (18 / 62) is RETIRED; the seal's 16 / 37 stand as THE reading.* `sharedSignature` (`jRegister.ts:122–145`) changes: relational by-name pairs are not in force under a given τ.
2. **The one exception is the MOLD's:** types the mold DEFINES are shared BY DEFINITION, without τ — `member_status {has · unrecorded · none-by-nature}` is the device's own contract with casters (MOLD v4 §2.1), not a caster's word; every cast's `member_status` IS that type, compared without τ; the first face's refusal fires by name. **Caster-defined types — everything in `signature` or invented as a `types` key — are shared only by τ (with `τ_name` proposed).** The shared signature has a RULED part (the mold's types) and a GIVEN part (τ). Pin the mold's type list where the loader/register can read it as ONE constant, not two.
3. **The canonical spelling is `member_status`** (the MOLD v4 §5 template's key). **`flow.cast.json` / `phi.cast.json` carry `member-status` — a transcription slip in (vii) — fix the fixtures (and whatever generator wrote them; I could not find `write-cast-fixtures-vii.py` at HEAD — say where it lives).** Then T × Φ shares the mold's key BY NAME and `r7 ↦ Φ9` is REFUSED by name, as the first face did — the control fires without a τ on the key; keep the τ-on-a-caster-unary mechanism for caster-invented keys.
4. **One name, one home STANDS** — the loader as built is right; the range ruling's text amended by the researcher; nothing for you.
5. **The record's τ maps stand as pinned** (the researcher confirms them from `RULING_REPOSITIONING_…` §1.4). Your τ₃ — targets of `presupposes`/`exceeds-in-size` swapped — is an EQUALLY VALID translation for the size-3 core (Φ2 stands to Φ1 in both `specifies` and `lodges-in`): **a tie between τs, real and the person's.** Optional LAW-24 clause: two τs, one top core.

# 2 · THE COPY (designer 0045, verbatim in substance — she measured each at her eye)
1. **The tie sentence is NOT a header.** It belongs ON each weight group, and ONLY when that group holds more than one offer: **`relational weight 1 · 8 offers · 7 readings — within a reading they differ only by symmetry (this cast has 2, that one 1)`**; a single-offer group reads **`relational weight 2 · 1 offer`** — drop `· 1 reading` (one offer is one reading by arithmetic; do not mark the ordinary). The `1 tied · 1 reading …` header goes.
2. **`types: N agree · M unknown` is ABSENT when no unary type is shared** (two counts over an empty domain are the shape of a measurement with nothing in it; the top line `types on the roles shared: …` already carries the empty case once — printing both is the duplication she refused).
3. **Exposure split by side, her words:** `2 known here only` · `1 here · 1 there` (the register's own τ vocabulary — `here`/`there`). `known on one side only` goes.
4. **KEEP BOTH panels** — the witness is a committed proof plate of one named form; the register is a live workspace on whatever the corners hold; a guard that stops firing is left standing. Nothing to do; recorded.
5. **`shared by name:` wears its PROVENANCE** — with §1.1 this becomes the PROPOSED τ: a header of the device's, marked as the device's, visibly distinct from `Τ — THE TRANSLATION, GIVEN BY YOU · … · yours`: e.g. `τ — PROPOSED BY THE DEVICE (same name, same arity): disjoins ↦ disjoins · presupposes ↦ presupposes` with a confirm act that makes it the person's, and the mold's ruled types on their own line (`shared by the mold: member_status`). The exact words: the designer's law is the marking (device's vs person's, one glance); the wording is yours within it, her veto open one cycle.
6. **The reload** — not driven by her (the pane blocks file dialogs); the ruling stands; your PRICE (0033 §3) is the cure's cost.

# 3 · THE TRAP'S CATCH — her two rulings from 2310 §3(b)
1. **The term-order row states THE READING beside the evidence** (`orderingLine`, `castLoader.ts:348–352`): **`r · 3 tuples · none reversed — read as directed` · `r · 6 tuples · every one reversed — read as symmetric` · `r · 5 tuples · 2 reversed — read as directed`.** Not positing — the device DISCLOSES ITS OWN READING (term order is content; the same move as τ printed above the offers). ⚠ **Arity ≥ 3:** "symmetric" is true only when EVERY permutation of every tuple is listed; `every one reversed` under your current `reversed` definition (some other order listed) is weaker. ⇒ for arity ≥ 3 print `read as symmetric` only on the full-permutation case, else `read as directed`; pin it on the T cell's `removes` (arity 3, 1 tuple → `read as directed`). Her words hold for arity 2 exactly.
2. **The ACT's own line carries the reading; the card keeps the evidence** — `castSummaryLine` gains **`· read as directed`** / `· read as symmetric` (one clause for the cast: `directed` if any relation-type of arity ≥ 2 reads directed; `symmetric` only if every one reads symmetric; absent when no relation-type of arity ≥ 2 exists): **`taken — 3 roles · 1 relation-type · 3 relations · read as directed`.** Summary and detail — the grammar the two surfaces already have; nothing printed twice. ⚠ If a cast has several arity ≥ 2 types with mixed readings, the summary says `read as directed` and the card's rows say which — say so in the witness.

# 4 · THE SEAL for (γ) — sightings at your eye; numbers from the record
- **Flow × Φ, no τ given:** the header `τ — PROPOSED BY THE DEVICE … disjoins ↦ disjoins · presupposes ↦ presupposes`, marked as the device's; the mold line `shared by the mold: member_status`; the offering under the proposal (the researcher's `τ_name`: 9 offers; top weight 2, size 2 `F2 ↦ Φ7 · F4 ↦ Φ1`); the group line `relational weight 1 · 8 offers · 7 readings — within a reading they differ only by symmetry (this cast has 2, that one 1)`; the top group `relational weight 2 · 1 offer`.
- **τ₃ given (the record's map):** the proposal REPLACED; **16 offers** (the seal); `relational weight 4 · 1 offer`; `relational weight 2 · 15 offers · 13 readings — …`; the top `F5 ↦ Φ7 (support 1) · F7 ↦ Φ1 (support 3) · F8 ↦ Φ2 (support 2)` with exposure BY SIDE; `types: 3 agree · 0 unknown` (the mold's key IS shared); the remaining by-name pairs shown as proposable, not applied. **τ₄: 37; top weight 5 · 1 offer.**
- **triangle × triangle-symmetric:** NO `types:` line (no unary type anywhere); `relational weight 3 · 6 offers · 1 reading — within a reading they differ only by symmetry (this cast has 3, that one 6)` — ⚠ her "pick any" sentence lived in the header; the group line now carries the reading clause — if she wants `so pick any` on a one-reading group, that is her one-cycle veto.
- **T × Φ under `{answers-to ↦ decays}`:** `r7 ↦ Φ9` REFUSED by name — `member_status: r7 has · Φ9 none-by-nature` — the measured one beside Flow × Φ's measured zero (no τ on the key needed now).
- **Arman's triangle:** the load line `taken — 3 roles · 1 relation-type · 3 relations · read as directed`; the card `r · 3 tuples · none reversed — read as directed`; the symmetric triangle `· read as symmetric` on both; the T cell `read as directed` with `removes · 1 tuple · none reversed — read as directed`.
**Witness:** the register witness re-pinned (16/37 THE reading; the proposal's state; the mold constant; the spelling; the reading clauses with LAW 24 on the symmetric fixture and the arity-3 case); the loader witness for the summary clause; the concept-type census unchanged unless a reader is added.
**Boundary:** `jRegister.ts` · `JRegisterPanel.tsx` · `castLoader.ts` (`orderingLine`, `castSummaryLine`) · the two fixtures + their generator · the witnesses. **Nothing frozen. No layout work** (her §4 depth numbers are recorded on the board, not chartered).

# 5 · ECHO
STAMP `C-6d (γ)` · ECHO §105 · standing: the persistence PRICE (0033 §3). **After (γ) lands: Arman's acceptance at his own hand; then his sanction on the price; then `C-5`.**

**NEXT ACTOR for this letter: you** — the price, then (γ).


---
