# THE BUILD — always current, self-contained

**Written by: mothership · 2026-08-21 · base `846737a` (+ `1727998`) · branch `team-arman`**
*Read `CLAUDE.md` at the repo root first. This file is rewritten per build — git carries the history. **If anything below contradicts what you MEASURE, your measurement wins** — say so in the report.*

---

## 1 · YOUR LAST BUILD — RATIFIED, and the finding is the real product

✅ **`846737a` accepted.** One line, alone, at the ruled boundary. I am not re-running your witnesses; **you are the last word on whether they passed. I am the last word on what their passing MEANS.**

★ **What you found is worth more than the cut:** *"not absent — unreadable."* The live skeleton exists (census: one `LineSegments2`, 12 cube edges) and it is a **17×27-pixel speck** at the bottom of the frame; after the map the two pair marks are **4 pixels apart** inside it, behind a plane at depth 26.9 while the marks sit at 47.3. ⇒ **You ran the count AND the eye and reported the gap between them.** That is clause 5 doing its whole job on your first build.

★★ **Two of your acts I want named, because they are the standard:**
- **`diagnose-the-aperture.cjs:436` regex-pinned the pre-ruling arming `summoned ?`** — so the suite read 112 @ 2. **You did not relax the pin; you made it name the DOOR**, so a door-blind revert now fails. ⇒ ***A witness that pins a STRING pins the implementation; a witness that pins the DOOR pins the fact.*** Booked as law.
- **`tsc -b --noEmit` TS6310s on this tsconfig.** You measured it, disclosed it, and did not quietly substitute. ⇒ **`CLAUDE.md` is corrected. That file is a claim like any other and you were right to test it on day one.**

## 2 · ⛔ THE MEANING RULING — my diagnosis was RIGHT IN PRINCIPLE AND INCOMPLETE IN FACT

I ruled mount 1 disarmed on the theory that *the finished answer was covering the question.* ✔ **The principle stands** — a finished, already-identified diagram may not hold the stage while the person is mid-question. ⛔ **But it was not why he saw nothing.** Your drive shows the mark is unreadable **after** mount 1 vanishes, because the live skeleton draws **at the row, at a scale and depth where no eye can resolve it**, behind a body that stays risen.

> ### ⇒ **THE DEFECT IS PLACEMENT AND SCALE, NOT ARMING.** The question was given the worst seat in the room and the answer was given the best one. **I removed the answer from the good seat and left the question in the bad one.**

## 3 · THE TASK

> ### **The live skeleton must occupy the stage the finished diagram vacated — the position and scale where the person is ACTUALLY LOOKING when the door is open.**

⛔ **I am not specifying the mechanism, deliberately.** The risen/inspection slot already exists and already works for mount 1; whether the live one takes that slot, or gets its own, or needs the aperture body handled too, is **yours** — you have measured that geometry and I have not.

⚠ **What I know and you should not have to rediscover:** the risen aperture body is selection-driven and was outside the last ruling. **If it must move or dim for the question to be visible, that is inside this one** — say what you changed and why.

**ACCEPTANCE — and it is not a census:**
> **Arman opens the door and SEES the wireframe, without being told where to look.** Two faces picked ⇒ he can see which two. A map picked ⇒ he can see the pair marks as two distinct things.
⛔ **A pixel measurement is not this acceptance.** If you can only find it by knowing where to look, it has not landed. **Report what you saw, at the frame he is in.**

## 4 · ✅ YOUR ROUTED CONTRADICTION — RULED

`THE_BUILD.md` says git carries the history; `.gitignore` says `.handoff/` is never committed. **You held instead of picking. Correct.**

> ### ⇒ **`.gitignore` YIELDS, for exactly two paths: `.handoff/THE_BUILD.md` and `.handoff/THE_BUILD_REPORT.md`. The rest of `.handoff/` stays ignored.**
> **Why:** the ignore rule exists because seat *mail* is noise. **These two files are not mail — they are the project's only record of what was built and what was seen**, and they are overwritten every build. Uncommitted, "git carries the history" is false. ⇒ **Negate the ignore for those two paths and commit the report with the build it describes.**

## 5 · HELD
**F.0b (`68d6a14` / stash `d5adab1`) stays parked.** ✔ Finding it uncommitted in the working tree, verifying byte-equality, and stashing it *before* measuring was exactly right.

## 6 · THE STANDING ASK
Arman: ***"i'm tired. it's been 3 months. i need a race to the finish line."*** ⇒ **Build, drive, report. Smallest measurement first.** Route only what genuinely needs a ruling, in three lines — as you did.
