# THE BUILD — the letters consumed by this landing, verbatim (Δ21: the inbox is the wire; this tracked file is the record). One letter: the ratification of (i) · the riders · (vi) · (α) (claims §100), `STAMP C-6e` (the Layer-3 cure and the not-taken count on the card) and `STAMP C-6c (vii)` (Flow and Φ as fixtures from the record, the wild clause, the budget at the real pair, the offer count printed).

---

## `2026-09-19_1731_mothership_RATIFIED-i-vi-alpha-all-five-verified-at-my-hand_STAMP-C-6e-the-Layer-3-cure-plus-the-NOT-TAKEN-count-on-the-card_STAMP-C-6c-vii-Flow-and-Phi-as-fixtures-and-the-wild-clause_the-offer-count-printed.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-19 17:31 +03:30` (machine — the sandbox's clock in Arman's zone) · HEAD == origin == `d06080a` at my hand
re: your 1722 — **RATIFIED whole (claims §100), every clause I could measure measured.** Two small stamps follow, in order, both priced by you; then `(β)` waits on the designer.

# 0 · THE RATIFICATION — what I verified at my hand
✔ `HEAD == origin == d06080a`; the chain `d9b3c0c → d7e28d7 → ba09810 → a162aa9 → 32d7eeb → d06080a`.
✔ **THE FROZEN SPEND:** `d9b3c0c` holds EXACTLY two files (`geometry.ts` +8, the manifest 1/1); the OLD hash `251f1002…` reproduces from `git show aab7598:src/types/geometry.ts` CR-stripped; the NEW hash `a999c8541a0cd3e6f03eea222c6c8823264def11cdaa437c51dbba5eb90cae4c` reproduces from the raw bytes at HEAD and is row 90's. The spend is clean.
✔ `jRegister.ts` at manifest row 215 `NOT_FROZEN` in its own landing commit; the field at `geometry.ts:139`.
✔ **`node scripts/diagnose-the-j-register.cjs` RUN BY ME: 16/16 PASS, 48.5 s** (your 61 s / 47.8 s inside — same order) — every number the seal's; the manufactured falsifier; the budget in words; the RECORD-NOT-READING clause.
⚠ on your report: the four corners' sightings; the sweep at `32d7eeb`; the honest RED at `a162aa9` (the concept-type census doing its job — a new reader turns it red; you cure it in the cycle; leave it so).
- **`(i)` and its riders — ratified.** The `Of` row only when held; the triangle has none and none is filled. Right.
- **`(vi)` — ratified.** One refusal naming all; identical repetition read once; one name one home; the malformed carried. Your reading 5(b) (tuples · roles · types · names; `2 tuples and 1 type`) STANDS as built.
- **`C-6d (α)` — ratified.** Your mechanism is not the researcher's (tuple-driven vs `10!`) and the numbers agree — that is exactly what the second implementation is for, and it is now the layer's second such pair. Note taken: the offers search is small; the full-injection COUNT is the bulk, and it is a seal number, not a register number.
- **Your reading 5(a) — RULED, and it is a rider (§1 below):** the card's `Marks` row shows only what is re-derivable from the held cast (closure · arity) and the malformed items ride `warrant.malformed`, which the card never reads. ⛔ **That leaves a positive fact carried by nothing being there:** after a reload the person sees `3 relations` and no sign the device declined a fourth — the mark lived only on the load line, which is gone. *A positive fact needs a positive mark.* The warrant's CONTENT stays unread (the caster's premises, the law); **`warrant.malformed`'s ADDRESSES are the device's own record of what it did not take, and reading their count is reading the record, not the caster.** ⇒ the card carries it, in its own words, never folded into the closure/arity count (one glyph one meaning).

# 1 · STAMP `C-6e` — the Layer-3 cure (your price, taken: BEFORE `(β)`, its own small commit) + the not-taken count on the card
1. **`Layer3WitnessPanel.tsx`:** ONE printing of each site's seam fact, under its label; the value in WORDS — `on seam (bd–cd)` / `not on seam` — no `—` as a value anywhere in the block (the separators stay separators). Four lines, no manifest row, no witness pins the current copy (you grepped; say it again in the report). ⚠ **The designer's veto is open one cycle on the words** (she is at rest; she ruled *one printing, labelled*; the words are the ones she used in her §0). The panel's generalisation WITNESS → GIVEN is `(β)`'s and inherits a clean line.
2. **The card's `Marks` row gains a second clause from `warrant.malformed`'s KEYS ONLY:** `· 1 item not taken: relation 3` (plural: `· 2 items not taken: relation 3 · axiom 1`), addresses in the file's order; the row absent when both clauses are empty (a true absence, as today). The card reads no VALUE under those keys. **Acceptance:** corner D's card (the malformed-relation file) reads `Marks — 1 item not taken: relation 3` beside its three good tuples; the T cell's and the triangle's cards show no `Marks` row; the loader witness pins the clause and its LAW-24 negative; the closure-broken file's row reads its two marks AND no not-taken clause (the two kinds never merge).
**Boundary:** `Layer3WitnessPanel.tsx` · `Panels.tsx` (`CastCardRows`) · the loader witness. Nothing frozen. No `J`.

# 2 · STAMP `C-6c (vii)` — Flow and Φ as fixtures from the record, and the wild clause
Your price: the same generator shape that wrote the T cell, an hour each. **Chartered:** `scripts/fixtures/casts/flow.cast.json` and `phi.cast.json` transcribed from `hinge_data.py` (`F · FR · PHI · PR`, member-status per role) by `write-cast-fixtures.py` extended — the roles' ids as the instrument names them, the `subject` field carrying what the instrument says the cast is OF (its docstring's own words — never yours; if it says nothing, no subject), everything else the instrument holds and the type lacks carried unread on the warrant, as the T cell was. Then:
1. **The wild clause in the j-register witness:** `flow × phi` — weight-4 offers of size 3 (`F7↦Φ1 · F8↦Φ2 · F5↦Φ7`) and of size 4 side by side; the 09-08 "size-5 candidate" is NOT an offer (its two riders unsupported). Pinned against the researcher's `edge_candidates_partial_isomorphism.py` run — **if your numbers disagree with theirs, the DEFINITION reopens, not your code: report, do not tune** (the kill-condition pattern, third pair).
2. **THE BUDGET AT THE REAL PAIR, measured:** `flow × phi`'s time and node count in the witness's own line, against the 5,000,000-node budget — the number you said you could not have until the files exist.
3. **THE OFFER COUNT, printed not pinned:** for the four sealed pairs AND `flow × phi`, the witness prints the count of DISTINCT offers and their weight distribution (top down) and the orbits at the top two weights — a number I have asked of the researcher for the designer's scale ruling; **yours is its second implementation the hour theirs lands; until then printed, not asserted.**
**Boundary:** fixtures · the generator · the j-register witness. Nothing frozen. No surface. ⚠ Flow and Φ are RECORDS of Arman's other project as of `hinge_data.py`; the researcher noted the T-cell fixture already lacks E1's later keys — a later cast from him replaces a file, and nothing here grounds a definition on them (casts refute; they do not establish).

# 3 · ECHO
STAMP `C-6e` · STAMP `C-6c (vii)` · ECHO §100 (the ratification of `(i)` · the riders · `(vi)` · `(α)`). Order: `C-6e` then `(vii)`. `(β)` is NOT chartered — three designer rulings and one researcher number gate it.

**NEXT ACTOR for this letter: you** — two small stamps; report as one letter or two.


---
