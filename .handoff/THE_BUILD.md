# THE BUILD — always current, self-contained

**STAMP: `B-2026-08-21-C`** ⛔ **Echo this stamp at the top of your report.** If it does not match what I wrote, one of us read a stale file — see §0.
**Written by: mothership · base `0109842` · branch `team-arman`**
*Read `CLAUDE.md` at the repo root first. **If anything below contradicts what you MEASURE, your measurement wins.***

---

## 0 · ⛔ TASK 0 WAS DROPPED — the mechanism was mine, and here is the fix

Last build opened with a **§0 TASK 0** — track `CLAUDE.md`, decide the untracked paths, **push**. ⛔ **None of it was done.** At this moment:
```
## team-arman...origin/team-arman [ahead 30]
CLAUDE.md  ⛔ STILL UNTRACKED
?? .claude/  ?? BACKLOG.md  ?? _cone_probe.cjs  ?? tasks/  ?? scripts/app-leg/ds_*.png
```

⚠ **THE CAUSE IS MINE AND IT IS STRUCTURAL, NOT DILIGENCE.** ⛔ **I added §0 by EDITING a build you had probably already read.** This file's whole contract is *"always current"* — **and I broke it by amending a live brief instead of issuing a new one.** ⇒ ***A brief is what the reader read at the moment they read it. Amending it after that is writing to a file nobody will open again.***

> ### ⇒ **THE MECHANISM, from now on: this file carries a STAMP, and your report ECHOES it.** A mismatch tells us instantly that the text you built from is not the text I wrote. ⛔ **I will not amend a live build again — a change means a new stamp and a new file.** *Enforced by construction, not by either of us remembering.*

### ⇒ TASK 0, RE-ISSUED. **Do this before anything else.**
1. ⛔ **Track `CLAUDE.md`.** It is the doctrine that tells every seat who it is, and it has now been unprotected for two days.
2. **Track the two evidence frames** `scripts/app-leg/ds_*.png` — ⚠ your earlier frames are in history; these two are not. **An untracked file is an unprotected file.**
3. **Decide the rest and say why for each:** `BACKLOG.md` (a referenced project file) · `tasks/` (report what it is) · `.claude/` (session state — probably `.gitignore`) · `_cone_probe.cjs`. ⚠ **Apply the instrument rule to the probe: *it earns space iff it can FAIL and someone will read the answer.*** Promote it into `scripts/` or delete it. **Do not commit debris to make the tree clean.**
4. ✅ **PUSH to `origin/team-arman`.** **Thirty commits — the whole campaign — exist on one disk.** ⚠ Fast-forward only. ⛔ Never force, never rewrite. **Arman ruled this explicitly.**

---

## 1 · ✅ THE WHOLE-FRAME BUILD — RATIFIED

✔ **Dots dropped via the mount's own `markRadius` dial, frozen `InkedDomain` untouched** — you honoured the ruling **without spending a union**, and *"the tick is now the only dot on a trace, and it means what a lone dot can mean."* **That is the vocabulary argument landing, not just the pixels.**
✔ **The legend stands verbatim.** ✔ **The forced-wall line's every number derived from a new `apertureParityCensus`** — computed, not hardcoded. ✔ **The #37 death-condition booked at its site**, dying by name.

★★★ **AND THE BEST THING IN THE REPORT IS THE WITNESS:** *"the census pinned both directions — fan: exactly one forced wall from 10×3+5×4; cube: zero — **the silence is a check, not an absence**."*
> ⇒ **You witnessed the case where the line does NOT print.** Our standing law is *a positive fact needs a positive mark; never carry a fact by nothing being there.* ⛔ **A conditional line's silence is exactly such a fact — and it was the one thing a witness could not normally see.** ⇒ ***A conditional mark needs BOTH pins: that it fires when it should, and that it stays silent when it should.*** **Standing, and it generalises past this line.**

★★ **AND YOU DISSOLVED THE DESIGNER'S QUESTION BY MEASURING IT.** She saw *"two distinct blue treatments — a dashed horizontal and a dotted vertical"* and correctly refused to rule on a frame. ✔ You measured: **byte-identical dash material (0.09/0.07, uniform), one code path — the "dotted vertical" was the SAME world-space dash foreshortened on a near-edge-on face.**
> ⇒ ***A difference visible only in the projection is not a difference in the thing.*** **Before a visual distinction is ruled into the language, check whether the projection invented it.** ★ Sibling of *a mark emitted only when a resolution fails is not vocabulary* — **this is: a mark distinguished only by viewing angle is not vocabulary either.** ✔ **The legend's first clause stands; nothing goes back to her.**

## 2 · ✅ PART A — measured cleanly, and both are now routed AWAY from you
- ✔ `chiConsistent = gate.sound ? χ === 0 : null` (`level3Invariants.ts:85`) — **the closed-3-manifold expectation applied to every sound complex, including a bounded chamber whose χ=1 is correct.** ⇒ **A verdict computed for one CLASS applied to another** — the comment-precondition family, living in a predicate. **Routed to the researcher; do not touch it.**
- ✔ `"the identified cube"` is a **hardcoded literal in `readDomainSpecimen` (`specimenModel.ts:144`)**, stamped on every domain card. ⇒ **Stale copy on a generic surface. Wording is the designer's.** ⚠ **And `specimenModel.ts` is FROZEN — so its fix is a union and it is NOT on the sanctioned list. Do not open it; I will price it when her words arrive.**

## 3 · AFTER TASK 0
**Report and stop.** ⛔ **Nothing else is queued for you** — the board is genuinely open for the first time in three days, and Arman decides what goes next. ⚠ **Do not start on the parked set (the four frozen unions, #37, D16, persistence) without a build file naming it.**

## 4 · THE STANDING ASK
Arman: ***"i'm tired. it's been 3 months. i need a race to the finish line."*** ⇒ **Build, drive, report. Read the whole frame. Echo the stamp.**
