# THE BUILD — the letters consumed by this landing, verbatim (Δ21: the inbox is the wire; this tracked file is the record). Three letters: the ratification of C-6a part 2 (nothing to build) · `STAMP C-6b` (two copy cuts; the bare branch priced then cut) · `STAMP C-6c` (the connection layer's first person-facing build: the subject-matter field PRICED and STOPPED; the loader; surface 1; surface 2; the fixtures from the record).

---

## `2026-09-18_0620_mothership_C-6a-PART-2-RATIFIED_item-0-of-the-layers-surface-DONE_the-two-relayed-premises-were-the-designers-and-I-should-have-marked-them_the-riders-stay-held_at-rest-until-surface-1.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-18 06:20 +03:30` (machine) · HEAD == origin == `8bda983` at my hand; the line's element at `Workspace3D.tsx:150`; the section at `Panels.tsx:978`
subject: ✅ **`C-6a` part 2 RATIFIED (§95). Item (0) of the layer's surface is DONE — the Ambo states its own gestures, in its own row, byte-identical across every hover, for the first time.** The two premises you corrected were the designer's claims relayed by me without a mark; the stamp should have said whose they were. **The riders stay held; at rest until her surface 1.**

- **The line, the open section, the true absence** — all three at the eye at two window sizes, no jump, no glyph, the walk's line pinned untouched. The drive family not triggered and said so — right.
- **The column's depth** (2178–2968 px of 2879; three screens down) is on the designer's desk beside her line as a depth, not a break — her call at the eye. **Your `Hovering …` observation is with her too.**
- **`fitLog` and the ghost:** both were her sentences, passed through my stamp as ground. *A relayed premise carries the relayer's reading* — filed at the stamp grain against me; you measured both, so no harm landed.
**Nothing to build. The riders (which verb; the three buttons) cut only on her one line; then 6c on her surfaces. ECHO this letter.**


---

## `2026-09-19_1204_mothership_STAMP-C-6b_two-copy-cuts-on-her-riders_the-vertex-tooltip-says-select_the-word-Hovering-leaves-every-readout-branch_and-the-bare-return-at-2506-priced-as-a-refusal.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-19 12:04 +03:30` (machine) · HEAD == origin == `8bda983`; `Hovering` at `Workspace3D.tsx:2506 · :2513 · :2514` verified
subject: ⇒ **`STAMP C-6b` — two copy cuts from the designer's riders (§96), one small price. Copy is behaviour: sweep the predicate, the sentence, the type and the witness (`diagnose-the-ambo-gesture-truth` grows).**

1. **The vertex row's tooltip (`Panels.tsx:2156`): `click: inspect` → `click: select`.** Her ground: the module's consumers are named on SELECTION (`Lift selection → Manuscript` · `Fit Selected` …); *inspect* names the consequence and severs the chain. The three rows now speak alike.
2. **Drop the leading `Hovering ` from every readout branch** (`:2513 · :2514` and the vertex/edge/face branches): `cell a | seed/tetrahedron g0 | id: …` — the identification, clean. Her ground: the person already knows they are hovering; *do not mark the ordinary* — and the empty slot's appearing (your ruling-2 cut) now IS the statement. **The line's `hover — preview what corresponds` is untouched** (it teaches; the readout records).
3. ⚠ **PRICE, then cut if it is what it looks like: the bare `return 'Hovering cell'` at `:2506` (the id not in the shape)** — she names it *a refusal wearing a report's clothes.* Say what that branch MEANS (a hover target the shape does not hold — how?); **the candidate is the true absence (`null`, the slot empty), never a word.** If it is a real state that needs a sentence, three lines up and she rules the words.
**Witness:** the three tooltips' text; no branch begins with `Hovering`; the `:2506` disposition pinned. No frozen file (`src/components`). **Then at rest until her surface 1. Report per §7. ECHO `C-6b`.**


---

## `2026-09-19_1332_mothership_STAMP-C-6c_THE-LAYERS-FIRST-PERSON-FACING-BUILD_price-the-subject-matter-field-FIRST-then-STOP_the-loader-two-refusals-by-name_surface-1-the-gesture_surface-2-the-card_fixtures-from-the-record.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-19 13:32 +03:30` (machine) · HEAD == origin == `8bda983` · `C-6b` still in your inbox (two copy cuts — take it first, it is small)
subject: ⇒ **`STAMP C-6c` — THE CONNECTION LAYER'S FIRST PERSON-FACING BUILD: a corner takes a cast, and its card reads it.** Surfaces 1 and 2 are ruled (§97, the designer's; the range by the researcher; two catches by Arman inside the hour). **Order inside the stamp: (i) a PRICE and a STOP · (ii) the loader · (iii) surface 1 · (iv) surface 2 · (v) the fixtures.** Surface 3 (the `J` register) is hers and charters separately.

# (i) FIRST — PRICE, then STOP: the subject-matter field
The type carries no field for what a concept is OF (the researcher: *a concept = a structure together with its subject matter — load-bearing for concept identity across isomorphic casts*). It rides in `warrant` or nowhere, and **never the corner's `label`**. **A typed field is right — subject matter is READ on the card beside the label; `warrant` is carried unread.** ⇒ **Price `subject?: string` (or the shape you find honest) on `ConceptSpace` in `geometry.ts` — a SECOND frozen spend on row 90. STOP there; Arman sanctions; it lands ALONE in its own commit (positive control first) before the loader maps a file that carries it.** Everything below assumes the field exists or is absent — build the rest so it works either way.

# (ii) THE LOADER — `.cast.json` → `cast?` on the selected corner's packet
- **THE RANGE IS THE TYPE.** `ConceptSpace` as landed (`7bd9cbc`): roles · signature (any arity) · relations with polarity · axioms carried · warrant carried. ⛔ **MOLD v4 §5 is ONE PROFILE — read it as one file-shape mapping to the type (`concept · roles[] · signature[] · relations[] · axioms[]`), and carry EVERYTHING it has that the type lacks (`gloss/source/reason/annotations/derived_check/open_forks/did_not_fit`) UNREAD under `marks`/`warrant` — never checked, never displayed as a verdict.** A bare relational structure (three roles, one binary type, three tuples) loads exactly as well. *The device checks what is true of a STRUCTURE, never of a PRACTICE.*
- **THREE STRUCTURAL CHECKS, and only these: CLOSURE** (every term resolves to a role id; every relation `type` resolves to the signature — an unresolved type has no arity, so arity presupposes closure) · **ARITY** (`terms.length === arity`) · **CONSISTENCY** (*the record is a FUNCTION*: no `(type, terms)` with both polarities; no duplicate role id; no signature name with two arities).
- **TWO REFUSALS, BY NAME — nothing else refuses:** **(1) not a cast** (unparseable, or no `roles`/`signature`/`relations`) → `this file is not a cast`. **(2) a contradictory record** → **refused at the CAST**, pointing at the line: `not taken — the record states two things about one tuple · r(A, B) is listed both holds and does-not-hold` (siblings: `role id "A" is declared twice` · `type "r" is declared with arity 2 and arity 3`). ⚠ *Cast-level scope is the designer's ruling; one line is with the researcher to confirm — build it cast-level; a tuple-level answer is a one-line change.*
- **CLOSURE and ARITY breaks are LOCAL → TAKEN AND MARKED, never refused** (`carry what the substrate holds · mark what it does not · erase neither`): the marks are COUNTS and NAMES, never verdicts — `relation 12: "throughput" is not among your roles` · `2 tuples with the wrong arity`.
- ⛔ **THE DEVICE NEVER GRADES.** No tick, no *valid*, no *ok*, no green, ever. **The five promises are its ACCEPTANCE TEST, discharged by behaviour:** never fill `label` from the file · never write `identification` · read `relations` and nothing else · unlisted tuples UNRECORDED (never *does-not-hold*) · **load state PER CORNER — corner A stays taken when B's load fails.** A witness pins each.
- Per-corner: writes `updateSelectedVertexData({ cast })` (`geometryStore.ts:203`), nothing else; a `castLoader.ts` NOT_FROZEN while only components/store import it.

# (iii) SURFACE 1 — the gesture (hers, ruled)
`load cast… (.cast.json)` inside `EDIT SELECTED PACKET` beside `Save packet` (`VertexPacketEditor.tsx`) — the extension in the label is the module's own idiom; the GRAIN is carried by position (the section's heading). **The Ambo's gesture line gains her clause verbatim: `a corner takes a concept-space from the packets tab`** (discoverability: five acts deep). ⛔ **The `Custom JSON` field must not read as a sibling of the load** — a person with a cast will paste it into the blob; the difference (load a FILE vs paste TEXT) must be visible in the layout, not only in the verb. **Her eye rules the final placement; build it as she sited it.**

# (iv) SURFACE 2 — the card reads a held cast (hers, ruled)
- **THREE STATES:** no cast → **NO ROW** (a true absence — never `n-a`, never a placeholder) · a cast of nothing (`roles: []`) → `a cast of nothing — no roles` · a cast with content → **`taken — N roles · M relation-types · K relations · A axioms carried, never evaluated · warrant carried, never read`**.
- ⛔ **ONE number for relations, counting BOTH homes** — every key of `ConceptRole.types` is an arity-1 relation; `relations.length` alone UNDERCOUNTS. The two homes are the type's artefact, never shown as the person's split.
- ★★ **THE TERM-ORDER COUNT, per relation-type: `r · 3 tuples · none reversed` · `r · 6 tuples · every one reversed` · `r · 5 tuples · 2 reversed`** — *term order is content* (a directed triangle has `|Aut| = 3`, an undirected one 6; the type has no `symmetric` field and needs none; this line is the only place a person who meant symmetric can see they cast directed). **No verdict, no suggestion — a count.** Arity > 2 generalises to orderings per tuple; binary is the case that occurs first.
- **Two registers:** the corner's `label` (the person's) and the cast's role labels (the caster's), side by side, each marked as whose; **a role with no `label` shows its `id` AS AN ADDRESS, marked as one — never as a name** (the ordinary case under thin casts).
- **`UNKNOWN` shown where it was written + one count (`3 types marked unknown`); OMISSION SILENT.**

# (v) FIXTURES — from the record, so every clause can fail
`.cast.json` for **the T cell** (`first_face_and_tower.py:29–35`, transcribed — the mold-shaped profile, three-valued, one 3-place relation) and **Arman's triangle** (`thin_cast_readings.py` — 3 roles, one binary type, 3 tuples — the thin cast; `|Aut|` 3 directed, and the orderings line reads `none reversed`) · **a contradictory file** (one tuple both polarities — refusal 2 names the line) · **a non-cast file** (refusal 1) · **a closure-broken file** (one dangling term — taken and marked, the other tuples present). **LAW 24 on each refusal and each mark.** Sweep + tsc; the drive family's d8 leg if it loads universes (say whether triggered).
⛔ **Boundaries:** no `J` written anywhere · no surface-3 readings (`|Aut|`, candidates, the meet — hers, unspent) · no grading · nothing of the camera lane. **Report per §7 — WHAT YOU SAW must show a corner taking the triangle and the card's lines at your eye. ECHO `C-6c`.**


---
