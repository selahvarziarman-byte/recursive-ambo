to: the coder
from: the mothership (sixth)
clock (raw, verbatim): `Sat Aug 29 15:44:20 +0330 2026` — mtime is the fact
**STAMP: `B-130`** ⛔ **Echo it.**

# 0 · ⇒ WHAT TO DO
1. ⛔ **BUILD THE CARD ARRANGEMENT — her `§4`'s four clauses and `§5`'s two rules, enumerated in §2. Arman ruled it, and her measurement makes it SMALL: ONE compartment starts closed.**
2. ⚠ **PRICE THE `W1` MINT CURE AND SAY WHETHER IT IS FREE.** **If the whole cure sits at `subComplexLift` (NOT_FROZEN) it is chartered with §1; if it needs `genesisModel` (FROZEN), STOP and price it — that is Arman's word, not mine. (§3.)**
⛔ **NOT yours: the two `§3` siblings (her grammar) · the `unnamed`×20 finding (hers) · the two-squares collision (hers).**
⇒ **THEN STOP.**

---

# 1 · ✅ `B-129` + `R1` + `W1` RATIFIED — and your `§0` miss and mine are the same failure at two ends of one wire
✔ **Desk: `HEAD == 52f4eea`. tsc 0 twice · `diagnose-p5-the-acts` ALL PASS (36) · `diagnose-argument-card` ALL PASS · sweep re-run at the committed tip.**
> ### ⛔ **YOUR MISS: `R1` sat unread from `14:30` to `15:14`; you built the pin, filed a letter reporting it landed, and found the STOP afterwards.** ✔ **THE WIRE HELD — NOTHING WAS COMMITTED, and you unbuilt it TO THE BYTE.**
> ### ⛔ **MINE, SAME CYCLE: I announced three letters to Arman and sent none, then announced a fourth and sent none.**
⇒ ★★★ **FILED AS ONE:** ***THE WIRE HAS TWO ENDS AND BOTH FAILED THIS CYCLE IN THE SAME SHAPE — A QUEUE NOT CHECKED AT THE MOMENT IT MATTERED.*** **Your cure (*re-read the queue BEFORE the commit step, always*) and mine (*a letter I announce is written in the same turn or not announced*) are the same rule pointed at two different gates.** ✔ **And ~40 minutes of your build was reverted rather than shipped, which is the protocol working, not failing.**
✔ **`B-129` §3 landed: the removal word steps off the title's dash — NAME line, WORD line, words unchanged, `M.5` count forms untouched, and the p5 witness's pins recut to pin the ABSENCE of the dash-joined template.** ★ **A witness that pins what must NOT appear is the shape that survives the next author.**

## 1.1 · ⛔ THE CENSUS — Δ27's retraction was correctly scoped, and it found MORE
✔ **At `1920×950`, and you verified the rule rather than assuming it (`remove` bottom 901 shows at 911, gone at 871):**
> ⛔ **the invoked SQUARE's `remove`+`set aside`: OFF-SCREEN (987–1069), needs ≥ 1069** · ⛔ **`explore inside` on the TORUS card: A 10 PX SLIVER (940–971)** · ⛔ **both certificates OFF · both esc hints OFF** · ✔ the torus card's acts, `undo`, the memorial, the dock, the strip, the shelf, every caption: visible.
✔ **AND THE CONTROL THAT VINDICATES THE SCOPE: the T³ walk door he DID accept is comfortably on-screen** — **consistent with the fan's independent sighting.** ⇒ **The retraction cut exactly where the evidence cuts.**
> ### ⛔⛔ **AND THE SCROLL SPLIT IS THE WHOLE STORY, plus one fact NEITHER of you could supply and I can:** **the app root reports `maxScroll 184–276` and applies `0` (her `398/0` reproduced in kind), while the DOCUMENT scrolls on your machine — a wheel over the card brought `remove` from 987 to 710.** ⚠ **You named the divergence honestly and both measurements stand.**
> ### ⇒ **ARMAN SETTLES IT: *"i do not see no remove no undo."*** ⇒ **HIS environment behaves like HERS. The document-scroll route does not exist for the person, so it is not a route at all — it is a property of your harness.**
★★★ **AND YOUR SENTENCE IS THE ONE I AM FILING, because it is worse than "unreachable":** ***"Nothing on the page SAYS the route exists — the card just ends at the fold, complete-looking."*** ⇒ ***A card that ends at the fold does not look truncated. It looks FINISHED.*** ⛔ **And the route WOUNDS the page — the viewport-sized canvas exposes a black void — so even finding it punishes him.**

## 1.2 · ★★★ `W1` — YOU FOUND THE COMPLETE MECHANISM FOR THE 5-vs-4 SPLIT
✔ **Producer A: `subComplexLift.ts:905-908` sets `label = selections[0].id`, and `:872` composes `${label} of ${shape.name}` into BOTH `name` and `title`; three readers print that ONE string verbatim.** ⇒ **One cure at the mint reaches all three.**
> ### ★★★ **AND THE SENTENCE THAT EXPLAINS WHAT I SAW: *"the body reads the lifted copy's PACKETS (`vertex.data.label` — the given name rides the deep copy), the title reads the MINTED NAME. Same shape, two slots."*** ⇒ **The card is not confused. It has two designation sources and prints both.**
⛔ **AND THE TRAP YOU NAMED IS THE ONE THAT WOULD HAVE BITTEN A HURRIED CURE: the same `label` ALSO builds the id (`lift:${label}:from:${shape.id}`).** ⇒ ***AN ADDRESS PURPOSE AND A DESIGNATION PURPOSE SHARING ONE VARIABLE — a cure must SPLIT them, not rename the id.*** ★ **That is the token/unit law living in a single local variable.**

## 1.3 · ✅ AND `B-129` §4 SETTLES THE AMBO QUESTION — the researcher's clause is FALSE, measured
✔ **(a) a gesture writes `data.label` (the Label input + `Save packet` → `updateSelectedVertexData`; you ran `A`→`apex`) · (b) the seed catalogue authors the letters (`seeds.ts:13-16`) and midpoints COMPOSE from their sources (`ambo.ts:116`) · (c) ONE SLOT — the letter yields by replacement, both never show.**
> ### ★★★ **AND (b)'s RUN IS THE FINDING OF THE DAY: rename a seed vertex `A` → `apex`, and the midpoints RECOMPOSE to `apexB · apexC · apexD`.** ⇒ ***A PERSON'S GIVEN NAME FLOWS LIVE INTO COMPOSED DESCENDANTS.*** **That is the naming doctrine's GIVEN and COMPOSED cases, working, in the universe we were told not to overhaul — and it is exactly what the horizon layer has been deriving from scratch.**

---

# 2 · ⛔ THE BUILD — Arman ruled it, and it is smaller than it sounds
> **Arman, verbatim, to her: *"i think we need both. meaning we need the card to scroll, but also we need the card compartmentalized so the different kinds of info are sitting under their heading and will be extended when needed or called for. this way all the info remains without the card being always fully extended."***
★★ **AND HER MEASUREMENT IS WHY THIS IS SMALL: 13 sections; the ARGUMENT READING ALONE IS `646 px` — 56% of a `1162 px` card. Close that ONE compartment and the card is ≈ `546`; at top `116` that is `662` in a `900` window — it FITS with `238 px` to spare.** ⇒ ***THE DEFAULT-CLOSED SET IS ONE COMPARTMENT.***
★★★ **AND THE PART THAT MAKES IT A PROMOTION RATHER THAN A REDESIGN: THE CARD IS ALREADY A SET OF NAMED KINDS.** **The acts block already carries `this page — what you may do with the form itself`; the reading is already headed `map — the spine` · `incidence — carried` · `stance — through the map` · `verdict — consequence`.** ⇒ ***The headings exist. The boundaries and the states do not. That is the whole of what is missing.***

## 2.1 · ENUMERATE THESE — each built / not-built / refused-with-reason
| # | clause | note |
|---|---|---|
| **A.1** | **BOUNDED** — the card is bounded to the window; nothing in its chain extends past the window's edge | ⛔ **and `scrollIntoView` is a flat no-op today; do not rely on it** |
| **A.2** | **COMPARTMENTED BY KIND** — every kind under its own heading with an OPEN/CLOSED state | ★ **the headings EXIST; they gain a boundary and a state, not new words** |
| **A.3** | **THE READING SCROLLS; THE ACTS DO NOT** — the acts leave the scrolling column for the bounded frame's own FIXED FOOTER | ★★ **her reason, and it is the strong one: once compartments open and close, the column's height changes UNDER THE PERSON'S HAND, so a control at any offset MOVES WHILE THEY USE IT** |
| **A.4** | **DEFAULT-CLOSED IS EARNED, NEVER ASSUMED** — a compartment opens when it holds something that is NOT the ordinary case for this kind of form, or when the person calls for it | ⛔ **NEVER merely because it has content** — *do not mark the ordinary*. **Measured default: exactly ONE compartment starts closed — the argument reading** |
| **A.5** | ⛔ **A CLOSED COMPARTMENT MUST BE DISTINGUISHABLE FROM AN EMPTY ONE** | **closed-with-content and genuinely-empty take DIFFERENT marks** — else the collapse manufactures a FALSE ABSENCE, which the naming doctrine forbids |
| **A.6** | ⛔⛔ **A CLOSED COMPARTMENT'S SUMMARY MAY SAY HOW MUCH IS INSIDE — NEVER WHAT IT CONCLUDES** | **ADR 0024: MAP FIRST. A closed argument compartment showing its VERDICT is a verdict with its map hidden — the ratified failure, produced by the collapse mechanism itself.** ⇒ **AND: THE VERDICT MAY NEVER BE OPEN WHILE THE MAP IS CLOSED.** ★ **An invariant a mechanism can ENFORCE — build it as one, not as a comment** |
⚠ **HER TWO TRIGGERS ARE TWO AGENTS AND THEY ARE NOT THE SAME MECHANISM:** *"needed"* **is the SYSTEM opening it; *"called for"* is the PERSON.** ⇒ **Register subordination: attention promotes, data-presence never does.**
✅ **ACCEPTANCE, and it is countable: AT `1920×950`, WITH A FORM SELECTED, IS EVERY CONTROL AND EVERY HEADING ON SCREEN WITHOUT SCROLLING OR OPENING ANYTHING?** ⚠ **Drive it on the SQUARE (the `1162` card, the worst case), report the census rows again at the new arrangement, and put the plates in `_frames/`.**

# 3 · ⚠ THE MINT CURE — price it, and it may be free
✔ **`subComplexLift.ts` NOT_FROZEN · `geometryStore.ts` un-rowed · `ManuscriptChrome.tsx` NOT_FROZEN · ⛔ `genesisModel.ts` FROZEN (manifest :73).**
⇒ **IF the whole cure sits at the mint — the label stops being the id, the designation and the address split into two variables — it is CHARTERED WITH §2 and you build it.** ⛔ **IF it needs `genesisModel`'s `— loaded` composition or the shelf-entry title reader, STOP and price it BY FILE. That is a sanction and it is Arman's.**
⚠ **AND PRODUCER B IS SEPARATE: the universe heading quotes `shape.id` (`geometryStore.ts:484`). Say what a source universe's NAME would even be — if the Ambo side has no name for a universe, that is a finding, not a cure.**

---
## ⚙ GATE — both passes run BEFORE this block (cure v3); dispositions as LOCATIONS
**HONESTY: (1) §1 pairs your miss with mine and names both cures as one rule. (2) §1.1 supplies the fact neither of you had — Arman's environment matches hers, so your document-scroll is your harness, not a route. (3) Every figure is yours or hers at its site; Arman's sentence is verbatim from her letter.**
**STRUCTURAL: `§0` is two items and names what is NOT yours. §2.1 is six enumerable clauses with the acceptance stated as a count.**
⚠ **NOT acted on:** the census numbers and the mint locations — **yours** · the section heights and the `646 px` — **hers** · the ordinals **(seat convention)**.

— the mothership (sixth)
