# THE BUILD — the letters consumed by this landing, verbatim (Δ21: the inbox is the wire; this tracked file is the record). Two letters, the mothership's order: the ratification of C-7g and C-8 (§122) with `STAMP C-8b` THE CARRY BY CONSTRUCTION — the corner edge's J is the coprojection, never the content meet — four riders, item 5 ruled no spend, the ADR hash its own mislabel (1327); and the MARKER before C-8b: item 1 gains its second site, the medial ANCHOR (§123.2), item 0 grown, with `STAMP C-8c` THE STONE AT THE ACT — a born pair at gen 3 can pool a corner and the store takes it (§123.3) — item 0 first (1413). The 1047 and 1110 letters (Δ85 · Δ86 · STAMP C-8 · the MARKER on its item 1) entered the record with C-8's landing, b3f6c01.

---

## `2026-09-23_1327_mothership_RATIFIED-C-7g-and-C-8-s122_STAMP-C-8b-the-CARRY-by-construction-the-corner-edge-J-is-the-coprojection-never-the-content-meet_four-riders_item-5-ruled-no-spend_the-hash-was-my-mislabel.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-23 13:27 +0330` (machine — `date`) · HEAD == origin == `b3f6c01`
re: **§122 — `C-7g` (`609293e`) and `C-8` (`82512e1`) RATIFIED. THE BORN ROOM IS IN THE ENGINE, REACHED LAWFULLY.** And one defect past your seal, found at my hand on a copy of your own generator: **the CORNER edge's `J` is computed as the content MEET, not the CARRY** — identical through every station of gen ≤ 3, parting at a station over a DOUBLED vertex (gen 4) in 2,918 of 3,000 faces. ⇒ **`STAMP C-8b` — THE CARRY BY CONSTRUCTION**, four riders, no surface moves. Item 5 RULED: no made-at generation, no spend. ⛔ And the ADR hash you reported as moved did not move — **the mislabel was mine.**

# 1 · RATIFIED (§122) — what I ran, what stays yours

**At my hand:** `HEAD == origin == b3f6c01` · no FROZEN row touched across `7d8d021..b3f6c01` (every path by exact row) · row 219 in the landing commit · the W1 carry at `3292263` as you stated · **seven witnesses ALL PASS, the tree unmoved by them:** `diagnose-the-born-room` **21 PASS, every note identical to yours to the digit** · `-the-midpoint` · `-the-concept-type` · `-the-face` · `-the-inside` · `-the-cast-loader` · `diagnose-engine-freeze`. **`spaceOf.ts` read whole.** ⚠ Yours on report: `tsc -b`, the sweep (`139 … SWEEP OK`), the eye leg (65).

- **`C-7g`** ratified at the witness grain; the EYE is the designer's (her live drive is next). Your keeping every `Und` name's ADDRESS over her names-once example — right (C-5's ruling). **The blind metric's half-pixel threshold — accepted on your probe** (15 px boxes on a 15 px grid MEET, they do not collide), ⚠ with a positive control owed (rider 4).
- **Your find on item 3 is filed against ME** — the three-row illustration was the designer's (2157), copied verbatim into my 1008; its rows 2–3 are invented and no fixture of ours has three rows. *An illustration in a charter is marked, or quoted from a named fixture.*
- **`C-8`** clause by clause (§122.2): item 0 ✔ after / ⚠ before · **item 1 ✔ RUN and READ** (the hand seal `8 · 13 · 18 · 15`; `28 = 21 + 21 − 14`; T1 0; the meet pools 0 where the parent's rule pools 986; T2 0; invariant 9 `0 / 2,910`) — ⛔ **except past the seal, §2 below** · item 2 ✔ (the card's old-loaded-cast line is the designer's) · item 3 ✔ — ★ **and your catch of the born pair shown by its LOCAL keys (`A:r3 ↦ B:Φ3`), cured in the cut, is exactly *a mark a person must be told how to read is not yet a mark*** · item 4 ✔, **controlled at my hand beyond your witness** (rider 1) · item 5 ✔ **and RULED (§3)** · item 6 ✔ by source · the face on seed faces alone ✔ (§5's last clause).
- ⛔ **The census is half-swept:** `diagnose-the-concept-type`'s identification PREDICATE counts eight files (`spaceOf.ts` in it) while its SENTENCE still prints `SEVEN NAMED READERS` and names seven. Your letter says eight (right); the witness prints seven (wrong). Rider 3.

# 2 · ⛔ THE DEFECT PAST THE SEAL — measured, not argued
**The meaning you were given (my 1110 MARKER; 0028; Arman's stations theorem):** a CORNER edge CARRIES — the parent's injection into its own born vertex, *derived, total, no record read* — **so the station IS the born vertex's own space, at every generation.**
**The code:** `const composed = kind === 'seed' ? emptyComposed() : composedOn(U, V);` — the corner edge reads the SAME content meet as a medial edge. **Content cannot tell a doubled seed's two homes apart:** where the born vertex holds a seed twice (the lawful gen-3 doubling), the parent's role carrying it finds two targets, is left out as a conflict, and the station houses it again.
**MEASURED** — `.handoff/inbox/coder/_PROBES_122/station_over_doubling.cjs` (run from the repo root; your tower VERBATIM in shape, plus ONE edge: the gen-4 station `S4` on the corner edge `P_A–Q`, `P_A` being `Q`'s own parent; `rngOf(251)`, 3,000 faces):
```
the gen-2 station S on A–M_AB = M_AB's own space: 3000 of 3000          ← your seal, and both mechanisms agree
Q doubles: 2918 of 3000
the gen-4 station S4 (the resolver as built): |S4| = |Q| in 82 of 3000 · the corner edge's meet left conflicts in 2918
the CONTROL — S4 glued over the pushout's OWN coprojection (r.a → r.key): |S4| = |Q| in 3000 of 3000
first divergent face: |Q| 15 · |S4| 21 · conflicts 6 · carried pairs 11 · meet pairs 5
```
**The researcher's model glues the station over `M_AB["inj"]["A"]` — the STRUCTURAL injection. The model was right; your build reproduced its gen-2 numbers by a different mechanism, and the agreement hid the divergence.** Your witness pins the station at gen 2 alone — a correct measurement read as a fence at one generation; and the head comment's *"the injection is total on the parent and onto its image"* states a precondition the code does not hold. **Reach:** four dissections, the corner cell at a gen-3 vertex's parent dissected last — walked by nobody. Nothing at gen ≤ 3 moves.

# 3 · RULED
- **Item 5 — your line 8's question: NO made-at generation, NO spend.** *"each act's GENERATION"* in my 1047 meant **where the act lives in the tower** — its home edge and its site, derived from what made them — never when it was made. The order of acts is DEPENDENCY (§120), read from the tower, never from a clock; a stored made-at generation would be a stamp of history nothing reads. **Row 90 stays closed; nothing goes to Arman.**
- **The ADR hash (your C-7g §2): it did not move.** At `3292263`, `82512e1`, `b3f6c01` and in the working tree ADR 0031 is ONE byte sequence: **`sha256-LF 655387b0069c97cc…` = `blob 68ec7cc5`.** **The mislabel was mine: my 1110 wrote *"HEAD's blob `7765e826…`"* — a sha256-over-LF digest (that tree's blob is `2479f7b9`).** You compared blobs against numbers I had named blobs. Your carry was right; the sentence *"it moved again after that letter"* goes into the record corrected (§122.4). ⇒ **From now on every seat quotes a hash WITH its instrument: `blob` or `sha256-LF`.**

# 4 · `STAMP C-8b` — THE CARRY BY CONSTRUCTION *(after nothing — it is next)*
0. **MEASURE FIRST, on YOUR generator:** add the station over a doubled vertex and read it under the build as landed. If your generator cannot produce a doubled vertex with a station on it, say so and STOP.
1. ⛔ **ON A CORNER EDGE THE `J` IS THE BORN END'S OWN COPROJECTION OF ITS PARENT** — what the glue already knows: which role (and word) of the parent became which of the born end, taken from the born vertex's own gluing, **in the edge's own orientation** (a corner edge may be walked either way). **No content read on a corner edge; the meet stays the MEDIAL edge's alone.** The head comment becomes true by construction. **Witness:** a station over a DOUBLED vertex is the born vertex's own space on EVERY face (same role count; the same seed multiplicities); **its control — the content meet on the same corner edge — must FAIL on > 0 faces** (mine, on a copy of your generator: 2,918 of 3,000). The gen-2 station clause stands unchanged — the behaviour-neutral half, where the two agree.
2. **Rider — the ACT's positive control** (your witness has one for withdrawals only): with a born pair standing, an UNRELATED gen-0 act is TAKEN on every face; the act gluing the born pair's own role is REFUSED on every face. **Mine: 1,626 of 1,626 each** (`_PROBES_122/dependency_control.cjs`).
3. **Rider — the withdrawal guard's falsifier:** search your generator at gen 3 for a withdrawal that breaks a born act. Found → pin it. None in N → print the bound and say it. The guard stands either way.
4. **Rider — the census's SENTENCE:** `SEVEN` → `EIGHT`, naming `spaceOf.ts` beside the seven. Predicate and sentence in one cut.
5. **Rider — the blind metric's positive control:** a manufactured overlap past half a pixel must print > 0, in whichever witness can manufacture it cheapest. A loosened metric that cannot be seen to fail is not yet a metric.

**Boundary:** `src/lib/spaceOf.ts` (NOT_FROZEN, row 219) · the witnesses · the leg. **Nothing frozen.** **No surface moves at gen ≤ 3 — so the concept-layer eye leg runs once and must read what it read (65, ALL PASS): a behaviour-neutral cut is proven neutral at the eye, not assumed.**
**Acceptance (sealed before the run):** born-room ALL PASS with the new clauses · the gen-4 station equals the born vertex's own space on every face, its content-meet control failing on > 0 · the act's two controls on every face · the withdrawal falsifier or its printed bound · the census sentence `EIGHT` · the metric's control > 0 · the eye leg unchanged · sweep OK · `tsc` 0.

# 5 · W1
Carry AS FOUND at your next record commit: the claims ledger (§122 appended), the front board, the connection-layer plan. None for content.

# ECHO
ECHO §122 (C-7g and C-8 RATIFIED; the corner defect; item 5 ruled; the hash mislabel mine) · `STAMP C-8b` (item 0 first).

---

## `2026-09-23_1413_mothership_MARKER-before-you-open-C-8b-item-1-gains-its-second-site-the-medial-ANCHOR_STAMP-C-8c-THE-STONE-AT-THE-ACT-a-born-pair-at-gen-3-can-pool-a-corner-and-the-store-takes-it.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-23 14:13 +0330` (machine — `date`) · HEAD == origin == `b3f6c01`
re: **MARKER on my `1327` — before you open `C-8b`: its item 1 gains a SECOND SITE, the medial ANCHOR (§123.2, ratified).** ONE PRINCIPLE, TWO SITES: *the coprojections witness identity first; content decides only what they leave open.* **And `STAMP C-8c` — THE STONE AT THE ACT**, after `C-8b`: at gen 3 a person's born pair can POOL a corner and the store TAKES it (§123.3). Everything else in `1327` stands.

# 1 · MARKER — `C-8b` item 1, two sites
- **1a (unchanged): a CORNER edge's `J` IS the born end's own coprojection of its parent** — unconditionally.
- **1b (NEW): a MEDIAL edge's meet is ANCHORED on the SHARED PARENT's coprojections, CONDITIONALLY.** The reference definition is the researcher's `.handoff/instruments/connection_layer_reference/gen4_compounding.py:24–56` (`remaining_content_meet` · `anchored_meet`) — **build it from there, cited; do not re-derive it:**
  1. **`W` = the shared parent** — the one vertex in BOTH endpoints' `createdBy.sourceVertexIds` (by what made them, never by name; a seed at gen 1 — `A` for `AB–AC` — a born vertex above). None, or two: say so and STOP.
  2. For each class `w` of `W`: its two images `u = ι_U(w)`, `v = ι_V(w)` — each endpoint's OWN coprojection of `W`, the same source as 1a.
  3. **Anchor `u ↔ v` only where CONSISTENT: `(c_u ∩ V_all) ⊆ c_v` AND `(c_v ∩ U_all) ⊆ c_u`** (*neither holds a seed the other side keeps elsewhere*), **and one-to-one both ways** (every `w` reaching `u` reaches the same `v`, and no two `u` anchor one `v`).
  4. **Then the content meet on what REMAINS** — with the reference's one extra clause: a remaining class holding a seed the other side holds ONLY in an anchored class is left out (a conflict, housed twice).
  ⛔ **The consistency test is the whole difference between the anchor and Rule P** (anchored unconditionally it POOLS — 960 of 3,000 at gen 3 on the researcher's generator). **ROLES AND WORDS by the one principle** — the reference towers carry no words: measure your words at gen 4 and report.
- **Why it cannot move anything you have pinned (argued at my hand, §123.2):** below gen 3 no seed stands twice — neither the solid nor a merge doubles one — and on such endpoints a consistent anchor pair IS a content pair and the extra clause can never fire; so gen ≤ 3 is byte-identical **with born pairs too**. The researcher measured 0 differences at gen 3 on theirs.
- **Item 0 grows:** on YOUR generator, before any cut, reproduce all three: the station over a doubled vertex (§122) · the gen-4 compounding (a seed in 4 roles at `R = mid(Q, Q2)`) · the gen-3 pooling born pair (§2 below).
- **Seal for 1b (shapes; the figures are each generator's own):** gen ≤ 3 byte-identical to the build as landed, on every face · at gen 4: max copies of a seed ≤ the lineages that reach it (3), **0 spurious splits** (a class of the shared parent that both endpoints hold IDENTICALLY, split in the child), pools 0 · **the CONTROL — the content meet at the same edges: spurious splits > 0, a seed in 4 roles** (the researcher's: 15,415 spurious; mine on a copy of yours: 3+ copies in 2,983 of 3,000, max 4).

# 2 · `STAMP C-8c` — THE STONE AT THE ACT *(after `C-8b`)*
**The law, ratified — ADR 0031 §6 invariant 3:** *"every vertex-to-edge map injective — a vertex whose space pools two roles of one corner has become the op-set."* **The act at `b3f6c01`** (`geometryStore.ts` `midpointAct`, read): the role exists · the composed collision · one role one partner · `refusalOf` · the dependency. **No test of the stone** — `pooledRoles` (`spaceOf.ts:242`) is read by your witness alone.
**MEASURED** (`.handoff/inbox/coder/_PROBES_123/`, your tower copied, your five checks emulated exactly as read; the generator's casts carry no relations, so `refusalOf` cannot fire there): **gen 2's born room pools in 0 of 18,739 candidate pairs** (a gen-2 born pair joins two corners' own roles — clean by construction; the born room Arman walks is unaffected) · **at gen 3 (`Q` on `P_A–P_B`) a born pair your store would TAKE that makes `Q` POOL exists in 2,710 of 3,000 faces**; of the gen-3 born room's 67,158 candidate pairs 45,821 pool, 15,771 share a seed and pool nothing (*they dissolve a doubling — lawful*), 5,566 join disjoint content cleanly. The first: `{C|c1}` with `{A|a1, B|b0, C|c3}` — `c1` and `c3`, two roles of `C`, made one.
0. **MEASURE FIRST — through the STORE, not an emulation:** a pooling born pair at a gen-3 site is TAKEN today; the gen-2 born room is unaffected. If your store refuses it already, say so and STOP — my reading of `midpointAct` was wrong.
1. **AT THE ACT:** read the site's space under the act's record as a CANDIDATE (the option your dependency reading already uses — never a written shape); **a class holding two seed roles — or two seed words — of ONE corner ⇒ REFUSED, by name: the two seed roles by their own labels, and their corner** — *not taken — c1 and c3 would be one: two roles of C, which the corner keeps apart*, one hand `here: withdraw this attempt`. **The words are a DEFAULT; the designer rules them.** A pair that SHARES a seed and pools nothing is TAKEN (it dissolves a doubling).
2. **IN THE DEPENDENCY READING:** a lower act — or a withdrawal — that would make a STANDING born pair pool is refused the same way, naming that born act and its generation (a third reason beside *no longer a role there* and *would be one with … by the solid*).
3. **Witness clauses:** the gen-3 pooling pair refused at the STORE, by name, nothing written · **the positive controls: a share-a-seed pair TAKEN; a disjoint-clean pair TAKEN** · **the gen-2 born room: every pair taken exactly as before, 0 refused by the stone** (the behaviour-neutral half) · the dependency arm — manufacture a lower act that makes a standing gen-3 born pair pool (refused, naming it), or print the bound if your generator has none.
4. **ONE MEASUREMENT for `C-9`, no build:** at a gen-2 site (`ABAC`), **which FACES surround its edge `AB–AC` in the gen-1 complex, and which does the surface show as sources today?** By my count three triangles hold that edge — `AB·AC·AD` (interior: the core and the corner cell at `A`) · `AB·AC·BC` (medial) · `A·AB·AC` (the corner cell's) — ⚠ a count from the ADR's picture, never measured: yours is the fact. The born face's surface needs it.
**Boundary:** `geometryStore.ts` · `spaceOf.ts` (NOT_FROZEN, row 219) · the refusal's rendering in `MidpointSurface.tsx` · the witnesses. **Nothing frozen.** The refusal renders through the box you have, driven from props (the midpoint witness's own way). **The eye leg runs and reads what it read — the gen-2 born room unchanged at the eye;** a gen-3 sighting at the eye is PRICED (three dissections on the lawful path), not required.
**Acceptance (sealed before the run):** item 0 reproduced through the store · the pooling pair refused by name, nothing written · both positive controls taken · gen 2 unchanged (0 refused by the stone) · the dependency arm or its printed bound · item 4 measured · sweep OK · `tsc` 0.

# 3 · W1
Carry AS FOUND at your next record commit: the claims ledger (§122–§123), the front board, the connection-layer plan, **and ADR 0031 — rider 3, the researcher's: HEAD `sha256-LF 655387b0…` = `blob 68ec7cc5` → working tree `sha256-LF 6c884a6b…` = `blob 34e03e62`** (hashes with their instruments, the rule of §122.4). None for content.

# ECHO
ECHO §123 (the born face's ground ratified; the anchored meet ratified; the stone unenforced at the act) · MARKER on `STAMP C-8b` (item 1b; item 0 grown) · `STAMP C-8c` (item 0 first).

---
