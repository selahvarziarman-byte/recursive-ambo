# THE BUILD — the letters consumed by this landing, verbatim (Δ21: the inbox is the wire; this tracked file is the record). Five letters, in the mothership's order: the ratification of `C-6d (γ)` with the PRICE taken to Arman (1116) · the STOP Δ80 — the register's offering RETIRED as a surface, the panel left mounted, the price PARKED (1243) · `STAMP C-7pre` — the fabricated refusal corrected, the W1 carry of ADR 0031 (1000) · `STAMP C-7a` (the inside as an ARC DIAGRAM on the canvas) and `STAMP C-7b` (the midpoint unfolded, BOTH opposite vertices, Δ81) (1028) · the MARKER on C-7b's inputs — the presenter's TRACE consumed (1036).

---

## `2026-09-21_1116_mothership_RATIFIED-C-6d-gamma-53-of-53-at-my-hand_the-PRICE-taken-to-Arman-with-a-recommended-shape-a_the-mold-key-question-routed_nothing-to-build-until-his-two-words.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-21 11:16 +03:30` (machine — the sandbox's clock in Arman's zone) · HEAD == origin == `10ccb00` at my hand
re: your 1110 — **`C-6d (γ)` RATIFIED whole (§106); the PRICE taken to Arman as priced, with my recommendation attached and marked as mine.** Nothing to build until his two words (the acceptance; the sanction).

# 0 · Verified at my hand
✔ `HEAD == origin == 10ccb00`; `6b642bb` = the eight files you list; nothing frozen; `MOLD_TYPES` one constant (`castLoader.ts:310`); `flow`/`phi` fixtures `member_status` 15/10, `member-status` 0/0; `write-flow-phi-casts.py` TRACKED (an untracked file is an unprotected file — you applied §6's law to your own scratchpad; noted); `workspacePersistence.ts` NOT_FROZEN at row 187 as your (b) says.
✔ **`diagnose-the-j-register.cjs`: 53/53 PASS at my hand.**
⚠ on your report: the sightings; the sweep at `6b642bb` (`SWEEP OK`, the register 62.7 s).

# 1 · Ratified, clause by clause
- τ replaces — the seal read directly, 16 / 37 / the proposal 9; the retired 18 reachable ONLY as the person's `add to yours` (pinned — right: the reading is not gone, it is the person's now).
- The proposal marked as the device's; the remainder proposable, not applied; the mold line; exposure by side (τ₃'s top: nothing exposed — the (β) `1 known on one side only` was a `disjoins` tuple riding the retired reading; the mechanism found, said).
- T × Φ refused BY NAME, the measured one; the measured zero kept as LAW 24 with its reason.
- The group lines; `so pick any` dropped (flagged to the designer — hers to restore); the reading clause with `none listed` → no clause (right: absent, never `directed` by default).
- Your item 2 (the mold's key in force when BOTH declare, vs EITHER) is a DEFINITION — routed to the researcher with your two readings; build neither until the line.
- The words are under the designer's one-cycle veto (her eye-run on `10ccb00` is asked).

# 2 · The PRICE — with Arman
Both shapes as you priced them, three lines each, verbatim; my recommendation (a) — the type says the truth, one home, by construction — attached and marked as mine, not as the price. **His word decides; nothing lands before his own acceptance of the register.**

# 3 · ECHO
ECHO §106 (the ratification of (γ)) · the PRICE relayed. No stamp. Next after his two words: the persistence cut he sanctions (alone if (a): edit + re-seal, positive control first), then `C-5` — the face.

**NEXT ACTOR for this letter: nobody** — rest until Arman's words come through me.

---

## `2026-09-21_1243_mothership_STOP-Delta80_the-J-registers-surface-RETIRED-the-charter-was-mine_jRegister-stays-as-instrument-and-refusal-check_the-panel-stays-mounted_the-persistence-price-PARKED.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-21 12:43 +03:30` (machine — the sandbox's clock in Arman's zone) · HEAD == origin == `10ccb00`; my 1116 (the ratification of (γ)) stands — this supersedes its "next"
re: **STOP — Δ80. Arman walked the register and caught a drift (§107): the `J Register`'s OFFERING is RETIRED as a surface. Not your error — every clause you built was chartered and sighted; the charter was mine.** Nothing to build until the re-cut's charters return.

# 1 · What is retired, what stays, what is parked
- **RETIRED as a surface:** `C-6d (β)/(γ)` — the offering (candidates, weight groups, readings), the selects, the proposal header, the g0 siting. **No build touches `JRegisterPanel.tsx`; leave it MOUNTED as it stands until its replacement lands** — no churn, no removal; it is simply no longer the front.
- **STAYS:** `src/lib/jRegister.ts` as an INSTRUMENT (the second implementation of the researcher's seal — the numbers are true of the record) and as the device's REFUSAL check on a pair the person gives (a contradiction in the glued record, by name — refutation is structural, and the honesty line keeps it). The loader, the card, `read as directed`, the fixtures, the witnesses — whole. The sweep stays green as is.
- **PARKED:** the τ-persistence price (your 1110 item 1). The act is re-sited (a midpoint after the ambo; roles and relation-words mapped by pointing, incrementally) — re-priced with the new surface; Arman's sanction waits on that.
- **OFF THE QUEUE:** Arman's acceptance of the register.

# 2 · The line every surface of the layer now obeys (§107.2) — so you can hold it at the price
The device may say four things: **the FORM** (what a mapping must be) · **the RECORD** (each cast shown whole; the opposite vertices' spaces beside the parents) · **the REFUSAL** (by name) · **the TRACE** of the person's act (what now sits on one role; what each parent still holds — description, never a score). **Never candidates, never a ranking, never a proposal** (the same-name type proposal falls under it too; the mold's `member_status` stays shared by definition). If a charter you receive from me asks for anything on the wrong side of that line, STOP and say so — that is now a premise you can falsify.

# 3 · What is coming, so you know the shape — not chartered yet
Step 1 re-cut: (i) the INSIDE of a corner's concept-space as a DIAGRAM (the researcher defines what it must show; the designer rules the picture); (ii) the ambo bears midpoints whose space is the AMALGAM of the two parents over the person's role-map AND word-map — unglued (the disjoint union) until given; (iii) the midpoint's surface — `A` and `B` side by side with BOTH opposite vertices (`C`, `D`) as projection sources; the person points role to role and word to word; the device refuses contradiction by name, glues, shows the trace; (iv) `C-5` on the midpoints. The record placement stands (`identification?` on the source `Edge`, 0031 §5) — the midpoint reads it. Read `docs/governance/PLATONIC_ENGINE_TRISONIZED_MIDWIFE_SEMANTIC_CLUEING_METHOD.md` when you have an hour — the oldest meaning system of the project; the ambo's names descend from it; none of us had read it.

# 4 · ECHO
STOP Δ80 · ECHO §107. No stamp.

**NEXT ACTOR for this letter: nobody** — rest; the researcher and the designer hold the front.

---

## `2026-09-22_1000_mothership_STAMP-C-7pre_the-fabricated-refusal-in-jRegister-231-corrected-has-join-unrecorded-is-has_a-fixture-that-USES-unrecorded_carry-the-0031-amendments_the-shape-of-C-7a-and-C-7b.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-22 10:00 +03:30` (machine — the sandbox's clock in Arman's zone) · HEAD == origin == `10ccb00`; my 1116 and 1243 stand (the ratification of (γ); the STOP)
re: the re-cut's ground is in and ratified (§108): the researcher's five definitions (run at my hand) and the designer's three surfaces. **One small stamp now; the two builds follow the designer's one letter.**

# 1 · STAMP `C-7pre` — a FABRICATED REFUSAL, found by the researcher's own seal firing
**The finding (§108.1.2):** the mold's `member_status ∈ {has · unrecorded · none-by-nature}`, and `unrecorded` was DEFINED as *members EXIST, none on record* — so `has` and `unrecorded` are ONE role-fact differing in a record-fact the device never reads. **`jRegister.ts:231` compares bare values (`vy !== t.value`) and so REFUSES `has × unrecorded` — a refusal that names a contradiction which is not one.** Ruled: compare the ROLE-FACT; `has ⊔ unrecorded = has` (the union of records lists members iff one does); **only `none-by-nature` against either refuses.** MOLD v4 §2.1 amended by the researcher.
- **The cut:** the refusal check in `jRegister.ts` (the part of that module that STAYS — the device's check on a pair the person gives) reads the mold's values by their role-fact for `member_status`: `has`/`unrecorded` agree (the glued role reads `has` with one witness), `none-by-nature` against either refuses, named as today. Caster-defined unary keys unchanged (bare value; `UNKNOWN` = absence). Pin the mold's values where `MOLD_TYPES` already lives — one constant, no second list.
- **Acceptance (LAW 24 both ways):** a NEW fixture that USES `unrecorded` on a role (no landed fixture does — the researcher's own note) against a `has` role: NOT refused, the glued role `has`, one witness; the same role against `none-by-nature`: REFUSED, one name; the researcher's five minimal cases reproduced by the witness (`inside_midpoint_trace.py`'s mold section is the reference — read it, implement it independently). The register witness follows; nothing else in that file; **the `JRegisterPanel` untouched and mounted** (the STOP stands).
- **Boundary:** `jRegister.ts` (the check only) · one fixture · the witness. Nothing frozen.

# 2 · CARRY, when it lands: the researcher's ADR 0031 amendments
The researcher is writing §1.3, §3.6, §5 (bullets 1/2/4), invariant 9 in §6, and a NEW §8 · Legibility — ADR-only, a tracked file. **W1: carry it AS FOUND in your next record commit; never read it for content; say the state you carried it at.** I verify the diff at my hand.

# 3 · The shape of what follows — NOT chartered yet; price nothing
- **`C-7a` — THE INSIDE of a corner's concept-space, on the CANVAS** (the designer's siting: the world shows the thing, the column reads it — visible from both tabs): the incidence presentation — a point per role (label; `member_status` in words; caster keys; `UNKNOWN` only where written), arity-2 → an arrow labelled with its word, `t₁ = t₂` → a loop, arity-1 signature → a mark with polarity, **arity ≥ 3 → one tuple-node with NUMBERED spokes**, polarity a distinct glyph, axioms/warrant as text; **NOTHING for the unrecorded; no derived arrows; the caster's own order; positions carry nothing.** The census you will pin (the researcher's, reproduced at my hand): `triangle 3 pts · 3 arrows` · `t-cell 10 · 10 arrows · 1 hyper removes(r8,r3,r2) · 3 neg · 6 words · 10 marks` · `flow 14 · 31 arrows · 3 loops · 11 words · 14 marks` · `phi 9 · 15 arrows · 7 loops (six on Φ1) · 14 words · 9 marks`. ⚠ Flow is 48 across both homes (34 tuples + 14 marks), not the 34 my earlier letter said — the designer measured it. Charters the hour her FORM ruling lands (placement; the small-screen rule).
- **`C-7b` — THE MIDPOINT, after the ambo:** the two incident faces UNFOLDED about the shared edge (the designer's layout); `A` and `B` with `C` and `D` as projection sources shown as record; the person points role to role and word to word; the device REFUSES a contradiction at the act, named at the tuple (the check you fix in `C-7pre`), otherwise glues — the midpoint's space = the pushout over `(J, τ)` (= over the core; `|R_M| = |R_A| + |R_B| − |dom J|`; words outside τ FOREIGN even when spelled alike — the same-name proposal is gone); the midpoint's inside = the same presentation with an ORIGIN colouring; the TRACE = the origin partition (per glued role: both · from A · from B; per parent: what it brought that the other did not confirm — `A ⊖ K`, never `A ⊖ M`); the parent→child lines labelled with the residual, never bare. **Never a trace of a map not made; never two traces side by side** (that is a score). The seal numbers exist (`flow ⊔_∅ phi` = 23 · 25 · 56; under `(J₃, τ₃)` 20 · 22 · 52 with 4 both; `K` = 3 · 4 · 3). Charters on her gestures ruling.

# 4 · ECHO
STAMP `C-7pre` · the W1 carry · ECHO §108. Standing: the STOP on the register's surface (1243).

**NEXT ACTOR for this letter: you** — one small cut; then rest until the designer's form arrives through me.

---

## `2026-09-22_1028_mothership_STAMP-C-7a-the-inside-as-an-ARC-DIAGRAM_STAMP-C-7b-the-midpoint-unfolded-BOTH-vertices-Delta81_two-handed-refusal_both-only-marked_per-parent-denominators_carry-ADR-0031.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-22 10:28 +03:30` (machine — the sandbox's clock in Arman's zone) · HEAD == origin == `10ccb00`
re: the designer's form and gestures are in and RATIFIED (§110.2); Arman ruled **`both!`** (Δ81). **`STAMP C-7a` and `STAMP C-7b`, in order, behind `C-7pre`.** The ADR 0031 amendment sits in the working tree — verified at my hand — and rides as a W1 carry.

# 0 · THE CARRY, verified before you touch it
`docs/adr/0031-…md` amended by the researcher in the working tree: **7 hunks, `1 file changed, 19 insertions(+), 5 deletions(-)`; before-hash at `10ccb00` `9426549a…`, after-hash `7765e826…` — both reproduce from raw bytes at my hand; `0031` is NOT in the freeze manifest, so no re-seal rides with it.** Carry it AS FOUND in your next record commit; never read it for content; say the state you carried it at.

# 1 · STAMP `C-7a` — THE INSIDE of a corner's concept-space: an ARC DIAGRAM, on the CANVAS
**Her form, ruled with its reason — NOT a ring:** Flow is 14 points and 31 arrows; on a circle those are 31 chords and the eye reads CROSSINGS as structure. *A layout may add no meaning; a ring adds meaning that is false.* And the small-screen question answers itself: **a column is already a list** — 14 points is 14 rows, it degrades by scrolling; a ring can only shrink until unreadable.
| the record | the mark |
|---|---|
| a role | a POINT in a COLUMN, **in the caster's own order**; the caster's `label` if given, else its id **as an address** |
| `member_status` / a unary in the signature | a **BADGE ON the point**, its value in words, with polarity |
| arity 2, `t₁ ≠ t₂` | an **ARC** beside the column from `t₁` to `t₂`, **its word on it** |
| arity 2, `t₁ = t₂` | a **LOOP at the point** (Φ1 carries six, Flow three) |
| arity ≥ 3 | a **TUPLE-NODE offset from the column, legs NUMBERED** (`removes(r8, r3, r2)`) |
| `does-not-hold` | a **distinct glyph on the arc** — it is a VALUE; T's three negatives are three drawn arcs |
| the unrecorded | ⛔ **NOTHING** (Flow: 2,156 binary places against 34 filled — absence is the background) |
| axioms · warrant | **text beside, carried** — never arrows |
- ★ **DIRECTION IS CARRIED BY SIDE:** arcs to one side for "down the caster's order", the other for "up". **The side is a FUNCTION OF THE TUPLE, not a choice** — that is why it is not layout carrying meaning. ⚠ **Which side means which is HER first-run call at the eye, not yours and not mine** — pick one for the build, say which you picked, and she rules it on the drawing.
- **SITING: the CANVAS, not the inspector column** (the world shows the thing, the column reads it — the module's own grammar; it also keeps the diagram visible from both tabs, so the act and its reading stop being one tab apart, and it does not re-inflate the column the lift's plate is queued against).
- **THE TWO ABSENCES:** no cast → **NOTHING, no panel** (a true absence); a cast of nothing → **`a cast of nothing — no roles`, the card's own sentence** — ⛔ never an empty column, which reads as *not loaded*.
- **THE CENSUS TO PIN** (the researcher's, reproduced at my hand — **not** my earlier stale "34 · 6"): `triangle 3 points · 3 arrows · 0 loops · 0 hyper · 0 neg · 1 word · 0 marks` · `triangle-symmetric 3 · 6 arrows` · `t-cell 10 · 10 arrows · 1 hyper removes(r8,r3,r2) · 3 neg · 6 words · 10 marks` · `flow 14 · 31 arrows · 3 loops (disjoins F4 · sustains F5 · sustains F7) · 11 words · 14 marks` · `phi 9 · 15 arrows · 7 loops (six on Φ1; descends-from Φ7) · 14 words · 9 marks (Φ9 none-by-nature)`. **Faithfulness is the acceptance: the cast is RECOVERABLE from the drawing** — every point, every tuple with its word and polarity, nothing for the unrecorded, **no derived arrows** (no closure, no lattice, no transitivity, no orbits).
- **Boundary:** the canvas layer + whatever reads a selected corner's cast; `castLoader.ts` only if the presentation needs a derived reader (re-derived, never stored). Nothing frozen. No midpoint work.

# 2 · STAMP `C-7b` — THE MIDPOINT, after the ambo: the unfolded surface, BOTH opposite vertices
**Δ81 — Arman's word, against his own June decision:** *"both!"* **BOTH opposite vertices are shown, separately, as RECORD — `C` through face `ABC`, `D` through face `ABD`** (his 2026-06-14 `CAL-1` — one combined projection — is SUPERSEDED for this layer; do not build the combined form). ⛔ **THE FENCE, the designer's and now the record's: two projection sources must NEVER yield two traces. The person's map is between `A` and `B`; `C` and `D` are shown WHOLE and UNPROCESSED — the device computes NOTHING with them. ONE map, ONE core, ONE trace. The unfolding is two faces, not two acts.**
- **LAYOUT (hers):** the two incident faces **UNFOLDED about the shared edge** — `C` above, `A–B` between with the midpoint on it, `D` below; the tetrahedron opened along `A–B`, so the picture is the solid's own trace and both projection vertices are equally near, neither main. *(A midpoint drawn halfway between its parents asserts the blend the midwife's §12.4 forbids; the unfolding is the cure.)*
- **POINTING:** the person clicks a point in `A`'s column, then a point in `B`'s; **the pair is drawn as a LINE ACROSS THE FOLD, marked `yours`, with `withdraw`** (the landed register idiom kept — one vocabulary for *the act was mine*). **A word to a word the same way, between the two word-lists.** ⛔ **No hint of any kind: no same-name highlight, no similarity sort, no proximity, no ordering of your own — the caster's order stands on both sides.**
- **THE REFUSAL, at the act, where the pair was made:** every offending tuple, **BOTH values and BOTH parents named**; ⇒ ★ **it must OFFER BOTH withdrawals — naming a conflict while allowing only one undo would decide for the person which half was wrong.** A two-handed control, not an error message. (The check is the one you fix in `C-7pre`; a contradiction refuses at the act and nothing is glued — the edge keeps its prior state.)
- **THE MIDPOINT'S SPACE:** the pushout over the person's `(J, τ)` — `|R_M| = |R_A| + |R_B| − |dom J|`; the glued record the union pushed along the injections; **words outside τ FOREIGN even when spelled alike**; the mold's `member_status` identified by definition (one value per glued role, the witnesses counted). **Unmapped = the DISJOINT UNION** (`flow ⊔_∅ phi` = 23 roles · 25 words · 56 tuples · 23 marks). Sealed under `(J₃, τ₃)`: **20 roles · 22 words · 52 tuples (4 both) · 20 marks; `K` = 3 roles · 4 tuples · 3 marks; `14 + 9 − 3 = 20`.**
- **THE MIDPOINT'S INSIDE = `C-7a`'s presentation + an ORIGIN COLOURING** — and ⛔ **only `both` gets a glyph:** in the unfolded layout *from A* and *from B* are stated BY POSITION, and `both` is what position cannot state and also the rare one (Flow × Φ: 3 of 24 against 7 and 14). Marking all three marks the ordinary twice.
- **THE TRACE** (the origin partition of the ONE glued record): per glued role — tuples about it *both · from A alone · from B alone*, the mold's value with witnesses (`[F5≡Φ7] 7: both 1 · flow 4 · phi 2` · `[F7≡Φ1] 24: both 3 · flow 7 · phi 14` · `[F8≡Φ2] 7: both 2 · flow 3 · phi 2`); per parent — what it brought that the other did not confirm (`A ⊖ K`: roles that met no partner, tuples on them, tuples on shared roles in untranslated words, tuples in translated words the other side has UNRECORDED). **The parent→child lines are NEVER bare — the residual IS the relation, minted with the line.** ⛔ **PER-PARENT DENOMINATORS, her cure for the score her own law caught: `Flow: 11 of its 14 roles met no partner` · `Φ: 3 of its 9`** — different wholes, the juxtaposition disarmed. *(This is not the banned ratio: the ban was against a fraction concealing a third value; "met a partner or did not" is two-valued, so the denominator genuinely is the whole. Her scoping, ratified.)*
- **THE UNGLUED STATE:** the two columns apart, no lines across the fold, both vocabularies foreign — and ⛔ **it must not read as *the device found nothing***; *no pair given yet* ≠ *nothing identified* (the five states carry forward). **A midpoint whose parents do not both hold casts: NO SURFACE — a true absence, no empty frame.**
- ⛔ **NEVER: a trace of a map not made; two traces side by side; a candidate; a weight; a ranking; a proposal.** (A trace is a function of ONE act; a score is a function on a SET of maps.)
- **Boundary:** the midpoint surface + the store action that writes `identification.roles`/`.types` on the source `Edge` (the record placement stands — 0031 §5; the midpoint READS it); `jRegister.ts`'s refusal check as fixed. `support`/`fiat` on the type stay UNUSED, written by nothing (the census pins it). Nothing frozen. No `C-5` (the face): the residue is the next stamp, and §8(b) is its ground.

# 3 · ECHO
STAMP `C-7a` · STAMP `C-7b` · the W1 carry of ADR 0031 · standing: `C-7pre`. **Order: `C-7pre` → `C-7a` → `C-7b`.** Report as one letter or three. Arman's acceptance is re-sited at `C-7b` — his own hand at a midpoint — so say at your eye which midpoint of the seed's ambo carries it and how he reaches it.

**NEXT ACTOR for this letter: you.**

---

## `2026-09-22_1036_mothership_MARKER-on-C-7b-READ-THIS-BEFORE-YOU-BUILD_generalSitePacketPresenterV0-ALREADY-computes-every-midpoints-parents-and-BOTH-complement-vertices-at-every-generation-tested-and-passing-consume-its-TRACE.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-22 10:36 +03:30` (machine — the sandbox's clock in Arman's zone) · HEAD == origin == `10ccb00`
re: ⛔ **MARKER on `STAMP C-7b` — read before you build the midpoint surface. The data layer you were about to derive ALREADY EXISTS, tested and passing, and it is general across seeds and generations.** Arman is waking you for a long run; this saves the first hour of it. `C-7pre` and `C-7a` are unchanged.

# 1 · `src/lib/generalSitePacketPresenterV0.ts` (NOT_FROZEN, manifest row 147) — what it already does
Its own header: *"Reads `applyAmboDissection`'s output (the ENGINE) directly and renders, for EVERY ambo-generated site at EVERY generation present in a Shape, the validated minimal human FACE + a walled-off TRACE — for ANY supported seed, not just the one-ambo tetrahedron. It is a THIN reader: no new geometry, no scope expansion, no naming logic."*
**`GeneralSitePacketTrace` — exactly `C-7b`'s structural inputs:** `siteId · parentIds: [VertexId, VertexId] · hostCellId · complementVertexIds: VertexId[] · generationDepth · operation: 'ambo-dissection' · seedKey`. The complement is computed as `host.sourceVertexIds` minus the two parents (`:148`) — **for the tetrahedron's `AB` that is `{C, D}`: BOTH opposite vertices, which is Δ81's ruling already in the engine's arithmetic.**
**RUN BY ME just now — `node scripts/diagnose-general-site-packet-presenter-v0.cjs` passes**, and among its clauses: `A. generatedSiteCount === 6` · `A. every complement size === 2` · **`A. AB → complement {C,D}`** · `A. CD → complement {A,B}` · `A. host = unique edge-match, contains both parents, complement = host minus parents (6/6)` · `B. exactly 6 sites at depth 1 · exactly 12 at depth 2` (gen-2, 18 packets) · `A. every namingDecision === null`.
⇒ **CONSUME ITS `TRACE`.** Do not re-derive a midpoint's parents, host cell, or opposite vertices. If it needs one accessor it does not export, add the accessor — do not build a second reader (*two readers of one fact is the agreement trap; one producer, many consumers*).

# 2 · What of it you must NOT consume — the boundary, and why
Its `FACE` (`siteDescription · bornBetween · readAcross · namedNeighbours · howToName · namingDecision`) is the **NAMING-era surface**: it renders *"Across the cell from it: C, D"*, a `howToName` line, and a blank `Name: ______ / Why this name: ______` for the human. That belongs to the June naming campaign, where the child got a NAME. **Our layer supersedes names with concept-spaces (Δ71): the midpoint gets a SPACE, not a name.** So: **the TRACE is structure and you consume it; the FACE is a different era's surface and you do not render it, extend it, or copy its sentences.** ⚠ Leave that file's FACE and its own diagnostic alone — it stands as it is (a working view is not yours to take down), and nothing of ours writes to it.
★ One thing of its discipline worth carrying, because it is the designer's rule stated in code three months earlier: *"state by STRUCTURE, not by caveat ('Unnamed' = `namingDecision === null`, never a status label); NO coded ids in the face (ids live only in the TRACE); NO machinery vocabulary"* — with a `FACE_FORBIDDEN_TOKENS` self-check and the diagnostic enforcing the same floor independently. If you want a model for how `C-7a`/`C-7b` keep machinery out of what a person reads, that is it.

# 3 · Two more existing things, named so you do not re-derive them either
- **`src/lib/siteWitnessCatalogueV0.ts`** (NOT_FROZEN, row 173) + `scripts/diagnose-site-witness-catalogue-v0.cjs` — the per-site witness catalogue of the same campaign. Read its exports before building any "what do we know about this site" list.
- **`src/lib/trisonizedMidwifeReadingV0.ts`** + `scripts/diagnose-trisonized-midwife-reading-v0.cjs` + `npm run diagnose:trisonized-midwife-reading-v0` — the honesty line built in July: *"FRAME + QUESTIONS ONLY … There is NO agent semantic reasoning here."* Its own scope note names the mandate we are now doing: *"deriving `(J; A, B)` from a generated site is a separate later mandate."* **`C-7b` IS that mandate** — and the presenter's TRACE is how `(J; A, B)` is derived (`parentIds` = A, B; `complementVertexIds` = the projection sources). Do not import its frame into our surface (it is the naming-era artifact) — but say in your report whether it should later be fed from the trace, and I route that to the researcher.

# 4 · What does NOT change
`C-7pre` (the fabricated refusal) and `C-7a` (the inside as an arc diagram on the canvas) as stamped at 1028. `C-7b`'s surface, gestures, refusal, pushout, colouring and trace as stamped — only its *structural inputs* come from the presenter's trace instead of fresh derivation. **Price the wiring in your report** (one accessor? a small adapter? nothing at all?) and say if consuming it costs more than it saves — you are the only office that can measure that, and a re-derivation you can defend is better than a consumption you cannot.

# 5 · ECHO
MARKER `C-7b`-inputs · standing: `C-7pre` · `C-7a` · `C-7b` · the W1 carry of ADR 0031.

**NEXT ACTOR for this letter: you** — `C-7pre` → `C-7a` → `C-7b` (with §1's inputs).

---
