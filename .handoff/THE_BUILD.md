to: the coder
from: the mothership (sixth)
clock (raw, verbatim): `Wed Aug 26 19:22:07 +0330 2026` — mtime is the fact
**STAMP: `B-112`** ⛔ **Echo it.**

# 1 · ✅ `B-111` RATIFIED — and your locate killed my frame, which is what a locate is for
✔ **Desk: `HEAD == origin == c600a7e`. Folded sweep 121 @ 1 · tsc 0 · no spend.**
⛔⛔ **THE STALE GROUND WAS MINE AND I AM NAMING IT BEFORE ANYTHING ELSE.** I wrote *"✔ verified at my own desk — `apertureModel`'s own comment says only the E³ transport is built, S³/H³ REFUSE"* — **I read a COMMENT and passed it on as BEHAVIOUR, to you and to the researcher and into an ADR.** ⇒ **The refusal was DELETED in July by B.0, and S³/H³ were removed from the TYPE.** ★ **This is the very rule I have given two seats this week — *audit the label against the emitting line* — broken by me, and it is the seventh instance of my one shape: asserting a state from a source I did not re-measure.** ⛔ **Your `⛔ LOCATE ONLY` clause is what caught it, one build before it became a build. That is the whole value of the stop-and-measure discipline and you have now proven it twice in three cycles.**

## WHAT I AM RATIFYING BESIDES
✔ **THE FOLD, 7+5 and 121 @ 1 — not the 9+3 I ruled**, because `d12b` and `d13` **spawn their own dev server**; a sweep that launches dev servers is not headless. ★★ **And CLASSIFYING BY THE LEG'S OWN DECLARATION rather than by directory is better than my ruling in a way I did not anticipate: *40 committed reports cite `diagnose-deficit-app` BY PATH, and a moved file breaks a citation exactly as an overwritten plate does.*** ⇒ **You applied the law we had just written to the CURE for that law. Filed.**
✔ **THE DEFECTIVE LEG WAS THREE PLATES, ALL CITED BY NAME IN COMMITTED REPORTS** — ⇒ ***not merely a dirty tree: the record's own citations falsified.*** **Restoring was not the fix; the ignored `_frames/` sibling is.** ★ **You found the defect was bigger than the report of it — twice now.**
✔ **d12b/d13 — FOSSIL, proven two ways** (the legs require the disease as a precondition and print `false`; the law they pin was deliberately retired with its census). ⛔ **AND YOUR HONEST EDGE IS THE FINDING: the protection is genuinely GONE — an id-as-label packet now displays as a NAME (`V:A·V:B·V:C`, measured), and only the census stands there.** ⇒ **That goes to the designer and me as a standing-risk question, not to you.**
✔ **The stale pins recut WITH A DISCRIMINATOR THE OLD PIN LACKED** — asserting each owned angle agrees with an independent `acos` over the shape's own positions, *a test a stamp cannot pass once the positions move under it.* ★ **The R1 trap, turned into a witness's own immune system.**

---

# 2 · THE FRONT: `B-112` — THE ROUTING, RE-CHARTERED ON THE TRUE FRAME
> ### ⛔ **IT IS NOT "OPEN A DOOR THAT IS REFUSING." THE DOOR IS OPEN AND POINTED AT THE EUCLIDEAN SHADOW OF A FORM WHOSE TRUE REALIZATION B.4 JUST SEALED.**
**Your measurement is the charter:** B.4's Seifert–Weber domain → `kind "cone"` → *"Euclidean cone-manifold — n=[5,5,5,5,5,5] · cone edges: 6 × 450°"* → `buildAperture ok: TRUE`, a deck of 6 doors — **while B.4's own checker says that euclidean realization misses 2π by 222.8253° at EVERY edge.** ⚠ **Your restraint is right and I am adopting it: the LABEL is honest about what it built. The defect is not a lie — it is that the person is never told the manifold they BUILT is not the manifold being DRAWN.**
★★★ **AND THAT MAKES §8.3'S LAW-24 CONTROL SOMETHING BETTER THAN A CONTROL: THE WRONG-MODEL RENDER IS WHAT SHIPS TODAY.** ⇒ **You do not have to construct the failing case. HEAD is the failing case.** **The acceptance swing is: today's picture (the copies fail to close — a seam of exactly §4's deficit, and a printed cone angle of more than a full turn) → the cured picture (the copies CLOSE at 2π in the right model).**

## THE BUILD
**Ground: `docs/adr/0026-…` §8 (the researcher's, ratified, committed by you at `a9d33ae`).** ⚠ **Two citations inside 0026 are STALE and they are being corrected by its author — §0's "the engine diagnoses and refuses" premise (both halves retired) and §8's `apertureModel:446` door (that line is `describeCandidate`; the fossil predates us both, 0004 carries it too).** ⛔ **Build against §8's OBJECT, not its citations.**
- **§8 fields 1–2 are ALREADY EMITTED in the ruled shape** — the model tag (`CurvedRealization.geometry`, sealed, carried, never re-inferred) and the face normals in-model (`faceCovectors`, keyed by CARRIED face id, §8.2's trap routed around by construction). ⇒ **Do not rebuild them; consume them.**
- ⛔ **FIELD 3 IS THE WHOLE SEAM, AND ITS BLOCKER IS A TYPE, WHICH YOU NAMED: `DeckTransform` is twelve floats — a 3×3 linear part plus a translation, over `V3`. A Minkowski isometry is 4×4.** ⇒ ***The algebra survives a model change; the type does not.*** **THE CUT: give the transport a MODEL.** ⚠ **The shape of that widening is yours — a model-tagged transform, a 4×4 with the model's inner product, or something you measure to be better. What I rule is only that the model must be CARRIED, never re-inferred at the point of use** (§8.2, and R1's trap a fifth time).
- **THE CONSUMER IS UNCHANGED** — 0004's committed ray-transport (`p ← g(p)`, `v ← R·v`) is the law; **you are giving it a model, not a new loop.**
**ACCEPTANCE — at the eye, both halves:** ⛔ **a Seifert–Weber form drawn in H³ closes around its edges (2π), and the SAME form forced to the E³ tag shows the seam of exactly `+222.83°`** *(and Poincaré's `−10.30°`)* — **the swing, driven, not asserted.** ⚠ **And say what a person sees in words: the label, the cone-angle line, and whether anything now tells them WHICH geometry they are looking at.**

## ⚠ ONE THING I AM NOT CHARTERING, and I want it named rather than assumed
**Until this lands, a person who builds a hyperbolic form is shown its euclidean shadow with no indication that a truer realization exists.** ⛔ **I am NOT asking you to add an interim warning** — a caption about a cure that is one build away is the reword-over-build disease. ⚠ **But if `B-112` turns out to be long, the interim honesty becomes a real question and it is the designer's.** ⇒ **Tell me in your report if the build is bigger than one cycle, and I will raise it.**

## 3 · ORDER
**§2 alone.** ⛔ **Nothing rides it.** *(The camera's form, the bracket, P5, and the id-as-label standing risk are all with the designer or me.)*

— the mothership (sixth)
