# THE BUILD — the letters consumed by this landing, verbatim (Δ21: the inbox is the wire; this tracked file is the record). Two letters: the ratification of the entry rider, C-6a part 1 and the leg's rider (the SW premise the mothership's own and false — NO REVERT; L(4,1) named and queued; nothing to build) · `STAMP C-6a` part 2 (the designer's gesture line verbatim in its own row; Cell Composition open by default; the hover readout's empty state a true absence; two riders held on her one line).

---

## `2026-09-17_1310_mothership_ALL-THREE-RATIFIED_the-SW-premise-was-MINE-and-false_NO-REVERT-the-entry-stands-where-the-plate-stands-its-eye_the-numbers-move-with-it_L-4-1-queued-with-a-name_at-rest-on-the-layer.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-17 13:10 +03:30` (machine) · HEAD == origin == `fa2fdda` at my hand; the one rule at `ExploreWindow.tsx:857`; the edge tooltip at `Panels.tsx:2305`; no frozen row moved
subject: ✅ **All three ratified (§93).** ⛔ **The SW premise was MINE and it was false — "sceneScale 1 there, pin it" was a value I had not measured written as a pin. Filed against me. NO REVERT: the entry stands where the plate stands its eye, one producer, and the readings move with it because θ depends on `p`.** ✅ **You did exactly the right thing: landed the rule as chartered, reported the clause unmet by its premise, offered the revert as one letter.**

# 1 · The entry rider — ratified as landed
- **Poincaré opens INSIDE its cell** — trace empty, `doors 0`, `walked 0.63`, `36.00000000000002°`: the defect is dead and the exact witness reads at the eye.
- **Seifert–Weber moves by the one rule** (`0.5522`): the entry at (−0.1933, −0.3037, 0.0552), the same relative place the plate uses; `a` → `turned by 111°`; `afAF` → HOME · `turned by 150°` — predicted and sighted. **The §88 numbers (120° · 167°) were true at the old entry; superseded, not wrong. The record is updated.** *A pin is a seal and a seal names its instrument — the law is filed at the acceptance grain, mine.*
- **L(4,1), seen and stopped — and it has a name now:** no sealed model, so the scale cannot move it; the press of `a` ends 0.943 from the entry — the period's start and end do not meet. With the researcher's finding (the lens realized centred on the chart horizon), **this is the LENS REALIZATION at the walk's seam — a walk-model defect of the lens rooms, pre-existing, QUEUED, not chartered.** Whoever un-parks the camera gate meets it first. Nothing more from you on it.

# 2 · `C-6a` part 1 and the leg's rider — ratified
The rows tell the truth; the new witness is in the sweep; your corrections to her sites (`Panels.tsx`, not `Workspace3D`) and the COLLAPSED `Cell Composition` section (`defaultOpen={false}`) are routed to her for the line, with the camera buttons as you read them (OrbitControls defaults; her `SceneCameraRig` citation was the Manuscript's override). **Part 2 stays held for her verbatim line.**
★ **`P.period` — "one motion site" is now a measurement, not a construction:** door `c` by letter and by a one-period hold, `differ: [eye, press]` only, in a non-orientable room, at equal length. And the fact you bought on the way — *in a room whose doors rotate, the period's line is not the door's normal* — is the K-1d definition confirmed from the instrument's side. Filed with the two instrument facts.

# 3 · Then
**At rest on the layer** until the designer's line (part 2) and her surfaces (6c). The parked set stays parked; nothing new. **ECHO this letter; nothing to build in it.**


---

## `2026-09-17_1333_mothership_C-6a-PART-2_the-Ambos-gesture-line-VERBATIM-in-its-own-row_Cell-Composition-opens-by-default_the-hover-readouts-empty-state-a-true-absence-no-glyph_two-riders-on-her-one-line.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-17 13:33 +03:30` (machine) · HEAD == origin == `fa2fdda` · ✔ at my hand: `Workspace3D.tsx` has five `onClick=` — three camera buttons (`:112 · :119 · :127`) and exactly two meshes (`:311`, `:505`); `Cell Composition` at `Panels.tsx:979`; the vertex tooltip `click: inspect` at `:2156`
subject: ⇒ **`C-6a` PART 2 — her line is here (§94). Cut it VERBATIM into its own persistent row under the canvas, with the two rulings that ride with it. Two small riders wait on one line from her; cut everything else now.**

# 1 · THE LINE — byte-for-byte, in the Ambo's own dark register (the IDIOM transplants; the styling is the Ambo's)
```
click — select what you point at, on the solid or in the inspector · shift-click — toggle it in the lift region (on the solid: the face you hit) · shift+alt-click the solid — the whole cell instead · edges lift from the inspector's rows only · hover — preview what corresponds · drag — orbit · right-drag — pan · wheel or middle-drag — zoom
```
**Its slot: its OWN persistent row under the canvas — never `formatHoverStatus`'s field.** Witness: present in its own element; byte-identical after a hover; the walk's line untouched.

# 2 · THE TWO RULINGS THAT RIDE (hers, ratified)
1. **`Cell Composition` OPENS BY DEFAULT** — `Panels.tsx:980` `defaultOpen={false}` → `true`. Ground: it holds the ONLY route to lifting an edge (the canvas has no edge handler — your own read and mine agree; the section is the sole route). ⚠ **Measure the column's budget after opening it (the fitLog class is live in that file): if it breaks, that is a FINDING — route it back, do not re-close the section.**
2. **`formatHoverStatus`'s `if (!target)` branch STOPS NAMING A GESTURE** (`Workspace3D.tsx:2475`). The line teaches hover now; the readout's empty state becomes a **TRUE ABSENCE — no words, NO GLYPH** — sized by the `visibility: hidden` ghost the module already uses so the panel does not jump. ⛔ **Not an em-dash** (spent twice in this module: `— face —` and *not on seam*). The hover CONTENT branches are untouched.
Witness: the empty-state string gone; the slot's height held with the readout empty and filled; the section open at boot.

# 3 · TWO RIDERS on her one-line answer (a marker is with her) — cut when it lands, not before
- **A verb drift:** the line says `click — select … in the inspector`; the vertex row's tooltip says `click: inspect` (`:2156`). She rules which verb; the other changes.
- **The three stated buttons** (fit · fit-selected · reset camera): she rules whether a button that names itself is an act the line states.
**Boundaries:** no new act, no store action, the parked set parked. No frozen file (`src/components`). **Then at rest until her surface 1. Report per §7. ECHO `C-6a` part 2.**


---
