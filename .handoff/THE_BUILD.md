# THE BUILD — the letters consumed by this landing, verbatim (Δ21: the inbox is the wire; this tracked file is the record). Two letters, the mothership's order: Δ85 and Δ86 — Arman acts at every generation, and cast loading is for the seed alone; the item-0 route measured the shortcut; `STAMP C-8` THE BORN ROOM, REACHABLE LAWFULLY, after C-7g (1047) — and the MARKER: C-8 item 1 REWRITTEN (the J by the edge's KIND; a medial edge composes the MEET, never the parent's identity, which pools), items 2–5 the designer's three, C-7g's five from the face block, both ADR riders carried (1110). The 1008 letter (the ratification of C-5 and STAMP C-7g) entered the record with C-7g's landing, 3292263.

---

## `2026-09-23_1047_mothership_Delta85-and-86_a-is-right-and-CAST-LOADING-IS-ONLY-FOR-THE-SEED_your-item-0-route-loaded-casts-onto-midpoints_on-the-LAWFUL-path-the-chain-breaks-at-its-second-act_STAMP-C-8-after-C-7g.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-23 10:47 +03:30` (machine — the sandbox's clock in Arman's zone) · HEAD == origin == `7d8d021`
re: **Arman answered twice this hour.** Δ85: *"yes (a) is right"* — he acts at every generation. Δ86, mid-turn: ***"no cast loading is only for the seed."*** ⇒ **`STAMP C-8` below, AFTER `C-7g`.** And one thing about your item-0 route that you need to hear plainly, because it is not your fault and it changes what item 0 measured.

# 0 · ⛔ YOUR ITEM-0 ROUTE LOADED CASTS ONTO MIDPOINTS — and Arman has now named that act as not his
Your route to `ABAC`: *"select AB in the selection tab, packets tab → `load cast… (.cast.json)`; the same for AC"* — **files loaded onto two gen-1 midpoints.** The researcher caught it reading your letter (*"lawful in FORM, on two spaces that were not lawful"*); Arman, independently, this hour: ***"did you mean … that you can load a haphazard cast for each midpoint again? no cast loading is only for the seed."*** ⇒ **Everything item 0 reached through that route measured the SHORTCUT, not the chain.** ⚠ **Not yours to carry alone: the loader offered the act with no guard, and I ratified §119.2 on your route without asking HOW the casts reached AB and AC — the premise was in the route's own words.** Filed against me.

# 1 · ⛔ WHAT THE SOURCE SHOWS ON THE LAWFUL PATH — read by me; confirm it at the eye (`C-8` item 0)
`ConceptSurface` (`MidpointSurface.tsx:686–698`) reads a site's two parents as `shape.vertices[site.a]?.data.cast` / `…[site.b]?.data.cast` — **the loaded-file slot.** `CastInsidePanel` (`CastInsideDiagram.tsx:348`) reads only `vertex.data.cast`. **A mapped midpoint's composed space (`gluedSpace`, `:287`) is computed ONLY while you stand AT that midpoint and is written nowhere a child can read.** ⇒ **Prediction: map AB, map AC by pointing, load NOTHING onto a midpoint, dissect, select `ABAC` → NOTHING DRAWN.** *"Every midpoint becomes a parent"* is true for the parent's own picture (your `C-7e`: AB selected at gen 2 redraws its own unfolding from its carried record) **and false for its children's act.** ⇒ **LAW 7's eighth firing, with a new shape: *a test that reaches its subject by an unlawful path has measured the shortcut, not the subject.***

# 2 · STAMP `C-8` — THE BORN ROOM, REACHABLE LAWFULLY (after `C-7g`)
**THE RULING (Δ85, in the researcher's form — ADR 0028's ratified split of 09-01, restored over 0031 §3.5):** a **CORNER** edge (a born vertex to its own parent) **CARRIES** — no act lands on it; an act at a corner SITE is an act on the PARENT edge (your record home already honours this) · a **MEDIAL** edge **COMPOSES** the identity on its shared corner — derived by the device, never entered, never withdrawable · **BEYOND the shared corner a medial edge is BORN — the person's to give.** That is Arman's (a).
0. **MEASURE FIRST (cheapest first):** (i) the lawful path above — confirm *nothing drawn* at `ABAC`; (ii) the researcher's risk — **does a medial edge's record start EMPTY rather than from the composed identity?** (If so, the shared corner's identity is missing and every medial transport is wrong — the tower theorem would fail in the build while holding in the ADR.)
1. **`spaceOf(vertex)` — ONE resolver, RE-DERIVED at every read, NEVER stored:** a seed vertex → its cast · a mapped midpoint → the gluing of its two parents' `spaceOf` over its edge's record, recursively (gen 2's parents are gen 1's gluings) · **an unmapped midpoint → the disjoint union** (the amalgam over nothing, §108.1.2). `ConceptSurface` and `CastInsidePanel` read `spaceOf`, never `data.cast` directly. ⛔ **RECORD, NOT READING: a composed space stored on a vertex is a stamp that drifts from the acts that made it.**
2. ⛔ **THE LOADER — ARMAN'S RULE, BY CONSTRUCTION: `load cast…` is offered ONLY on the SEED's own vertices (a WHITELIST), and is ABSENT on every other vertex** — not *"absent on generated midpoints"* (a blacklist that would leave any other born vertex open). The component already knows `isGeneratedMidpointVertex` and `generationDepth`; the precondition lived in the tooltip's copy (*"a corner takes a concept-space"*) and was never written as a guard. ⚠ **Measure whether any non-seed vertex already holds a loaded cast in the reachable path (a saved workspace; your own witness fixtures) and price what the surface says of it — never silently prefer it over `spaceOf`.**
3. **AT A MEDIAL EDGE'S SITE (a midpoint of gen ≥ 2):** the shared corner's identity is **DERIVED and shown as COMPOSED** — never entered, never withdrawable, **and never a proposal** (a consequence of structure, not an offer); **pairing offered ONLY in the born room** (a role of one parent's non-shared part to a role of the other's); a pair colliding with the derived identity **refused by name.**
4. **THE DEPENDENCY REFUSAL, across generations** (the researcher's, forced by the lift law): a later act that would break an earlier born act — e.g. a gen-0 act that re-glues a role the born act named — is **REFUSED at the act, by name, naming the born act it would break**; the person may withdraw the older act first. *Accepting it and dropping the born act ERASES; accepting both FABRICATES; refusing the new act is neither.*
5. **THE TRACE** carries each act's **GENERATION** and **SITE-versus-HOME**; a refusal names the act it collided with.
6. ⛔ **THE DRIVE FAMILY RUNS ON THE LAWFUL PATH ONLY — the leg NEVER loads a cast anywhere but on the seed** (Arman's rule, not a precaution). Re-measure the born pair at `ABAC` this way, by pointing at AB and AC first.
**Surface for items 3–5 goes to the designer first** (how the composed identity reads; how the dependency refusal reads; the loader's absence) — build the mechanism with defaults named; she rules at her eye. **Nothing frozen; the ADR is the researcher's (the §3.5 restoration text) and you carry it as W1.**

# 3 · THE W1 CARRY waiting in the tree
ADR 0031 §3.2/§3.3 — the researcher's four hunks, verified at my hand: `4 insertions(+), 3 deletions(-)`, before `7765e826…` (= HEAD's blob) → after `aed06e20…`. **Carry it AS FOUND with `C-7g`'s record commit.** The §3.5 restoration and two §1.5 lines follow from the researcher; carry them as found too.

# 4 · ECHO
Δ85 · Δ86 · ECHO §120 · STAMP `C-8` (after `C-7g`) · the W1 carry. **Your `C-5` stands whole; only item 0's route is re-read.**

**NEXT ACTOR for this letter: you** — `C-7g`, then `C-8` with item 0 first.

---

## `2026-09-23_1110_mothership_MARKER-before-you-build-C-8-item-1-REWRITTEN-the-J-by-the-edges-KIND-a-medial-edge-composes-the-MEET-never-the-parents-identity-which-POOLS_C-7g-absorbs-the-face-blocks-four_carry-BOTH-ADR-riders.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-23 11:10 +03:30` (machine — the sandbox's clock in Arman's zone) · HEAD == origin == `7d8d021`
re: ⛔ **MARKER — read BEFORE `C-8` item 1. The resolver's rule in my 1047 was wrong on two of the three edge kinds, and the first correction of it was wrong too; the researcher found both before a line was built.** And `C-7g` (not yet started) grows by the face block's four. **Your queue is still `C-7g` → `C-8`.**

# 1 · `C-8` ITEM 1 — REWRITTEN (supersedes my 1047 §2.1 and the researcher's 1055 §0)
My charter said *"a mapped midpoint → the gluing of its two parents' `spaceOf` over its edge's RECORD."* **Right on SEED edges only.** ⇒
> **`spaceOf(M_e) = glue(spaceOf(u), spaceOf(v), J(e))`, where `J(e)` is the one the edge's KIND fixes:**
> - **SEED edge** → the person's record (possibly empty — an unmapped midpoint is then the disjoint union by the same construction; no branch);
> - **CORNER edge** (a born vertex to its own parent) → the **CARRIED injection** of the parent into its born vertex — derived; **no record is read, because none can exist**;
> - **MEDIAL edge** → ⛔ **the MEET of the identities on EVERYTHING both endpoints hold** — each class of one endpoint paired with the ONE class of the other that holds all of its seed roles and words; **a class spread over two, or two onto one, is a CONFLICT: left out, and the pushout houses it twice** — **∪ the person's BORN pairs.**
**Why each clause, measured by the researcher and RUN BY ME** (`space_of_resolver.py`, `g3_meet_vs_parent.py`, 3,000 random faces each):
- **Record-only** makes a STATION `A ⊔ M_AB` — **A twice, 3,000 of 3,000** (13 roles where it must be 8) — **it breaks Arman's stations theorem at the first station**; at `ABAC` it duplicates the shared corner 3,000 of 3,000.
- ⛔⛔ **The PARENT'S IDENTITY ALONE** (the researcher's own 1055, and my reading of it) **POOLS two roles of ONE corner at a vertex in 960 of 3,000 gen-2 faces — 0031 §6 invariant 3, THE STONE, broken** — because two gen-2 endpoints share their parent AND the corner each reaches by a different route. *"It pools the disagreement and doubles the agreement: exactly backwards."* **The MEET never pools (0 of 3,000)**, equals the shared corner's identity on gen 1's medial edges (0 differences), and **reproduces the ratified 09-16 doubling set exactly (3,000 of 3,000), built recursively — an independent reproduction of that theorem.** That last number is the one your witness should reach.
- **Name-matching** duplicates an A-role in 2,926 of 3,000 (a glued role is labelled `a≡b` in AB and `a≡c` in AC). **And the build's keys are LOCAL:** `midpointGlue.ts:60` `Side = 'A' | 'B'` is the EDGE's first or second parent, and `roleKey` (`:132–133`) keys `A:x` / `B:y` by that side — **on edge `C–A` the prefix `A:` marks C's roles.** ⛔ **Never find the shared corner by name or by prefix.**
⇒ **THE RESOLVER'S TYPE:** every role and word of every `spaceOf` **carries the SEED roles and SEED words it holds, composed down through the injections at every level** (`GluedRole.a` / `.b` at `:64–67` carry the first level; the meet needs all of them). **Derived at every read; stored nowhere.**
⇒ **THE SEAL for your witness:** the hand-sealed face — `|A| = 5 · |M_AB| = 8 · |M_CA| = 10` ⇒ **station on `A–M_AB` = 8 roles, 0 duplicated · `ABAC` (no born pair) = 8 + 10 − 5 = 13 roles, 0 duplicated** (record-only 18; name-matched 15); **the meet pools in 0 faces where the parent-only rule pools in 960**; **the doubling at a medial midpoint = exactly the meet's conflicting classes, and with every born room empty it equals the 09-16 set.** ⚠ **Quote the researcher's figures from its named run; pin your own generator's SHAPE** (as you did in `C-5`).
⚠ **And invariant 9 at gen ≥ 1** (the ADR's new clause): an opposite vertex can be an **ANCESTOR** of both endpoints — it enters through their own spaces and cannot be re-cast without changing them ⇒ **the byte-identical falsifier re-casts NON-ancestor opposite vertices only** (for `AB–AC`, that is `BC`: vary `J_BC`, `ABAC` must not move; control: vary `J_AB`, it moves — 2,993 of 3,000 in the model).

# 2 · `C-8` ITEMS 2–5 — the designer's three, ruled (her 1054)
- **Item 3 — the composed identity is NOT A PAIR and is NEVER DRAWN AS ONE:** never your yellow stroke across the fold, never a control (`withdraw`, `take`); **shown ONCE, marked `composed · corner A`, and the fold does not cross it.** The drawing is yours to propose with the default named, hers to judge. ★ **A fourth origin word, `composed`, beside `from A` · `from B` · `both`:** `both` = witnessed by both parents **through the person's act**; `composed` = in both parents **because the solid made it so.**
- **Item 4 — the dependency refusal, the SAME grammar as the face's hands, the generation in the WHERE:**
  ```
  not taken — your pair at ABAC, one generation up, rests on the role this would re-glue
    here, on A–B: withdraw this attempt  ·  at ABAC (a generation up): withdraw F7 ↦ X first
  ```
  The refused-against act attributed as **the person's own**; both hands; the far one names its generation.
- **Item 2 — the loader ABSENT off the seed, NEVER DISABLED, and no word added** (the reason is already on screen in three places; a fourth copy marks the ordinary). ⛔ **My proposed sentence is withdrawn.**

# 3 · `C-7g` GROWS BY THE FACE BLOCK'S FOUR (all hers, ruled from your plates)
5. **The site stays** — and it is right *because* of the unfolding: `ABC`'s block under `C`, `ABD`'s under `D` — one face seen from three doors, not a fact printed twice.
6. ⛔ **The face orphans its verdict** (the derived core breaks off on the wrap, as term order did). **She refused "lead with the verdict" because it breaks ADR 0024 (map first, the verdict a consequence).** ⇒ **keep the order, break the lines: each clause on its own line; the core CLOSES the block as its own line; `did not return` leads with counts per edge before names** — `12 did not return — 11 broke at A–B · 1 at B–C: F1 F2 …`.
7. **No control to walk the other way — D14:** *a reversed cycle is a FLIPPED face*, not a face of this solid. ⇒ reword `the reading is the walk's; the other way round reads differently` → **`walked in the face's own direction, A → B → C → A`**.
8. **Every hand leads with WHERE, the local one says `here`:** `on C–A: withdraw r8 ↦ F7 · here, on A–B: withdraw F7 ↦ Φ1 · on B–C: withdraw Φ1 ↦ r1`.
9. ⛔ **One word, before `composed` becomes an origin in `C-8`:** the face refusal's *"the three acts around it, **composed**, make…"* → **`the three acts around it, walked in turn, make…`** (one glyph, one meaning — `composed` is the solid's register, not the person's).

# 4 · THE W1 CARRY — BOTH ADR RIDERS, verified at my hand
`docs/adr/0031-…md` against HEAD: **8 hunks, `9 insertions(+), 7 deletions(-)`**; HEAD's blob `7765e826…` → rider 1 `aed06e20…` → **both riders `655387b0…`** (reversing rider 2's five edits reproduces rider 1 byte for byte — the researcher's proof). **Carry it AS FOUND with `C-7g`'s record commit.**

# 5 · ECHO
MARKER (`C-8` item 1 rewritten; items 2–5 the designer's) · `C-7g` +5 items · the W1 carry · ECHO §121. **Order unchanged: `C-7g` → `C-8` (item 0 measure first). The drive family on the lawful path only — never a cast off the seed.**

**NEXT ACTOR for this letter: you.**

---
