# THE BUILD — the letters consumed by this landing, verbatim (Δ21: the inbox is the wire; this tracked file is the record). Two letters, the mothership's §145 — `STAMP C-12b`, THE STONE: M⁺, the born concept carries its opposite corners' FEET (1941) — and its MARKER (1947): `≡_X` IN the signature, ruled (a). The §148 letter entered the record with the two rulings' landing, 4cd39e8.

---

## `2026-09-24_1941_mothership_s145_STAMP-C-12b-THE-STONE-M-plus-the-foot-after-C-12a.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-24 19:41 +0330`
re: **§145 — `STAMP C-12b` — THE STONE: M⁺, the born concept carries its opposite corners' FEET. After `C-12a`.** Δ95: *"i want the stone re-build before the final use."* Race discipline stands (§138); STOP only on a frozen file, a meaning question, a falsified premise.

**Ground — ADR 0031 §3.10 + invariant 9's positive half** (the researcher's, in the working tree; `blob 833d63f7` = `sha256-LF eeaf397b`, verified at my hand; **carry it W1**). The reference: `.handoff/instruments/connection_layer_reference/the_stone_foot_runs.py` (ruling §25), cited, not re-derived.
- The FOOT of opposite corner X on edge AB: `foot_X = J_XB ∘ J_AX` (partial), from the person's own records on the carried/derived spaces through the one resolver.
- **M⁺_AB = M_AB as built PLUS one relation ≡_X on M's points per opposite corner X**: FIX (`J_AB(a) = foot_X(a)`, a loop) · MOV split into DISAGREEMENT (`J_AB(a)` defined, different) and PROPOSAL (`J_AB(a)` undefined) · UND (silence). **Nothing merges; `spaceOf` unchanged; `J_AB` untouched; derived every read, stored nowhere.**
- **How many feet:** one per distinct triangle (by vertex set) of the complex the born vertex came from that holds its edge — gen 0: two (C's and D's); gen-1 corner edge: two (loops when born rooms are empty); gen-1 medial edge: three.
- ⛔ **A PROPOSAL is a mark, never an offer** — no control, never lit, never first (Δ80; §8(c)).

**Surface — the designer's `1939`** (`.handoff/inbox/mothership/2026-09-24_1939_designer_s144-THE-FOOTs-SURFACE_…` — read it whole; I archive it after you consume it): **WORDS, not glyphs** (a ring and a line between points are both spent in the inside diagram). **One block per opposite corner under the midpoint's own column**, C then D, headed *"C's view of your pairing on A–B — read from your pairings on A–C and C–B"*; its lines *agrees on N* · *would pair X otherwise* · *would join what you left apart* (a conditional sentence, same weight, no control, never first); silence absent per point, **one line if the whole corner is silent**; feet told apart by the named head — never `[C]` (the word-corner bracket) and never `C:` (reads as leaked keys).

**Witness:** the reference's four runs reproduced on the fixtures (Flow · T · Φ, the 42 hand triples) — R1 6 distinct feet per fixed `J_FT` (control 0) · R2 non-loop links = |MOV|, flat control 0 · R3 (a)(b)(c) 0 failures and the EXCESS on 40 of 42 triples, 77 roles · R4 the tower (corner triangle all loops; medial ⇔ seed face, 0 disagreements) · no point added, `J_AB` byte-equal. **Eye leg arm** at 1689 × 897: at AB after pairs on A–B, A–C, C–B — C's block reads, D's block reads (or its one silent line), no control inside either.
**Acceptance:** those · sweep OK · tsc 0 · no frozen file (else STOP). **Ring me when filed.** W1: ADR 0031 (§3.10), the researcher's ruling file, the claims ledger (§145).

---

## `2026-09-24_1947_mothership_MARKER-on-C-12b-the-foot-is-IN-the-signature-ruled-a.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-24 19:47 +0330`
re: **MARKER on `STAMP C-12b` (my 1941) — the hold is lifted. The researcher ruled (a) (their 1945, ruling §27, ADR 0031 §3.10 amended — `blob e7565a44` = `sha256-LF b0795661`, verified at my hand; carry it W1): `≡_X` is IN M⁺'s SIGNATURE.** Take `C-12b` after `C-12a`, with these changes to item 1:

1. **`spaceOf` composes `≡_X` into the born vertex's space** — one binary relation-type per face holding the edge, named by its warrant corner X; **holds-only** (a link is `holds`, silence `unrecorded`, never `does-not-hold`) ⇒ it can never cause a refusal (`refusalOf` must read it and find nothing to contradict — pin that). Derived every read, stored nowhere, never a record entry of the edge.
2. **It CARRIES:** the lift (re-derived on the carried record — nothing extra) · the next generation's pushout (the parents' `≡` tuples ride as FOREIGN words, never shared by spelling — Δ80; that generation's own feet derived afresh) · the door (preserved like any tuple; holds-only on both sides, so no conflict).
3. **It does NOT carry into the walk's transport** — the cargo is a role, moved by `J` and `e` only; `C-11`'s readings unchanged (pin K1–K7 unchanged).
4. **The surface is still the designer's 1939** (words, one block per opposite corner under the own column; a proposal a conditional sentence, never a control).

**⚠ Numbers WILL move, and each move must be explained:** a born vertex's word and tuple counts grow by its feet (the card's `Space · … roles · … words · … tuples`, the born-room and midpoint witnesses' pinned counts). **Acceptance addition:** every pinned number that moves is accounted for by the feet's words and tuples ALONE — role counts, the meet, the stone, the dependency refusals, the door census and the cargo's K1–K7 unchanged. **And measure, don't rule:** at the next generation the foot words appear in the word rows as foreign words — say how they read there (the designer rules it after, if it needs ruling). STOP on a frozen file.

---
