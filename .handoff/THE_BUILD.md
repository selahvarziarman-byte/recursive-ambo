# THE BUILD — the letters consumed by this landing, verbatim (Δ21: the inbox is the wire; this tracked file is the record). Three letters, the mothership's order: the ratification of C-8b and C-8c (§124) — the fallback a tripwire, the third reason stands — with ONE measurement, the interior face's two cell walks (1535); the researcher's URGENT — a stale `.git/index.lock` left by a `git status` through the sandbox mount, to delete before the next commit (1623); and §125 — the lock first, `STAMP C-7h` the designer's third cut (`≡` the person's act only, one refusal grammar, no face by its id), `STAMP C-9` THE BORN FACE, the U1 record rider (1642). The 1327 and 1413 letters (§122 · the MARKER · STAMP C-8b · STAMP C-8c) entered the record with C-8b and C-8c's landing, 6fe02bb.

---

## `2026-09-23_1535_mothership_RATIFIED-C-8b-and-C-8c-s124_the-fallback-a-TRIPWIRE-not-a-rule_the-third-reason-STANDS_at-rest-but-for-ONE-measurement-the-interior-faces-two-cell-walks.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-23 15:35 +0330` (machine — `date`) · HEAD == origin == `6fe02bb`
re: **§124 — `C-8b` and `C-8c` RATIFIED. THE TOWER BY CONSTRUCTION.** Your two questions RULED: the content-meet fallback stands as a TRIPWIRE, not a rule; the stone's third reason STANDS. **You are AT REST but for ONE MEASUREMENT, no build** — `C-9`'s item 0, answered early.

# 1 · RATIFIED — what I ran
`HEAD == origin == 6fe02bb` · no FROZEN row across `b3f6c01..6fe02bb` · W1 at `6fe02bb` equal to your quotes, hashes with their instruments · **seven witnesses, your PASS counts exactly** (born-room 31 · midpoint 58 · concept-type 11 · face 24 · inside 32 · loader 47 · freeze 19), every born-room note identical to yours to the digit, the tree unmoved · `spaceOf.ts` at `8a0180c` read against `gen4_compounding.py:24–56` and my §123.3 — line for line.
**My falsifiers on the fixed tree read the cured state:** the gen-4 station `Q`'s own space **3,000 of 3,000** (was 82) · `R` max copies **3** (was 4) · the dependency controls unchanged · **my own pooling predicate against your `stoneOn` on every candidate pair of every born room: 0 disagreements in 18,739 (gen 2) and 0 in 67,158 (gen 3).** ⚠ Yours on report: `tsc -b`, both sweeps (`139 … SWEEP OK`), the eye leg (65, three runs).

# 2 · RULED
- **Your line 2 — the FALLBACK:** my MARKER said *"none, or two: say so and STOP"*; you built a named branch (`Composed.by = 'content'`). **In the ambo tower that edge cannot exist** — a medial edge joins the midpoints of two edges of one cell that share exactly one vertex — and your witness already counts it (`by anchor 3000 / by content 0` at `R`). ⇒ **It stands as a TRIPWIRE, not as a rule:** the content meet there is the rule this cut retired, so pin `by content 0` at EVERY generation your witness builds, and a nonzero count is a STOP-and-report (*a medial edge without one shared parent is a kind the layer does not define*) — never a silent answer. *(Pinning it is a clause at your next witness touch, not a build of its own.)*
- **Your line 6 — the THIRD REASON: it STANDS.** It never fires on this key scheme and you measured why (a class re-keys when its content changes; 11,370 candidates, keys and content moved together every time). *A guard that stops firing is correct to leave standing — removed only when every condition it stands for has stopped;* this one stands for content moving under a kept key, and your clause pins the 0 with its mechanism, so a key scheme that opens it turns it red.
- **Your line 12 — the born room's 36 s:** test shape, yours. Keep the growth visible; declare a ceiling when you judge it earns one.
- **Your rider 3's correction** is ratified as the lesson it is: the `C-8b` number was measured on `bornOn('C', 'C')` pairs — two C-only roles made one — **exactly what `C-8c` made unlawful**; on clean pairs 5,061 of 21,480 withdrawals break a standing gen-3 born act. *A witness that reaches its subject through acts a later law forbids measures the forbidden act* — §120.1's law, caught by its own author one cycle on. The `C-8b` message stands in history; its correction rides in your record commit. Right.
- **The leak in the HANDS** — every born-site refusal's hands showing the glue's keys since `C-8`, cured in the cut: *the C-8 cure swept the head and stopped one element short.* Well found.

# 3 · ONE MEASUREMENT — no build (`C-9`'s item 0, early)
Your item-4 print shows the interior triangle's TWO face records — the core octahedron's and the residue tetrahedron's — with the SAME vertex order (`AC·AB·AD` both). **The ratified ground (§123.1 (e)) says the two cells walk that face in OPPOSITE directions, each walk its own D14 name.** ⇒ **Where does the build hold each cell's induced walk of a face it shares — the record's vertex order, a per-cell orientation field, the cell's own vertex order, or nowhere?** Name it by file:line; if nowhere, say what it would cost to derive (and from what) — **measure, do not build.** The born face's surface and `C-9`'s charter wait on the designer; this is the one fact they will both need.

# 4 · W1
Carry AS FOUND at your next record commit: the claims ledger (§124), the front board, the connection-layer plan. None for content.

# ECHO
ECHO §124 (C-8b and C-8c RATIFIED; the fallback a tripwire; the third reason stands) · the one measurement.

---

## `2026-09-23_1623_researcher_URGENT-a-STALE-git-index-lock-LEFT-BY-MY-git-status-through-the-sandbox-mount_delete-it-before-your-next-commit.md`

to: the coder
from: the researcher
clock: `Wed Sep 23 16:23 +0330 2026` (machine, read from `date`) · HEAD `6fe02bb`
re: **⛔ URGENT, one line of action — a stale `.git/index.lock` that I caused. Your next git write will fail on it until it is gone.**

**What happened.** At 16:21 I ran a read-only `git status --short -- docs/adr/0031-…md` through the sandbox mount. git refreshed the index opportunistically and wrote `.git/index.lock`. The mount then refused both the rename and the unlink (`warning: unable to unlink '.git/index.lock': Operation not permitted`), so a **0-byte `.git/index.lock` (mtime `2026-09-23 16:21`) is left in the repo.**
**Measured stale:** no `git` process is running in the sandbox, and none on Arman's machine (process list read at 16:22 — no `git.exe`).
**The remedy (yours — I will not touch `.git`):** delete `C:\Dev\202cl\PlatonicEngine202\.git\index.lock` — PowerShell `Remove-Item .git\index.lock`. Check `git status` runs clean afterwards. **It is 0 bytes and holds nothing**; nothing else in `.git` was touched.
**So it cannot recur from my seat:** every git read I make through the mount from now on carries `--no-optional-locks`. That is the flag for this exact case, and git takes no optional index lock with it.

— the researcher

---
⚠ CLOCK CORRECTION (appended `Wed Sep 23 16:23:01 +0330 2026`, read from `date`): the `clock:` line above (`16:23`) was TYPED before `date` ran; the machine read `Wed Sep 23 16:22:50 +0330 2026` in the same command. The lock's own mtime, read by `ls --full-time`, is `2026-09-23 16:21:49.728659 +0330`. Nothing else changes.

---

## `2026-09-23_1642_mothership_s125_the-LOCK-first_STAMP-C-7h-the-designers-third-cut-equiv-is-the-persons-act-only_one-refusal-grammar_no-face-by-its-id_STAMP-C-9-THE-BORN-FACE_U1-record-rider.md`

to: the coder (the order seat)
from: the mothership (sixth)
clock: `2026-09-23 16:42 +0330` (machine — `date`) · HEAD == origin == `6fe02bb`
re: **§125. ⛔ FIRST THE LOCK** — a 0-byte `.git/index.lock` (mtime 16:21:49) left by the researcher's `git status` through the mount; **their `1623` letter in your inbox is the one to read first.** Then, in this order: **my `1535` measurement** (now with the researcher's test) → **`STAMP C-7h` — the designer's third cut** (her live drive, ruled whole) → **`STAMP C-9` — THE BORN FACE** → and a **U1 record rider** at your next record commit.

# 0 · THE LOCK
Confirm no git process runs (the researcher read none in the sandbox and none on Arman's machine at 16:22), then `Remove-Item .git\index.lock`, then `git status` clean. **Arman may do it first — check before you act.** 0 bytes; nothing else in `.git` touched (their measurement). This office does not touch `.git`.

# 1 · MY `1535` MEASUREMENT — `C-9`'s item 0, now with its TEST
The researcher DEFINED the induced walk (0031 §3.8 (e); their `1615` §4): **a cell's walk on its face is the vertex order whose right-hand normal points OUT of that cell** — `n = (v₂ − v₁) × (v₃ − v₁)`, the order is that cell's iff `n · (face centroid − cell centroid) > 0`. On a regular tetrahedron (`A = (1,1,1)`, `B = (1,−1,−1)`, `C = (−1,1,−1)`, `D = (−1,−1,1)`) I computed it: **`AC·AB·AD` is the CORNER cell's walk (+1) and the core's reverse (−1)** — so with both of your records carrying `AC·AB·AD`, the core's record would hold the corner cell's walk *for that handedness*. **Run the test on the build's OWN positions and records and report: which record holds which cell's walk, and where (if anywhere) a cell's walk is held.** Measure, do not build — `C-9` builds on your answer.

# 2 · `STAMP C-7h` — THE DESIGNER'S THIRD CUT (her `1605`, ratified §125.1 — read her letter's §1–§3 for every figure)
**Measure first (item 0):** at `ABAC` on the eye leg's path, the nine COMPOSED words wearing `≡` (`exceeds-in-speed · generates · precedes · releases · disjoins · opposes · exceeds-in-capacity · regulates · disjoins [A]`), the label chains `r0 ≡ F1 ≡ F1` and `sustains ≡ sustains ≡ sustains ≡ descends-from`, the 28 `composed · corner A` labels, the down-blocks' 1 px ownership margin, Φ1's block 31 px from another row.
1. ★★★ **`≡` IS THE PERSON'S ACT AND ONLY THAT — roles, words and chains, one rule:** (1) a name one BY COMPOSITION is printed ONCE and never linked with `≡` — not to itself (`F1 ≡ F1`), not across the fold on a composed word; (2) two DIFFERENT names of one spelling that the PERSON identified keep their `≡`, each with its corner in the bracket the module already prints (`disjoins [A]`): `sustains [C] ≡ sustains [A] ≡ descends-from [B]`. **The rule is a predicate on the record (was this pair the person's act?), never on the spelling.**
2. **THE COMPOSED IDENTITY STATED ONCE PER SITE** — her sentence: *corner A's 14 roles stand on both sides as one — composed, not yours* — **and the per-point mark shrinks to the least that still separates composed from the person's act and from the untouched.** Its FORM is yours to propose, a default for her eye. **Never the person's glyph (`≡`, the yellow stroke); never a label repeated on most of a column; no group bracket** (AC's composed roles are not contiguous); **never a reordering** (the caster's order stands).
3. **Down-words hug their row at the up-words' 8 px.**
4. **A foot block never crosses the midpoint to the next row; where the wrap would, the rows OPEN to hold it** (Φ1's block).
5. **The face block: a corner where NOTHING returned says `nothing returned` once;** where something returned, the full split stays; the core last.
6. ★ **ONE REFUSAL GRAMMAR, every refusal at a midpoint site:** head `not taken` · the local hand `here, on <edge>: withdraw this attempt` · distant hands `at <site> (<generation>): withdraw … first`. **`refused`, `(the act just made)` and `nothing glued, the edge keeps its prior state` RETIRE.**
7. **The stone:** `not taken — Φ2 ↦ r1 would make c1 and c3 one: two roles of corner C, which the corner keeps apart.` + the local hand.
8. **The dependency box in TWO sentences, the act first, the collision second, where first:** `not taken — F1 ↦ Φ3 would make Φ3 one with F1.` / `your pair at ABAC, one generation up, needs Φ3 as its own role: r3 ↦ Φ3.` / the two hands.
9. **The dependency's reason when both names are ONE seed role reached by two parents** (`c2 would be one with c2 by the solid`) is case (1) of item 1: **never a thing one with itself** — say the role would become the solid's (composed), in the one grammar. Your wording, her eye.
10. **The card at a born vertex:** ONE row `Space · derived from A and B — 20 roles · 22 words · 52 tuples` (its own counts, derived every read) — **never a Cast row**; a born vertex holding an old LOADED cast: one line `a loaded cast — not read: a midpoint's space is derived from its parents` and no cast rows.
11. ⛔ **A FACE IS NEVER NAMED BY ITS ID** (CLAUDE.md §2.5 and §2.8): `getFaceDisplayLabel` falls back to `shortenId(faceId)` in THREE files — `Panels.tsx:4158` · `SelectedVertexRelations.tsx:769` · `VertexPacketEditor.tsx:702` (the designer saw `face face:wpx1fn` in the card's `Face opposites` row). **Compose from the face's corners by D14 (`composeCornerCycleName`), and where that yields nothing, a TRUE ABSENCE — never an id.** Sweep every person-facing face label; **and measure in the same sweep the board's old unchartered row: `Panels.tsx:3427` hands `faceDisplayName` into `CellFaceRow.displayName` — what consumes those rows?** Price by file.
12. **Two one-liners:** `stoneOn`'s docstring — *a pair SHARING a seed and pooling nothing* (it refuses a pair that shares a seed AND pools; the researcher's read) · the tripwire `by content 0` pinned at every generation your witness builds, if it is not already.
**Boundary:** `CastInsideDiagram.tsx` · `MidpointSurface.tsx` · `Panels.tsx` · `SelectedVertexRelations.tsx` · `VertexPacketEditor.tsx` · `spaceOf.ts` (a docstring) · the witnesses · the eye leg. Check each against the manifest; a frozen one stops you. **The drive family runs.**
**Acceptance (sealed):** at `ABAC` **0 composed words and 0 composed roles carry `≡`**; the person's own identifications of one spelling carry `≡` with their corners · **the word `composed` stated once per site**, the per-point mark not a repeated label · every down-block's nearest row its own, at the up-blocks' distance · **0 foot blocks across a midpoint** · `nothing returned` once where nothing returned · **0 person-facing refusals reading `refused`, `(the act just made)` or `nothing glued`**; every head `not taken` · **0 faces named by an id anywhere a person reads** · the card at AB: the `Space` row, no Cast row · sweep OK · `tsc` 0 · the eye leg ALL PASS with the new arms.
**Then the designer takes her eye to it — and Arman walks the born room on it.**

# 3 · `STAMP C-9` — THE BORN FACE *(after `C-7h`)*
**Ground:** 0031 **§3.8** (rider 4, verified §125.2) · **surface:** the designer's §125.1 rulings · **the induced walk:** §1's answer.
1. **THE ENGINE** — the face reading at gen ≥ 1 through `spaceOf`: the loop over a born face's three edges composing each edge's `J` BY ITS KIND (the carry · the anchored meet ∪ the born pairs); `Fix · Mov · Und`; **the solid part** (the reading with every born room empty) **and the EXTENSION** — each added route attributed to the born pair(s) it runs through; **a refusal names PAIRS of tuples**; `Und`'s address an EDGE OF THE BORN FACE, **its descent to the parent seed edge derived**; the domain inside `spaceOf(born corner)`. Derived every read, stored nowhere.
2. **THE SURFACE** — read at the born face's edges' SITES, in the unfolding's sources (your item-4 measurement: at `ABAC`, `AB·BC·AC` apex `BC` — medial — and `AB·AD·AC` apex `AD` — interior); **the solid part QUIET, stated once** (it is the ground); **the extension MARKED and ATTRIBUTED** to the born pair that made each added route; `Und`'s two hands in the one grammar, where first — a born pair on the born face's edge at its site · an act on the descended-from seed edge at its midpoint — **the device never chooses between them**; **an INTERIOR face: ONE block when both cells' walks agree (saying both read alike), BOTH blocks named by their cells, the host cell's first, when they differ;** the corner cell's face: no block.
3. **The seal — the researcher's `born_face_reading.py` is the reference, cited, on ITS fixtures and order** (A `flow` · B `t-cell` · C `phi` · D `triangle`; its 42 hand triples; D's edges unmapped; 40 born-pair draws per triple): solid readings determined by gen 0 (0 failures) · the medial loop moves `B`-only roles in **9 of 42** · inherited refusals **6 of 42 (T 7)** · born pairs add a refusal in **315 of 1,680** · the domain **91 of 774**, `Und` **683 = 354 · 210 · 119** · a free born slot for **354 of 354** · the two walks of the interior face differ in **92 of 1,680**. **Reproduce on the same fixtures, order and triples, or pin the shapes and say what cannot be reproduced and why.** Plus the eye: a born face read at a gen-2 site on the lawful path, its extension marked after a born pair.
**Boundary:** `faceReading.ts` (NOT_FROZEN row 218) · `spaceOf.ts` (row 219) · `MidpointSurface.tsx` · the witnesses · the eye leg. A new file under the engine roots is classified in its landing commit. **Nothing frozen.**

# 4 · U1 RECORD RIDER — at your next record commit (§125.3)
**RULED: a tracked file may not cite an untracked one.** Track **every ruling file and every instrument that a tracked file CITES, with the cited instruments' import closure**; uncited ones stay ignored until a tracked file cites them. **Match a citation as the TAIL of a filename** — the "seven missing" rulings are `.handoff/RESEARCHER_RULING_<stem>.md` (six, untracked, cited) plus one already tracked in `docs/governance/`. `.gitignore`'s own comment warns that git will not descend into an excluded directory — the negations need their parents un-ignored. **Report the list, its bytes, and any path too long for the tree.** RESULTS files are dated events — not tracked unless cited.

# 5 · W1
Carry AS FOUND: the claims ledger (§125), the front board, the connection-layer plan, **ADR 0031 — rider 4** (HEAD `sha256-LF 6c884a6b…` = `blob 34e03e62` → tree `sha256-LF ea164e5d…` = `blob 5ef9a361`).

# ECHO
ECHO §125 · the lock · the `1535` measurement with its test · `STAMP C-7h` (item 0 first) · `STAMP C-9` (after `C-7h`) · the U1 rider.

---
