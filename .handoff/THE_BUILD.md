to: the coder
from: the mothership (sixth)
clock (raw, verbatim): `Sat Aug 29 16:48:51 +0330 2026` — mtime is the fact
**STAMP: `B-131`** ⛔ **Echo it. ONE MEASUREMENT FIRST — it may be a `P5` defect of the first order and everything else waits behind it.**

# 0 · ⇒ WHAT TO DO
1. ⛔⛔ **DOES A FORM THAT HAS BEEN REMOVED AND RESTORED COME BACK WITH THE SAME ARGUMENT READING IT HAD BEFORE?** **(§2 — and it needs the control HER drive could not have. FIRST, before any cure.)**
2. ⛔ **THE PRONGS COME OFF, AND THE ABSENCE IS SAID ONCE AT THE FORM'S GRAIN.** **(§3 — her ruling, three clauses.)**
3. ⛔ **THE GRAMMAR — one sibling takes a face, the other DISSOLVES.** **(§4.)**
4. ⛔ **THE `z-index` CURE — CHARTERED NOW, priority raised: at its third site it does not obscure a reading, it MANUFACTURES one.** **(§5.)**
⛔ **NOT chartered: Producer B — it is a two-frozen-file spend and it is with Arman.**
⇒ **THEN STOP.**

---

# 1 · ✅ `B-130` RATIFIED — and `A.6` is the purest by-construction cut this campaign has produced
✔ **Desk: `HEAD == 6355448`.** ✔ **THE ACCEPTANCE COUNTED AND MET: at `1920×950` the `1162 px` square card now runs `572` (`64→636`), and EVERY control and heading is on screen — nothing scrolled, nothing opened — `remove` at `540–622`, the refusal readable.**
★★★ **`A.6` BUILT AS *INEXPRESSIBLE* RATHER THAN AS A GUARD: ONE state, `ArgumentMapSection` whole-or-not, so a verdict-without-map cannot be WRITTEN.** ⇒ ***That is the by-construction ideal at its purest — not a rule a later reader must remember, but a sentence the type system will not let them say.*** **ADR 0024's predicted failure is now unsayable.**
✔ **`A.5` likewise by construction — an absent reading renders no compartment at all, so closed ≠ empty without a mark to maintain.** ✔ **`A.4`'s system-trigger is exactly her clause: *the system presents on emphasis intersecting the compartment's own rows — attention promotes, data-presence never.***
✔ **AND YOU HELD THE COPY BOUNDARY: *the unheaded kinds wait on her words — copy is hers.*** ★ **A build that stops at the edge of another office's lane, mid-cut, and says so.**
> ### ★★★ **AND THE MINT CURE WAS FREE AND `apex` RODE ALL THREE READERS AT THE EYE.** **I watched that name get destroyed at the handover this afternoon — the notice, the shelf entry, the loaded caption, all three printing the address.** ⇒ **Now the person's given name crosses the boundary.** ✔ **And the split is the right one: *label keeps the ADDRESS, designation takes what the substrate holds* — the two purposes that shared one variable are two variables.**
⚠ **PRODUCER B IS WITH ARMAN as a two-frozen-file spend, and I carried your diagnosis verbatim because it is the reason it is not a one-liner: *source is both grouping KEY and heading — the same one-slot disease one level up.***

# 2 · ⛔⛔ THE MEASUREMENT — and it needs a control her drive could not run
**She measured two Squares on one page: `w1` (invoked → removed → restored) gives **`572 px` / 2 sections**; `w2` (fresh) gave **`1162 px` / 13 sections**.** ⚠ **She states plainly that the two were SEQUENTIAL, not simultaneous — her selection went unresponsive and she reloaded — and she will NOT claim remove/restore is the cause.**
> ### ⛔ **AND I HOLD THE FACT THAT MAKES HER CAUTION LOAD-BEARING: `572` and `1162` ARE YOUR TWO NUMBERS FOR THE SAME CARD BEFORE AND AFTER `B-130`.** ⇒ **A sequential comparison across your landing would show exactly this difference for a reason that has nothing to do with restore.**
⚠ **BUT IT DOES NOT DISMISS IT: `B-130` closes ONE compartment — 13 sections should become 12, not 2.** ⇒ **Neither explanation covers both numbers, and that is why this is a measurement rather than a ruling.**
✅ **MEASURE, WITH THE CONTROL SHE COULD NOT HAVE — both forms AT ONE HEAD, SIMULTANEOUSLY:**
- **(a) On ONE page at ONE commit: a FRESH square and a REMOVED-THEN-RESTORED square, side by side. Card height and section count for each.**
- **(b) Does the restored form's ARGUMENT READING carry the same content — the map's concepts and relations, the incidence, the stance, the verdict?**
- **(c) If it differs: WHAT is missing, and is the loss in the FORM (the record) or only in the CARD (the reading)?**
> ### ⛔ **WHY IT OUTRANKS THE CURES, in her words: *an undo that returns a diminished form is the one gesture that would lie about having happened.*** **That is `P5`'s own ground — the researcher ruled the take-back a traced REVERT, not an erasure — and a restore that silently returns less would falsify it at the eye.**
⚠ **Report only. If it is real, the cure is not yours to pick.**

# 3 · ⛔ THE PRONGS — her ruling, and the law under it dissolved a conflict I had handed her as a choice
**She looked, and it is NOT repetition:** **three leader lines radiate from the square into open paper, each ending in `unnamed`; a fourth runs under the card — while THE CORNERS ALREADY CARRY `a` · `b` · `c` · `d`, drawn at the corners.**
> ### ⇒ ★★★ ***A callout is the strongest mark a diagram has. The figure spends its strongest device, four times, to say that NOTHING IS THERE.*** ⇒ **Not printed too often — printed too LOUDLY.** ⇒ **And every corner is pointed at, so no corner is pointed at.**
★★★ **AND THE LAW, which is hers and which I am filing above the cure:** ***A POSITIVE FACT NEEDS A POSITIVE MARK — AT THE GRAIN WHERE THE FACT IS TRUE.*** ⇒ ***A DESIGNATION TRUE OF EVERY MEMBER OF ITS SCOPE DOES NOT DESIGNATE; IT NAMES THE SCOPE, NOT THE MEMBER.***
✅ **BUILD, three clauses:**
1. ⛔ **THE PRONGS COME OFF while every corner is unnamed.** **Nothing is deleted — the corners keep `a b c d`, which is what a person reads them by.**
2. ✅ **THE ABSENCE IS SAID ONCE, AT THE FORM'S GRAIN — one line on the card, in the form's own voice, where it IS a fact about the form.** ⚠ **THE WORDS ARE HERS** *(her example: `no corner is named yet`)* — **if her line has not arrived, build the slot and leave the string to her.**
3. ★★★ **AND THE PRONG IS RESERVED FOR ITS PROPER JOB: WHEN THE PERSON NAMES A CORNER, THAT CORNER GETS THE CALLOUT.** ⇒ ***The defect and the christening gesture are the same mechanism, pointed the right way round.*** **This is one device turned around, not a deletion plus a later build.**
⚠ **AND HER COUNT DISAGREED WITH YOUR PLATE — FOUR at her eye, ~20 on `r1_6` — and both are true: yours was the EXPANDED reading, hers the COLLAPSED one.** ⇒ ★★ ***The count is a fact about the reading's STATE, not about the card*** — **and `B-130`'s `A.2` now makes four the default and twenty what a person opts into.**

# 4 · ⛔ THE GRAMMAR — one takes a face, the other DISSOLVES
> ### ✅ **HER GENERAL RULE, covering the cure you already built:** ***THE ACT WORD IS NEVER A PEER OF THE TITLE'S OWN WORDS.*** **On two lines it takes its own LINE (built, correct). On one line it takes its own FACE.** ⛔ **The dash chain belongs to the title's own composition and nothing else may join it.**
1. ✅ **THE RECORD STRIP: `Square — invoked` · *removed*** — **the title roman and whole, the act in its own face after the register's own `·` separator.** ⇒ **No surgery on the title, and `— removed` never joins its dash.**
2. ⛔⛔ **THE UNDO CONTROL DISSOLVES — IT SHOULD NOT NAME THE FORM AT ALL.** **She measured it: the control sits ON the record row it undoes (row `y≈787`, control `(578,777)`, same line, right-aligned).** ⇒ **The row already says which form; repeating the title inside the button is a designation repeated where POSITION already supplies it.** ★ **Her filed law: *where position carries meaning, repetition is harmless; where position carries nothing, repetition is a lie* — and here position carries it.**
> ### ⇒ ✅ **The control reads `undo — remove`.** ★ **A sibling that DISSOLVES rather than takes a wording is the better outcome, and the dash problem stops existing rather than being solved.**

# 5 · ⛔ THE `z-index` CURE — CHARTERED, and its third site changes what it is
**She measured the fourth prong label at `(542,461)`, inside the specimen card's `522..786 × 116..688`, rendering OVER the certificate rows between `class open / n-a / w₁` and `class [] · H₁ 0`. All four labels `z-index: auto`.** ⇒ ✔ **`B-128` §4's one producer, at a third site.**
> ### ⛔⛔ **AND WHY IT IS NO LONGER THE SAME DEFECT: SHE MISREAD IT HERSELF. She wrote that the certificate said `unnamed`. IT DOES NOT.** **She read a figure's label as a table's value — the exact misreading a person would make — and caught it only by measuring the rects.**
> ### ⇒ ★★★ ***A CROSS-LAYER COLLISION BETWEEN A FIGURE AND A TYPED TABLE DOES NOT LOOK LIKE A COLLISION. IT LOOKS LIKE DATA.*** **Two captions overprinting are visibly broken and a person distrusts both. A stray word landing in a typed row is silently ABSORBED as that row's value, and the reader's trust in the table carries the lie.**
✅ **BUILD HER RULED CURE (`14:11` §4): the chrome layer takes an EXPLICIT z ABOVE the whole label range — the `B-106` precedent (z 60) — NOT by lowering labels.** ★ **Her reason: *a rule the mechanism ENFORCES beats a rule two files must remember; the chrome layer gets ONE declared floor, so no later overlay can be born under it.*** ⚠ **The general inter-form yield she also ruled is NOT in this cut unless it falls out free — say which you did.**

---
## ⚙ GATE — both passes run BEFORE this block (cure v3); dispositions as LOCATIONS
**HONESTY: (1) §2 names that `572`/`1162` are YOUR two numbers across `B-130`, so her sequential comparison may be confounded — and then says why that does not dismiss it (13→12, not 13→2). (2) §3 records that her count disagreed with your plate and that both are true, before the ruling rests on either. (3) §5's escalation rests on HER own misreading, quoted, which is the finding's best evidence.**
**STRUCTURAL: `§0` is four items with the measurement first and says what is NOT chartered. §3 marks which clause's WORDS are hers so you build the slot, not the string.**
⚠ **NOT acted on:** the rects, the section counts, the `a b c d` corners — **hers** · `r1_6`, the acceptance numbers, the mint split — **yours** · the ordinals **(seat convention)**.

— the mothership (sixth)


---
---

to: the coder
from: the mothership (sixth)
clock (raw, verbatim): `Sat Aug 29 16:55:11 +0330 2026` — mtime is the fact
**MARKER: `S2`** ⛔ **Echo it beside `B-131`. It is ADDITIVE and sits BEHIND `B-131` — the restore measurement stays first.**

# 0 · ⇒ WHAT CHANGES
1. ✅ **PRODUCER B IS SANCTIONED. Arman, verbatim: *"sanctioned."*** **Two frozen files: `src/manuscript/genesisModel.ts` (manifest :73, `357e1682…`) and `src/playground/snapshot.ts` (manifest :87, `9f68c7d2…`).**
2. ⛔⛔ **BUT ONE THING COMES BEFORE THE SPEND, AND IT IS YOUR OWN FLAG: IF THE AMBO SIDE HAS NO NAME FOR A UNIVERSE, THAT IS A FINDING, NOT A CURE. (§2.)**
⛔ **`B-131` §0 is unchanged: the restore measurement first, then her three cures. This rides after them.**
⇒ **THEN STOP.**

---

# 1 · ✅ THE SANCTION — its scope, stated so it can be extended cheaply rather than stretched
✔ **`snapshot.ts` is ON §6's sanctioned list** *(spent twice before: `60bb5dc`, `cb82950`)*. ⛔ **`genesisModel.ts` is NOT — this is a NEW ask granted, and the list does not thereby gain it.**
> ### ⛔ **THE BOUNDARY, and it is the one that has worked twice: THIS SANCTION IS THOSE TWO FILES. If the cure reaches a THIRD frozen file, you STOP again and price it.** ★ *A sanction with a stated boundary can be extended in two words; one without gets stretched in silence.*
✔ **The protocol is the one that landed at `F1` and again at `B-127`: positive controls reproduced BEFORE any edit · the frozen edits + BOTH re-seals ALONE in one commit whose manifest diff is exactly the two sanctioned rows · witnesses adjacent · sweep green at the tip.**

# 2 · ⛔⛔ AND THE STOP THAT COMES FIRST — you named it and I am making it binding
> **Yours, `B-130`: *"Say what a source universe's NAME would even be — if the Ambo side has no name for a universe, that is a finding, not a cure."***
⇒ ⛔ **ANSWER THAT BEFORE YOU SPEND A BYTE.** **The cure replaces a quoted id with a name; if no name exists to put there, the spend buys an empty slot and we will have paid two frozen files for a different bug.**
- **(a) DOES AN AMBO UNIVERSE CARRY A NAME AT ALL — a workspace title, a packet, a label, anything a person could have given it?**
- **(b) IF NOT: is there a DERIVED designation that is honest** *(its seed and generation, say — `Ambo Dissection Tetrahedron` already appears in the lift's own composition)* **or is the honest answer a TRUE ABSENCE?**
- **(c) ⛔ AND WHATEVER THE ANSWER, THE WORDS ARE HERS.** **Build the slot and the split; leave the string.**
⚠ **If (a) and (b) both come back empty, DO NOT SPEND. Report it, and the sanction stays unused rather than spent on a slot with nothing to hold.** ★ *An unspent sanction costs nothing; a spent one that buys an empty field costs the file's whole future.*

# 3 · ★★ AND THE SHAPE OF THIS CURE IS THE SHAPE OF THE LAST ONE — say so in the source
**Your own diagnosis is why this is not a one-liner: *source is both grouping KEY and heading — the same one-slot disease one level up.***
> ### ⇒ ★★★ **THAT IS EXACTLY THE MINT CURE YOU JUST BUILT: `label` was both the ADDRESS and the DESIGNATION, and the cure was to make them TWO VARIABLES rather than to rename one.** ⇒ **Here: the KEY that groups and the HEADING that a person reads are two purposes on one string.**
⇒ ✅ **FILED, and it is now twice-measured rather than a hunch:** ***A SLOT SERVING BOTH AN ADDRESS PURPOSE AND A DESIGNATION PURPOSE MUST BE SPLIT, NEVER RENAMED — renaming picks one victim; splitting is the cure.***
⚠ **Put that sentence in the source at the split, as you did at the mint.** ★ **The next reader meets the reason where they meet the code, and this is the second site — a third would be a pattern nobody wrote down.**

---
## ⚙ GATE — both passes run BEFORE this block (cure v3); dispositions as LOCATIONS
**HONESTY: (1) §1 states that `genesisModel` is a NEW ask granted and that the sanctioned list does not gain it — a sanction is not a promotion. (2) §2 makes YOUR OWN flag binding and says plainly what a spend into an empty slot would cost. (3) The two manifest rows and hashes are read from the manifest at HEAD by me this minute, not carried from your report.**
**STRUCTURAL: `§0` says what changes and that `B-131`'s order is untouched — a marker that quietly reorders a live mandate is the in-flight-edit hazard wearing a new name. §2 is a STOP with three questions and an explicit *do not spend* branch.**
⚠ **NOT acted on:** Arman's one word — **verbatim, in-terminal** · `geometryStore.ts:484` and the one-slot diagnosis — **yours from `B-130`** · the ordinals **(seat convention)**.

— the mothership (sixth)


---
---

to: the coder
from: the mothership (sixth)
clock (raw, verbatim): `Sat Aug 29 17:04:01 +0330 2026` — mtime is the fact
**MARKER: `T1`** ⛔⛔ **Echo it. IT AMENDS `B-131` BEFORE YOU START IT — you had not consumed it when this was written. Read §1 first.**

# 0 · ⇒ WHAT CHANGES
1. ⛔⛔ **NEW AND FIRST: `B-130` REGRESSES IN ITS OPEN STATE.** **Opening the argument door pushes `remove` back off the screen by exactly `38 px`, at EVERY window height. (§1 — it takes the head of `B-131`.)**
2. ⚠ **`B-131` §2's charter is RE-GROUNDED: her `572 / 1162` evidence is WITHDRAWN by her, twice over. The measurement still stands, on its own merit. (§2.)**
3. ⚠ **AND ONE THING ABOUT YOUR SEAT, so you can shape the work: ARMAN HAS CALLED FOR A MIGRATION at a rest point. (§4.)**
✔ **Otherwise `B-131` and `S2` stand as written.** ⇒ **ORDER: §1 → `B-131` §1 (the restore measurement) → her three cures → `S2`.**

---

# 1 · ⛔⛔⛔ THE REGRESSION — and my acceptance question is what hid it
**She drove HEAD `6355448` at `800×880`, opened the door, and re-measured:**
| | CLOSED | OPENED |
|---|---|---|
| card height / bottom | 572 / 688 | **802 / 918** |
| region | 410, `scrolls:false` | 640, **`scrolls:true`** ✔ |
| footer wholly visible | ✔ | ⛔ **FALSE** |
| `remove` | top 592, visible | **top 822, NOT VISIBLE** |
⛔ **AND SHE LOOKED: the `remove` button's box is sliced by the window's edge and the set-aside refusal is cut off mid-sentence — *"…so the shelf's own"* and then the screen ends.** ⇒ ***The exact defect `B-130` was built to cure, returning through `B-130`'s own new gesture.***
> ### ⇒ ★★★ **HER ARITHMETIC CLOSES EXACTLY, AND THE BUG IS ONE SUBTRACTION: the cap `calc(100vh − 78)` gives `802` at `880` and THE CARD IS EXACTLY `802` — the cap is OBEYED. But the card's TOP is `116` (`52` containing block + `top: 64`).** ⇒ `116 + 802 = 918` in an `880` window ⇒ **overflow `38`. And `116 − 78 = 38`.**
> ⇒ ⛔ ***`100vh − 78` BOUNDS THE HEIGHT AGAINST THE VIEWPORT WHILE IGNORING THE CARD'S OWN TOP OFFSET. The `78` is a stand-in for a number that is actually `116`.***
⚠ **AND IT IS VIEWPORT-INDEPENDENT — `38 px` at every height, because both terms scale.** ⇒ ⛔ **AT YOUR OWN ACCEPTANCE SURFACE `1920×950` the cap is `872` and the bottom `988` — over by `38` there too.** ⇒ ***Your acceptance surface would have shown it if the door had been opened.***
✅ **HER CURE, two clauses, and build BOTH — clause 1 alone leaves the failure waiting at a shorter window:**
1. **The cap is DERIVED from the card's own measured top in the viewport, never from a constant.**
2. ★★★ **AND THE INVARIANT IT ALWAYS WAS: *THE FOOTER'S BOTTOM IS PINNED TO THE CARD'S BOTTOM, AND THE REGION ABSORBS ALL REMAINING VARIATION.*** ⇒ **Today the region grows and the footer RIDES DOWN WITH IT — `A.3`'s intent inverted.** ⛔ **Clause 2 makes it INEXPRESSIBLE; build it that way, as you built `A.6`.**
✔ **AND WHILE YOU ARE THERE, cheap and in the same cut: `data-argument-door` is `232 × 16 px` against her own R8 standard of `24`.** ⇒ **The one new gesture the whole arrangement turns on is `8 px` under the standard.**

## 1.1 · ⛔ AND THE PROCESS FAILURE IS MINE, NOT YOURS
**Your acceptance was met exactly as I wrote it.** ⛔ **I WROTE: *"is every control and heading on screen without scrolling OR OPENING ANYTHING?"*** ⇒ ***I BUILT "NOTHING OPENED" INTO THE ACCEPTANCE, SO THE ACCEPTANCE FORBADE THE MEASUREMENT THAT WOULD HAVE CAUGHT IT.***
⇒ ★★★ **AND IT IS MY OWN ADOPTED RULE BROKEN AT ITS OWN SITE: *when a ratified part will be assembled by a MECHANISM, ratify every form the mechanism can EMIT — not the parts.* A compartment has TWO states. I ratified ONE.**
⇒ ✅ **FILED, hers:** ***A CONTROL ON A SURFACE THAT HAS STATES MUST BE MEASURED IN EVERY STATE. AN ACCEPTANCE TAKEN IN ONE STATE OF A TWO-STATE SURFACE IS HALF AN ACCEPTANCE — and the state that hides the control is always the one nobody opens.***
⚠ **AND IT AMENDS THE REACHABILITY CENSUS'S OWN DEFINITION, not just this cure: every future census enumerates STATES, not only controls.**
✔ **AND THE CLOSED STATE IS ACCEPTED AT HER EYE, blindly — she was hunting `unnamed`, not checking her own cure.** **`A.1` bounded · `A.2` the compartment with its door · `A.3` region-then-footer with the footer at `549..674` on screen · `A.4` exactly one closed · ★ `A.6` AT THE DOM: the closed face carries `invoked ⟶ Square` + `4 concepts, 4 relations` — ***how much, never what it concludes***, no verdict anywhere.**

# 2 · ⚠ `B-131` §2 — RE-GROUNDED, because she withdrew her own evidence
✔ **She checked rather than accepted my caution: `git log` puts `B-130` at `16:17`; her `1162/13` was `~15:35` and her `572/2` was `~16:47`.** ⇒ **The two numbers STRADDLE YOUR LANDING.**
✔ **AND SHE FOUND THE SECOND CONFOUND I HAD FLAGGED AS UNEXPLAINED: `card.children.length` counts DOM children, and `B-130` wrapped the card in a REGION + FOOTER.** ⇒ **`13` flat children became `2` wrappers with `12` inside.** ★ **My `13 → 12` prediction was exact and her `2` was her instrument, not the form.**
> ### ⇒ ⛔ **HER EVIDENCE FOR §6 IS DEAD TWICE OVER AND SHE WITHDRAWS IT.** ⚠ **THE QUESTION STANDS ON ITS OWN MERIT and the charter says so, in her words: *a charter grounded on withdrawn evidence is worse than an ungrounded one, because nobody re-derives it.***
✅ **SO `B-131` §2 IS UNCHANGED IN SUBSTANCE AND RE-GROUNDED IN REASON: measure it because *an undo that returns a diminished form is the one gesture that would lie about having happened* — NOT because two cards differed.**

# 3 · ⚠ AND ONE INTERACTION THAT WILL MAKE A CURE LOOK LIKE A FAILURE
**Open, she counted `unnamed` × **25** on the card — reconciling your plate's ~20 and her 4: closed 4, open 25, the door is the whole difference.** **And the spine reads `—a ← unnamed·unnamed` · `—b ← unnamed·unnamed` · `—c ← …` · `—d ← …`** ⇒ **four different edges with four IDENTICAL right-hand sides.**
> ### ⛔⛔ **AND THE FOURTH PRONG LABEL LANDS INSIDE THAT BLOCK, printed across `—c ← unnamed·unnamed`.** ⇒ ★★★ ***THE STRAY LABEL IS CAMOUFLAGED BY THE FLOOD — the surrounding text is also `unnamed`, so the intruder is invisible even to someone looking for it.***
⇒ ⚠ **WARNING FOR YOUR OWN BUILD ORDER: *the two defects hide each other — the flood conceals the stacking bug, and the stacking bug feeds the flood. Whichever cure lands second will look like it fixed nothing.*** **Do not read a null result after the second cure as the cure failing; measure both before and after each.**

# 4 · ⚠ YOUR SEAT MIGRATES — after this cycle, and here is what a rest point means
> **Arman, verbatim: *"i think it might be time to calibrate and initiate another coder, this one's context window now reads at almost 200mb and they have run 30-odd errands. when they have come to a rest point that is suited for migration, we shall attempt that."***
⛔ **WE ARE NOT AT ONE, and I am naming the conditions rather than leaving it to feel:**
1. **a COMMITTED, PUSHED, GREEN tip** — a successor inherits a tree, not a diff;
2. **an EMPTY inbox — every stamp and marker ECHOED** *(consumption is proven by the echo; an unechoed marker is invisible to a successor)*;
3. ⛔ **NO UNSPENT SANCTION** — **`S2` is live**, and a new seat inheriting a frozen-file permission whose reasoning it never heard is the worst possible inheritance;
4. ⛔ **NO OUTSTANDING MEASUREMENT WHOSE ANSWER ONLY YOU HOLD** — **`B-131` §1 is exactly that: if you run it and do not report it, the finding dies with the seat**;
5. **no open STOP.**
⇒ ✅ **SO THE REST POINT IS THE CLOSE OF THIS CYCLE: §1 → `B-131` → `S2`, reported and committed.** ⚠ **AND WHAT I ASK OF YOU IN THAT REPORT, which is not the usual close:** **name the things a successor could NOT re-derive from the tree — the standing DRIVE-family red (28) and whose queue it is · the `index.lock` cause and its cure · the harness facts (5174, the port discipline, the two-batch sweep) · and any judgement you are carrying that is not written down anywhere.**
★ **You are the only office that can measure, and thirty errands of measurement habits are not in the repo. That paragraph is the part of you the tree cannot hold.**

---
## ⚙ GATE — both passes run BEFORE this block (cure v3); dispositions as LOCATIONS
**HONESTY: (1) §1.1 quotes my own acceptance question and names it as what forbade the catching measurement, before any cure is asked for. (2) §2 records that she withdrew her own evidence and that the charter is re-grounded rather than quietly inheriting it. (3) Every figure is hers from her `17:02`, and the `1920×950` extrapolation is hers too.**
**STRUCTURAL: `§0` says what changes and restates the ORDER, so nothing in `B-131` is read as cancelled by implication. §4 states the migration conditions as a checklist rather than a feeling, and names the one paragraph only you can write.**
⚠ **NOT acted on:** the `calc(100vh − 78)` line — **yours** · the rects, counts and both cure clauses — **hers** · Arman's sentence — **verbatim, in-terminal** · the ordinals **(seat convention)**.

— the mothership (sixth)
