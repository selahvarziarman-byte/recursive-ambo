# RULING — THE REFRAME: the passage is not found, it is forced; what populates it is the AMALGAM; the neighbor we were ignoring is colimits (amalgamation), and it corrects the hinge's frame while keeping its computation

- **Seat:** researcher · **Date:** 2026-09-06 · at `b67e728` · computed: `.handoff/instruments/connection_layer_reference/amalgam_pushout_flow_phi.py`.
- **Arman:** *"the engine is not about 'there is a passage and we find it' — quite the opposite. Given that there MUST be passages (the geometry of the ambo, its facts and constraints), how is the passage populated? When we force two disparate spaces onto the tetra's vertices, what is the passage that allows that? I suspect a mathematical neighbor focusing exactly on that question that we are ignoring."*
- **Verdict: he is right, and I own the frame error. The hinge computation stands entire; its FRAME was wrong.** I asked "does a relation-preserving map exist?" — an existence question — and stopped at the SPAN (the shared core). The geometry does not ask whether the edge exists; the edge is there and is walked. **The neighbor is the theory of COLIMITS — amalgamation (Fraïssé), pushouts of relational structures, and their monodromy — whose whole content is: when no direct morphism exists between two structures, the universal construction supplies the canonical object both embed into. The passage is never missing. What varies is what identifies.**

---

## 1 · THE CORRECTION — the span was not the object; the cospan is, and it always exists
What I computed as "the maximum carried core" is a **span**: `J → Flow`, `J → Φ` (four roles, a type identification τ). I treated it as a partial map with leftovers marked *did not carry*. **The neighbor completes the span to a COSPAN by the pushout: `M = Flow ⊔_J Φ`** — the amalgam — **and the pushout always exists in the category of relational structures (the amalgamation property holds trivially: glue along J, take the union of the rest).** Computed on the two casts: `|M| = 14 + 9 − 4 = 19` roles, `21` types, `51` relations (`34 + 22 − 5` coinciding on J); **Flow → M total, injective, all 34 relations preserved; Φ → M total, injective, all 22 preserved.** ⇒ ***There is never a missing passage. The passage between two disparate spaces is the cospan through their amalgam, and the geometry's midpoint is where the amalgam lives.***

## 2 · WHAT POPULATES THE PASSAGE — the born midpoint is the amalgam
The ambo dissects every edge at a midpoint — a BORN concept (0028; its name his, its reading derived). **RULED: at the concept-space level, the born midpoint of the edge `A–B` IS `A ⊔_J B` — the amalgam of the two parent spaces over what they share.** This is 0028's COMPOSED clause made exact for spaces: **composition = colimit** (D14's `A·B`, which for names was a join and for the global fiber an interpolant, is for spaces the amalgamated sum). Consequences, each computed on Flow–Φ:
- **"Did not carry" is abolished as a negative.** The ten Flow-only and five Φ-only roles are M's own content — positive presence. The uncarried roles do not fail to travel; **they arrive at the midpoint and are at home there.** A walker carrying *the finger* from Flow toward Φ reaches the midpoint holding it; the finger has no Φ-counterpart, so it stays at the midpoint — which is exactly what a born concept between *what moves* and *what stays the same* should contain.
- **The midpoint's size measures the parents' disparity:** `|M| − max(|A|,|B|)` = 5 here. `J = ∅` gives the disjoint union (`23`: *Flow beside Φ*, sharing nothing); `J` = an isomorphism gives `M ≅ A ≅ B` (the parents were the same concept). **The gradient from 23 down to 9 is the whole range of "how much these two concepts are one."**
- ★ **The stone is INTACT — not reopened.** Each half-edge (`A → M`, `B → M`) is a total injection: undoable, no pooling. **Traversing the full edge `A → B` is forward through one injection and BACKWARD through the other** — and walking back through an injection is exact on its image and undefined off it. The "partiality" of the hinge ruling was never a weakening of the stone; it was the backward half of a traversal, in the stone's own vocabulary. *This replaces "totality dropped" — nothing was dropped.*
- **The residue, derived rather than given:** around a loop of edges, the colimit of the loop's diagram identifies roles across its vertices; **the residue at a basepoint is the MONODROMY of those identifications — which basepoint roles the cycle identifies with which.** Three outcomes per role, now canonical: identified with itself (returned unchanged) · with another basepoint role (returned changed) · with no basepoint role (did not return — it lives at some midpoint of the loop). The EMPTY-CORE vacuity survives as: a loop whose colimit identifies no basepoint role with any basepoint role.

## 3 · WHAT IS GIVEN, WHAT IS DERIVED — and the op-set boundary, preserved
- **GIVEN:** the concept-spaces (cast) and **J — the identification of roles across two concepts** (*"Flow's selective transport IS the phylum's form"*) with its type identification τ. **The device OFFERS the computed candidate cores (by size); the person CHOOSES J; the machine never mints it.** J may be smaller than the maximum (identify less; a larger midpoint) or empty (*these two share nothing* — a finding, and a lawful edge).
- **DERIVED:** the amalgam; the monodromy; the size gradient. Register 2, re-derivable, never stored.
- ⛔ **The op-set does not bend, and here is the exact reason it looks as if it might:** J identifies ROLES inside the concept-spaces attached to the cells; the op-set identifies CELLS of the complex. The tetra keeps four vertices and six edges; nothing in the geometry is glued. **The connection layer is a DIAGRAM of concept-spaces over the ambo's incidence — an assignment of a structure to each cell with amalgamation along incidences (a cosheaf on the complex): a FIELD on the complex, never an identification OF it.** The boundary I drew in the gauge reading holds verbatim.
- ⚠ **The stone, re-read one more time:** J is an identification of roles — and identification was the op-set's word. The distinction is grain (roles in fibers vs cells of the base) and effect (the base is untouched). Filed so nobody reads this as the layer quietly becoming the op-set.

## 4 · THE NEIGHBOR, NAMED PRECISELY (so it can be used, not gestured at)
- **Pushouts / colimits of relational structures** — the canonical mediating object when no direct morphism exists; **Fraïssé amalgamation** as the guarantee and, for a whole class of concept-spaces of one signature, the **Fraïssé limit** as the universal structure containing every amalgam (Φ9 — the possible form — at the level of concept-spaces).
- **Kan extensions** — the canonical extension of a partial map to a total one ("all concepts are Kan extensions"); the pushout is the instance for spans.
- **Profunctors / spans as generalized morphisms** — the formal home of passages that are correspondences rather than functions; the hinge's partial map is a span, i.e. a profunctor of a simple kind.
- **Ehrenfeucht–Fraïssé games / partial isomorphisms and, computationally, structure-mapping (Gentner's SME)** — the theory of HOW MUCH two structures identify, graded by extension depth; **this is what I actually computed (the maximal common substructure)** and it gives the graded notion the global fiber lost: *k-equivalence* as "how far along the edge the two concepts remain one."
- **Descent / cosheaves on a cell complex** — 0028's DESCENT clause is the gluing condition for the diagram; the Bianchi identity is its cocycle condition on a closed tetra.
⇒ **One sentence for the record: the device, at the concept level, is a diagram of relational structures over the ambo's incidence; every born cell is the colimit of its parents over what they share; the residue is the monodromy of the colimit around a cycle. The geometry forces the diagram; the universal construction populates it; the person's gift is which roles are one.**

## 5 · WHAT THIS DOES TO THE HINGE RULING AND THE PLAN
- **Hinge §2.1–2.2 REWORDED, not retracted:** the passage is the cospan through the amalgam; "partial injective map" was the span; **the stone holds intact (each half-edge a total injection); "totality dropped" is withdrawn.** §2.3–2.6 (τ as content; candidates offered; three-outcome residue; empty core; directedness) stand, now derived.
- **The born midpoint's reading (Δ52's placeholder, asked again under P0) is ANSWERED: the amalgam.** Not a name-join, not an interpolant of amounts: the amalgamated structure of its parents.
- ⚠ **Conjecture, not ruled, worth one computation:** the eight-step subdivision's interior stations may be a FILTRATION of the amalgam by relational distance from J (station `k` = A amalgamated with the part of B within `k` relation-steps of the core) — a derived graded series with no amounts. If it holds, the "decreasing series" returns in structural form.
- **For the mothership:** C-3SURF's slot is now *choose J from offered cores*; the surface must show the midpoint's population (shared / A-only / B-only); Step 1's residue is the monodromy of a triangle's colimit — three outcomes, empty-core guarded. **Three spaces are needed for the first monodromy; two give only the first midpoint — which is itself a reading worth showing: the amalgam of *what moves* and *what stays the same*.**
