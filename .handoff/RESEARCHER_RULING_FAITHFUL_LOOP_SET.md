# RESEARCHER RULING → Engineer (build Phase 2 to this) + Mothership · The faithful loop-set = a CERTIFIED H₁ basis; class is certified (researcher/`globalW1`), representative is craft (designer). Option A is its realization for single-polygon closed surfaces; Option B fills what A misses.

**To:** Engineer office (operating, c173e0ca) + Mothership · **From:** Researcher (seat) · **Via:** Arman · **Date:** 2026-07-08 · **Self-contained.**
Grounded on the domain faithfulness law (`docs/CONTEXT.md` #5 — "the whole assembled complex with its signed loops") and the committed `globalW1` (`basisCycles`).

## §0 The faithful set, in one line
The manuscript draws a **certified basis of H₁** — `b₁` loops covering the free generators **and** the torsion generators — as **any certified representatives**. Two clean layers:
- **The CLASS** (is a loop a genuine H₁ generator, not invented?) — **researcher-certified**, grounded in `globalW1`'s computed basis.
- **The REPRESENTATIVE** (which homologous cycle to draw) — **designer craft**; all cycles in a class are equally faithful.

**Option A (boundary-identification generators) IS this set, realized, for single-polygon closed surfaces.** **Option B (`globalW1.basisCycles`) is the general set**, required exactly where A is incomplete. Not A-vs-B — A is B's realization for the closed zoo; use A's boundary representative where it exists (boundary-aligned, clean), else a B representative (interior cores).

## §1 Part 1 — Option A is complete-and-faithful for single-polygon CLOSED surfaces: CONFIRMED (canon)
**Proof (grounded):** a closed surface presented as **one fundamental polygon** with boundary-identifications has a CW structure of **one 2-cell** whose **1-cells are exactly the identified boundary edge-classes**. So `H₁ = ker∂₁/im∂₂` is generated by those edge-classes — **the boundary-identification set IS a generating set of H₁**. Complete and faithful for **torus (a,b) / Klein (a,b) / RP² (a·b) / genus-g (2g) / N_k (k)** — the whole canonical closed zoo. **State as canon.** (Verified against Phase 1's trio: torus→2, sphere→0, RP²→1. And `selectIdentifiedLoops`' derivation proof — blank the word → 0 loops — confirms it's derived, not invented.)
- **Flag (the boundary of A's completeness):** a closed surface **not** presented as a single polygon — a multi-face assembly, an ambo'd mesh, a subdivided complex with **intrinsic interior cycles** — can have H₁ generators the boundary word does not reach. For those, A is incomplete → use B (§4). (Disk patch-lifts are safe: a disk interior is contractible, so its H₁ comes only from the boundary self-glue — A stays complete. My route-B disk-precondition guarantees this.)

## §2 Part 2 — open surfaces (cylinder / Möbius): 0 loops is a PARTIAL ERASURE — DRAW the core
An open surface with `b₁=1` **has a real generator** (the core circle). Drawing **0** loops leaves a genuine H₁ generator **undrawn** — an **erasure**, honestly captioned but still incomplete. The faithfulness law is "draw the real generators"; a `b₁=1` form must draw its **one** generator. **Ruling: draw the interior core.**
- **The core is a certified generator** — `H₁(cylinder)=H₁(Möbius)=Z`, provable; it is **not invented**. The current "0 + n-a" is **honest but incomplete** (not a violation — it draws no fake loop and flags the gap — but Phase 2 should complete it).
- **Derivation (certified):** the core is a basis cycle of `globalW1.basisCycles`. **CLASS certified by `globalW1`; REPRESENTATIVE (the roundest core) is craft.**
- **Build caveat to verify:** confirm `globalW1` returns `b₁=1` + a core `basisCycle` on the **open** (bounded) cylinder/Möbius complex — its barycentric machinery was tuned for *closed* assembly; if boundary edges (in 1 face) need handling, that is a small certified extension (H₁ of a bounded 2-complex is standard `∂₁/∂₂`). The **ruling stands** (draw the certified core); the extraction is the engineer's, certified by `globalW1`.

## §3 Part 3 — RP² pinch-mark: FAITHFUL as-is; the rounder is craft, not a faithfulness fix
The ℤ/2 generator **is drawn** (present, coloured, legended) — **faithfulness is satisfied**. That it reads as a **pinch-mark** on the cross-cap immersion (the identified boundary collapses onto the self-intersection) is **awkward, not unfaithful**. A rounder representative is a **craft improvement**.
- **The split the engineer asked for:** the **ℤ/2 CLASS** (RP² has exactly one torsion generator) is **researcher/`globalW1`-certified** (grounded: `H₁(RP²)=ℤ/2`). **WHICH homologous cycle** to draw — the boundary pinch-curve **or** a rounder interior cycle — is **designer craft**: any cycle in the ℤ/2 class is equally faithful.
- **Charter:** a rounder RP² is **authorized as a designer representative choice**, *provided the chosen cycle is certified to lie in the ℤ/2 class* (a `globalW1` basis cycle in that class — not a hand-drawn guess). Certification bar: **researcher/`globalW1`**; selection among certified representatives: **designer**.

## §4 The unifying principle (the A/B resolution + the certification bar)
**Faithful = every drawn loop is a certified H₁ generator, and the drawn set is a full basis (`b₁` loops, free + torsion).** Mechanically:
- **The certified set already exists in committed code:** `analyzeGlobalW1(...).debug.basisCycles` = a certified Z/2 H₁ basis (`b₁` cycles, counting torsion generators like RP²'s ℤ/2). **That is the faithful set.**
- **Option A** (boundary-identification loops) **realizes** that basis for single-polygon closed surfaces — prefer it there (boundary-aligned, already proven-derived).
- **Option B** (`basisCycles` → immersion) supplies the representatives A cannot reach: **open-surface cores** and **intrinsic cycles** of non-single-polygon forms.
- **The bar for "not invented":** a loop is faithful **iff** it is certified to lie in a `globalW1` H₁ class. Class = **researcher/`globalW1`**; representative = **designer**. This is the definition of "faithful" for the loop-set.

## §5 Answers to the three, direct
1. **Yes** — Option A is complete-and-faithful for single-polygon closed surfaces (torus/Klein/RP²/genus-g/N_k); state as canon (§1). *(Caveat: non-single-polygon closed forms with intrinsic cycles need B.)*
2. **Draw the core** — 0 loops is a partial erasure of a real `b₁=1` generator; certified via `globalW1.basisCycles`; class certified, representative craft (§2).
3. **Pinch-mark is faithful**; a rounder is **chartered as craft**. Cycle-**class** = researcher-certified; cycle-**representative** = designer choice (§3).

## Boundary
Mine (ruled, grounded): the faithful set = a certified H₁ basis; A complete for single-polygon closed surfaces (canon); the open-surface core must be drawn (0 = erasure); RP² pinch-mark faithful, rounder = craft; the certification/representative split (class = researcher/`globalW1`, representative = designer). Not mine: the visual representative craft (designer), the `basisCycles`→immersion build + the open-surface `globalW1` verify/extend (engineer). Non-blocking on Phase 1. Phase 2 draws exactly the certified basis; where A realizes it, canon-as-is; where it doesn't (open cores), the small B extension, certified by `globalW1`.
