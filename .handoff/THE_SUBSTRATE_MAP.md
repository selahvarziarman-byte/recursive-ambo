# THE SUBSTRATE MAP — the person's world, read end to end

**Coder seat · regenerated at the B-105 tip (post-`e788ed3`; cite SHAs, never dates) · every fact below carries its file:line, read from the WORKING TREE == HEAD THIS SESSION (`git diff HEAD -- src` empty at write time) — no anchor inherited unverified except where a row says CARRIED.**
**Read at wake. Regenerate at every arc boundary.** *Predecessor baselines: `6436736` (3rd seat's full walk) → `3c4ff8d` (render-arc scope) → THIS (B-105 A1, Arman-ruled Δ16.3): the walk (rung 1, CLOSED at Arman's hand 2026-08-21), the aperture/verdict arc, persistence, the legibility arc (the card · the names · the affordance line), and rung 2 (the deck-tiling, ADR 0025 as amended §7) all postdate the old baselines and are freshly grounded here.*
**Rung discipline: every claim here is RUNG 1 (committed bytes / import closure) unless it cites a measured artifact.** LINKED ⇒ MOUNTS ⇒ REACHABLE ⇒ REACHED — a claim about "the person" made from rung 1 is not a claim.

---

## 0 · THE ENTRIES — what runs where (`src/main.tsx:16-25`, re-read)

| route | what | status |
|---|---|---|
| **(default)** | **`AppShell` = [ Ambo Universe ⇄ Manuscript ]** — both mounted, visibility toggle | **PRODUCTION** |
| `?playground` | the standalone playground shell (`main.tsx:24` — `dev && params.has`) | dev-only |
| `?manuscript` | standalone ManuscriptView (lazy) | dev-only |
| `?design` | designer workbench (`main.tsx:25`) | dev-only |
| `?field` / `?witness` | DirectorFieldRenderV0 (C.1 landed in the specimen; retirement still open) | dev-only |

> ### ⚠ **THE PRODUCTION PERSON LIVES IN AMBO + MANUSCRIPT.** The playground is a dev shell — but it is where the ambo-side invariants panel prints (`PlaygroundInvariantsPanel.tsx:59` — the `b₁` row: level-1 branch field-independent; cert branch **names its ring `(ℤ/2)`**, B-105 B1).

## 1 · THE STORES

- **`src/store/geometryStore.ts` (973 lines)** — the ambo workspace. Seed selector over `seedRegistry` (tetra · octa · cube); the lift set; `liftSelectionToManuscript` (`:454`) · `thickenLiftToManuscript` (`:171` decl) · **`closeSegmentManuscript` (`:595`)** — P1 THE LOOP-MAKER: the fold word on a SEGMENT closes it into the circle through `closeSegmentIntoLoop` (`src/lib/closeEdgeIntoCircle.ts:138`), no rim panel, the segment parent riding the snapshot ancestry. *The old map's §7.5 gap ("closing an arc into a circle — missing") is CURED: the door exists and the manuscript's fold chip fires it on segments (`ManuscriptView:4022-4032`).*
- **`src/store/playgroundStore.ts` (484)** · **`src/store/liftStore.ts` (48)** — the ambo→manuscript channel ({title, file} push/drain), CARRIED (spot-verified: both files exist at these sizes; internals not re-walked this boundary).
- ### ★ **`src/manuscript/pageStore.ts` (251, NEW since the old baselines)** — the MANUSCRIPT PAGE STORE: the page's built rooms + **`foldedBodies` (`:81`)** + **the LEDGER (`recordBuilt` `:104`, RECORD-NOT-READING: door + inputs verbatim; `loadPage` `:110` returns NAMED per-record refusals)**. §2B persistence: **save the page… / load the page…** buttons on the sources shelf (`ManuscriptView` — the restore RE-RUNS each recorded door, so a folded verdict reproduces its body). ⚠ MEASURED (B-102/B-104/B-105 drives): an HMR touching this store's import graph (apertureModel · writtenFormModel) RESETS the page — drive-relevant, not person-relevant.

## 2 · DOOR 1 — THE AMBO (production; CARRIED strata spot-verified)

- Ops registry: ambo dissection · pyritohedral diagonalization (2, exactly — CARRIED).
- **THE LIFT:** shift-click rows (`Panels.tsx:2107-2129` — `toggleLiftSelection`, re-read) → downward closure → `extractSubShape` → serialize → `liftStore.push` → the manuscript shelf drains it. Intervals AND circles lift (the person's S¹ — CARRIED, §7 of the old map still true by the loop-maker's own existence).
- **THE THICKEN BUTTON** (their circle × I → the band beside its parent, non-consuming product) — CARRIED.
- ⚠ `subComplexLift.ts` is grep-BINARY at HEAD (committed NUL; instruments must `grep -a`) — CARRIED, still true (the B-104 witness idiom works around it).

## 3 · DOOR 2 — THE MANUSCRIPT (production; `ManuscriptView.tsx` = **6139 lines** at this tip; every anchor re-read this session)

### 3a · The written row — invoke · the dock · the gestures
- **INVOKE** (right-click paper → palette): Segment · Triangle · Square · Pentagon · Hexagon (`writtenFormModel.invokePrimitive`). An invoked polygon's corners are **BORN = unnamed** (the naming doctrine's fourth case) — load-bearing for the fold tap and the roles rows below.
- **THE OP DOCK:** `DOCK_OPERATION_GROUPS` (`writtenFormModel:121-…`) — **EIGHT groups**: glue · flip-glue · collapse · cut · dualize · sew · **fold** · **thicken**, plus the **identify** and **explore inside** gesture chips (`DockGroupKey` union — the trapdoor class is a compile error now). Availability rows carry `reason` when shut (measured B-105: total on the loop — every shut door says why).
- ### ★★ **THE FOLD — the 7th word, and its pick is now ON THE DRAWN POLYGON (B-105 W3 §1, designer-ruled P.1–P.6):** the chip opens `FoldGatePanel` (`ManuscriptChrome:1710` region — **no edge list, no addresses**; pair rows `a·a` + mode arrows, each wearing its pair's hue) and mounts **`FoldTapOverlay` (`ManuscriptView:822`)** on the risen figure: tap an edge → it pairs (the one reducer `tapFoldEdge`, `handGestureModel:83`); **pending dashed · decided solid · one hue to a pair · the tick is the run's first corner and the run shows the mode (→⇄ reverses the partner's tick)**; the legend rides the figure (the aperture's ratified string, one word changed: EDGES). Hit meshes NAMED `fold-edge:{slot}`; targets + state published on the **`__manuscriptFold` dev seam** (the CycleTraceOverlay idiom). ⚠ MEASURED (B-105, at the eye): the tap fires on **POINTERDOWN** — the selectable's inert single-CLICK handler stops the click chain from closer ink hits; the correspondence pick layer stands down while the fold is open (its colliders stole the first tap). On a SEGMENT the same chip closes the loop instead (`:4022-4032`) — no panel.
- **Fold commit** → `applyFoldTo` (`handGestureModel:131`) → `executeCustomGlue` → the born immersion (torus abAB · Klein abaB · RP² abab · 3 cross-caps aabbcc · …). **The fold begat both acceptance surfaces of rung 2's drives.**
- **COMBINE** (shift-click pair) → connectedSum with the subdivide path + the aimed-chord fork — CARRIED (spot-verified: `refineToDisk` pass-through catch now at `:3782-3789`; it still swallows `surfaceRefinement`'s four throw families — see §8.3).
- **IDENTIFY (cycle-identify):** the trace gesture — entry gate at `:4048-4082` (**B-105 §4c: the entry sentence for single-face quotients ends at "(glue / flip-glue on the face)" — the constant-list pointer clause is CUT; the computed affordance line answers the reroute question, once**); `CycleTraceOverlay` (`:645`) with named `trace-tail:` targets; the ring (`CorrespondenceRing`, `src/components/`).

### 3b · The card — the argument reading + the affordance line + the records
- **`SpecimenCard` (`ManuscriptView:1250` region):** title · subtitle · **the AFFORDANCE LINE** (`data-affordance-line`) — `composeAffordanceLine` (`affordanceLine.ts:40`, **return type `string`**: the line is TOTAL including zero — **B-105 W3 §4(b): the empty open set SPEAKS her sentence verbatim, `EMPTY_AFFORDANCE_SENTENCE = 'this form takes — nothing · each door says why'` (`:22`)**; MEASURED: no page form composes empty at this tip — the loop takes `identify`, the arc `fold · identify` — the sentence stands ready, witness-pinned) · the quotient bound (closed volumes) · **the ARGUMENT READING** (`argumentReadingModel.ts`, 923 lines): map — the spine (concept rows · **relation/roles rows — B-105 W3 §4(a): endpoint positions read the BARE base name, `—a ← unnamed·unnamed`, no disambiguating handle (`endpointNameOf`, `:530-560`); the concept-set position KEEPS the counted/handled forms — held for the designer**) · incidence · stance · verdict-as-consequence · the demoted certificate (prefix-matched by `certificateLabels`) · ### ★ **THE DECK RECORD (`data-deck-record`, B-105 ADR §7): `deck-tiling {p,q}` · `cells at a vertex q` · `descent −I ∈ Sym ∧ free — N cell pair(s)` — the window's demoted proof-tokens, rows not sentences, present exactly when the tiling resolves.**

### 3c · The dim-3 band — the aperture, the verdict, the folded shelf, the walk
- **THE APERTURE** (`aperture — build a 3-manifold (on …)` bottom-right): pair rows (⌊menu/2⌋), face menus in the corners' OWN names, the map menu via `dihedralMapCandidates` (`apertureModel:464`; **refusals COLLECTED — an all-refused menu speaks `NO_MAP_FITS_SENTENCE` (`apertureModel:555`) at the emptiness**), the live skeleton + pending/decided pair marks with the ratified legend, R3's strings (`leave it bounded — its {N} unpaired faces…` live; `▼ {N} pairs in all — scroll for the rest`).
- **THE VERDICT DOOR:** glue/leave-bounded → `buildPersonDomainVerdict` (`apertureModel:1008`) — sound → the room joins the dim-3 band; **FOLDED → the ORBIFOLD verdict: the wall (`foldedEdgeWall` `:955` — the LIMIT only; the subdivide clause CUT per V3 §1) and the SUBDIVIDE DOOR stand as ONE ATOM (B-106 B1: `apertureWall` {sentence, rows} → the panel's one `wall` prop {sentence, onSubdivide} — they live and die together by construction), and the BODY joins the FOLDED SHELF (`dim3f:` — `built 3-manifold N — folded`, no card rises).**
  > ### ⛔ **THE DOORLESS WALL (B-105 §2/§3, measured at the eye — reported, cure is the designer's):** `apertureFoldedRows` dies on ANY aperture-volume change (`:3171-3178` effect — including selecting the folded body the wall just made), on the panel toggle, and on every later exit; **the wall NOTICE is not cleared by the volume-change effect** — so the wall's subdivide-promise can stand with NO door on screen. Selecting the folded body also HIDES its shelf label (`FormLabel` `hidden={selected===id}`) and raises no card ⇒ `/built 3-manifold/` leaves the DOM. One tap reproduces the designer's whole negative.
- **THE WALK (rung 1, CLOSED at Arman's hand):** `explore inside` — rooms (`dim3:`) AND folded bodies (`dim3f:` — the one live door of the no-card frame) open the GPU `ExploreWindow` (1003 lines): the transport loop, the authored inhabitants (plaque + right-handed coil — `apertureProbes.ts`; **the mask counts recurrence, the COIL is the chirality counter** `apertureModel:2011-2016`), `N of the M coils come back mirrored — count them` (`:2449`). ⚠ B-105 A4 FIRST-MEASURE: null control (flat T³) clean — 0 mirrored; the FOLDED room shows copies through its one glued corridor TILTED by the fold transport, but the coil's handedness is ILLEGIBLE at resting distance — the FLIP does not yet READ at the eye (reported, unruled).

### 3d · Rung 2 — THE DECK-TILING (ADR 0025 **as amended §7**)
- **`deckTilingModel.ts` (712):** `readSchlafli` (`:109`) over the ACQUIRED QUOTIENT complex (`acquireComplex`, `complexIdentification:840` — the same object the identify trace reads); `hyperbolicTiling` (the reflect-across-edge generator; the plane its flat limit) · `sphericalTiling` (seed projection, pole PLACED at a face centre — the pole cell is the EXTERIOR by construction) · `tilingDescends` (σ ∈ Sym ∧ free, patch-centroid-keyed — the {4,2} dihedron's degeneracy) · **`resolveDeckTiling` (`:699`)** — the door's judge; eligibility IS the resolution (true-predictive, `ManuscriptView:3335/:3360`).
- ### ★ **THE WINDOW (`DeckTilingWindow.tsx`, 193) NAMES AND SHOWS, NEVER NARRATES (ADR §7, B-105 §4d):** the geometry + its model as a label (`spherical — a stereographic plate` …), the drawing (far side faint · the rim a dashed horizon · the LOD honesty line) — **no ring, no count, no arithmetic, no descent prose** (all demoted to the card's deck record). ### ★ **THE INHABITANT (ADR §7.1, B-105 §5):** the chiral COIL computed on the sphere in the anti-pole cell (`deckTilingModel` — half-inradius anchor so the double lands finite), projected; **descended ⟺ the coil AND its computed antipodal image (far-side faint, mirror-wound BY THE MATH — measured −2.31 vs +2.08 turns); non-descended ⟺ once (the LAW-24 control = the lifted cube, driven at the eye).** Non-spherical tilings carry none.
- Page-reachable symbols (measured at the eye): fold-born torus `{4,4}` (plane) · hexagon-aabbcc `{6,6}` (Poincaré disk) · zoo RP² `{4,2}` (plate, descent CHECKED, doubled coil) · lifted cube `{4,3}` (plate, single coil). The RP² census: **1 quotient 4-gon · 2 edges · 2 vertices · q=2** — the dihedron-quotient presentation; a hemicube-presented RP² is not person-reachable today (word ops are 1-face; the aperture is 3-cell).

## 4 · DOOR 3 — THE PLAYGROUND (dev): CARRIED (ops registry, custom glue ≤8, snapshots) + the `b₁` ring row (§0 note).

## 5 · THE ENGINE VOCABULARY (`src/lib` + frozen manuscript models) — deltas since the old baselines
- **FROZEN SPENDS this arc (each edit+re-seal alone, positive control run):** `multiform` · `worldModel` (pre-B-104) · `writtenFormModel` ×3 (`b08848d` · `e60df35` · **`a2bf8df` — B2: the girdered-plain arm routes on `gate.pinchVertexIds.length === 0`, the endpoint-proxy retired; route bodiless iff ∃v pinch**) · `playgroundOperations` (`e60df35`) · **`complexIdentification` (`85436b2` — the gate CARRIES `pinchVertexIds` it always computed; one decomposition per vertex, valence + pinch)**. Manifest: **46 frozen rows · 125 NOT_FROZEN rows** (the completeness law classifies every src file).
- `incidenceTraceRegistry.decomposeLink` (`:478`) — the one link decomposer: strata · junction loci · **`pinch` (`:173` of the type)** · 4-valued valence.
- CARRIED: surfaceOperations word ops · connectedSum's six refusals · surfaceRefinement (`recoverState` throws at `:367-384` — the R-5 alarm family) · faceIdentification partial matchings · level3 gate/tower · thicken · closeEdgeIntoCircle (now with `closeSegmentIntoLoop` `:138` PERSON-DOORED, §1).

## 6 · WHAT THE PERSON CAN HOLD (deltas over the old table)
Everything in the old §6 table, PLUS: **fold-born words by FIGURE TAPS (any pairing of the rim, any modes — aabbcc reachable by hand)** · **folded ORBIFOLD bodies on their own shelf (walkable; subdividable while the fold cure stands)** · **bounded chambers with CONSUMED picks (leave-bounded, R1)** · **the saved/restored PAGE (the ledger replays every door by record)** · **rung-2 windows on any closed regular surface the resolution accepts (with the inhabitant on spherical ones)**.

## 7 · THE LEVEL-1 STRATUM — CLOSED (the arc→circle door landed: §1's loop-maker). The old §7.1–.4 stand as history; §7.5's "still missing" row is dead.

## 8 · THIS REGENERATION'S CORRECTIONS (each measured this session)
1. **The old map's §0 "SIX dock groups" is stale** — EIGHT groups + two gesture chips at this tip (`DOCK_OPERATION_GROUPS`, `DockGroupKey`); the trapdoor's cure (the union) LANDED — the old "OPEN CHARTER" row is dead.
2. **The old §7.5 gap (arc→circle) is CURED** — `closeSegmentManuscript` (`geometryStore:595`) + the fold chip's segment arm; the old "no person op" claim is dead.
3. **The `:1085`→`:1153` "bare catch" re-pins AGAIN at `:3782-3789`** (`subdivided()`'s pass-through) — B-105 A3: it still swallows ALL `refineToDisk` throws, including `surfaceRefinement`'s internal-consistency alarm ("the derived gluing convention does not reproduce the born cycle", `:381-384` family); the faces≠1 guard sits BEFORE it, so only the recovery/word/convention families land in it — two are the documented pass-through, the CONVENTION alarm is the eaten one. Reported; no cut (the sweep's own kill stands open).
4. **The suite baseline is the sweep's OWN canonical verdict line** (`scripts/sweep.cjs` judges itself; `dual-inspection` is the one accepted fail). The census is not restated here — a copied count drifts (this row itself carried 93 → 112 → 113 across regenerations before that law was bought).
5. **The R-6 quartet's first three premises were STALE at this head** (the chord seal file is TRACKED since `7477729` and already dead-marked; the manifest's surfaceRefinement row names BOTH ratifiers; `diagnose-the-subdivision.headBlobOf` pins `01da8ed`) — the retired BACKLOG's rows outlived their cures; only the VIEW-PIN regex was live, recut structurally at `e788ed3`.
6. **`ManuscriptView` is 6139 lines** (was 2026 at `6436736`); `geometryStore` 973; `apertureModel` 2455; the map's old line counts are history.

*Doors: ambo · manuscript · playground(dev) + the EXPLORE WINDOW (rung 1) + the DECK-TILING WINDOW (rung 2). Stores: 3 + the page store. Suite: **the sweep's canonical line — exactly ONE accepted fail (`dual-inspection`); read the line, never a copied count**. Manifest: 46 frozen · 125 NOT_FROZEN. Regenerated at the B-105 tip; the ambo/lift/playground strata carried with spot-verified anchors — a full re-walk of those is owed at a later boundary, named here so nothing is carried silently.*
