to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-24 16:13 +0330` (machine — `date`) · the chain `67eb506 → f12e6cc → the record commit`, this report the record commit's own letter
re: **`STAMP C-10b` LANDED (`f12e6cc`) — THE DESIGNER'S FOUR BLOCKERS, ONE CUT (§131; her 1500 §0). 1 · the lifted inside on the card is WHOLE OR WORDS: one line per corner (`AB · holds a space of 20 roles · 22 words · 52 tuples · open the drawing`), no drawing in the column; opened, the Ambo's own inside panel mounts on the SHEET at its own size. 2 · the born face's reading mounts WHERE ITS FACE IS SELECTED — a face selection with its meaning (the store's `selectFace`; the solid's plain click on the face you hit, Arman's route; the Cell Faces list; the line at a midpoint site) and THE FACE'S HOME in the selection panel (`SelectedFaceReading`: the face by its D14 name, its kind by the cells holding it, C-5's block on a seed face, C-9's on a born face, the hands as acts); the site keeps its sources and gets, at its TOP, one line per born face through it (`the face AB·AD·AC — interior, between … · select it to read it`) and no born-face block — the reading printed once. 3 · the canvas note names a face `face AB·BC·BD`, never kind and id. 4 · the lineage line names a dissected source face through the workspace's ancestry — `face derived from the seed face A·B·C, dissected` — the absence phrase kept only where no ancestor holds the face. No frozen file. Ten witnesses follow the ruling (the gesture truth's C-6a pins flipped by §131); the leg 87 → 91 ALL PASS (three runs; the third whole); sweep at f12e6cc `141 … SWEEP OK — the one expected fail, nothing else` (wall 99.4 s); tsc 0.** ECHO §131 (`STAMP C-10b`) · §129 and §133 (W1) · the measurement letter of 15:11 (`C-11` item 0, ratified §133) enters the record with this landing.

## TO THE MOTHERSHIP — ten lines up

1. **A face selection now EXISTS in the Ambo, with its meaning** (item 2). C-6a (§92.3) ruled "a plain click does NOTHING on a face row … the face's plain-click act is C-5's to give, with a meaning"; §131 gives it: the face's reading mounts at its home. Mechanism: `selectedFaceId` · `selectFace(id)` in the store (src/store/geometryStore.ts — keeps the cell, clears vertex and edge, the edge's own rule; cleared by every other selection; carried by the undo snapshot; the lift's most-specific chain edge > vertex > FACE > cell, so a selected face lifts as itself); set by the solid's plain click on the face you hit (Workspace3D — the cell is selected as before, and the face with it), by the Cell Faces row's plain click (Panels — `click: read the face · shift-click: toggle in the lift region`, the row a control now), and by the site's line (`select it to read it`). **The gesture-truth witness's three C-6a pins were flipped to the new rule and say why** (a ruling that changes what the person reads sweeps the witnesses).
2. **A default of mine, for her eye:** pointing at the SOLID now names the FACE under the pointer (`face AB·AD·AC`), the cell only where no face resolves — before, every canvas hover named the cell (`cell residue/tetrahedron g1 | id: …`). Arman's route is "explode, point at the face"; the thing the click will select is the thing the note names. The cell's own note left the canvas; the cell stays named in the workspace tree and the selection panel. Reversible in one line if she wants the cell back.
3. **Where the home mounts:** the selection panel, section `Selected Face` (`selection-face-home`), above the vertex section — the head `the face AB·AD·AC — interior, between the parent octahedron and the residue tetrahedron at A`, then the block. The hands are ACTS there (the Ambo's own store) and, no site being local, every hand names where it is (`at ABAC, on AC–AB: a pair of yours · or at AC, on A–C: …`) — never `here`. The seed faces keep C-5's block at the midpoint site too (not moved; not a blocker); a born face prints once, at its home.
4. **THE DRAWING ON THE SHEET** (item 1): an overlay in the Manuscript's root after the Canvas — `left 284 · right 300 · top 64 · bottom 100`, the chrome layer's z — holding the Ambo's own `CastInsidePanel` (inline). Measured: the overlay's box at 1689 × 897 is (284, 116) 1,105 × 681 px with scrollWidth = clientWidth (unclipped) and the drawing's svg 436 px wide (the corner's OWN diagram is one column — the 938 px was the midpoint's unfolding of two parents); at 1400 × 900 the box is 816 px wide, the same svg, unclipped.
5. **THE LINEAGE LINE** (item 4): `faceThroughAncestors` (src/components/faceNames.ts, NEW — `src/components` carries no manifest rows) walks the parent-pointer chain through the store's shapes AS THEY STAND AT THE READ — a plain state read inside pure formatters (never a hook; under a headless render the store holds no shapes and the absence word stands as before — the witnesses that render these panels under node are unchanged). Said: this is reader B reading the store's map in a formatter; the alternative (threading the map through eleven formatter signatures) was priced and not taken.
6. **NOT cut, said:** `unsupported · missing-source-face-context` — the atomic registry's reason (src/lib/atomicRegistry.ts:184, :273, NOT_FROZEN): it fires when no `dissection-core-face` carrying a `sourceFaceId` contains the vertex — a REGISTRY finding about the vertex's contexts, not a name dropped into a name's slot. Where the designer saw it and for which vertex is not in her letter; when she names the site I measure it.
7. **The witnesses that moved** (ten, all ALL PASS): the ambo gesture truth 17 (the face row; the one named action; the note's face branch) · the born face 13 (the site's lines; the blocks read through `SelectedFaceReading`) · the born room 34 (the site's seed-face mount alone; ⚠ my first re-pin broke the file — a regex replacement expanded `\n` inside a JS string — rewritten by plain replacement) · the lift carries 31 (words on the card; the view's overlay; the Ambo's mounts as placed) · the midpoint 58 and the face 24 (the siting pins: the SURFACE stays on the canvas; the face BLOCKS are shared by ruling with the home and the card) · the cast loader 47 · the concept type 11 · the inside 33 · the freeze exit 0.
8. **The leg** `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS`, 91 clauses (87 → 91) on both viewports, served `67eb506` dirty; three runs on this stamp — a duplicate local in the leg's own scope, then two measures of mine (the home's visibility before the scroll; the canvas route pointing through the midpoint panel a selected vertex mounts over the solid, and the explode label carrying its number), then whole. The plates `concept-layer-face-home-{interior,one-cell}-*.png`, `concept-layer-canvas-face-*.png`, `concept-layer-lift-drawing-*.png` in the ignored `_frames/`.
9. **Not sighted by a person:** the designer's eye on the four cures; Arman's STEP-2 walk on this tree.
10. **The measurement letter** (`C-11` item 0, 15:11) enters the record with this landing as the second section of THE_BUILD_REPORT.md; the §129 letter it answered rides THE_BUILD.md beside §131.

## 1 · What I SAW

**At the eye (the drive family — served `67eb506`, dirty (26 paths) — the C-10b tree, both viewports):** at ABAC (the born room, gen 2) the site's top: `the face AB·BC·AC — the parent octahedron's · select it to read it` · `the face AB·AD·AC — interior, between the parent octahedron and the residue tetrahedron at A · select it to read it`, no block below the sources; the interior line taken → the selection tab's `Selected Face`: the head, one block walked as the parent octahedron's, alike (one block, the line), `the solid's ground: … returned to itself · … elsewhere · … did not return` ×3, `nothing added by a pair of yours yet` ×3, three hands each naming its site (`at ABAC, on AC–AB …` · `at ABAD, on AB–AD …` · `at ACAD, on AD–AC …`); the one-cell face's home `the face AB·BC·AC, walked in the face's own direction, AB → BC → AC → AB`; after the born pair r3 ↦ Φ3 that face's home: `+ r3 returns as Φ3 — through your pair r3 ↦ Φ3 at ABAC` with two `nothing added` lines left; Arman's route: the residue tetrahedron at A selected, the explode view at 60, the solid pointed at — the note `face …` by its corners — and clicked: its face's home mounted (the plate `concept-layer-canvas-face-1689x897.png`: the cells pulled apart, the residue at A selected); hovering a Cell Faces row: `face AB·…`, no id; the Cell Faces list's lineage lines `face derived from the face …, dissected`, none `no longer holds`; on the Manuscript: the seven rows of words, `open the drawing` on AB → the drawing on the sheet, whole (`AB · the inside of the space it holds, derived from its parents`, 20 points, `F5 ≡ Φ7` among the labels), the card untouched by it.

**Under node:** the born-face witness renders the site at ABAC — two lines (`the face AB·BC·AC — the parent octahedron's · select it to read it` · `the face AB·AD·AC — interior, between the parent octahedron and the residue tetrahedron at A · select it to read it`), no block — and the face's home for each (`SelectedFaceReading`): the same readings C-9 pinned (states read, the ground once per corner, the interior face alike with its line), the hands `at ABAC, on …`; the lift-carries witness renders the card's rows as words (`AB · holds a space of 21 roles · 16 words · 45 tuples · close the drawing` with AB picked, 7 `open the drawing` buttons, no inside panel in the section).

## 2 · What I RAN

- **sim-diff:** `git diff --stat f12e6cc -- <the eighteen committed paths>` EMPTY right after the STAMP commit — the committed tree is the tree the witnesses and the leg read; the working tree's remaining diffs are the three W1 files, the two unlisted modified files (ADR 0031, the J-registers ruling), the two `tsconfig*.tsbuildinfo` and `scripts/app-leg/__pycache__/` (listed, none staged into the STAMP).
- **the manifest, every touched engine file:** `src/manuscript/liftedConceptModel.ts` (NOT_FROZEN) · `src/manuscript/LiftedConceptSection.tsx` (NOT_FROZEN) · `src/manuscript/ManuscriptView.tsx` (NOT_FROZEN 196) · `src/store/geometryStore.ts`, `src/components/Workspace3D.tsx`, `src/components/Panels.tsx`, `src/components/MidpointSurface.tsx`, `src/components/SelectedVertexRelations.tsx`, `src/components/VertexPacketEditor.tsx`, `src/components/faceNames.ts` (NEW) — `src/store` and `src/components` carry no rows. **No frozen file; no re-seal.**
- **the sweep at the landed HEAD, whole, read by eye in its own call:** at `f12e6cc`, whole, read by eye in its own call before this record was written: `141 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (exit 0 — the census 141, the one accepted fail and the rest PASS; the lift-carries witness 2.5 s, the gesture truth 0.7 s; the gate 88 s, the door column 86 s, the j-register PASS at 69 s inside its declared ceiling, the born room 50 s; wall 99.4 s).
- **`npx tsc -b`:** exit 0.
- **THE READING:** the drive family — `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS` (91 clauses — 87 → 91, two new arms per viewport: §0 + 45 at 1689 × 897 + 45 at 1400 × 900; `/__whereami` = `67eb506`, dirty — the C-10b tree; at ABAC the site's TOP carries the two lines `the face AB·BC·AC — the parent octahedron's · select it to read it` and `the face AB·AD·AC — interior, between the parent octahedron and the residue tetrahedron at A · select it to read it` and 0 blocks; the interior face's line taken, its HOME in the selection tab reads `the face AB·AD·AC — interior, between the parent octahedron and the residue tetrahedron at A` with one block (two cells, alike, the alike line), the ground once per corner, `nothing added by a pair of yours yet` ×3, the three hands `at ABAC, on AC–AB: a pair of yours · or at AC, on A–C: an act on the edge it descends from` …, in the viewport once scrolled to; the one-cell face's home `walked in the face's own direction`; after the leg's born pair r3 ↦ Φ3 the medial face's home carries `+ r3 returns as Φ3 — through your pair r3 ↦ Φ3 at ABAC`; Arman's route — the explode view at 60, a face under the pointer named `face <D14>` (no kind, no id), the click mounting that face's home; the note on a face row `face AB·…` with 0 `id:`; the Cell Faces list 0 rows reading `no longer holds`, the derived rows `face derived from the face …, dissected`; on the Manuscript the lifted residue's card rows `A · holds a space of 14 roles · 11 words · 34 tuples · open the drawing` … (7 rows) and `open the drawing` on AB mounting the drawing on the SHEET at (284, 116), 1,105 × 681 px at 1689 × 897 — outside the card, inside the viewport, UNCLIPPED (scrollWidth = clientWidth = 1,105; the drawing's svg 436 px wide), 20 points, 3 labels wearing `≡`; at 1400 × 900 the box 816 px wide, unclipped too; the leg ran three times on this stamp — the first found a duplicate local in the leg's own scope, the second the home's visibility read before the scroll and the canvas route pointing through the midpoint panel (a selected vertex mounts it over the solid — the cell alone is selected now) with the explode label carrying its number — the third whole on both viewports).
- **the node witnesses on the landed tree:** the gesture truth 17 · the born face 13 · the born room 34 · the lift carries 31 · the midpoint 58 · the face 24 · the cast loader 47 · the concept type 11 · the inside 33 ALL PASS · the engine freeze ALL PASS (exit 0).
- **the inbox gate:** `--mark-reviewed --expect 3` before each commit (the C-10b, C-11a and C-11b letters all read whole); `--expect 2` after the C-10b letter's archive.

## 3 · What I CHANGED

- `src/store/geometryStore.ts` — `selectedFaceId` · `selectFace` · the resets · the undo snapshot · the lift chain.
- `src/components/Workspace3D.tsx` — the plain click selects the face you hit; the hover names the face under the pointer; the note's face branch `face <D14>` (the relation helper, uncalled, removed).
- `src/components/Panels.tsx` — `SelectedFaceReading` (exported) and its section; the Cell Faces row a control; `formatFaceLineageSummary` and `getFaceDisplayLabel` through the ancestry; `data-face-row`, `data-face-row-lineage`.
- `src/components/MidpointSurface.tsx` — `faceCellsOf` (exported, one producer); the site's top lines; `SourceRecord` without the born-face block.
- `src/components/faceNames.ts` (NEW) — `faceThroughAncestors`, `dissectedFaceWords`.
- `src/components/SelectedVertexRelations.tsx`, `src/components/VertexPacketEditor.tsx` — `getFaceDisplayLabel` through the ancestry; the face-opposites' missing row named.
- `src/manuscript/liftedConceptModel.ts` (`tuples`), `src/manuscript/LiftedConceptSection.tsx` (words; `open the drawing`), `src/manuscript/ManuscriptView.tsx` (the drawing overlay).
- the witnesses named in line 7; `scripts/app-leg/diagnose-the-concept-layer-eye.cjs` + `concept_layer_eye_driver.py` — the C-9 arm moved to the face's home; arms for items 1–4 (`face_home_arm`, `canvas_face_arm`, the drawing on the sheet).

## 4 · What I could not reach

- **The designer's sighting** of the four cures (blockers only, her pass). **`unsupported · missing-source-face-context`** — where she saw it is not in her letter; not cut (a registry finding), measured when the site is named. **A face selection across export/import** — the persisted workspace JSON (`PersistedWorkspaceV1`) carries the cell and the vertex, not the face; the undo snapshot carries it; adding it to the file is one optional field when wanted, said.
- The designer's eye and Arman's STEP-2 walk.

## The record commit

- **U1 (§126.2):** the third wave, found by the sweep run from its home at f12e6cc and APPLIED (§126.2 — every wave at each record commit): 2 cited RULINGS (133,523 B) — `RESEARCHER_RULING_ZOO_EXPANSION_GATED_ON_CONSTRUCTOR.md` (5,629 B, cited by the ZOO_EXPANSION_CORRECTED ruling tracked last time) and **the researcher's RENAMED ruling** `RULING_THE-INSIDE-MIDPOINT-TRACE-REFUSAL_on-the-honesty-line_midpoint-is-the-pushout-over-the-CORE_trace-is-its-origin-partition.md` (127,894 B, 140 characters — the too-long file of the two earlier reports, renamed with its head kept, cited by the J-registers ruling; tracked now as ruled, `core.longpaths` untouched; the old name is gone from the tree — the sweep's TOO LONG list is empty) — and 3 cited INSTRUMENTS (20,548 B): `RESULTS_2026-09-23_descent_at_the_door.txt` (5,466 B) and `RESULTS_2026-09-23_descent_at_the_door_run1_N-read-one-way.txt` (5,278 B, both cited by `descent_at_the_door.py`), and `cargo_in_the_room.py` (9,804 B — C-11b's reference, cited by the claims ledger); the import closure adds none; `.gitignore`'s U1 block regenerated whole (100 negations); the 5 files tracked in this record commit.
- **W1 riders AS FOUND:** the claims ledger `blob b32d94af` → `dfae77fc` (`sha256-LF 41af5720` → `e2c73e64`, 60/0 — §129 to §133 by its clock, 15:21) · the front board `blob 0e01231b` → `5d859796` (`sha256-LF 1d72486f` → `ba5f3348`, 5/3) · the connection-layer plan `blob 8f1cf333` → `49dbf871` (`sha256-LF c9fbbff2` → `5e140d58`, 1/1) — every tail closes on a finished sentence; the sovereign ledger (`blob f6072bed`), ADR 0030 (`blob 66cb9c3f`) and the designer-seat handoff (`blob 3dd84885`) unchanged. FOUND MODIFIED AND NOT ON ANY W1 LIST — LEFT UNCARRIED, SAID: ADR 0031 `blob 5ef9a361` → `dde61338` (`sha256-LF ea164e5d` → `a2a3b1c2`, 3/1, mtime 15:04:05, the tail closing) and the researcher's J-registers ruling, tracked by U1 (`RULING_THE-J-REGISTERS-QUANTITIES-ON-THE-TYPE_…`, `blob d7985810` → `06671edf`, 1/1, mtime 15:05:47 — its HEAD digest read through the blob: `sha256-LF 95bdeb0f`; the working file `fdf4eccb`) — both stay in the working tree until named.
- The 1502 (`STAMP C-10b`) letter archives with this landing; the 1355 (§129) letter, consumed by the measurement, was archived at its filing; C-11a and C-11b stay in the inbox for their cycles.

---

# THE MEASUREMENT LETTER FILED BEFORE THIS LANDING — `2026-09-24_1511_coder_C-11-ITEM-0-MEASURED_two-doors_the-rooms-door-costs-no-frozen-file_the-surface-glue-five_the-walk-knows-its-door-not-its-edge_ECHO-129.md` (C-11 item 0, no build, no commit; ratified §133; read from `.handoff/inbox/mothership/archive/`)

to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-24 15:11 +0330` (machine — `date`) · HEAD == origin == `67eb506` · a MEASUREMENT, no build, no commit (the W1 claims-ledger rider waits for the next record commit — `C-10b`'s)
re: **`C-11` ITEM 0, EARLY — the door's record · persistence · the walk · the lifted edges' `J`s in the room, measured on the lawful path (casts on A · B · C · D, pairs on A–B and A–C, two dissections, the born pair Φ2 ↦ r1 on AB–AC; the gen-2 core and the residue at A lifted through the store, loaded through the shelf's door). THE FINDING THAT SHAPES `C-11`: there are TWO doors in the tree and they cost differently. THE ROOM's door — the aperture's pairing row, the door the walk actually crosses — is a NOT_FROZEN record (`AperturePairRow`) resolved to a per-corner correspondence, persisted verbatim in the page file, re-run on load; the room keeps the lifted form's ids WHOLE (12 of 12 corners, 24 of 24 edges reach the carried record; the record's 3 `J`s survive the page round trip), and the walk knows at every crossing WHICH door (`{pair, side}`) it took. A per-corner transport `e_i` can ride that row and that door with ZERO frozen files. THE SURFACE glue (the fold, the traced identify, the combine) keeps its record as a RECIPE IN THE SHAPE ID inside four frozen files; a per-corner map cannot ride an id string, would need a named field on `ShapeGenealogy` in the frozen types file, written by two frozen enactors and replayed by the frozen loader — five frozen files — and the born form reads NO record by `C-10`'s ratified rule (measured: `born-on-page`; the merged corner minted with no lineage). Where the walk does NOT know: which LIFTED EDGE a step crossed — it crosses faces; edges are rods, drawn, never crossed.** ECHO §129 (`C-10` ratified; hands as words ruled) · `C-11` item 0 (measure only).

## THE TABLE — item · file:line · manifest row · frozen? · cost

**1 · THE DOOR's RECORD**

| what | file:line | row | frozen? | can it carry `e_i`? · cost |
|---|---|---|---|---|
| **THE ROOM's door — the person's row** `AperturePairRow {faceA, faceB, candidateKey}` | src/manuscript/apertureModel.ts:579 | 203 NOT_FROZEN | no | **YES, additively**: `transports?: Array<{ corners: [VertexId, VertexId]; roles: [string, string][]; types: [string, string][] }>` keyed by the candidate's corner pairs — the row is the person's INPUT ledger already; cost: one NOT_FROZEN file |
| the row resolved to a pairing: `resolvePersonPairings` → `FacePairing {faceA, faceB, mode, map}` | apertureModel.ts:1003 → src/lib/faceIdentification.ts:171 | 203 / **51** | the type is **FROZEN** (faceIdentification.ts) | the `map` is the per-corner bijection (faceA cycle vertex → faceB image) — the `e_i` would sit BESIDE it, never inside `FacePairing` (a frozen row otherwise) |
| the picked map's corner correspondence `ApertureMapCandidate.correspondence: [string, string][]` | apertureModel.ts:431-434 (built at :481 `dihedralMapCandidates`) | 203 NOT_FROZEN | no | the corner pairs an `e_i` is indexed by — measured on the lifted gen-2 core: `ABBC→ADBD · ACBC→ABBD · ABAC→ABAD` (candidate `d+0`), both ends' spaces readable on the carried record (30↔33 · 31↔30 · 27↔30 roles) |
| the door as the DOMAIN records it: `DomainPairMark {mode, faceIds, centers, correspondence?}` in `DomainModel.pairs` | src/manuscript/worldModel.ts:109-117, :130 | **81 FROZEN** (spent at b08848d) | yes | `correspondence` optional and ABSENT on the measured mark (`{mode, faceIds, centers}` printed); a transport ON the mark = one frozen row — **not needed if the walk reads the rows** |
| the door the WALK crosses: `ApertureCellFace.door: { pair, side }` built by `readCellSurface(domain, …)` | apertureModel.ts:1557 · :1826 | 203 NOT_FROZEN | no | an `e` per door rides the same face entry additively (`door.transport`), fed from the record's rows handed beside the domain; cost: NOT_FROZEN |
| the person's ledger of built rooms: `BuiltDomainRecord {door, key, title, seed: Shape (VERBATIM), rows: AperturePairRow[], baseId, baseRefusal}` | src/manuscript/pageSnapshot.ts:35-43 | 224 NOT_FROZEN | no | the rows ride verbatim — a transport on a row is in the file for free |
| **THE SURFACE glue — the fold** (`handleFoldCommit` → `applyFoldTo` → `executeCustomGlue` → `materializeSurfaceResult`) | ManuscriptView.tsx:5325 → handGestureModel.ts:134 → customGluing.ts:221 → materializeOperation.ts:253 | 196 · 228 · **84** · **63** | customGluing **FROZEN**, materializeOperation **FROZEN** (sanctioned) | the record: `BoundaryPairing {edgeA, edgeB, mode}` (surfaceOperations.ts:61, **69 FROZEN**) pairs EDGES by index; the born id `shape:materialized:<surface>:<parent>:<face>:<pairs>` (materializeOperation.ts:281-282) IS the recipe, read back by `parsePairingSuffix` (bornFormRouting.ts:56, **83 FROZEN**); no corner-level field anywhere |
| **the traced identify** (`handleCycleConfirm` → `identify`) | ManuscriptView.tsx:5259-5275 → complexIdentification.ts:769 | 196 · **44** | complexIdentification **FROZEN** (spent at 85436b2) | the record: `ComplexIdentificationSpec {cycleA, cycleB, modes}` (:103) — edge ids; the born id `shape:identified:<parent>:idn[modes][cycleA][cycleB]` (:622, `encodeIdentificationSuffix` :391 · `parseIdentificationSuffix` :397) IS the recipe; `IdentifiedComplex` (:126) carries `seamEdgeIds` and the spec — no corner map |
| **the combine** (`handleCombine` → `birthChild` → `connectedSum`) | ManuscriptView.tsx:4898 → genesisModel.ts:112 → connectedSum.ts:86 | 196 · **75** · **46** | genesisModel **FROZEN**, connectedSum **FROZEN** | the same class: a new shape, the record in its genealogy and id |
| the shape's own record type: `ShapeGenealogy {parentShapeId, operation, generationDepth, sourceVertexIds, createdVertexIds, createdAt, resolution?}` | src/types/geometry.ts:277-289 | **90 FROZEN** | yes | the ONE named home a surface-glue transport could have (the `resolution?` idiom: a record ON the shape, never beside it) — one frozen row in the types file, plus the writers below |
| the edge's `J` (`EdgeIdentification {roles, types, support?, fiat?}` on `Edge.identification`) | geometry.ts:202-207 · :227 | 90 FROZEN | yes | per EDGE — the wrong grain for a door's per-corner map (a door pairs corners across two faces; after the glue the pair is ONE merged vertex `idn:…`/`mat:…`) |

**2 · PERSISTENCE**

| what | file:line | row | frozen? | measured · cost |
|---|---|---|---|---|
| the page file `ManuscriptPageFile {written, shelfFiles, shelfPlacedShapeIds, builtRecords, builtCount, zooLoaded?}` — the RECORD, not a reading | src/manuscript/pageSnapshot.ts:67-77 · `serializePage` :88 · `parsePage` :106 | 224 NOT_FROZEN | no | a round trip of a page with the lifted gen-2 core placed, its shelf file and one built room: 189,929 B; the built record's `rows` verbatim (`{faceA, faceB, candidateKey}`), its `seed`'s ids verbatim, the written form's shape id verbatim |
| hydration: ids VERBATIM (:18), the shelf files re-loaded through `loadUniverseSnapshot` → `shelfAncestors` (the `C-10` record), the built records re-run through `buildPersonDomainVerdict(seed, rows)` | src/manuscript/pageStore.ts:324-380 (:331-333 the ancestors · :364-366 the door re-run) | 225 NOT_FROZEN | no | after the reload the shelf's ancestor holds every seed corner; 12 of 12 corners resolve, 24 of 24 edges reach the record; the record's 3 `J`s survive (= the Ambo's 3). **A transport on the rows survives save/load for free — no re-keying, 0 frozen** |
| the Ambo's snapshot layer — the ONLY re-keying loader (`deserializeSnapshot` prefixes every id; GAP2C reconstruction REPLAYS `identify` from the id's recipe) | src/playground/snapshot.ts:190 · :404-470 (the replay at :408 and :456) | **89 FROZEN** (sanctioned) | yes | NOT on the page's path (0 `serializeSnapshot` call sites under src/manuscript but standardBodies' translation of a fixture); a transport keyed by the seed's vertex ids is safe within the page; **a surface-glue transport on `ShapeGenealogy` would be DROPPED by the replay** (the rebuilt link is minted from the recipe alone) → a snapshot.ts spend to carry it |
| "a later lift" | — | — | — | a room is never lifted again; the lift runs Ambo → Manuscript only (C-10); no route carries a doored form back through the re-keying loader today |

**3 · THE WALK**

| what | file:line | row | frozen? | measured |
|---|---|---|---|---|
| the walker's state `ExploreSeam`: `doors` (transports taken), `frameHanded`, `trace` (one letter per crossing), `sentence`, `returnCount`, `doorsAtLastReturn`, `faces` (the room's faces with their door letters) | src/manuscript/ExploreWindow.tsx:69-130 (:75 · :76 · :113 · :119 · :106) | 189 NOT_FROZEN | no | the seam is the ONE state; every order reading is derived from `trace` |
| **the crossing knows its door**: `seam.doors += 1; if (face.door) seam.trace += doorLetter(face.door)` at BOTH crossing sites (the projective door · the affine door); the letter minted by `orderTrace.doorLetter` from `{pair, side}` (side `a` = the forward map → lowercase; side `b` = the inverse → capital) | ExploreWindow.tsx:1000-1001 · :1023-1024 · src/manuscript/orderTrace.ts:87 | 189 · 212 NOT_FROZEN | no | **YES — at each step the walk knows WHICH door (the pairing's index in the room's order) and WHICH SIDE (the map or its inverse)** — exactly what composing `e` or `e⁻¹` needs; a WALL is not a door (no letter) |
| the readings `trace · tally · sentence` — DERIVED in `refreshOrderSurface` (the tally :729, the sentence :735, the window :742), one producer | ExploreWindow.tsx:719-747 | 189 | no | — |
| where they MOUNT: `data-explore-trace` · `data-explore-tally` · `data-explore-sentence` · the return line `data-explore-return` | ExploreWindow.tsx:1789 · :1801 · :1814 · :1836 | 189 | no | **a CARGO reading sits as a fourth field on `ExploreSeam` (`cargo: { role, fix · mov · und … }`), written at the two crossing sites beside the letter (compose the door's `e` by `side`), refreshed in `refreshOrderSurface`, mounted as a fourth line beside the tally (`data-explore-cargo`)** — NOT_FROZEN |
| what the window is HANDED: `ExploreWindowProps.cellSurface: ApertureCellSurface` (faces with `door {pair, side}` and `g`/`g4`, rods) — no rows, no domain | ExploreWindow.tsx:619-622 | 189 | no | a transport reaches the walk through `ApertureCellFace.door` (additive) or one more prop — NOT_FROZEN |
| **which LIFTED EDGE a step crossed: NOT KNOWN** — the walk crosses FACES (plane + bounds, :941-999 `prev`/`eye`); edges are RODS (`ApertureCellRod`, drawn, never crossed); the crossing POINT on the face is computed and not stored | apertureModel.ts:1560 · ExploreWindow.tsx:941-1030 | 203 · 189 | no | composing the lifted edges' `J`s along a walk needs a rule the tree does not hold — WHERE on the door the cargo passes (the researcher's owed word: where the cargo sits between corners); today the walk has the door and the side, nothing finer |
| the walk's own metric (transport along a geodesic, the frame) | src/manuscript/walkMetric.ts:116 · :71 | 213 NOT_FROZEN | no | — |

**4 · THE LIFTED EDGES' `J`s IN THE ROOM**

| what | file:line | row | frozen? | measured |
|---|---|---|---|---|
| the room keeps the lifted form's shape VERBATIM: `DomainModel.shape` = the pointed-at volume; `boundaryFacesOf` offers RAW ids on a single-cell volume | worldModel.ts:133 · apertureModel.ts:779 (:1119 `buildPersonDomain`, :1033 the verdict door) | 81 FROZEN (the type) · 203 | the type only | **`domain.shape.id === the loaded form's id; the same vertex ids; the same edge ids** (the lifted gen-2 core, one door between two opposite triangles, 6 candidates, `folded: false`) |
| **the `C-10` reader on the room's seed** with the lifted form's carried ancestors | src/manuscript/liftedConceptModel.ts:84 | 221 NOT_FROZEN | no | **`read` — 12 of 12 corners held and resolved; 24 of 24 edges with both ends resolving on the record** — every edge of the room reaches its `J` through the record by the seed's shape id (`shelfAncestors.get(seed.id)`) |
| where it is NOT reached TODAY: the view computes the concept layer for `w:` selections alone — a built room is a `dim3:` key; its card gets no section | ManuscriptView.tsx:3445-3448 | 196 NOT_FROZEN | no | a view-side wiring (the built record's `seed.id` → `shelfAncestors`); no lib change |
| **where the glue LOSES it — the SURFACE glue** (`identify` on the lifted form): a NEW shape (`genealogy.operation` `glue`, the parent pointer = the lifted form, the recipe in a 300-char id); the paired corners MERGED into minted `idn:…` vertices with no lineage into the record; the kept corners keep their ids | complexIdentification.ts:622 · :769 | **44 FROZEN** | yes | the residue at A, the arcs (A–AB, AB–AC) ~ (A–AD, AD–AC): 6 vertices (5 kept · 1 minted) · 13 edges (11 kept · 2 minted seams); **the `J` on AB–AC RODE onto the merged seam edge `AC–AB·AD`** (another id) — bytes kept, unreadable: its merged end resolves to nothing; the `C-10` reader: `born-on-page` (by the ratified rule); by ids alone 5 of 6 corners would resolve, 1 not held; edges reaching the record 6 of 13 |
| the same on the gen-2 core (two opposite triangles, closed cycles) | — | — | — | 7 vertices (6 kept · 1 minted), 21 edges (18 kept · 3 minted), 3 seams; by ids alone 6 of 7, edges 8 of 21 — ⚠ with cycles given as raw edge ids and one uniform mode ALL SIX corners merged into one: the view derives the modes from the TRACED directions (`modesFromDirectedCycles`, ManuscriptView.tsx:5267), my probe did not — the corner census is the probe's, the kept/merged law is the engine's |
| the subdivision door (`subdivideAndReadPersonDomain` → `bisectEdges`) — a REFINED room's half-edges are new ids | apertureModel.ts:1154 · src/lib/level3Subdivision.ts | 203 · 155 NOT_FROZEN | no | NOT measured here (no refined room on the path): the Manuscript's bisection mints its own ids without `createdBy` lineage into the record — where `C-10`'s "subdivision composes" (the Ambo's grain) does not reach; to be measured when `C-11` charters a refined room |

## THE FROZEN SPENDS `C-11` WOULD NEED — by option

- **Option R — the door is the ROOM's door (the walk's door); `e` rides the pairing row; the walk composes `e` by `{pair, side}` at each crossing:** **NONE.** The record (`AperturePairRow`, `BuiltDomainRecord.rows`), the read (`readCellSurface` → `ApertureCellFace.door`), the state and the reading (`ExploreSeam`, the fourth line), the persistence (the page file, hydration) and the room's reach to the lifted `J`s (`liftedConceptModel`, the view's wiring) are all NOT_FROZEN. Two rows become spends only if you rule the transport must live ON the domain's own record rather than beside it: `src/manuscript/worldModel.ts` (81 — `DomainPairMark`; spent once at b08848d) and/or `src/lib/faceIdentification.ts` (51 — `FacePairing`).
- **Option S — a transport at a SURFACE glue's paired corners (the fold · the traced identify · the combine):** `src/types/geometry.ts` (90 — a named field on `ShapeGenealogy`) · `src/lib/complexIdentification.ts` (44 — `identifyOnComplex` writes it; spent at 85436b2) · `src/lib/materializeOperation.ts` (63 — the fold's enactor writes it; sanctioned) · `src/playground/snapshot.ts` (89 — the GAP2C replay would drop it; sanctioned, spent twice) · `src/playground/bornFormRouting.ts` (83 — if the recipe grows) — and, for the fold's path, `src/playground/customGluing.ts` (84). **Five to six frozen files**, and `C-10`'s ratified rule reopened: a form born on the page reads NO record, so the two ends of an `e_i` at a fold corner have no spaces to map between unless the merged corner is given a lineage — a MEANING question before a spend.
- **Neither option touches** `subComplexLift.ts`, `spaceOf.ts`, `faceReading.ts` or `bornFace.ts` (NOT_FROZEN) — the cargo's `Fix · Mov · Und` composes with the engine C-9 and C-10 already hold (`composeThroughCorner`, the born steps' maps, the resolver on the record).

## What I RAN
`probe-c11-item0.cjs` (scratchpad, node, the lawful path through the store; nothing written into the tree): the room's door built by `buildPersonDomainVerdict` on the loaded gen-2 core with one pairing row (the bounded chamber refuses by name through the verdict door and goes through `buildFormDomain` — pageStore.ts:362, said); `identify` on the loaded residue at A (arcs) and on the core (closed cycles); `serializePage` → `parsePage` → `loadUniverseSnapshot` round trip. Every number above is from that run. No witness, no leg, no commit; `git status` unchanged but for the four record files the mothership edits and the build-info files.

## Not measured
- a REFINED room (the subdivision door) and its half-edges' reach to the record;
- a multi-cell volume's prefixed `c{i}:` ids (`boundaryFacesOf`'s other branch — apertureModel.ts:588-592) against the reader's id match — the lifted regions on the path are single cells;
- Arman's word on where the cargo sits between corners (the researcher's, owed) — the walk's composition of the lifted edges' `J`s waits on it.
