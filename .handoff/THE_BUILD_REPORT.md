to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-25 01:14 +0330` (machine — `date`) · HEAD == origin == `5d2d6e9` (the STAMP) · the record commit follows this letter
re: **§149 rider LANDED — THE PICK MOVES NOTHING. Each half's pick words now live in a line of their own, reserved in every state, one line high, never wrapping; at both viewports the badge's box, the partner point's and every block of the surface down to the drawing stand at the same y before and after the first pick — 0 px — for a role pick and for a word pick. ECHO the 0047 letter. Nothing owed; the build is done — Arman's use begins.**

## TO THE MOTHERSHIP

1. **The word half is cured by the same law, beyond the letter's example.** Your ruling named the role pick; the surface has a second pick, the word pick, whose words were appended to the pairs row — a flex row that wraps — so a word pick could move the sentence line and everything below it the same way. Its words now stand in the word half's own reserved line, under the pairs row. ⚠ Its shift BEFORE the cure was not measured at the eye; it is inferred from the construction (the same append into a wrapping row). The cut makes it impossible either way, and the eye measures 0 px now.
2. **At rest the surface is taller by the two reserved lines.** Each empty line holds one 16 px line of space; at 1400×900 the drawing's top at rest stands at y 488, against y 448 before the cure (+40 px) and at 1689×897 the drawing's top at rest stands at y 446, against y 406 before the cure (+40 px). Nothing is written in them until a pick is made.
3. **On a panel narrower than the words, the line cuts with an ellipsis instead of growing.** Measured whole at both viewports (the line's scroll width within its client width, pinned). That width is below any panel the leg drives.

## What I SAW — the drive family, `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS` (139 clauses — 135 → 139, two new clauses per viewport; `/__whereami` = `7ea625b`; the leg ran once on this cut, whole on both viewports — served `7ea625b` (HEAD `7ea625b`, dirty 8: the cut's four files, the claims ledger and the excluded build files))

At 1689 × 897 and at 1400 × 900, at AB with (i) on A–B: F1 picked by a click on its badge — the badge's box, the partner point's (Φ1, the first B point, the act's second target) and every top-level block of the surface down to the drawing stand at the same y and height before and after the pick: **0 px at both viewports** (the badge's shift printed 0 px at 1689×897 · 0 px at 1400×900). The words `F1 in A chosen — now a point in B` read in the role half's own line, one line of 16 px, whole, none left in the sentence line. Then a word pick (`disjoins` in A's row): the first chip of B's row and every block unmoved, 0 px, the words in the word half's own line, whole, none in the pairs row; the same chip clicked again unpicks. The positive control is the record's own: the same block instrument measured the 16 px shift at 1400 × 900 at `cd1fea0`. Everything else the leg pins stands unchanged.

## What I RAN (verbatim tails)

- `scripts/diagnose-the-midpoint.cjs` — ALL PASS (59, 58 → 59): §4 NEW `THE PICK MOVES NOTHING, BY CONSTRUCTION` — the two reserved lines rendered in order (word, role) in the unglued and the glued renders, each EMPTY at rest and one line high by its classes (`h-4 leading-4 truncate`); each pick span written once in the source and only inside its line; none in the sentence line or the pairs row.
- The stone 20 · the born room 34 · the found bugs 24 · the lift carries 33 · the doors act 35 · `diagnose-engine-freeze` 19 — ALL PASS · `npx tsc -b` exit 0.
- The drive family: `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS` (139 clauses).
- **The sweep, whole, read by eye in its own call** — at `5d2d6e9`, before this record was written: `145 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (exit 0 — the midpoint 13.8 s · the stone 2.4 s · the born room 50.4 s · the door column 77.8 s · the j-register 64.2 s; wall 93.3 s).

## What I CHANGED (`5d2d6e9`)

- `src/components/MidpointSurface.tsx` (not under an engine root) — the role pick's words moved out of the sentence line into a reserved line under it; the word pick's words moved out of the pairs row into a reserved line under it, inside the word half; both lines rendered in every state, one line high, never wrapping. The words, their colour and the attributes the witnesses read are unchanged.
- `scripts/diagnose-the-midpoint.cjs` — §4's construction clause.
- `scripts/app-leg/concept_layer_eye_driver.py` — the surface's block instrument hoisted to one constant; the partner point and the role line read around the badge arm's pick; the word arm NEW.
- `scripts/app-leg/diagnose-the-concept-layer-eye.cjs` — §12's two clauses, both viewports.

## What I could not reach

- Nothing owed. The word pick's pre-cure shift is unmeasured (item 1 above), said.

## W1 — the courier, at the record commit

- **ADR 0031** (`docs/adr/0031-the-concept-layer-is-a-cosheaf-of-partial-symmetries.md`) — byte-equal to HEAD: `blob e7565a44` = `sha256-LF b0795661` (`git hash-object` · `tr -d '\r' | sha256sum`), mtime 19:45:24; nothing to carry, said.
- **the researcher's ruling** (`.handoff/RULING_THE-INSIDE-MIDPOINT-TRACE-REFUSAL_on-the-honesty-line_midpoint-is-the-pushout-over-the-CORE_trace-is-its-origin-partition.md`) — byte-equal to HEAD: `blob c54fa6f2` = `sha256-LF 862f90de` (`git hash-object` · `tr -d '\r' | sha256sum`), mtime 19:45:46; nothing to carry, said.
- **the claims ledger** (`.handoff/THE_CLAIMS_LEDGER.md`) — CARRIED AS FOUND, not read for content, no re-seal: `blob d4ed5b43` → `46b46229` (`sha256-LF ca92ceaf` → `b21f6802`), numstat 1/0, mtime 00:46:15; the tail's last characters: `s land on a still target at every width). One small cut; then the build is done. *(00:47)*` (closing on a finished line).
- **the sovereign ledger** (`.handoff/THE_SOVEREIGN_LEDGER.md`) — byte-equal to HEAD: `blob 2d731659` = `sha256-LF c74b98ce` (`git hash-object` · `tr -d '\r' | sha256sum`), mtime 19:38:46; nothing to carry, said.
- **U1** (§126.2, every wave at each record commit): the sweep run from its home at `5d2d6e9` finds NO new wave — 0 rulings, 0 instruments (candidates 75); `.gitignore` regenerated whole and unchanged; the files to add []. Nothing tracked in this record commit beyond the record pair and the rider(s) .handoff/THE_CLAIMS_LEDGER.md.
- **The inbox**: the 0047 letter archived with this landing; the coder's inbox holds nothing — the build is done; Arman's use begins.
