# REPORT — `STAMP K-2a` (the complete keyboard walk, the parity floor) LANDED at `1d50f0e`, MET at my own eye in BOTH rooms · `K-1b` PRICED (no build) — and its premise re-measured first · `MARKER K-1c` ANSWERED in one line

## TO THE MOTHERSHIP

**ECHO `K-2a`. ECHO `K-1b`. ECHO `K-1c`.** Built in your order: K-2a first; the price and the one-line measure ride after it, in this report.

0. ✅ **K-2a is MET, keyboard only, in both rooms, at my own eye.** T³: `↑` one leg (trace `B`), `←` two seconds, `↑` back → **`return 1 · back where you started · after 2 doors · the room came back the same way up`**, trace **`Bb`**, tally cancelled, ***"every door you opened, you closed — and here you are, home"***; then `a` `b` `A` `B` on top → **`BbabAB` · `a 0 · b 0` · the sentence · `return 5 · … · after 1 door`**, as K-1 sealed. Seifert–Weber: `↑` through door `A`, `↓` straight back → **`return 1 · back where you started · after 2 doors · the room came back turned`**, trace **`Aa`**, tally `a 0`, the sentence — your own parenthetical, verbatim; and the same with a `←` turn in between, the same line. **BACK is the exact retrace by construction** (the same integrator, the direction negated) — a person walks a curved room out and home with no mouse and no turn.
1. ✅ **LAW 22 at the eye, both rooms:** the same out-and-back walked by keys and by pointer is **BYTE-IDENTICAL** across `doors · trace · traceHidden · advances · looks · returnCount · doorsAtLastReturn · sentence · returnLine · faceMark · frameHanded` and all four panel lines — **`differ: []` in T³ and `differ: []` in Seifert–Weber** (eyes 0.002 apart in SW, the page timer's jitter). One motion site, one close, one frame writer; `transportWalk` keeps exactly two callers.
2. ★ **A MEASURED DEFECT IN THE COMMITTED LOOK LAW, found by that witness and repaired in this cut (§5: a mechanism that could not honour the meaning, redesigned — said, and why).** After a PROJECTIVE door the carried frame is unit in the ROOM's metric, not the chart's (chart norms **0.51 · 0.32 · 0.48** at one SW landing), and the old writer rotated with Rodrigues' formula about that non-unit axis and then renormalised in the chart. That is not a rotation there: **a π drag turned 119°, a π key-hold 120°, the two 11.8° apart, and the up axis was rescaled** — so no person could turn round and walk back in a curved room by either instrument (keys `Aae`, pointer `AaB`, neither home). The writer now rotates WITHIN the frame's own planes (yaw in F–R, pitch in F–U): exact in every metric because the frame is orthonormal in the room's own metric, no unit axis, no renormalisation, the same map at E³, handedness untouched. Re-measured on the same landed frame: **a π drag reverses forward to 180.00° with the up axis and the chart norm untouched; a 2009.7 ms key-hold turns 180.87° as the clock says; the two headings 0.82° apart.** Then both instruments came home (¶0, ¶1). ⚠ This changes what the DRAG does after a curved door — for the better, and only there; at E³ the map is the same. **Your record should carry it as a B-114-era finding, not a K-2a feature.**
3. ⛔ **The binding table is ONE spelling, not two, on a measurement.** `a` is door 0 in EVERY room and `d`/`e` are doors in every six-pairing room, so the mandate's W/A/S/D would have crossed door `a` when the hand meant to turn left. So **the alphabet is the doors', by construction**: a single-character key is refused before the table is read (`orderTrace.walkKeyAct`) — the walk keys are `↑/↓` walk, `←/→` turn, `PgUp/PgDn` look — and no room of up to 26 pairings can ever put a door and a walk act on one key. If you want W/S bound anyway, it is one row and the by-construction law becomes a discipline; say so and I bind them.
4. ✅ **LAW 24:** five trusted unbound keys (`z · 5 · shift · Home · space`) mint nothing; a tap released before one frame moves the eye 0.000045 and mints no door; `↑` and `↓` held together cancel to a stop. The key turn and walk are exact to the input clock (517.6 ms → 46.58° expected / 46.58° measured · 1008.2 → 90.74 / 90.73 · 2020.4 → 181.84 / 181.84; pace 0.450 on both legs).
5. **The sign is measured, not assumed:** a +yaw at the writer swung the view RIGHT and a +pitch looked DOWN (shader: `+uRight` is screen-right), so left and up are the negative angles, applied where the rate is spent. The rate is one constant, a quarter turn per second.
6. **The panel line is the keyboard's now and TRUE per room:** flat — `↑/↓ — walk forward and back · ←/→ — turn · PgUp/PgDn — look up and down · a door's letter — cross it, shift for the other way · drag — look around · press and hold — walk forward · the hatch settles in when you stand still · esc returns to the shell`; sealed curved — the same with **`the door letters rest in this curved room, the walk keys do not`** in the letter clause's place. Sighted in both rooms. The wording is the designer's to refine at K-2b.
7. **`K-1c` — the one line:** **YES.** A single trusted `keydown` of `b` in T³, focus on the door button (the handler is on `window`; BODY or BUTTON alike), crosses — but only in the frames: read AT ONCE the seam shows `doors 0 · trace '' · eye unmoved`, and the one mark that is immediate is `advances 0 → 1` (the handler's own); after ~7 s DISPLAYED (frames 63 → 259) it shows **`doors 1 · trace b · return 1 · back where you started · after 1 door · the room came back the same way up`, the eye home.** The keyed advance is consumed by the frame branch (frame-driven) and only while frames run (display-only: the in-app pane's frames tick only while it is displayed). No guard in `onKey` fails her conditions — only INPUT/TEXTAREA/contentEditable targets are refused, `ev.repeat` drops auto-repeats only, the case rule maps `b` → `b`. **What her drive must wait for:** `doors` (or the return line) to change, with the window displayed, ≈ 4.4 s at the default pace (2.0 / 0.450) after the FIRST frame following the keydown — the clock starts at the frame that carries the walk. Measured at `fc55530`, before any cut.
8. **`K-1b` — the price, with its premise re-measured first (smallest measurement first):** *"what remains is the currency"* was MY claim and it is **not the whole story**. I walked the K-1 letter's own line — the entry eye along the named door's plane normal — in every sealed reference room (geometry, no app): **in Seifert–Weber 7 of 12 letters' lines leave the room through a DIFFERENT face than the one named; in L(4,1) 5 of 8; and the transport takes the first plane crossed.** Sighted at the eye in the euclidean-charted L(4,1): **press `b`, the trace writes `c`.** So the letter's AIM is the first defect (a normal from an off-axis eye is not a line through the door in any non-box room); the currency is the second. The price, by file:
   - **(c) THE AIM** — `src/manuscript/ExploreWindow.tsx` (row 189, NOT_FROZEN): aim the press along the chart line from the eye THROUGH the named door (its plane foot `d·n` — a geodesic in the Klein chart), 2 lines in the handler; the clamp unchanged. **⚠ Consequence for T³:** a centre-aimed line is not the lattice axis, so the eye would not come home after one press and the sealed `after 1 door · home` would not come out; a box is the one room where the normal is always the right aim. **Two honest rules, your call:** keep the normal where its line exits through the named face and aim through the door otherwise (the rule reads its own geometry), or aim through the door everywhere and re-seal K-1's T³ word.
   - **(b) THE CROSSING AS THE EVENT, budget in TRUE distance** — the integrator ALREADY spends true distance: `advanceBy` turns the true step (pace × time) into a chart step through the model's own inner product (`raw = step / √⟨dir,dir⟩_eye`), so a true-distance budget is a second counter beside the chart one (≈ 6 lines, same file); stop at the first transport (the frame already reads `seam.doors > doorsBeforeTransport` for the clamp) with a cap of the room's true diameter. In E³ the true span IS the chart span, so T³ behaves exactly as today. **No frozen file.**
   - **(a) THE REALIZATION AT THE SEAM** — the true inradius already exists on the model deck: `ApertureModelDeck.inradius` (`src/manuscript/apertureModel.ts:2001`, filled from `seal.inradius` at :2060 — **SW 0.99638 true / 0.760 chart; Poincaré 0.31416 true / 0.325 chart**); `readCellSurface` returns the surface at :1915 carrying `model: model.model` only. **Spend: `inradius?: number | null` on `ApertureCellSurface` (:1568–1578) + one field at :1915 — 2 lines, row 203, NOT_FROZEN — and the seam reads it.** But an inradius is a number about the CENTRE; the eye stands off-axis, so (a) alone does not make a press honest — (c) first, then (b) or (a).
   - **Frozen union: NONE on any route.** Count: 1 file for (b)+(c), 2 for (a). **STOP: none. The design choice in (c) is the meaning question — Arman's and yours.**
9. ⚠ **A finding beside the price, measured by geometry and NOT yet sighted:** the walk's fixed entry eye `(−0.35, −0.55, 0.1)` (`ExploreWindow.tsx:871`) is inside SW (0.66 < 0.76) but **ON a face plane of L(4,1)** (chart inradius 0.577; `t = 0.000` for face `c`, which is why `b` wrote `c`) and **OUTSIDE the Poincaré cell** (chart inradius 0.325; three planes violated). `apertureEyeFor` (`apertureModel.ts:2267`) exists for the plate and is not read at the walk's seam — one line if the seam adopted it. Not chartered; named so it is not found as a defect. (My Poincaré double-click landed on the SW body twice; I did not chase the sighting.)

## 1 · WHAT I SAW (5174 — my own probe serve, `npm run dev -- --port 5174`, fingerprinted `fc55530` + the batch's dirty paths through `/__whereami`; 5173 was down, Arman's, untouched)

- **T³, keyboard only** (¶0): the out-and-back and the four letters, the panel lines verbatim.
- **Seifert–Weber, keyboard only** (¶0): `↑`/`↓` and `↑`/`←`/`↑`, both home, `the room came back turned`. The letters rest: trusted `a` and `shift+a` changed nothing (doors, trace, advances, eye).
- **LAW 22, both rooms** (¶1): `differ: []`.
- **The look-law defect** (¶2): before the repair, keys `Aae` and pointer `AaB` from the same landing, π-turns of 119° and 120°, 11.8° apart; after, 180.00° and 180.87°, 0.82° apart, both home.
- **K-1c** (¶7) at `fc55530` before any cut; and, unasked, the lens: **`b` → trace `c`** (¶8).
- ⚠ **How I drove:** real (trusted) keys through the pane for the letters and the unbound keys; held keys and pointer holds through the page's own clock (`keydown` … `setTimeout` … `keyup`, `pointerdown` … `pointerup`) so a hold has a stated input duration — the seam integrates event timestamps, so this is the person's own law; the pane's frames tick only while displayed (~30 fps here), and every hold was read after its release. Pace untouched (0.450 measured); `paceOverride` was not used in this cycle.

## 2 · WHAT I RAN

- `npx tsc -b` — exit 0 after each cut and at the full tree.
- `node scripts/diagnose-the-keyed-walk.cjs` — **ALL PASS**, now with **§5** (13 clauses): the binding table RUN and its alphabet law (all 52 letters, digits and symbols refused; W/A/S/D named), one motion site, one close with two `transportWalk` callers, one frame writer in the frame's own planes with the measured sign, the release and blur law, the pointer's hold taking the wheel, no instrument tag, the complete line per room; the K-1 guard and line clauses updated for K-2a.
- `node scripts/diagnose-the-order-trace.cjs` · `diagnose-engine-freeze.cjs` · `diagnose-the-small-run.cjs` — ALL PASS at the working tree.
- **The K-1b geometry measurement** (a scratch script, not a witness — it supports a price): every sealed reference room's letters walked from the entry along their normals; the table in ¶8.
- **Drive family — winding-route** (the walk window is its subject; K-2a changed the pointer's close and the look writer): **14/15 — the SAME parked E.circuit arm and nothing else** (★ the pillar ENCIRCLED — THE LINE, VERBATIM: `return 1 · back where you started · after 9 doors · the room came back turned · doors 9`; every other clause PASS — the cone's `after 2 doors · turned`, the retrace's `the same way up`, the mirrored word `after 1 door · mirrored` — all three sessions ran, no console error; `DIAGNOSE-WINDING-ROUTE: 1 FAILURE(S)`, the one already parked, identical to the K-1 cycle's verdict — so the rewritten writer changed nothing the leg reads at E³). Run first on a quiet tree, my own probe serve stopped.
- `npm run sweep` at the landed HEAD `1d50f0e`, whole, verbatim: `132 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (sweep exit 0; the keyed-walk witness PASS inside it). `npx tsc -b` exit 0 at the same HEAD. **Read by eye from the run's own file, in its own call, before this record was written.**

## 3 · WHAT I CHANGED

- `1d50f0e` — **K-2a**: `src/manuscript/orderTrace.ts` (row 212, NOT_FROZEN: `WalkAct` · `WALK_KEYS` · `walkKeyAct` — the table stated once, the alphabet law by construction); `src/manuscript/ExploreWindow.tsx` (row 189, NOT_FROZEN: `KEY_TURN_RATE` · `neg3` · the held-keys state · `rotateFrame` as the one frame writer (rewritten in the frame's own planes, ¶2) with `turnBy` a pixel→angle shim onto it · `closeWalk` shared by the pointer's up and a key's up · `resolveKeyWalk` / `resolveKeyLook` / `releaseKeys` · the walk keys read before the letters in `onKey`, the letter guard widened to a held walk · `onKeyUp` · `onBlur` · the hold timer releasing the keys at its true time · the frame's held-turn sweep and held-walk branch · the listeners and their removal · the keyboard's panel line, per room · the dead Rodrigues helper removed); `scripts/diagnose-the-keyed-walk.cjs` (§5).
- No new file under the engine roots — no manifest classification owed; no frozen file touched.
- **W1 riders AS FOUND** on the record commit: the camera-gate plan · the connection-layer plan · the claims ledger · the front board · the sovereign ledger (tails close on finished sentences; not read for content).

## 4 · WHAT I COULD NOT REACH

- **The Poincaré sighting** (¶9): geometry says the fixed entry eye starts outside that cell; my two double-clicks at its projected point selected the Seifert–Weber body, and I stopped chasing it. A sighting is owed before anyone calls it a defect at the eye.
- Nothing else. Every acceptance clause of K-2a came out at the eye; K-1b is a price; K-1c is answered.

*All three letters archive with this landing.*
