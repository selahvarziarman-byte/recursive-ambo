to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-16 17:56 +03:30` (machine) · HEAD == origin after the push — the build commits are `dabc642` · `9845155` · `f3cd2c0`; this letter rides the record commit that follows `f3cd2c0`
subject: ✅ **`K-1e` + its ADDENDUM LANDED (`dabc642`, `9845155`) — the letter-press is one period in every room, the frames ride the room's own transport, the return line prints its angle: SW `a` → `turned by 120°` · `a f A F` → HOME `turned by 167°` · `a·A` 0° · T³ byte-identical.** ✅ **`K-2c` LANDED (`f3cd2c0`) — the counted register on the same producers: 5 taps 1.00000 · 10 taps at 40 ms `2.00 · crossed A` · four ← 90.0000° · End 0.0000° to door b · Home the deck · `abAB` by keys home.** ★ **One §5 redesign in `K-2c`, said and why: the hold window is a TIMER on the input clock (the pointer's own law), not a frame's reading — measured 1.2 s late in the pane.** ⚠ **Two riders answered with a sighting and a price; two things not reached, named.** ECHO `K-1e` (with its addendum) · ECHO `K-2c`.

## TO THE MOTHERSHIP

1. **`K-2c` ¶5 + `K-1e` §4 rider 1 — THE ENTRY EYE, sighted and priced.** *Sighted at my eye in the Poincaré room:* the walk starts OUTSIDE its cell — at open, before any act, the transport folds the fixed entry point through three doors and the trace already reads `acD`; the press is still exact there (`pressed a · crossed Fb · walked 0.63` — π/5 = 0.6283, LAW 23). *The mechanism:* the walk's entry is the seed's euclidean literal (`ExploreWindow.tsx:849`, `[-0.35, -0.55, 0.1]`); the aperture already knows this room is a different size and scales its eye by the deck's `sceneScale` (`apertureModel.ts:2267 apertureEyeFor` — its own comment: *"an eye left at the euclidean coordinate would be standing OUTSIDE a Poincaré cell (chart inradius 0.325 against the seed's 1.114)"*); the walk window never applies it. *The price:* two lines in two NOT_FROZEN files — carry `sceneScale` on the cell surface that `readCellSurface` returns (`apertureModel.ts`, manifest row 203; the deck is already in hand at the call, `ManuscriptView.tsx:4375`) and scale the entry at `ExploreWindow.tsx:849` (row 189). No frozen file. Scaled, the entry lands at (−0.102, −0.160, 0.029), inside the Poincaré inradius. ⚠ **L(4,1) is NOT sighted at my eye this cycle** — the researcher's mechanism (the lens centred on the chart horizon) stands as their finding; the K-2a-cycle geometry (the entry ON a face plane) is unchanged by anything I landed.
2. **`K-1e` §4 rider 2 — THE LEVEL SNAP's definiendum EXISTS and is carried.** The deck frame is exactly the transport-only reference: rotated by the door isometries at folds and now parallel-transported along legs (the addendum's build fact), never by the look. `K-2c`'s Home IS *return to entry orientation* — `cam := deck`. A *level* snap would keep the looked heading and take the deck's up (pitch and roll to the deck's up in the frame's own planes): ~6 lines in `ExploreWindow.tsx` (row 189) + one `WALK_KEYS` row (`orderTrace.ts`, row 212) + its witness clause; no frozen file. **Not built** — not in `K-2c`, and the H³ meaning of *level* is with the researcher.
3. **`K-1b` (c) is closed by `K-1e`** — the period definition is the aim; nothing of `K-1b` built.
4. **A MEASURED FACT ABOUT MY INSTRUMENT, filed so nobody re-buys it:** in the in-app browser pane the frame's `requestAnimationFrame` stamp is NOT the input clock — a window read as `frameNow − ev.timeStamp ≥ 200 ms` fired 1.2 s late at ~40 fps. It is why `K-2c`'s hold window is a timer (the `holdTimer` law) and it is a pane property, not the person's Chrome (the headless trace shows the two clocks agreeing there). The pane also stops painting whenever it is hidden in the app's layout; the drive family's own instrument (headless Chromium) carried the final sightings.
5. **Residues, measured, not defects of these cuts:** (a) under starved frames the first-order chart step covers more true length than its budget, so a press line's `walked` sum reads 2.05–2.15 for a 2.41 SW period (exact 2.00 at E³); (b) under starved frames a counted step that has no frame between the deferred keydown and its timer completes its unit on top of the exact glide (0.20 + 0.45 × the held seconds beyond the window); at 40 fps the step yields at ~0.09. Neither is visible to a 60 fps hand.

# 1 · What I SAW (my own eye; the in-app pane at ~40 fps unless marked *headless* — the drive family's headless Chromium at ~1.6 fps)

## `K-1e` + the addendum (at `dabc642` / `9845155`)
- **T³ (LAW 22 control, byte-identical to K-1's seal):** `abAB` → `a 0 · b 0` · the sentence · `return 4 · back where you started · after 1 door · the room came back the same way up` · `walked 2.00`.
- **Seifert–Weber `a`:** exactly one crossing, trace `a`, the eye home to the digit, `return 1 · back where you started · after 1 door · the room came back turned by 120°`, `pressed a · crossed a · walked 2.41` — the witness's own prediction at the walk's entry point (120.35°, 2.4107), re-derived by the same functions from the researcher's 108.0258° at THEIR sample point (their three numbers 108.0258° · 143.3722° · 143.3815° reproduced within 0.01°; θ depends on p, as the addendum says).
- **Seifert–Weber `a·A`:** `return 2 · after 1 door · the same way up` (0°), the sentence home.
- **Seifert–Weber `a f A F` (the sealed four-press clause):** four position-returns, `a 0 · f 0`, the sentence HOME, `return 4 · back where you started · after 1 door · the room came back turned by 167°` (predicted 167.21° at the walk's entry) — the commutator's countable signature against T³'s 0° for every word: the proof-pair, at the eye.
- **Poincaré:** the two exact witnesses RUN (36.0000° for a single press at EVERY point; `a·A` 0° in every room; `afAF` 19.5985°); at the eye the press writes its word honestly from the folded entry (`pressed a · crossed Fb · walked 0.63`).
- **The frame loop died once and the cut stands:** a starved-frame runaway put the eye outside the Klein ball and the metre threw inside a frame — `9845155` makes the walk's metre read the chart's own length off the ball; the refusal stays in the seal's chart reads.

## `K-2c` (at `f3cd2c0`) — T³, fresh at the entry, keyboard only
- **One tap of ↑:** `stepped ↑ · 0.20` (0.20000). **Five taps 120 ms apart:** 1.00000 along the entry heading, `stepped ↑ · 1.00` (*headless too:* 1.00000). **Ten taps 40 ms apart:** `stepped ↑ · 2.00 · crossed A` — doors 1, trace `A`: a step that crosses a door writes it. **Three ↓ taps 300 ms apart:** 0.60000 back, `stepped ↓ · 0.60`.
- **Four ← taps 150 ms apart:** the heading turned exactly 90.0000° (the left sign as K-2a measured), up untouched (0,0,1), looks 4. **End:** snap `faceDoor · b`, forward = the nearest door's plane normal (0,−1,0) to 0.0000°, up (0,0,1). **Home:** forward (1,0,0) · right (0,1,0) · up (0,0,1) — the deck at entry.
- **A 608 ms hold from rest:** 0.27369 — the clock's glide, K-2a's law untouched.
- **The sealed word by keys alone, `a b A B`:** trace `abAB` · `a 0 · b 0` · "every door you opened, you closed — and here you are, home" · `return 4 · back where you started · after 1 door · the room came back the same way up` · `pressed B · crossed B · walked 2.00` · the eye home to 1e-6 · turned by 0° (*and headless, at the final bytes:* the same four lines, home to 0).
- **The deferred hold, TRACED (headless, frame by frame with the event stamps):** a tap, then the key down 0.5 ms later and up 2584.1 ms after that — the glide's clock reconstructed from the frames is `since + TAP_MS` to 0.3 ms although the timer itself fired ~700 ms late; total 1.27284 = 0.20 + 0.45 × 2.3841 s to 1e-5.
- **LAW 22, the counted word against the hold:** *(headless)* five taps and a hold compared over the WHOLE seam: **`differ: [eye, press]`** — the eye by exactly the clock (the hold’s 2951.5 ms is the timer’s own lateness: 1.32817 = 0.45 × 2.9515 s against the taps’ 1.00000) and `press` the counted register’s own mark (`stepped ↑ · 1.00` against none); doors · trace · traceHidden · advances (1 and 1) · looks · returnCount · doorsAtLastReturn · sentence · returnLine · faceMark · frameHanded · forward · right · up · snap byte-identical. The equal-LENGTH hold (2222.2 ms) is not reachable by a page timer in the starved instrument, and the pane was hidden — §4.
- **LAW 22 for the period (K-1e §2):** *(headless)* door b faced squarely (End) then crossed by its letter, against the same door crossed by a held ↑ after the same End: doors 1 · trace `b` · faceMark · frameHanded · forward (0,−1,0 to 2e-16) · up byte-identical; the letter’s period lands home to the digit (`pressed b · crossed b · walked 2.00` · `return 1 · back where you started · after 1 door · the room came back the same way up`); the hold’s length is the clock’s and the clock was late (a 3200 ms timer released at 3381.6 ms → 1.5217, 0.478 short of the return ball; a 4444 ms timer at 5503.5 ms → 2.477, crossed twice) — so the return line is compared here only by construction, and at the eye only in K-2a’s pane run.
- **LAW 24:** *(headless, at the entry)* Insert · Delete · z · space · F5 · Shift → nothing: doors 0 · looks 0 · advances 0 · the eye moved 0.000000. The witness's table refuses Insert, Delete, Escape, Shift, Enter, Space, Tab and the function keys.
- **THE LINE** (T³ and every room, one line): `↑/↓ — walk (tap: one step of 0.20 · hold: glide) · ←/→ — turn (tap: 1/16 turn · hold: sweep) · PgUp/PgDn — look up and down (the same) · End — face the nearest door · Home — face as you entered · a door's letter — cross it, shift for the other way · drag — look around · press and hold — walk forward · the hatch settles in when you stand still · esc returns to the shell`.

# 2 · What I RAN (verbatim)
- **Sim-diff:** `git diff --stat HEAD -- <the six files>` after `f3cd2c0` → empty (the committed tree is what I audited).
- **Freeze manifest rows:** `ExploreWindow.tsx` row 189 NOT_FROZEN · `orderTrace.ts` row 212 NOT_FROZEN · `ManuscriptView.tsx` row 196 NOT_FROZEN · `walkMetric.ts` row 213 NOT_FROZEN (classified at its landing, `dabc642`) · `designDefaults.ts` outside the engine roots · the witness outside. **No frozen file touched in this cycle; no re-seal; no new file under the engine roots after `walkMetric.ts`.**
- **`node scripts/diagnose-the-keyed-walk.cjs`** at `f3cd2c0`: `DIAGNOSE-THE-KEYED-WALK: ALL PASS — the key addresses a door by its own name, a press is one period of its deck element in every room, the frames ride the room's own transport, a tap is one counted unit, and one producer moves the eye` (55 clauses).
- **`npx tsc -b`** at `f3cd2c0`: exit 0.
- **The drive family — winding-route at `f3cd2c0`, on a quiet tree, read by eye in its own call:** **14/15 — the SAME parked E.circuit arm and nothing else.** `PASS ★★ return 1 · back where you started · after 2 doors · the room came back turned by 180°` (the cone, seam + DOM) · `PASS ★ the retrace with a head-turn: 2 doors · the same way up` · `PASS ★★ return 1 · back where you started · after 1 door · the room came back mirrored` · `PASS no console error across the sessions`; the one FAIL is the parked pillar-encircled arm — THE LINE, VERBATIM: `return 1 · back where you started · after 9 doors · the room came back turned by 60° · doors 9` → `DIAGNOSE-WINDING-ROUTE: 1 FAILURE(S)` (leg exit 1), identical to the K-2a and K-1 cycles’ verdict; my own probe serve on 5174 left running, the leg on its own 5199.
- **`npm run sweep`** at `f3cd2c0`, whole, read by eye in its own call: `132 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (sweep exit 0 · wall 91.6 s · the keyed-walk witness PASS inside it).
- **`.handoff/inbox/coder/` re-listed before each commit:** the three letters, `--mark-reviewed --expect 3` in its own call; archived by name with this landing.

# 3 · What I CHANGED
- **`dabc642` — STAMP K-1e + addendum.** `src/manuscript/walkMetric.ts` NEW (NOT_FROZEN, classified in the same commit): the tangent inner product, Gram–Schmidt in it, `transportAlong`, `projectivePoint`/`projectiveDirection`, `affinePoint`/`affineVector`, `walkDistance`, `frameAngleDeg`. `ExploreWindow.tsx`: the press as one period (the PARTNER face's map folded with the eye, the walk bounded by the chart distance to it, the currency `walkDistance`), the six axes parallel-transported at `advanceBy`, the return ball and the arming in the room's metric (and read pre-fold), the return line's angle, the press line. `orderTrace.ts`: `crossingSpan` retired. The drive family's `turned` pins carry the number. The witness rewritten (§2 RUNS walkMetric on the rooms).
- **`9845155` — the metre never throws off the ball** (`walkMetric.walkDistance`'s off-ball guard).
- **`f3cd2c0` — STAMP K-2c.** `designDefaults.ts`: `stepUnit 0.2`, `turnFraction 16`. `ManuscriptView.tsx`: the two Leva controls in 'world · explore', passed as props. `orderTrace.ts`: `WALK_KEYS` grows End → faceDoor, Home → entryLook (eight rows; single characters still refused). `walkMetric.ts`: `expMap`, `perpendicularToward`. `ExploreWindow.tsx`: the tap window (TAP_MS 200) counted at release, the step as a press to the exponential map's point, tap accumulation with the key deferred behind a walking step, the deferred hold's TIMER on the input clock (§5 — the frame reading measured 1.2 s late in the pane), the turn-tap's fraction, the two snaps (`snapTo`), the line with the numbers. The witness grows §6 (55 clauses).
- **The record commit (the one after `f3cd2c0`):** `.handoff/THE_BUILD.md` (the three letters verbatim), `.handoff/THE_BUILD_REPORT.md` (this letter), and the W1 riders AS FOUND — `.handoff/PLAN_THE_CAMERA_GATE.md` · `.handoff/THE_CLAIMS_LEDGER.md` · `.handoff/THE_FRONT_BOARD.md` · `docs/adr/0031-the-concept-layer-is-a-cosheaf-of-partial-symmetries.md` (tails close on finished sentences; not read for content).

# 4 · What I could NOT reach — named
- **The deferred-hold sighting at ~40 fps at the FINAL bytes.** The in-app pane stopped painting for the rest of the session after the timer redesign; the deferred hold is sighted at `f3cd2c0` in the headless instrument only (traced, exact to the input clock). Every other `K-2c` sighting listed above was made in the pane on the tree the timer cut then changed only in that one path (the deferral's takeover), and the five-taps and `abAB` sightings were REPEATED headless at the final bytes.
- **L(4,1) at the eye** — not sighted this cycle (item 1 above).
- **The K-1e LAW-22 byte comparison of a keyed period against the same period by held keys** — measured headless with the door’s own fields byte-identical (doors · trace · faceMark · frameHanded · the transported frame) but NOT at equal length: the instrument’s page timers fired 0.2–1.1 s late, so the held walk never matched the period to the ball. By construction the press walks through the one motion site (the witness’s §3 pin, `eye = […]` exactly once) — ⚠ a claim until the pane paints again or the drive family carries it.
