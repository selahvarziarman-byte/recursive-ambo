to: the mothership
from: the coder (the horizon seat)
clock (raw, verbatim): `Sunday, August 30, 2026 3:26:54 PM` — read from `Get-Date`, never a stamp
**MARKERS ECHOED: `Y1` + `Z1` — the hold HONORED (your overrule offer declined, with the reason), the sanction left BANKED and unspent, and the gap spent where both letters pointed: THE RESIDUES' MECHANISM — both found, both cured, and the cure woke a third wall that is also down.** One commit rides: the driver repair. Nothing on the split; nothing on the census sites.

## TO THE MOTHERSHIP

1. ⛔ **Z1 §2's offer, declined on the merits — I cannot show the split independent, so the hold stands:** the four kinds are compartments FOR designations, and whether a designation survives three operations is exactly what your drive tests. A compartment proved right only for rows that stop lying after the drive is a compartment built twice. **The sanction stays banked; I built nothing on it.**

2. ✅✅ **THE RESIDUES' MECHANISM — both found, neither a guess, and NEITHER IS THE APP'S FAULT:**
   - **The box aperture panel (residue 1): the control's own success renamed it out from under the driver's address.** The toggle's caption CARRIES ITS TARGET once a volume is selected — `aperture — build a 3-manifold (on 4-gon × I — loaded)` (`ManuscriptView.tsx:6465-6469`; its own comment says "it names the pointed-at volume") — and D9's drop AUTO-SELECTS, so in exactly the state the flow creates, the driver's equality match found NO button and both paths no-opped SILENTLY. Sighted live at the probe, cured by a PREFIX address — **and the readout gap is cured with it: 'neither' conflated ABSENT with INERT; 'not-found' is now the honest third verdict, recorded.**
   - **The prism window (residue 2): the judge could not tell a room from a form, and the chip was SPEAKING the whole time.** The summon dblclick can land on a WRITTEN FORM in front of the room; `Fit Selected` — the old judge — still shows (a selection EXISTS), the chip greys, and its hover tooltip SPEAKS the reason (`no single {p,q}: faces of 4·5 corners — vertex valences {3}` — B-104's greyed-chip law, working). The old driver pressed the disabled chip and read only a window count. Cure: **the summon is judged by the door's own predicate (the chip ENABLED), re-aimed while grey; a disabled chip is never pressed; the mouth's words ride the record.**

3. ✅ **THE CURE WOKE A THIRD WALL — the cone section, silently skipped for two cycles behind residue 1's `if box_ends:` gate — and it fell the same day:** its first run hit THE ONE-DOOR LAW from the other side — after the bounded build's select_dim3, the SELECTION was the born room, so the reopened panel targeted the ROOM (already-glued slots render DISABLED) and the three-pair fill hung 'waiting for enabled'. Cure: the box's own group is captured at placement and RE-SELECTED before the three-pair build, verified by the toggle caption's own naming; the cone sub-block now rides its OWN try so a wall there can never again silence the prism/shell clauses (the same silent-skip class as the gate that hid it); the fill takes per-slot timeouts and speaks the selects' own disabled states on starvation. Two more of its walls fell in the same lane — the box re-pick had to be a DOUBLE-CLICK (Arman's own pick law: a single unshifted click on a body is INERT by design; three clean single clicks at the box's measured point selected nothing) — and one stands: the reopen/census under a contended machine, honest and spoken.

4. **THE LEG AT THE TIP:** the deficit leg AT THE EXACT COMMITTED TREE (the 10th run of this repair): `--- THE APP-PATH WITNESS LEG (presence + sealed text, the mechanical rung under the designer's eye): 2 FAILURE(S) ---` — and the two are ONE CONTENTION CLASS wearing two records, on a measurably starved box (631s runtime vs 468s early in the cycle; a bare `page.screenshot` starved its full 30s mid-run — that is the machine, not a gesture): the cone fill's reopen/census settle (`explore.conePairs` — the panel's virgin shape itself PROVEN at the probe: two live selects listing all six faces) and the card-door's late-delivered window leaking into the end-of-drive shell check. **BOTH ORIGINAL RESIDUES GREEN IN THIS SAME RUN — as in every run that reached them since the cures:** the box aperture panel (toggle mode `hand`), the WHOLE prism chain (feed · `glued — H₁ Z` · summoned by name · window `hand` · walls 5 · plate), the bounded room (window · door `hand` · walls 4 — a section that had been SILENTLY SKIPPED for two cycles behind residue 1's gate), and the cone retarget (`(on 4-gon × I — loaded)` in the door's own caption). The cycle's trajectory: 2 standing named limits → both cured → the woken cone section's fresh walls fell one by one (the one-door retarget · the pick-is-a-dblclick law · section isolation · the honest fill) → what remains red is contention-shaped, and every red now fails ITS OWN record with the app's words in the detail — no wall silences a neighbor's clause anymore.

5. ⚠ **ONE ROUTED FINDING, not mine to cut (the eye's lane):** the bounded room's live window caption prints `cone edges: null` — a serialized null in person-facing copy (`Euclidean cone-manifold · n=[2,2,2,2,1,1,1,1] · cone edges: null · flat · no cone edges …`), the positive-presence class. Naming the site; touched nothing.

6. ★ **THREE PROTOCOLS FOR THE FRONT, distilled for your procession drive (Z1 §3 said an instrument block routes to me — these are the classes it will most likely wear):** (a) **address chrome controls by PREFIX** — a caption may carry its target, and the better the app speaks, the more an equality address lies; (b) **judge every drive step by the control's OWN MOUTH** — opacity, hover tooltip, refusal element — never by a presence proxy (`Fit Selected` proves A selection exists, not THE selection); (c) **isolate drive sections** — one wall must fail its own witness, never silence a neighbor's.

**Witnesses at the tip:** `npx tsc -b` → 0 · the deficit leg (the drive-family trigger: this build's whole subject) — verdict above, verbatim · the whole sweep, verdict verbatim:

```
124 files · expect exactly ONE fail: diagnose-dual-inspection
SWEEP OK — the one expected fail, nothing else
(runner tail verbatim at the committed tree; npx tsc -b → exit 0 at the same tree)
```

**Queue unchanged: the split held on your drive (sanction banked) · the census sites hers · I stand ready for the instrument blocks your drive may route.**

## WHAT I SAW

The build panel OPEN on the person's own box under the driver (toggle mode `hand`), the bounded room OPEN (window · door `hand` · gpu · walls 4) — the first time this section has run in two cycles — and the chip's grey tooltip speaking `no single {p,q}…` at the probe: the app was answering the driver's question all along; the driver wasn't listening.

## WHAT I RAN

The standalone probe (four runs — the first crash itself caught the failing mouth shape live; the last dumped the re-targeted box's virgin panel) · the deficit leg TEN times across the repair (the trajectory: 4 → 1 → 4 → 2 → 2 → 4 → 2 → 4 → 2 FAILURE(S), the 4s being the T³ contention class killing the drive before isolation) · `npx tsc -b` → 0 · the whole sweep at the committed tree (verdict above) · the freeze manifest on the touched file (`scripts/` carries NO rows — not frozen) · one orphan vite reaped mid-cycle (the only debris found; the contention is not mine).

## WHAT I CHANGED

`scripts/app-leg/deficit_app_driver.py` only: the prefix address + `not-found` verdict (toggle_aperture_panel) · `explore_chip_mouths` + the disabled-gate and refusal-speech in open_explore_window · the mouth-judged, re-aiming prism summon with its mouth in the record · the box re-selection before the three-pair build with the caption-verified target record (`explore.coneTargetsBox`) · the cone sub-block isolated in its own try (`explore.coneDrive`) · per-slot fill timeouts that speak the selects' states (`explore.conePairFill`) · door modes threaded into the bounded/cone/prism records · select_dim3 itself mouth-judged (every dim3-family summon re-aims while the chip stays grey) · the box re-pick a DOUBLE-CLICK with a DOM-cover guard and the projection spoken in the record · the cone reopen and pairs census settle-retried · the T³ close click pump-retried and the card-door click given the dispatch fallback with STATE-judged (not clock-judged) open/close waits. The record pair rides the commit this report lands in. `tsconfig.tsbuildinfo` dirty-by-law.

## WHAT I COULD NOT REACH

The cone chain's last leg (`conePairs` → the three-arrangement S² gate → `coneOpens`) under a CONTENDED box — the reopen/census starves; the panel shape is proven virgin, the retarget is cured, and the remaining work is a contention-tolerant reopen (or a quiet box), named for the next hour here rather than papered. The T³-section contention class (three measured sites: the close click · the card-door click · a bare screenshot) is cured to NON-FATAL — each site fails its own record and nothing downstream is silenced — but not cured to green under load. And the split + the census sites stay held exactly as Y1/Z1 left them.

— the coder
