to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-24 17:30 +0330` (machine — `date`) · HEAD == origin == `7986b57` (the STAMP `0086075` + the cure the sweep asked for `7986b57`) · the record commit follows this letter
re: **`STAMP C-11a` LANDED — THE DOOR's ACT at the ROOM's door (§133, Option R): the transport rides the pairing row; one pointed pair takes its WHOLE line; refused by the lines at the parting edge, by the record at a corner; descent read from EITHER face, on the carried record through the C-10 reader; the reference's fixture census reproduced number for number; persisted by the page file as it stands; no frozen file. ECHO §133.**

## TO THE MOTHERSHIP

1. **Measured, not built — the grain-law residue cannot be glued today.** The residue at A lifted from the gen-2 shape (7 corners — the three midpoints of its shared face ride, C-10's grain law) refuses EVERY candidate of the door A·AC·AB ~ A·AB·AD inside `buildPersonDomainVerdict`: `level3LinkExtractor: edge … lies on 0 faces of cell …` (the cell's record names its coarse face; the lifted shape holds the four finer ones). The door's ACT works on it (the lines read, the acts take and refuse — measured), the GLUE does not. Printed in the witness, not pinned, not cured: the cell reader is `src/lib/level3*` — frozen ground I did not open. **The room at the eye is therefore built from the residue lifted from the gen-1 shape** (4 corners; the hinge map `d-0` glues sound, H₁ = 0).
2. **Measured — the Ambo mounts no way back to an earlier shape.** `GenealogyViewer` (the shape list with `selectShape`) is defined in Panels.tsx and mounted nowhere; a person who has dissected twice cannot select the gen-1 shape again in the UI. The eye leg lifts the gen-1 residue at A BEFORE the second dissection. Said, not cured (a surface question — the designer's).
3. **The D10 control, sharpened on the record.** The one-way reading's blind spot is a role at the END of a path on the face called B (a predecessor there through K, none on A through J, nothing ahead on either): (N)'s one diagonal sees A's predecessors and never B's. On the gen-2 residue's door read with the born pair's face as A the one-way reading admits nothing the two-way refuses; read the OTHER way it admits 7 — the born pair's line ends at r1 on AB, and the one-way reading lets the other face's r1 cross to it. The model refuses them from either face. On the fixtures: 51,256 pointed pairs the one-way reading admits and the two-way refuses, 0 the reverse.
4. **The word pairs at the door — no act exists.** `DoorTransport.types` is carried as the record's shape you named; every act writes `[]` (a true absence — the designer's 1500 §1 gives no word gesture at the door). Said.
5. **One words rule of mine, said:** when the two role names coincide (the hinge corner AB the two faces share — or two casts spelled alike), the refusal names the faces: `r1 (on A·AC·AB) has a predecessor and r1 (on A·AB·AD) has none` — the sentence never says one name of two roles. Each side carries its face's OWN D14 name (never the door's order, which runs the other way on B).

## What I SAW — the drive family, `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS` (107 clauses — 91 → 107, eight new arms per viewport; `/__whereami` = `5be36f9`, dirty — the C-11a tree; the leg ran three times on this stamp — the first found the C-10b arm leaving the lifted drawing open on the sheet (its overlay intercepts a drag onto the canvas — closed first), the second the driver's hardcoded role names (at the eye C holds the T cell, so AC's classes read otherwise — the picks chosen by structure now) and the panel's bottom toggle under the selected form's acts bar (closed by the panel's own ×), the third whole on both viewports)

At 1689 × 897 (and at 1400 × 900): the gen-1 residue at A lifted before the second dissection sits on the shelf beside the C-10 form and, placed by a drag, reads its four corners on the record (`A · AB · AC · AD`); `aperture — build a 3-manifold (on …)` opens the aperture ON it; in the first row A·AC·AB and A·AB·AD picked from the two face selects and the hinge map `A→A · AC→AD · AB→AB — preserving (derived)` from the third; **under the row the door's act mounts in the EMPTY STATE — the designer's words verbatim: `the door A·AC·AB → A·AB·AD — glued by you · carries no concept yet: a cargo crossing it arrives did not return`** — the three corner pairs `A→A · AC→AD · AB→AB`, both sides' roles as chips (14 · 19 · 20 and 14 · 23 · 20 — the record as the run's earlier acts left it), ONE computed style over every untaken chip whether or not its role can cross (nothing lit), no line, no refusal, no cannot-cross line (on this door every role has a line opposite). The door block's box (30, 291) 274 × 477 px inside the rows region (30, 202) 274 × 197 px (scroll height 649 px; its top INSIDE the region (its first 108 px show under the row's three selects) at scroll 0, inside once scrolled to — printed for the designer's plate). **F1 at A pointed, then F1 at A on the other side → `taken — F1 ↦ F1 at A, and with it the whole line along A→AC→AB→A: F1 ↦ F1 · r2 ≡ F1 ↦ F1 · F1 ↦ F1`** (F1's class at AC named by the run's own pair on C–A — at the eye C holds the T cell); the line stands `yours · a whole line along A→AC→AB→A: …` with exactly ONE hand `withdraw the line F1 ↦ F1`, 6 chips marked yours, the head `carries 3 role pairs on 1 whole line`. **F2 at AC (an A-role — every A-role rides the cycle through the corner edges) pointed at Φ1 at AD (D's own, a line of one) → `not taken — along AC→AB, F2's line runs on and Φ1's stops` · `here, at the door: withdraw this attempt`** (the standing line keeps its own hand; the local hand clears the attempt). **at the hinge corner AB the two faces share, Φ4 pointed at Φ9 (two of B's Φ roles, lines of one, equal in shape) → `not taken — at AB, the record would say two things about member_status(Φ4): has here, none-by-nature there`.** The one hand withdraws the whole line (the empty state returns, its words again); F1 ↦ F1 given again; **`glue — the S² gate judges` → `glued — H₁ 0 · the aperture opens in the dim-3 band`**, the rows reset, the built record carrying the transport. Plates: `concept-layer-door-{empty,taken,refused,record,glued}-1689x897.png` in the ignored `_frames/`. At 1400 × 900 the same words, the same boxes (the rows region 274 × 198 px).

## What I RAN

- **`scripts/diagnose-the-doors-act.cjs` (NEW) — ALL PASS, 34 clauses.** §1 THE FIXTURES — the reference's D8 reproduced NUMBER FOR NUMBER: the prism of two first faces (F · T · Φ), the translation door, the 42 hand triples verbatim from `born_face_reading.py`, 42 × 42 ordered doors, every pointed pair at every corner (665,028): at F taken 90,036 · along 184,742 · into 70,542 · rounds 424; at T 26,416 · 96,668 · 53,012 · 304; at Φ 19,480 · 72,174 · 51,006 · 224; of the taken, refused by the RECORD F 0 · T 0 · Φ 5,880 (jRegister's `refusalOf` and the reference's glue agree); the line census 803 (795 paths, 8 cycles); the identity control 42 of 42; (N) total at F 42 classes of one; the roles a door CAN carry F 18,714 of 24,696 · T 12,434 of 17,640 · Φ 10,269 of 15,876; the largest lawful transport unique 0 of 1,764; D2 — the extension exists ⇔ equal shape and place (0 disagreements against the (shape, place) mechanism carried in-memory), lawful two-way (0 failures), exactly the whole line-pair (0 failures); D3 — the empty transport refused on 0 of 1,764 doors; D10 — the one-way reading (forward propagation, carried in-memory) admits 51,256 pointed pairs the two-way refuses and none the reverse; every refusal class in the one grammar (3.0 s). §2 THE LAWFUL PATH — casts on A · B · C · D, the pairs on A–B (3 + τ) and A–C (2), two dissections, the born pair Φ2 ↦ r1 on AB–AC; the residue at A lifted from gen 1 (4 corners) and from gen 2 (7) and loaded through the shelf's door; the door A·AC·AB ~ A·AB·AD: the six candidates' verdicts (gen 1: `d-0` sound H₁ = 0 · `d-1` · `d-2` sound H₁ = ℤ · `d+1` folded · `d+0` · `d+2` not sound; gen 2: every one throws — item 1 above); THE SIDES READ ON THE RECORD (the seed edge A–AC the person's record, the corner edge AB→A the carried coprojection, the medial edge AC→AB the anchored meet): face A·AC·AB 28 lines (14 cycles of one round — A's roles carried all the way round — 7 + 7 lines of one at AC and AB), face A·AB·AD 30 (14 · 9 + 7), the hinge edge's J identical both sides; every pointed pair on the gen-1 door on an empty transport: A 196 taken; AC 245 taken · 224 along · 14 record; AB 245 taken · 196 along — no other class; TAKEN the whole cycle (one transport entry per corner pair, one role pair each, the word pairs empty); the parting-edge words; the record words; THE FORM (`not taken — F1 is already on a line of yours (F1 ↦ F1 — withdraw that line to point it again)`); two lines standing lawful with ONE hand each; the whole-line withdrawal to a true absence; on the gen-2 residue **the person's own born pair is exactly what cannot cross** (`cannot cross this door — no line opposite: on A·AC·AB Φ2 at AC · r1 at AB`); the one-way control read from both faces (item 3). §3 PERSISTENCE — the verdict door ignores the transport (the same H₁, the same pairing map); `serializePage → JSON → parsePage → loadPage`: the built record's rows carry the transport byte-equal (three entries; the open row without the field), one built domain restored, no refusal, the file holds no line/verdict; the restored record (the shelf's ancestor under the seed's id) re-derives the same 28 · 30 lines, the transport lawful on them, the same two hands. §4 THE SURFACE under node — the empty state verbatim, 114 chips one style, 12 chips marked for 6 pairs, ONE hand per line (2 for 6 pairs), the refusal with its local hand, the cannot-cross line italic and once, C-10's absence on a form with no record; the chrome mounts the section under the complete row between the map select and the exits (`data-aperture-select` faceA · faceB · map); the view's wiring; purity (the model's six from-clauses; the section imports the model alone); the manifest rows; the record's type beside the row.
- **the lift carries 31 · the aperture ALL PASS · the freeze exit 0** (the two new src files intent-staged and classified NOT_FROZEN in the STAMP). Every earlier concept-layer witness stands as it was — the sweep at the landed HEAD reads them all, whole, in the record commit.
- **`tsc` exit 0** at the STAMP.
- **THE DRIVE FAMILY** — above. The served tree fingerprinted before anything was believed (`/__whereami` = the commit the leg was started from, 15 dirty paths — this build's). the leg ran three times on this stamp — the first found the C-10b arm leaving the lifted drawing open on the sheet (its overlay intercepts a drag onto the canvas — closed first), the second the driver's hardcoded role names (at the eye C holds the T cell, so AC's classes read otherwise — the picks chosen by structure now) and the panel's bottom toggle under the selected form's acts bar (closed by the panel's own ×), the third whole on both viewports.
- **The sweep, whole, read by eye in its own call — at `0086075` (the STAMP): `142 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP RED — unexpected fail(s): scripts/diagnose-the-concept-type.cjs` (140 PASS, the accepted fail, one red; wall 97.1 s; the doors-act witness in the census at 4.5 s).** THE RED: the concept-type witness §4 (THE CAST HAS EXACTLY ITS TEN — the files that may mention `.cast` or `ConceptSpace`) found the door model naming `ConceptSpace` for the spaces it holds per corner. THE CURE, by construction (`7986b57`): the model reads spaces AS THE RESOLVER HANDS THEM, so its type IS the resolver's output — `type CornerSpace = Resolved['space']` — and the file names neither `.cast` nor `ConceptSpace`; a type alias erases, no behaviour moves (the from-clauses unchanged; the eye leg's third run served the tree before the cure — not re-run, said). **At `7986b57`, whole, read by eye in its own call before this letter was written: `142 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else`** (exit 0 — the census 142, the doors-act witness in the sweep by its own declaration at 4.8 s, the lift carries 1.8 s, the concept type 14.8 s; the gate 79 s, the door column 76 s, the j-register PASS at 69 s inside its declared ceiling, the born room 48 s; wall 92.1 s). tsc 0 at both.

## What I CHANGED (no frozen file)

1. **`src/manuscript/apertureModel.ts`** (203 NOT_FROZEN) — `AperturePairRow.transports?: DoorTransport[]` beside the row's three fields; `DoorTransport { corners: [faceA's corner, its image]; roles: [x, y][]; types: [s, t][] }` — the person's INPUT beside `FacePairing`, never inside it (`faceIdentification.ts` untouched, 0 mentions).
2. **`src/manuscript/doorTransportModel.ts`** (NEW, NOT_FROZEN, react-free) — `sideOf(record, cycle, memo, name)`: each corner's space through the one resolver, each boundary edge's J by its kind (`bornStepOf`) in the door's direction, the LINES (`linesOf` — paths and cycles by shape and place; the reference's `line_structure`); `extendPair` (the reference's `extend`, propagated both ways — the least lawful e or the first place the lines part); `lawful` ((N) both diagonals); `actAt` (the form · the lines · the record through `refusalOf` on the whole e at each corner touched · TAKEN with the record `transportsFrom`); `withdrawLine` (the whole line-pair); `doorReadingOf` (the corners with both sides' roles and who crosses, the taken lines grouped by A's line with one hand each, the cannot-cross line once, the standing transport's lawfulness); the words (`emptyDoorWords` verbatim hers; `takenWords`; `refusalWords` in the one grammar; `doorHandWords`).
3. **`src/manuscript/DoorTransportSection.tsx`** (NEW, NOT_FROZEN) — the section under the row: the empty state as a positive mark, the head with the counts once lines stand, each line with its ONE hand, the taken sentence, the refusal box with the local hand, the corner pairs with both sides' chips in the caster's order (no style by `crosses`; a taken chip bold and titled with its partner; the picked one outlined), the cannot-cross line quiet, an unlawful standing transport said (a guard for a record that changed under it).
4. **`src/manuscript/ManuscriptChrome.tsx`** (194 NOT_FROZEN) — `AperturePairRowView.door?`; the pick row mounts the section under the map select; the panel threads three hands (`onDoorPick` · `onDoorWithdrawLine` · `onDoorWithdrawAttempt`); the three selects addressable (`data-aperture-select`).
5. **`src/manuscript/ManuscriptView.tsx`** (196 NOT_FROZEN) — the door's SIDES re-derived per door identity (faces + map) on the C-10 reader's record (`liftedConcept.record`), guarded on the render path; `doorStates` (the pick, the taken sentence, the refusal — transient, per row); the row views carry `door` (read · absent · C-10's `no-record` / `born-on-page` absences); the handlers (a pick, then the act through `actAt` — the transport written on the row; the whole-line withdrawal; the attempt withdrawn); a changed face or map a NEW door (a fresh row of three fields, the old transport and attempt gone); every glue exit and the volume change clear the door states. The built record copies the rows verbatim (unchanged code — the transport rides for free).
6. **`docs/governance/ENGINE_FREEZE_MANIFEST.txt`** — two NOT_FROZEN rows (the model, the section).
7. **`scripts/diagnose-the-doors-act.cjs`** (NEW) — above.
8. **`scripts/app-leg/concept_layer_eye_driver.py` · `diagnose-the-concept-layer-eye.cjs`** — the gen-1 lift before the second dissection; the C-10 arm places the MOST RECENT lift (the shelf holds two now); the door arm and its eight clauses per viewport.

## W1 · U1 — carried at the record commit (hashes with their instruments; none read for content; no re-seal)

**W1 riders AS FOUND:** the claims ledger `blob dfae77fc` → `77a8681d` (`sha256-LF e2c73e64` → `5bd7824c`, 3/0, mtime 16:16:15 — your §136 by its clock; the tail closes on a finished sentence) · **ADR 0031 `blob 5ef9a361` → `dde61338` (`sha256-LF ea164e5d` → `a2a3b1c2`, 3/1, mtime 15:04:05) — NAMED by your §136 (the values you named, both instruments, exactly) — carried** · **the researcher's J-registers ruling `blob d7985810` → `06671edf` (`sha256-LF 95bdeb0f` → `fdf4eccb`, 1/1, mtime 15:05:47 — the one-string rename edit in its §3f, the retired ruling's new name) — NAMED by your §136 — carried**. Unchanged since the last record: the front board (`blob 5d859796`), the connection-layer plan (`49dbf871`), the sovereign ledger (`f6072bed`), ADR 0030 (`66cb9c3f`), the designer-seat handoff (`3dd84885`).

**U1 (§126.2, every wave at each record commit) — the FOURTH wave,** found by the sweep run from its home at `0086075` and APPLIED: 19 files, 73,571 B — 1 cited RULING (`RULING_THE-RANGE-OF-A-CAST_the-range-IS-the-ruled-kind-…`, 14,676 B), 12 RESULTS files (20,261 B: 2026-09-22 `inside_midpoint_trace` and its run 1 · 2026-09-23 `born_face_reading` · `g3_meet_vs_parent` · `gen4_compounding` · `lift_in_type_theory` · `midwife_frame_in_the_tower` · `s35_carried_composed_born` · `space_of_resolver` · 2026-09-24 `cargo_between_doors` · `cargo_in_the_room` and its run 1) and 6 INSTRUMENTS (38,634 B: `angle_cannot_see_the_foot.py` · `cargo_between_doors.py` · `conceptual_angle_candidate.py` · `flatness_norm_two_forms.py` · `midwife_frame_in_the_tower.py` · `triangulated_midpoint.py`) — cited by the renamed INSIDE-MIDPOINT-TRACE ruling (tracked at the last record) and by `cargo_in_the_room.py`; the import closure adds none; `.gitignore`'s U1 block regenerated whole (119 negations); the 19 tracked in this record commit. A fifth wave, if these cite further untracked files, is the next record commit's.

## Not reached · said

- **The designer's eye** on the act at the row (her plate: the chips in a 306 px panel inside a rows region with a bounded scroll — the boxes printed above).
- **A word act at the door** — none exists (item 4).
- **The grain-law residue's glue** (item 1) and **the way back to an earlier shape** (item 2) — measured, not cured, not mine.
- **A multi-cell volume's door** (`c{i}:` ids stripped for the record — written, not driven: the lifted regions on the path are single cells).
