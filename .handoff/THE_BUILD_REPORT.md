to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-17 13:52 +03:30` (machine) · HEAD == origin after the push — the build commit is `6b378ab`; this letter rides the record commit that follows
subject: ✅ **`C-6a` PART 2 LANDED (`6b378ab`) — her line VERBATIM in its own row under the canvas, byte-identical across every hover; `Cell Composition` open by default; the hover readout's empty state a true absence (no words, no glyph, the height held).** ⚠ **The column's budget MEASURED, not broken: the composition sits 2178 px down a 2879-px scrolling column (795-px window) — a depth, reported.** ⚠ **Two premises of the stamp corrected on measurement (§2): the `fitLog` instrument and the `visibility: hidden` ghost both live in the walk module, not in the Ambo's files — the ghost idiom was transplanted; the budget was read by the column's own numbers.** ECHO the 13:10 ratification (nothing built in it) · ECHO `C-6a` part 2. **The two riders on her one line are NOT cut; at rest until her surface 1.**

## TO THE MOTHERSHIP

1. **THE COLUMN'S BUDGET (ruling 1's measure).** With the seed tetrahedron selected and the Selection tab open, the right column scrolls (`overflow-y: auto`, nothing clipped). The `Cell Composition` section, open, spans **2178–2968 px** of a **2879-px** column; the window shows **795 px** at 1400×900 and **615 px** at 1280×720 — the rows are reachable, three screens down. Opening it added its rows' height (~790 px) to the column. **Not a break by the column's own law (it scrolls); a depth you may want on the designer's desk with her line.**
2. **TWO PREMISES OF THE STAMP, CORRECTED BY MEASUREMENT:** (a) *"the fitLog class is live in that file"* — `fitLog` lives in `ExploreWindow.tsx:1647` (the walk's canvas-fit) and is named in `ManuscriptChrome.tsx`; `Panels.tsx` has none, so the budget was measured by the column's scroll numbers above. (b) *"the `visibility: hidden` ghost the module already uses"* — `Workspace3D.tsx` had no ghost; the idiom is the walk's (`ExploreWindow`'s sentence ghost), transplanted: a hidden, `aria-hidden` span holding one line's height.
3. **THE RIDERS WAIT ON HER LINE, uncut:** the vertex row still says `click: inspect` against the line's `click — select … in the inspector`; the three camera buttons stay out of the line.
4. **One reading for her, since the line now teaches `hover`:** the readout's content branches say `Hovering cell …` / `Hovering vertex …` / `Hovering edge …` / `Hovering face …` — the word *Hovering* in the readout beside *hover — preview what corresponds* in the line is the same act said twice in two registers; not this stamp's; named.

# 1 · What I SAW (my own eye — headless Chromium, the Ambo at boot, at 1400×900 and 1280×720)
- **The line, in its own row under the canvas:** `click — select what you point at, on the solid or in the inspector · shift-click — toggle it in the lift region (on the solid: the face you hit) · shift+alt-click the solid — the whole cell instead · edges lift from the inspector's rows only · hover — preview what corresponds · drag — orbit · right-drag — pan · wheel or middle-drag — zoom` — its element `data-ambo-gesture-line`, its top exactly the canvas's bottom (825 at 900 tall; 645 at 720), 76 px tall; **byte-identical** at boot, while hovering a cell, after the hover leaves, and after a selection and a tab change.
- **The readout's empty state:** at boot the slot shows **nothing** (visible text empty; the hidden ghost `visibility: hidden`, `aria-hidden="true"`; no em-dash), height **34 px**; hovering the solid it reads `Hovering cell seed/tetrahedron g0 | id: cell:seed:e7g5rs`, height **34 px**; after the pointer leaves, empty again, **34 px** — no jump.
- **`Cell Composition` at boot:** after a plain click on the solid and the Selection tab, the section is `aria-expanded="true"` with its four face rows present — no click on its header.
- **The walk's line** untouched (pinned by the witness against the keyed-walk witness's own string).

# 2 · What I RAN (verbatim)
- **Sim-diff:** `git diff --stat HEAD -- <files>` after the commit → empty.
- **Freeze manifest rows:** `src/components/Workspace3D.tsx` · `src/components/Panels.tsx` — no rows (`src/components` carries none); the witness outside the roots. **No frozen file; no new file.**
- **`node scripts/diagnose-the-ambo-gesture-truth.cjs`** (14 clauses): `DIAGNOSE-THE-AMBO-GESTURE-TRUTH: ALL PASS — the rows say what a click does, the line states every act in its own row, the readout's empty state is a true absence, and the composition opens by default`.
- **`npx tsc -b`**: exit 0.
- **`npm run sweep`** at `6b378ab`, whole, read by eye in its own call: `133 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (sweep exit 0; the Ambo witness PASS inside it with its §2)
- **The drive family:** not triggered — the reading touches the Ambo's overlays and inspector, not the walk (the winding leg's subject); said here, not skipped silently.
- **`.handoff/inbox/coder/` re-listed before each commit:** two letters, `--mark-reviewed --expect 2` in its own call; both archived by name with this landing.

# 3 · What I CHANGED
- **`6b378ab` — C-6a part 2.** `Workspace3D.tsx`: the wrapper a flex column (the canvas `min-h-0 flex-1`, the line a `shrink-0` row after it, in the Ambo's dark register); the readout slot `data-ambo-hover-readout` rendering `formatHoverStatus(...) ?? <ghost>`; `formatHoverStatus` returns `string | null`, its empty branch `null` (the sentence gone), the content branches untouched. `Panels.tsx`: the `selection-composition` section `defaultOpen`. The witness grows §2.
- **The record commit:** `.handoff/THE_BUILD.md` (the two letters verbatim), `.handoff/THE_BUILD_REPORT.md` (this letter), the W1 riders AS FOUND — `.handoff/PLAN_THE_CAMERA_GATE.md` · `.handoff/THE_CLAIMS_LEDGER.md` · `.handoff/THE_FRONT_BOARD.md` (tails close on finished sentences; not read for content).

# 4 · What I could NOT reach — named
- Nothing of the stamp. The two riders are held by its own order.
