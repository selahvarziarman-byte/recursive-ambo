to: Mothership
from: Coder
date: 2026-09-25 17:10 +03:30
ECHO: **C-14** (the triad of respects and the meet-core, parts a–f; Arman's Δ111 "yes, adopt it. go build") · **C-14 · M1** (part f, the designer's D111 ruling, ratified for meaning)
base: `7abb8f7` → a–e `40a3814` · f `ee4a211` · the record `the commit carrying this letter (its SHA in the ring)` (HEAD == origin at filing)

## TO THE MOTHERSHIP

1. **A design decision, said (part a's home):** the record lives in the FACE's packet, `face.data.triads` — the person's triads positional in the face's own corner order, NO id inside — and the per-edge respects keyed by (edge, the opposite corner) are READ from the faces holding the edge at every read. The first cut homed the record in the edge's packet keyed by the corner's id, as the letter's key suggests; it did not cross the lift's namespace hop — the frozen loader (snapshot.ts `namespaceOne`) re-roots only the ids it names, and the carried record read `⟨vertex:tetrahedron:c⟩` (measured; the 08-22 ruling forbids re-rooting a key later from the strings). The lawful cure without a snapshot.ts spend is a record that holds no id. The ADR's own words support it — "INPUT, serialized as his act" (§1.3) — and every requirement of a stands: keyed by (edge, opposite corner) at the read, holds-only, no one-to-one, words likewise, survives Export/Import, the dissection and the lift (all measured, §a and §e of the witness). If you want the record inside the edge's packet after all, that is a snapshot.ts spend and Arman's word.
2. **The remaining `apart` in person-facing strings, for a ruling:** the designer's sweep found the surface's three sites and I changed them (and the foot's proposal line, `would join what you have not paired:` — the same family). Two person-facing strings elsewhere still say `apart` in a DIFFERENT sense, the stone's — `c1 and c3 one: two roles of corner C, which the corner keeps apart` (`stoneClause`, spaceOf.ts:642, ratified §125.1) and the dependency refusal's `needs r0 apart from r1 — under this act the solid would make them one` (spaceOf.ts:690). They say two roles of ONE corner are distinct, not that A and B stand apart; I left them and ask whether §31's family reaches them. `foreign`: none person-facing remain (the rest are comments).
3. **Word triads:** the lib and the store take word triads (`kind: 'word'`); the surface points ROLE triads only — the designer's ruling names points in the columns and the corner's drawing, and the word rows have no third corner to point in. A word respect, if given by another route, reads in the corner's block like a role's. Not built at the surface until ruled.
4. **A gap in the eye I could not close from the chair:** the pending line's `withdraw this attempt` and the refusal's are not exercised by the leg (the picks always complete; no refusal is reachable through the surface, since a corner without a space offers no drawing to open) — measured under node instead (the respects witness §b).

## What I SAW

**The drive family — `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS` (169 clauses, 149 → 169; §16 ten per viewport), served `40a3814` at the f working tree (HEAD `40a3814`, dirty 17 — the f files before their commit; `/__whereami` read fresh per viewport), both viewports 1689 × 897 and 1400 × 900.** The leg ran twice on this cut: the first 163 PASS · 4 FAIL — two instrument reds at both viewports (a picked point's label going bold widened its group by one pixel while its place and every block's height stood — the clause now compares place and height and prints the width; the listing's index read `4F13` with no space in the browser's text — the strip re-pinned) and a null state read 300 ms after closing D's drawing, which the second run probed over time: the surface stands at 300 ms and after, no re-selection — a transient of the read, not the app. The second run whole.

At AB (gen 1; (i) on A–B, S1 on A–C, Q on B–C), what a person sees:
- **Entering the light.** Before the open: no light, no head clause. C's drawing opened (`open the drawing` on the source above): the head reads `AB · the midpoint between A and B — the two faces unfolded about their edge · pointing a triad on the face A·B·C — in C's light`; the surface names the light; D's source stays `closed`. Opening D's later closes C's (`— in D's light`; the sources `closed · open`).
- **One act, three picks, any order** (A · C · B here): after F13 in A's column the pending line reads `a triad in C's light: F13 · — · — · withdraw this attempt` BELOW the drawing; after r0 in C's opened drawing `F13 · — · r0`, both picks lit (`data-midpoint-triad-picked`, `data-midpoint-light-picked`); Φ8 in B's column completes it — the pending line gone, no line added to the drawing (a respect glues nothing on its own), no refusal.
- **§149, structural.** Every block of the surface down to the drawing at the same y and height after the open and after each of the three picks, in the surface's own coordinates; the drawing's box unmoved; the picked point in C's drawing and the second target (Φ8's point) at the same place (x, y, height) — the bold label widens the picked group by one pixel, printed.
- **The respects read first in C's block** (under the own column, before the derived `would join what you have not paired:` lines): `you said: F13 is Φ8, as regards r0 — honored` (S1 pairs r0 with F13, Q pairs r0 with Φ8) · `you said: F9 is Φ5, as regards r1 — broken at C–B: there you paired r1 with Φ1` · `you said: F2 is Φ3, as regards r3 — not yet: F2 not paired on A–C · r3 not paired on C–B`, each with its one hand `withdraw this triad`; no `should`, no repair; nothing of it in the drawing's text. D's block its silent line.
- **One light spoken:** the pairs listing under the drawing carries `only C's light has spoken on A–B; nothing glues here by respects until D's does`; the plain pairs read `1 F5 ↦ Φ7 · yours, plain · withdraw` and so on.
- **The one hand:** `withdraw this triad` on the third removes it whole — C's block reads two.
- **Both lights — glued by respects:** (F13, Φ8, Φ1) given in D's light: a fourth line across the fold marked `4`, its listing entry `4 F13 ≡ Φ8 · glued — you gave it in C's light and in D's` with no withdraw (its hands are the triads' own); withdrawn in D's block the line is gone and the listing plain again; every triad withdrawn, the lines as before the arm; the drawing closed, the light left, the surface standing.
- **The word pairs below the drawing:** a whole word pair `disjoins ↦ component-of` by two clicks — B's chip at the same place and size before the first click, after it, and after the pair (colour only); the pairs box lies below the drawing; withdrawn from it, the state restored.
- **The copy:** unglued, the sentence reads `A and B together, as two — 23 roles · 27 words · 56 tuples · 23 marks · 2 of the words are the corners' views (no tuple yet)`; the word-pairs box reads `no word translated yet — each word still its own side's, alike spellings included`; `apart` and `foreign` in none of the ten measured states of the surface.
- **The 149 clauses of before** all green on the same runs (C-13e's listing regex takes `, plain`; the §14 foot lines, §1's sentence and §6's born-room sentence re-pinned by name for the ruled copy).
- **The plates** (the ignored `scripts/app-leg/_frames/`): `concept-layer-triad-pending-*.png` (C's light, r0 lit), `concept-layer-triad-respects-*.png` (the three `you said` lines leading C's block), `concept-layer-triad-glued-by-lights-*.png` (the fourth line and its entry beside the plain pairs), at both viewports — read by me.

## What I RAN

**Commit `40a3814` (a–e).** `npx tsc -b` exit 0 · `node scripts/diagnose-engine-freeze.cjs` ALL PASS (respects.ts NOT_FROZEN in the same commit; `git add -N` before the run) · `scripts/diagnose-the-respects.cjs` NEW — 35 clauses ALL PASS (§0 2 · §a 3 · §b 5 · §c 9 · §d 8 · §e 6 · §f 2), each with its positive control at the base by the rule as it stood; the researcher's instruments reproduced: `meet_core_P6.py` over its own 4,000 draws (`scripts/fixtures/meet_core_P6_configs.json`, the RNG replayed through both phases; the generator's self-check prints 47 · 958) → M1 47 (each `twice`, both colliding pairs out) · M2 0 exceptions · M3 958 · the one-light control 4,000 of 4,000; `respect_pairing_P2.py`'s P2d census by the port AND through the app (2,106 triads given through the store, read, withdrawn — 0 refused): HONORED 6 · BROKEN 1,905 (A–C 1,161 · C–B 744) · NOT YET 195, P2a 0 of 91, P2c 0 of 728; the dependency reading at gen 2 (a born pair `A:Φ1 ↦ B:r0` on AB–AC; the second light's F7 ↦ r0 held back, `needs r0 as its own role`; the born pair withdrawn, the pair enters) · the stone · the midpoint · the born room · the inside · the doors act · the cargo · the lift carries · the found bugs · the concept type · the cast loader · the born face · the road · workspace persistence · the census · the J register · ambo · snapshot · P5 ALL PASS · the sweep, whole, at `40a3814` after the push: `147 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (wall 106.9 s). The reading for a–e is the witness's: no person-facing change before f.

**Commit `ee4a211` (f).** `npx tsc -b` exit 0 · the freeze ALL PASS · `scripts/diagnose-the-respects.cjs` 40 clauses ALL PASS (§g NEW, the surface under node) · the midpoint · the stone · the born room · the road · the inside · the found bugs · the census · the concept type · the cast loader · the doors act · the cargo · the lift carries · the born face · workspace persistence · the J register · ambo · snapshot · P5 ALL PASS at the f tree · THE DRIVE FAMILY as above (two runs) · the sweep, whole, at `ee4a211` after the push: `147 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (wall 105.9 s).

**The pre-commit witness list, both commits:** (1) the committed tree IS what was audited — each commit staged from the tree the witnesses ran on; HEAD == origin after each push (`40a3814`, `ee4a211`); (2) the manifest: `NOT_FROZEN src/lib/respects.ts — C-14 …` landed with the file; spaceOf.ts, bornFace.ts, ambo.ts, castInside.ts, feet.ts NOT_FROZEN rows; geometryStore.ts, MidpointSurface.tsx, CastInsideDiagram.tsx under no engine root; no frozen file touched (types/geometry.ts, designation.ts, cornerCycleName.ts, writtenFormModel.ts, genesisModel.ts, dualization.ts, incidenceTraceRegistry.ts, snapshot.ts); (3) no re-seal; (4) the sweep green at its own canonical line at both HEADs, tsc 0; (5) the reading — the leg and the plates above.

## What I CHANGED

**Commit `40a3814` — C-14 a–e.**
- `src/lib/respects.ts` NEW NOT_FROZEN (classified in the same commit): the record (`triadsOn` · `withTriad` · `withoutTriad` · `hasTriad` · `carriedTriads`; `facesThrough` · `respectsOn` — the per-edge respects read from the faces; `hasRespect`), the act (`triadLegsOf` · `triadOf`, with the refusals by name), the meet-core (`unconditionalOn` · `lightsOf` · `meetPairsOf` · `meetConflictsOf` · `meetCoreOf` — conditions (i) and (ii), `leftOut` and `heldBack` said), the readings (`legReading` · `verdictOf` · `coreMapOf` · `readRespects`), the space (`respectLinksOf` · `withRespects` — one holds-only `⟨X⟩` per spoken light on M's points).
- `src/lib/spaceOf.ts`: the ONE reader `recordOn(shape, e, options)` returns the core in force from `meetCoreOf`; the midpoint branch glues over the core (`born` = the core's pairs), adds the respects' types beside the feet, carries `core` and `respects` on `Resolved`; `SpaceOfOptions.meetDependency` (the guard's inner reads) and `respects: false` (a witness's control byte, the feet's twin).
- `src/lib/bornFace.ts`: the one reader called with the shape.
- `src/lib/ambo.ts`: the dissected cell's face record carried onto its parent-cell-face (`carriedTriads`, the corner order kept); the edge carry is the pairing's alone, as before.
- `src/lib/castInside.ts`: a respect's tuple counted (`census.respects`), drawn in no glyph.
- `src/lib/feet.ts`: `respectTypeName` · `isRespectType` beside the foot's (`⟨X⟩`).
- `src/store/geometryStore.ts`: `giveTriad` (atomic; a refusal into `triadRefusals[faceId]`), `withdrawTriad`, `withdrawTriadAttempt`.
- `docs/governance/ENGINE_FREEZE_MANIFEST.txt`: the NOT_FROZEN row for respects.ts.
- `scripts/diagnose-the-respects.cjs` NEW; `scripts/fixtures/gen_meet_core_P6_configs.py` + `meet_core_P6_configs.json` NEW (the instrument's 4,000 draws, its RNG replayed through both phases; the generator's self-check prints 47 · 958).
- Re-pinned by name: the stone §5, the born room §5, the inside §5, the concept type §4 (the eleventh cast reader; the ninth identification reader).

**Commit `ee4a211` — C-14 f.**
- `src/components/MidpointSurface.tsx`: the light (one open drawing, held by the surface; opening D's closes C's; the head clause `pointing a triad on the face A·B·C — in C's light`); the triad's picks in any order (a column pick in a light is the triad's; the corner's drawing's points pointable); the pending line and the triad's refusal BELOW the drawing; the word-pairs box (and the word refusal) moved BELOW the drawing, the rows and the reserved pick lines above; the corner's block reads the respects FIRST (`you said: … — honored` · `— broken at C–B: there you paired … with …` · `— not yet: …`), each with `withdraw this triad`, in the unglued state too; the listing says by which lights (`F13 ≡ Φ8 · glued — you gave it in C's light and in D's`, no withdraw; plain pairs `· yours, plain ·` only where a light has spoken); the lights' lines (one-spoken · left out · held back · differ); the copy (`A and B together, as two — …`, the born-room variant, `no word translated yet — each word still its own side's, alike spellings included`, the foot's `would join what you have not paired:`); the gesture line names the third gesture.
- `src/components/CastInsideDiagram.tsx`: an optional `pointExtra` on the whole drawing (the source drawing's points pointable in a light).
- `src/lib/respects.ts`: `legReadingWith` (HIS pair on a broken leg); `RespectReading.brokenPair` and `legs[i].given`.
- `src/store/geometryStore.ts`: `withdrawTriad` removes from every face record of the vertex set (a face two cells hold is two records).
- The witnesses re-pinned by name for the ruled copy and the new hands: the midpoint §4–§5, the stone §5, the born room §4, the eye leg (§1, §6, §14, C-13e's listing regex); the respects witness §g NEW (the surface under node: the copy, the block order below the drawing, the respect lines, HONORED/BROKEN naming his pair, the glue by lights); the driver's `triad_arm` and `word_pair_below`; the leg's §16 (10 clauses per viewport).

**Commit `the commit carrying this letter (its SHA in the ring)` — the record:** `THE_BUILD.md` (the C-14 letter and M1 verbatim), `THE_BUILD_REPORT.md` (this letter), the W1 riders as found, U1.

## What I could not reach

- The pending line's and the refusal's `withdraw this attempt` at the eye (§4 above) — measured under node.
- Word triads at the surface (§3 above) — not ruled.
- The stone's two `apart` strings (§2 above) — asked, not changed.

## W1 — carried AS FOUND (never read for content; no re-seal)

- **the researcher's ruling THE INSIDE-MIDPOINT-TRACE REFUSAL (named by the mothership for this record)** (`.handoff/RULING_THE-INSIDE-MIDPOINT-TRACE-REFUSAL_on-the-honesty-line_midpoint-is-the-pushout-over-the-CORE_trace-is-its-origin-partition.md`) — CARRIED AS FOUND, not read for content, no re-seal: `blob c54fa6f2` → `3c864dd8` (`sha256-LF 862f90de` → `00a639fa`), numstat 100/0, mtime 14:05:17; the tail's last characters: `nd B together as two"*, never *"apart"*.` (closing).
- **ADR 0031 (the researcher's amendment, §3.11)** (`docs/adr/0031-the-concept-layer-is-a-cosheaf-of-partial-symmetries.md`) — CARRIED AS FOUND, not read for content, no re-seal: `blob e7565a44` → `e0e04454` (`sha256-LF b0795661` → `850d5e79`), numstat 41/4, mtime 14:52:29; the tail's last characters: `od that names the child uses the second.` (closing).
- **the claims ledger** (`.handoff/THE_CLAIMS_LEDGER.md`) — CARRIED AS FOUND, not read for content, no re-seal: `blob 2a9cfef8` → `a1e18326` (`sha256-LF c9fa3a77` → `933a7a44`), numstat 49/0, mtime 14:53:40; the tail's last characters: `s in their letter, held for the landing.` (closing).
- **the sovereign ledger** (`.handoff/THE_SOVEREIGN_LEDGER.md`) — CARRIED AS FOUND, not read for content, no re-seal: `blob 4b47dfa5` → `62ae00a8` (`sha256-LF 0af079d4` → `89a7cdc0`), numstat 6/0, mtime 14:51:20; the tail's last characters: `§177's two grains. Built as C-14 (§179).` (closing).
- **U1** (§126.2, every wave at each record commit): a NEW wave — 0 rulings, 2 instruments (candidates 80); `.gitignore`'s U1 block regenerated whole; the files to add [
 ".handoff/instruments/connection_layer_reference/meet_core_P6.py",
 ".handoff/instruments/connection_layer_reference/respect_pairing_P2.py"
].

The C-14 letter and its M1 archive with this landing; the coder's inbox holds nothing.

⇒ NEXT: wake the mothership (ratify C-14 a–f; rule §1–§3 above).
