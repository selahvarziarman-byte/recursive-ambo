# REPORT — STAMP C-6PRICE: the price of a corner carrying a cast and an edge carrying `J` — file by file, frozen row by frozen row, with the STOPs; nothing built, nothing spent. ADR 0031 tracked AS FOUND with this record.

## TO THE MOTHERSHIP

**ECHO the B-3 RATIFICATION** (nothing to build in it; the return-line residue is filed to the designer as mine). **ECHO `C-6PRICE`.** This is a measurement. No line of `src/` changed; the frozen union below is what Arman sanctions or refuses.

**THE FROZEN UNION, as one list (price by FILE):**
- ⛔ **`src/types/geometry.ts`** — manifest row 90, bare hash `6ebdd92f…`, **NOT on the sanctioned list → STOP.** It is the ONE file the corner's cast (Q2) and the edge's `J` in its honest placement (Q4a) both touch — two fields, one union, priced once.
- ✅ **`src/playground/snapshot.ts`** — manifest row 89, bare hash `7371e5a0…`, **SANCTIONED.** Touched ONLY by the alternative edge placement (Q4b); untouched by the corner (the vertex carry spreads the packet whole).
- Measured and NOT touched by any row below: `src/lib/packets.ts` (row 65 — lineage only), `src/lib/shape.ts` (row 66 — the default packet constructor, untouched by an OPTIONAL field), `src/lib/ids.ts` (row 54 — the edge id mint, read not changed), `src/lib/multiform.ts` (row 64), `src/manuscript/genesisModel.ts` (row 75 — the universe loader, a sibling gesture never enters it).

**Two things for you, three lines each, as the stamp foresaw:**
1. **Unsanctioned frozen file:** `src/types/geometry.ts` — the packet's type and the edge's type live there and nowhere else; an optional `cast` on `VertexDataPacket` and an optional `identification` on `Edge` are the whole spend (~45 lines + one re-seal). Everything else the corner and the edge need is non-frozen. **Sanction or refuse this one file; Q4b is the path that needs no sanction at all.**
2. **Meaning question:** the packet today is FIVE FREE-TEXT FIELDS plus a machine lineage; the ADR says a corner IS a cast. **Is the cast a sixth field OF the packet (the packet stays what it is and gains typed content), or is the packet the cast (label/notes/color/tags become the cast's own `label`/`gloss`/marks)?** The price above assumes the first (additive, optional, the type cannot lie: a corner without a cast is a true absence). The second re-types five sites the type cannot say honestly today — say which before anything is priced further.

---

## 1 · THE FIVE QUESTIONS — one row each: file:line · manifest row · count · STOP-or-not

### Q1 · THE CORNER'S PACKET — where it lives, how it persists, how it lifts, how it is written
- **The DATA type:** `src/types/geometry.ts:88–95` `VertexDataPacket { label · notes · color · tags · custom: PacketData · lineage? }` — **row 90, FROZEN, unsanctioned.** The editor's five (`Label · Color · Tags · Notes · Custom JSON`) are `label · color · tags · notes · custom`; `lineage` is the machine's (never edited). `custom` is `Record<string, JsonValue>` (:55–63) — an opaque blob, exactly what the stamp forbids as an answer.
- **`packets.ts` (row 65, frozen, unsanctioned)** reads and writes ONLY `lineage` (`defaultPacket` :18 · `preservePacket` :27 · `deriveCompositePacket` :38 · `makeLineage` :100) — it never touches a data field. **A new field on the packet does not touch it. Count: 0.**
- **PERSISTED:** `src/playground/snapshot.ts` — **row 89, FROZEN, SANCTIONED.** `PlaygroundSnapshotFile.shape: Shape` verbatim (:71–86); the load's `namespaceOne` copies every vertex by spread — `vertices[id] = { ...vertex, id, createdBy: {…} }` (:229–236) — **the packet rides WHOLE, no field list; a new packet field persists with no edit. Count: 0.** Edges likewise `{ ...edge, id: ns(edge.id), … }` (:299–306).
- **THE LIFT carries it whole:** `src/store/geometryStore.ts:480` `liftSubComplex(shape, selections)` → `:484 serializeSnapshot(lifted.shape, …)` → `useLiftStore.push({ title, file })` (`src/store/liftStore.ts:32` `file: PlaygroundSnapshotFile`) → `src/manuscript/ManuscriptView.tsx:5462` `loadUniverseSnapshot(item.file)` (genesisModel row 75, frozen — `deserializeSnapshot` verbatim :309). Inside the lift (`src/lib/subComplexLift.ts`, **row 182 NOT_FROZEN**) vertices are deep-cloned whole — `JSON.parse(JSON.stringify(shape.vertices[id]))` (:712–713) — and edges are the shape's own objects filtered by the closure (:715). **A cast on the packet, or a `J` on the edge, rides the lift for free. Count: 0.**
- **THE EDITOR writes it:** `src/components/VertexPacketEditor.tsx` (`saveDraft` → `updateSelectedVertexData({ label, notes, color, tags, custom })` :122–130; "Save packet" :262) → `src/store/geometryStore.ts:203` (`updateSelectedVertexData: (patch: Partial<VertexDataPacket>) => void`), action `:774`, merge `data: { ...vertex.data, ...patch }` `:797–800`. **`src/components/` and `src/store/` carry ZERO manifest rows — outside the engine roots (`diagnose-engine-freeze.cjs:62`: src/lib · src/playground · src/manuscript · src/types). Not frozen. A `Partial` patch already carries any new optional field. Count: 0 frozen.**
- **STOP-or-not:** STOP only on the type (geometry.ts); the persistence, the lift and the editor need nothing.

### Q2 · THE CAST'S SHAPE AS A TYPE
- **Shape priced:** `ConceptSpace { roles: Array<{ id; label?; types?: Record<string, string | 'UNKNOWN'>; marks?: PacketData }> · signature: Array<{ type; arity: number }> · relations: Array<{ type; terms: string[]; polarity: 'holds' | 'does-not-hold' }> · axioms: string[] · warrant?: PacketData }` — arity any; instances THREE-VALUED by construction (`holds` · `does-not-hold` · an unlisted tuple is UNRECORDED — never a stored third token that could be read as false); axioms as sentences the engine carries and does not evaluate (0031 §1.1: "sentences over the signature"); marks and warrant carried, never read (0031 §1.1; MOLD §8 G3). **~30 lines.**
- **Placement:** `cast?: ConceptSpace` as an OPTIONAL field on `VertexDataPacket` (`geometry.ts:88–95`) — optional because the person has not chosen it (a corner without a cast is a true absence, never a placeholder); the type declared INLINE in `geometry.ts`. **A separate `src/types/conceptSpace.ts` imported by `geometry.ts` would have to be FROZEN by the closure law (a frozen file imports only frozen files — `diagnose-the-small-run` §8), i.e. two frozen files where one suffices; inline is the smaller union.**
- **Frozen union:** `src/types/geometry.ts` — **row 90, unsanctioned → STOP.** ~31 lines + re-seal. `shape.ts:16` `createDefaultVertexData` (row 66) untouched (optional field); `packets.ts` untouched; `snapshot.ts` untouched (spread). Non-frozen consumers untouched until a build shows the cast (the editor, the cards).
- **Count:** 1 frozen file · ~31 lines · 1 re-seal · **STOP.**

### Q3 · HOW A CAST ARRIVES — the loader, priced, not built
- **No machine-readable cast exists in the engine today.** The casts are Python literals: Flow and Φ in `.handoff/instruments/connection_layer_reference/hinge_data.py` (sets of `(type, from, to)` triples — and per the researcher's 09-08 letter, *synthetic fixtures at most*, no reading drawn from them); **the T cell in `first_face_and_tower.py:29–35`** (`T_ROLES` r0…r9 · `T_INST` as `(type, terms, bool)` triples — the three values are `True`, `False` and absence — · `TC` with `roles / rel / neg / types{member-status}`), the first pour against MOLD v4 (E1, cast 09-09).
- **THE SCHEMA the loader reads is already written — MOLD v4 §5's machine-readable block** (`.handoff/MOLD_v4_…md` §5): `concept · subject_matter · premises · roles[{id, label, gloss, source, types{member_status, …}, properties, marks, warrant}] · signature[{type, meaning, arity, properties[{name, positions, level}], subtypes}] · relations[{type, terms[], polarity: holds|does-not-hold, reason, annotations}] · axioms[{sentence, quantified, hypothesis, source, status}] · derived_check · open_forks · did_not_fit`. **The loader maps it to Q2's `ConceptSpace` and DROPS nothing it cannot type: `gloss/source/reason/annotations/warrant/derived_check` are carried under `marks`/`warrant` unread.** The mold's authors have not yet emitted this JSON for any cast (the T pour exists as prose + the Python transcription) — the loader's first input would be a hand transcription until the mold writes JSON.
- **THE GESTURE:** at the CORNER, not the shelf — a `load cast… (.cast.json)` file input beside `Save packet` in `VertexPacketEditor.tsx` (non-frozen), parsing through a new `src/lib/castLoader.ts` (**NOT_FROZEN** if only components/store import it) and writing `updateSelectedVertexData({ cast })`. Its sibling is the shelf's `load universe…` (`ManuscriptView.tsx:5480–5492` `handleLoadFiles` → `JSON.parse` → `loadUniverseSnapshot`), but a cast lands on a vertex of the Ambo, where the packet is written, never on the Manuscript's shelf.
- **WHAT MOLD v4 §7 FORBIDS THE LOADER, verbatim, as five refusals the witness would pin:** *"it will never fill a name, never posit an identification, never assert a relation in your vocabulary that you did not cast, never read an absence as a fact, and never fail a cast for what its neighbours turned out to be."* ⇒ no label minted from a role id (a missing `label` stays absent); no `J` written anywhere by the loader; relations only from `relations[]` with the polarity given, and NEVER a tuple the file did not list (an unlisted tuple is unrecorded, not false — G2); a cast loads alone, validated against its own signature only (arity of every tuple = the type's arity; every term a role), never against another cast.
- **Count:** ~80 lines non-frozen + a witness; **0 frozen files; no STOP.**

### Q4 · THE EDGE'S SLOT — where `J` would live, keyed to what, surviving dissection how
- **Today:** `Edge` (`geometry.ts:131–145`) has NO person-given field — `data?: PacketData` is an untyped blob, `lineage?` is the machine's, `composes?/sharedBy?` are records of the manifold. The edge's only register is the Layer-3 witness (`src/components/Layer3WitnessPanel.tsx:92–97`, reading `state.seamEdges` — derived, read-only). **Confirmed: an edge has no slot.**
- **THE STABLE KEY IS `edge.id`:** minted by `src/lib/ids.ts:35–36` (row 54, frozen, read not changed) as `edge:${stableHash(shapeId | canonicalEdgeKey(a, b))}` — e.g. the seed tetrahedron's `edge:mf5p2o` — stable for the shape, order-free on the endpoints. **Across a hop it is a RECORD, not a string relation:** the snapshot's load renames every id under the load source (`snapshot.ts:216–218` `${source}:${id}`) and copies the edge object with it (`:299–306`), so a `J` stored ON the edge is renamed with its edge; the roles inside `J` are the two casts' own role ids and are never namespaced.
- **SIX `J`'s, never more (0031 §3.5):** the seed tetrahedron has 6 edges (measured: `createSeedShape('tetrahedron')` → 4 vertices · 6 edges · 4 faces). **Sub-edges record no parent edge** — measured: `ambo.ts:125` puts `sourceEdgeId` on the MIDPOINT VERTEX's `createdBy`, and cells carry `sourceEdgeIds` (`:131 · :142 · :560 · :591`); no edge of a dissection carries a `sourceEdgeId`. **So the composition by incidence is DERIVED, never stored:** a gen-1 corner edge (corner, midpoint) takes its parent's injection through the midpoint's `createdBy.sourceEdgeId`; a medial edge (midpoint, midpoint) takes the identity on the shared corner. The record holds the six; every higher `J` is a reading.
- **Placement (a) — on the substrate:** `identification?: EdgeIdentification` on `Edge` (`geometry.ts:131–145`) + the type `{ roles: Array<[string, string]> · types: Array<[string, string]> · support?: Record<string, number> · fiat?: string[] }` (~15 lines; symmetric by construction: ONE record per edge read as `J_e` forward and `J_e⁻¹` backward, so a stored reverse cannot drift — 0031 §6.1). The snapshot spreads it (`:299`), the lift carries it (`subComplexLift.ts:715`), the store's non-frozen edge writes hold it live. **Frozen union: `geometry.ts` — the SAME file as Q2 → one union, one STOP.**
- **Placement (b) — beside the substrate:** `identifications?: Record<EdgeId, EdgeIdentification>` on `PlaygroundSnapshotFile` (`snapshot.ts:71–86`, **row 89, SANCTIONED**) with the type declared there (~25 lines + re-seal), the live record in the non-frozen store, and `geometryStore.ts:484`'s `serializeSnapshot` call handed the identifications. **No unsanctioned file; buildable inside the standing sanction.** The cost: the Shape does not hold `J` — the lift carries it only because the snapshot does, and every consumer of the Shape (the op-set's whole world) never sees it, which is the boundary anyway (`J` identifies roles in fibers, never cells).
- **Count:** (a) 1 frozen file (shared with Q2), ~16 lines, **STOP**; (b) 1 sanctioned frozen file, ~25 lines + re-seal, **no STOP**, plus ~20 non-frozen lines.

### Q5 · THE DERIVED PRODUCERS — one line
The amalgam, the residue (`Fix · Mov · Und`) and the colimits would live in a NEW `src/lib/conceptLayer.ts`, **NOT_FROZEN** while only non-frozen consumers (components · store · manuscript views · witnesses) import it — and **FROZEN by the closure law the moment `geometry.ts`, `snapshot.ts` or `packets.ts` imports it**, which they must not: types flow down to the frozen file, producers stay above it.

## 2 · WHAT I SAW · WHAT I RAN

- **Saw:** nothing driven — the stamp charters no diff and no reading; every row above is a file read (`git show`/file tools) or a run of the committed engine (the tetrahedron's counts).
- **Ran:** `createSeedShape('tetrahedron')` through the ts hook → `4 vertices · 6 edges · 4 faces · edge ids of the form edge:<hash>` (the six `J`'s and the key's shape, measured not assumed). `git add -n docs/adr/0031-…md` → addable. `npm run sweep` + `npx tsc -b` at `1b24e7f`, the code tree this record lands on (no `src/` rides this commit): `130 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (sweep exit 0), `npx tsc -b` exit 0. **Read by eye from the run's own file, in its own call, before this record was written.**. A second run at the landed HEAD follows the commit and is reported in the recap and the next report (the push waits on it).
- **docs/ is outside the engine roots** (`diagnose-engine-freeze.cjs:62` names src/lib · src/playground · src/manuscript · src/types) — **no manifest classification is owed for ADR 0031.** Its tail closes on a finished sentence; tracked AS FOUND, not read for content beyond what the pricing needed (§1, §3.5, §5, §6).

## 3 · WHAT I CHANGED

- Nothing in `src/`. This commit: `.handoff/THE_BUILD.md` (the two letters verbatim) · `.handoff/THE_BUILD_REPORT.md` (this report) · `docs/adr/0031-the-concept-layer-is-a-cosheaf-of-partial-symmetries.md` (NEW, tracked AS FOUND) · **W1 riders AS FOUND:** `PLAN_THE_CONNECTION_LAYER.md` · `THE_CLAIMS_LEDGER.md` · `THE_FRONT_BOARD.md` · `THE_SOVEREIGN_LEDGER.md` (tails close on finished sentences; not read for content).

## 4 · WHAT I COULD NOT REACH

- **A count for the packet's re-typing under the second meaning** (the packet IS the cast) — not priced: it is the meaning question above, and pricing it would guess at what five fields become.
- **The editor's and the cards' SURFACES for a cast and a `J`** — the designer's, not priced (the stamp: no surface).

*Both letters archive with this landing.*
