# THE BUILD REPORT — B-2026-08-23-C: the sweep ran FIRST and is RANKED; the LAST producer-stop landed (v0 is dead as a label); D16 landed (the card takes the door's resolver ENTIRE — marks, not letters); the face register exists on BOTH surfaces

**STAMP ECHO: `B-2026-08-23-C`** — the build I executed is the one you wrote (the -B text was never worked from).
**coder · 2026-08-22 · commits `ce1995e` (mandate record) · `8bc64d1` (TASK B, frozen union) · `f2915f4` (TASK C, D16) · `402a608` (TASK D, the face register) · `4d32b93` (witness recut) on base `fba21a2` · branch `team-arman` · pushed after this report lands**

---

## TO THE MOTHERSHIP

**1 · An unsanctioned frozen file carries two measured copy defects:** `specimenModel.ts` prints `face-pairs 1 (all mixed)` on a ONE-pair REVERSING room (the 'all' is hardcoded before the fork, and 'mixed' is wrong over one pair — it should read the pair's own mode), and its χ `·` clause is the row that wraps (finding R3). I did not spend the file — sanction wanted if the copy is to move.

**2 · The sweep's top finding needs a ruling, not a cut:** the aperture gate's **"leave bounded" builds with a PICKED pair silently dropped** (measured twice: pair committed + drawn solid on the domain, then face-pairs `0 (all preserving)`, orientable `yes` — the person's reversing pick vanished without a sentence; "glue — the S² gate judges" takes the same pair correctly). Whether leave-bounded should refuse-while-a-pair-stands, consume the pairs, or speak a drop-sentence is MEANING — yours.

**3 · The D16 acceptance's menu-agreement clause, disclosed:** the card and the menu now share ONE function (`lineageCornerDisplay` — reach + level mark), so agreement is BY CONSTRUCTION, and the witness pins the strings (`A₀/A₁/C₀/C₁/AC₀/AC₁`). A live dim-3 ×I ROOM for a side-by-side menu reading does not exist through any committed door I could reach — thicken refuses surface × segment ("the product of two surfaces is a 4-manifold") — so the eye-level comparison ran on the ×I band's card vs the same band's port-face menu (finding R2: the port menu never got the reach).

**4 · §6 honored: the sweep answer is YES and the disambiguator is HERS.** Surfaces named in §1.3 below; I designed nothing — the born-corner rows now read `unnamed·A/B/C…` (the standing duplicate-name handle doing its ruled job on absences), and the copy that makes `unnamed` an invitation is hers.

---

## 1 · WHAT I SAW — the sweep first (TASK A), then each cut at the eye

**SHA PIN: the sweep's pre-state was driven at tree `ce1995e`, whose `src/` is byte-identical to `fba21a2`; the post-cut acceptances were driven live at the final head's content (vite HMR + full reloads). Windows Chrome, viewport 1568×704-ish (the height flaps with the download shelf — instrument note re-confirmed).**

### 1.1 · The three predicates — where the controls stand on the RIGHT side (the deck's pattern honored)

Measured working, named: the **thicken chip** speaks `thicken — select two forms — click the shape, shift-click the segment` on hover with nothing armed; the **connect-sum gate** refuses at pick with `Both forms need a face with a boundary edge.`; the **aperture gate** refuses at pick with `point at a solid first — select a form to build on (nothing is selected)` and, pointed at a surface, `this form is a surface, not a solid — there is no room to build on it`; the **cut door** declares `cut consumes the face YOU pick — no default is taken` before any act; the **map menu** prints every correspondence WITH its derived mode (`a→e · d→f · c→g · b→h — reversing (derived) · cone room · edges wind`) before the person commits; the **ambo thicken** refuses at the control (`thicken: the product of two surfaces is a 4-manifold; this engine stops at 3`).

### 1.2 · THE RANKED FINDINGS (each measured at the eye; none fixed beyond the ordered tasks)

- **R1 — "leave bounded" silently discards a picked pair** (predicate 2's inverse: not a misplaced refusal — a missing one). Pair A·D·C·B ↔ E·F·G·H with a reversing map, committed and drawn solid on the domain; "leave bounded" birthed `built 3-manifold 2` with `face-pairs 0 (all preserving) · orientable yes`. "glue — the S² gate judges" on the identical state birthed the true room (`face-pairs 1 · orientable no · H₁ ℤ`). The person's deliberate pick vanished with no sentence.
- **R2 — the port-face / cut-face menu never got what the deck (and now the card) has:** on the ambo ×I band the picker lists faces the CARD names `◻ AB₀·BC₀·BC₁·AB₁ · 4 corners` — as **`unnamed · 4 corners` × N, all identical** (12 on the 12-gon band). Two readers, same faces, one blind: the menu's `faceDisplayName` calls carry no resolver. The cure is the D16 mechanism handed one seam further — unordered, so reported, not cut.
- **R3 — HER ITEM (a): the χ clause does NOT look right.** On every bounded room the row renders as `Euler` / `1 · the closed-world check` / `χ` / `does not apply to a room` — the two-word label breaks between its lines and the clause interleaves, so the eye reads "Euler 1 · the closed-world check χ does not apply to a room". Measured twice (both chambers). The collapsed certificate line has the same class of trouble on the argument card: the χ clause's own ` · ` blends into the row-separator ` · `.
- **R4 — the record strip's ⚠ label promises what the view does not render:** `⚠ integrity violations (shown, not hidden)` appeared after the band placement, but `genesis.violations` (the sentences, carried by the model) render NOWHERE — the label shows only that violations exist. A label describing intent the code does not fulfil.
- **R5 — id-shaped text still on his screen, by the NAME channel (not the label channel the migration cured):** the cut op's mint bakes a face id into the shape NAME — the shelf chip, the notice, the caption and the card header all read `cut(12-gon:face:multiform:plain:12-gon:0) × I`; the lift door's mint leads with a cell id (`cell:core:f5xozg of Ambo Dissection Tetrahedron`); the loaded-universe provenance prints the raw universe id (ruled honest as a source tag); the conformalAtom refusal prints a full raw edge id in the card. Contrast: the region door mints clean (`2-entity region of Ambo Dissection Tetrahedron × I`).
- **R6 — the argument-card header mislabels a loaded product's source:** `invoked ⟶ cut(12-gon…) × I` on a thing that was never invoked (sourceNameFor's fall-through word). The map is the spine and its left arm lies there.
- **R7 — the thicken refusal's sentence misfits its operands** (`the product of two surfaces is a 4-manifold` over a surface × SEGMENT; the notice also doubles the word: `thicken: thicken: …`), and **specimenModel's `1 (all mixed)`** (see TO THE MOTHERSHIP 1).
- **R8 — the dim-1 pick target is a hairline:** the Loop's select fought five real clicks (rim ~2px; clicks inside the ring's hollow are misses); the Arc selected first try. The person's hand has live feedback, but the target is still the stroke alone.
- **R9 — a full reload keeps only what "save the page…" wrote** — my in-session exhibits vanished on navigation; the explicit-save design read as data loss mid-drive. Noted for the designer/mothership as UX, not defect-claimed.

**Exclusions (a census that states what it did not count):** the Packets and History tabs, the dev-only views (`?witness`, `?field`, stories), the PlaygroundInvokePanel, the save/load round-trip doors (driven and accepted in B-2026-08-22-B), the fold door's own panel (driven in B-2026-08-23-A), collapse/dualize/sew execution paths (only their pick-time gates observed), and walk legs beyond one plate (below).

### 1.3 · §2(3) — the one-glance question: **YES**

Two-plus born corners appear as TEXT side by side on: **the argument card** (spine rows, word rows `—a ← v2·v0` two-on-a-line, incidence `a ⊾ b @ v0`, stance `v0 : 60 = 60`, verdict `v0 +120°`) and **the correspondence ring** (leader labels around the figure — four at once on a square). Post-Task-B these read `unnamed·A/B/C…` — still side-by-side text, so **a disambiguator is owed and the design is the designer's** (§6). The letter-handle mechanism already stands; whether it should LOOK like that is hers.

### 1.4 · Her two open items, at the screen

**(a) the χ clause: NO — finding R3 above** (label wrap + clause interleave, plated). **(b) the non-orientable bounded room: YES, it works — three notes at once, three registers, nothing clobbered:** `built 3-manifold 3` (one reversing pair, four walls) shows the twist note **`w₁ ≠ 0 — non-orientable (the twist)`** in its bordered slot AND the χ room clause in the value AND `(the room is the body — you stand inside it)` in the subtitle, simultaneously. The slot built for one holds exactly one — nothing else targets it. Her layout call is which register these should share; the mechanism does not collide.

### 1.5 · The look plate carried its station

Inside `built 3-manifold 3`: **`seam.settle 1.00 · gpu true · doors 0 · walls 4 · frameHanded +1`** — at rest, the bounded room's own four walls read from within, the caption speaking `Euclidean cone-manifold · n=[2,2,2,2,1,1,1,1] · flat · no cone edges · the manifold ends here; the orbit recurs only through the glued corridors · copies shown to depth 6`.

### 1.6 · TASK B at the eye — `v0` is gone

A fresh invoked Triangle reads `•unnamed·A — born / •unnamed·B — born / •unnamed·C — born` through EVERY register (spine, `—a ← unnamed·A·unnamed·B`, incidence, stance, verdict, ring). The zoo torus (zoo-as-act through the same catalogue) reads `•unnamed·A·unnamed·B·unnamed·C·unnamed·D ← unnamed·A unnamed·B unnamed·C unnamed·D` — the composed class through four honest absences; χ 0 (certified), genus 1, orientable yes — the math never depended on the labels. Nothing id-shaped remains on the invoked-primitive surfaces. (The reading is DENSE and overflows the card column on the torus — the §6 presentation lane, hers.)

### 1.7 · TASK C (D16) at the eye — marks, not letters

The PRE-state, measured on a fresh plain-12-gon band: leaders `v0·A / v0·B / v1·A / v1·B …` — ids gone, letters remaining, exactly as ruled. The POST-state, on an ambo 2-edge-region × I band: **leaders read `AB₀ · AB₁ · AD₀ · AD₁ · BC₀ · BC₁`** — reach + level mark, `·A`/`·B` NOWHERE on the copies; the rungs read `—f ← AB₀·AB₁` (never the level-blind `AB·AB`); incidence rides the marks (`a ⊾ c ⊾ f @ AB₀`). **And the guard the terminal cut left standing did its job in the open:** on the pre-migration plain-12-gon band the reach REFUSES the base's id-copy labels (`raw === vertex.id`) and the letters remain THERE — the record is the record, measured, disclosed.

### 1.8 · TASK D at the eye — the 08-19 finding closed

**Ambo → Selection → Cell Composition → CELL FACES** rows now LEAD with the composed name — `AC·CD·BC` / `AD·BD·CD` — the id demoted to the mono sub-line exactly as vertex rows keep theirs, the lineage sentence printed ONCE. **The argument card has a face register:** `◻ AB₀·BC₀·BC₁·AB₁ · 4 corners / ◻ AB₀·AD₀·AD₁·AB₁ · 4 corners` on the band — the same composer, the same resolver, the menu's own `· n corners` grammar. ⚠ the `◻` glyph and the row form are flagged to the designer.

## 2 · WHAT I RAN (the five witnesses)

```
1  working tree clean at the tip; the frozen commit (8bc64d1) = the edit + the
   re-seal alone, nothing else in it
2  freeze manifest: src/playground/primitiveCatalogue.ts → FROZEN, SANCTIONED
   by this mandate (§3); apertureModel / argumentReadingModel / ManuscriptView /
   Panels.tsx / diagnose-argument-card → NOT_FROZEN (verified rows/absence)
3  positive control REPRODUCED at the real base ce1995e:
   b02c1c2daf58ec632513aec088eff597a76ce3d09d218d9dd85374d183eb1266 recomputed
   (tr -d '\r' | sha256sum), THEN the edit; re-seal b764254e60bce20133595e0f6c
   70254eee986f15ef3f76e195e225a8059b3894 written in the SAME commit
4  suite at the FINAL head: 112 files @ exactly 1 fail = diagnose-dual-inspection
   (accepted baseline). npx tsc -b → exit 0 after every commit. The argument-card
   witness recut to the new truth + ONE new leg (D16+FACES: A₀/A₁/C₀/C₁/AC₀/AC₁
   distinct by mark alone; faceRows never a face id) — ALL PASS
5  THE READING — §1 above: the sweep at the screen BEFORE the cuts (SHA pinned),
   then every cut re-driven at the eye (Task B invoke + zoo; D16 pre AND post
   bands + the standing-guard case; Task D both surfaces); the walk plate with
   seam.settle 1.00
```

## 3 · WHAT I CHANGED

- **`src/playground/primitiveCatalogue.ts` + `docs/governance/ENGINE_FREEZE_MANIFEST.txt`** (`8bc64d1`, ONE frozen union): all THREE label mints (nGonSpec `v${index}`, segmentSpec `'v0'`/`'v1'` — priced by file) → TRUE ABSENCE; ids stand.
- **`src/manuscript/apertureModel.ts`** (`f2915f4`): the door's per-corner lineage arm EXTRACTED as `lineageCornerDisplay` (reach + level mark, one function); `faceDisplayName` calls it byte-equivalently.
- **`src/manuscript/argumentReadingModel.ts`** (`f2915f4`, `402a608`): `buildArgumentReading(form, resolveAbsent?)`; the arm reads BEFORE the root composition at the concept label AND `endpointNameOf`; `faceRows` built through `faceDisplayName` + the threaded resolver.
- **`src/manuscript/ManuscriptView.tsx`** (`f2915f4`, `402a608`): `shapeById` + `resolveAbsentLabel` moved above the argument memo (byte-identical bodies), the resolver handed in; the `◻` face-register render (grouped past 8, like the spine).
- **`src/components/Panels.tsx`** (`402a608`): CELL FACES rows lead with `faceDisplayName`, id demoted, `sourceRole` (the doubled lineage sentence) removed with its now-unused producer.
- **`scripts/diagnose-argument-card.cjs`** (`4d32b93`): pins recut to the post-migration truth via an INDEPENDENT recomputation of the ruled reading; the D16+FACES leg added.
- **`.handoff/THE_BUILD.md`** (`ce1995e`): the mandate record.

## 4 · WHAT I COULD NOT REACH / DID NOT TOUCH

- **A live aperture-menu-vs-card side-by-side on a dim-3 ×I room** — no committed door builds one (thicken refuses surface × segment); the agreement is by construction + the witness leg (TO THE MOTHERSHIP 3).
- **The ranked findings R1–R9** — measured and reported, not fixed: none were ordered, one needs a frozen sanction (R7/specimenModel), one needs a meaning ruling (R1), one is the designer's (R3, and the §2(3) disambiguator + `unnamed`-as-invitation copy per §6).
- **The D-index sites** (`dualization`/`surfaceDual`) — the ruled SEPARATE cut, untouched.
- **The gesture-level pixel drives I split around:** the Loop's select and one thicken arming went through the committed handlers at their own seams (the raycast's hairline target is finding R8); everything downstream verified at the eye. The zoo/deck/aperture/panel drives were real clicks.
