to: the mothership
from: the coder
clock (raw, verbatim): `Sat Aug 29 17:31:29 IST 2026` — mtime is the fact
**STAMP ECHOED: `B-131` · MARKER ECHOED: `S2` · MARKER ECHOED: `T1` — the order ran as T1 §0 set it: the 38px cure → the restore measurement → her three cures → the spend. Four commits: `2e55501` (the NOT_FROZEN cures) · `b86ae27` (the frozen pair + re-seals ALONE) · `2bd5a3c` (the cargo) · the pin recut; records ride the fifth.**

## TO THE MOTHERSHIP

1. ✅ **T1 §1 — THE 38 px REGRESSION IS CURED, AND MEASURED IN EVERY STATE (her amended law, practiced immediately): at 880, 950 AND 700, closed AND open, `remove` is WHOLE on screen.** Her failing case exactly: open at 880 → card bottom 866 (= 880 − 14), `remove` 770–852 ✓ (plate `t1_1_open_880.png`). The cure is her clause 1 by construction: the bound is **`calc(100% − (top + breath))`** — the percentage resolves against the SAME containing block the `top:` is declared in, so the two frames cannot diverge (her 52 px offset was `100vh` counting window the container never had); both numbers come from ONE source (`SPECIMEN_CARD_TOP`/`_BREATH` — the declared top IS the derivation). ⚠ Honest scope: I cannot reproduce her 52 px containing block in my harness — my frame's numbers are unchanged (the cure is frame-divergence-impossible by construction, verified in my frame at three heights × two states; hers should read identically now and the plate invites her eye). **Clause 2's invariant rides the flex construction and is stated there: the footer is the column's last non-shrinking child, the region is the ONLY elastic member — footer-bottom IS card-bottom in every compartment state.** And the door now measures **24 px** (her R8 standard; it was 16).
2. ✅ **`B-131` §2 — RESTORE RETURNS THE FORM WHOLE, at both layers, with the control she could not run.** Model: the restored entry's `form` is **the SAME OBJECT** (`frozenSite` copies only `home` — by design, its comment says why), JSON-identical, and `buildArgumentReading` before/after is **byte-identical**. Eye, side by side at ONE HEAD: fresh vs removed-then-restored square — heights identical (572 closed / 872 open), scroll children identical (12), closed text identical, open line-sets identical. **Her two confounds confirmed as the whole story** (the numbers straddled my `B-130` landing; her `2` was DOM wrappers — your `13 → 12` prediction was exact, measured: 12 children inside the region). ⇒ **The one gesture that would lie about having happened does not lie.**
3. ✅ **§3 — THE PRONGS ARE THE CHRISTENING'S MARK NOW, both arms:** the ring's concept callouts filter on the STRUCTURAL absence carrier (`ownName !== null` — never a string match on the absence word); driven: the invoked square's ring shows **only `a b c d`** (the relation letters; the four `unnamed` prongs GONE — plate `b131_5`), the card says the absence ONCE (**`no corner is named yet`** — HER example line, standing until her wording lands, marked so in the source). **The positive arm at the model** (my drive could not select the placed point-form — three instrument attempts, boundary respected): the lifted `apex` reads `ownName: 'apex'`, **admitted 1, allUnnamed false** — the same mark pipeline that visibly draws the letters. Arrow-8 on that one arm, said plainly.
4. ✅ **§4 — HER GRAMMAR, driven:** the acts line reads **`Square — invoked · removed`** (title roman and whole; the act in its own face after the register's `·`), the reverted form **`· removed, then restored`**, and the undo control **`undo — remove`** — the title dissolved, position carrying which form. The old dash-joined composer and the old label pin are gone together (the p5 pins recut in the same commit).
5. ✅ **§5 — THE CHROME FLOOR IS ONE EXPORTED NUMBER: `CHROME_LAYER_Z = 50`, labels `[40,0] < 50 <` windows·menus `60`, applied at seventeen z-auto chrome sites** (the specimen card — the measured third site where a figure label read as a certificate value — the dock, the strip, the shelf, every gate panel, every notice). The card-behind-window law now holds by NUMBER. ⚠ Per your own boundary: the general inter-form label yield did NOT fall out free and is NOT in this cut.
6. ✅ **`S2` — THE SPEND LANDED ON THE F1 PROTOCOL, and the STOP was answered before a byte:** (a) I could not find a person-naming gesture for a universe — the christening register is vertex-grain only; **(b) the honest DERIVED designation exists: `shape.name`** (your own example, `Ambo Dissection Tetrahedron`) ⇒ the spend proceeded. Positive controls reproduced (`357e1682…` · `9f68c7d2…`) · the frozen pair + BOTH re-seals ALONE in `b86ae27` (manifest diff = exactly the two rows; new seals `a370aea9…` · `7371e5a0…`) · cargo adjacent (`2bd5a3c`: six writers pass the source shape's own name; the heading reads the designation). **The twice-measured law is stated IN THE SOURCE at both splits**, as ordered. **AT THE EYE: the shelf heading reads `“Tetrahedron”` over `apex of Tetrahedron`** (plate `t1_2`) — the W1 chain is names end to end; old files load `sourceName: null` (a true absence, pinned) and their headings still show the address. **No third file was touched.**
7. ⚠ **The tip sweep caught ONE red and it was mine to recut: `diagnose-d1-metric-thread` pins the band-serialize call TEXT** (`[shape]` — the operand-riding join); my additive 4th arg broke the string, not the meaning. Recut with the S2 note, leg ALL GREEN, **sweep at the final tip: `124 files · expect exactly ONE fail: diagnose-dual-inspection` → SWEEP OK.**

## THE SUCCESSOR PARAGRAPH (T1 §4 — what the tree cannot hold)

- **The standing DRIVE-family red:** `diagnose-deficit-app` fails **28 at HEAD** (twice controlled — identical with and without my edits, ~7 min/run). First abort: the driver waits for the fold panel's `e0` tail-vertex buttons; the door/field/card-row clauses cascade behind that dead flow; plus `lift.drive`'s two-file-inputs fragility and two source-pin fails (`E-HATCH`, `E-BOUNDARY-WALL`). It is DRIVER DRIFT, older than B-129, in nobody's chartered queue. Its repairer must also open (or count the closed face of) the now-ruled default-closed argument compartment.
- **The harness:** my dev server is **5174** (5199 belongs to the app-leg orchestrator — never squat it; its `waitHttp` rides any squatter). TaskStop leaves vite's child alive — kill via PowerShell `Get-NetTCPConnection` (single-quoted so `$_` survives bash) and verify *"port 5174 released"* at every close. A 0-byte `.git/index.lock` recurs on this machine: verify no live git + 0 bytes + old mtime → remove + disclose; use `git --no-optional-locks` for reads. `npx tsc -b` (never `--noEmit` — TS6310). Run the sweep WHOLE and read its own verdict line — never through a pipe that masks the exit; pre-commit runs show correct transient reds (manifest-differential pins, freeze legs pre-staging); the authoritative run is at the committed tip.
- **Drive idioms no file records:** selection is a DOUBLE-click (Arman's law, 2026-08-07 — a single click is deliberately inert; I burned an hour learning it); a form's body sits ~60–110 px ABOVE its label (dblclick a lift-ladder over `label.y`); the ops menu is a right-click ON the form; hide Leva by CSS injection; the Ambo tabs are lowercase small-caps DOM text (`packets`); the packet workbench filter defaults to the unresolved queue — select *All vertices* to reach the seeds; the ring's DOM labels are pointer-inert BY DESIGN (the pick layer is invisible 3D meshes — do not burn retries hovering labels); plates for another seat go to `scripts/app-leg/_frames/` (a plate in the scratchpad is not delivered).
- **Judgements in my head and nowhere else:** any new DOM chrome panel must import `CHROME_LAYER_Z` or it is born under a label (the sandwich is labels `[40,0]` < chrome 50 < windows/menus 60). After ANY view cut, grep `scripts/` for the old text BEFORE the whole sweep — several legs pin exact source strings (this cycle's one red was exactly that; the cheap order is targeted legs first, whole sweep at tip). The record strip's `?? ''` resolver terminal is an ADMITTED absence, structurally unreachable — never "fix" it to a token. `genealogyLayout.ts` is CARVED OUT (`dcdde159…` stands, Arman's word). S2's `genesisModel` grant did NOT add it to the standing sanctioned list. The F.5 wake-trigger stays parked: any form measured ≥10 distinct doors. Re-read the queue BEFORE the commit step — the habit this seat bought twice, and it caught T1/S2 in time.

## WHAT I RAN

tsc → 0 at every commit boundary · `diagnose-p5-the-acts` · `diagnose-argument-card` (§14 recut + §15 added) · `diagnose-p1b-granular-save` (§5b/§5c) · `diagnose-multi-region-lift` · `diagnose-conformal-lift` · `diagnose-d1-metric-thread` (recut) — ALL PASS · the restore probe (model) · the side-by-side drive · the cures drive · the T1 every-state drive · the S2 heading drive · **sweep at the final tip: SWEEP OK, runner exit 0** · the queue re-read before this letter (still only the three consumed). Plates: `b131_1..7`, `t1_1`, `t1_2` in `_frames/`. Port released (killed 11576).

## WHAT I CHANGED

`2e55501`: ManuscriptView + ManuscriptChrome (the frame-derived bound, the door target, the prong filter + absence slot, the acts/undo grammar, the 17-site floor) + the two legs' pins. `b86ae27`: `snapshot.ts` + `genesisModel.ts` + the two manifest rows, ALONE. `2bd5a3c`: geometryStore's six writers + the shelf/heading readers + p1b's §5c. The pin recut. This letter + the record pair + the three consumed letters archived ride the closing commit.

## WHAT I COULD NOT REACH

The §3 positive arm at the eye (three instrument attempts — the point-form will not take my double-click; the model half is closed). Her 52 px containing block (not reproducible in my harness; the cure makes the divergence inexpressible). The attention agent's eye-sighting (standing from B-130 — source-pinned, unsighted).

**The migration checklist, against T1 §4: after the closing commit — a committed, pushed, green tip ✓ · the inbox empty, every stamp and marker echoed ✓ · no unspent sanction (S2 spent whole) ✓ · no measurement only I hold (B-131 §2 is reported above) ✓ · no open STOP ✓. This seat is at the rest point.**

— the coder
