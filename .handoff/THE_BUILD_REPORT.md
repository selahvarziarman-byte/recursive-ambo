to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-23 12:24 +0330` (machine — `date`) · the chain `609293e → the record commit`, this report the record commit's own letter
re: **`STAMP C-7g` LANDED (`609293e`) — the designer's second cut, her four and the face block's five: the binary words have left the label lane (every one right of its point, none end-anchored — measured at the eye, 0 in the lane in every column), the width is bounded by construction (the AB drawing 938 px in the 1025 px panel at her viewport, from 1618), the card's term order groups by reading (the T cell's card 327 × 953 from 327 × 1133, its orderings one row of 60 px from seven rows of 240), and the face block reads one clause per line with the verdict never orphaned, the direction the face's own, the hands leading with WHERE.** C-8's item 0 measured ahead at the store level (§ line 9). ECHO §119 · `STAMP C-7g` · §121 (the MARKER's C-7g five) · Δ85 · Δ86 received, C-8 next.

## TO THE MOTHERSHIP — nine lines up

1. **Item 1's placement is a DEFAULT for her eye, and the measurement that forced it.** An up-arc of span *s* rows bulges 9.3·*s* px into a 118 px label lane, so every up-arc of span ≤ 12 lives ENTIRELY inside the lane; a word set horizontally beside a short up-arc lands where the caption was (the apex placement for span 1 reproduces the C-7f position to 2 px), and a word set ON the arc's path is clipped below a span of 2 (the path is 38 px long). ⇒ the up-arc's word stands to the RIGHT of its foot, across the point from its arc, in a block ABOVE the row line (the down-arcs' block below it, the loops' block led by the rings — moved below the line to be the bullet of a block that reads top to bottom). Nothing binary is end-anchored or left of the point. The reading she will judge: *the word is at the arc's foot, on the side of the row line the arc goes.*
2. **Item 2's width: two levers, both hers.** `WRAP` = 200 px by the geometry's own 6 px/char estimate (≈ 30 characters at the dense size) — measured over the fixtures before the cut: below 200 nothing narrows further and rows only multiply; at 200 Flow's column 636 → 358, Φ's 816 → 340, Φ-with-origins 1200 → 372 (estimated). At the eye: **1689 × 897 — the AB drawing 938 px in a 1025 px panel (fits; was 1618); 1400 × 900 — 938 in a 736 px panel (a horizontal scroll remains there).** To fit 1400 the levers are `WRAP` and the fold (150 px); a narrower fold she ruled out at 1689 by her own slack measurement, and I have not moved either. The widest positioned line at the eye 165 px against the 200 estimate (the estimate is generous, ≈ 13 %).
3. **The column is ONE GRID — every baseline a half-row (15 px) apart — after four leg runs and a probe; and the blind metric now counts an overlap only past half a pixel.** At a 12 px line pitch with the blocks hung at −5 / +13 around the row line, the metric rose from 0 to 13 word-to-word on Flow's column with NO glyph meeting another; 13 px and 15 px pitches left it at 13 and 11. A probe of the boxes found the cause: every word box is exactly 15 px tall, so lines on a 15 px grid MEET at the same y to a few millionths of a pixel (the strict test counted the touch), and one row's down block sat 12 px from the next row's up block (30 − 13 − 5). So the blocks hang at −3.5 / +11.5 (the down block's first line centred on the rings), every baseline in the column is 15 px from the next, and the driver's test asks for more than half a pixel of overlap (said in its comment — the metric's definition since C-7g). The metric after: 0 · 0 word-to-word and 0 · 0 word-to-label on the two columns, at both viewports. The C-7f `11 word-to-label` was the same kind of touch (up-feet 14.5 px above 12 px labels).
4. **Item 3's "at most THREE rows, whatever the cast" holds for binary types listed — and two more classes exist, both pinned.** An arity ≥ 3 type whose every tuple is reversed yet not every order listed reads `read as directed — all reversed: t 5` (the loader witness's own C-6d clause); a type with nothing listed gets `nothing listed: r` without a reading. On the fixtures: the T cell and Φ each collapse to ONE row (all directed, none reversed); the mothership's illustration (`disjoins 6` symmetric, `opposes 5 (2 reversed)`) is not the T cell's or Φ's — the fixture that shows three rows is none of ours.
5. **The card's heights after (her ask): at C (the T cell) 327 × 953 px, the cast's rows 348, the orderings row 60 px (was 327 × 1133 with the orderings 240); at B (Φ, 15 relation-types) 327 × 885, the cast's rows 280, the one orderings row 100 px** (fourteen words in one row wrap to five lines at the sidebar's width — a list broken between items, as she ruled).
6. **Item 6's `did not return` form — read with the addresses kept.** Her example `12 did not return — 11 broke at A–B · 1 at B–C: F1 F2 …` lists the names once after the counts; C-5's ruling gives every `Und` role its address, so the line reads `13 did not return — 8 broke at A–B: F2 F4 … · 4 at B–C: F1 F7 … · 1 at C–A: F13` (the counts per edge before the names). The block is 262 px tall at 1689 × 897 now (was 102): five lines per corner. The word half beneath it stays inside the box (y 380–504 with the panel scrolled to the block).
7. **Item 7's phrase applied to all three states.** She reworded the read state's head; the absent and refused heads said `walked A → B → C → A` — one grammar now: `the face A·B·C, walked in the face's own direction, A → B → C → A` in every state, the sentence and the witnesses swept together. Said, not hidden.
8. **Item 4: the plates are where she found the C-7f ones — `scripts/app-leg/_frames/concept-layer-face-refused-{1689x897,1400x900}.png` and `concept-layer-face-read-…png`** (the leg's ignored frames; this run's plates are the 15 px pitch). The sentence that a hand at AB changes the BC record is NOT written (item 4: hers to rule; not pre-empted). The lane's plate: `concept-layer-ab-foot-words-…png`; the wrapped Φ1 block is in `concept-layer-ab-glued-…png`; the card at Φ in `concept-layer-card-phi-…png` (new).
9. **C-8 item 0, MEASURED at the store level ahead of the build (1047 §2.0), both questions.** (i) THE LAWFUL PATH — casts on the seed's four corners only; AB mapped by pointing (2 pairs on A–B), AC by pointing (2 on A–C); nothing loaded on a midpoint; the core cell selected and the store's own `Apply Ambo Dissection`; the gen-2 site between AB and AC found (`ABAC`) — **the surface at it renders NOTHING (0 characters; no midpoint surface, no inside panel)**: your prediction holds in the mechanism. (ii) THE RESEARCHER'S RISK — **the medial edge AB–AC's record is EMPTY at gen 1 (the octahedron's own edge, `undefined`) and EMPTY at gen 2 (the new edge, `undefined`)**: no composed identity is in any record; it must come from the resolver (C-8 item 1, the MEET), never from a record. The eye's confirmation and the born room are C-8's; the leg's item-0 arm (which loads casts onto AB and AC — the shortcut) is retired there, not here (C-7g's boundary).

## 1 · What I SAW

- At the AB midpoint (Flow · Φ), the unfolding: no word left of any point; the up-arcs' words in a block above the row line right of the point, the down-arcs' below; Φ1's six loop words in a block led by six rings below its row line, four lines; the drawing 938 px wide in the 1025 px panel at 1689 × 897 (fits) and 938 in 736 at 1400 × 900 (scrolls). The blind metric printed, not judged: 0 · 0 word-to-word and 0 · 0 word-to-label on the two columns, at both viewports.
- The card at C (the T cell): `Term order` one row — `read as directed — none reversed: sustains 5 · outlasts 1 · individuates 1 · removes 1 · starts 2 · answers-to 1`; at B (Φ): one row of fourteen words. Heights in line 5.
- The face at AB's source C: refused → `NO FACE: the three acts around it, walked in turn, make …` with `on B–C: withdraw r1 ↦ Φ1 · on C–A: withdraw Φ1 ↦ F7 · here, on A–B: withdraw F7 ↦ r8`; one hand clicked → read, the head `walked in the face's own direction, A → B → C → A`, each corner five lines: `at A` / `returned to itself: none` / `returned elsewhere: F3 as F8` / `13 did not return — 8 broke at A–B: … · 4 at B–C: … · 1 at C–A: F13` / `the face's core at A, derived: 1 of its 14 roles`.
- Not sighted by a person: the designer's eye on any of it (she takes the first live drive at 1689 × 897); Arman's eye.

## 2 · What I RAN (verbatim)

- `npx tsc -b` → exit 0 (at the working tree of `609293e`).
- `node scripts/diagnose-the-inside.cjs` → `DIAGNOSE-THE-INSIDE: ALL PASS` — its C-7g notes: `Flow's feet: 7 down blocks · 10 up blocks · words per block [3,2,3,1,1,1,2,3,3,2,1,1,1,1,1,1,4] · lines per block [2,1,2,1,1,1,1,2,2,1,1,1,1,1,1,1,2]` · `the wide point's block: 4 lines · pitch to the next point 75 px (control 30) · right reach 204 (WRAP 200; control 72) · the span-8 arc's rx 74.4 ry 142.5 · Φ1's loop block 4 lines`.
- `node scripts/diagnose-the-cast-loader.cjs` → `DIAGNOSE-THE-CAST-LOADER: ALL PASS` — `the T cell's term-order rows: ["read as directed — none reversed: sustains 5 · outlasts 1 · individuates 1 · removes 1 · starts 2 · answers-to 1"]` · `Φ's term-order rows: ["read as directed — none reversed: component-of 2 · converges-on 2 · decays 1 · descends-from 4 · disjoins 2 · displaces 2 · exceeds-in-power 1 · fills 1 · inverts 1 · lodges-in 1 · presupposes 2 · re-creates 1 · specifies 1 · transmits 1"]`.
- `node scripts/diagnose-the-face.cjs` → `DIAGNOSE-THE-FACE: ALL PASS` (the seal line unchanged: `42 triples · Mov pairs Flow 76 · T 39 · Φ 45 · corner-local refusals Flow 0 · T 7 (in 6 triples) · Φ 0`).
- `node scripts/diagnose-the-midpoint.cjs` → `DIAGNOSE-THE-MIDPOINT: ALL PASS` · `diagnose-the-concept-type` ALL PASS · `diagnose-the-ambo-gesture-truth` ALL PASS · `diagnose-engine-freeze` exit 0.
- `node scripts/app-leg/diagnose-the-concept-layer-eye.cjs` (DRIVE FAMILY, spawned on 5199, `/__whereami` = `7d8d021` = HEAD at the run, dirty) → `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS — … her second cut read at the eye — the lane, the width, the card, the face's lines` (57 clauses: §0 + 28 at 1689 × 897 + 28 at 1400 × 900; run three times — at 12, 13 and 15 px line pitch — the last is the landed one). Its C-7g notes at 1689 × 897, verbatim: `foot rows 27 · arc-word box overlaps per column (the blind metric, printed): [{"arcWords":31,"wordWordOverlaps":0,"wordLabelOverlaps":0},{"arcWords":15,"wordWordOverlaps":0,"wordLabelOverlaps":0}]` · `C-7g the width: the AB drawing 938 px wide in a 1025 px panel (the C-7f run read 1618 in 1025 at 1689 × 897) · the widest positioned line 165 px against WRAP 200 by the geometry's estimate · 7 of its blocks wrapped · the own drawing: widest line 168 px, 11 blocks wrapped` · `the card at C (the T cell): 327 × 953 px, its term-order rows ["directed-none 60 px"], the cast's rows 348 px · at B (Φ): 327 × 885 px, ["directed-none 100 px"], the cast's rows 280 px` · `the face's reading at the eye (fix · mov · und · core per corner): ["A 0·2·12·2","B 0·0·9·0","C 0·0·10·0"] · the block {"y":109,"h":262,"insidePanel":true} · the word half beneath it {"y":380,"bottom":504,"insidePanel":true} at scroll 104 · the refusal's hands ["edge:2f1akb|r8|F7","edge:251ovc|F7|Φ1","edge:dy8738|Φ1|r1"]`
- The sweep at `609293e`, whole, read by eye in its own call: `138 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (sweep exit 0; 137 PASS and the one accepted fail; the inside 1.1 s, the face 1.4 s, the midpoint 1.9 s, the loader 2.8 s, the concept-type 12.6 s, the ambo gesture truth 3.5 s, the freeze 17.4 s; the j-register PASS at 71.4 s inside its declared ceiling; wall 92.5 s)
- The W1 riders carried AS FOUND with the record commit: ADR 0031 (9 insertions, 7 deletions; its working blob `68ec7cc5` — not the `655387b0` the 1110 letter verified: it moved again after that letter, and I carry what is there) · the connection-layer plan (1 insertion, 1 deletion) · the claims ledger (85 insertions) · the front board (11 insertions, 5 deletions) · the sovereign ledger (8 insertions) — each tail-probed by bytes and closing on a finished sentence (the ADR on "…uses the second.", the plan on its kill-condition, the ledger on "…both ADR riders as found.", the board on "…because you intend a read.", the sovereign ledger on "…not the chain (§120.1)."); none read for content; none in the freeze manifest, no re-seal

## 3 · What I CHANGED (`609293e`)

- `src/components/CastInsideDiagram.tsx` (no manifest row) — the geometry lays the rows by prefix sum (`yOf` variable; `columnBottom`; `lines(index)` — the wrapped blocks per point; `WRAP` exported; the line pitch half a row), the word blocks as positioned lines to the right of the point (`data-inside-line`), the rings below the line leading the loops' block, the bulge by span in rows.
- `src/lib/castLoader.ts` (NOT_FROZEN, row 214) — `orderingRows` replaces `orderingLine` (one row per reading × reversal class; the fourth and fifth classes named in its head).
- `src/components/Panels.tsx` (no row) — the card renders the rows (`data-cast-orderings-row`).
- `src/components/MidpointSurface.tsx` (no row) — `FaceRecord` takes the site's own edge as `here`; the head in one grammar; the refusal's `walked in turn`; the hands lead with WHERE; the read state's four lines per corner (`data-midpoint-face-line`).
- `scripts/diagnose-the-inside.cjs` · `scripts/diagnose-the-cast-loader.cjs` · `scripts/diagnose-the-face.cjs` — the witnesses swept with the sentences (lines 6–7 above; the new lane and wrap clauses).
- `scripts/app-leg/diagnose-the-concept-layer-eye.cjs` + `concept_layer_eye_driver.py` — the C-7g arms (the lane per column, the width and the widest line, the card at B, the face block's lines and the word half beneath it).
- No frozen file. No new file under the engine roots. The manifest untouched.

## 4 · What I could not reach

- The 1400 × 900 fit (line 2): the drawing is 202 px wider than that panel; the levers are hers.
- A person's eye on any of it.
- C-8: its item 0 is measured at the store level (line 9); the eye's confirmation, the resolver, the loader's whitelist, the composed identity's drawing, the dependency refusal and the trace's generation are the next letter's.

## ECHO

ECHO §119 (the ratification of C-5) · item 0 of C-5 routed to Arman — built for neither branch · `STAMP C-7g` (1008 §3, four; 1110 §3, five) · ECHO §121 (the MARKER: C-8 item 1 rewritten — read, not yet built) · Δ85 · Δ86 · ECHO §120 · `STAMP C-8` next, item 0 first.
