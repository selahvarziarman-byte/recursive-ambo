# REPORT — STAMP C-3 + STAMP A-3 + STAMP B-1 LANDED, MARKER W-3 RODE, the B-0 arc PARKED as superseded — echo of the eight-letter batch

## TO THE MOTHERSHIP

**ECHO: `STAMP C-3` · `STAMP A-3` · `STAMP B-1` · the `1710` SUPERSEDE (B-0 · B-0-v2 · B-0a PARKED whole — not built, the pre-measurement not spent) · `MARKER W-3`.** Internal order taken: B-1 → C-3 → A-3 (B-1 first because its STOP condition would have been the loudest news; it wasn't needed).

1. **B-1 — THE BIT IS ANSWERED, and the control is clean to the last digit.** At the walk's own transports (`sealDomainRealization(...).seal.deck.entries` — the same witnessed isometries the live walk applies; SW built by the zoo's own recipe, `buildFormDomain` over `dodecahedralTwistPairings(seed, 3)`; T³ by `buildThreeTorusDomain`), composed in WALK ORDER (`C = m_b⁻¹·m_a⁻¹·m_b·m_a`, the GPU loop's own left accumulation): **Seifert–Weber — NOT the identity at both door-pair choices**, `[f0⇄f9, f1⇄f10]` and `[f2⇄f11, f4⇄f7]`, max |C−I| entry = **2.642e+2** each (O(100): the far shore of the deckAbelian model's measured gulf; its 1e-6 tolerance and metric reused verbatim). **T³ — the identity EXACTLY at both axis pairs**, `[left⇄right, front⇄back]` and `[front⇄back, bottom⇄top]`, max |C−I| = **0.000e+0** — not even float noise. The instrument is right; SW means what it says. ⚠ The optional line: the `[f0⇄f9, f1⇄f10]` commutator word does NOT return within 200 repetitions (≤ 800 doors) — hyperbolic, plausibly infinite order. One observation: both SW choices gave the SAME magnitude (2.642e+2) — the dodecahedral pairing's symmetry, distinct doors, not a fixture artefact. **⇒ Station B1 reads NO-identity; B2 (the order-reading surface) is yours to charter.** The measurement lives as a sweep witness (`scripts/diagnose-commutator-bit.cjs`, census 128) — it runs in seconds, so it earned the census.
2. **C-3 — THE PAGE LEFT THE SHELF, and the price question CLOSES THE CLIP CLASS with the number: the cured shelf measures 102px against the 148px lawful ceiling** (empty state: scrollHeight 102 = clientHeight 102 — no internal scroll at all; 46px under the ceiling). Her 131 hypothesis was close; the real cured chrome is 102. **The clip class dies with this cut.** Her destination landed verbatim: at her measured 1401px chrome the pair sits at x1168..1383 — flush right (18px = the bar's own padding), 340px clear of the switch's end at 828. The shelf holds ONE kind. The notice attaches to `save the page…` and is drawn ONLY when unsaved — sighted both ways (absent on a clean page; present with the sentences verbatim after one invoke). ⚠ **One route-finding, cured in the same cut and named for the record:** the `?manuscript` dev route mounts the view WITHOUT AppShell — no bar, no slot — so a portal-only cut would have made the doors VANISH from the route you and every drive leg actually use. The same strip therefore renders fixed at the top-LEFT on the barless route (measured: the top-right there is the leva panel's — a right-end strip hides behind it). The ruled right-end destination is the real app's bar; the barless fallback is reachability, not a second design.
3. **A-3 — the survivor graded, strings byte-exact (Δ58).** The Cell Vertices pick rows read name-over-address in the two registers: `AB / vertex:mid:1qudnfs` … `CD / vertex:mid:1qpafic`, six reference seams counted at the eye, midpoint labels exactly as minted. One structural note: this composer's absence branch is an **id echo** (`?? shortenId`), so the non-designation discriminator is `displayLabel === shortId` — a structural mark, not a word match — graded italic+dim like item 4's absence register. No fixture reached that branch (every ambo vertex carries a label); the branch is compile-checked and honestly unsighted, as item 4's was. **The dualization rider:** no researcher line had landed when I opened A-3 — then your `1740` (A-3b) brought it, and it is cut below as a sanctioned frozen spend.
4. **W-3 rode:** `!.handoff/PLAN_THE_CAMERA_GATE.md` in the negation block; the file tracked AS FOUND (4883 bytes, tail closes on your kill-condition sentence; not read for content).
5. Forewarned, nothing owed: the Arc-`thicken` dead-button finding and the toolbar occlusion stay where Arman's word holds them.

## A-3b + C-3b — the `1740` markers, resolved into this batch

- **C-3b — ADJACENCY MEASURED: the notice's right edge sits 8px from `save the page…`'s left edge, same row** — so her sentence stands VERBATIM and the verbs stay split lawfully (position carries the link). No control moved registers.
- **A-3b — the dualization rider, LIVE and cut, as a FROZEN SPEND sanctioned by Arman in-terminal (2026-09-03): the positional `D${index+1}` mint at `dualization.ts:462` is dead. The mint now composes the derived reading THROUGH THE ONE COMPOSER — `faceDisplayName` (D14 rotation, never raw concat) — from the primal face AT THE ACT, and writes TRUE ABSENCE (`''`, the six sibling frozen mints' own idiom) where the face composes over unnamed corners. A lib→manuscript import with precedent (`noncubeDomain`'s `DomainModel`); no cycle (apertureModel reaches neither of dualization's two importers). **The spend landed as its own commit `e330084` with NOTHING else in it: the edit + the re-seal — manifest :49 `f450a61c…` → `94f6aad3…`, the new hash from the INDEX blob, the positive control (the old hash reproduced at HEAD before the edit) printed first.** Δ57 untouched: no GIVEN register enters the dual; read-only stays forced. **Its witness earned a finding of its own, cured before the spend committed:** my first clause composed from the POST-op shape and found NO FACE — dualization RETIRES the primal cell's faces from the live face list (the vertex keeps `sourceFaceId` as lineage) — so the clause ruled `''` against the mint's correct `ABAC·ABBC·ACBC` and redded on all twelve dual vertices. Instrumented PRE-vs-POST on one face (PRE corners `"ABBC","ACBC","ABAC"` → the composer's `ABAC·ABBC·ACBC` = the mint's word, byte-equal; POST: no face), the clause now composes from the shape AT THE ACT — the record the mint read — and pins every dual vertex against it plus a D-n resurrection guard: **Diagnostics passed**, both scenarios (tetrahedron→…→dodecahedron and cube→…→dodecahedron). The witness recut rides commit C (NOT_FROZEN, the completeness law kept: nothing rode inside the spend). ⚠ One observation for the vocabulary lane, no action: the dual vertices over the pyritohedral core compose over CONCAT-LINEAGE corner strings (`ABAC`, `BCCD` — the ADR 0029 §2 placeholder class one level deeper), so their derived names read `ABAC·ABBC·ACBC` — lawful carry of present content, and exactly the placeholder-composed survivor class already routed.**

## 1 · WHAT I SAW (5174 — 5173 was another's serve; served HEAD fingerprinted 061faef/3d83089 + this batch's dirty paths)

- **C-3, the real app** (root → Manuscript): the bar reads `Platonic Engine · [Ambo Universe ⇄ Manuscript] · save the page… load the page…`; after invoking a square the italic notice appears left of `save the page…` — *"there is work here that is not written down / switching modules keeps it · a full reload loses it — save the page… writes it down"* — and nowhere else. The shelf: title · "nothing loaded yet" · `load universe…` — one kind.
- **C-3, the dev route** (`?manuscript`): the strip at (14,8)/(126,8), clear of leva; notice absent on the clean page; the shelf at 102px.
- **A-3, ambo**: octahedron core selected → Selection → CELL COMPOSITION expanded → Cell Vertices rows graded as above; the 8 Cell Faces seams from 39f530e still standing beside them.

## 2 · WHAT I RAN

- `npx tsc -b` — exit 0 after each stamp and at the full tree.
- `node scripts/diagnose-commutator-bit.cjs` — **ALL CLAUSES PASS** (SW ★★ ×2 non-identity at 2.642e+2 · T³ ★★ ×2 identity at 0.000e+0 · the optional no-return line).
- **Drive family, the C-3 trigger (the shelf file ⇒ d8 + winding; the card untouched this batch ⇒ d12b/d13/deficit not triggered):**
  - **d8-shelf-route: ALL GREEN** (D10's boxes hold with the shelf at 102px; the universe door still the only multiple input). · **winding-route: 14/15 — the SAME parked remainder (E.circuit, the fan-chamber pillar arc), but its detail MOVED and I say so:** the return line now FIRES — `return 1 · back where you started · after 9 doors · the room came back turned` — where the last three runs read `None · doors 10` (never fired); the arm pins `after 1 door`, so it stays red. The shelf's smaller box shifts the fan-lift drop position, the wander path changed with it — the driver-side placement sensitivity the standing finding already names; nothing in the walk model changed this batch. Queued with rungs 3–4 as filed.
- `npm run sweep` at the landed HEAD — SWEEP_PLACEHOLDER

## 3 · WHAT I CHANGED (files, and why each)

- `scripts/diagnose-commutator-bit.cjs` (NEW, sweep census) — B-1's measurement as a witness: the seal's runtime deck, walk-order composition, the deckAbelian metric + tolerance, the T³ control wearing the STOP as a loud red.
- `src/AppShell.tsx` — C-3: the bar's right-end SLOT (`#shell-bar-page-slot`, `marginLeft:auto`), shown only while the manuscript is the active module — the shell owns the slot and its visibility, nothing else.
- `src/manuscript/ManuscriptChrome.tsx` — C-3: the page doors + the §7 unsaved mark LEAVE `SourcesShelf` (which drops `dirty`/`onSavePage`/`onLoadPage`); NEW `PageChrome` portals them into the shell slot (handlers stay beside the page state), with the barless-route fallback (fixed top-left) so the doors never vanish from `?manuscript`. Sentences verbatim; frame bar-sized; button copy still holds the §2B designer slots.
- `src/manuscript/ManuscriptView.tsx` — C-3: renders `PageChrome`; the shelf's three page props removed.
- `src/components/Panels.tsx` — A-3: the Cell Vertices name line styled by register (`displayLabel === shortId` ⇒ italic+dim), the address line wearing `title="reference — the vertex's address"`. Strings untouched.
- `.gitignore` — W-3: the one negation. `.handoff/PLAN_THE_CAMERA_GATE.md` — tracked AS FOUND.
- **Commit A (`e88c05f`) touched no frozen file and left the manifest untouched** (the commutator witness lives in `scripts/`, outside the engine roots).
- **Commit B (`e330084`) — the sanctioned frozen spend, alone:** `src/lib/dualization.ts` (A-3b: the mint composes through `faceDisplayName` from the primal face at the act; `''` for unnamed corners; the `index` param dropped with the label it served) + `docs/governance/ENGINE_FREEZE_MANIFEST.txt` :49 re-sealed `f450a61c…` → `94f6aad3…` from the index blob, positive control reproduced at HEAD first.
- **Commit C — the record:** `scripts/diagnose-dualization.cjs` (the A-3b clause: per-vertex pin against the composer's read of the primal face AT THE ACT + the D-n resurrection guard; the pre-op shape plumbed into the verifier), the record pair, the W1 riders.
- **W1 courier:** the mothership's dirty record files ride AS FOUND (tails probed at staging).

## 4 · WHAT I COULD NOT REACH

- The id-echo branch of A-3's grading — no live fixture reaches it (every ambo vertex is labeled); compile-checked, unsighted, said so.
- Standing residues unchanged and queued as you filed them: conePairs+coneFormBuilt and E.circuit with rungs 3–4; the camera gate parked whole.

*All eight letters archive with this landing — the three stamps discharged, the three parked as provenance, the supersede and the marker consumed.*
