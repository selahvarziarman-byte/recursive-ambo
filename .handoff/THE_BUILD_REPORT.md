to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-20 0009 +03:30` (machine) · HEAD == origin after the push — the build commits, in the order stamped: `41e2735` ((α) re-pinned) · `e2f8fa4` (riders 1–2) · `2a193b6` (C-6d (β)); this letter rides the record commit that follows
subject: ✅ **`(α)` RE-PINNED (`41e2735`) — weight and support relational, the unary home refuses and is counted apart, the seal renumbered and read to the digit, the wild seal under the instrument's exact τ maps.** ✅ **riders 1–2 LANDED (`e2f8fa4`).** ✅ **`C-6d (β)` LANDED (`2a193b6`) — the J register on the selected cell's seams: τ above the offers, all offers by reading, the five states, the take, the fiat pair with its conflict by name — sighted at my eye.** ⚠ **THREE LINES UP (items 1–3): a DEFINITIONAL divergence at the shared signature (τ ADDS to by-name sharing in the ruling's words; the instrument shares τ's types only — the seal's 16/37 are the instrument's reading, the register reads 18/62 under the ruling's and matches theirs to the digit when read their way); the "rides sustains alone" inference FALSIFIED by measurement; the T × Φ control fires only under a τ that translates the unary key (the fixtures spell it two ways).** ECHO §101 · §102 · §103 · MARKER `(α)`-support · MARKER `(α)`-weight · the ratio fix · riders 1–3 · STAMP `C-6d (β)`.

## TO THE MOTHERSHIP

1. **A DEFINITIONAL DIVERGENCE, FOUND AND MEASURED — reported, not tuned.** The instrument's `evaluate` reads `if t not in tau: continue`: under a τ it shares τ's types ONLY. The ruling's words (1621, as my header carries them: *by NAME with the same arity … and the person's τ makes differently-named types shared*) make τ ADD to the by-name sharing. Flow and Φ share `disjoins` and `presupposes` by name; τ₃ names `presupposes`, τ₄ names neither. Under the ruling's words the register reads the SAME top offers with MORE beneath — τ₃ 18 (4×1 · 2×17, 15 readings at weight 2) against the seal's 16; τ₄ 62 (5×1 · 4×14 · 3×30 · 2×17) against 37. With the by-name types renamed away in a copy of Flow (so only τ shares) the register reads the seal TO THE DIGIT (16 · 4×1 · 2×15 · 13 readings; 37 · 5×1 · 4×5 · 3×24 · 2×7) — the difference is that reading and nothing else. Both are pinned; the pin on the ruling's reading changes with the ruling. **One line: does τ add to the by-name sharing, or replace it?** (The surface reads the ruling's words today: Flow × Φ under τ₃ offers 18.)
2. **THE "RIDES SUSTAINS ALONE" INFERENCE IS FALSE, measured:** the size-3 core's four agreements are `sustains(F5,F5)↦descends-from(Φ7,Φ7)` · `sustains(F7,F7)↦descends-from(Φ1,Φ1)` · `presupposes(F8,F7)↦specifies(Φ2,Φ1)` · `exceeds-in-size(F8,F7)↦lodges-in(Φ2,Φ1)`. The two transcriptions of τ₃ agreed on the core because the Flow pair (F8,F7) holds BOTH `presupposes` and `exceeds-in-size` and the Φ pair (Φ2,Φ1) holds BOTH `specifies` and `lodges-in` — swapping the two τ targets swaps which tuple matches which, and the count stays 4. Under `sustains↦descends-from` alone the top is weight 2, 8 tied, and the core is not reached. My (vii) τ maps had come from the 09-08 instrument's own τ-search output at my run (the premise, disclosed); the seal's are the 09-19 file's literals, pinned verbatim.
3. **THE T × Φ CONTROL AND THE SPELLING:** the T cell's key is `member_status` (first_face_and_tower.py), Φ's is `member-status` (hinge_data.py) — by name they are not one key. Under `{answers-to↦decays}` alone the casts share NO unary key, so `r1↦Φ2 · r7↦Φ9` is an OFFER of weight 1 with 0 refusals (the measured zero, with its reason). With τ also translating the key (`{member_status↦member-status}`, arity 1 ↦ 1 — a categorical key is a type of arity 1 in the roles' home, within the ruling's "arity-preserving") the same matching is REFUSED, the conflict named `member_status: r7 has · member-status: Φ9 none-by-nature`, the register empty core (b) with 1 refusal counted (the measured one). Whose spelling is canonical is the researcher's to say.
4. **ONE MECHANISM ERROR OF MINE, found by the seal and corrected:** my maximality check extended a core one pair at a time and could not see an extension by a whole tuple matching (two new pairs at once), so cores another core strictly contained passed as offers — 19 on the T cell where the seal says 10. Offers are now the INCLUSION-maximal supported cores (the instrument's reading of "maximal"). The definition was not in question. The T cell reads the ten exactly: 11 (size 10) · 8 (9) · 4 (7) · 3 ×4 (4 readings) · 2 ×3 (3 readings).
5. **RIDER 1's PREMISE FALSIFIED, measured:** the T cell carries 0 axioms, so its line changes too — `taken — 10 roles · 7 relation-types · 21 relations · warrant carried, never read`. The rule is built as chartered; LAW 24 on `one-axiom.cast.json`.
6. **THE RECORD'S SHAPE, one line:** the FROZEN `EdgeIdentification` has `roles` and `types` both required, so "τ given, not yet acted" cannot share a record with "none taken" (`roles: []`). τ before a take lives in the store's `edgeTauDrafts` and is NOT exported with the workspace (the record is; an export carries every taken J with its τ); the take writes τ into the record; a withdrawal hands it back. If τ-before-a-take must persist, that is either a τ-only record (`roles` optional — a frozen spend) or a persisted draft; neither built.
7. **THE LAYER 3 WITNESS PANEL STANDS:** the register is mounted as its own section (`J Register`) before it and before the composition; the committed witness form was not removed — nothing ruled its fate, and a working view is not mine to take down.
8. **`not computed` at the eye:** no fixture pair is beyond the surface's 200,000-node budget (the whole offerings run in ≤ 5,000 nodes), so the state is pinned RENDERED by the witness at a budget of 10 (LAW 24), not sighted on a real pair.

# 1 · What I SAW

## `C-6d (β)` — headless Chromium, the Ambo, my serve on 5174 (the served tree carried the new section)
- **The seam `A - B` between the two corners holding Flow (A) and Φ (B) carries the one register; the other five seams show no register (a true absence): 6 rows, 1 state.** The load lines read the both-homes counts (`taken — 14 roles · 12 relation-types · 48 relations · warrant carried, never read`; `taken — 9 roles · 15 relation-types · 31 relations · warrant carried, never read`).
- **By name (no τ):** `offered` · `shared by name: disjoins · presupposes` · `types on the roles shared: member-status` · `1 tied · 1 reading — one offer at the top weight (this cast has 2, that one 1)` · 9 offers in 8 readings (`relational weight 2 · 1 offer · 1 reading` / `relational weight 1 · 8 offers · 7 readings`) · the top offer `F2 ↦ Φ7 (support 1) · F4 ↦ Φ1 (support 2)` with `relational weight 2 · 5 unrecorded · 1 known on one side only · types: 2 agree · 0 unknown` · `take none` offered.
- **τ₃ given through the selects (three `add pair`s):** τ printed above the offers, each pair `yours` with `withdraw`; `shared under τ: sustains ↦ descends-from · presupposes ↦ specifies · exceeds-in-size ↦ lodges-in`; the mark `τ given — the offers below were re-derived for the τ now in force`; **18 offers in 16 readings** (`relational weight 4 · 1 offer · 1 reading` / `relational weight 2 · 17 offers · 15 readings`); the top offer `F5 ↦ Φ7 (support 1) · F7 ↦ Φ1 (support 3) · F8 ↦ Φ2 (support 2)` · `relational weight 4 · 31 unrecorded · 1 known on one side only · types: 3 agree · 0 unknown`.
- **`take` on it:** state `given`; `J given` with `F5 ↦ Φ7 · support 1` · `F7 ↦ Φ1 · support 3` · `F8 ↦ Φ2 · support 2` and the counts line; that offer wears `given — this one`; `take none` gone; `withdraw` present.
- **One τ pair withdrawn (presupposes ↦ specifies):** the J survives (`given`), its marks re-derive — `F5 1 · F7 2 · F8 1`, `relational weight 3 · 29 unrecorded · 4 known on one side only · types: 3 agree · 0 unknown`; the mark `τ withdrawn — the offers below were re-derived for the τ now in force`; the offering re-derived to 13 offers under `2 tied · 2 readings — genuinely different choices; within a reading they differ only by symmetry (this cast has 2, that one 1)`, the given J one of the two (`given — this one`). The pair re-added: the 18 return.
- **A fiat pair (F1 ↦ Φ9) added:** `F1 ↦ Φ9 · support 0 — yours, backed by no offer under this τ` with `in conflict: member-status: F1 has · Φ9 none-by-nature` under it; the three offered pairs keep 1 · 3 · 2; the counts `relational weight 4 · 58 unrecorded · 2 known on one side only · types: 3 agree · 0 unknown` — no count of conflicts anywhere; no offer wears `given — this one` (the record has four pairs).
- **`withdraw`:** `offered` again, τ still in force (the draft), `take none` back. **`take none`:** `given` with `nothing identified`, withdrawable. **Withdrawn:** `offered`.
- **t-cell × t-cell (both corners the T cell; the edge's τ from before names three types these casts do not declare and is marked so — `"sustains ↦ descends-from" names a type a cast does not declare` — never erased):** 10 offers weight-ordered `11 · 1 offer · 1 reading` / `8 · 1 · 1` / `4 · 1 · 1` / `3 · 4 offers · 4 readings` / `2 · 3 · 3`; `1 tied · 1 reading — one offer at the top weight (this cast has 1, that one 1)`; the top offer the identity with `r0 5 · r1 5 · r2 3 · r3 1 · r4 1 · r5 1 · r6 2 · r7 1 · r8 3 · r9 1` and `relational weight 11 · 1489 unrecorded · 0 known on one side only · types: 10 agree · 0 unknown`; `types on the roles shared: member_status`.
- **triangle × triangle-symmetric:** `6 tied · 1 reading — they differ only by symmetry, so pick any (this cast has 3, that one 6)`; 6 offers in 1 reading; the first `x ↦ x (support 2) · y ↦ y (support 2) · z ↦ z (support 2)` · `relational weight 3 · 3 unrecorded · 3 known on one side only · types: 0 agree · 0 unknown`.
- **triangle × t-cell:** `empty core (a)` — `no relation-type in common — a translation is yours to give`, the two type selects beside it, no offer.
- **The composition section stands untouched below; the Layer 3 Witness section stands.** `not computed` not sighted on a real pair (item 8).

# 2 · What I RAN (verbatim)
- **Sim-diff:** empty after each of the three commits.
- **Freeze manifest:** nothing frozen touched; the new file is `src/components/JRegisterPanel.tsx` — src/components carries no rows (no classification due); `jRegister.ts` and `castLoader.ts` NOT_FROZEN as before.
- **The researcher's instruments, RUN by me:** `offers_count.py` 9 s (the ten, at the 1835 weights); `flow_phi_offers_under_tau.py` 1 s — identical to their RESULTS file but for the order of the four sample lines printed per weight group.
- **`node scripts/diagnose-the-j-register.cjs`** (48 clauses): `DIAGNOSE-THE-J-REGISTER: ALL PASS — the second implementation reads the corrected seal's numbers, the offers follow relational support, the unary home refuses and is counted apart, the empty core is named both ways, and the surface renders the five states, the take and the fiat pair by name` (81 s standalone; the head declares 900 s, left standing). **`node scripts/diagnose-the-cast-loader.cjs`** (43): ALL PASS. **`node scripts/diagnose-the-concept-type.cjs`**: ALL PASS. **`node scripts/diagnose-the-ambo-gesture-truth.cjs`** (17): ALL PASS.
- **`npx tsc -b`**: exit 0 at every commit.
- **`npm run sweep`** at `2a193b6`, whole, read by eye in its own call: the header `THE SWEEP — 135 legs · 23 workers · per-leg ceiling 420s`; the one non-PASS row `FAIL    scripts/diagnose-dual-inspection.cjs  12.3s`; `PASS    scripts/diagnose-the-j-register.cjs  72.6s` (inside its declared 900 s), `PASS    scripts/diagnose-the-concept-type.cjs  3.6s`, `PASS    scripts/diagnose-the-cast-loader.cjs  0.8s`, `PASS    scripts/diagnose-the-ambo-gesture-truth.cjs  1.1s`; wall 79.5 s; the canonical lines `135 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else`; sweep exit 0.
- **The drive family:** not triggered — the reading is the inspector, not the walk; said.
- **`.handoff/inbox/coder/` re-listed before each commit:** three letters, `--mark-reviewed --expect 3` in its own call each time; archived by name with this landing.

# 3 · What I CHANGED
- **`41e2735` — (α) re-pinned.** `src/lib/jRegister.ts`: weight and support relational; the unary home refuses (`unaryRefusals` counted) and is printed as two counts; τ on unary keys at arity 1; `assess` with conflicts by name and `describeConflict`; `gaugeOrbitGroups` and `readings`; `countFull`; (a) on no relational type; inclusion-maximal offers. The witness rewritten against the renumbered seal, the instrument's τ maps, both readings of the shared signature, the T × Φ control; its head carries the measured runtime (44 s standalone, 73 s beside a concurrent run; the 900 s declaration left standing).
- **`e2f8fa4` — riders 1–2.** `castLoader.ts`: the axioms clause conditional. `one-axiom.cast.json` new; `both-homes.cast.json` → `one-name-two-homes.cast.json`. The loader witness follows (43 clauses).
- **`2a193b6` — (β).** `src/components/JRegisterPanel.tsx` (new) · `Panels.tsx` (the mount, the drafts passed) · `geometryStore.ts` (the drafts, three actions, the one writer) · the j-register witness §10 · the concept-type census (five readers; one write site).
- **The record commit:** `.handoff/THE_BUILD.md` (the three letters verbatim), `.handoff/THE_BUILD_REPORT.md` (this letter), the W1 riders AS FOUND — `.handoff/PLAN_THE_CONNECTION_LAYER.md` · `.handoff/THE_CLAIMS_LEDGER.md` · `.handoff/THE_FRONT_BOARD.md` (tails close on finished sentences; not read for content).

# 4 · What I could NOT reach — named
- **The wild seal under the ruling's words** — the seal's 16/37 are the instrument's reading; the register reads them exactly only when read the instrument's way (item 1). Pinned both ways; the line is yours.
- **`not computed` on a real pair at the eye** (item 8) — rendered under the witness's small budget instead.
- Nothing else of the three letters.
