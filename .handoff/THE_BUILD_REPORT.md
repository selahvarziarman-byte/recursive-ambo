to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-24 22:35 +0330` (machine — `date`) · HEAD == origin == `e95e000` (ruling 3 `a8d02e9` · ruling 1 `b2dcb80 (the cure the sweep asked for e95e000)` · the record `e95e000`) · this letter is the record's
re: **§148's two rulings LANDED, before the stone's commit — ruling 3 (THE WAY BACK KEEPS THE CHILD) at `a8d02e9`; ruling 1 (ITEM 4 — THE FINER BOUNDARY, in the lift) at `b2dcb80 (the cure the sweep asked for e95e000)`. Each with its witness; the eye leg ALL PASS with a new §13 (the gen-2 corner cell glued into a room, the doors act and the cargo on it) and the way-back pinned at the eye; the sweep OK at each landed HEAD; tsc 0; no frozen file. Item 5 stands withdrawn; naming stays parked. ECHO §148. C-12b next.**

## TO THE MOTHERSHIP

1. **Ruling 3 was cured one step wider than ruled, measured and said (`a8d02e9`).** While curing "the existing child made current" I measured the frozen mint (`ids.ts`, row 54, untouched): it hashes parent + operation + depth alone, so two DIFFERENT cells of one parent minted ONE id — gen 1's core and its residue at A both `…:2:czpwku` — and the second child silently overwrote the first (shapes held 3, not 4): the same data-loss class as your ruling's. `ambo.ts` (NOT_FROZEN) now rides the parent's id AND the dissected cell's id in the mint's hashed argument; the id's form is unchanged; only NEW dissections mint differently (a saved page's ids are carried, never re-minted). The store returns the person to the held child by that id. Witnessed both ways (§2: the same object, no history entry; a different cell a different child, both held). If you want the mint change held back, it is one line to revert; I judged it inside your ruling's own reason.
2. **Ruling 1's title — a naming line, parked as you parked naming.** The lift's title reads the SOURCE cell's record (`the tetrahedron A·AB·AC·AD of Ambo Dissection Tetrahedron`, its four recorded corners) while the LIFTED cell's record now names the seven corners its boundary walks (the three midpoints on its face). The card's rows show all seven; the notice and the shelf show four. Yours to rule with the other naming lines.
3. **C-12b begins now** from its ground (ADR 0031 §3.10 as amended, your 1947 MARKER — `≡_X` IN the signature, holds-only, carried by the lift, the pushout and the door, not the cargo — the researcher's §27, the designer's 1939, the reference runs R1–R4). I ring when it is filed.

## What I SAW — the drive family, `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS` (131 clauses — 127 → 131, one new arm per viewport; `/__whereami` = `a8d02e9`, dirty — the ruling-1 tree; the leg ran three times on this cut — the first found the selection reading a group name the sheet never mints (the placed forms are `written:w:w1`, `w2`…) and §8's hand grammar pinned for a corner edge where the finer face's edges are medial; the second measured everything right and left one pin of mine counting the menu's placeholder; the third whole on both viewports)

At 1689 × 897 (and at 1400 × 900):
- **Ruling 1 — the gen-2 corner cell glued into a room (§13).** The residue at A lifted from gen 2 (7 corners) selected on the sheet by a double-click (the aperture opened on it offering SEVEN faces is the selection's own instrument — a room offers none, the gen-1 residue four; taken on the 3rd candidate at 1689 × 897 and the 5th at 1400 × 900, the earlier double-clicks opening the still-selected gen-1 form's aperture or dismissing); its aperture offers its FINER boundary by D14 name — three sides of 4 corners (`A·AC·ABAC·AB` · `A·AB·ABAD·AD` · `A·AD·ACAD·AC`) and four triangles (`ABAC·ABAD·ACAD` and the three at AB · AC · AD); the two sides at the hinge A–AB picked, the map `A→A · AC→AD · ABAC→ABAD · AB→AB — preserving (derived)`; F1 ↦ F1 at A taken with its line; `glue — the S² gate judges` → glued, the second built room joining the dim-3 band. **The doors act and the cargo on it:** the room summoned at the FIRST candidate, its walk opened, the cargo line in the PICK state at the entry corner A; F1 picked and carried along A–AC; the door `a` pressed — one period, doors 1 — the cargo across by the transport at AC; the rod AD–A pressed — home, Fix, the route `A–AC · a · AD–A` (`carrying F1 — at the corner AC, came along A–AC as r2 ≡ F1` · `carrying F1 — at the corner AD as F1, by A–AC · a` · `carrying F1 — returned to itself by A–AC · a · AD–A` — the same words as gen 1's room, on the room of seven corners). Plates `concept-layer-gen2-door-{taken,glued}-*` and `concept-layer-gen2-{pick,rod,door,home}-*` in the ignored `_frames/`.
- **Ruling 3 at the eye (§12).** In the workspace tab's Genealogy panel g1 pressed is current; gen 1's core dissected AGAIN returns the EXISTING gen 2 — still three rows, g2 current, no fourth shape — and the arms that follow read gen 2's acts as given.
- **§8 re-pinned:** the lifted form's face list offers the FINER medial face `ABAC·ABAD·ACAD` (the coarse `AB·AC·AD` is recorded on its tiles, not offered); its born-face reading mounts on the record as before.

## What I RAN (verbatim tails)

- `scripts/diagnose-the-found-bugs.cjs` — `DIAGNOSE-THE-FOUND-BUGS: ALL PASS` (24 clauses). Ruling 3 (§2/§3): the same dissection after the way back returns gen 2 itself — the same id AND the same object, history 3 → 3, the order and the count unchanged; a different cell of the same parent mints a different child, both held, each marking the cell it dissected as its `parent` cell; **the born pair stands 1 → 1** (it dropped 1 → 0 before). Ruling 1 (§6): the gen-2 residue at A lifts as **7 corners · 12 edges · 7 faces** — three sides opened through their midpoints (60/60/180/60), four finer triangles; the cell's record names those 7 faces and 7 corners; EVERY edge on exactly two of the cell's faces; the coarse face AC·AB·AD recorded on its four tiles' `composes`, each coarse side on its two halves'; the aperture's menu of seven by D14 name; two maps at the hinge, the preserving `A→A · AC→AD · ABAC→ABAD · AB→AB`; F1 ↦ F1 at A taken with **four corner pairs**; **the glue SOUND** (v 5 · e 9 · f 6 · c 1, χ 1 — bounded, five walls) where it threw; the cell surface 7 faces (4/4/4/3/3/3/3) · 12 rods; the cargo room entry A · 12 rods · 7 faces · one door; the control — the gen-1 lift 4 · 6 · 4, nothing composed.
- `npx tsc -b` — exit 0 at both cuts. `scripts/diagnose-engine-freeze.cjs` — ALL PASS.
- **The sweep, whole, read by eye in its own call** — at `a8d02e9` (ruling 3): `144 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (exit 0, wall 97.8 s); at `b2dcb80 (the cure the sweep asked for e95e000)` (ruling 1): `144 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP RED — unexpected fail(s): scripts/diagnose-the-doors-act.cjs · scripts/diagnose-the-lift-carries.cjs` (wall 100.4 s) — both read the gen-2 lift and pinned its three-corner sides; re-pinned at `e95e000` (the doors act: the quad door A·AC·ABAC·AB ~ A·AB·ABAD·AD, its own one-way D10 control sized by the door's corners, the born pair's line now a path of length TWO through the born corner — `cannot cross this door — no line opposite: on A·AC·ABAC·AB Φ2 at AC · Φ2 ≡ r1 at ABAC · r1 at AB` — and a new clause pinning the gen-2 glue SOUND where every candidate threw; the lift carries: four born faces and no corner-cell face on the gen-2 concept, the finer medial face's hands in the medial-edge grammar); at `e95e000`, before this letter was written: `144 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (exit 0 — the doors act 10.6 s, the lift carries 3.3 s, the cargo 1.9 s, the found bugs 1.7 s; the gate 89 s, the door column 87 s, the j-register PASS at 74 s inside its declared ceiling, the born room 54 s; wall 98.0 s). (A sweep run on the DIRTY tree before ruling 3's commit went red on three working-tree guards — argument-card, the folded edge, the ink — that pin ambo.ts and the store byte-identical to HEAD; they are green at the committed HEAD, which is where the doctrine reads the sweep.)
- The drive family: `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS` (131 clauses).

## What I CHANGED

- **Ruling 3 (`a8d02e9`)** — `src/store/geometryStore.ts` `applyOperationToSelection`: the act's result derived; when its id is already held, `selectShape` to it and return (nothing stored, nothing minted, no history entry). `src/lib/ambo.ts`: the mint's hashed argument = `${parent.id}/${sourceCell.id}`. `scripts/diagnose-the-found-bugs.cjs` §2/§3 re-pinned on the cure (+ the sibling clause). The driver's genealogy arm dissects again at the eye; the leg's §12 way-back clause pins it.
- **Ruling 1 (`b2dcb80 (the cure the sweep asked for e95e000)`)** — `src/lib/subComplexLift.ts` (NOT_FROZEN): the coarse-side/coarse-face compositions run for `cellIds.size <= 1` (they ran for surfaces only — a comment-stated precondition true of multi-cell lifts); the validator counts a composed entity as present through its live parts; the extract opens each lifted face's composed sides through the record's stations and re-records the one cell's live faces and corners. `scripts/diagnose-the-found-bugs.cjs` §6 (new) and §1 (the title reads the source). The driver: `cargo_arm_body` parameterized by room prefix and plate tag; `select_written` (a written form summoned as a room is); `door_arm_gen2`; the medial pick moved to the finer medial face. The leg: §8 and §12 re-pinned, §13 new. No frozen file: `level3LinkExtractor.ts` (row 58) and `faceIdentification.ts` (row 51, the seed census) untouched — the lifted cell now meets their law.
- **The cure the sweep asked for (`e95e000`)** — `scripts/diagnose-the-doors-act.cjs` (the gen-2 quad door; the one-way control generalized; the born pair's line through ABAC; the glue SOUND pinned) and `scripts/diagnose-the-lift-carries.cjs` (four born faces, no corner-cell face on the gen-2 concept; the corner cell's face read on the gen-1 lift; the medial-edge hand grammar).

## What I could not reach

- Nothing owed by §148 is left. 

## PARKED (one line each)

- The lift's title reads the source cell's four corners while the lifted cell's record names seven (above, 2).
- The driver's `roomCaption` note still looks for `built 3-manifold 1` on the gen-2 room (a note, not a pin).
- New dissections mint ids that differ from the same act's ids before `a8d02e9` (the cell now in the hash); nothing tracked pins an id.

## W1 — the courier, at the record commit

Carried AS FOUND with their hashes in the record commit's message, as your 1941/1947/2117 name them: ADR 0031 (§3.10 as amended), the researcher's INSIDE-MIDPOINT-TRACE ruling (§25–§27), the claims ledger (§145–§148 by its clock); the sovereign ledger if moved. Carried AS FOUND at the record commit (hashes with their instruments; none read for content; no re-seal): ADR 0031 `blob dde61338` → `e7565a44` (`sha256-LF a2a3b1c2` → `b0795661`, 3/1, mtime 19:45:24 — §3.10 as amended by its clock; the tail closing on a finished sentence) · the researcher's INSIDE-MIDPOINT-TRACE ruling `blob 3b15acd3` → `c54fa6f2` (`sha256-LF 60dfa55e` → `862f90de`, 13/0, mtime 19:45:46 — §26–§27; the tail closing) · the claims ledger `blob 33666555` → `d650e2e9` (`sha256-LF 7e4aa0d8` → `cc94efb9`, 3/0, mtime 21:37:47 — §145–§148, eight marks on disk against six at HEAD; the tail closing on `*(21:38)*`); the sovereign ledger byte-equal to HEAD (`blob 2d731659`) — nothing to carry, said.
