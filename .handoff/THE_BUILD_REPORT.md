# REPORT — STAMP C-1 (the card cycle, five items) + RIDER R-1 (the inbox gate) — echo of the 0910 ruling, marker-free run

## TO THE MOTHERSHIP

1. **STAMP C-1 echoed — the batch acceptance is SIGHTED AT THE EYE:** two tori glued from two different squares now read *"abAB : the 4-gon you made first ⟶ Torus (T²)"* and *"abAB : the 4-gon you made second ⟶ Torus (T²)"* — WHICH ONE, not WHAT KIND, on the card a person actually opens (I opened both, shift-click by shift-click).
2. **RIDER R-1 echoed — the gate stands and its bootstrap act is named** (your own front-board law, applied the hour it was written): the hook is tracked (`scripts/hooks/pre-commit` → `check-coder-inbox.cjs`), the wire is `git config core.hooksPath scripts/hooks` — **run by me in this checkout at 05:36Z today**, and installed for every future checkout by a `postinstall` line in package.json. Five arms measured before it guarded its first real commit: refusal-naming-letters · loud override · the gesture · pass · arrived-after-the-mark. This very landing passed through it live — and the gate SURVIVED the power-out and profile change unbroken (config + marker rode the disk).
3. **Item 5 re-priced at my hand, and the re-price REFUSED half the ask:** the three bound surfaces are walk panel (fitLog, landed last cycle), specimen card (already bounded: `calc(100% − …)` + its own scroll), aperture panel (already bounded: clamp + rows scroll). **No extraction** — fitLog's machinery is walk-specific (canvas yield, log-line drop, hysteresis); nothing else has a canvas or a log. The fourth surface the sweep found is the DESIGNER's: the shelf's 140px floor now clips its own grown chrome (~241px since the page-door pair landed — her located 103px-hidden clip), **but a bigger floor is arithmetically unlawful**: non-overlap with the panel needs shelfHeight ≤ 100vh − 192 − min(560, 100vh−340) = **148px at 900vh**. I cut the floor to 252, derived that, and REVERTED in the same hour — the cure is the flagged column layout (or a measured shared budget), her plate, routed with these numbers written at the site.
4. **The deficit leg's 7 reds, diagnosed to two classes (§2 below):** (i) FOUR faces of ONE stale locator in the deficit driver — `get_by_text("the argument reading", exact=True)` can never match the C-1 item 1 header ("the argument reading— show it"), so the door never opened and the card-section arms cascaded; CURED by the ruled-copy recut (the app's own `[data-argument-door]` seam + the closed-copy pin + raise-by-name, one helper, four sites). (ii) ONE remainder, PRE-EXISTING by control: `explore.conePairs` ("the box's six faces did not resolve (ends by @0/@1 stem + four sides)") and its downstream `coneFormBuilt` fail IDENTICALLY at clean `4123ddc` (the worktree control) — they predate this cycle (drift somewhere between `3f096a4`'s green and the recut landing; driver-side resolution heuristic by the smell of the detail string, unconfirmed). Named as a standing remainder beside winding's E.circuit; one letter away if you want it hunted. The pre-outage extras (shellIntactOnReturn · cardDoorAndEsc · emphasisRowSide · promoteCardRow) were all DOWNSTREAM of the closed argument door — the recut cured them with it (7 → 1).
5. **A word-register observation for your lane, no action taken:** the invoke palette names the form **"Square"**; the argument header names its source **"the 4-gon"**. Same concept, two nouns, both pre-date this cycle. If the card should speak the palette's word (or the palette the card's), that is a meaning ruling.
6. Item 3's refusal copy is verified **at the seams by its witness leg** (R1–R5, real lib sentences, both card seams pinned to route through the filter); I did not construct a live refusal fixture at the eye this run — named, not worked around.
7. **A power-out mid-ceremony, disclosed:** the machine lost power between the five-leg run and the landing; the session resumed on a fresh Windows profile (the old one's python + playwright gone). Every edit, the gate, and the record assembly survived on disk; tsc re-verified exit 0 on the new profile; python + playwright + chromium were reinstalled on the new profile (Arman's own setup act) and both outstanding measurements then ran to completion.

## 1 · WHAT I SAW (drove the app, 5173, served HEAD fingerprinted 4123ddc + this cycle's dirty paths)

- **Item 1 — the three doors speak their state:** an invoked square's card read *"the argument reading — show it"*, *"the measures — shown"*, *"the field — show it"*, *"certificate — show it"*. Clicking the argument door flipped its span to *"— shown"* and the reading opened MAP-FIRST (spine → incidence → stance), per 0024.
- **Item 2 — THE ACCEPTANCE:** built square #1 → Glue → Torus (abAB); square #2 → Glue → Torus (abAB). Torus #1's card: *"abAB : the 4-gon you made first ⟶ Torus (T²)"*. Torus #2's card: *"…you made second…"*. The single-source case keeps the bare phrase (the square's own card showed no ordinal — rank/total compose only when the same shape name counts ≥ 2, in append order = act order).
- **Item 4 — the grading, live in ambo:** dissected the tetrahedron, selected the residue cell, expanded CELL COMPOSITION → Cell Faces: the NAME line leads (*A-AC-AB*, composed from corners), the address sits under it in mono (*face:6j9j5c*) carrying `title="reference — the face's address"` (4 rows, 4 title seams counted in the DOM). Zero italic-absence rows here — every ambo face composes a name; the absence styling is the `displayName === 'unnamed'` branch. ⚠ The composed names stand on midpoint placeholder content (*AC*, *AB* — the ADR 0029 §2 carve-out): that is the named SURVIVOR, routed uncut, untouched by this grading.
- **The shelf** showed its three doors whole at my viewport; the clip class is real on short viewports and designer-routed (§TO THE MOTHERSHIP ¶3).

## 2 · WHAT I RAN

- `npx tsc -b` — **exit 0** (run after every item; re-run exit 0 on the post-outage profile).
- `node scripts/diagnose-refusal-copy.cjs` — **ALL CLAUSES PASS (R1–R5)**: the designer's third instance cured verbatim; junk tail drops to the bare entity word; prefix goes without quotes; clean copy rides byte-identical; both card seams (`deficitRegisterModel` refusal row · the view's acquire line) pinned to route through `personReadableRefusal`.
- **The R-1 gate, five arms** (before its first real duty): (1) unconsumed letters ⇒ COMMIT REFUSED, exit 1, both real letters NAMED with arrival times; (2) `CODER_INBOX_OVERRIDE=1` ⇒ loud pass naming what it jumped; (3) `--mark-reviewed` gesture; (4) pass with census line; (5) marker aged backward ⇒ refusal wearing the *arrived-after* diagnosis. No fake mail was ever minted — the arrived-after arm aged the MARKER.
- **The drive family — ALL FIVE legs triggered** (this cycle touches card copy ⇒ d12b+d13, the shelf box file ⇒ d8+winding, deficit rows ⇒ deficit-app):
  - **d8-shelf-route: ALL GREEN** (the shelf file's change is comment-only — the bound value is byte-identical; D10's boxes undisturbed).
  - **d12b-carried-names: 1 red = (f2)**, the vs-HEAD byte-pin of `argumentReadingModel.ts` — the in-flight class (the pin compares working tree to `git show HEAD:`, so it self-heals at this landing); its HELD subject (f1, the scaffold law) PASSED untouched, and the 0910 charter for item 2 postdates the hold and names this file.
  - **d13-the-door-speaks: ALL GREEN** (the door spans did not disturb its seam-anchored pins).
  - **winding-route: 14/15** — the SAME one named remainder (E.circuit, the fan-chamber pillar arc, `None · doors 10`), parked with rungs 3–4; the cone room's ★★ return line, the retrace falsifier, and the mirrored fourth string all PASSED.
  - **deficit-app: 7 reds, two classes.** Class (i): the door-locator domino — the driver's `exact=True` text locator predates item 1's state span (the DOM text is "the argument reading— show it"; flex gap contributes no text node, measured in the driver's own doorCopy output), the miss silently skipped the click, and mapSection/incidence/stance/verdict/rimTurn/cardIdentity/ringMerged×2/emphasisRowSide/promoteCardRow cascaded. CURED: one `open_argument_door` helper — the app's existing `[data-argument-door]` seam as locator, the closed-state copy PINNED ("show it", C-1 item 1 cited), a missing door RAISES BY NAME (I-1a) — replacing the identical block at four sites. RE-RUN at my tree with the recut driver: **1 failure — only the pre-existing §E-DOOR-LAW pair** (7 → 1; every card arm, ringMerged, shell-intact, cardDoorAndEsc, emphasisRowSide, promoteCardRow now GREEN).
  - Class (ii): the CONTROL at clean `4123ddc` (fresh worktree, same leg): **1 failure — the SAME conePairs+coneFormBuilt pair, byte-identical detail** ⇒ pre-existing, not this cycle's. Both runs' full ok:false census: exactly those two arms, in each tree.
- `npm run sweep` PRE-COMMIT (the signal run, census 127 — the new witness adopted): **14 unexpected reds, all diagnosed, ZERO live defects** — 13 are the manifest-vs-HEAD in-flight class (each pins the manifest's diff-vs-HEAD as empty-or-its-own-rows; my refusalCopy row is the foreign delta until it lands — thicken's own label names the healing condition, "or EMPTY … once the re-seal has ARRIVED at HEAD"), and 1 was a REAL ruled-copy pin (deficit-register §4-FIX-B E3, pinning the raw refusal sentence the item-3 filter now lawfully rewrites) — recut from the ruling, re-run, GREENS. Notably `diagnose-argument-card` — the witness of the ordinal-touched file — passes EVERY behavior pin; its one red was the manifest class.
- `npm run sweep` at the landed HEAD `39f530e` — VERBATIM: `127 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (all 13 in-flight pins healed at the landing, exactly as their own labels promised; deficit-register whole-greens with the recut pin).

## 3 · WHAT I CHANGED (files, and why each)

- `src/manuscript/ManuscriptView.tsx` — item 1 (three door state-spans); item 2 (the `sourceOrdinalByShape` memo — groups written forms by shape name in APPEND order, the act-order carrier; threads `{rank,total}` into the reading only when ≥2); item 3 seam 2 (the acquire-refusal line routes through `personReadableRefusal`).
- `src/manuscript/argumentReadingModel.ts` — item 2: `ordinalWord` (first…twelfth, then `13th`), third parameter `sourceOrdinal`, source phrase becomes *"the 〈name〉 you made 〈ordinal〉"* when total > 1. The model composes the PHRASE; the view owns the CONDITION — the reading stays a pure function of its inputs.
- `src/manuscript/refusalCopy.ts` (NEW, manifest row NOT_FROZEN) — item 3: the class cure at the seam. Strips the leading module prefix; rewrites `entity "id"` to *"the 〈entity〉 〈tail〉"* when the tail reads like a name, else *"the 〈entity〉"*. The frozen lib producers stay byte-untouched — their sentences are lawful as lib errors; the CARD is where a person reads.
- `src/manuscript/deficitRegisterModel.ts` — item 3 seam 1: the refusal row's value routes through the filter.
- `scripts/diagnose-refusal-copy.cjs` (NEW) — item 3's witness: R1–R5 on real lib sentences + the two-seam routing pin.
- `src/components/Panels.tsx` — item 4: the researcher's grading — name line styled by register (absence italic+dim, designation plain), address line demoted under it with the reference-position title.
- `src/manuscript/ManuscriptChrome.tsx` — item 5: NET COMMENT-ONLY — the shelf bound's comment now carries the clip finding, the 148px arithmetic, and the designer routing; the `maxHeight` VALUE is byte-identical to before this cycle (my 252 cut lived under an hour and died by its own arithmetic).
- `scripts/app-leg/deficit_app_driver.py` — the door-locator recut: `open_argument_door` (seam locator + ruled-copy pin + raise-by-name, C-1 item 1 cited) replacing the stale exact-text block at its four sites.
- `scripts/diagnose-deficit-register.cjs` — the §4-FIX-B E3 pin recut from the C-1 item 3 ruling: the old `includes(raw sentence)` can never green again (the filter lawfully strips the module prefix); the pin is now the ROUTING (`value === 'not measured · ' + personReadableRefusal(refusal)`) — the filter's own content stays pinned by its own witness, so reader B never reads reader A's judgement — with the °/flat honesty constraints unchanged. Re-run: the clause GREENS; the leg's one remaining red is its manifest-vs-HEAD in-flight pin.
- `scripts/hooks/pre-commit` + `scripts/hooks/check-coder-inbox.cjs` (NEW) — RIDER R-1, riding beside this commit as chartered. Read-only over mail; refuses-or-passes; letters named; override loud; worktree commits resolve the ONE queue via `--git-common-dir`.
- `package.json` — the `postinstall` wire (`git config core.hooksPath scripts/hooks || exit 0`) — the self-installing cure's standing half; the bootstrap act in THIS checkout was manual and is named above.
- `docs/governance/ENGINE_FREEZE_MANIFEST.txt` — the refusalCopy.ts NOT_FROZEN row (classified at the landing, per the completeness law).
- **W1 courier:** `.handoff/THE_CLAIMS_LEDGER.md` + `.handoff/THE_FRONT_BOARD.md` ride AS FOUND (both tails close on finished sentences; not read for content, not amended).
- NOT staged: `tsconfig*.tsbuildinfo` (build-dirtied, excluded per standing practice); `scripts/app-leg/__pycache__/` (python compile artifact, untracked noise).

## 4 · WHAT I COULD NOT REACH

- A live refusal fixture at the eye for item 3 (leg-verified at the seams instead) — named in ¶6 above.
- The shelf clip's cure — refused by arithmetic, designer-routed (¶3 above).
- The conePairs/coneFormBuilt remainder — pre-existing by control, out of C-1 scope; named, not hunted tonight.
- Standing residues unchanged: winding E.circuit (parked with rungs 3–4), the ambo placeholder cure (Arman's, 2b), the survivor's deeper cut (researcher's).

*Both letters (0723 + 0910) archive with this landing — the 0910 ruling's own instruction. The record pair carries them verbatim.*
