to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-21 1110 +03:30` (machine) · HEAD == origin after the push — the build commit: `6b642bb` (C-6d (γ)); this letter rides the record commit that follows
subject: ✅ **THE PRICE (0033 §3), both shapes, three lines each — item 1; neither built.** ✅ **`C-6d (γ)` LANDED (`6b642bb`) — τ replaces (16 / 37 THE reading, read under the instrument's maps directly); the device's by-name pairs are a PROPOSAL marked as the device's, confirmable; the mold's `member_status` shared by definition under the canonical spelling — T × Φ refused BY NAME; exposure by side; the tie sentence on each group; `read as directed` / `read as symmetric` on the row and the act's line — sighted at my eye.** ⚠ **Two lines up (items 2–3): the mold's key is shared "when both casts declare it" (my reading of "by definition"); the generator had lived untracked in my scratchpad — now tracked beside its fixtures.** ECHO §104 · §105 · the PRICE · STAMP `C-6d (γ)`.

## TO THE MOTHERSHIP

1. **THE PRICE — τ before a take must persist. Priced, not built; whichever Arman sanctions lands after his own acceptance of `(β)`.**
   **(a) the frozen spend — `roles` optional on `EdgeIdentification`** (`geometry.ts:194–199`, manifest row 90, the C-6TYPE seal at `a999c854…`): ONE line (`roles?:`) and the re-seal in ONE commit alone, the positive control first (the old hash must reproduce at the base); absent `roles` = τ given, not yet acted; `[]` = none taken; `types` present from the moment τ is given. Then, in the commit after: the store's `edgeTauDrafts` DISSOLVES (`setEdgeTau` on a fresh edge writes `{ types }`; the take adds `roles`; a withdrawal deletes `roles` and keeps `types`), the panel's `draft` prop goes, every reader of `identification.roles` (the panel · the store's one writer · the register witness §10) gains the absent branch — about 40 lines across three NOT_FROZEN files; the export carries τ for free (the record rides the edge; the import validator only checks that `edges` is an array); the concept-type census's §1 fixture still compiles. **Cost:** the C-6TYPE seal moves a second time; τ has ONE home, and the type says the truth.
   **(b) a persisted draft outside the frozen type** — `PersistedWorkspaceV1` and `WorkspacePersistenceSnapshot` in `src/lib/workspacePersistence.ts` (NOT_FROZEN, row 187) gain `edgeTauDrafts?: Record<EdgeId, Array<[string, string]>>` (optional — an older export lacks it, a true absence); `exportWorkspace` writes the store's drafts, `importWorkspace` reads them back (the validator gains one optional-record check) — about 25 lines across two NOT_FROZEN files and one witness clause. **`snapshot.ts` (FROZEN, sanctioned) is NOT touched:** `serializeSnapshot` serialises LIFTED shapes into the manuscript's lift store, not the workspace export; the undo stack (`WorkspaceSnapshot`) stays without drafts (no history entry for a J act). **Cost:** no frozen file — but τ keeps TWO homes (the draft before a take, the record's `types` after), one input in two places, their exclusivity held by the store's discipline (the take clears the draft; a withdrawal hands τ back), not by the type.
   **Measured, not chosen:** (a) is the smaller union in MEANING (one home, the type true) at one frozen spend; (b) is the smaller union in FILES at the price of a second home. The line is Arman's.
2. **THE MOLD'S KEY, "shared by definition" — my reading, one line:** the register shares `member_status` when BOTH casts declare it on at least one role; a cast that never declares it has nothing to compare (every cell unrecorded on its side), so it is not put in force there — the triangle × triangle-symmetric reading has no `types:` line, as the designer ruled. If "by definition" means in force whenever EITHER declares it (the other side all unrecorded), that is one condition in `sharedSignature` and the `types:` line would then print on every pair where one side carries the key.
3. **THE GENERATOR'S HOME:** `write-cast-fixtures-vii.py` lived in my session scratchpad — untracked, hence not at HEAD (an unprotected file, §6's own law). It is now `scripts/fixtures/casts/write-flow-phi-casts.py`, tracked beside its fixtures, with the canonical key; its INPUT (`hinge_data.py`) stays in the gitignored instruments dir and its head says so — the transcription is reproducible, the record is the researcher's.
4. **τ REPLACES — the measured consequences:** under the instrument's τ maps the register now reads the seal directly (16 · 37; the proposal 9) with no renamed-away copy; `disjoins ↦ disjoins` sits beside τ₃ as PROPOSABLE and the retired 18 is reachable only by the person adding it (pinned). The (vii) τ₃ with the two targets swapped is pinned as an equally valid translation for the size-3 core ((γ) §1.5). Exposure is counted by side, the sum pinned to the seal's exposure; the split printed by the witness: τ₃ top offer here 0 · there 0 (23 unrecorded); the proposal's top here 0 · there 1 (`1 known there only`); the T cell's identity 0 · 0; the fiat J at the eye `1 known here only`.
5. **THE T × Φ CONTROL, by name now:** under `{answers-to ↦ decays}` alone `r1 ↦ Φ2 · r7 ↦ Φ9` is REFUSED — `member_status: r7 has · Φ9 none-by-nature`, empty core (b), 1 refusal — the measured one; the measured zero kept as LAW 24 with its reason (Φ's key spelled as a caster key is not shared; τ on that key refuses again). Flow × Φ: no offer touches Φ9, 0 refusals under every premise.
6. **THE COPY'S WORDS, hers, my wording within her marking — her veto open one cycle:** the proposal header `τ — proposed by the device (same name, same arity)` · `the device's, until you confirm it — the offers below rest on it` · `confirm as yours`; the proposable remainder `proposable by the device (same name, same arity, not in your τ — not applied)` · `add to yours`; the mold line `shared by the mold: member_status`; the group line `relational weight W · N offers · R readings — within a reading they differ only by symmetry (this cast has a, that one b)` for every group with more than one offer, ONE reading included (`so pick any` dropped, as you flagged); `relational weight W · 1 offer` for one; exposure `N known here only` · `N known there only` · `known N here · M there`, absent when none; the reading sub-heading (`reading g · m members`) only on groups with more than one offer.
7. **THE READING CLAUSE — one line:** a relation-type with NOTHING listed states no reading (`none listed`, no clause), and a cast whose arity ≥ 2 types list nothing has no clause on its line — absent, never `read as directed` by default (pinned).

# 1 · What I SAW

## `C-6d (γ)` — headless Chromium, the Ambo, my serve on 5174
- **Flow (A) × Φ (B), no τ given:** the premise `proposed` — the header `τ — proposed by the device (same name, same arity)` · `disjoins ↦ disjoins · presupposes ↦ presupposes` · `the device's, until you confirm it — the offers below rest on it` · `confirm as yours`; the mold line `shared by the mold: member_status`; 9 offers; the group lines `relational weight 2 · 1 offer` and `relational weight 1 · 8 offers · 7 readings — within a reading they differ only by symmetry (this cast has 2, that one 1)`; the reading sub-headings only on the 8-offer group (`reading 1 · 2 members`, then singles); no tie header; the top `F2 ↦ Φ7 (support 1) · F4 ↦ Φ1 (support 2)` with `relational weight 2 · 5 unrecorded · 1 known there only · types: 2 agree · 0 unknown`. The load lines: `taken — 14 roles · 12 relation-types · 48 relations · read as directed · warrant carried, never read` and `taken — 9 roles · 15 relation-types · 31 relations · read as directed · warrant carried, never read`; every ordering row `… — read as directed`.
- **τ₃ given through the selects:** the premise `given`, the proposal REPLACED; the proposable remainder `proposable by the device (same name, same arity, not in your τ — not applied): disjoins ↦ disjoins` with `add to yours`; the mark `τ given — the offers below were re-derived for the τ now in force`; **16 offers in 14 readings** — `relational weight 4 · 1 offer` and `relational weight 2 · 15 offers · 13 readings — within a reading they differ only by symmetry (this cast has 2, that one 1)`; the top `F5 ↦ Φ7 (support 1) · F7 ↦ Φ1 (support 3) · F8 ↦ Φ2 (support 2)` with `relational weight 4 · 23 unrecorded · types: 3 agree · 0 unknown` (nothing exposed under τ₃ alone — the (β) `1 known on one side only` was a `disjoins` tuple riding the retired reading).
- **`take` on it:** `J given`, the pairs `F5 ↦ Φ7 · support 1` · `F7 ↦ Φ1 · support 3` · `F8 ↦ Φ2 · support 2`, the counts `relational weight 4 · 23 unrecorded · types: 3 agree · 0 unknown`; the offer wears `given — this one`.
- **The fiat pair (F1 ↦ Φ9):** `F1 ↦ Φ9 · support 0 — yours, backed by no offer under this τ` with `in conflict: member_status: F1 has · Φ9 none-by-nature` under it — the mold's key, by name; the counts `relational weight 4 · 43 unrecorded · 1 known here only · types: 3 agree · 0 unknown` (no count of conflicts). **Withdrawn:** `offered`, τ kept.
- **The proposable pair added by the person (`add to yours`):** τ₃ + `disjoins ↦ disjoins` — 18 offers in 16 readings, the top now `relational weight 4 · 31 unrecorded · 1 known there only · types: 3 agree · 0 unknown` — the retired reading, reachable only as the person's act. **All τ withdrawn:** the proposal returns with `τ withdrawn — the offers below were re-derived for the τ now in force`, 9 offers. **`confirm as yours`:** the premise `given` with τ = `disjoins ↦ disjoins · presupposes ↦ presupposes`, 9 offers.
- **T (A) × Φ (B):** before any τ `empty core (a)` — `no relation-type in common — a translation is yours to give`, `no translation given — none proposed (no caster type shares a name and arity)`, the mold line present; τ `answers-to ↦ decays` given → `empty core (b)`: `no relation to agree on · 1 matching refused by a type on the roles` — the measured one, by the mold's name, no τ on the key.
- **T cell × T cell:** the proposal of its six signature types; 10 offers; the group lines `relational weight 11 · 1 offer` · `8 · 1 offer` · `4 · 1 offer` · `relational weight 3 · 4 offers · 4 readings — within a reading they differ only by symmetry (this cast has 1, that one 1)` · `relational weight 2 · 3 offers · 3 readings — …`; the identity with `r0 5 · r1 5 · r2 3 · r3 1 · r4 1 · r5 1 · r6 2 · r7 1 · r8 3 · r9 1` and `relational weight 11 · 1489 unrecorded · types: 10 agree · 0 unknown`. The load line `taken — 10 roles · 7 relation-types · 21 relations · read as directed · warrant carried, never read`; the card `removes · 1 tuple · none reversed — read as directed`.
- **Arman's triangle × the symmetric triangle:** the load lines `taken — 3 roles · 1 relation-type · 3 relations · read as directed` and `taken — 3 roles · 1 relation-type · 6 relations · read as symmetric`; the cards `r · 3 tuples · none reversed — read as directed` and `r · 6 tuples · every one reversed — read as symmetric`; the register: the proposal `r ↦ r`; NO mold line and NO `types:` (no unary type anywhere); `relational weight 3 · 6 offers · 1 reading — within a reading they differ only by symmetry (this cast has 3, that one 6)`; the first offer's counts `relational weight 3 · 3 unrecorded · 3 known there only`.
- **triangle × T cell:** `empty core (a)`, the sentence, the type selects beside it, `no translation given — none proposed …`.
- **Both panels stand** (the composition and the Layer 3 Witness sections present). `not computed` not sighted on a real pair.

# 2 · What I RAN (verbatim)
- **Sim-diff:** empty after the commit.
- **Freeze manifest:** nothing frozen touched; no new file under the engine roots (the generator lives under `scripts/fixtures/casts/`).
- **`node scripts/diagnose-the-j-register.cjs`** (53 clauses): `DIAGNOSE-THE-J-REGISTER: ALL PASS — the second implementation reads the seal's numbers under τ as the person's, the mold's types by definition, the offers follow relational support, the unary home refuses and is counted apart, and the surface renders the proposal, the group lines, the counts by side, the take and the fiat pair by name` (66 s standalone; the head declares 900 s, left standing). **`node scripts/diagnose-the-cast-loader.cjs`** (46): ALL PASS. **`node scripts/diagnose-the-concept-type.cjs`**: ALL PASS. **`node scripts/diagnose-the-ambo-gesture-truth.cjs`** (17): ALL PASS.
- **`npx tsc -b`**: exit 0.
- **`npm run sweep`** at `6b642bb`, whole, read by eye in its own call: the header `THE SWEEP — 135 legs · 23 workers · per-leg ceiling 420s`; the one non-PASS row `FAIL    scripts/diagnose-dual-inspection.cjs  13.3s`; `PASS    scripts/diagnose-the-j-register.cjs  62.7s` (inside its declared 900 s), `PASS    scripts/diagnose-the-cast-loader.cjs  1.1s`, `PASS    scripts/diagnose-the-concept-type.cjs  4.3s`, `PASS    scripts/diagnose-the-ambo-gesture-truth.cjs  1.3s`; wall 90.6 s; the canonical lines `135 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else`; sweep exit 0.
- **The drive family:** not triggered — the reading is the inspector, not the walk; said.
- **`.handoff/inbox/coder/` re-listed before each commit:** two letters, `--mark-reviewed --expect 2` in its own call each time; archived by name with this landing.

# 3 · What I CHANGED
- **`6b642bb` — (γ).** `src/lib/castLoader.ts`: `MOLD_TYPES` (one constant) · `isMoldType` · the ordering's `reading` · `orderingLine` with the reading · `castReading` · the summary line's clause. `src/lib/jRegister.ts`: the RULED/GIVEN shared signature, the proposal, `proposable`, exposure by side, the retired reading gone. `src/components/JRegisterPanel.tsx`: the proposal header with its confirm act, the proposable remainder, the mold line, the group lines, the counts' words, the absent `types:` line. `scripts/fixtures/casts/flow.cast.json` · `phi.cast.json` regenerated; `scripts/fixtures/casts/write-flow-phi-casts.py` (tracked). The register witness rewritten whole; the loader witness +3.
- **The record commit:** `.handoff/THE_BUILD.md` (the two letters verbatim), `.handoff/THE_BUILD_REPORT.md` (this letter), the W1 riders AS FOUND — `.handoff/PLAN_THE_CONNECTION_LAYER.md` · `.handoff/THE_CLAIMS_LEDGER.md` · `.handoff/THE_FRONT_BOARD.md` (tails close on finished sentences; not read for content).

# 4 · What I could NOT reach — named
- **`not computed` on a real pair at the eye** — as before, rendered under the witness's small budget (no fixture pair is beyond 200,000 nodes).
- Nothing else of the two letters; the PRICE is item 1.
