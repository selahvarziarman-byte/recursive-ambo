to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-24 19:21 +0330` (machine — `date`) · HEAD == origin == `a71f4b2` (the STAMP) · the record commit follows this letter
re: **`STAMP C-11b` LANDED — THE CARGO ON THE WALK (§135): a role at a corner, the fourth field of the walk's seam; a door crossing applies C-11a's transport at every corner of the crossed face (a cargo off that face stays behind); inside the cell only the person moves it, one rod per press, by the rod's J on the carried record (a break withdrawable by its one hand); one reading line beside trace · tally · sentence — home Fix · Mov · Und with the reduced route, a spur told from a residue, away identity and presence by the route; the researcher's K1–K7 reproduced number for number; no frozen file. ECHO §135. The last build of the race — Arman's STEP-3 walk follows.**

## What I SAW — the drive family, `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS` (117 clauses — 107 → 117, five new arms per viewport; `/__whereami` = `a2d44e1`, dirty — the C-11b tree; the leg ran four times on this stamp — the first found the cargo arm returning early on a missed summon with the page left on the Manuscript (the next arm found no workspace tab; the arm now always returns to the Ambo and reports what it saw), the second took the room at 1689 × 897 on a vertex point but missed it at 1400 × 900 (the first mesh's projected sphere centre lands on empty space inside the room's outline — a real double-click there selects nothing), the third missed at both widths with triangle centroids (two candidates, neither on a raycastable surface), the fourth whole on both viewports with points on the segment from each mesh's projected centre toward its projected vertices — on a rod they lie on the rod (the 12th candidate took the room at 1689 × 897, the 13th at 1400 × 900))

At 1689 × 897 (and at 1400 × 900): the room built by C-11a's arm from the lifted gen-1 residue (the hinge door carrying F1's whole line) summoned on the sheet by a double-click, `explore inside` opening the walk window; **the cargo line stands beside trace · tally · sentence in the PICK state — `carry from the corner A:` with A's 14 roles as buttons** (the legend gains `carry it along — move the cargo corner to corner, one rod per press`); **F1 picked → `carrying F1 — at the corner A · carry it along: A–AC · A–AB · A–AD`** (the rods by their two corners, nothing lit); **the rod A–AC pressed → `carrying F1 — at the corner AC, came along A–AC as r2 ≡ F1`** (the class this run's own pair on C–A names — at the eye C holds the T cell); **the door `a` pressed — one period, the trace `a`, doors 1 — the transport at AC carries the cargo across: `carrying F1 — at the corner AD as F1, by A–AC · a`**; **the rod AD–A pressed → `carrying F1 — returned to itself by A–AC · a · AD–A`** (home, Fix, the reduced route printed). The window closed and reopened (a room opened is a walk begun — nothing carried), **F2 picked, carried along A–AC, the door pressed → `carrying F2 — not here by A–AC · a: lost at the door a, which does not carry it`** and the picks return (`carry again from the corner A:`). Plates `concept-layer-cargo-{pick,rod,door,home,lost}-1689x897.png` in the ignored `_frames/`. At 1400 × 900 the same words, the line one row of 548 × 19 px beside the sentence at both widths.

## What I RAN

- **`scripts/diagnose-the-cargo.cjs` (NEW) — ALL PASS, 25 clauses.** §1 THE FIXTURES — the reference's K1–K7 NUMBER FOR NUMBER on the reference's rooms (the prism of two first faces, bottom X and top Y over the 42 hand triples, the vertical rods the lifted copies, ONE door top → bottom with the largest lawful transport built line by line — the person's stand-in, one lawful choice among many): K1 the reverse walk is the inverse map on four paths, every role both ways — 0 failures; K2 the door loop at F brings a cargo home MOVED in 1,694 of 1,764 rooms (e.g. X = A+P+(i), Y = A+P+(ii): F10 ↦ F6 · F11 ↦ F9 · F14 ↦ F10 — the reference's sample); K3 presence route-dependent in 1,764 of 1,764 (e.g. F10 · F11 · F14 · F2); K4 identity route-dependent in 803 (e.g. F12 as F10 by the door loop, as F13 by the side loop); K5 a spur moves nothing (0 failures) and loses a cargo the door loop delivers in 1,764 rooms; K6 the room's T³ (X = Y, the door the identity, 42 rooms) 0 moved · 0 lost; K7 total and flat (2,000 rooms of random bijections — a seeded JS generator, the Python seed not reproducible; the pins are 0 · 0 · 0) 0 · 0 · 0; the door empty at F in 0 rooms (0.5 s); the reading on a K2 room in the designer's words (`carrying F10 — returned as F6 by F–F′ · a` · `at the corner F′, came along F–F′ as F10` · `returned to itself — the route reduces to nothing`). §2 THE LAWFUL PATH — the residue at A lifted from gen 1, the hinge door with C-11a's act (F1's whole line), the room built and its cell surface read: the four faces carry their corners and the six rods their ends; the entry corner A; the door A · AC · AB → A · AD · AB with one pair at each corner; the crossed face the door's side a; THE PICK (`carry from the corner A:`, 14 roles, F1 first); CARRYING (`carrying F1 — at the corner A`, the rods A–AC · A–AB · A–AD); THE PRESS along A–AC (`came along A–AC as Φ1 ≡ F1` — the corner edge's carried coprojection); THE CROSSING at AC → AD (`at the corner AD as F1, by A–AC · a`); HOME (`returned to itself by A–AC · a · AD–A`); F2 the same route LOST AT THE DOOR (`carrying F2 — not here by A–AC · a: lost at the door a, which does not carry it`, the picks again); the walker's word going on after a loss — crossing back cancels the letter: with the rod step standing `did not return: lost around A–AC, at the door a …` (home by the door count), lost at the first crossing from A and the walker back, `a spur — the route reduces to nothing; lost at the door a, which does not carry it`; LEFT BEHIND (`carrying nothing — F1 stayed at AD, in the cell you left: the face you left by, A·AC·AB, does not hold AD`); BROKE AT THE ROD, withdrawable (a role of AC's own along AC–A: `carrying nothing — Φ2 broke at the rod AC–A: its J does not carry Φ2` · `here, on AC–A: withdraw this step` — the hand restores the cargo to AC and strikes the step); a rod the J carries moves the cargo by EXACTLY that J (all 14 of A's roles along A–AC and back); the free reduction. §3 THE SURFACE — the window's fourth seam field, the crossing hook at exactly the two crossing sites, the reset on open, the line mounted once between the sentence and the return line, the rods · picks · hand as buttons, the legend's clause; the view hands the walk its room from the built record and the carried record; the cell surface's additive corners and ends; purity (the model's seven from-clauses, no react, no store); the manifest row; neither `.cast` nor `ConceptSpace` named.
- **the doors act 34 · the lift carries 31 · the aperture · the concept type 11 ALL PASS · the freeze exit 0** (the new src file intent-staged and classified NOT_FROZEN in the STAMP).
- **`tsc` exit 0** at the STAMP.
- **THE DRIVE FAMILY** — above. The served tree fingerprinted before anything was believed (`/__whereami` = the commit the leg was started from, 13 dirty paths — this build's). the leg ran four times on this stamp — the first found the cargo arm returning early on a missed summon with the page left on the Manuscript (the next arm found no workspace tab; the arm now always returns to the Ambo and reports what it saw), the second took the room at 1689 × 897 on a vertex point but missed it at 1400 × 900 (the first mesh's projected sphere centre lands on empty space inside the room's outline — a real double-click there selects nothing), the third missed at both widths with triangle centroids (two candidates, neither on a raycastable surface), the fourth whole on both viewports with points on the segment from each mesh's projected centre toward its projected vertices — on a rod they lie on the rod (the 12th candidate took the room at 1689 × 897, the 13th at 1400 × 900).
- **The sweep at `a71f4b2` (the STAMP), whole, read by eye in its own call before this letter was written: `143 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else`** (exit 0 — the census 143, the cargo witness in the sweep by its own declaration at 1.9 s, the doors act 4.8 s, the lift carries 2.5 s, the concept type 12.3 s, the aperture 13.5 s; the gate 84 s, the door column 82 s, the j-register PASS at 70 s inside its declared ceiling, the born room 52 s; wall 96.8 s). tsc 0.

## What I CHANGED (no frozen file)

1. **`src/manuscript/cargoModel.ts`** (NEW, NOT_FROZEN, react-free) — `cargoRoomOf(seed, carried, rows, cellSurface)`: the room as the walk reads it — the corners, the entry corner (the first in D14 order), the roles at each corner through the one resolver on the carried record (the C-10 reader finds the record), each rod's J by its kind (`bornStepOf`), the faces by the surface's corners with their D14 names, the doors by the rows' correspondences and C-11a's transports (e_c per corner index), the glued classes; `cargoRoomFrom` (the witness's fixture prism); the acts — `pickCargo` · `stepRod` (one rod, by its J, or `broke`) · `withdrawStep` (the one hand) · `crossDoor` (the transport at the crossed face's corners, side b the inverse; a cargo off the face stays behind; after a loss the walker's word goes on); the route — `reduceRoute` (the free reduction), `routeWords`, `doorNetZero`; `cargoReading` — the designer's words per state (pick · carrying · home-fix · home-mov · away · lost-door home/away · lost-spur · lost-rod · stayed).
2. **`src/manuscript/apertureModel.ts`** (203 NOT_FROZEN) — `ApertureCellFace.corners?` and `ApertureCellRod.ends?` (additive), set on the euclidean and the sealed-model reads of `readCellSurface`; the developed cone surface names none.
3. **`src/manuscript/ExploreWindow.tsx`** (189 NOT_FROZEN) — the prop `cargoRoom`; the seam's fourth field `cargo` (on `window.__exploreWindow` for the drivers); React state mirrored by one writer (`cargoSet`); the reset on open; `cargoCross(exited, face.door)` at BOTH crossing sites beside the letter; the line `data-explore-cargo` (state · route attributes; the words; the rods, picks and hand as buttons) after the sentence, before the return line; the legend's clause.
4. **`src/manuscript/ManuscriptView.tsx`** (196 NOT_FROZEN) — `builtRecords` read; `exploreRoom` gains `cargoRoom` from the built record (`seed` + `rows`) and the carried record by the seed's shape id; the prop passed.
5. **`docs/governance/ENGINE_FREEZE_MANIFEST.txt`** — one NOT_FROZEN row.
6. **`scripts/diagnose-the-cargo.cjs`** (NEW) — above.
7. **`scripts/app-leg/concept_layer_eye_driver.py` · `diagnose-the-concept-layer-eye.cjs`** — the cargo arm and its five clauses per viewport.

## PARKED (one line each — Arman's Δ92: nothing more than the acceptance)

- The entry corner is the cell's first corner in D14 order; the model picks anywhere, the window picks there (the designer's "picked at the walk's entrance").
- HOME for a carried cargo is the walk closed at the starting VERTEX; a cargo standing on that corner's paired copy across a door reads as away, by its route.
- HOME or AWAY for a LOST cargo is the walk's word (every door's net count zero — the trace's own `cancelled` predicate), never the position-return machinery.
- A multi-cell room carries no cargo (its developed cone surface names no corners); a sealed-model room (S³ · H³) carries corners — written, not driven.
- After a loss the rods are gone (nothing to carry); the picks return at the entry corner.
- No word (τ) travels with the cargo — a role is the cargo (§137 item 4).
- The K7 control runs on a seeded JS generator (the reference's Python seed is not reproducible here); its pins are 0 · 0 · 0.
- The explore room's guarded read returns null SILENTLY on a throw (the view's `resolve`, B-101's class — an unexplained empty); a dev-register warning there was cut back out under Δ92 — one line here, not a side cut.
- The summon of a room at the eye: the first mesh's projected sphere centre lands on empty space inside the room's outline (a real double-click there selects nothing); the driver now tries the meshes' projected vertices in order (the fifth took the room) — a driver's fact, not the person's.

## W1 · U1 — carried at the record commit (hashes with their instruments; none read for content; no re-seal)

**W1 riders AS FOUND** (hashes with their instruments; none read for content; no re-seal): the claims ledger `blob 77a8681d` → `90dd6acb` (`sha256-LF 5bd7824c` → `91b36788`, 16/0, mtime 19:20:37 — §137 and after, by its clock; the tail closes on a finished sentence) · the sovereign ledger `blob f6072bed` → `c7fd299a` (`sha256-LF a27cd6ce` → `272ffa22`, 5/0, mtime 17:56:30 — Δ92 by its clock; the tail closes on a finished quote). Unchanged since the last record: the front board (`blob 5d859796`), the connection-layer plan (`49dbf871`), ADR 0030 (`66cb9c3f`), ADR 0031 (`dde61338` — carried at a2d44e1), the designer-seat handoff (`3dd84885`), the J-registers ruling (`06671edf` — carried at a2d44e1). **U1** (§126.2, every wave at each record commit): the sweep run from its home at `a71f4b2` finds NO new wave — 0 cited rulings, 0 cited instruments; `.gitignore` regenerated whole and unchanged; the ambiguous heads kept both (already tracked); nothing tracked in this record commit.
