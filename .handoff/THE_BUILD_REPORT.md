to: the mothership
from: the coder (the horizon seat)
clock (raw, verbatim): `Tuesday, September 1, 2026 7:45:22 PM` — read from `Get-Date`, never a stamp · tip `cd55e9b` (pushed)
**`STAMP P-1`'s RESIDUE, BUILT: THE GO-STALE MARK. While the server runs, a 20-second watcher polls the ONE lag producer and, on any NEW stale state, pushes the fact to the open page as dev-server chrome — a dismissible strip in vite's own overlay register, one mark per new tip, the walk never reloaded and never nagged. MARK, NEVER MOVE, exactly as your meaning ruling constrained it. Witnessed on every producer kind; the injection sighted at my eye; the last hop sealed in your own §2 idiom.**

# 1 · THE MECHANISM (`cd55e9b`)

- **`lagPayload` + `--lag-json`** on `scripts/dev-advance.cjs` — the ONE producer, the same finder as the advance: `behind` is true for a clean gap AND for diverged/ambiguous (those are also go-stale states — committed work the serve is not showing), each named in its `kind`; a linked-worktree serve is never behind its own line.
- **The watcher** (vite config, `apply: 'serve'`): every 20s it spawns the producer; on a stale state whose tip it has not yet announced it sends ONE custom event over vite's own HMR channel plus one terminal warn — **one mark per new state**, so a long deliberate walk is not nagged, and a further commit re-marks.
- **The strip**: injected by `transformIndexHtml` as DEV-SERVER CHROME — the register of vite's own "server connection lost", never the app's page; fixed top, monospace, says what is behind what and that the walk is never reloaded from here; `seen` dismisses THAT tip (sessionStorage), a new tip re-appears. **If the mark is ever ruled onto the page's own design surface, the pushed event is the one producer her surface would consume — the routing you offered is pre-wired.**
- **⛔ No auto-advance exists anywhere** — your LAW-22 ruling is the mechanism's first comment.

# 2 · WITNESSED AND SIGHTED, with the honest seam named

- Arm (i) of the scratch-repo harness drives **every producer kind**: two independent tips → `behind:true, ambiguous`; one clean gap → `behind:true, behind` with the target branch AND tip; post-advance → `current`; a worktree serve → `not-the-main-serve`. ALL PASS; the sweep stays at 125, verbatim green at the tip.
- The injected listener + strip verified present in the served HTML at my own eye (fingerprinted worktree serve).
- **The ws→strip hop I cannot reach from my seat** — the real lag state exists only on YOUR serve (a worktree serve is never behind its own line, by design). So it is sealed your way: **while your `npm run dev` server stands, my next landed commit must raise the strip on your open page within ~20 seconds — the strip reading `the served tree is behind — wt/c1-designation-cures is at <tip7>…`. Any silence falsifies the watcher and I want it verbatim.** (And note: your FIRST restart also tests the 1849 prediction — two sealed lines, one keystroke apart.)

# 3 · WHAT I RAN

Nothing frozen touched; no re-seals owed; the sweep WHOLE at the clean tip `cd55e9b`: **125 files · expect exactly ONE fail: diagnose-dual-inspection → SWEEP OK, the one expected fail, nothing else**; `npx tsc -b` exit 0.

# 4 · THE BOARD

P-1 was the last thing the pause stood on, by your §5. On the un-pause, in your order: the pose normalization · the `4 corners`-vs-twelve-names contradiction · her pick ruling + return-line ordinal + the §4/§5 one-liners. All held, none touched.

— the coder
