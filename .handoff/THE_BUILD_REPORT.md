to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-19 1817 +03:30` (machine) · HEAD == origin after the push — the build commits, in the order stamped: `e8a891e` (C-6e) · `e045451` (C-6c (vii)); this letter rides the record commit that follows
subject: ✅ **`C-6e` LANDED (`e8a891e`) — the Layer-3 panel prints each site's seam fact once, labelled, in words; the card's `Marks` row carries the device's own not-taken count from the warrant's KEYS — at my eye corner D reads `Marks — 1 item not taken: relation 3`.** ✅ **`C-6c (vii)` LANDED (`e045451`) — Flow and Φ from the record; the wild clause READ against the researcher's 09-08 run on the relations-only reading under their own τ (weight-4 offers of size 3 and of size 4 side by side; the size-5 candidate not an offer).** ⚠ **A DIVERGENCE, REPORTED NOT TUNED (item 1): under the ruling's both-homes record the shared `member-status` makes every mapped pair an agreement at Flow × Φ — the tie breaks, the 09-08 riders become supported, and the offers search is beyond the budget at that pair. The definition of the shared unary home there is the researcher's to rule.** ECHO §100 · STAMP `C-6e` · STAMP `C-6c (vii)`.

## TO THE MOTHERSHIP

1. **THE DIVERGENCE AT FLOW × Φ — the definition reopens, not the code.** The 09-08 instrument (`edge_candidates_partial_isomorphism.py`, RUN by me at `e8a891e`: size 3 weight 4 `{F5↦Φ7 · F7↦Φ1 · F8↦Φ2}` under τ `{sustains↦descends-from · presupposes↦lodges-in · exceeds-in-size↦specifies}`; size 4 weight 4 `{F1↦Φ3 · F2↦Φ2 · F3↦Φ4 · F5↦Φ1}` under τ `{exceeds-in-speed↦transmits · generates↦lodges-in · precedes↦component-of · sustains↦disjoins}`; size 5 SAMPLED, weight 4) reads RELATIONS ONLY and SEARCHES τ. The ruling's record (§99 §1) reads BOTH homes, and the fixtures carry `member-status` on every role of both casts as the stamp ordered. So: on the relations-only reading under THEIR τ my register reads their numbers exactly (§1 below); on the ruling's reading every mapped pair of two `has`-roles is a `member-status` agreement — weight grows with size, the size-3/size-4 tie breaks, the 09-08 riders `F3↦Φ5 · F6↦Φ3` become supported (so the size-5 candidate is no longer cut to its size-3 core, and the offers grow toward the size-maximal — NOT measured: the search is beyond the budget), and the tuple-driven offers search is `beyond the budget` at this pair (the bound cannot prune when every pair may agree). **Three lines for the researcher:** (a) is `member-status` a shared unary type at Flow × Φ, or is a categorical key that every role carries with one value no content (it individuates nothing — every injection agrees on it)? (b) if it counts, the offers at Flow × Φ are the size-maximal ones and the 09-08 wild clause is not the register's reading; (c) if it does not, the register needs a rule for which unary keys count — the T cell's `member_status` counted in the seal's 21 (10 of them), so the rule cuts both ways. I built neither rule; the witness pins the relations-only reading to theirs and PRINTS the both-homes reading beside it.
2. **THE BUDGET AT THE REAL PAIR, measured:** relations-only under the size-3 τ: 13,225 ms · 4,000,088 nodes in all (the offers inside the first 1,000,000; the count's own 4,000,000 spent and exceeded); under the size-4 τ: 27,204 ms · 4,001,248 nodes. Both homes under either τ, and by name alone: `beyond the budget` at 1,000,000 nodes. The full-injection count at 14 × 9 is P(14,9) = 726,485,760 injections — beyond any budget the register should spend at pick-time (the researcher's own instrument sampled size 5); it says so as `null`, never a number. The offers are found inside a 1,000,000-node budget on the relations-only reading.
3. **THE OFFER COUNT, printed not pinned** — the witness's own lines: 

   ```
   triangle × triangle-symmetric: 6 distinct offers · distribution 3×6 · weight 3: 6 offers · 1 orbit(s)
   triangle × triangle: 3 distinct offers · distribution 3×3 · weight 3: 3 offers · 1 orbit(s)
   t-cell × t-cell: beyond the budget
   triangle × t-cell: empty core (a)
   Flow × Φ (relations only, size-3 τ): 18 distinct offers · distribution 4×1 · 2×17 · weight 4: 1 offers · 1 orbit(s) · weight 2: 17 offers · 15 orbit(s)
   Flow × Φ (relations only, size-4 τ): 55 distinct offers · distribution 4×6 · 3×16 · 2×26 · 1×7 · weight 4: 6 offers · 6 orbit(s) · weight 3: 16 offers · 16 orbit(s)
   Flow × Φ (both homes, size-3 τ): beyond the budget
   (a distribution that reads `beyond the budget` is the keep-all search past 1,000,000 nodes — the count is not known, and is not guessed)
   ```

   The T cell's top-weight count is §3's `1 tied` (the offers search at 5,000,000 nodes); its whole keep-all distribution is beyond the 1,000,000-node keep-all budget and says so — not known, not guessed. Asserted the hour the researcher's number lands.
4. **THE LAYER-3 WORDS** are the designer's from her §0 — `on seam (bd–cd)` / `not on seam` — her veto open one cycle. No witness pinned the old copy (grepped again: the four `scripts/` files matching `seam` say it in other senses — the conformal atom's invocation seam, the octahedron's seam graph, the deficit driver's projection seam); the Ambo witness pins the cure now.
5. **THE NOT-TAKEN CLAUSE reads keys only:** `notTakenAddresses` maps `warrant.malformed`'s keys (`relations.3` → `relation 3`) and never a value; the witness pins the source (`Object.keys(malformed)` and no `malformed[…]` read). The two kinds never merge: the closure-broken file's row reads its two marks and no not-taken clause.
6. **Flow and Φ carry NO subject:** `hinge_data.py` has no docstring; its two provenance comments ("Φ, exactly as cast (role grain; duplicate role-level edges collapse)"; "Flow, in-universe as cast; the two structurally inconsistent self-loops removed") are carried unread as `source` on the warrant — they say where the record came from, not what the concept is OF, and the stamp said the instrument's words or none.

# 1 · What I SAW

## `C-6e` — headless Chromium, the Ambo
- **Corner D** (the malformed-relation file): the load line `… · 1 mark: relation 3: has no type or no terms — not taken`; the card's rows `summary · roles · orderings` and **`Marks — 1 item not taken: relation 3`**. **Corner A** (the T cell) and **corner B** (the triangle): no `Marks` row.
- **The Layer-3 panel** (source, pinned): one `{seamLabel}` in the site block, under `on seam?`; the value `on seam (…)` / `not on seam`; no dash value.

## `C-6c (vii)` — the register's own numbers on Flow × Φ (the witness RUN)
- **Relations only, the size-3 τ:** top weight 4; the weight-4 offers include `F5↦Φ7 · F7↦Φ1 · F8↦Φ2`; no offer of size 5. **The size-4 τ:** top weight 4; the weight-4 offers include `F1↦Φ3 · F2↦Φ2 · F3↦Φ4 · F5↦Φ1`. **The 09-08 size-5 candidate** under the size-3 τ: conflict-free at weight 4, `F3↦Φ5` and `F6↦Φ3` at support 0 — its supported core is the size-3 offer.
- **Both homes** (the ruling's record) under either τ, and by name alone: `beyond the budget` (item 1).
- **The witness's own §7 lines, verbatim:**

  ```
  BOTH HOMES (the ruling's record: member-status on the roles of both) under the size-3 τ: beyond the budget
  BOTH HOMES under the size-4 τ: beyond the budget
  BY NAME ONLY (no τ — presupposes and disjoins are shared by name, plus member-status): beyond the budget
  ⚠ THE DIVERGENCE, REPORTED NOT TUNED: the 09-08 instrument reads relations only; under the ruling's both-homes record every mapped pair of two has-roles is a member-status agreement, so weight grows with size and the size-3/size-4 tie of the relations-only reading breaks — the definition of the shared unary home at Flow × Φ is the researcher's to rule.
  Flow × Φ (relations only, size-3 τ) took 13225 ms · 4000088 nodes in all · consistent full injections null; under the size-4 τ 27204 ms · 4001248 nodes · null full; both homes by name beyond the budget
  ```

# 2 · What I RAN (verbatim)
- **Sim-diff:** empty after each commit.
- **Freeze manifest:** nothing frozen touched; no new file under the engine roots (the fixtures and the generator live under `scripts/`).
- **`node scripts/diagnose-the-cast-loader.cjs`** (42 clauses): ALL PASS. **`node scripts/diagnose-the-ambo-gesture-truth.cjs`** (17): ALL PASS. **`node scripts/diagnose-the-j-register.cjs`** (20 clauses): `DIAGNOSE-THE-J-REGISTER: ALL PASS — the second implementation reads the seal's numbers, the offers follow support, the empty core is named both ways, and nothing person-facing exists yet` (t-cell × t-cell 47,252 ms; the whole witness 242 s standalone against the sweep's 420 s per-leg ceiling).
- **`npx tsc -b`**: exit 0 at both commits.
- **`npm run sweep`** at `e045451`, whole, read by eye in its own call: the header `THE SWEEP — 135 legs · 23 workers · per-leg ceiling 420s`; the one non-PASS row `FAIL    scripts/diagnose-dual-inspection.cjs  13.1s`; `PASS    scripts/diagnose-the-j-register.cjs  220.7s` (inside its own declared 900 s ceiling); wall 222.1 s; the canonical lines `135 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else`; sweep exit 0.
- **The drive family:** not triggered (a card row, a panel's copy, fixtures and a pure module) — said.
- **`.handoff/inbox/coder/` re-listed before each commit:** one letter, `--mark-reviewed --expect 1` in its own call; archived by name with this landing.

# 3 · What I CHANGED
- **`e8a891e` — C-6e.** `Layer3WitnessPanel.tsx`: the unlabelled second printing removed; the value in words. `castLoader.ts`: `notTakenAddresses` · `notTakenLine` (keys only). `Panels.tsx`: the `Marks` row's second clause, the row when either kind is present. The loader witness +3, the Ambo witness +2.
- **`e045451` — C-6c (vii).** `scripts/fixtures/casts/flow.cast.json` · `phi.cast.json` (from `hinge_data.py` by `write-cast-fixtures-vii.py`). `jRegister.ts`: the `keepAll` option (every maximal supported core, for the printed distribution) and `gaugeOrbits` exported. The j-register witness §7 (the wild clause against the 09-08 run, the divergence printed, the budget at the real pair) and §8 (the offer count printed); the witness declares `SWEEP-CEILING: 900s` in its head on its measured 242 s standalone runtime against the sweep's 420 s default under 23 workers — the neighbourhood that killed the field leg under co-load (I-1 clause 3's mechanism, the number measured, never guessed).
- **The record commit:** `.handoff/THE_BUILD.md` (the letter verbatim), `.handoff/THE_BUILD_REPORT.md` (this letter), the W1 riders AS FOUND — `.handoff/PLAN_THE_CONNECTION_LAYER.md` · `.handoff/THE_CLAIMS_LEDGER.md` · `.handoff/THE_FRONT_BOARD.md` (tails close on finished sentences; not read for content).

# 4 · What I could NOT reach — named
- **Flow × Φ under the ruling's both-homes record** — beyond the register's budget at that pair, and its reading diverges from the 09-08 clause (item 1). Not tuned; the definition is with the researcher.
- Nothing else of the letter.
