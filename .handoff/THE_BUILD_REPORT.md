to: Mothership
from: Coder
date: 2026-09-25 21:10 +03:30
ECHO: **F3** (Δ113, persistence only — the word pairs given alone survive saving; §190) · **USE-1** (the use server's scripts tracked as they stand) · **USE-2** (the page says which version it runs — MEASURE FIRST; landed as the label only, under Arman's ruling of 20:54) · **C-14h RATIFIED §189** (the letter of 19:41, consumed into the record with this landing)
base: `2027c6d` → F3 `cc41ec4` → USE-1 `4d8fead` → USE-2 `aa5fd56` · the record: the commit carrying this letter (its SHA in the ring)

## TO THE MOTHERSHIP

1. **USE-2, step 0, measured** (a scratch port 5211 from my checkout; :5180 untouched). A VISIBLE page is reloaded by vite's own client within a second of its server coming back — in Arman's Chrome the tab reloaded at 17:15:03.9Z against the server's first `/__whereami` answer at 17:15:04.1Z (the ping succeeded before curl did); the same in the in-app pane. The workspace store is not persisted, so that reload wipes unsaved work. A HIDDEN page does NOT reload while hidden: its client logs `[vite] server connection lost. Polling for restart...` and waits for the window to be shown (`client.mjs` `waitForWindowShow`, as you read it) — in the pane three hidden tabs were still their own load 66 s after the server returned; in Arman's Chrome two hidden tabs still were 45 s after. When a hidden page is next shown, its client reloads it — measured through a stub of the show event in the pane (0.3 s), and seen once by hand in Chrome; the extension cannot bring a tab to the front, so I could not drive the real show. Your 19:30 observation is case 2: the pane was not on screen, so every tab in it was hidden, and hidden tabs keep their version until shown.
2. **Arman's ruling, 20:54, verbatim:** *"this is a problem can be worked around cheaply and will never come back again after the build is finished, it is implementation scaffolding and it can be addressed much cheaper.. i will halt use when you are working. simple as that. commit the already built solution only if it does not become redundant upon my workaround."* So your item 2 (the moved-on line; asked on visibility and every minute; gated by vite's own `vite:ws:disconnect` so that a landing on the bench, whose edits HMR already carried, never said *reload*) was built, witnessed, driven, and CUT before landing, with the scaffolding that measured it; item 3's price (turning vite's reload off for :5180) is not asked — his workaround replaces it. Your rule stands: the customer exports when a release is announced.
3. **What landed is item 1 alone:** `this page: 4d8fead` under the Save / Load heading, asked of `/__whereami` once at the page's load; nothing where the server does not answer (no endpoint, the error body, non-JSON, a network failure) — a true absence. It is not redundant under the workaround: a reader of the PAGE learns what the page runs, and a label that ever differs from `/__whereami` is the falsifier of "no page is in use across a release". **Virgin Land should take its version from that line, not from `/__whereami`** — your own finding, now a mark on the page.
4. **Instruments, for the record:** Playwright's Chromium reports every tab `visible` (even one created `background: true`); the in-app pane, when not displayed, reports every tab `hidden`; Arman's own Chrome through the extension reads honestly and its tools do not change a tab's visibility — but cannot show one. A scratch Chrome launched by a leg put a window on his screen; he called it off, and it is gone.
5. **F3's lift, measured and parked as you said:** a word pair alone does NOT cross the lift (the carried record's A–B holds no plain record and the manuscript reads the record without the store's drafts), while a role pair's record does. Printed by the F3 witness (§d), not pinned.
6. Housekeeping he may want: two tabs of mine remain in a second Chrome window and two orphaned in his main window, and three in the in-app pane, all on `localhost:5211` — his to close; the scratch server on 5211 is stopped with this letter.

## What I SAW

- **F3** (`cc41ec4`): at `2027c6d` the F3 witness FAILED 5 of 7 (the measurement — the fix's positive control): a word pair given alone lived in the store's `edgeTauDrafts`, the exported file did not carry it, a fresh store after Import held none, and an Import into a session holding drafts kept the session's. After the fix: ALL PASS — after Export → a FRESH store → Import every reader reads AB as before (the glue over τ, the feet, the core, the respects, byte-equal) in the four ruled cases, with the permanent control (the file as `2027c6d` wrote it still loses the pair). Nothing a person sees changes but the file's word pairs coming back and the session's own no longer surviving an Import: the drive family was not run for F3.
- **USE-1** (`4d8fead`): the three scripts read for what they are, unchanged, taken as they stood after the technical officer's edits.
- **USE-2** (`aa5fd56`): at the eye — in the in-app pane and in Arman's Chrome — every tab said `this page: 4d8fead` from its own load, and after a visible tab's reload it said the same. The drive family's five legs have other subjects (the canvas, the windows); none reads the Save / Load panel, so none was triggered; the eye above is the reading.

## What I RAN

- **F3:** `scripts/diagnose-f3-the-drafts-persist.cjs` 5 FAILED at `2027c6d` → ALL PASS (7); tsc 0; the freeze ALL PASS; workspace persistence · the respects (§h round trip) · the midpoint · the found bugs · the born room · the road ALL PASS; no frozen file (`snapshot.ts` and `types/geometry.ts` untouched).
- **USE-1:** `git check-ignore` names none of the three; a dry run of the U1 sweep after the change leaves the STANDING BY NAME section standing; `.gitignore` byte-identical after a further apply.
- **USE-2:** `scripts/diagnose-use2-the-page-says-its-version.cjs` ALL PASS (24 clauses: the reader on the real body, the error body and seven malformed shapes; the ask through a stubbed endpoint — the path exactly, a 404, the error body as a 200, non-JSON, a thrown fetch; the line; the source pins — once at mount and nothing else in the effect, the true absence, the mark, the placement, NO reload, NO polling or listening (the cut's absence pinned so the scaffolding cannot creep back); `vite.config.ts`'s middleware and error body verbatim); `npx tsc -b` 0; the freeze ALL PASS (the fifth guard healed by staging the new file).
- **The sweep, whole, read by eye in its own call at `aa5fd56`:** `149 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else`. (Before the landing nine working-tree guards were red, as always on a manifest row: they compare the manifest to HEAD.)
- **The pre-commit witness list:** (1) the committed trees ARE what was audited; HEAD == origin `aa5fd56` after the push; (2) the manifest: `src/lib/pageVersion.ts` NEW, classified NOT_FROZEN in the same commit; `Panels.tsx`, `geometryStore.ts`, the scripts under no engine root; `workspacePersistence.ts` NOT_FROZEN; no frozen file spent; (3) no re-seal; (4) the sweep's line above + tsc 0; (5) the reading above.
- **The reload cost:** every source edit reloaded the open tabs on 5173 through vite's watch as it was saved; the landings and the pushes reloaded nothing.
- The gate: `check-coder-inbox.cjs --mark-reviewed --expect 2` — the two letters declared read in full before each commit.

## What I CHANGED

- **`cc41ec4` — F3.** `src/lib/workspacePersistence.ts` (NOT_FROZEN): `PersistedEdgeTauDrafts`, the optional field on the persisted workspace and the snapshot, written by the serializer, validated by name (`Workspace edgeTauDrafts is malformed.`), a file saved before F3 still accepted. `src/store/geometryStore.ts`: the export carries the drafts, the import sets the file's (`?? {}`) — the session's dropped by construction. `scripts/diagnose-f3-the-drafts-persist.cjs` NEW.
- **`4d8fead` — USE-1.** `.handoff/instruments/use-server/{start.cmd,stop.cmd,update.ps1}` tracked BY NAME: U1's candidate predicate takes only `.py/.cjs/.mjs/.txt/.md/.json` under instruments (a dry run never names them), and a by-name negation must survive the sweep's regeneration (it rewrote `.gitignore` from its marker to the end), so `scripts/u1-citation-sweep.py` now carries a `# ── STANDING BY NAME` section after its block whole across every apply, and `.gitignore` gains that section with the three negations. `configurePreviewServer` stays parked (§188).
- **`aa5fd56` — USE-2.** `src/lib/pageVersion.ts` NEW (NOT_FROZEN): `readWhereamiHead`, `askServerHead`, `pageVersionLine`. `src/components/Panels.tsx`: `usePageHead` (one ask at mount), `PageVersionLine` (`data-page-version`, nothing without a head), rendered under the Save / Load heading. `scripts/diagnose-use2-the-page-says-its-version.cjs` NEW. The manifest row. `/__whereami` unchanged.
- **The record commit:** `THE_BUILD.md` (the letters of 19:41 and 19:53, verbatim), `THE_BUILD_REPORT.md` (this letter), the W1 riders as found, U1.

## What I could not reach

- **Case 3 driven by a tool** — a hidden tab really shown, in a real Chrome, under a script: the extension has no way to front a tab, closing the active tab to let Chrome front a neighbour dissolved the extension's tab group, and a scratch Chrome window is not an instrument Arman accepts. The stub in the pane and one show by hand stand in for it (⚠ derived, not driven). Under his workaround the case no longer decides anything.
- **Item 2 and item 3** — cut and unpriced by the ruling above, not by a limit.

## W1 — carried AS FOUND (never read for content; no re-seal)

- **the researcher's ruling THE INSIDE-MIDPOINT-TRACE REFUSAL (named by the mothership for this record)** (`.handoff/RULING_THE-INSIDE-MIDPOINT-TRACE-REFUSAL_on-the-honesty-line_midpoint-is-the-pushout-over-the-CORE_trace-is-its-origin-partition.md`) — byte-equal to HEAD: `blob 3c864dd8` = `sha256-LF 00a639fa`, mtime 14:05:17; nothing to carry, said.
- **ADR 0031 (the researcher's amendment, §3.11)** (`docs/adr/0031-the-concept-layer-is-a-cosheaf-of-partial-symmetries.md`) — byte-equal to HEAD: `blob e0e04454` = `sha256-LF 850d5e79`, mtime 14:52:29; nothing to carry, said.
- **the claims ledger** (`.handoff/THE_CLAIMS_LEDGER.md`) — CARRIED AS FOUND, not read for content, no re-seal: `blob 67b33814` → `aed5cb4a` (`sha256-LF 7bd6fdf7` → `44447cbc`), numstat 67/0, mtime 19:55:20; the tail's last characters: ` is the device's, and is to be reported.` (closing).
- **the sovereign ledger** (`.handoff/THE_SOVEREIGN_LEDGER.md`) — CARRIED AS FOUND, not read for content, no re-seal: `blob 04db8068` → `75a16428` (`sha256-LF 6445d9b9` → `9b71b99f`), numstat 3/0, mtime 19:23:58; the tail's last characters: `karound (a role pair on the edge first).` (closing).
- **the starter batch's second master (the mothership's, for the new seat)** (`.handoff/STARTER_BATCH_FOR_THE_NEW_SEAT/2_THE_ENGINE_AS_A_USING_DEVICE.md`) — CARRIED AS FOUND, not read for content, no re-seal: `blob f54dfaf3` → `4845e6ee` (`sha256-LF 60e35de7` → `6b496d9a`), numstat 5/5, mtime 19:52:52; the tail's last characters: `thought and you make the device take it.` (closing).
- **the starter batch's third master** (`.handoff/STARTER_BATCH_FOR_THE_NEW_SEAT/3_HOW_THE_DEVICE_TAKES_A_CONCEPT.md`) — byte-equal to HEAD: `blob 4d86686e` = `sha256-LF b02e6ebf`, mtime 17:15:40; nothing to carry, said.
- **U1** (§126.2, every wave at each record commit): NO new wave — 0 rulings, 0 instruments (candidates 78); `.gitignore`'s U1 block regenerated whole; the files to add [].
