# REPORT — STAMP A-4 (five of six items) + STAMP B-2 (items 0–4) LANDED; A-4 item 1 STOPPED at its frozen ask; the 1910 rider rode — echo of the three-letter batch

## TO THE MOTHERSHIP

**ECHO: `STAMP A-4` · `STAMP B-2` · §59 + the rider (the fold and the opening tracked).**

1. **A-4 item 1 — STOPPED, as your letter foresaw, and ASKED:** `specimenModel.ts` is FROZEN (manifest :78, spent once at `3f096a4`); the `'n-a'` fallbacks live at `readSkeletonSpecimen` :175–178 and `readSurfaceSpecimen` :129–132, and the carried `h1Label` is minted in the also-frozen `writtenFormModel`. Priced for Arman in-terminal: one frozen edit (~25 lines — derive components + cycle rank E−V+c from the shape's own 1-skeleton inside the frozen file, no new import so the closure holds; H₁ = ℤ^b₁, orientable yes, w₁ = 0; the carried label stops being read) + re-seal, its own commit. **Awaiting his word; the batch landed without it. Nothing spent.**
2. **A-4 item 2 — the seam carries CHECKED sentences (SIGHTED on a thickened band's card, §1).** Three conformalAtom refusals are mapped WHOLE at `refusalCopy` (the frozen throws byte-untouched): the face refusal → *"this face's corner angles are not owned yet — nothing is fabricated"* (OWNED kept, the reroute tail dropped); the pillar refusal → *"this edge's dihedral is not owned yet — nothing is fabricated"* (the same law, mirrored); the isolated-vertex refusal → *"there is a vertex here with no face corner — an isolated vertex carries no angle and no clause"* — **the QUANTIFIER law honoured by naming NONE:** the lib throws at the first vertex it meets and a Segment has two, so an address would individuate what the fact does not; "says so" would claim a count the seam cannot see. Anything unmapped falls to the C-1 field-strip beneath. Both print sites route through the one filter (R5 stands).
3. **A-4 item 3 — the fourth field site.** `genesisModel` is FROZEN (:75), so the cure is the C-1 pattern at the SEAM: `personReadableProvenance` maps the loaded form's provenance line to *"loaded from the universe “〈its name〉”"* where the shelf's S2 split carries a designation, else *"loaded from a universe file"* — the id stays a machine handle; the eight subtitle sites in the specimen memo speak it.
4. **A-4 item 4 — certificate-door keying, cut TWICE because the first key was the forbidden one.** My first cut keyed the door on `certificateRows.length > 0` — which is exactly *the section existing* (the B-132 gate at :2196 renders the section on that very condition), so the branch was dead and the Arc still showed `certificate — show it`. Measured on the Arc's card before the recut: collapsed `certificate — show it · class n-a (1-complex)`; opened `certificate — shown` + one row `class n-a (1-complex)` — **her text-diff verbatim: zero words added.** The recut keys the door on what opening ADDS by construction: the collapsed heading carries the FIRST row as its digest, `certificateDoor = certificateHidden.length > 0`, and the pointer, the door grammar and the toggle all key on it. The Arc (one row) is now inert; a Square (two rows) opens onto `w₁ class []`, which the digest did not carry. Witnessed at the argument-card witness (§5 A-4 item 4 — the honest key present, the forbidden key absent).
5. **A-4 item 5 — lights-to-point.** The deficit summary row no longer lights on hover: its register never recedes (the FULL exception), and a summary row over every wedge has nothing to point at — of the two lawful shapes, NOT LIGHT is the honest one here. **Sighted:** pointer held on a Square's `deficit · rim turn · 90° ×4` row (`:hover` true by the DOM's own word) — weight 400, opacity 1, transparent ground, unchanged from rest.
6. **B-2 item 0 — `after K doors` is PER-CIRCUIT** (doors since the previous return, `seam.doorsAtLastReturn`). Winding's four return-1 pins stand unchanged (per-leg = cumulative at a first return); the parked E.circuit arm's detail moved to its honest value — the pillar circuit crosses no doors of its own, so the line now reads `after 0` where the cumulative count used to borrow the session's earlier 9.
7. **B-2 items 1–4 — the order-reading surface, ONE producer:** the room's face record now carries `door: {pair, side}` at both portal producers (`readCellSurface`, curved + euclidean); every crossing writes its letter to `seam.trace` (side a → `a`, the inverse side → `A`, ratcheting); the tally, the sentence, the two trace spans (most recent letter alone at full ink; eliding from the left behind a plain `…` — ⚠ the glyph is hers, deferred), and the way-back marks are ALL derived from that string. The sentence fires only when every count is 0 and its second clause is licensed by the position-return machinery alone (`doorsAtLastReturn === doors` at a fired return — LAW 20). The marks are INK: a per-face `uFaceMark` uniform and a faint graphite veil on rays through the door just crossed (full) and the one before (half) — plain first form for her sighting.
8. **B-2 at the eye.** **T³, the control, SIGHTED.** A fresh window, the heading aimed to exactly +y by the seam's own `forward` (drag rate measured −0.004 rad/px; pace 0.450 u/s → one cell = 4702 ms), then four legs of exactly one cell width with −90° turns: `B` → `BA` → `BAb` → `BAba` — every leg exactly one door, the tally to `a 0 · b 0`, **the sentence fired *"every door you opened, you closed — and here you are, home"* and the return line agreed** (`return 4 · back where you started · after 1 door · the room came back the same way up`). **Item 0 is visible in the same run:** the four return lines read `after 1 door` each — cumulative would have read 1, 2, 3, 4. (A finding rides with it: in T³ a leg of exactly one lattice period is itself a closed loop, so a return honestly fired after every leg; the eye landed on the entry point each time. True of the room, not a defect.) The earlier displaced run (my first probe advanced without a door) fired *"not home"* with no return — the licence withheld exactly as LAW 20 demands. **Seifert–Weber, the other class, SIGHTED by the MARKS:** a fresh SW window, one door crossed (`A`), the turn-back made by the veil alone — the way-back face carries the full graphite veil and the door before it the half — then the word `A f a F` (two door pairs): tally `a 0 · f 0`, **the sentence fired *"every door you opened, you closed — and you are not home"*, and no return fired** — the commutator of two SW transports is not the identity (B-1's own number at 2.642e+2), so the eye stands displaced and the licence is withheld: the two clauses disagree exactly where the geometry says they must. The panel lines were read from the DOM seams and the veil from a capture with the pane displayed (the RAF ticks only while the pane is on screen — a measured property of the pane, named so the next eye-run does not read a frozen frame as a missing veil).
9. **Two walk-model findings from the eye-run, named (not this batch's cut):** (i) the walk starts OFF-CENTRE (`eye = (−0.35, −0.55, 0.1)`) facing an OBLIQUE heading (1.2 rad) — a straight leg crosses faces of different pairings, which is why a synthetic driver's first words came out `ABB…`; a person aims by sight, a driver must aim by `seam.forward`. (ii) A transport lands the eye essentially ON the partner plane; a leg that ends there and then turns walks parallel to a door plane and can phantom-cross on float noise — legs of one full cell width avoid it. Both are the walk model's, pre-existing, and worth a line on the board.

## 1 · WHAT I SAW (5174 — 5173 another's serve; served HEAD fingerprinted 5d55cb1 + this batch's dirty paths)

- **The order-reading panel at rest** (T³ window open, no crossing): caption `E³ · n=[4,4,4] · flat · no cone edges · …`, trace `""`, tally `""`, sentence `""`, return `""` — nothing marked on the ordinary.
- **After one crossing:** trace `B`, tally `b -1`, marks `[0,0,1,0,…]` (the way back through pair b's partner face), sentence and return honestly empty.
- **After a four-crossing word whose tally cancelled** (`BAba`, then later `BbAbBa`): tally `a 0 · b 0`, **the sentence fired** — *"every door you opened, you closed — and you are not home"* — and the return line stayed empty, correctly: my first probe had advanced without a door, so the word cancelled while the eye stood displaced; the machinery withheld `home` exactly as LAW 20 demands.
- **A-4 at the eye (the zoo's own forms, 5174):**
  - **item 4, before the recut** — the Arc's card: `certificate — show it · class n-a (1-complex)`; click: `— shown` and the same words as one row. **After the recut:** the Arc's heading reads `certificate · class n-a (1-complex)` — no door grammar, cursor `default`, a click changes nothing (one node before and after). The Square's reads `certificate — show it · class open / n-a`, cursor `pointer`; a click → `— shown` with the rows `class open / n-a` and `w₁ class []` — **opening added the row the digest did not carry**; a second click closes it again.
  - **item 5** — the Square's `deficit · rim turn · 90° ×4` row with the pointer on it: weight 400 · opacity 1 · no ground — quiet.
  - **item 2** — the person's own thicken (a zoo Square as the shape, the Arc as the segment, `thicken — the band`; the parcel `4-gon × I` dragged from the shelf onto the paper): the band's card reads **`deficit · not measured · this face's corner angles are not owned yet — nothing is fabricated`** — the checked sentence verbatim, no field, no address; its certificate heading reads `certificate — show it · class genus 0 (closed, orientable)` (two rows, so the door is honestly live).
  - **item 3** — NOT sighted: the loaded-form subtitle needs the `load universe…` file chooser, which the browser pane cannot drive and no drive leg reads; the seam carries it (R6 · R6b · R6c in the refusal-copy witness, byte-exact on the frozen mint's own line). Named in §4.

## 2 · WHAT I RAN

- `npx tsc -b` — exit 0 at every stage and at the full tree.
- `node scripts/diagnose-refusal-copy.cjs` — **ALL CLAUSES PASS**: R1/R2 recut from A-4 item 2 (the checked sentences), plus R2b (the unmapped case keeps the C-1 field-strip), R2c (the pillar form), and **R6/R6b/R6c (item 3: the loaded provenance speaks the designation · falls to the plain sentence when the source is unnamed · the invoked subtitle rides byte-identical).**
- `node scripts/diagnose-deficit-register.cjs` — ALL PASS (E3 routes through the filter by construction). `node scripts/diagnose-argument-card.cjs` — ALL PASS, **with the new §5 (A-4 item 4) clause: the honest door key present, the forbidden key absent.**
- **Drive family, the triggers (card copy ⇒ d12b · d13 · deficit-app; the walk window ⇒ winding; the shelf untouched ⇒ d8 not triggered):**
  - **d12b-carried-names: ALL GREEN.**
  - **d13-the-door-speaks: 1 red, then ALL GREEN on re-run** — its (f4) is a source-SPELLING pin (`return 'unnamed'`) that my A-3b delegation had rewritten as `return composed ?? 'unnamed'`; re-pinned to the delegated spelling with the reason, behavior unchanged.
  - **deficit-app: 2 reds, then only the queued pre-existing `conePairs`+`coneFormBuilt` pair on re-run** — the second red was §E-GPU-SUBSTRATE, a source pin that forbids the phrase "you are here" anywhere in ExploreWindow; my new panel comment had quoted it. Reworded; the pin's law (no minimap) stands. **Re-run at the final tree after the item-4 recut: 49 PASS, the same single §E-DOOR-LAW arm (conePairs + coneFormBuilt, parked) and nothing else.**
  - **winding-route: 14/15 — the same parked E.circuit arm**, its detail now the honest per-leg `after 0` (¶6); every ordinal-1 pin passed under the per-circuit count.
- `npm run sweep` at the landed HEAD `9bd8a1b`, whole, verbatim: `128 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (sweep exit 0; the three in-flight reds of the uncommitted tree — aperture · folded-edge · ink, each a *content moved vs HEAD* clause — healed at the commit as their own text predicts). `npx tsc -b` exit 0 at the same HEAD. **Read by eye from the run's own file, in its own call, before this record was written.**

## 3 · WHAT I CHANGED (files, and why each)

- `src/manuscript/refusalCopy.ts` — A-4 item 2 (the CHECKED_SENTENCES table ahead of the field-strip) and item 3 (`personReadableProvenance`).
- `src/manuscript/ManuscriptView.tsx` — item 3 (the `sourceNameBySource` memo from the shelf's S2 split; the eight subtitle sites route through the provenance filter); item 4 (`certificateDigest` / `certificateHidden` / `certificateDoor` — the door keyed on what opening adds; the digest is the first row); item 5 (the deficit row's hover no longer lights).
- `src/manuscript/apertureModel.ts` — B-2: `ApertureCellFace.door` at both portal producers.
- `src/manuscript/ExploreWindow.tsx` — B-2 items 0–4: `doorsAtLastReturn` and the per-leg line; `trace`/`faceMark`/`sentence` on the seam; `doorLetter` + `refreshOrderSurface` (the one producer); the three panel lines with their data seams; `uFaceMark` declared, accumulated at the transport branch, veiled at the final colour, uploaded per frame.
- `scripts/diagnose-refusal-copy.cjs` — R1/R2 recut from the ruling; R2b/R2c added; R6/R6b/R6c (item 3) added.
- `scripts/diagnose-argument-card.cjs` — the §5 (A-4 item 4) door-key clause.
- `scripts/app-leg/diagnose-d13-the-door-speaks.cjs` — the (f4) spelling re-pin.
- `.gitignore` + `.handoff/HANDOFF_THE_DESIGNER_SEAT_V1.md` + `.handoff/OPENING_THE_FOURTH_DESIGNER.md` — the 1910 rider: two negations, both files tracked AS FOUND (tails close on finished sentences; not read for content).
- No frozen file touched; the manifest untouched. **W1 courier:** the mothership's dirty record files ride AS FOUND.

## 4 · WHAT I COULD NOT REACH

- A-4 item 1 — the frozen ask stands with Arman.
- **A-4 item 3 at the eye** — the file chooser. The pane has no file-chooser gesture and no drive leg loads a universe and reads the placed form's subtitle; the seam witness carries the mapping byte-exact against the frozen mint's own line. The gap is a rung, not a claim: whoever next drives d8 (which does load universes) can add the one clause that reads the subtitle.
- Standing residues as filed: E.circuit + conePairs with rungs 3–4; the camera gate parked whole.

*All three letters archive with this landing.*
