to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-24 21:13 +0330` (machine — `date`) · HEAD == origin == `d3b9656` (the STAMP `5abf1c0` · the cure the sweep asked for `d3b9656`) · the record commit follows this letter
re: **`STAMP C-12a` LANDED — THE FOUND BUGS, ONE CUT (§144, Δ95): six of the eight cured, each with its witness clause or eye-leg arm (items 1 · 2 · 3 · 6 · 7 · 8); item 4 STOPPED on the frozen extractor with its measurement; item 5 did NOT reproduce under twelve measured clicks and is pinned at the eye as it stands; a ninth hazard found by measurement beside item 3 (a re-dissection after the way back drops gen 2's own acts) — reported, not cut. No frozen file spent (one met and reverted). ECHO §144.**

## TO THE MOTHERSHIP

1. **Item 4 — STOP, a frozen file.** The throw is `level3LinkExtractor: edge ABAC–ABAD lies on 0 faces of cell …residue:1u8g0d` in `src/lib/level3LinkExtractor.ts` (manifest row 58, bare — FROZEN; not spent). Measured on the gen-2 residue at A as lifted: the cell's record names its 4 coarse faces and ALL FOUR are held (every coarse edge on exactly 2 of them — a lawful solid); the lifted shape ALSO holds the 4 finer triangles that subdivide its face AC·AB·AD (3 midpoint corners, 9 finer edges of which 6 lie on ONE face — an open, doubled layer on that face); the throw is on the medial finer edge, on 2 faces of the shape and 0 of the cell's. **The letter's premise is half true: the shape holds BOTH grains, not the finer instead of the coarse.** A NOT_FROZEN cure lives in the LIFT (`subComplexLift.ts`, row 182): carry the cell's boundary at ONE grain — its own record (4 corners; the glue then works as at gen 1) or refine the cell to the finer grain (its other three faces re-cut at the midpoints — a REFINE, not a lift). Which grain a lifted cell's boundary is, is the meaning question; I recommend the cell's own record and can land it on your word in C-12b's cut or a courier cut.
2. **Item 5 — the premise did not reproduce.** At AB's A column (1689 × 897, Chromium), twelve clicks inside the `· has` badge's box beside F1 — its centre, its top edge 1.5 px down (whitespace between glyphs), the ` · ` gap, the bottom-right corner, the glyph centre, and the locator's own click — EACH toggled the pick (`data-midpoint-picked`), the same as the label; `elementFromPoint` there is the badge's tspan inside the point's `<g onClick>`. Chromium hit-tests SVG text by glyph cell, whitespace included. No cure was manufactured; the leg's §12 pins the badge click at both viewports as it stands. **Where did it fail for you — which column (the A/B columns pick; the own diagram and the lifted inside never did, by C-8 item 3), which viewport?** I re-measure there.
3. **Beside item 3 — a measured hazard, not in the eight, not cut.** `selectShape` is safe (measured: keeps only the selections the chosen shape holds, clears the lift set and the inspection, refuses an unknown id). But the way back followed by THE SAME dissection re-mints gen 2 under its own id (`shape:ambo-dissection:2:czpwku` — the id indexes the act) as a NEW object derived from gen 1's records alone: **a born pair given at gen 2 (r0 ↦ Φ3 on AB–AC) is dropped, 1 → 0, while gen 1's three pairs on A–B carry** (witness §3, pinned as found). The person sees no mark. The one-line NOT_FROZEN cure in the store: when the child of that act already exists, make it current instead of re-deriving it (the same act, the same result — the meaning-trace law). Yours to rule; I did not cut it under Δ92.

## What I SAW — the drive family, `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS` (127 clauses — 117 → 127, five new arms per viewport; `/__whereami` = `4332af2`, dirty — the C-12a tree)

At 1689 × 897 (and at 1400 × 900):
- **Item 1 — the door's taken line names each pair's corner.** The line reads `yours · a whole line along A→AC→AB→A: at A F1 ↦ F1 · at AC r2 ≡ F1 ↦ F1 · at AB F1 ↦ F1` with its ONE hand, and the taken sentence `taken — F1 ↦ F1 at A, and with it the whole line along A→AC→AB→A: at A F1 ↦ F1 · at AC r2 ≡ F1 ↦ F1 · at AB F1 ↦ F1` (§10, re-pinned).
- **Item 2 — the cell by its kind and corners, the universe once.** The lift notice reads `lifted “the tetrahedron A·AB·AC·AD of Ambo Dissection Tetrahedron” → the Manuscript shelf` at gen 1 AND at gen 2 (the finer grain rides the shape; the name says the dissection once); the shelf titles carry the words and no `cell:`, no `Ambo Dissection Ambo Dissection` (§12).
- **Item 3 — the way back.** The workspace tab now holds the Genealogy panel: `Tetrahedron g0` · `Ambo Dissection Tetrahedron g1` · `Ambo Dissection Tetrahedron g2`, the current marked; g1 pressed is current — the selection tab lists gen 1's 10 vertices — and g2 pressed again (22 vertices) (§12; plate `concept-layer-genealogy-*`).
- **Item 5 — the badge.** the `· has` badge beside F1 clicked on its box's top edge (whitespace between glyphs) picks F1, the label click unpicks (the toggle), the badge's centre picks again and once more unpicks — at both viewports (§12 — the premise not reproduced; pinned as it stands).
- **Item 6 — the lone word wears its corner.** After the three word pairs the midpoint's own space reads `presupposes ≡ specifies` and `presupposes [B]`, no bare `presupposes` in the own drawing or the own block; the sources' chips keep their plain names (§12).
- **Item 8 — the room taken where its body is hit.** The summon's FIRST candidate takes the room at both viewports (1689 × 897: 1 try, the plaque's centre at (989, 707) — chip 1; 1400 × 900: 1 try, the plaque's centre at (845, 709) — chip 1); with the room selected, a double-click inside its drawn domain now reaches the room's own group (its click handler fires with detail 2, the paper's does not — the hit hull, 4 triangles), toggling the room off and, at the same point, on again (§12).
- Item 7 has no live case at the eye (the gen-2 residue refuses at the GATE, before a room exists); its refusal is witnessed on the read itself (below).

**The mechanism of item 8, measured before the cure — two defects, one in the instrument:** (i) the eye driver projected every point through `document.querySelector('canvas')`, which on this page is the Ambo's HIDDEN canvas (760 px at x=280); the Manuscript's is the LAST canvas. Through the right canvas the plaque's centre takes a room on the first double-click (probed on the world's T³ and on the built room). (ii) With a room selected its domain is drawn above the plaque as a wireframe; a double-click inside the wireframe met only the paper (whose double-click DISMISSES) — a ray dump through the new `window.__manuscriptRaycaster` seam showed no surface there. `pick` toggles an already-selected form (Arman's law: dismiss is a double-click), so the hull's mark at the eye is the ROOM's handler firing, not the chip staying lit.

## What I RAN (verbatim tails)

- `scripts/diagnose-the-found-bugs.cjs` (NEW, 18 clauses) — `DIAGNOSE-THE-FOUND-BUGS: ALL PASS — a cell named by its kind and corners, the universe once, the way back safe, the lone word bracketed beside its chain, the explore read's refusal said`. §1 item 2 (gen 0 `the tetrahedron A·B·C·D of Tetrahedron`; gen 1 and gen 2 `the tetrahedron A·AB·AC·AD of Ambo Dissection Tetrahedron`, the placed forms `… — loaded`; the names `Tetrahedron` · `Ambo Dissection Tetrahedron` ×3 at gens 1–3; a given cell name wins; a vertex lift unchanged). §2 item 3 (`selectShape` measured; a cell both shapes hold survives — the residue at A keeps gen 1's id; the re-mint under the same id). §3 item 6 (`presupposes [A]`·`[B]` lone against lone before the pair; `presupposes ≡ specifies` + `presupposes [B]` after; `disjoins [A]`·`[B]`, `component-of`, `generates` unchanged) + the hazard pinned as found. §4 item 7 (the T³ read: 6 faces · 12 rods; a surface that throws → `cannot walk this room — its surface could not be read: Cannot read properties of null (reading 'shape')`… the very message; a throw in the cargo's room caught by the same guard). §5 sources, purity, the manifest (InkedDomain and level3LinkExtractor bare and untouched).
- `scripts/diagnose-the-doors-act.cjs` — `DIAGNOSE-THE-DOORS-ACT: ALL PASS` (§2/§4 re-pinned on `at A F1 ↦ F1 · at AC Φ1 ≡ F1 ↦ F1 · at AB F1 ≡ r0 ↦ F1 ≡ r0` and F5's line).
- `scripts/diagnose-the-lift-carries.cjs` — `DIAGNOSE-THE-LIFT-CARRIES: ALL PASS` · `scripts/diagnose-the-concept-type.cjs` — `ALL PASS` · `scripts/diagnose-engine-freeze.cjs` — `ALL PASS` (the two new src files intent-staged and classified NOT_FROZEN; InkedDomain byte-equal to HEAD after the revert).
- `npx tsc -b` — exit 0.
- The drive family: `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS` (127 clauses; the leg ran three times on this stamp — the first found the hull's double-click leaving the room deselected (the cargo arm then found no window), the genealogy rows read through textContent (the spans concatenate with no word boundary before `g1`), and two wrong instruments of mine (the selection tab's vertex rows list a SELECTED CELL's vertices, cleared by the way back; the midpoint's own drawing strips a lone name's bracket by C-7f item 4 and writes the origin instead — the lifted drawing, which writes no origin, is the site); the second re-pinned those and left one miscount in the driver's own cell-row instrument; the third whole on both viewports).
- **The sweep, whole, read by eye in its own call** — at the STAMP `5abf1c0`: `144 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP RED — unexpected fail(s): scripts/diagnose-multi-region-lift.cjs · scripts/diagnose-p1b-granular-save.cjs · scripts/diagnose-the-cargo.cjs` (wall 91.8 s) — the full suite catching three pins my own runs did not: two on the lift title `<cellId> of …` (moved by item 2), one on the view's direct `cargoRoomOf` call (moved by item 7); **cured at `d3b9656`** (the three witnesses re-pinned, no source file moves, each ALL PASS on its own); at `d3b9656`, before this letter was written: `144 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (exit 0 — the census 144, the found-bugs witness by its own declaration at 1.6 s, the cargo 2.0 s, the doors act 6.3 s, the multi-region lift 8.3 s, p1b 14.1 s; the gate 81 s, the door column 79 s, the j-register PASS at 66 s inside its declared ceiling, the born room 51 s; wall 96.9 s).

## What I CHANGED

- `src/manuscript/doorTransportModel.ts` (NOT_FROZEN) — item 1: each pair on a whole line names its corner in the line's words and in the taken sentence.
- `src/lib/subComplexLift.ts` (NOT_FROZEN) — item 2: `cellDesignationOf` — a cell with no given name is designated `the <topology ?? kind> <corners alphabetical>`; the lift's designation reads it before the address. `src/lib/ambo.ts` (NOT_FROZEN) — the child keeps a parent's name already beginning `Ambo Dissection `.
- `src/components/Panels.tsx` — item 3: `WorkspacePanel` mounts `<GenealogyViewer />` (existing, exported, mounted nowhere before).
- `src/lib/spaceOf.ts` (NOT_FROZEN) — item 6: a lone name wears its corner when ANY other name in the space — lone or a chain's segment — is spelled alike under another seed.
- `src/manuscript/exploreRead.ts` (NEW, NOT_FROZEN, react-free) — item 7: `exploreReadOf` returns the surface + cargo room, or `{ refusal }` in the one grammar with the engine's reason; `src/manuscript/ManuscriptView.tsx` (NOT_FROZEN) reads through it (no `catch` in the memo), tells the two outcomes apart, prints the refusal in the door's `data-explore-refusal` register; the `cargoRoomOf` import moves to the read module; the `window.__manuscriptRaycaster` seam beside the scene/camera seams.
- `src/manuscript/DomainHitHull.tsx` (NEW, NOT_FROZEN) — item 8: the domain's faces as a mesh that draws nothing and only raycasts, mounted in `ManuscriptView` beside the ROOM's frozen `InkedDomain` (one site; the live aperture skeleton is no room a person picks). **InkedDomain.tsx (FROZEN, row 71) was edited first by mistake, caught by the freeze witness (6 clauses red), reverted byte-for-byte, and the hull relocated — no frozen file spent.**
- `scripts/app-leg/concept_layer_eye_driver.py` — the projection through the LAST canvas (three sites); the summon measured from an unselected room, `summonTakenAt`; the hull's instrumented double-click; `badge_click`; `genealogy_arm` (ends on gen 2); `WORDS_AT_AB`. `scripts/app-leg/diagnose-the-concept-layer-eye.cjs` — §10 re-pinned (item 1); §12 five clauses per viewport (items 5 · 6 · 3 · 2 · 8).
- `scripts/diagnose-the-doors-act.cjs` — §2/§4 re-pinned. `scripts/diagnose-the-found-bugs.cjs` (NEW, 19 clauses). `docs/governance/ENGINE_FREEZE_MANIFEST.txt` — two NOT_FROZEN rows (exploreRead · DomainHitHull).
- At `d3b9656` (the cure the sweep asked for): `scripts/diagnose-multi-region-lift.cjs` §5 and `scripts/diagnose-p1b-granular-save.cjs` §5/§5b re-pinned on the kind-and-corners designation (`the octahedron AB·AC·AD·BC·BD·CD of …`; the address still mints the distinct shape id); `scripts/diagnose-the-cargo.cjs` §3 re-pinned on the read through `exploreReadOf` and the module's own `cargoRoomOf` call.

## What I could not reach

- **Item 4** — stopped at the frozen extractor (above, 1). **Item 5** — not reproduced (above, 2).

## PARKED (one line each, Δ92)

- The universe's name at gen 2 equals gen 1's now (`Ambo Dissection Tetrahedron`); the generation rides `g1`/`g2` beside the name in the Genealogy panel and the id in the shelf's provenance — a lift from gen 1 and from gen 2 of the same cell share a title.
- The shelf's provenance line still names the universe by id (`universe “shape:ambo-dissection:2:czpwku”`) — not in the eight.
- The argument-reading line on the card reads `Ambo Dissection Tetrahedron ⟶ the tetrahedron A·AB·AC·AD` now (it prints the title's two halves) — read, not ruled.
- A re-dissection after the way back grows the undo stack by one and re-mints the child (above, 3).
- `exploreRefusalWords` carries the engine's message verbatim (an engine sentence in the person's register) — the door prints it as it stands.

## W1 — the courier, at the record commit

The two riders you named for C-12a: the sovereign ledger (Δ95: 0 at HEAD → 1 on disk) and the claims ledger (§144: 0 → 1; §145 also on disk) — carried AS FOUND with their hashes in the record commit's message. ADR 0031 and the researcher's INSIDE-MIDPOINT-TRACE ruling are also modified on disk (your 1941/1947 name them for C-12b's W1) — NOT carried in this record; they ride C-12b's, said.
