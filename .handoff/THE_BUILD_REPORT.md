to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-22 16:56 +0330` (machine — `date`) · the chain `df2d2df → 678fce5 → the record commit`, this report the record commit's own letter
re: **`STAMP C-7e` LANDED (`df2d2df`) — THE CARRY, and it MIRRORS: twelve of seventy-eight surviving pairs are walked the other way by a later dissection (measured before the cut), so a blind copy by pair would have named each role as the other cast's. `STAMP C-7f` LANDED (`678fce5`) — the designer's eight: six cut, one cut in structure but not in height (measured), one priced; her census premise measured and named. Step 1's Ambo end CLOSES at this report. The C-5 parameters reading, free.** ECHO §115 · `STAMP C-7e` · Δ84 · §116 · `STAMP C-7f` · §117.

## TO THE MOTHERSHIP — thirteen lines up

1. **THE MIRROR (C-7e item 1 — a premise measured before the cut, and it changed the cut).** The letter said *copy the record onto the same PAIR*. A pair is unordered; the record is not — it reads FIRST corner ↦ SECOND, and `deriveEdges` walks a re-derived edge by whichever face it meets first. Measured (`probe-orientation`, now §2 of the midpoint witness): seed → gen 1 keeps all 6 pairs' order; gen 1 → gen 2 (the core, or a residue) keeps all 30; **gen 2 → gen 3 on the core walks 12 of 78 surviving pairs THE OTHER WAY** (`ABAC→ABAD` becomes `ABAD→ABAC`, and eleven more — the dissected cell's own edges, whose first face was a face of the cell being replaced). A blind copy on those twelve would have carried `[F5 ↦ Φ7]` onto an edge whose first corner is the OTHER cast — an identification the person did not give. So the carry MIRRORS the pairs on a pair walked the other way: the same act, said from the new first corner. Built as the faithful reading of *copy, never re-derive*; said, not hidden. ⚠ In the reachable path today no record sits on those twelve at the moment they reverse (a record needs a midpoint site, and those edges' sites are minted by the very dissection that reverses them) — the falsifier was built by placing a record there by hand; a workspace import could do it for real.
2. **THE FENCE (C-7e item 2).** You priced ~10 lines. The helper `carryDraftsByPair` is 17 lines with braces (12 statements), the call site 1, the import of `canonicalEdgeKey` from ids.ts (FROZEN — imported, not edited) 1. The excess is (a) the mirror on the drafts' τ pairs and (b) the pending attempt RE-MADE on the new edge through `midpointAct` — the same check every act takes, so its refusal and its hands are a reading re-derived from the person's act, never a stored one (RECORD, NOT READING). Nothing frozen reached. One word from you drops (b) to a plain copy of the refusal object.
3. **STEP 1's AMBO END CLOSES AT THIS REPORT** — the person-gate discharged by Δ84 (both halves at Arman's hand), the chain by `C-7e`: a mapped midpoint, dissected again, selected as a corner, draws its glued space through one code path with the engine's own carry — witnessed (§4) and driven at the eye at both viewports (the gen-2 arm: the same 3 role pairs and 3 word pairs, the own diagram's 20 points, the trace, the source C still carrying the A–C act). Arman's reason — *every midpoint becomes a parent* — is true at gen 2 now.
4. **THE DESIGNER'S CENSUS PREMISE, MEASURED (C-7f item 7).** Her five worst numbers reproduce TO THE HUNDREDTH under an ALPHA-BLIND reading — the text's colour against the nearest ancestor's background colour with its alpha IGNORED, a tinted badge read as its opaque colour: `workspace` (the active tab, `text-teal-100` on `bg-teal-400/15`) reads **1.65** blind and **13.75** composited over the app's ground; `lineage-only` (amber-200 on amber-400/10) 1.34 blind; `candidate` (amber-100 on amber-300/5) 1.29; `Ambo enabled` (emerald-200 on emerald-400/10) 1.50; `Primal` (cyan-100 on cyan-400/10) 1.61 — every one a badge on a 5–15 % tint over near-black, every one ≥ 10:1 as the pixels show it. The leg now prints BOTH readings for every text node it censuses (`composited` · `blind`). What is REAL under the composited reading and was cut: the layer's `text-stone-500` sentences on the ground (4.09) → stone-400 (7.83); the disabled `Fit Selected` (stone-600, **2.6** — real either way) → stone-400 (7.84 disabled, 18.11 enabled); the packets tab's `empty` status badge (stone-500 on stone-900, 3.6) → stone-300. **NOT cut: the `workspace` tab** — 13.75 active, 7.85 inactive, composited; a control a person can read. ⇒ **A MEANING QUESTION: which reading defines the floor?** Mine composites the ancestors' translucent backgrounds down onto `#0c0a09` (what the eye is shown); hers evidently did not. The 39 % rests on the blind reading; under the composited one the midpoint's panel is 284 nodes with 0 below 4.5, the card 75 with 31 below (all of them the inspector's `dt` labels and ` · ` separators — item 8), the packets tab's cast section 3 with 0.
5. **THE CARD (item 5): the GRID MISMATCH cured, the HEIGHT NOT — measured.** Every cast row now spans the card's full width (`col-span-2` on its label and its sentence; the witness pins all six). Her numbers before: 312 × 1119 at a 793 px viewport. After, at C holding the T cell: **327 × 1133 px at an 897 px viewport (ratio 0.29); the same at 1400 × 900** — the card is still taller than the viewport. The cause of the height is not (only) the grid: `sustains · 5 tuples · none reversed — read as directed` is ≈ 48 characters at 14 px ≈ 380 px, and the card's full content width at the sidebar's width is 301 px, so each of the seven orderings STILL wraps to two lines (the `orderings` row is 240 px tall); and the `dl` she measured holds the vertex's own rows and the relations rows beside the cast's (528 px of the 1133 are the cast's). ⇒ her *"the height and the aspect ratio have ONE cause"* is falsified by measurement: there are two, and the second is the sentence's own length at 360 px. The wording is C-6d (γ) §3.2's ruling (the act's line carries the device's reading of term order) — not mine to shorten; the number is up.
6. **ITEM 8's PRICE (the app-wide contrast sweep — priced, not cut), three lines.** (i) By the composited reading the failing shades are the inspector's conventions, not the badges: `text-stone-500` on stone-950 (4.09) at **228 sites** and `text-stone-600` (2.6) at **36 sites** across 17 files under src/components (134 + 12 in Panels.tsx alone — every card's `dt` label, every ` · ` separator, every disabled control's text). (ii) The cut is one shade per convention (`stone-500 → stone-400` for labels a person reads; `stone-600 → stone-500` only where the text is decorative punctuation, else `stone-400`) — a class sweep, not a redesign; ~250 edits, one commit, one witness. (iii) The witness: the leg's census over the WHOLE document at his viewport (the instrument exists now; it runs on a selector), pinning the floor for every non-decorative text node by the composited reading and PRINTING the blind reading beside it so the two instruments can never be confused again. GROUND_PLAN §11 honoured: not in this pass.
7. **THE FOOT PLATE (item 1) is hers; the blind metric, printed as ordered.** On Flow's column the box overlaps read word-to-word **133 → 0** and word-to-label **93 → 11** (the words now sit in their point's rows; the metric counts a word's box against its own point's label text beside it — still blind, still printed, not blessed). Two facts for her eye that the placement produced: (a) the up-feet ride the LABEL LANE, 11 px above each label (the up-arc leaves leftward through the lane; there is no other left) — a default; (b) the feet WIDEN the columns: at 1689 × 897 the two-column drawing is **1618 px in a 1025 px panel**, at 1400 × 900 1618 in 736 — a horizontal scroll to reach the right column's own right side. The scroll PREDATES this cut (Φ1's six loop words in one row are ≈ 450 px; her ruling kept them in a row), but A's left reach grew by the widest up-foot row (`exceeds-in-capacity · presupposes · regulates`). Hers to rule: a second row for feet past a width, or the fold narrower.
8. **ITEM 4 — one glyph, one meaning.** In the glued column the `≡` GLYPH before a `both` tuple is gone: the word `both` carries it, written beside the tuple like `from A` and `from B` beside the others (72 origin words on the AB column: 20 roles + 52 tuples, 7 `both`). `≡` stays where it is the person's ACT — a glued role's label `F5 ≡ Φ7` and a translated word `sustains ≡ descends-from` (her rider: translated carries the mark; `disjoins [A]` / `disjoins [B]` are shown plain, `disjoins from A` / `disjoins from B`). In the UNFOLDED columns `both` tuples keep the `≡` glyph (§2.5 stands: position states the other two). So `≡` means one thing everywhere — *one with something across the fold*. A default named.
9. **ITEM 6 — the line and the retirement.** The source reads `C · through face A·B·C · holds a concept-space of 10 roles · 11 relations · 6 words · open the drawing` (relations = arrows + loops + tuple-nodes from `insideOf`'s census, never a stored count; `holds a cast of nothing — no roles` for the empty cast; `holds no cast` for none); opened, the drawing is the corner's own — row 30, labels 12, words 11 — and `close the drawing` folds it. `compact` is RETIRED from the module (no thumbnail can be drawn any more); your held question "compact's size" dissolves with it.
10. **C-5 — THE FREE READING: yes, both as PARAMETERS, and here is what is not one.** A ~40-line `composeThroughCorner(records: { AB, BC, CA }, corner: 'A', options)` can take (i) `domain: 'A' | 'core'` — the roles the residue `h_A = proj_C⁻¹ ∘ J_AB` is read on (A's own, or the core's = the roles glued on all three edges); and (ii) `gap: (where: 'edge-missing' | 'role-unpaired' | 'not-in-image') => 'Und' | 'excluded'` — what `Und` means when an edge's record EXISTS but is PARTIAL is exactly the `role-unpaired` branch of that function, so the module never assumes it. Two things are LOAD-BEARING and NOT parameters: (a) ORIENTATION — every record reads first corner ↦ second, and the path A→B→C→A traverses each edge in the direction the edge happens to be walked, so the composite must invert a record wherever the path runs against its `vertexIds` — and C-7e measured that this orientation is NOT stable across dissections (12 of 78), so the composite reads each edge's `vertexIds` at the moment of reading, never a stored direction; (b) THE EMPTY-CORE GUARD — any of the three edges without a record ⇒ no composite, a true absence in words, never an empty map read as an identity. The residue's three-way reading `Fix · Mov · Und` is a partial permutation's own partition and needs no definition beyond (i) and (ii). Buildable the hour the researcher rules; I will not guess either.
11. **THE ASSOCIATIVITY QUESTION (the researcher's, via your §2): nothing at gen 2 assumes an order.** The carry copies records PER EDGE and composes nothing; the glued space at gen 2 is `gluedSpace(A, B, M)` of the ONE record on the one edge, as at gen 1. The only place order could matter is C-5's composite — flagged in line 10 as the guard's territory.
12. **ITEM 7 (d) in the card — holds, and the labels are item 8's.** No value is dimmer than its label anywhere in the cast card (values stone-200 at 15.4, labels stone-500 at 4.09); the labels themselves sit under the floor by the inspector's app-wide convention (line 6). Within the layer's own surfaces every text node now reads ≥ 4.5 composited (the panel: 0 of 284 below).
13. **ITEM 3's HOME.** The attribution of a re-made pair (`yours · re-made when you withdrew r8 ↦ Φ6`) lives in transient store state beside the refusal (`midpointRemade`, never exported), leaves with its pair, and rides a dissection by pair with both its acts mirrored. A reload loses it, as it loses a pending refusal and a draft — the parked persistence price's territory, said.

## 1 · What I SAW

**C-7e, the gen-2 plate at 1689 × 897** (`_frames/concept-layer-ab-gen2-carried-1689x897.png`): after the pairs at AB and AC and `Apply Ambo Dissection` on the core, the octahedron (the gen-1 core, now the parent — `octahedron … parent … g1 · 7 children` in the tree) selected, AB selected from its corners: `3 role pairs · 3 word pairs — yours`; the three word pairs each `yours · withdraw`; the source C `on C–A: r0 ↦ F1 · sustains ↦ sustains · no identification given yet on B–C`; the lines `F7 ↦ Φ1 · yours · withdraw`, `F8 ↦ Φ2`, `F5 ↦ Φ7` across the fold; the own diagram below the fold (the leg: 20 points, 4 both); the trace `AB: 20 roles (14 + 9 − 3) · 22 words · 52 tuples (4 both) · 20 marks`. The word half and the drawing's top inside the box at scroll 0. `Apply Ambo Dissection` reads `Cell has already been expanded.` for the selected parent — right.

**C-7f, the feet** (`concept-layer-ab-foot-words-1689x897.png` — the plate is the designer's): Flow's column reads `F1 · has ●  exceeds-in-speed · exceeds-in-speed · generates` with the down-feet just below each row to the right of the point, and the up-feet above each label on the left — `presupposes` over F4, `opposes · presupposes` over F6, `exceeds-in-capacity · presupposes · regulates` over F7, `≡ exceeds-in-size · ≡ presupposes` over F8 (the `both` tuples with their glyph, in the unfolded column) — no pile, one row per point per side; Φ's column the same (`≡ lodges-in · ≡ specifies` over Φ2, `re-creates · transmits`, `converges-on · descends-from`); the apex band empty. Φ1's loop row and B's down-feet lie past the panel's right edge at this width (line 7). At 1400 × 900 the drawing container had scrolled to its right end when the plate was taken (A's column off to the left) — the same content.

**The source opened** (`concept-layer-source-open-1689x897.png`): `C · through face A·B·C · holds a concept-space of 10 roles · 11 relations · 6 words · close the drawing`, then the T cell WHOLE at the corner's size — `r0 · has` … `removes` with legs 1 · 2 · 3, `starts · ¬ sustains`, `warrant carried, never read` — every word legible; the acts line beneath it; the word half and the drawing below.

**The re-made pair at CD** (`concept-layer-cd-remade-1689x897.png`): `1 role pair · 1 word pair — yours`; the one line across the fold reads `r0 ↦ Φ1 · yours · re-made when you withdrew r8 ↦ Φ6 · withdraw`; the source A above reads `holds a concept-space of 14 roles · 34 relations · 11 words · open the drawing` with `on C–A: r0 ↦ F1 · sustains ↦ sustains · no identification given yet on A–D`; the residual lines present (glued). D's column with its feet, `Φ9 · none-by-nature` alone.

**The empty map**: before any pair the AB panel carries no residual sentence (the leg: `residuals: null`); once glued, both parents' lines.

**The card at C** (`concept-layer-card-1689x897.png`): the sidebar scrolled to the card: `Cast` on its own line, then `taken — 10 roles · 7 relation-types · 21 relations · read as directed · warrant carried, never read` at the full width; `Of` with the corner and the subject on their own lines; `Roles` with `r0 · r1 · … · r9` and `an id is an address, not a name`; `Term order` with the seven orderings — each STILL two lines (`sustains · 5 tuples · none reversed — read as` / `directed`) at 301 px; below them `Antipodal none`, a label·value pair keeping the two-column grid. The T cell's own inside on the canvas beside it with its feet (`r0 · has ● sustains`, `starts · ¬ sustains` above r6, `removes` with legs 1 · 2 · 3).

**Not sighted by a person:** the designer's eye on the foot plate (item 1 is hers to rule), the up-feet's lane, the drawing's width (line 7), the card's remaining height (line 5). Arman's eye on none of this yet.

## 2 · What I RAN (verbatim)

**C-7e, before `df2d2df`:**
- `npx tsc -b` → exit 0.
- `node scripts/diagnose-the-midpoint.cjs` → `DIAGNOSE-THE-MIDPOINT: ALL PASS` (56 clauses; §2's note: `gen-1 edge edge:251ovc → gen-2 edge edge:1njwrxo · the gen-2 edge carries: {"roles":[["F5","Φ7"]],"types":[["sustains","descends-from"]]} · records in gen 2: 1 · the AB surface at gen 2 reads glued`; `gen 2 → gen 3 on the core: 12 of 78 surviving pairs walked the other way, 66 the same way (gen 1 → gen 2: 0 of 30)`).
- `node scripts/diagnose-the-concept-type.cjs` → ALL PASS (six readers; the writer and the carrier named).
- `node scripts/diagnose-engine-freeze.cjs` → exit 0 · `diagnose-the-j-register` 61 ALL PASS · `diagnose-ambo` · `diagnose-workspace-persistence` · `diagnose-p1b-granular-save` · `diagnose-the-inside` · `diagnose-the-cast-loader` all pass.
- `node scripts/app-leg/diagnose-the-concept-layer-eye.cjs` (DRIVE FAMILY, spawned on 5199, `/__whereami` = `c96fd46` = HEAD at that run) → `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS` (23 clauses; the gen-2 arm at 1689 × 897 and 1400 × 900).

**C-7f, before `678fce5`:**
- `npx tsc -b` → exit 0.
- `node scripts/diagnose-the-inside.cjs` → `DIAGNOSE-THE-INSIDE: ALL PASS` (29 clauses; `Flow's feet: 7 down rows · 10 up rows · words per row [3,2,3,1,1,1,2,3,3,2,1,1,1,1,1,1,4]`).
- `node scripts/diagnose-the-midpoint.cjs` → `DIAGNOSE-THE-MIDPOINT: ALL PASS` (57 clauses).
- `diagnose-the-cast-loader` · `diagnose-the-concept-type` · `diagnose-the-ambo-gesture-truth` · `diagnose-ambo` · `diagnose-workspace-persistence` → exit 0 each · `diagnose-engine-freeze` → exit 0 · `diagnose-the-j-register` → ALL PASS.
- The DRIVE FAMILY leg, first run: ALL of the C-7f arms passed at both viewports except the card's, which read the rows at 301 px in a 303 px box — the two pixels the card's own borders, an instrument error (the content width had not subtracted them); the collision metric's `labels` selector had also caught the new foot rows inside a point group (a word counted against its own row). Both corrected in the instrument; the leg re-run whole:
- `node scripts/app-leg/diagnose-the-concept-layer-eye.cjs` (DRIVE FAMILY, spawned on 5199, `/__whereami` = `df2d2df` = HEAD at that run, dirty paths 18 — the C-7f tree) → `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS — both halves of the midpoint's act are in the person's visible box and made by clicks; the midpoint draws its own space; the sources carry the person's neighbouring acts; the acts survive a second dissection; the designer's eight read at the eye` (43 clauses: §0 + 21 at 1689 × 897 + 21 at 1400 × 900). Its notes, verbatim (the same at both viewports):
  - `foot rows 27 · arc-word box overlaps per column (the blind metric, printed): [{"arcWords":31,"wordWordOverlaps":0,"wordLabelOverlaps":11},{"arcWords":15,"wordWordOverlaps":0,"wordLabelOverlaps":7}]`
  - `the drawing 1618 px wide in a 1025 px panel — the feet widen the columns; the right column's own right side is beyond a horizontal scroll at this viewport` (at 1400 × 900: `1618 px wide in a 736 px panel`)
  - `the card at C: 327 × 1133 px (ratio 0.29) at a viewport 897 px tall — of which the cast's rows 528 px · rows ["summary 301×40","subject 301×72","roles 301×68","orderings 301×240"] · content width 301`
  - `contrast census — the midpoint's panel: 284 nodes · below 4.5 composited 0 · alpha-blind 0 · the card: 75 nodes · below 4.5 composited 31 · alpha-blind 31 · the packets tab: 3 nodes · below 4.5 composited 0 · alpha-blind 0`
  - `the two named controls at the start (Fit Selected disabled, the workspace tab active): [{"text":"Fit Selected","size":12,"composited":7.84,"blind":7.83,"disabled":true},{"text":"workspace","size":14,"composited":13.75,"blind":1.65,"disabled":false}]`
  - `the two named controls with a selection: [{"text":"workspace","size":14,"composited":7.85,"blind":7.85,"disabled":false},{"text":"Fit Selected","size":12,"composited":18.11,"blind":18.11,"disabled":false}]`
  - the panel's five faintest by the composited reading all at 7.83 (the stone-400 sentences).
  - the word half now begins at y 220 and the drawing's top at 368 at 1689 × 897 (were 484 and 632 at C-7d) — the source above no longer draws a thumbnail; 220 / 394 at 1400 × 900.

**The sweep at `678fce5`, whole, read by eye in its own call:**
```
THE SWEEP — 137 legs · 23 workers · per-leg ceiling 420s
…
FAIL    scripts/diagnose-dual-inspection.cjs  13.3s
…
PASS    scripts/diagnose-the-j-register.cjs  79.0s
PASS    scripts/diagnose-the-concept-type.cjs  9.7s
PASS    scripts/diagnose-the-ambo-gesture-truth.cjs  2.4s
PASS    scripts/diagnose-the-cast-loader.cjs  1.8s
PASS    scripts/diagnose-the-midpoint.cjs  1.3s
PASS    scripts/diagnose-the-inside.cjs  1.2s

slowest: the-gate.cjs 85s · the-door-column.cjs 83s · the-j-register.cjs 79s · the-noncube-domain.cjs 39s · the-field-in-the-specimen.cjs 31s
wall: 90.4s (sum of legs 1917s)

137 files · expect exactly ONE fail: diagnose-dual-inspection
SWEEP OK — the one expected fail, nothing else
```
(sweep exit 0; 136 PASS lines and the one accepted FAIL, read by eye; the j-register leg PASS at 79.0 s inside its declared ceiling.)

## 3 · What I CHANGED

**`df2d2df` — STAMP C-7e.**
- `src/lib/ambo.ts` (NOT_FROZEN row 93) — after `deriveEdges`, `applyGenericAmboDissection` copies `identification` (`roles` and `types` only) from the parent's edge of the same pair onto the re-derived edge, MIRRORED when the new edge's first corner is the old second (8 lines of code). shape.ts and ids.ts untouched.
- `src/store/geometryStore.ts` — `carryDraftsByPair(set, get, from, to)` after the ambo's set, scoped to `operation.id === 'ambo-dissection'`: the drafts copied by pair (mirrored), a pending attempt re-made through `midpointAct`; the import of `canonicalEdgeKey`.
- `scripts/diagnose-the-midpoint.cjs` — §2's clause flipped (the id-mint sentence kept as the reason) + the true-absence clause + the orientation clause (the 12-of-78 falsifier with the un-reversed control); §3 the drafts through the store's own action; §4 one code path at gen 2 with no hand.
- `scripts/diagnose-the-concept-type.cjs` — six readers; two sites named (the writer, the carrier).
- `scripts/app-leg/diagnose-the-concept-layer-eye.cjs` + `concept_layer_eye_driver.py` — the gen-2 arm; the 1442 §4 sentence in the head.

**`678fce5` — STAMP C-7f.**
- `src/components/CastInsideDiagram.tsx` — item 1: the arc words at their FEET (one `<text data-inside-foot-words="P|side">` per point per side, the words as tspans naming their arc's ordinal; no textPath, no offsets); item 4: `MarkExtra.word` / `.origin`, `PointExtra.origin` — the origin written after the word/label; item 7: `LABEL = 12`, `DENSE = 11` (was 10), every text haloed; item 6: `compact` retired (`insideGeometry` takes `footExtra` for the reach estimate instead); the reach estimates take the foot rows into account.
- `src/components/MidpointSurface.tsx` — item 2: the residuals only when glued; item 3: `remade` prop, `data-midpoint-remade` on the line and the word pair, the sentence `yours · re-made when you withdrew …`; item 4: `ownColouring(own, inside, originTint, la, lb)` writes `both` / `from A` / `from B`, strips `[A]`/`[B]`, drops the `≡` glyph on tuples (the tint kept as reinforcement); item 6: `SourceRecord` — the words line, `open the drawing` / `close the drawing`, the drawing whole on request; item 7: stone-500 → stone-400 on the layer's sentences; the fold's line labels 11 px with a halo.
- `src/store/geometryStore.ts` — `MidpointRemade { act; withdrawn }`, `midpointRemade` state; set in `midpointWithdraw` when the re-made act was MADE, cleared when that act is withdrawn; carried by pair (mirrored) in `carryDraftsByPair`.
- `src/components/Panels.tsx` — `CastCardRows`: `col-span-2` on all six labels and six sentences; the five sub-lines stone-500 → stone-400.
- `src/components/Workspace3D.tsx` — `Fit Selected` disabled text stone-600 → stone-400 (the border and ground still say disabled). The `workspace` tab untouched (line 4).
- `src/components/VertexPacketEditor.tsx` — the `empty` status badge stone-500 → stone-300; the lineage line, `No tags.`, the tag-remove control stone-500 → stone-400.
- `scripts/diagnose-the-inside.cjs` — the feet (the rows' y measured against the points' cy: down +13, up −11), every word once, the sizes (12 / 11 only; no `compact` in code), the halos on every text.
- `scripts/diagnose-the-midpoint.cjs` — the sources in words with no drawing until asked, the bare lines, the origins written (72 / 7), the attribution in §3 and carried across the dissection.
- `scripts/diagnose-the-cast-loader.cjs` — the card's grid pinned (six + six `col-span-2`, no stone-500 sub-line).
- `scripts/app-leg/diagnose-the-concept-layer-eye.cjs` + `concept_layer_eye_driver.py` — the C-7f arms (§3 × 2 viewports): the feet and the blind metric printed, the sizes, the bare lines, the origins, the source opened, the re-made attribution at CD, the card measured, the census (`CONTRAST`, both readings) over the panel, the card, the packets tab and the two named controls; the plates.

**No frozen file touched across the span** (`git diff c96fd46..678fce5 -- src/lib/shape.ts src/lib/ids.ts src/types/geometry.ts src/data/seeds.ts src/lib/cornerCycleName.ts` empty). No new file under the engine roots; the manifest unchanged.

**W1 riders AS FOUND on the record commit:** connection-layer plan (1 insertion, 1 deletion) · claims ledger (81 insertions) · front board (11 insertions, 5 deletions) · sovereign ledger (4 insertions) — each tail-probed by bytes and closing on a finished sentence (the plan on its kill-condition, the ledger on "the chain walk when `C-7e` lands.", the board on "…because you intend a read.", the sovereign ledger on "…gate nothing."); none read for content; not in the freeze manifest, no re-seal — tail-probed for a finished sentence only, not read for content.

## 4 · What I could not reach

- The designer's eye on the foot plate (item 1's judge), on the up-feet's lane, on the drawing's width and on the card's height — the four are hers; the numbers are up in lines 5 and 7.
- Item 8 (the app-wide sweep) — priced, not cut, as ordered.
- The `workspace` tab — not cut: its measured contrast (composited) is above the floor; the meaning question of which instrument defines the floor is line 4.
- Arman's eye on any of C-7f.

## ECHO
`STAMP C-7e` · Δ84 · §116 · `STAMP C-7f` · §117 · §115 · the blind-metric law (§115 §2) applied twice more (the box-overlap count; the alpha-blind census). NEXT ACTOR: the mothership (the ratification; the floor's instrument; the card's second cause; the foot plate to the designer).
