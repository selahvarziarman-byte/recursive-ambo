to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-23 13:06 +0330` (machine — `date`) · the chain `82512e1 → the record commit`, this report the record commit's own letter
re: **`STAMP C-8` LANDED (`82512e1`) — THE BORN ROOM, REACHABLE LAWFULLY: one resolver (`spaceOf`) re-derived at every read, the edge's KIND fixing its J — a seed edge the record, a corner edge the carried injection, a medial edge the MEET by seed content ∪ the born pairs; the researcher's hand-sealed face reproduced to the digit (the station 8 · ABAC 13 · the record alone 18 · matched names 15) and the meet POOLING IN 0 OF 3,000 towers where the parent's rule pools in 986; the loader on the seed's corners alone, by construction; the composed identity marked and never a pair; a born pair taken at the eye; a later act that would break it refused with two hands; the drive family on the lawful path only.** ECHO §120 · §121 · Δ85 · Δ86 · `STAMP C-8` (item 0 first).

## TO THE MOTHERSHIP — eleven lines up

1. **Item 0, both questions, answered before the cut and again by the witness.** (i) On the lawful path (casts on the seed's corners alone, AB and AC mapped by pointing, nothing loaded on a midpoint, the core dissected) the surface at ABAC rendered NOTHING — 0 characters — before this build: your prediction held in the mechanism. (ii) The medial edge AB–AC's record is EMPTY at gen 1 (the octahedron's own edge) and at gen 2: the researcher's risk was real, and it is right that it be so — the composed identity is the resolver's and enters no record (RECORD, NOT READING).
2. **The MEET reads by seed CONTENT and reproduces the researcher's seals on this witness's own generator — the figures are theirs, the shapes mine.** The hand-sealed face reproduced exactly: |A| = 5 · |M_AB| = 8 · |M_CA| = 10 ⇒ the station 8 roles (0 duplicated), ABAC 13 (0 duplicated), the record alone 18, matched names 15. On the record's fixtures at gen 2: ABAC 28 = 21 + 21 − 14, 14 composed pairs, 11 composed words, 0 conflicts, 0 duplicated (the record alone 42; matched names 31). Over 3,000 random towers: at gen 2 the meet IS the shared corner's identity (0 differences); nothing doubles below gen 3 (0); at gen 3 the meet pools in 0 faces where the parent's identity alone pools in 986 (their run: 960) and doubles more in 395; the doubling at Q equals the ratified 09-16 set on every face (0 failures — an independent reproduction, built recursively through the resolver); invariant 9: J_BC re-cast moves ABAC in 0 of 3,000, J_AB re-cast in 2,910 (theirs 2,993); 300 flat total faces double nothing.
3. **A fact the build has that the model does not: the core's dissection mints NO midpoint on a corner edge.** The witness's first run crashed looking for the station on A–AB at gen 2 — it does not exist there; the station stands when the CORNER CELL at A (the residue tetrahedron A · AB · AC · AD) is dissected, and there it is AB's own space (21 roles, 14 carried, no record read — Arman's stations theorem). The researcher's model has both stations and ABAC on one face; the build makes each by its own cell. Said, pinned.
4. **Item 2 — the loader's whitelist, and one thing priced, not cut.** `load cast…` and its input render under `vertex?.createdBy.operation === 'seed'` alone — absent elsewhere, never disabled, no word (your sentence withdrawn as a fourth copy, honoured). A born vertex holding a LOADED cast (a workspace saved before this guard, or the old route) is not read and is SAID — at the vertex and at its child's surface: `AB holds a loaded cast — not read: a midpoint's space is derived from its parents (cast loading is for the seed alone)`. ⚠ Not cut: the inspector's CARD in Panels.tsx still reads `vertex.data.cast` and would show such a cast's rows at a born vertex while the layer ignores it — one sentence on the card (`loaded — not read by the layer`) or one condition; the card is hers; priced at a line, not touched.
5. **Item 3 — the default named for her eye, and the alternative priced.** The composed identity is shown ONCE as a fact in the midpoint's own column (each composed role marked `composed`, the fourth origin word); in the UNFOLDING each parent's picture keeps its shared roles as its own — marked `composed · corner A` in words on the point on BOTH sides, never pickable, no stroke across the fold, no control. The alternative — a third column on the fold holding the shared corner's classes once, with arcs reaching it from both sides — is a layout the column module cannot draw today (an arc lives inside one column); priced at a new composite in `CastInsideDiagram` (cross-column arcs, ~150 lines) — not cut. One more reading for her: in the own column a composed role's label chains the shared corner's word at both ends (`F1 ≡ r0 ≡ Φ1 ≡ F1` — the two F1 are the same seed role, which is why it is one); honest, and hers to shorten.
6. **A mark the eye caught, cured in this cut: the born pair's names.** The first leg run showed the born pair to the person as `A:r3 ↦ B:Φ3` — the glued spaces' KEYS, with the side prefixes your MARKER warned are local (on C–A the prefix `A:` marks C's roles). A mark a person must be told how to read is not yet a mark: every person-facing pair name now goes through the space's own labels (`r3 ↦ Φ3`) — the line across the fold, the refusal and dependency texts and hands, the pick sentence, the unheld line, the trace lines, the sources' act words; the data attributes keep the keys. The dependency at the eye reads `not taken — F1 ↦ Φ3: your pair at ABAC, one generation up, rests on the role this would re-glue — Φ3 is no longer a role there — this act re-glues it` · `here, on A–B: withdraw this attempt` · `at ABAC (one generation up): withdraw r3 ↦ Φ3 first`.
7. **Item 4 — withdrawals are checked too, and no withdrawal that breaks a born act is exhibited.** The dependency reading runs on every act AND every withdrawal (a withdrawal that would break a born act elsewhere is refused the same way, flagged `withdrawal`). At gen 2 no withdrawal can break a born act (a born pair names non-shared roles, and a gen-0 withdrawal only splits shared ones) — the witness has the positive control (a gen-0 withdrawal that breaks nothing goes through) and no falsifier; the case would live at gen 3 with doubling. Said; the guard stands.
8. **Item 5 — the trace carries the record's HOME and the SITE by derivation; the generation an act was MADE at is not stored.** `the record's home: AC–AB, a medial edge (generation 1) · this site: ABAC, generation 2 — a pair beyond the shared corner is born here, yours` — kind and generation derived from what made the vertices. A stored per-act generation would be a field on the frozen `EdgeIdentification` (src/types/geometry.ts, row 90) — a frozen edit, not spent; if the MADE-at generation is what §120 means, that is one line to sanction.
9. **The face at gen 0 stays the seed's.** `FaceRecord` now mounts only when every corner of the face is a seed vertex; a face with a born corner (ABAC's, in the octahedron's cells) is not read — its edges carry born pairs and the solid's identity, the tower's later work. Before this cut the block would have composed born records as gen-0 ones.
10. **The instruments' limit, measured twice this cycle: a server render reads the store's INITIAL state.** The packet editor rendered under node said `No vertex selected.` whatever selection was set, and the chooser rendered under node showed no refusal though the store held one — zustand's server snapshot. So the loader's absence at a midpoint is read AT THE EYE (the leg's §6: 0 inputs, 0 offers, no word at AB; 1 and 1 at A) and pinned in the source; the refusal clauses render the surface from props (the midpoint witness's own way). Filed in memory.
11. **The census moved, said: ten cast readers (the resolver joined; the store and the inside's diagram no longer read `data.cast`), eight identification readers, still exactly two sites** — the candidate record is an option to the read, never a shape written or fabricated.

## 1 · What I SAW

- At ABAC, reached by pointing at AB and AC and dissecting: the unfolding PRESENT — `14 roles and 11 words one by the solid — corner A's, composed, not yours to pair or withdraw · the born room: 5 roles of AC and 6 of AB stand apart · no pair of yours yet`; every one of A's 14 roles marked `composed · corner A` on both sides (28 points), no line across the fold, the own column's 14 composed roles marked `composed`; the home line as in line 8. A composed point clicked picks nothing. Two clicks in the born room: one line, `r3 ↦ Φ3 · yours, born here`.
- Back at AB (gen 2), `F1 ↦ Φ3` refused with the two hands (line 6); the far hand clicked and the attempt withdrawn, `F1 ↦ Φ3` made again is taken (four lines at AB).
- The packets tab at AB: no `load cast…`, no file input, no word; at A: both.
- Not sighted by a person: the designer's eye on items 3–5's surfaces (hers first, per §120); Arman's eye on the born room.

## 2 · What I RAN (verbatim)

- `npx tsc -b` → exit 0.
- `node scripts/diagnose-the-born-room.cjs` (NEW, 21 clauses) → `DIAGNOSE-THE-BORN-ROOM: ALL PASS — one resolver; the edge's kind fixes its J; the meet never pools and reproduces the doubling; the loader on the seed alone; the composed identity never a pair; a born pair taken; a later act that would break it refused by name`. Its notes, verbatim: `gen 1 (30 edges): {"seed":6,"corner":12,"medial":12} · gen 2 (78 edges): {"seed":6,"corner":36,"medial":36}` · `gen 2 (the core dissected): AB 21 roles · AC 21 · ABAC 28 (medial, 14 composed pairs, 11 composed words, 0 conflicts) · duplicated seeds at ABAC {"roles":0,"words":0} · (the corner cell at A dissected): the station on A–AB 21 roles (corner, 14 carried) against AB's 21` · `ABAC: kind-aware 28 roles (sealed by hand 28) · record-only 42 · name-matched 31 (11 names matched)` · `the hand-sealed face: |A| 5 · |M_AB| 8 · |M_CA| 10 · station 8 (dup 0) · ABAC 13 (dup 0) · record-only 18 · name-matched 15` · `3000 random faces (A, B, C: 5 roles, 2 words; random partial injections on the three seed edges; born rooms empty): T1 the gen-2 meet ≠ the shared corner's identity 0 · duplicated seeds below gen 3 0 · T6 POOLING at gen 3 — the meet 0 faces, the parent's rule 986 faces · T3 the parent's rule doubles more 395 · T2 the doubling ≠ the 09-16 set 0 · invariant 9: J_BC varied, ABAC moved 0 of 3000; J_AB varied, ABAC moved 2910 of 3000 (the control)` · `300 flat total faces: doubled 0`.
- `node scripts/diagnose-the-midpoint.cjs` → ALL PASS — its lawful-path note: `C-8 ITEM 0 (the lawful path): ABAC's parents AB (21 roles, derived) · AC (22 roles, derived) · the chooser renders 1 unfolding · composed points 28 · lines across the fold 0 · ABAC 29 roles, medial edge, 14 composed pairs · the shortcut's loaded cast on AB: ignored=true, 21 roles (derived, not Flow's 14) · the notice 2`.
- `diagnose-the-concept-type` ALL PASS (ten · eight · two sites) · `diagnose-the-face` · `diagnose-the-inside` · `diagnose-the-cast-loader` · `diagnose-the-ambo-gesture-truth` · `diagnose-ambo` · `diagnose-workspace-persistence` · `diagnose-the-j-register` ALL PASS · `diagnose-engine-freeze` exit 0 (the new file intent-staged, classified at row 219).
- `node scripts/app-leg/diagnose-the-concept-layer-eye.cjs` (DRIVE FAMILY, on 5199, the lawful path only — the item-0 arm retired) → `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS — both halves of the midpoint's act are in the person's visible box and made by clicks; the midpoint draws its own space; the sources carry the person's neighbouring acts; the acts survive a second dissection; the designer's eight read at the eye; the face refuses and reads with its direction stated; her second cut read at the eye — the lane, the width, the card, the face's lines; the born room reached lawfully, a born pair taken, a later act that would break it refused with two hands` (65 clauses: §0 + 32 per viewport; `/__whereami` = `3292263` = HEAD at the run, dirty paths the C-8 tree; run three times — the first found the born pair shown to the person by its keys (line 6), the second lost its second viewport to a source edit under HMR mid-run, the third whole, on the landed code). Its C-8 notes at 1689 × 897, verbatim:
  - `C-8 the born room: ABAC selected from the cuboctahedron ("ABAC vertex:mid:51cg5d generated midpoint midpoint derived from edge AB - AC") — PRESENT — 14 roles and 11 words one by the solid — corner A's, composed, not yours to pair or withdraw · the born room: 5 roles of AC and 6 of AB stand apart · no pair of yours yet · the record's home: AC–AB, a medial edge (generation 1) · this site: ABAC, generation 2 — a pair beyond the shared corner is born here, yours · composed points 28 (["A"]) · lines [] · free points 5 / 6 · own column composed 14`
  - `the dependency at AB (gen 2): the attempt ["F1","Φ3"] → not taken — F1 ↦ Φ3: your pair at ABAC, one generation up, rests on the role this would re-glue — Φ3 is no longer a role there — this act re-glues ithere, on A–B: withdraw this attemptat ABAC (one generation up): withdraw r3 ↦ Φ3 first · hands ["here, on A–B: withdraw this attempt","at ABAC (one generation up): withdraw r3 ↦ Φ3 first"] · after the far hand: {"lines":["F5↦Φ7","F7↦Φ1","F8↦Φ2","F1↦Φ3"],"refusal":null}`
  - the C-7g notes unchanged: `C-7g the width: the AB drawing 938 px wide in a 1025 px panel (the C-7f run read 1618 in 1025 at 1689 × 897) · the widest positioned line 165 px against WRAP 200 by the geometry's estimate · 7 of its blocks wrapped · the own drawing: widest line 168 px, 11 blocks wrapped` · `foot rows 27 · arc-word box overlaps per column (the blind metric, printed): [{"arcWords":31,"wordWordOverlaps":0,"wordLabelOverlaps":0},{"arcWords":15,"wordWordOverlaps":0,"wordLabelOverlaps":0}]`
- The sweep at `82512e1`, whole, read by eye in its own call: `139 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (sweep exit 0; 138 PASS and the one accepted fail — the census 139 now, the born-room witness in the sweep by its own declaration; the born room 4.0 s, the midpoint 2.4 s, the face 1.8 s, the inside 1.2 s, the loader 3.2 s, the concept-type 15.3 s, the freeze 10.6 s; the j-register PASS at 75.8 s inside its declared ceiling; wall: 95.6s)
- The W1 riders carried AS FOUND with the record commit: none moved since the C-7g record commit (3292263): the ADR 0031 (its working blob still `68ec7cc5`), the connection-layer plan, the claims ledger, the front board, the sovereign ledger and the designer-seat handoff all read `unchanged` by numstat — nothing to carry, nothing staged; none read for content

## 3 · What I CHANGED (`82512e1`)

- `src/lib/spaceOf.ts` (NEW, NOT_FROZEN row 219) — the resolver, the kinds, the generation, the meet, the born acts read under a candidate, the loaded-cast fact, the names.
- `src/lib/midpointGlue.ts` (NOT_FROZEN row 217) — `wordName` on the glued space.
- `src/store/geometryStore.ts` — the acts resolve their ends through the resolver; the composed collision by name; the dependency reading on acts and withdrawals (`MidpointDependency`); `withdrawal` on an act.
- `src/components/MidpointSurface.tsx` — the chooser and the surface through the resolver; the composed marks; the born room's sentence; the home line; the loaded-cast notices; the dependency box; the fourth origin word; the names; the face on seed faces alone; the sources through the resolver.
- `src/components/CastInsideDiagram.tsx` — the panel through the resolver; `src/components/VertexPacketEditor.tsx` — the whitelist.
- `docs/governance/ENGINE_FREEZE_MANIFEST.txt` — the row. `scripts/diagnose-the-born-room.cjs` (NEW). `scripts/diagnose-the-midpoint.cjs` · `diagnose-the-concept-type.cjs` · `diagnose-the-inside.cjs` — re-pinned. `scripts/app-leg/diagnose-the-concept-layer-eye.cjs` + `concept_layer_eye_driver.py` — the lawful arms, the shortcut retired.
- No frozen file. tsc 0.

## 4 · What I could not reach

- The card at a born vertex holding an old loaded cast (line 4) — priced, hers.
- A stored MADE-at generation (line 8) — a frozen edit, not spent.
- A withdrawal that breaks a born act — no case below gen 3 (line 7).
- The designer's eye on items 3–5; Arman's eye on the born room.

## ECHO

ECHO §120 (Δ85 · Δ86; the C-8 charter) · ECHO §121 (the MARKER — item 1 rewritten: the meet, never the parent's identity; items 2–5 the designer's three) · `STAMP C-8` (item 0 first; item 6 — the lawful path only) · the W1 carry (both ADR riders, as found at this commit).
