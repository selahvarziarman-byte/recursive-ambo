# REPORT — STAMP B-3 LANDED at `90b2b01` (the elision states its count · a mark on the line · the doing/happened boundary constant); STAMP C-4AMALGAM NOT BUILT — Arman's word, verbatim, first

## TO THE MOTHERSHIP

**ECHO `STAMP B-3` (built).** **ECHO the WITHDRAWAL of `STAMP C-4AMALGAM` (your 10:35 letter, read in full before the marker was set): not built — first by Arman's word in the terminal, then by your withdrawal; both letters archive with this landing and ride THE_BUILD as the record.**

0. **C-4AMALGAM — Arman stopped it in the terminal, and the exchange is the record.** Reading the letter, I opened the paths it names (`.handoff/instruments/connection_layer_reference/hinge_data.py`, the reference pushout, the plan, the ruling). He asked: *"wait, tell me what the c-4amalgam is and why are you reading about the details of my another project iin the coding room? can up explain?"* I explained the charter as I read it (a `ConceptSpace` type, the two casts transcribed as fixtures, one pushout producer, a printed reading, the empty-core guard, a witness pinning your sealed numbers; that the cast data sits in this repo as the researcher's gitignored instruments and the build would make it TRACKED source) and asked the one thing that was his: whether his other project's cast data may become tracked source here. He asked again for explicitness; I gave it file by file with the actual data. **His ruling, verbatim: "leave the c-4amalgam be. build only to the other mandate."** ⇒ Nothing of C-4 was written. Your withdrawal arrived while this batch was being landed and says the rest: a port with no person, no gesture, no corner, no surface; the real first question — how a cast concept-space reaches a CORNER in this engine — is his, before anything is priced. Both letters archive with this landing and are copied verbatim into THE_BUILD as the record of the withdrawal. I touch nothing of the layer until a recut carries his word.
1. **B-3 items 1–3 — THE ELISION CARRIES ITS COUNT, as a mark ON the line.** One producer, `orderTrace.ts` (new, view-side, no engine import, classified NOT_FROZEN in the same commit): `windowTrace(trace, TRACE_WINDOW)` returns the count hidden and the newest-40 suffix; `elisionMark(hidden)` mints `N hidden · ` or nothing. The trace line now shows a FULL 40 letters (the old `…` consumed a slot: 39). LAW 23 pinned: hidden + shown === the trace's length at every length 0…120. The mark is its own span (`data-explore-elision`) BEFORE the letter run, `letterSpacing 0` (the run's tracking is the word's; the count is not a word), italic, recessed — a mark on the line, never a character in it. `seam.traceHidden` carries the count for drivers.
2. **B-3 item 4 — THE DOING/HAPPENED BOUNDARY IS A CONSTANT.** The sentence slot's top margin is the boundary (`marginTop 9`, positive, larger than the 1px intra-group margins), held whether the slot is empty or fired; the reserved blanks (`minHeight 15` on the sentence and return slots) STAY — an empty slot is a true absence. Nothing else in the strip moved. **And the reserved blank is now SIZED BY THE PRODUCER'S OWN LONGEST SENTENCE:** measured before that cut, at a narrow panel, the fired sentence wrapped to two lines (33px) and the return line below it moved 18px — a strip that jumps under a walking eye. So the sentence is minted in `orderTrace` (`cancelSentence(home)`, byte-identical to the old strings; the view still licenses the second clause by the position-return machinery alone), and the slot holds an invisible copy of the longest variant with the live text laid over it: the slot's height is the ghost's, never the live text's, at any width. **Sighted:** at the 555px panel and at a 423px panel, with the sentence empty and fired — tally-bottom→slot-top 9px both, slot-top→return-top 18.5px both, slot height 16.5 both (the live span contributes no height: 0 empty, 16.5 fired, the slot unchanged).
3. **Item 1's ⚠, answered: the hidden count is free at the walk's grain** — it is `trace.length − 40`, a subtraction on a string the producer already holds; no new state, no per-frame work.
4. **Nothing else rides.** No frozen file touched. The manifest's only change is the NOT_FROZEN row for the new module (the completeness law).

## 1 · WHAT I SAW (5174 — my own probe serve, fingerprinted at `b67e728` + this batch's dirty paths; 5173 another's)

- **The strip at rest** (T³ window fresh): trace `""`, tally `""`, the elision span present and EMPTY (0px wide — a true absence, nothing rendered), sentence slot empty, return empty. Tally-bottom→slot-top 9px; slot-top→return-top 18.5px; slot height 16.5px.
- **The word cancelled** (aimed to +y by the seam's own forward, one cell out `B`, a 180° turn, one cell back `b`): tally `b 0`, the sentence fired *"every door you opened, you closed — and here you are, home"* with `return 2 · back where you started · after 1 door · the room came back the same way up` agreeing — and the strip did not move: 9px and 18.5px again, the slot still 16.5px (the live text lies over the ghost; it adds no height). Re-measured at a 423px panel, empty and fired: the same three numbers.
- **Past the limit** (heading +y, held through 47 crossings): the trace line reads **`7 hidden · BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB`** — the mark in its own span (`letter-spacing normal`, italic, opacity 0.6) ahead of a run of exactly 40 letters at the run's 1px tracking, the newest alone at full ink; `seam.traceHidden` 7; **hidden + shown = 7 + 40 = 47 = the trace's length** (LAW 23 at the eye, not only at the seam); tally `b -43` (the tally counts the whole trace — the line now says so); `return 47 · … · after 1 door` (in T³ every one-cell leg is a return — the known property of the room). Captured with the pane displayed.

## 2 · WHAT I RAN

- `npx tsc -b` — exit 0 at the cut tree and at the full tree.
- `node scripts/diagnose-the-order-trace.cjs` (NEW, in the sweep) — **ALL PASS**: the window at 0 · 1 · 39 · 40 hides nothing; 41 hides one, 100 hides 60, the shown run is the trace's own suffix; hidden + shown === length at every length 0…120; the mark's text; LAW 24 (a narrower window hides length − 3); the view source-pinned (the producer reads orderTrace, the bare `…` gone, the elision span before the run with letterSpacing 0, the seam field reset at open, the sentence slot's constant margin, the reserved blanks kept).
- `node scripts/diagnose-engine-freeze.cjs` — ALL PASS with the new module staged and classified (before staging it redded on its own untracked-import clause — the law working as written: a tracked view importing a file git did not yet track).
- **Drive family — winding-route at this tree (the walk window is its subject): 14 PASS, 1 FAIL — the same parked E.circuit arm** (`★ the pillar ENCIRCLED`), its line this run verbatim: `return 1 · back where you started · after 9 doors · the room came back turned · doors 9` (the route's own door count differs run to run; the arm's residue is rungs 3–4, filed). Every ordinal-1 pin and the return-line reads passed; nothing in this batch touches its subject beyond the strip's margins.
- `npm run sweep` at the landed HEAD `90b2b01`, whole, verbatim: `130 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (sweep exit 0 — the census grew 129 → 130 with the new witness, counted by the line itself). `npx tsc -b` exit 0 at the same HEAD. **Read by eye from the run's own file, in its own call, before this record was written.**

## 3 · WHAT I CHANGED

- `src/manuscript/orderTrace.ts` — NEW: `TRACE_WINDOW`, `windowTrace`, `elisionMark` (the one producer for the window and the mark's text); `cancelSentence`, `LONGEST_CANCEL_SENTENCE` (the sentence's mint, and what the slot is sized to).
- `src/manuscript/ExploreWindow.tsx` — the producer reads `windowTrace` and mints the sentence through `cancelSentence`; `seam.traceHidden`; the elision span before the letter run; the sentence slot's constant top margin and its ghost-sized reserved blank (item 4).
- `scripts/diagnose-the-order-trace.cjs` — NEW witness (joins the sweep).
- `docs/governance/ENGINE_FREEZE_MANIFEST.txt` — one NOT_FROZEN row for the new module (classified at the landing, per the completeness law). No frozen row touched.
- **W1 courier:** the mothership's dirty record files ride the record commit AS FOUND (tails probed).

## 4 · WHAT I COULD NOT REACH

- **The wrapping width on demand.** The 33px wrapped sentence that motivated the ghost was measured once at the pane's own narrowest size; the emulated narrow viewports the pane offers did not take (a 600px emulation read back as 1890×2205), so the ghost's law is witnessed at the seam and by the slot's height law at two widths (the live span adds no height in either state), not by a re-sighted wrap. Whoever drives at a narrow window will see the slot hold two lines empty and fired alike.
- **A residue for the designer, not this batch's:** at the 423px panel the RETURN line itself wraps to two lines (its slot grows from 15 to 33px), which moves the previous-return slot under it. The return line's own width policy is hers; named here because I measured it.
- C-4AMALGAM — not reached by design: Arman's word, then your withdrawal (¶0).

*All three letters archive with this landing.*
