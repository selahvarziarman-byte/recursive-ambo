to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-24 23:43 +0330` (machine — `date`) · HEAD == origin == `2ab8f9c` (the STAMP `caffcf5` + the cure `2ab8f9c`) · the record commit follows this letter
re: **`STAMP C-12b` LANDED — THE STONE: M⁺, the born concept carries its opposite corners' FEET, IN the signature (§145; the §147 MARKER ruled (a)). The foot composed by the resolver at every read through C-9's one reader; holds-only, never a refusal's cause; carried by the lift, the next pushout (as foreign words) and the door, not by the cargo; WORDS under the own column in the designer's grammar and order. The reference's four runs reproduced number for number and the tower through the app's own resolver; the eye leg ALL PASS with §14; every moved number the feet's alone. ECHO §145 · §147. The last build before Arman's use.**

## TO THE MOTHERSHIP

1. **Measured, not ruled — how the foot words READ at the next generation (your MARKER's ask, for the designer).** At ABAC the word rows carry the parents' feet as foreign words: `≡_C` (from AB), `≡_B` (from AC), and the two feet of D told apart by the lone-name bracket — `≡_D [AB]` · `≡_D [AC]`; ABAC's own three feet read `≡_A` · `≡_AD` · `≡_BC`. They are pairable as words by τ like any other (nothing special-cases them) — the designer's to rule.
2. **Two silent-line wordings are the coder's, beside the designer's one.** Her line covers both edges unpaired (`D says nothing about A–B — you have paired nothing on A–D or D–B yet`). When exactly one edge is paired the line names it alone (`… you have paired nothing on A–D yet`); when both are paired and nothing composes it reads `… your pairings on A–C and C–B do not meet`. Both are positive marks in her grammar; hers to amend.
3. **One foot per DISTINCT triangle, by vertex set — measured to matter.** The dissected core's own marker cell keeps the coarse face beside the residue's copy, so AB–AC at gen 2 met the face AB·AC·AD twice; the resolver counts it once (ADR 0031 §3.10), and ABAC carries exactly the ADR's three feet: the corner cell's face (foot of A, all loops), the interior face (foot of AD, all loops), the seed-boundary face (foot of BC, which carries the seed face's disagreement forward — links).
4. **Parked, one line each:** a caster's own word beginning `≡_` would read as a foot (said, not enforced); the midpoint's own column keeps M's counts in its head line while the card counts M⁺ (the feet's words and tuples added — each moved number is the feet's).

## What I SAW — the drive family, `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS` (133 clauses — 131 → 133, one new arm per viewport; `/__whereami` = `2ab8f9c`, dirty — the C-12b tree; the leg ran three times on this cut — the first, on the STAMP's tree, found ONE clause moved: §6's card row at AB, `22 words · 52 tuples` → `24 words · 55 tuples`, the feet's alone (two foot types `≡_C` · `≡_D`, silent or not; C's three proposal links), no other clause moved — §6 re-pinned to COMPUTE its counts from the foot blocks measured on the same screen, never restated; the second whole on both viewports (133, served `4cd39e8`, dirty 12 — the STAMP's tree); the third whole at the cure's HEAD (133, served `2ab8f9c`, dirty 4 — the claims ledger and the excluded build files), the blocks C then D at both viewports)

At 1689 × 897 (and at 1400 × 900): at AB with (i) on A–B, S1 on A–C and Q on B–C, **two blocks stand inside the midpoint's own column, under the drawing, in the unfolding's order.** C's, headed `C's view of your pairing on A–B — read from your pairings on A–C and C–B`, reads three conditional sentences — `would join what you left apart: F13 with Φ8` · `would join what you left apart: F9 with Φ1` · `would join what you left apart: F3 with Φ2` (nothing on A–C's route is paired on A–B, so nothing agrees and nothing is contradicted; the head leads); D's reads its one silent line, `D says nothing about A–B — you have paired nothing on A–D or D–B yet`. No control inside either; the lines in the head's own size, weight and colour (`12px · 400 · rgb(214, 211, 209)` for the head and every line, both viewports); no foot drawn as a glyph in the own drawing. Plate `concept-layer-feet-*` in the ignored `_frames/`. Everything C-11 and C-12a pinned at the eye stands unchanged beside it — the door's act, the cargo through both rooms, the way back.

## What I RAN (verbatim tails)

- `scripts/diagnose-the-stone.cjs` (NEW, 16 clauses) — `DIAGNOSE-THE-STONE: ALL PASS — the born concept carries its opposite corners' feet, in the signature, as words under the own column; the four runs reproduced, the tower through the app's own resolver`. **§1 the reference's R1–R3 on the 42 hand triples, by a port of its `links`, number for number:** R1 distinct feet per J_FT `[6, 6, 6, 6, 6, 6, 6]` (control 0 differences) · R2 0 failures, the flat control 0 · R3 (a) 0 · (b) 0 · (c) 0, **the EXCESS on 40 of 42 triples, 77 roles** (the KILL condition), the kinds over 42 × 14 roles `{UND 497 · PRO 46 · DIS 39 · FIX 6}`, the per-triple histogram's eleven classes, the 1,000-random flat control 0. **§2 R4 the tower through the APP's resolver** (the second implementation of the researcher's build — A flow · B t-cell · C phi · D triangle, the J's given on the seed's edges, the core and the corner cell dissected, the feet read at the born midpoints): **gen-0 non-loop 307 · medial 573 · medial ⇔ gen-0 disagreements 0 · the corner triangle's 126 feet all loops** — the reference's sealed numbers exactly (705 ms). **§3** the feet at AB in the eye's state — two silent feet with pairs on A–B alone; after S1 and Q, C's foot `{F13 ↦ Φ8 · F9 ↦ Φ1 · F3 ↦ Φ2}` all proposals, D silent; M's points 20 both ways, J_AB untouched, holds-only; the counts moved by the feet alone — words 25 → 27, tuples 56 → 59; the drawing 46 arcs · 10 loops as M's, 3 feet left to the words; the `feet: false` control byte-as-before. **§4** the lift re-derives the feet on the carried record (the same three proposals, 27 words at AB); the next pushout carries the parents' feet as foreign words and derives ABAC's own three afresh (words 31 → 38); `refusalOf` on AB–AC equal with and without the feet over every pointed pair (360, 0 differ). **§5** the surface rendered under node — the blocks in the designer's words and order, no control, no `[C]`/`C:`; `agrees on 1: F13 ≡ Φ8` leading when F13 ↦ Φ8 is paired on A–B, `would pair F13 otherwise: with Φ8 — you paired it with Φ5` when F13 ↦ Φ5 is; the own drawing free of foot glyphs; the manifest row; purity.
- `npx tsc -b` — exit 0. `scripts/diagnose-engine-freeze.cjs` — ALL PASS (feet.ts intent-staged, classified NOT_FROZEN in the STAMP).
- The drive family: `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS` (133 clauses).
- **The sweep, whole, read by eye in its own call** — two sweeps. At `caffcf5` (the STAMP): `145 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP RED — unexpected fail(s): scripts/diagnose-the-born-room.cjs · scripts/diagnose-the-inside.cjs · scripts/diagnose-the-midpoint.cjs` (wall 100.1 s) — **none of the three a count**: the born room §5 and the inside §5 pin the resolver's and the inside builder's IMPORT LISTS (5 and 2 — now 7 and 3, the feet's name and C-9's reader), the midpoint §4 its Δ80 guard against any sort call in the surface (my block ordering used one); every count-bearing witness the MARKER named passed unchanged — the cargo (K1–K7 number for number, its own pins), the door column, the doors act, the j-register, the concept type, the lift carries, the found bugs, the born face, the keyed walk. Cured at `2ab8f9c`. At `2ab8f9c`, before this record was written: `145 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (exit 0 — the census 145, the stone by its own declaration at 2.4 s, the cargo 2.7 s, the lift carries 4.9 s, the midpoint 10.2 s, the inside 13.7 s, the born room 53.8 s; the door column 79.8 s, the j-register PASS at 66.1 s inside its declared ceiling; wall 100.2 s).

## What I CHANGED

- `src/lib/feet.ts` (NEW, NOT_FROZEN, a leaf) — `footTypeName` (`≡_<label>`) and `isFootType`.
- `src/lib/spaceOf.ts` (NOT_FROZEN) — `Foot`, `Resolved.feet`, `SpaceOfOptions.feet` (the control); `facesHolding` (one per distinct triangle), `feetOf` (each edge's J through `bornStepOf` — a deferred import of bornFace, the cycle resolving at call time; the three states against the class holding both an A-role and a B-role; the links on M's points), `withFeet` (M⁺'s signature and relations, holds-only); the midpoint branch composes them, the feet's word content and name segments their own, the drawn amalgam (`glued`) left as M.
- `src/lib/castInside.ts` (NOT_FROZEN) — a foot's tuple never a glyph; `census.feet`.
- `src/components/MidpointSurface.tsx` — the blocks under the own drawing (`data-midpoint-foot` · `-state` · `-head` · `-line`), in the sources' apex order.
- `docs/governance/ENGINE_FREEZE_MANIFEST.txt` — the row for feet.ts. `scripts/diagnose-the-stone.cjs` (NEW). The driver (`MEASURE_FEET` after the C-5 face is read) and the leg (§14 NEW; §6's card clause RE-PINNED by the feet alone — the first run found the card's Space row at AB moved `22 words · 52 tuples` → `24 words · 55 tuples`, no other clause moved; the clause now COMPUTES the expected counts from the foot blocks measured on the same screen: one word per foot's type (2: `≡_C` · `≡_D`, silent or not), one tuple per point a foot reads (3: C's three proposals; an `agrees on N` line N loops), M's 20 roles untouched — never restated).
- **The cure, `2ab8f9c`** — `src/components/MidpointSurface.tsx`: the foot blocks ordered by the unfolding WITHOUT a sort (each apex's foot taken in its turn; an apex stood twice places its foot once; a foot the unfolding does not stand — none, measured — follows in the resolver's order), the Δ80 guard left standing unweakened (my first cure's own comment quoted the banned call and tripped the same guard — reworded); `scripts/diagnose-the-born-room.cjs` §5 and `scripts/diagnose-the-inside.cjs` §5 re-pinned on the import lists with the two new imports NAMED and their own purity pinned (feet.ts a leaf with no import; bornFace importing no store, component, manuscript or three). No behaviour moves: the midpoint 58 · the inside 33 · the born room 34 · the stone 16 ALL PASS on the cut; tsc 0.

## What I could not reach

- Nothing owed by §145/§147 is left. The eye ran three times on this cut (below); nothing worked around.

## PARKED (one line each)

- A caster's own word beginning `≡_` would be read as a foot (said; not enforced by the loader).
- The own column's head line counts M (the amalgam); the card counts M⁺ — the difference is exactly the feet's words and tuples.
- At the unglued state (no pair on A–B) the feet are composed but not shown — the own column does not exist there; a block of nothing but proposals would put a proposal first.

## W1 — the courier, at the record commit

- **ADR 0031** (`docs/adr/0031-the-concept-layer-is-a-cosheaf-of-partial-symmetries.md`) — byte-equal to HEAD: `blob e7565a44` = `sha256-LF b0795661` (`git hash-object` · `tr -d '\r' | sha256sum`), mtime 19:45:24; nothing to carry, said. (Your 1941 named `833d63f7`, your 1947 `e7565a44` — the latter is what HEAD has held since `4cd39e8`.)
- **The researcher's ruling** (`.handoff/RULING_THE-INSIDE-MIDPOINT-TRACE-REFUSAL_on-the-honesty-line_midpoint-is-the-pushout-over-the-CORE_trace-is-its-origin-partition.md`) — byte-equal to HEAD: `blob c54fa6f2` = `sha256-LF 862f90de`, mtime 19:45:46; nothing to carry, said (carried at `4cd39e8`; its tail begins `**Ruled (a).**`).
- **The claims ledger** (`.handoff/THE_CLAIMS_LEDGER.md`) — CARRIED AS FOUND, not read for content, no re-seal: `blob d650e2e9` → `3893c888` (`sha256-LF cc94efb9` → `1e802ada`), numstat 1/0, mtime 22:38:36 — one line by its clock (its head reads the §148 ruling-1 LANDED entry); the tail's last characters: `ord holds seven. *(22:39)* ⇒ **`C-12b` (the stone) is the last build before Arman's use.**` (closing on a finished line).
- **The sovereign ledger** — byte-equal to HEAD: `blob 2d731659` = `sha256-LF c74b98ce`, mtime 19:38:46; nothing to carry, said.
- **U1** (§126.2, every wave at each record commit): the sweep run from its home at `2ab8f9c` finds NO new wave — 0 rulings, 0 instruments (candidates 75); `.gitignore` regenerated whole and unchanged; the sweep's standing AMBIGUOUS line (a truncated head `RESULTS_2026-09-19_` naming several files counts for none) printed as it always is. Nothing tracked in this record commit beyond the record pair and the one rider.
- **The inbox**: the 1941 letter and the 1947 MARKER archived with this landing; the coder's inbox holds nothing — the ball is the mothership's.
