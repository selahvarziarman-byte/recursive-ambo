to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-23 09:58 +0330` (machine — `date`) · the chain `08b9a77 → the record commit`, this report the record commit's own letter
re: **`STAMP C-5` LANDED (`08b9a77`) — THE FACE at gen 0: the residue reads the researcher's seal to the digit at every corner, every `Und` role carries its address, the direction is stated, and THE FACE IS REFUSABLE — the guard attempted at every corner, a refusal naming four things with three hands as controls. ITEM 0 MEASURED and printed ahead of your ruling: a pair between two BORN corners is reachable, in the mechanism and at the eye; built for neither outcome.** ECHO §118 · `STAMP C-5` (item 0 first) · Δ84.

## TO THE MOTHERSHIP — ten lines up

1. **ITEM 0 — THE MEASUREMENT (1705 §4), the route with it.** *In the mechanism:* the midpoint witness §4 loads casts onto the gen-1 midpoints AB and AC (born corners), dissects the core, and finds the gen-2 midpoint `ABAC` with a site whose parents are AB · AC; the chooser renders the UNFOLDING for it (a surface for pairing); the store's `giveRolePair` on the AB–AC edge WRITES `{"roles":[["Φ1","F1"]]}` — a pair whose two roles no gen-0 record relates. *At the eye* (the leg, both viewports): the person's route is — at gen 1, select the core in the tree, select AB in the selection tab, packets tab → `load cast… (.cast.json)`; the same for AC; select the core, `Apply Ambo Dissection`; select the cuboctahedron in the tree and `ABAC` in the selection tab → the midpoint surface with both halves; two clicks give the pair. At 1689 × 897 (the same at 1400 × 900) the leg printed: `a surface for pairing: PRESENT — no pair given yet — AC and AB stand apart: 23 roles · 25 words · 56 tuples · 23 marks · a pair given by two clicks: ["Φ1↦F1"] — 1 role pair · 0 word pairs — yours` (the casts loaded onto AB and AC read `taken — 14 roles · 12 relation-types · 48 relations …` and `taken — 9 roles · 15 relation-types · 31 relations …`; the tree row `ABAC vertex:mid:… generated midpoint · midpoint derived from edge AB - AC`). The plate `concept-layer-item0-born-parents-1689x897.png`: `ABAC · the midpoint between AC and AB — the two faces unfolded about their edge`, the source `BC · through face AB·BC·AC · holds no cast`, both word rows, the line `Φ1 ↦ F1 · yours · withdraw` across the fold. ⇒ **the surface exposes an edge between two born vertices for pairing, and §3.5 as written is violable by a person today, by that route.** Nothing was built for either outcome: the face is gen 0 only (the seed's faces through the AB site's sources), the chooser is untouched, the writer is untouched. The witness clause pins the mechanism as a MEASUREMENT (the ruling flips the clause, not the mechanism — C-7c's pattern).
2. **THE SITING (1705 §4.7 — mine; a default named).** The face's reading stands at the MIDPOINT, beside the opposite corner seen through that face: the source there already carries the person's acts on the two edges that reach it, and the face is those two edges and this one, walked. Measured before choosing: a face is not selectable in the store (no `selectedFaceId`, no `selectFace`; the inspector's `Face` section lists the faces of a selected vertex) — so a face-selection site would have needed a new selection kind, a scope word. The block reads `the face A·B·C, walked A → B → C → A …` under the source's acts line, above `open the drawing`. Hers to rule; the numbers at his viewport are in the leg's notes below.
3. **THE DIRECTION IS D14's AND ONLY ONE IS SHOWN.** The walk starts at the alphabetically-first corner and runs the face's own way (`d14NameRotation` from the frozen composer — a rotation, never a reversal), and the block says so: `the reading is the walk's; the other way round reads differently`. A control to walk the other way is NOT built (A5: 1,503 of 4,000 of this witness's random triples read differently reversed) — a default; yours or hers to ask for.
4. **A 2-CYCLE NEEDS TWO WITHDRAWALS — measured, and the surface says which.** `(i)+B'+P` at T merges r2 ≡ r0 through two Mov pairs (r0 ↦ r2 and r2 ↦ r0); withdrawing one of the first path's three hands leaves the other path merging them, and the block reads NO FACE again with the other path's three hands. The hands offered per merged pair are the path's over the Mov pairs (BFS on the merge graph) — three per Mov pair; a person walks it down. Said, not hidden; the witness pins it.
5. **A HAND WITHDRAWN HERE CHANGES ANOTHER MIDPOINT'S RECORD.** The three hands live on three edges; the AB surface's hand `withdraw r1 ↦ Φ1 on B–C` calls the store's own `withdrawRolePair` on the B–C edge — the BC midpoint's record loses that pair (and any pending attempt there is re-made by the existing rule). The button names the edge; the record is one. Said.
6. **THE MARK REFUSAL UNDER THE MOLD'S RULE — a definition note for the researcher.** The instrument's corner-local check compares mark values raw (`v != seen`); the engine compares through the ratified join (`valuesAgree`: has ⊔ unrecorded = has, C-7pre) — so a merge of `has` with `unrecorded` does not refuse here, `has` with `none-by-nature` does. No seal number depends on it (the first face's marks are all `has` but Φ9's `none-by-nature`, never merged in the 42). If the instrument's raw comparison is the definition, one line changes; I took the ratified rule.
7. **THE RANDOM COUNTS ARE THE RESEARCHER'S RUN'S, NOT REPRODUCED.** 7,204 · 5,860 of 20,000 and 524 of 4,000 come from Python's generator at their seeds; this witness runs its own generator (4,000 triples; 2,000 corners) and pins the SEALED SHAPE of each law — A1–A4 at 0 failures, A5 · A6 > 0, B1 > 0, the projection identity at 0 with its control failing on 1,680 — never their figures. Quoted from the named run in the module's head, as the rule requires.
8. **THE LOWER BOUND, kept as one.** The check is corner-local; the cross-corner colimit with the τ's is unbuilt (1705 §4.6's own scope). The module's head and the witness name it a lower bound; the surface never prints a count of refusals as "the" count — it prints the refusals it found, each in four things.
9. **`domain` AND `gap` ARE PARAMETERS WITH THE RULED DEFAULTS; ORIENTATION IS NOT.** `ambient` (ruled) reads Und against the corner's own roles; `core` shrinks the reading to dom h (Und empty — measured as the un-ruled alternative, present in the function, absent from the surface); `gap` classifies a break (ruled: every break is Und with its address; `excluded` measured). Orientation is read from each edge's `vertexIds` at the moment of reading — the same records stored the other way round give the same reading and the same refusal with each hand in its edge's own orientation (pinned); nothing stores a direction (source-pinned).
10. **THE CONE-DEFICIT (§3.3) is not built** — the letter named it only as presupposing the colimit; on a refused face nothing else is read, and on a read face the block carries `Fix · Mov · Und` and the derived core, no deficit. Yours to charter if wanted.

## 1 · What I SAW

**The face refused** (`_frames/concept-layer-face-refused-1689x897.png`, the AB midpoint scrolled to the block): a rose box at the top of the panel — `the face A·B·C, walked A → B → C → A — NO FACE: the three acts around it, composed, make a corner's own record say two things about one tuple; the edges keep their records` · `C's own record: sustains(r8, r0) does-not-hold against sustains(r1, r0) holds — with r8 and r1 made one, one tuple with two values; merged by the walk through A–B · B–C · C–A · withdraw one of the three acts: withdraw r8 ↦ F7 on C–A · withdraw F7 ↦ Φ1 on A–B · withdraw Φ1 ↦ r1 on B–C` — three underlined controls, each naming its edge. (At the eye the corners are A = Flow, B = Φ, C = the T cell, so the walk runs the instrument's face the other way; the refusal is the same merge — orientation-independent, as the module measures — and the hands read in each edge's own stored orientation.)

**The face read** (`concept-layer-face-read-1689x897.png`, after the first hand `withdraw r8 ↦ F7 on C–A`): `the face A·B·C, walked A → B → C → A — the reading is the walk's; the other way round reads differently` · `at A — returned to itself: none · returned elsewhere: F7 as F9 · F8 as F3 · did not return: F1 F2 F3 F4 F6 F9 F10 F11 F12 F13 F14 broke at A–B · F5 broke at B–C · the face's core at A, derived: 2 of its 14 roles` · `at B — … did not return: Φ3 Φ4 Φ7 Φ9 broke at B–C · Φ5 Φ6 broke at C–A · Φ1 Φ2 Φ8 broke at A–B · the face's core at B, derived: 0 of its 9 roles` · `at C — … r3 r5 r7 r8 r9 broke at C–A · r0 r1 r2 r4 r6 broke at A–B · the face's core at C, derived: 0 of its 10 roles`. The block is 102 px tall at y 109, inside the panel's box (the leg's note); dense, in the panel's own sentence size — the designer's eye is the judge of the lines.

**The guard in words** (the leg, before B–C held a record): `the face A·B·C, walked A → B → C → A: no reading yet — it needs a record on each of its three edges; none on B–C`.

**Item 0 at the eye** (`concept-layer-item0-born-parents-1689x897.png`): line 1 above. The sidebar there also shows the cuboctahedron's two operations (`Apply Ambo Dissection` · `Apply Pyritohedral Diagonalization`) — untouched by this cut, noted only.

**Not sighted by a person:** the designer's eye on the face block (its site, its lines); Arman's eye on the face; your ruling on item 0.

## 2 · What I RAN (verbatim)

- `npx tsc -b` → exit 0 (with the new module and the surface).
- `git add -N src/lib/faceReading.ts scripts/diagnose-the-face.cjs` then `node scripts/diagnose-engine-freeze.cjs` → `ALL PASS`, exit 0 (the manifest row for the module in the same commit).
- `node scripts/diagnose-the-face.cjs` (NEW) → `DIAGNOSE-THE-FACE: ALL PASS — the residue reads the seal at every corner, the direction is stated, every Und role has its address, the face is refusable and names four things with three hands` (24 clauses). Its notes, verbatim: `42 triples · Mov pairs Flow 76 · T 39 · Φ 45 · corner-local refusals Flow 0 · T 7 (in 6 triples) · Φ 0`; `4000 random triples of partial injections (6+6+6 roles, this witness's own generator): A1 0 · A2 0 · A3 0 · A4 0 failures · A5 1503 · A6 1251 occurrences · projection identity 0 failures · the reversed convention fails on 1680`; `2000 random corners with declared negatives, one Mov pair each: refusals 613 · one instance: merging x0 ∼ x2 — q(x1, x0) holds against q(x1, x2) does-not-hold — once x0 ≡ x2 · hands x0 ↦ y on xy · y ↦ z on yz · z ↦ x2 on zx`.
- `node scripts/diagnose-the-midpoint.cjs` → `DIAGNOSE-THE-MIDPOINT: ALL PASS` (58 clauses); item 0's note verbatim: `ITEM 0: the gen-2 midpoint ABAC — parents AC · AB (both born at gen 1) · the chooser renders 1 unfolding · the store's giveRolePair on AB–AC wrote {"roles":[["Φ1","F1"]],"types":[]} — REACHABLE in the mechanism; the surface's reachability at the eye is the drive leg's`.
- `node scripts/diagnose-the-concept-type.cjs` → ALL PASS (eleven cast readers, seven identification readers, the two sites unchanged).
- `diagnose-the-inside` · `diagnose-the-cast-loader` · `diagnose-the-ambo-gesture-truth` · `diagnose-the-j-register` → exit 0 each.
- The DRIVE FAMILY leg, spawned on 5199 at this tree, fingerprinted:
- `node scripts/app-leg/diagnose-the-concept-layer-eye.cjs` (DRIVE FAMILY, spawned on 5199, `/__whereami` = `2859a06` = HEAD at that run, dirty paths 15 — the C-5 tree) → `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS — both halves of the midpoint's act are in the person's visible box and made by clicks; the midpoint draws its own space; the sources carry the person's neighbouring acts; the acts survive a second dissection; the designer's eight read at the eye; the face refuses and reads with its direction stated` (51 clauses: §0 + 25 at 1689 × 897 + 25 at 1400 × 900). Its C-5 notes, verbatim:
  - `the face's reading at the eye (fix · mov · und · core per corner): ["A 0·2·12·2","B 0·0·9·0","C 0·0·10·0"] · the block {"y":109,"h":102,"insidePanel":true} · the refusal's hands ["edge:2f1akb|r8|F7","edge:251ovc|F7|Φ1","edge:dy8738|Φ1|r1"]` (at 1400 × 900 the block is 118 px tall, inside the panel)
  - `ITEM 0 (1705 §4): … a surface for pairing: PRESENT — no pair given yet — AC and AB stand apart: 23 roles · 25 words · 56 tuples · 23 marks · a pair given by two clicks: ["Φ1↦F1"] — 1 role pair · 0 word pairs — yours` (both viewports)
  - the reachability numbers with the face block above the word half: the word half begins at y 238 (was 220 at C-7f) and the drawing's top at 386 (was 368) at 1689 × 897 — both inside the panel's box (108–817) at scroll 0; 238 / 412 in 108–820 at 1400 × 900.
  - the first run of the leg failed its own gen-2 carry clause because the face arm had changed the AC record the clause compared against a fixed count (one pair became five); the clause now compares gen 2 against the records measured just before the dissection — the carry's own claim — and the leg was re-run whole.

**The sweep at `08b9a77`, whole, read by eye in its own call:**
```
THE SWEEP — 138 legs · 23 workers · per-leg ceiling 420s
…
FAIL    scripts/diagnose-dual-inspection.cjs  13.2s
…
PASS    scripts/diagnose-engine-freeze.cjs  10.2s
PASS    scripts/diagnose-the-j-register.cjs  62.7s
PASS    scripts/diagnose-the-concept-type.cjs  14.8s
PASS    scripts/diagnose-the-ambo-gesture-truth.cjs  3.3s
PASS    scripts/diagnose-the-cast-loader.cjs  2.8s
PASS    scripts/diagnose-the-midpoint.cjs  2.0s
PASS    scripts/diagnose-the-inside.cjs  1.3s
PASS    scripts/diagnose-the-face.cjs  1.7s

slowest: the-gate.cjs 78s · the-door-column.cjs 75s · the-j-register.cjs 63s · the-noncube-domain.cjs 43s · the-field-in-the-specimen.cjs 33s
wall: 91.6s (sum of legs 1919s)

138 files · expect exactly ONE fail: diagnose-dual-inspection
SWEEP OK — the one expected fail, nothing else
```
(sweep exit 0; 137 PASS lines and the one accepted FAIL, read by eye; the census is 138 now — the face witness joined the sweep by its own declaration; the j-register leg PASS at 62.7 s inside its declared ceiling.)

## 3 · What I CHANGED (`08b9a77`)

- `src/lib/faceReading.ts` (NEW, NOT_FROZEN — the manifest row in this commit) — `readStep` (the record read in the walk's direction from the edge's `vertexIds`), `walkOf` (the empty-core guard), `composeThroughCorner` (the residue, `Fix · Mov · Und` with addresses, the derived core, `domain` / `gap`), `cornerRefusals` (the guard: union-find over the Mov pairs inside the corner's own record; tuples and marks; the hands as the path's acts in each edge's own orientation), `faceOf` (absent · refused · read), `undByStep`, `firstBreak`, `edgeBetween`. Imports the types and the register's `valuesAgree` only.
- `src/components/MidpointSurface.tsx` — `ProjectionSource.cycle` (D14's order from `d14NameRotation`); `SourceRecord` mounts `FaceRecord` under the source's acts line; `FaceRecord` — the three states in words, the four things, the three hands as buttons through the store's `withdrawRolePair`, the direction stated, `returned to itself` · `returned elsewhere (x as y)` · `did not return … broke at` · `the face's core at X, derived`.
- `scripts/diagnose-the-face.cjs` (NEW, 24 clauses) — the seal, the structure, the guard, orientation at reading, the empty-core guard, the parameters, the store and the surface by behaviour, the boundaries, the manifest row.
- `scripts/diagnose-the-midpoint.cjs` — §4 item 0 measured (printed, not blessed); the surface's action reads six (the face's one hand).
- `scripts/diagnose-the-concept-type.cjs` — eleven cast readers, seven identification readers, each named.
- `docs/governance/ENGINE_FREEZE_MANIFEST.txt` — `NOT_FROZEN src/lib/faceReading.ts` after the midpointGlue row.
- `scripts/app-leg/diagnose-the-concept-layer-eye.cjs` + `concept_layer_eye_driver.py` — §5: the guard in words, the face refused with three hands as controls, one hand clicked and the face read with the direction stated, item 0 walked and printed; the plates `concept-layer-face-refused-*`, `concept-layer-face-read-*`, `concept-layer-item0-born-parents-*`.

**No frozen file touched** (`cornerCycleName.ts` imported, not edited; `git diff 2859a06..08b9a77 -- src/lib/shape.ts src/lib/ids.ts src/types/geometry.ts src/data/seeds.ts src/lib/cornerCycleName.ts` empty).

**W1 riders AS FOUND on the record commit:** the designer-seat handoff (1 insertion) · connection-layer plan (1 insertion, 1 deletion) · claims ledger (40 insertions) · front board (7 insertions, 5 deletions); the sovereign ledger unmodified this cycle — each tail-probed by bytes and closing on a finished sentence (the handoff on "You do not file beside it.", the plan on its kill-condition, the ledger on "THE CHAIN and then THE FACE.", the board on "…because you intend a read."); none read for content; none in the freeze manifest, no re-seal — tail-probed for a finished sentence only, not read for content.

## 4 · What I could not reach

- Your ruling on item 0 — the measurement is up (line 1); nothing built for either outcome.
- The designer's eye on the face block; Arman's on the face.
- The cross-corner colimit with the τ's (the necessary condition) — unbuilt, the count kept a lower bound.
- The face walked the other way as a control; the cone-deficit — not chartered, named (lines 3 and 10).

## ECHO
§118 · `STAMP C-5` · Δ84. NEXT ACTOR: the mothership — item 0's ruling; the ratification; the face block to the designer.
