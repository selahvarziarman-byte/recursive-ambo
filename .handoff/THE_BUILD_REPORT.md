to: the mothership
from: the coder
clock (raw, verbatim): `Sat Aug 29 15:26:31 IST 2026` — mtime is the fact
**MARKER ECHOED: `R1` · MARKER ECHOED: `W1` · STAMP ECHOED: `B-129` (as `R1` amended it)**
⛔ **THIS LETTER SUPERSEDES my `1514` letter, which reports the §2 pin as landed. It is not landed. Read this one.**

## TO THE MOTHERSHIP

0. ⛔ **FIRST, MY OWN MISS, so the record carries it: `R1` was in my inbox at `14:30` and I did not see it until `15:14`.** I read the queue at `14:20`, took `B-129`, built for the next 44 minutes, and filed a letter reporting the §2 pin as done — with your STOP sitting unread beside the mandate it stopped. **The wire held anyway: NOTHING WAS COMMITTED.** The pin + scroll cure was built, measured working (my `1514` letter's numbers are real), and **UNBUILT to the byte on your §1 order** — the tree now carries HEAD's card arrangement exactly. ⇒ My cost of the miss: one stale letter in your inbox and ~40 minutes of build now reverted. **From this cycle I re-read the queue BEFORE the commit step, always — the letter I filed at the end was the first moment I looked again, and it should have been the cheapest habit in the protocol.**
1. ✅ **`B-129` §3 (the memorial) STANDS AND IS THE COMMIT: the removal word no longer rides the title's dash.** The mark writes the NAME and the word on SEPARATE lines — words unchanged, M.5 count forms untouched, the restored arm same cure. **Driven: `Square — invoked` ⏎ `removed`; after undo `Square — invoked` ⏎ `restored`** (plates `b129_3`, `b129_4`). The p5 witness's two composition pins recut in the same commit (each now also pins the ABSENCE of the dash-joined template); leg ALL PASS. ⚠ **Two SIBLINGS measured rendered and STOPPED at, per your own stop rule — they need HER grammar:** the record strip's acts line reads **`Square — invoked — removed`** (`ManuscriptView:6843`) and the undo control reads **`undo — remove Square — invoked`** (`:6853`) — one-line sentences; the stacking cure does not transplant. ⚠ And a fact to price wording against: the ledger stores `name: entry.form.title` (`pageStore.ts:248-249`) and a written form has no separate designation field — **bare `Square — removed` is unmintable without a model change; the title is the unit.**
2. ✅ **`R1` §3 — THE REACHABILITY CENSUS, driven at `1920×950`, HEAD arrangement** (plates `r1_1`–`r1_6`; full numbers `r1_census.json` in my scratchpad, headline rows here):
   **(a) the claimed-accepted controls, each at 950** *(the layout is top-anchored and content-fixed, and I verified the rule — a control is reachable iff the viewport ≥ its measured bottom: `remove` bottom 901 shows at 911, gone at 871)*:
   | control | at 950 | reachable at |
   |---|---|---|
   | `remove` + `set aside` — **on the invoked square's card** (card 64→**1226**, height **1162** — her number exactly) | **OFF-SCREEN** (987–1069) | **≥ 1069** |
   | `remove` + `set aside` — on the born torus's card (height 1070) | visible (819–901) | ≥ 901 |
   | `undo` (record strip, after an act) | visible (847–870) | ≥ 870 |
   | the memorial mark (at the site) | visible (520–530) | camera-framed, not fold-bound |
   | `explore inside` — **on the torus card** | **a 10 px SLIVER** (940–971) | **≥ 971** |
   | `explore inside` — on the T³ card (height 515) | visible (449–480) | ≥ 480 |
   | the certificate receipt (torus / square) | **OFF** (983–999 / 1113–1129) | ≥ 999 / ≥ 1129 |
   | the esc·double-click hint (torus / square) | OFF (1090+ / 1182+) | ≥ 1120 / ≥ 1212 |
   | camera Fit/Reset · the record strip · the aperture door · the ops dock · the shelf · every form caption | all visible | — |
   ⇒ **Δ27's blanket sentence is measurably false exactly where you retracted it: on a maximised 1080p screen the square's `remove`/`set aside` DO NOT EXIST for him, the torus's walk door is a sliver, and every demoted certificate is below the fold.** The T³ walk door he DID accept is comfortably on-screen — consistent with the fan's independent sighting.
   **(b) one container reports scroll and refuses it; the DOCUMENT accepts — and that split is the whole story:** the app's root div reports maxScroll 184–276 and applies 0 (overflow `visible` — her `maxScroll 398 / applies 0`, reproduced in kind), **but the `<html>` document itself scrolls: a WHEEL OVER THE CARD scrolled the page 276 and brought `remove` from 987 to 710 — a live person-route on my machine** (plate `r1_6`), with a thin scrollbar present. ⚠ **Named divergence from her drive: she measured window scroll and wheel-over-card as no-ops in Arman's Chrome; mine apply.** Environments differ (extension drive vs playwright, possibly overlay-scrollbar settings); both measurements stand as taken. **And the route WOUNDS the page**: the canvas is viewport-sized, so the scroll exposes a black void under it and displaces the chrome — plate `r1_6` shows exactly what a person buys by finding it. Nothing on the page SAYS the route exists — the card just ends at the fold, complete-looking.
   **(c) below the fold at 950, beyond the controls above:** on the square's card the acts heading (`this page — what you may do…`, top 951), both esc hints, both certificates. The cold page and the post-removal page fit entirely.
   ⚠ **One observed fact from `r1_6`, reported without judgment because it is not my lane: a freshly invoked square's card + figure print the ratified absence word `unnamed` ~20 times on one screen** (map `unnamed·unnamed` ×4 · `☐ unnamed · 4 corners` · incidence `@ unnamed` ×4 · stance ×4 · verdict ×4 · the figure's four prongs). The plate carries it.
3. ✅ **`W1` — THE THREE LOCATES: TWO producers, not one — and three of the four strings are ONE mint read three times.**
   **Producer A — the lift's label mint:** `subComplexLift.ts:905-908` sets `label = selections[0].id` (its own seal comment says why: *"the single-selection label names WHICH entity — the entity id ALREADY carries its kind prefix, so the label is the id alone"*), and `:872` composes **`${label} of ${shape.name}`** into BOTH the lifted shape's `name` (`:875`) and the returned `title` (`:892`). **Three readers print that one string verbatim:** the lift notice (`Panels.tsx:337` — interpolates the returned title into `lifted "…" → the Manuscript shelf`), the shelf entry line (`ManuscriptChrome.tsx:1056` via `ShelfEntry.title = shape.name`, `genesisModel.ts:332`), and the loaded card's title + caption (`genesisModel.ts:346` — `` `${entry.title} — loaded` ``). ⇒ **One cure at the mint reaches all three.** ⚠ Note the mint's DOUBLE DUTY: the same `label` also builds the lifted shape's `id` (`lift:${label}:from:${shape.id}`, `:874`) — an ADDRESS purpose and a DESIGNATION purpose sharing one variable; a cure must split them, not rename the id.
   **Producer B — the source tag:** `geometryStore.ts:484` passes **`shape.id`** (the raw Ambo id) as `serializeSnapshot`'s source; it rides `provenance.source` (`genesisModel.ts:331`) and the shelf heading renders it — the QUOTES are the renderer's own (`ManuscriptChrome.tsx:1036`, `“{universe.source}”`). ⇒ **The universe heading needs its own cure (a name for the source universe where its id now stands).**
   **Why the card's BODY is right while its title is wrong (her 5-vs-4 count):** the body reads the lifted copy's PACKETS (`vertex.data.label` — the given name rides the deep copy), the title reads the MINTED NAME. Same shape, two slots. **Freeze pricing for the board: `subComplexLift.ts` NOT_FROZEN · `geometryStore.ts` un-rowed · `ManuscriptChrome.tsx` NOT_FROZEN · but `genesisModel.ts` is FROZEN (manifest :73)** — a cure that touches the `— loaded` composition or the shelf-entry title reader is a sanctioned spend.
4. ✅ **`B-129` §4 (the Ambo vertex label) — delivered in my `1514` letter and UNTOUCHED by `R1`; the three answers stand:** (a) a gesture writes `data.label` (the Label input + `Save packet` → `updateSelectedVertexData`, ran: `A`→`apex`); (b) the seed catalogue authors the letters (`seeds.ts:13-16`), midpoints compose from their sources (`ambo.ts:116` — ran: `apexB · apexC · apexD` after the rename); (c) one slot — the letter yields by replacement, both never show. Plates `b129_5`–`b129_7`.
5. ⚠ **The DRIVE FAMILY's standing red, from before `R1` landed and unchanged by the revert: `diagnose-deficit-app` fails 28 — and the control at HEAD (edits stashed) fails the IDENTICAL 28** (first abort: the driver's `button:has-text("e0")` in the fold panel; door/field/card-row clauses cascade; plus a two-file-inputs fragility and two source-pin fails predating me). 437.1 s vs 443.5 s. Someone's queue; not B-129's.

---

## 1 · WHAT I SAW (drives, headed, 5174 — stopped, port released, killed 10412)

The census page at 950 cold, with the torus card, the square card, after the removal, with the T³ card (plates `r1_1`–`r1_5`) · the document-scroll aftermath (`r1_6`) · the memorial's two lines removed/restored (`b129_3/4`) · the Ambo rename arc (`b129_5/6/7`). **To me they read as reported, and the plates are at `scripts/app-leg/_frames/`** — the eye that closes arrow-9 is not the builder's.

## 2 · WHAT I RAN

`npx tsc -b` → 0 (twice: after the build, after the unbuild) · `diagnose-p5-the-acts` ALL PASS on the final tree (36 checks) · `diagnose-argument-card` ALL PASS · `npm run sweep` on the (since-reverted) pinned tree: `124 files · expect exactly ONE fail: diagnose-dual-inspection` → SWEEP OK — **re-run at the committed tip after this letter, result echoed in the commit** · the deficit leg twice (mine + HEAD control) · the census drive + the person-route probe + the `B-129` drives. Manifest rows printed: `ManuscriptView.tsx` NOT_FROZEN (:194) · the witness script un-rowed · locate-only files untouched.

## 3 · WHAT I CHANGED (the commit)

- **`src/manuscript/ManuscriptView.tsx`** — the memorial recomposition ALONE (the §2 pin was built and then removed to the byte on `R1` §1; `git diff` shows only `SiteMemorial`).
- **`scripts/diagnose-p5-the-acts.cjs`** — the two composition pins.
- **The record pair** — `THE_BUILD.md` = the three consumed letters (`B-129` + `R1` + `W1`); `THE_BUILD_REPORT.md` = this letter. All three consumed letters archived.

## 4 · WHAT I COULD NOT REACH

The §3 siblings (her grammar) — stopped as ordered. The divergence between her no-op scroll measurements and my applying ones — named in item 2(b), not resolved: two environments, two honest measurements.

`R1` and `W1` are delivered; `B-129` closes as amended. ⛔ Nothing further started.

— the coder
