# THE BUILD REPORT — TASK 0: the campaign is off this disk

**STAMP ECHO: `B-2026-08-21-C`** — the build I executed is the one you wrote.
**coder · 2026-08-21 · TASK 0 `cf5b189` on `b7468db` (mandate record) · branch `team-arman` · PUSHED**

---

## 1 · WHAT I DID — Task 0, in order, each path adjudicated by measurement

1. **`CLAUDE.md` — TRACKED.** The doctrine, unprotected two days, now in history.
2. **`scripts/app-leg/ds_*.png` (both) — TRACKED.** (The earlier frames entered history when the interior-transport witness commit swept `scripts/app-leg/`; these two postdated it.)
3. **The rest, decided, with the why:**
   - **`BACKLOG.md` — TRACKED.** Not debris: it carries Arman's own 2026-08-06 ruling on the cone-hatch period defect, a falsifiable test written before any build, and a freeze-verdict note — a live project record.
   - **`tasks/plan.md` — TRACKED, and here is what it is:** the DESIGNER's "inside-view gate" plan (owner: designer seat, opened 2026-08-09) — the flatlander premise-correction and the recession-law table (Euclidean/spherical/hyperbolic) that the shipped aperture recession law traces back to, with its own ⛔ SUPERSEDED section. Another seat's only copy of a real design record; an untracked file is an unprotected file, so it is protected first — relocating it is cheap now and is its owner's call.
   - **`.claude/` — RULED SESSION-STATE, in-repo.** My measurement differed from the brief's: git did not list it here because its one file (`settings.local.json`) is covered by the USER'S GLOBAL ignore (`~/.config/git/ignore`) — a one-machine accident. The decision is now durable: a `.claude/` line in the repo's own `.gitignore`, with the why in place.
   - **`_cone_probe.cjs` — DELETED, by the instrument rule.** 21 lines that PRINT readings (cone-word verdicts, deck dets, rodK) with no PASS/FAIL — it cannot fail, and nobody will read its answer again: its findings are pinned in `diagnose-winding-headings` (1a: the d+0·d+0·d+0 cone word declares `2 × 180°`; 1b: the 2-door half-turn walk). Not promoted — promoting a non-failing printer would commit debris wearing a script's name.
4. **PUSHED — fast-forward, no force:** `2b51468..cf5b189 team-arman → team-arman` (github.com/selahvarziarman-byte/recursive-ambo). Pre-push divergence check: behind 0 · ahead 32. The whole campaign — F.0c through Task 0 — now exists off this disk. The branch reads in sync.

## 2 · WHAT I RAN

```
git rev-list --count HEAD..origin/team-arman → 0   (no divergence; fast-forward safe)
git rev-list --count origin/team-arman..HEAD → 32  (the campaign)
git push origin team-arman → 2b51468..cf5b189      (exit 0)
git status --porcelain → (empty — the tree is clean, nothing untracked remains)
```
No source file moved ⇒ no tsc/suite/drive owed this build (the last full audit — 112 @ 1, tsc 0, the app leg ALL GREEN — stands at `346e678`, one commit below). One stale 0-byte `.git/index.lock` removed mid-build (20:27, no live git — the standing recurrence; fourth occurrence today, still worth its own look someday).

## 3 · WHAT I CHANGED

- `cf5b189` — the six decisions above in one commit (tracked: CLAUDE.md · BACKLOG.md · tasks/plan.md · 2 × ds frames · the `.gitignore` ruling; deleted: the spent probe).
- `b7468db` — this build's mandate into history, stamp and all.
- This report, committed and pushed behind them.

## 4 · WHAT I COULD NOT REACH

Nothing. Per §3 I stop here — the board is Arman's. The parked set (the four frozen unions, #37, D16, persistence) stays parked until a build file names one.
