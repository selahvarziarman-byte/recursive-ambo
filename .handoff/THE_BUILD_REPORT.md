to: the mothership
from: the coder
clock (raw, verbatim): `Sat Aug 29 16:16:10 IST 2026` — mtime is the fact
**STAMP ECHOED: `B-130` — §2 built to the six clauses and driven to its countable acceptance · §3 priced FREE at the mint and BUILT, driven through all three readers · Producer B priced as a SPEND, not built**

## TO THE MOTHERSHIP

1. ✅ **THE ACCEPTANCE, ANSWERED WITH THE COUNT IT ASKED FOR: at `1920×950`, the invoked SQUARE — the `1162 px` worst case — now runs `572 px` (64→636), and EVERY control and EVERY heading is on screen with nothing scrolled and nothing opened** (plate `b130_1`; rows: on-select 78 · affordance 160 · the argument door 205 + its closed face 220–261 · certificate 389 · the acts heading+row 497–622 with `remove` at 540–622 and the set-aside refusal readable beside it · Fit/Reset 896–926 · the aperture door 894–926; the scroll region reads 410/410 — nothing left to scroll at defaults). Her arithmetic held almost exactly: closing the one 646 px compartment took the card from 1162 to 572.
2. ✅ **THE SIX CLAUSES, each one built / not-built / refused-with-reason:**
   - **A.1 BOUNDED — BUILT.** `maxHeight: calc(100vh − 78px)` (64 top + 14 breathing); measured with the compartment open the card stops at 936 = the cap; nothing in its chain passes the window's edge; `scrollIntoView` is not used anywhere in the cut.
   - **A.2 COMPARTMENTED BY KIND — BUILT FOR EVERY KIND THAT HAS A HEADING, and the boundary is named.** The argument reading gained its compartment (`data-compartment-argument`, the small-caps door `the argument reading`, `data-compartment-state` machine-readable); the acts stand under their existing heading in the footer; the field door and the certificate were ALREADY compartment-shaped (their sealed mechanisms untouched). ⚠ **The record rows, the legend and the notes carry NO existing heading — a heading is COPY, and copy is the designer's: they stand as the always-open body until she mints their words.** Your ★ note held for exactly the kinds it described.
   - **A.3 THE READING SCROLLS; THE ACTS DO NOT — BUILT.** The acts block sits AFTER the scroll region as the bounded frame's own footer (`flexShrink: 0`). Driven: door open → the column grows, the region scrolls (a 250 px write applies), the footer stays whole at 840–922 under the cap. ⚠ **One measured fact for the spirit-reading, named rather than hidden: in the UNDER-CAP regime the footer tracks the card's own growth** (`remove` top 540 closed → 840 open — the frame resizes; the column offset is gone). Arman ruled *"without the card being always fully extended,"* so the frame must be allowed to shrink — a screen-fixed foot would hold the acts at 922 over 300 px of nothing. If her rule wants the foot screen-fixed anyway, that is a re-rule, not a bug report.
   - **A.4 DEFAULT-CLOSED IS EARNED — BUILT, exactly one.** `argumentOpen` starts `false` (the one earned default — her measurement); the PERSON's *called for* is the door (toggle, the certificate's expand-in-place idiom); the SYSTEM's *needed* is attention: emphasis intersecting the compartment's own row ids PRESENTS it for the duration without ever writing the person's state — *attention promotes, data-presence never does*, and no content-based auto-open exists (pinned).
   - **A.5 CLOSED ≠ EMPTY — BUILT BY CONSTRUCTION.** An absent reading renders NO compartment at all (a true absence — the ordinary unmarked); a closed one shows its heading + the map's own O-line + the counted words. The two states cannot be confused because one of them does not exist.
   - **A.6 HOW MUCH, NEVER WHAT IT CONCLUDES — BUILT BY THE STRONGEST CONSTRUCTION.** ONE state governs the whole reading: `ArgumentMapSection` mounts whole-or-not-at-all, so *verdict open while the map is closed* is a state the mechanism cannot express. The closed face is the map's own `source ⟶ result` line (MAP FIRST even closed) + the counted words line (`4 concepts, 4 relations — the seed's own` on the plate) — no verdict token can reach it (pinned: the closed branch touches neither `argument.verdict` nor `stance` nor `incidence`). ⚠ A future per-section split must RE-PROVE the invariant — the reveal-order prefix (closed → map → +incidence → +stance → all) is the state shape that keeps it structural; the pin `!/mapOpen|verdictOpen|stanceOpen|incidenceOpen/` is the tripwire.
3. ✅ **§3 — THE MINT CURE WAS FREE AND IS BUILT; driven end-to-end through all three readers.** The whole cure sits at `subComplexLift.ts` (NOT_FROZEN): `label` keeps the ADDRESS purpose (the minted id byte-identical — `lift:<entityId>:from:<shape.id>`, the seal's distinctness), a new `designation` takes the TITLE — **the packet's own label where the substrate holds one** (a given name, a seed letter, a composed midpoint letter — CARRY), **the address where it holds none** (an absence WORD there would be her copy to mint, not this seam's; the latent branch is pinned). `extractSubShape` defaults `designation = label`, so every other caller — the region lifts, the direct extract callers, thicken, open-lift — is byte-identical. ⛔ **`geometryStore.ts`, `genesisModel.ts`, `ManuscriptChrome.tsx`: UNTOUCHED — and the argument-card leg's §11 byte-pin proves the first two === HEAD every run.** **AT THE EYE** (plates `b130_4/5/6`): rename `A → apex`, lift — the notice reads **`lifted “apex of Tetrahedron” → the Manuscript shelf`**; the shelf entry reads **`apex of Tetrahedron`**; dragged onto the sheet the caption reads **`apex of Tetrahedron — loaded`**. The store-level control: `Territories of Ambo Dissection Tetrahedron` with the id still `…lift:vertex:tetrahedron:a:from:…`. Witnesses: `p1b` gained §5b (the named-vertex title pin through the REAL rename gesture · the id-keeps-address pin · the latent-branch pin), and its §5 cell pin **survived unchanged** — cells hold no packet label, measured live.
4. ⚠ **PRODUCER B — PRICED, NOT BUILT, and the §3 question answered: the Ambo side DOES have a name for a universe — `shape.name`** (`Tetrahedron`, `Ambo Dissection Tetrahedron`). **But the `source` slot the heading reads is ALSO the shelf's GROUPING KEY** (`shelfUniverses` groups by it) **and the snapshot's provenance** — one slot carrying an address purpose and a designation purpose, the same disease one level up. A cure = a SECOND slot (a display name beside the key), and that crosses **`genesisModel.ts` (FROZEN, manifest :73 — `ShelfEntry.source` and both readers)** and likely **`snapshot.ts` (FROZEN — the provenance schema)**. **Priced BY FILE: two frozen files. Arman's sanction, not mine.** The plate shows the standing state: the cured entry `apex of Tetrahedron` under the still-quoted heading `“shape:seed:tetrahedron”`.
5. ⚠ **What I could not sight, said as arrow-8:** the attention agent's auto-present at the eye. Two instruments missed inside my retry boundary — the DOM ring labels are pointer-inert BY DESIGN (the pick layer is the invisible 3D mesh), and the seam handle exposes no marks list on this route; the 3D pick hover is the deficit leg's own flaky clause at HEAD. The mechanism is compiled, source-pinned (§14's exact-expression pin), and rides D2's one emphasizedIds channel — but no eye has seen a hover open the compartment yet.
6. ⚠ **The DRIVE FAMILY's standing red, retriggered and rerun at the final tree: `diagnose-deficit-app` = 28 failures — the SAME count as the HEAD baseline both B-129 runs measured** (the flow still aborts at the fold panel's `e0` locator, upstream of every card clause). **For whoever repairs it: the repair must now account for the ruled default-closed compartment — the map rows sit behind the door until opened or attended.**

---

## 1 · WHAT I SAW (drives, headed, 5174 — stopped, port released, killed 7752)

The whole square card in one 950 px look — closed compartment, counted face, readable refusal, `remove` at hand (`b130_1`) · the door open with the map heading inside, the card stopped at the cap, the footer whole (`b130_2`) · the lift notice, the shelf entry and the loaded caption each carrying `apex` where `vertex:…` stood (`b130_4/5/6`). **They read right TO ME, and the plates are at `scripts/app-leg/_frames/`** — the closing eye is not the builder's.

## 2 · WHAT I RAN

`npx tsc -b` → 0 · `diagnose-p1b-granular-save` ALL PASS (§5b added; one pin recut after measuring the loader's #37 re-root prefix) · `diagnose-argument-card` ALL PASS (§14's five compartment pins added) · `diagnose-multi-region-lift` · `diagnose-p5-the-acts` · `diagnose-conformal-lift` ALL PASS · the acceptance drives (two) · the deficit leg at the final tree (28 = the standing baseline) · the queue re-read BEFORE this commit step (the R1 habit, now practiced) · `npm run sweep` at the committed tip — result echoed in the commit line.

## 3 · WHAT I CHANGED

- **`src/manuscript/ManuscriptView.tsx`** — A.1/A.3 (the cap, the scroll region, the acts footer — the B-129 build restored under its now-ruled charter, comments recut to B-130 + Arman's words) · the argument compartment (state, door, closed face, attention presentation).
- **`src/lib/subComplexLift.ts`** (NOT_FROZEN, manifest :180) — the designation/address split + `givenLabelOf` (the substrate's own packet label, all four kinds).
- **`scripts/diagnose-argument-card.cjs`** — §14: the five structural pins (one-state construction · closed-face counts-not-conclusions · earned default + attention expression · closed≠empty · bounded+footer).
- **`scripts/diagnose-p1b-granular-save.cjs`** — §5b: the mint-split pins through the real gesture.
- **The record pair + this letter; `B-130` archived.**

## 4 · WHAT I COULD NOT REACH

The attention agent's eye-sighting (item 5 — source-pinned, unsighted). The universe heading's name (item 4 — a two-frozen-file spend, priced for Arman). The unheaded kinds' compartments (item 2, A.2 — waiting on her words).

`B-130` is delivered. ⛔ Nothing further started.

— the coder
