to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-17 13:01 +03:30` (machine) · HEAD == origin after the push — the build commits are `d681899` (the entry rider) · `226f004` (C-6a part 1) · `b0e01b8` + `4eca9fc` + `e1003a7` (the leg's rider: the clause, its quote repaired, then made to read what it claims); this letter rides the record commit that follows
subject: ✅ **The ENTRY RIDER LANDED (`d681899`) — the walk stands where the plate stands its eye: Poincaré opens INSIDE its cell (trace empty · `doors 0` · `a` → `walked 0.63` · `turned by 36°`).** ⛔ **One premise of its acceptance FALSIFIED by measurement: Seifert–Weber's `sceneScale` is 0.5522, not 1 — the one rule moves the SW entry, and its K-1e numbers move with it (`a` → 111° · `afAF` → 150°, predicted and sighted).** ✅ **`C-6a` part 1 LANDED (`226f004`) — the edge row says `click: select`, the face row is no longer dressed as a control; part 2 waits on her line; the camera buttons READ for her clause.** ✅ **The K-1e LAW-22 rider at EQUAL LENGTH rides the winding leg now (`e1003a7`) — RUN and PASS at the eye: door c by its letter and by a held ↑ of one period give the same return line and trace, `differ: [eye, press]` only, the hold 2.1155 on the clock against the period 2.1190.** ECHO the 10:20 re-park (nothing built in it but the rider) · ECHO `K-2d` (recut) · ECHO `C-6a`. `Z-1` · `K-2e` · the LEVEL snap · L(4,1) · her SW tap-count falsifier: PARKED, unbuilt, their letters archived with this landing (their text is in THE_BUILD.md for the day they un-park).

## TO THE MOTHERSHIP

1. **AN ACCEPTANCE I COULD NOT REACH AS WRITTEN — the SW premise (three lines).** The rider read *"T³ and SW entries byte-identical to today (sceneScale 1 there — pin it)"*. Measured with the engine's own `buildAperture`/`readCellSurface`: Seifert–Weber's `sceneScale` = **0.5522** (the Poincaré cell's 0.2361; both the deck's own ratio of the chart inradius to the seed's; every euclidean room exactly 1). The chartered rule — the aperture's own `apertureEyeFor`, one producer — therefore MOVES the Seifert–Weber entry to (−0.1933, −0.3037, 0.0552), inside the cell, the same relative place the plate already uses; and θ depends on p, so the K-1e readings at the walk's entry become `a` → **111°** (111.07 predicted, 2.0913) and `a f A F` → HOME, **`turned by 150°`** (149.94 predicted) — sighted, exact. I landed the rule as chartered (a second rule for SW would be two definitions wearing one key) and report the clause as unmet BY ITS PREMISE, not by the cut. **If the walk's SW entry should stay where it was, that is one letter and one revert (`d681899`).** T³ is byte-identical (scale 1).
2. **L(4,1), SEEN AND STOPPED (as ordered):** it has no sealed model (euclidean realization, scale 1), so the scale cannot move its entry off the face plane it sits on. At open: `doors 0`, trace empty. The press of `a`: the trace writes `ac`, a return fires in passing (`return 1 · back where you started · after 1 door · the room came back turned by 132°`), and the press ENDS **0.943 from the entry** with `pressed a · crossed ac · walked 2.00` — the period's end and its start do not meet. Not traced further: the lens realization is not this rider's.
3. **THE AMBO'S CAMERA BUTTONS, READ for the designer's clause (she did not read them):** the Ambo renders `SceneCameraControls` with NO `orbit` overrides (`Workspace3D.tsx`; only the Manuscript's plate passes `mouseButtons: { LEFT: ROTATE, MIDDLE: PAN }` at `ManuscriptView.tsx:6776`), so the Ambo's buttons are three's OrbitControls defaults (`OrbitControls.js:112`): **left-drag ORBITS (rotate) · middle-drag DOLLIES (zoom) · right-drag PANS (screen-space panning) · wheel ZOOMS**; touch: one finger rotates, two fingers dolly and pan; damping on. The rig's literals at `SceneCameraRig.tsx:196–210` (`enablePan · enableRotate · enableZoom · screenSpacePanning · rotateSpeed 0.75 · zoomSpeed 0.9 · panSpeed 0.9`) carry no button map. **`SceneCameraRig.tsx:201–203` holds no `mouseButtons` override — her citation is to the Manuscript's, not the Ambo's.** If her camera clause says otherwise, this is what the buttons DO.
4. **A CORRECTION TO HER SITES:** the three rows are in `src/components/Panels.tsx` (vertex `:2156`, face `:2219–2224`, edge `:2288–2306`), not Workspace3D; `Workspace3D.tsx:2473` (`formatHoverStatus`) is right. `src/components` carries no manifest rows — no frozen file.
5. **Part 2 of `C-6a` (the gesture line in its own slot) is HELD for her verbatim line**, as ordered; the place is not yet made — I will cut it from her line when it lands. ⚠ One reading for when it does: the Ambo's inspector rows sit inside a **collapsed** `Cell Composition` section (`defaultOpen={false}`, `Panels.tsx:980`); a person who has not opened it has never seen a row's tooltip. Not this stamp's; named so the line's author knows where the rows live.

# 1 · What I SAW (my own eye — the drive family's headless instrument, since the in-app pane does not paint while hidden)

## The entry rider (`d681899`)
- **The Poincaré cell, the cure:** opens at (−0.0826, −0.1298, 0.0236) — inside the inradius; trace EMPTY, `doors 0` (yesterday: `acD` at open). `a` → `pressed a · crossed a · walked 0.63` (π/5) · `return 1 · back where you started · after 1 door · the room came back turned by 36°` — `returnTurnDeg` 36.00000000000002, the exact witness at the eye; home to 0.
- **T³, byte-identical:** opens at (−0.35, −0.55, 0.1); `a` → `pressed a · crossed a · walked 2.00` · `return 1 · … · after 1 door · the room came back the same way up`.
- **Seifert–Weber, moved by the rule (item 1):** opens at (−0.1933, −0.3037, 0.0552); `a` → trace `a` · `pressed a · crossed a · walked 2.09` · `return 1 · … · after 1 door · the room came back turned by 111°` (111.07); `a f A F` → four position-returns (111° · 102° · 113° · 150°), `a 0 · f 0`, "every door you opened, you closed — and here you are, home", `return 4 · back where you started · after 1 door · the room came back turned by 150°` (149.94 predicted).
- **L(4,1):** item 2.

## `C-6a` part 1 (`226f004`) — the Ambo at boot, the seed tetrahedron selected by a plain click on the canvas, the Selection tab, the Cell Composition section opened
- Four face rows: computed cursor **`default`**; a plain click on one changes nothing — className byte-identical before and after (LAW 24).
- Six edge rows: tooltip `vertex:tetrahedron:a - vertex:tetrahedron:b · click: select · shift-click: toggle in the lift region`, cursor `pointer` (a control that acts).
- Four vertex rows: `click: inspect · shift-click: toggle in the lift region`, cursor `pointer` — unchanged.
- `formatHoverStatus` untouched: `Hover a cell or inspector row to preview correspondence` before a hover, `Hovering cell seed/tetrahedron g0 | id: cell:seed:e7g5rs` after.

## The leg's rider (`b0e01b8` · `4eca9fc` · `e1003a7`)
- **`P.period`, RUN in the leg's cone room at `e1003a7` — PASS:** `door c · 44 drags replayed (aimed True) · by letter: return 1 · back where you started · after 1 door · the room came back mirrored · trace c · period 2.1190 · by hold: the same line and trace · held 4701 ms = 2.1155 on the clock · differ ['eye', 'press']` — every other seam field byte-identical (doors · trace · traceHidden · looks · returnCount · doorsAtLastReturn · sentence · returnLine · faceMark · frameHanded · forward · right · up · snap); the eye within the clause's 0.05 (the clock's own shortfall 0.0035; the aim's ≤ 0.012 rad). Two instrument facts bought on the way, both in the driver's comments: the look's drag has a dead zone (a sub-threshold correction turns nothing — sent as a there-and-back pair), and a CDP key-down call returns only after the starved renderer acknowledges it (a frame late), so the hold's remaining time is computed from the page's own key-down stamp. And one about the claim itself: in a room whose doors ROTATE (the cone word), the period's line is NOT the door's normal — `End` faced the plane squarely and the held walk went elsewhere (0.81 apart); the comparison is "along the same chart line" only when the hold follows the press's own line, measured from the press.

# 2 · What I RAN (verbatim)
- **Sim-diff:** `git diff --stat HEAD -- <files>` after each of the three commits → empty.
- **Freeze manifest rows:** `src/manuscript/apertureModel.ts` row 203 NOT_FROZEN · `src/manuscript/ExploreWindow.tsx` row 189 NOT_FROZEN · `src/components/Panels.tsx` — no row (`src/components` carries none) · the witnesses and the leg outside the roots. **No frozen file; no new file under the engine roots** (the new witness lives in `scripts/`).
- **`node scripts/diagnose-the-keyed-walk.cjs`** at `d681899`: `DIAGNOSE-THE-KEYED-WALK: ALL PASS — …` (61 clauses; the K-2d clauses RUN the scale on the engine's rooms: 0.5522 · 0.2361 · 1; the raw entry outside the Poincaré cell kept as the positive control; `apertureEyeFor` ≡ `scaleToRoom` bytewise; the lens unmoved; the period from the scaled entries — SW home ≥ 1.99, Poincaré π/5 and 36.0000°; the source pinned).
- **`node scripts/diagnose-the-ambo-gesture-truth.cjs`** (NEW, in the sweep by its own declaration): `DIAGNOSE-THE-AMBO-GESTURE-TRUTH: ALL PASS — the edge row says click: select, the face row is no longer dressed as a control, the vertex row is as it was` (8 clauses).
- **`npx tsc -b`**: exit 0 after every cut.
- **The drive family — winding-route at `e1003a7`, on a quiet tree, read by eye in its own call:** **15/16 — the SAME parked E.circuit arm and nothing else.** `PASS ★★ return 1 · back where you started · after 2 doors · the room came back turned by 180°` (the cone, seam + DOM) · `PASS ★ the retrace with a head-turn: 2 doors · the same way up` · `PASS ★ LAW 22 AT EQUAL LENGTH (the K-1e rider)` (the line above) · `PASS ★★ return 1 · back where you started · after 1 door · the room came back mirrored` · `PASS no console error across the sessions`; the one FAIL is the parked pillar-encircled arm — THE LINE, VERBATIM: `return 1 · back where you started · after 9 doors · the room came back turned by 60° · doors 9` → `DIAGNOSE-WINDING-ROUTE: 1 FAILURE(S)` (leg exit 1), identical to the last three cycles' verdict. (The clause's first run at `b0e01b8` died at load on a bare apostrophe — `4eca9fc`; its second, at `4eca9fc`, FAILED honestly with the hold on the door's normal, 0.81 from the press's end — `e1003a7`.)
- **`npm run sweep`** at `e1003a7`, whole, read by eye in its own call: `133 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (sweep exit 0; the new Ambo witness counted and PASS inside it; the keyed-walk witness PASS inside it).
- **`.handoff/inbox/coder/` re-listed before each commit:** four letters, `--mark-reviewed --expect 4` in its own call; all four archived by name with this landing.

# 3 · What I CHANGED
- **`d681899` — the entry rider.** `apertureModel.ts`: `ApertureCellSurface.sceneScale` (stated on every return: the deck's on the sealed one, 1 on the two euclidean ones); `scaleToRoom(sceneScale, p)` exported, and `apertureEyeFor` now calls it — one rule. `ExploreWindow.tsx:849`: `ENTRY_SEED` named once; `eye = scaleToRoom(cellSurface.sceneScale, ENTRY_SEED)`. The keyed-walk witness grows the K-2d clauses.
- **`226f004` — C-6a part 1.** `Panels.tsx`: the liftable edge row's tooltip `… · click: select · shift-click: toggle in the lift region` (the identified pair keeps its refusal); the face row `cursor-default` (its onClick already acted only under shift). NEW witness `scripts/diagnose-the-ambo-gesture-truth.cjs`. No new act, no store action, `formatHoverStatus` untouched, `selectFace` nowhere in the Ambo.
- **`b0e01b8` · `4eca9fc` · `e1003a7` — the leg's rider.** `scripts/app-leg/winding_route_driver.py`: clause `P.period` in the cone session — W0 names the nearest door (End) and measures the press's own chart line from the eye's first pre-fold motion; W1 aims the frame along it by recorded drags and presses the letter; W2 replays the same drags and holds ↑ for `period / pace` on the page's own key-down stamp; the seam compared field by field. `diagnose-winding-route.cjs`: its verdict line. Witness-only; no product file.
- **The record commit:** `.handoff/THE_BUILD.md` (the four letters verbatim), `.handoff/THE_BUILD_REPORT.md` (this letter), the W1 riders AS FOUND — `.handoff/HANDOFF_THE_DESIGNER_SEAT_V1.md` · `.handoff/PLAN_THE_CAMERA_GATE.md` · `.handoff/PLAN_THE_CONNECTION_LAYER.md` · `.handoff/THE_CLAIMS_LEDGER.md` · `.handoff/THE_FRONT_BOARD.md` · `.handoff/THE_SOVEREIGN_LEDGER.md` (tails close on finished sentences; not read for content).

# 4 · What I could NOT reach — named
- **The SW clause of the rider's acceptance, by its premise** (item 1 above) — the rule is landed; the meaning of where SW's entry stands is yours.
- **The in-app pane** still does not paint while hidden in the app's layout; every sighting above is the drive family's headless instrument (frame stamps and event stamps agree there; the pane's do not — filed yesterday).
