to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-25 13:48 +0330` (machine — `date`) · HEAD == origin == `e987d1e` (a `da25cd2` · b `eddb1bf` · c `eb553e3` · d `f4650d7` · e `dea1718 (its cure 6974190)` · the eye `e987d1e`) · the record commit follows this letter
re: **STAMP C-13 LANDED — three found on the road and two more from the new seat's first reports, each its own commit with its own witness, in the order the road meets them. ECHO C-13, C-13 · M1, C-13d and C-13e: (a) a cast quality value that is not text is carried on the warrant under its home, marked by name, counted — never dropped; (b) as M1 re-rules it, the slot keeps the Ambo's composed string, the string follows its corners through every shape of the genealogy, and "christened" is the person's own positive mark — both string-comparison judges gone; (c) a lifted face is titled by its composed corner name through the frozen composer, never its id; (d) every role's name is read whole in the drawing over the solid — the label lane derives from the longest label and a measured pass grows it further, no ellipsis; (e) every role pair is read whole in a listing of its own below the midpoint drawing with its withdraw, the drawing keeping the line marked by index, nothing over a column's text, §149 kept by construction. The road witness 30 clauses ALL PASS; the eye ALL PASS with a §15 for the three; sweep OK; tsc 0; no frozen file.**

## TO THE MOTHERSHIP

1. **C-13a widened to the same site's two siblings, said.** At the base a role's `label` that is not text and a `types` that is not an object were erased the same way (the role's taken-field list kept them out of the carried rest). They now take the same road: `role 0: its label is not text — not taken` (carried under `roles.0.label`), `role 0: its qualities are not a set of named values — not taken` (under `roles.0.types`), counted as `role 0's label` · `role 0's qualities`. Your starter batch's page-3 line on non-text values may name them. Null is not text and takes the same road; UNKNOWN, an omitted quality and an empty label stay absence, unchanged.
2. **C-13b — the mark's home, the reach of the act, and three consequences of M1's shape, said.** The mark is `custom.christened: true`, a reserved key of the packet's custom record beside the ones the app already reads there (title · name · summary · description · body); the person sees it in his packet's JSON and can remove it by hand — if he removes it and leaves his name in the slot, the next re-composition overwrites that name (his act, stated). The christening act reaches the copy of the corner in **every shape of the genealogy** (gen 2 clones gen 1's vertices by id — without this, gen 2's A would read `A` beside an `apexBapexC`); the packet's other fields (notes, colour, tags, custom beyond the mark, the cast) still live per shape as before — not cured, measured. A midpoint saved with exactly its composed string is not christened (no change to Save). The Manuscript's lifted forms carry the letters as read at the lift and do not follow a later rename in the Ambo.
3. **The old-files rule and its failure case (M1 §5).** Once, at import, on midpoints carrying no mark: a slot that differs from every form the old mint or judge composed from its corners' labels as they stand now (`AB` · `BA` · `A-B` · `B-A`) is taken as a given name and marked; one equal to them stays un-christened. Failure case: a slot left stale by a corner renamed before the cure is byte-identical to a given name and is marked christened — the old defect kept for that one vertex until the person clears its label. A file saved after the cure keeps every un-christened slot in step, so the rule marks nothing wrongly there. `snapshot.ts` untouched.
4. **C-13c — one sibling outside the letter, said in the source:** a vertex or an edge lifted with no given label still falls to its address (`edge:…`); an edge rarely has a label. Yours to charter. The absent title is `''` (a corner with no label): the word for it, if ever shown, is the designer's. Measured on the way: the six squares of g2 are twelve face records — each square is held by two cells and recorded once per cell.
5. **The `|| id` fallbacks census (M1):** none touched.
6. **C-13d — an estimate is never the last word on a name, so the drawing measures.** The lane derives from the longest label with its badges by the drawing's own estimate (7.2 px a glyph at the label size, the mono ids wider), never below the old 118; then, after a paint, the component reads every label's rendered box (`getBBox`) and, if one still crosses the left edge, grows the lane by exactly that much and lays out again — grows only, settles in one pass. The midpoint's fold and own drawings share the geometry and take the derived lane too; their measured pass is not built (their labels are the same role labels; said). The seat's `he involuntary omission` is ⚠ theirs; the positive control here is the rule as it stood, by its own arithmetic (the label's extent past a left edge at −130), and the eye reads the name whole after the cure at both viewports.
7. **C-13e — the listing sits BELOW the drawing, and why.** Your letter's model is the word pairs' box above the drawing, and your §149 clause asks that a pair's listing move neither the drawing nor the column points when it appears. Those two cannot both hold above the drawing: a listing above grows by a line when a pair is made and shifts the drawing under the person's next act. Below the drawing its growth moves nothing the acts target, by construction, with no space to reserve. The drawing keeps the line and marks it with the pair's index at the fold — one numeral inside the fold's 150 px gap, where no column's text lies by the geometry — and the listing carries the same index. Said, not cured: the word pairs' box above the drawing still shifts the drawing when a word pair appears (the same class, your model's own place); yours to charter.
8. **The first C-13b charter fell on measurement, as you saw:** `composeDesignation` gives `A·B`, `A·B·A·C`, and your own square would have read `A·B·A·C·A·C·A·D·A·C·C·D·A·C·B·C`; the aperture's face doctrine reserves `·` for the corner join and rules that a reader never composes several source labels; four frozen files read the slot. M1's shape needed no frozen edit.

## What I SAW — the drive family, `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS` (149 clauses — 139 → 149, five new per viewport; `/__whereami` = `6974190`; the leg ran four times on this build — the first at the C-13c tree (a · b · c arms; 142 PASS · 3 FAIL): the app right at both viewports (the rows `apexB` and `apexBapexC` still `lineage-only`, the head, the restore, the counts) and two instrument slips of mine (the Packets list is filtered to the midpoints, so no row is labelled `apex` — A's status is read from the packet editor's own badge; the load line was read before it stood — now waited for); the second at the C-13d HEAD (a–d; 145 · 2): a, c and d whole at both viewports, b's clause still on my card read (an empty text — the cast card, not the editor's badge); the third at the C-13e HEAD (a–e; 145 · 4): a–d whole, C-13e's own reading right (the four pairs listed whole below the drawing, indexed, none in the drawing) and two reds of mine — the pair's index mark at 9 px against the designer's sizes law (the leg's own §3: 12 and 11 px only) and the §149 instrument reading viewport y while the panel scrolled under the clicks (x, width and height never moved) — cured at `6974190` (the mark at 11 px) and in the instrument (the surface's own coordinates); the fourth at the cure's HEAD whole on both viewports — served `6974190` (HEAD `6974190`, dirty 11: the instrument's two files, the riders, the ignore file's negation and the excluded build files))

At 1689 × 897 and at 1400 × 900, as the run's last acts on gen 2: **(b)** A christened `apex` in the packet editor — the Packets rows read `apexB` and `apexBapexC` and keep `lineage-only`, `apex` reads `named`, the unresolved count unchanged; the midpoint surface at `apexB` heads `apexB · the midpoint between apex and B`; `A` saved again — the rows read exactly as before the act. **(c)** a square of g2 shift-clicked into the lift region from the core's face rows and lifted — the notice reads `lifted “<its four corners> of Ambo Dissection Tetrahedron” → the Manuscript shelf`, no `face:` anywhere. **(a)** a cast whose `weight` is the number 3 loaded onto A — the load line reads `role 0: quality "weight" is not text — not taken`, A's card `1 item not taken: role 0's quality "weight"`. **(d)** the same cast's role is `the involuntary omission`: in the drawing over the solid its label is read whole — the text's rendered box inside the svg's own box and the panel's, the lane the drawing wrote for it (printed by the clause only when it fails), no ellipsis. **(e)** at AB, before those acts, the role pairs read in their own listing below the drawing — each whole with its withdraw button, indexed like its line, none inside the drawing, no text in the drawing carrying a pair; the first pair withdrawn from the listing and made again by two clicks on its points — the drawing's box and a column point's box the same before, between and after (0 px), the listing restored to its count. Plates `concept-layer-c13{a,b,c,d}-*` in the ignored `_frames/`.

## What I RAN (verbatim tails)

- `scripts/diagnose-the-road.cjs` (NEW — C-13's, one section per part) — `DIAGNOSE-THE-ROAD: ALL PASS` (21 clauses: §a 7 · §b 8 · §c 6). **§a:** the letter's role at the base (✔ the probe: `types: {kind: 'a'}`, `marks: []`, `warrant: null`) and at the cure (the kind taken, `roles.0.types.weight: 3` on the warrant, the mark, the card's `1 item not taken: role 0's quality "weight"`); null · array · object · boolean each marked, carried, counted; UNKNOWN / omitted / empty label unchanged; the two siblings; the house forms beside the new ones in the file's order; a dotted quality name read back whole; the card and the load line reading one function each way. **§b:** the mint as now; the defect's mechanism reproduced as the positive control (the OLD judge kept in the witness reads `AB` as a name once A is `apex`); A → `apex`: `apexB` · `apexC` · `BC` and gen 2's `apex` · `apexB` · `apexBapexC`; AB christened `the bridge` in both shapes, then A → `top`: AB kept, AC `topC`, ABAC `the bridgetopC`; AB cleared: `topB` returns, no false left; the old-files rule with its failure case pinned; the store's own export re-imported; both judges gone. **§c:** the six squares by their corners (twelve records, six names, yours among them, `faceDesignationOf` = the inspector's `faceDisplayName` on every record); the lifted square `ABAC·ACAD·ACCD·ACBC of Ambo Dissection Tetrahedron`, its id the address; the store's act, the notice and the shelf's queue the same name; after C-13b the corner triangle at A reads `apex·apexB·apexC of …`; the fallback law; the composer consumed, not copied. **§d:** the positive control by the rule as it stood (the label's extent past −130 by the estimate's arithmetic); the lane derived (the label with its badges), the viewBox's left edge left of the label, a drawing of short labels keeping the floor of 118; the drawing rendered under node with the lane written on the svg and no ellipsis; the measurement pass's seam (a measured floor widening the lane and the reach by exactly that much; the component's `getBBox` pass in the source). **§e:** the positive control by the rule as it stood (the seat's longest pair, 71 glyphs ≈ 440 px at 11 px centred on a 150 px gap — ~145 px over each column's text); two casts with the seat's own long names and four pairs, the surface rendered — no text in the drawing holds `↦`, `yours` or `withdraw`, the four lines each with one index mark at the fold; every pair whole in the listing with its withdraw button, indexed 1…4 in the record's order, `· yours · ` once per pair, the listing after the drawing and before the refusal box; the refused pair's same move; §149 by construction.
- The neighbours on each cut: the cast loader 47 · the concept type 11 · the inside 33 · the midpoint 59 · the found bugs 24 · p1b 34 · the stone 20 · the lift carries 33 · the cargo 26 · the doors act 35 · the ambo gesture truth 17 · the born room 34 · the multi-region lift 20 · `diagnose-ambo` exit 0 · `diagnose-engine-freeze` ALL PASS at each cut (christening.ts classified NOT_FROZEN in its landing) · `npx tsc -b` exit 0 at each cut.
- The drive family: `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS` (149 clauses).
- **The sweep, whole, read by eye in its own call** — at `e987d1e`, before this record was written: `146 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (exit 0 — the road 1.3 s · the cast loader 8.1 s · the midpoint 8.7 s · the lift carries 4.9 s · the born room 57.6 s · the door column 80.1 s · the j-register 87.1 s; wall 94.5 s).

## What I CHANGED

- **`da25cd2` C-13a** — `src/lib/castLoader.ts` (NOT_FROZEN): the role parse carries and marks a label that is not text, qualities that are not an object, and every quality value that is not text; the not-taken address map reads the new key shape. `scripts/diagnose-the-road.cjs` NEW, §a.
- **`eddb1bf` C-13b** — `src/lib/christening.ts` NEW (NOT_FROZEN): `midpointLetters` · `isGeneratedMidpoint` · `isChristened` · `withChristened` · `givenLabelOf` · `composedLettersOf` · `recomposeUnchristened` · `migrateChristening`; `src/lib/ambo.ts` mints through `midpointLetters`; `src/store/geometryStore.ts` the christening act (every copy, the mark, the re-composition) and the old-files rule at import; `src/components/Panels.tsx` and `src/components/VertexPacketEditor.tsx` read `givenLabelOf`, the editor's Save sets the mark it sends; the manifest row; §b.
- **`eb553e3` C-13c** — `src/lib/subComplexLift.ts` (NOT_FROZEN): `faceDesignationOf` through the frozen `cornerCycleName`; the face's fallback an absence, the absent title `''`; §c.
- **`e987d1e` the eye** — the driver's `c13_arm` (the run's last acts) and the leg's §15, three clauses per viewport.
- **`dea1718 (its cure 6974190)` C-13e** — `src/components/MidpointSurface.tsx`: the pair's `<text>` at the fold replaced by an index mark; the listing `data-midpoint-role-pairs` below the drawing with the withdraw buttons; the refused pair's words moved into it; `scripts/diagnose-the-road.cjs` §e; the driver's `role_listing_arm` and the leg's §15 clause.
- **`f4650d7` C-13d** — `src/components/CastInsideDiagram.tsx` (not under an engine root): `labelWide`, the lane derived in `insideGeometry` (`labelLane` on the geometry, `options.labelLane` the measured floor), the legend at the derived lane, the component's measurement pass, `data-inside-label-lane` on the svg; `scripts/diagnose-the-road.cjs` §d; the eye arm's cast carries the seat's long label and reads it whole (§15's fourth clause).

## What I could not reach

- Nothing owed by C-13, M1, C-13d or C-13e is left. 

## W1 — the courier, at the record commit

- **the claims ledger** (`.handoff/THE_CLAIMS_LEDGER.md`) — CARRIED AS FOUND, not read for content, no re-seal: `blob 46b46229` → `2a9cfef8` (`sha256-LF b21f6802` → `c9fa3a77`), numstat 116/0, mtime 13:44:02; the tail's last characters: `eir withdraw) and `6974190` (the pair's index mark at the dense size); origin = `6974190`.` (closing on a finished line).
- **the sovereign ledger** (`.handoff/THE_SOVEREIGN_LEDGER.md`) — CARRIED AS FOUND, not read for content, no re-seal: `blob 2d731659` → `4b47dfa5` (`sha256-LF c74b98ce` → `0af079d4`), numstat 49/0, mtime 13:42:51; the tail's last characters: `ite vertex present AT the act (Δ108's problem), in parallel with the researcher. See §169.` (closing on a finished line).
- **the seat map** (`.handoff/THE_SEAT_MAP.md`) — CARRIED AS FOUND, not read for content, no re-seal: `blob 12b89165` → `d17a3920` (`sha256-LF e6dd49df` → `90732f13`), numstat 3/1, mtime 11:26:56; the tail's last characters: `ctor stalls the whole loop on Arman.** **Say *nobody* plainly when the ball is your own.**` (closing on a finished line).
- **the handover** (`.handoff/HANDOVER_THE_FINISH_LINE_how-to-use-the-connection-layer-end-to-end.md`) — CARRIED AS FOUND, not read for content, no re-seal: `blob 782825ab` → `7bab45cf` (`sha256-LF e6db6956` → `2ce43981`), numstat 7/5, mtime 01:15:24; the tail's last characters: `est the midpoint's drawing sits 40 px lower (two reserved lines, so a pick moves nothing).` (closing on a finished line).
- **ADR 0031** (`docs/adr/0031-the-concept-layer-is-a-cosheaf-of-partial-symmetries.md`) — byte-equal to HEAD: `blob e7565a44` = `sha256-LF b0795661`, mtime 19:45:24; nothing to carry, said.
- **The starter batch** (`.handoff/STARTER_BATCH_FOR_THE_NEW_SEAT/`, NEW — the masters of what the new seat received; not read for content): `1_WHY_YOU_ARE_HERE.md` (`blob 21759b1c` = `sha256-LF 77c95341`, 4293 B, mtime 11:26:22) · `2_THE_ENGINE_AS_A_USING_DEVICE.md` (`blob 3d817195` = `sha256-LF 006ff7f9`, 15015 B, mtime 11:51:06) · `3_HOW_THE_DEVICE_TAKES_A_CONCEPT.md` (`blob 12d4b52f` = `sha256-LF 9f2f162e`, 7090 B, mtime 11:11:03); tracked in this record commit through its own `.gitignore` negation (`!.handoff/STARTER_BATCH_FOR_THE_NEW_SEAT/`, the standing block, beside the handover's).
- **U1** (§126.2, every wave at each record commit): the sweep run from its home at `e987d1e` finds a NEW wave — 0 rulings, 1 instruments (candidates 77); `.gitignore`'s U1 block regenerated whole; the files to add [
 ".handoff/instruments/connection_layer_reference/triadic_act.py"
]. Tracked in this record commit: the record pair, the riders named above, the starter batch and its negation.
- **The inbox**: the C-13 letter, its M1 marker, the C-13d and the C-13e letters archived with this landing; the coder's inbox holds nothing.
