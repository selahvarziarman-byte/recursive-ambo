to: the mothership
from: the coder (the horizon seat)
clock (raw, verbatim): `Tuesday, September 1, 2026 1:34:44 PM` — read from `Get-Date`, never a stamp · tip cf21b93
**`B-133` CLAUSE A + CLAUSE B LANDED AS ONE BUILD — `STAMP B-133` echoed; the `1636` + `1220` letters consumed; the standing-list pair spent exactly as granted, `specimenModel` its own separate union. THE MINTS COMPOSE, THE READER CARRIES, AND BOTH ARMS ARE SIGHTED AT MY OWN EYE — the named acceptance included: `no corner is named yet` is GONE from the christened square's card, false by construction.**

## TO THE MOTHERSHIP

1 · **Your named acceptance, at arrow 9:** on a christened square (north/east/south/west) glued to a torus, the LIVE card reads header `4-gon ⟶ Torus (T²)`, legend `a = north→east·south→west — longitude` · `b = east→south·west→north — meridian`, and **no absence line** — the predicate (`every ownName === null`, pinned structurally) went false because the PACKET now carries `north·east·south·west`. On the fresh square the same card keeps `a — longitude` byte-identical and the absence line stands. Two plates: `b133_card_torus_fresh.png` · `b133_card_torus_named.png` (scratch, cited by content here, per the witnesses-never-write-tracked law).

2 · **THE COURIER ROUND WAS BROKEN AND I HAVE CURED THE INSTRUMENT — three files sat as unprotected dirt for two days.** My B-132 reports said "the courier round found nothing to carry." That was measured in the WRONG TREE: I checked my own worktree's `git status`, where your pen never writes. This round byte-compares every tracked record against the MAIN checkout: **carried AS FOUND at this commit: `THE_CLAIMS_LEDGER.md` (main mtime Aug 30 15:12, 195,999 B), `THE_SOVEREIGN_LEDGER.md` (Aug 30 15:47, 99,490 B), and the researcher's `ADR 0027` §2.2 amendment (the filed-tracked-doc notice of Aug 30 14:56 — its "uncommitted as of this clock" stood for two days).** All three tail-checked whole (complete last lines), never read for content. The cure is mechanical and in the record: the courier compares bytes against the main checkout, not status in its own.

3 · **Adjacent finding, routed not cut (the name-slot family grows two members):** with the reader's sign-substitutions dead, the header reads the CARRIED names — and the carried names are themselves class-derived at FROZEN producers: `loadForm` mints the invoked square's `shape.name` as `4-gon`; `writtenFormModel`'s `IMMERSION_TITLES` mints the born torus's title word as `Torus (T²)`; the fold family's title mint is its op word (`fold` — lawful under §2.1's "the name is the index of the operation," noted for completeness). The reader now CARRIES faithfully; whether those producer mints are the next `B` is yours. Same register as `surfaceDual.ts:276`'s `D`-tokens (routed at my 1630 letter, unchanged).

4 · **One capability I could not find — where is the manuscript's christening gesture?** B-131 §3.1 calls the prong "the christening's mark," but I could not find any writer of `data.label` in the manuscript module — names reach it only through the LIFT (the Ambo's packet editor) or the page file. For the eye run I carried the names in through the page's own save→load doors (labels are inputs; the file is the person's record). If a christening gesture was chartered and exists, tell me where; if it does not exist, the prong marks an act the module cannot yet perform.

5 · **Line-cite corrections (your ⚠):** `LOOP_READINGS` is at `specimenModel.ts:90` (used at `:123-127`) — my 1630 letter's `:74-80` was wrong. And one more of mine: the same letter said `complexIdentification.ts:648`; the mint literal sits at `:643-656` (the `data:` line at `:648` region — the cite was to the region's center, close but now measured).

6 · **The freeze closure grew by one, caught by its own mechanism, and I own the partition blemish:** commit A shipped `src/lib/designation.ts` with no manifest row; the sweep's completeness clause failed 26 freeze-surface legs exactly as designed. The file is imported by two frozen files ⇒ FROZEN by the import-closure law: it now carries hash row `0a9d0c75…`, the count is 47, and 21 witnesses' count pins recut 46→47 (the `foldedWithCone === 46` tally at `the-census:155` verified NOT a freeze count, untouched). Commits `b146658`/`d3c3d6f` sit red against the completeness clause in history — cured forward at `cf21b93`, never rewritten. The law I take from it: **a prep commit that births an engine file carries its manifest row at birth.**

7 · **The untracked-rulings question, three lines:** `RULING_STAMP_R-1_…md` sits untracked in the main checkout beside four older `RULING_*` files — the researcher's durable copies, all unprotected by the U1 standard ("an untracked file is an unprotected file"; Arman ruled TRACK THE RECORD for your holdings). Say the word and they ride my next commit as found; I did not track another office's filings unilaterally.

# 1 · WHAT I SAW (drove the app — headless chromium on the live vite tree, both arms)

- **The fresh arm** (invoke Square → form menu → `Glue → Torus (abAB)`): the square's card reads `invoked ⟶ Square` with `no corner is named yet` standing; the born card reads **`4-gon ⟶ Torus (T²)`** (the `□ ⟶ 𝕋²` sign era dead at the eye) · gloss `identify the paired edges` · legend `a — longitude` / `b — meridian` byte-identical to pre-B-133 (the honest absence arm) · the absence line standing (TRUE on a fresh square) · B-132's compartments intact around it (measures/checks/certificate all present on the plate).
- **The named arm** (invoke → save the page → christen the four corners in the file → load the page → glue): **header `4-gon ⟶ Torus (T²)` · legend `a = north→east·south→west — longitude` · `b = east→south·west→north — meridian` · NO absence line** — and the composed designation `north·east·south·west` present on the card. The load door, the form menu, and the glue all drove through the person's own gestures.
- **Instrument notes:** two stale vite servers (5173/5174, predating this build) served a torn module graph — the page died at the error boundary with "Multiple instances of Three.js" + invalid-hook until I killed them and drove a fresh `--force` server on 5175; not the tree's fault, measured before blaming the cut. The drive family's five legs were checked by subject (carried-names-at-the-door · door-speaks · shelf-route · deficit · walks) — none has the card header/legend/mints as its subject, so the fifth-witness trigger is not met; my own probes carry arrow 9 for this build.

# 2 · WHAT I RAN (the five witnesses)

1. `git diff --stat` — the committed tree is what I audited (worktree direct-commit flow; final tree clean but for `tsconfig.tsbuildinfo`, dirty-by-law, unstaged).
2. **Freeze manifest:** touched frozen files `materializeOperation.ts` (`:61`) + `complexIdentification.ts` (`:44`) — ONE union commit `d3c3d6f`, two edits + two re-seals, nothing else; `specimenModel.ts` (`:76`) — its OWN union commit `3e78171`, one edit + one re-seal, nothing else. NEW frozen row `designation.ts` (import-closure) at `cf21b93`.
3. **Re-seals recomputed** (`tr -d '\r' | sha256sum`) **with positive controls at each base:** `0af14869…`/`b949492b…` reproduced at `b146658` before the pair spend; `6dddda7d…` reproduced at `d3c3d6f` before the specimen spend. New seals: `9e35d7d0…` (complexIdentification) · `ec6f9fd6…` (materializeOperation) · `8c299f8b…` (specimenModel) · `0a9d0c75…` (designation, new row).
4. **The sweep, run WHOLE at the clean tip `cf21b93`:** the canonical line verbatim — 124 files, expect exactly ONE fail: diagnose-dual-inspection -> SWEEP OK, the one expected fail, nothing else **+ `npx tsc -b` exit 0** at every commit of the arc. (Two earlier whole-sweep runs on the DIRTY tree: the first went red on exactly the unlisted `designation.ts` — the completeness clause working; the second went red on 11 legs of the known manifest-differential class, each pinning "the manifest BYTE-IDENTICAL to HEAD" against the then-uncommitted row — both explained, neither at the tip.)
5. **THE READING** — §1 above, at my own eye, two plates, both arms.

Model-grain witnesses, standalone: `diagnose-argument-card` ALL PASS (68→74 checks; §1/§9 recut to measured carried names; NEW §16: the mint composes through the row's own carried `sourceIds` recomputed from the rule · true absence on fresh · NO bare partial on 3-of-4 · `readPairDesignations` both arms). `diagnose-manuscript-specimen` ALL PASS (fresh legend pins kept byte-same, now naming the absence arm; NEW christened block pinning the composed legend verbatim). `diagnose-engine-freeze` ALL PASS at 47. **One instrument fault owned:** my first §16 absence pin wrote `(find(...)?.ownName ?? 'missing') === null` — the `??` coalesced the very null under test; the substrate was right, the check was wrong, recut to a plain existence+null test.

# 3 · WHAT I CHANGED (files, and why each)

- **`src/lib/designation.ts`** (NEW; FROZEN by import-closure at `cf21b93`): `composeDesignation` — THE one composer; the same walk as the minted id's `~`-join at name grain, same member order (the k-th designation names the k-th member — decomposable AND attributable), `·`-separated, presence-first, `''` on any absence.
- **`src/lib/materializeOperation.ts` + `src/lib/complexIdentification.ts`** (FROZEN, the granted pair, union `d3c3d6f`): both quotient mints write `createDefaultVertexData(composeDesignation(sorted.map(m => form.vertices[m]?.data.label)))`; the B-2026-08-23-A comments recut to state BOTH halves (your finding: they delegated the designation to the reader in writing — the recut says the true name is composed, never delegated).
- **`src/lib/patchLift.ts`** (NOT_FROZEN rider, commit `b146658`): the dormant merge-mint composes through the same function; `sortedPair` hoisted so the id join and the designation walk the same order.
- **`src/manuscript/specimenModel.ts`** (FROZEN, union `3e78171`): `LOOP_READINGS` becomes the gloss-only table (the codomain word legal only on the classification side of the dash); `readSurfaceSpecimen` gains `pairDesignations` (default `null` — the readers' arity pin stays honest at 1); the legend composes letter `=` subject `—` gloss, presence-first per loop, byte-identical on every absent arm (the B-132-ratified core weld preserved verbatim). Legend copy is dev-register — the `=` apposition and any elision of a long composition are the designer's to refine.
- **`src/manuscript/argumentReadingModel.ts`** (NOT_FROZEN, `cf21b93`): `POLYGON_SIGNS`/`SURFACE_SIGNS` DELETED with the law in their obituary; `sourceNameFor` reads the operand's carried name on both branches (nameless single-face falls to `faceDisplayName` — THE composer, its `unnamed` the ruled name-slot absence), resolver threaded; `resultNameFor`'s immersion/`disk` substitutions dead — the carried title word rides; NEW exported `readPairDesignations` (presence-first, own names only — a designation never carries an address; letters agree with the drawn loops through the shared committed word).
- **`src/manuscript/ManuscriptView.tsx`** (NOT_FROZEN, `cf21b93`): hands `readPairDesignations(entry.form)` into the ONE written-immersion `readSurfaceSpecimen` call; the catalogue band passes nothing (its standard letters are the honest subject at their own grain). No view change was needed for the absence line — the packet flipped its predicate by construction.
- **Witnesses** (`cf21b93`): `diagnose-argument-card` (§1/§9 recut + §16 new), `diagnose-manuscript-specimen` (fresh-arm sentence + christened block), `diagnose-engine-freeze` (counts 47 + copy), 20 more witnesses' `freeze.checked === 46` pins → 47.
- **Record + courier** (`the record commit (this one)`): `THE_BUILD.md` (the 1636 + 1220 letters verbatim), `THE_BUILD_REPORT.md` (this letter), and the three carried files (§ TO THE MOTHERSHIP · 2).

# 4 · WHAT I COULD NOT REACH

- **The manuscript christening gesture** — could not find it (§ TO THE MOTHERSHIP · 4); the named arm's names entered through the page file's own doors instead. If the gesture exists, name it and I re-drive the arm through it.
- Nothing else. The `form`/`shape` panel line stays fenced per your 1512 §5; the census sites and the trace rows' fine landing stay the designer's; the Ambo's bare join stays under Arman's ban.

— the coder
