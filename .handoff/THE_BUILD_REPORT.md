to: Mothership
from: Coder
date: 2026-09-25 18:18 +03:30
ECHO: **C-14g** (word triads, the false line, two checks) · **C-14g · M1** (the import input) · **C-14g · M2** (saving first — Arman's Δ112) · **C-14 RATIFIED §182** (the letter of 17:17, consumed into the record with this landing)
base: `0f7bf0d` → C-14g `1ef86ac` · the record: the commit carrying this letter (its SHA in the ring)

## TO THE MOTHERSHIP

0. **M2 (saving first), as it happened:** M1 — the import input's reachability and the export → import round trip, byte-equal with its positive control — landed inside `1ef86ac` at 18:13:09, together with the rest of C-14g, and was pushed; your M2 asking for M1 FIRST as its own commit arrived at 18:15. Splitting it now would rewrite published history, which the git-safety law forbids; I did not. What the use needs is in `1ef86ac`: the import input (`data-workspace-import-input`, hidden beside its button, the cast input's construction), the round trip witnessed under node (§h: four casts, a plain pair, a plain word pair, a role triad and two word triads; the shapes byte-equal, AB's reading the same; the control pair made after the export absent) and at the eye (§17: the download captured, a pair made after it, the file set on the input, the state as exported). The pinned server for the use can serve `1ef86ac` or any commit after it. If you want a commit that carries M1 ALONE for the release's announcement, that is a revert-and-reapply pair on top of history, not a rewrite — say so and I make it.
1. **§2a, in one line:** the count moves only with WORD PAIRS and SPOKEN LIGHTS, never with a withdrawn act — measured at the eye's casting on AB: unglued **27** (25 words of the two casts plus the two FEET `≡_C` and `≡_D`, in the signature from the first read — C-12b, ratified §147), with C's triad 28, withdrawn **27**; with the three word pairs and three role pairs **24** (three words translated), with C's and D's triads 26, both withdrawn **24** — the reading byte-equal after every withdrawal (the witness §h, with its control). The designer's 25 and 27 are two states, not a residue: 25 = the glued state with one light spoken (24 + `⟨C⟩`, the trace line's number on her plate), 27 = the unglued state (no word pairs). **`the corners' views`:** yes, it should name what it counts — the 2 words are the feet. Proposed copy, not built (it is §149's sentence, pinned in the midpoint, the stone and the eye): `2 of the words are the corners' views — ≡_C · ≡_D (no tuple yet)`. Your ruling and I cut it.
2. **§2b, the index:** the plate at 1689 (`concept-layer-triad-glued-by-lights-1689x897.png`, from the f leg) shows the index spaced to the eye by its margin; the TEXT had no space (`1F13`), which is what her pane and a copy read. Cured: the index is now one text node `1 ` — spaced in the text and to the eye alike; the witness §h pins the source and the rendered text, the eye reads the browser's text at both viewports (§17).
3. **M1, the two inputs, measured:** the cast input is `display:none` (`hidden`), always mounted with its panel, with a data attribute (`data-cast-file-input`), opened by a sibling button through a ref. The import input was a visually-hidden (`sr-only`: in layout, 1 px, clipped) `<input type=file>` INSIDE a `<label>`, with no attribute, id or name — reachable only by a click on the label (the OS dialog) or by a bare `input[type=file]` selector, which on a page with a seed selected finds the cast input first. **That is why an upload tool reaches one and not the other.** Cured by the same construction: a button `data-workspace-import` and a hidden input `data-workspace-import-input`; the export button carries `data-workspace-export`. Witnessed under node (§h: the round trip byte-equal with its control) and at the eye (§17: the download captured, a pair made after it, the file set on the input, the state as exported).
4. **The reload cost, named:** `/__whereami` on 5173 answers `checkout: main, head 0f7bf0d` — Arman's server serves THIS working tree. A landing (commit, push) changes no served file and reloads nothing; **every source edit I make under vite's watch does** — a component edit is a hot module swap, a store or lib edit re-evaluates the store's module and the page's in-memory state (casts, the Ambo, pairs) is gone. Today's C-14 cuts edited the store and the resolver several times. A pinned server on a separate checkout (or my building in a worktree, `wt/*`, which the serve advances from) ends it; until then every source edit of mine is a wipe of every open tab on 5173, and I will say so before each cut.
5. **Word triads, one thing said:** the light's word row is C's WHOLE vocabulary (the caster's order, nothing lit, sorted or pre-paired); with the light open, a word in A's or B's row is the word triad's pick, as a point in the columns is the role triad's — the plain word pair is made with no light open, as the plain role pair is.

## What I SAW

**The drive family — `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS` (181 clauses, 169 → 181; §17 6 per viewport), served `0f7bf0d` (HEAD `0f7bf0d`, dirty 12 — the C-14g working tree before its commit), both viewports 1689 × 897 and 1400 × 900; the leg ran twice on this cut — the first 179 PASS · 2 FAIL, one instrument red at both viewports (the D row pinned at twelve words where phi carries fourteen; my earlier print of the cast was cut at twelve), cured in the instrument; the second whole.**

At AB (gen 1), what a person sees:
- **The cured line** (§1): with nothing given on the edges that reach C or D, each source reads `its light is open to a triad now; its own reading of A–B needs your pairs on C–A and B–C`; and the listing's index is spaced in the text — `1 F5 ↦ Φ7 · yours, plain · withdraw` (§2b).
- **The light's word row** (§3): before the open two rows (A's, B's); C's drawing opened — a third row `C's words` with the T cell's six words in the caster's order (sustains · outlasts · individuates · removes · starts · answers-to), none lit, marked translated or picked; D's opened — `D's words` (phi's twelve) in its place.
- **A word triad, three picks in any order, one act** (A · C · B here): after `disjoins` in A's row the pending line reads `a word triad in C's light: disjoins · — · —` below the drawing with `withdraw this attempt`; after `removes` in C's row `disjoins · — · removes`; `component-of` in B's row completes it — the pending line gone; nothing above the drawing changed height across the picks; the picked chips kept their place and size (colour only).
- **The word respect in C's block, the same grammar:** `you said: disjoins is component-of, as regards removes — not yet: disjoins not paired on A–C · nothing paired on C–B` with `withdraw this triad`; the word-pairs box below reads its plain pairs `· yours, plain ·`.
- **A word pair glued by respects:** (disjoins, component-of, decays) given in D's light too — `disjoins ≡ component-of · glued — you gave it in C's light and in D's`, no withdraw; both word triads withdrawn by their hands and the light left — the word pairs as before, no light row.
- **The import input** (M1): `Export Workspace JSON` downloads the workspace (? B); a pair made after the export changes AB; the file set on `[data-workspace-import-input]` — a hidden input beside its button, the cast input's construction — imports it (`Workspace JSON imported.`), and AB reads the lines and word pairs as exported, the later pair absent.
- **The 169 clauses of before** all green on the same run (§1's source line re-pinned for the cured sentence).
- **The plates** (the ignored `scripts/app-leg/_frames/`): `concept-layer-word-triad-pending-*.png`, `concept-layer-word-triad-said-*.png`, `concept-layer-word-triad-glued-*.png`, and the workspace file `workspace-export-*.json`, at both viewports.

## What I RAN

**Commit `1ef86ac`.** `npx tsc -b` exit 0 · the freeze ALL PASS · `scripts/diagnose-the-respects.cjs` 45 clauses ALL PASS (§h NEW — its measured line: `the eye's AB: words unglued 27 (the feet ["≡_C","≡_D"] among them) · with C's triad 28 · withdrawn 27 · glued (three pairs, three words) 24 · with C's and D's triads 26 · withdrawn 24`) · the midpoint · the stone · the born room · the road · the inside · the census · the concept type · the found bugs · the cast loader · workspace persistence · the doors act · the cargo · the lift carries · the born face · the J register · ambo · snapshot · P5 ALL PASS at this tree · THE DRIVE FAMILY as above · the sweep, whole, at `1ef86ac` after the push: `147 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (wall 102.1 s).

**The pre-commit witness list:** (1) the committed tree IS what was audited — staged from the tree the witnesses and the leg ran on; HEAD == origin after the push; (2) the manifest: MidpointSurface.tsx and Panels.tsx under no engine root; no `src/lib` file touched; no frozen file; (3) no re-seal; (4) the sweep green at its own canonical line, tsc 0; (5) the reading — the leg and the plates above.

**The reload cost of this landing:** the source edits of this cut (MidpointSurface.tsx, Panels.tsx) reloaded every open tab on 5173 through vite's watch as they were saved; the commit and the push reloaded nothing.

## What I CHANGED

**Commit `1ef86ac` — C-14g · M1.**
- `src/components/MidpointSurface.tsx`: the false line cured — each source reads `its light is open to a triad now; its own reading of A–B needs your pairs on C–A and B–C` (no sibling says the corner is inert until mapped — swept); the listing's index `1 ` as text; the light's word row (`data-midpoint-words="light"`, chips `data-midpoint-light-word`, none lit, sorted or pre-paired; D's replaces C's); the word triad's picks in any order (a row's word in a light is the word triad's; the third pick makes the act through the store's `giveTriad(…, 'word', …)`), its pending line `a word triad in C's light: sustains · — · removes` below the drawing with `withdraw this attempt`; opening a light clears both kinds of picks.
- `src/components/Panels.tsx`: the import input by the cast input's construction (a button through a ref; a hidden input with `data-workspace-import-input`); `data-workspace-export` on the export button.
- The witnesses: the midpoint §4 and the eye §1 re-pinned by name for the cured line; the respects witness §h NEW (the cured line and the source sweep; withdraw-all byte-equal at the eye's casting with the counts printed; the index; word triads through the store read on the surface, a word pair glued by lights; the export → import round trip byte-equal with its control and the input's construction pinned) — 45 clauses ALL PASS; the driver's `word_triad_arm` and `import_arm`; the leg's §17 (6 clauses per viewport).

**The record commit:** `THE_BUILD.md` (the ratification letter of 17:17, C-14g and its M1, verbatim), `THE_BUILD_REPORT.md` (this letter), the W1 riders as found, U1.

## What I could not reach

- Nothing of the mandate. The feet's naming (§1) waits on your ruling.

## W1 — carried AS FOUND (never read for content; no re-seal)

- **the researcher's ruling THE INSIDE-MIDPOINT-TRACE REFUSAL (named by the mothership for this record)** (`.handoff/RULING_THE-INSIDE-MIDPOINT-TRACE-REFUSAL_on-the-honesty-line_midpoint-is-the-pushout-over-the-CORE_trace-is-its-origin-partition.md`) — byte-equal to HEAD: `blob 3c864dd8` = `sha256-LF 00a639fa`, mtime 14:05:17; nothing to carry, said.
- **ADR 0031 (the researcher's amendment, §3.11)** (`docs/adr/0031-the-concept-layer-is-a-cosheaf-of-partial-symmetries.md`) — byte-equal to HEAD: `blob e0e04454` = `sha256-LF 850d5e79`, mtime 14:52:29; nothing to carry, said.
- **the claims ledger** (`.handoff/THE_CLAIMS_LEDGER.md`) — CARRIED AS FOUND, not read for content, no re-seal: `blob a1e18326` → `e38814b0` (`sha256-LF 933a7a44` → `4d824d16`), numstat 14/0, mtime 17:23:57; the tail's last characters: `andings never touch the customer's page.` (closing).
- **the sovereign ledger** (`.handoff/THE_SOVEREIGN_LEDGER.md`) — CARRIED AS FOUND, not read for content, no re-seal: `blob 62ae00a8` → `04db8068` (`sha256-LF 89a7cdc0` → `6445d9b9`), numstat 3/0, mtime 18:14:55; the tail's last characters: `ores it; the import input is reachable).` (closing).
- **the starter batch's second master (the mothership's, for the new seat)** (`.handoff/STARTER_BATCH_FOR_THE_NEW_SEAT/2_THE_ENGINE_AS_A_USING_DEVICE.md`) — CARRIED AS FOUND, not read for content, no re-seal: `blob 3d817195` → `4e9e4ade` (`sha256-LF 006ff7f9` → `a6b0d93c`), numstat 21/5, mtime 17:15:49; the tail's last characters: `thought and you make the device take it.` (closing).
- **the starter batch's third master** (`.handoff/STARTER_BATCH_FOR_THE_NEW_SEAT/3_HOW_THE_DEVICE_TAKES_A_CONCEPT.md`) — CARRIED AS FOUND, not read for content, no re-seal: `blob 12d4b52f` → `4d86686e` (`sha256-LF 9f2f162e` → `b02e6ebf`), numstat 2/2, mtime 17:15:40; the tail's last characters: `ion, because it is shared by definition.` (closing).
- **U1** (§126.2, every wave at each record commit): NO new wave — 0 rulings, 0 instruments (candidates 78); `.gitignore`'s U1 block regenerated whole; the files to add [].

The four letters (the ratification, C-14g, C-14g · M1, C-14g · M2) archive with this landing; the coder's inbox holds nothing.

⇒ NEXT: wake the mothership (ratify C-14g; rule the feet's naming).
