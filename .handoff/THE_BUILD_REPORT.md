to: the mothership
from: the coder (the horizon seat)
clock (raw, verbatim): `Saturday, August 29, 2026 7:30:24 PM` — read from `Get-Date`, never a stamp
**STAMP ECHOED: `C-1` (the ratification consumed) · MARKER ECHOED: `V1`.** The queue re-read before the commit step — the three letters, no new mail. The build order ran as your §4 set it: the measurement pasted → the resolver + the three concept-reference sites → `joinNames` → both comments replaced with the principle → the two doctrine counts → (the driver untouched, sequenced second).

## TO THE MOTHERSHIP

1. ✅ **THE §3 MEASUREMENT, PASTED (run before the cut, at the committed doors):** root ids `["w970:v0","w970:v1","w970:v2","w970:v3"]` → `idTail` = `["v0","v1","v2","v3"]`; edge letters (relation row labels) = `["a","b","c","d"]`; **collision set `[]` — NO COLLISION.** The `:529-530` comment's claim is now a run, not a comment. ⚠ One scope named, not ruled: **the tail is universe-relative** — on a COMBINE-born card two parents' corners can both read `v0` across rows; the full id would distinguish; the address's long form is the designer's if she wants one. The principle comment in the source names this scope as unruled.

2. ✅ **THE PRODUCER QUESTION ANSWERED (yours in C-1 §3, left to me in V1): TWO producers, now ONE LAW.** The spine's `unnamed·unnamed` came from `endpointNameOf`; the incidence/stance/verdict `@ unnamed` came from the concept-row LABEL map (`:801/:820/:842` reading the name slot into a reference position). The cut makes `endpointNameOf` the one reference resolver (own name ?? address; a merged class composes its members' reference reads — which converges with the row label everywhere the label is real and diverges exactly where it was the absence token), and the three sites read THROUGH it — their `?? raw-id` fallback chains, including `:842`'s full-id arm, die by construction. `rootDisplayBase` (`:387`) is UNCHANGED, as you ordered unless it changed a reading: its remaining consumers are the memorial (a name slot), the dedup census (term machinery), and the concept row's `← …` rootLabels (held for the designer) — stated here, not cut for symmetry.

3. ✅ **`joinNames` — the kind-based separator landed:** unconditional `·`, the glyph left hers; the length-rule comment died with its rule; the witness's independent comparator recut to the same law (it re-implemented the old one — `'unnamed'` + length-join — and would have redded the cure).

4. ✅ **BOTH STALE COMMENTS DIED AS ORDERED, replaced by the PRINCIPLE with both scopes** (within a row position carries — across rows it carries nothing; an address is not a handle — the form already has it), per the ledger's §11 law. The B-103 §2e id-key rider kept, its example corrected (two universes' `v0` can still share a label — the id-keying stays load-bearing).

5. ✅ **THE DOCTRINE COUNTS: `CLAUDE.md` §6 clause 4 and the substrate map's TWO rows** (`:74` and the `:78` footer — the ruling's own sweep law; you named one, the file held two) now point at the sweep's self-judgment and restate no number.

6. ⚠ **YOUR STOP DID NOT FIRE, and here is the measurement:** the cure is sighted at the eye WITHOUT `lift.drive` — the invoked square (one gesture: invoke + select) and the zoo torus (one click: load the reference zoo + select) carry the eye rung. **What I could not sight is narrower and named in §WHAT-I-COULD-NOT-REACH:** the fold-GESTURE-born cone's card specifically — the fold tap would not take my instrument's pointerdown (the same block class your predecessor's report recorded for the point-form), while the model half is measured (`v0·v2` rim · `v1` apex) and the zoo torus sights the same merged-class mint (`mat:`) through the same one resolver. The gesture-instrument question belongs to the driver repair you sequenced second; I did not repair it unsanctioned.

7. **SHAs:** the cure `2b80fae` (4 files, 54+/38−) · the record pair rides the record commit (this report rides it). Both pushed to `origin/team-arman`, fast-forward.

**Queue: empty. Next actor: the mothership** — the cure awaits your ratification and the designer's eye (the glyph and any long-address form are hers); the deficit-driver repair (priced in my C-1 letter, two witness files) awaits your word, `webapp-testing` read first per V1 §1.

## WHAT I SAW

**The invoked square's card, at my own eye on the running worktree app (5174; 5199 untouched):** spine `—a ← v2·v3 · —b ← v1·v2 · —c ← v0·v1 · —d ← v3·v0` — four DISTINCT right-hand sides; incidence `c ⌐ d @ v0 · b ⌐ c @ v1 · a ⌐ b @ v2 · a ⌐ d @ v3`; stance `v0 : 90 = 90 …`; verdict `v0 +90° — a rim turn …` four distinct; the name registers untouched — `•unnamed — born` ×4, `◻ unnamed · 4 corners`, `no corner is named yet` once. **The zoo torus's card:** spine `—a ← v0·v1 → v2·v3 · —b ← v1·v2 → v3·v0`; the concept row `•unnamed ← four unnamed roots` (the count form, untouched, correct beside the cure); incidence `a ⌐ b @ v0·v1·v2·v3`; stance `v0·v1·v2·v3 : 90 ⊕ 90 ⊕ 90 ⊕ 90 = 360`; verdict `Σδ = 0 ⇄ tiles · at its Form`. Three registers on one screen, each in its own grammar, and the figure's ring letters (`a b c d` at the corners) share no glyph with the addresses. Her thirteen identical `unnamed`s are gone from the reading; the one that remains means something.

## WHAT I RAN

The §3 measurement probe (pasted above, before the cut) · `npx tsc -b` → exit 0 at the cut · `node scripts/diagnose-argument-card.cjs` → **ALL PASS** (comparator recut to the ruled law first) · the whole sweep PRE-commit → RED with exactly the two DOCUMENTED manifest-differential transients (`diagnose-the-folded-edge` · `diagnose-the-ink`, each `content-moved vs HEAD: [argumentReadingModel.ts]` — the §3-harness transient class, and I ran both individually to confirm the clause) · **the authoritative sweep at the committed tip `2b80fae`, verdict verbatim:**

```
124 files · expect exactly ONE fail: diagnose-dual-inspection
SWEEP OK — the one expected fail, nothing else
(runner exit 0)
```

· the after-cut probe on five committed subjects (square · glue-torus · fold cone · placed edge lift · dual-of-torus): the lift control UNCHANGED where names exist (`@C @A @AC`), the cone model reads `v0·v2` / `v1` · the eye drive above. Port 5174 released at close (verified).

## WHAT I CHANGED

`2b80fae` — `src/manuscript/argumentReadingModel.ts` (the reference resolver + the three sites + `joinNames` + the principle comments; NOT_FROZEN row printed pre-edit) · `scripts/diagnose-argument-card.cjs` (the independent comparator to the ruled law) · `CLAUDE.md` + `.handoff/THE_SUBSTRATE_MAP.md` (the census pointers). the record commit (this report rides it) — `.handoff/THE_BUILD.md` (your three letters verbatim) + `.handoff/THE_BUILD_REPORT.md` (this report); the two-file delta touches no witness subject, so the tip verdict above is the record commit's verdict too. `tsconfig.tsbuildinfo` dirty-by-law, unstaged, both commits.

## WHAT I COULD NOT REACH

**The fold-gesture-born card at the eye.** The tap targets are alive to a real pointer (hover flips the cursor — measured), but a trusted synthesized pointerdown at the measured mesh center does not fire the reducer (logged arriving on the canvas at the exact pixel; state unchanged), and the seam's published targets were overwritten between two overlay instances during the summon — the same instrument-block family the departing seat recorded for the point-form. Sighted instead: the zoo torus (the same `mat:` merged-class mint through the same one resolver) and the whole square card. The mechanics belong to the driver repair, sequenced second. **And two disclosures:** (1) the two tracked files modified by another hand during my first session (`INITIATION_…` and the claims ledger) remain uncommitted in the main checkout — my commits do not touch them and I did not commit them; (2) this letter reached the inbox by shell copy again (the background-isolation guard; gitignored mail, no tracked state touched).

— the coder
