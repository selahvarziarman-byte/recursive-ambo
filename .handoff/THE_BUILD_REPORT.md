# THE BUILD REPORT — B-2026-08-25-A: the count form landed (her table, verbatim, live at the eye) and the reason is total over the enabled — the formerly silent conjunct speaks on the case that reaches it

**STAMP ECHO: `B-2026-08-25-A`** — the build I executed is the one you wrote.
**coder · 2026-08-22 · commits `e0c20d9` (§2 + §3, one commit — ManuscriptView carries both cuts, disclosed) · `a00fd98` (mandate record) on base `4caf00a` · branch `team-arman` · pushed after this report lands**

---

## TO THE MOTHERSHIP

**1 · §2's boundary held and nothing degraded — no STOP condition fired.** The handle came out of the SET LINE only. The other two term positions were then measured on the same cards: the composed class label still reads `•unnamed·A·unnamed·B·unnamed·C·unnamed·D ← …` and relation sources still read `—a ← unnamed·C·unnamed·A` — exactly as before the cut, terms distinguishable by their handles. Her ruling was not extended to a position she has not seen.

**2 · The §3 sweep's one adjacent finding, named:** with a FOLDED verdict body selected, the op-group rows read `Select a form first.` — a speaking-but-WRONG sentence (something IS selected; `operationAvailabilityFor`'s null-shape branch cannot tell "nothing selected" from "selected but unresolvable"). Same root as the cured gap (targetFor's domain), one seam over. Reported, not cut — it is a sentence choice on a shared branch and the cure's scope was the identify chip.

**3 · A protocol note in your own direction:** the new standing rule (in-flight rulings arrive as their own files in `.handoff/inbox/coder/`) is received; this build's mandate was read once, whole, and nothing arrived mid-flight.

---

## 1 · WHAT I SAW — both items at the eye

### 1.1 · §2 — the count form, her cases live

The glue-born torus's ring line reads **`unnamed ← four unnamed roots`**; the fold-born disk's reads **`unnamed ← two unnamed roots`** — where the false sentence `{unnamed, unnamed, …}` (identical manufactured tokens claiming distinct places) stood. The named/mixed/collision cases are pinned FROM THE RULE in the witness (her table verbatim): `{AB, CD}` · `two unnamed roots` · `{AB, one unnamed root}` · `two roots named AB`. The composer (`mergedRootsPhrase`) reads the roots' PLAIN names (`rootOwnNames`: the label, the honest id tail when the packet is out of reach, `null` on true absence — no handle, no word) and counts what it cannot tell apart; a single counted group stands bare (braces would dress a count as a set of one); a mix keeps braces with the counted phrase riding as a term.

### 1.2 · §3 — the cure, measured on the case that reaches it

`identifySewReason` is derived FIRST and `enabled` IS `reason === null` — the fold/thicken idiom, totality **by construction**, one branch per conjunct of the old predicate. **The formerly silent case fired live:** a folded verdict body (built through the aperture — the orbifold wall spoke its full researcher sentence naming the folded edge class) selects as `folded:…`, a band `targetFor` resolves nothing for; the identify chip greyed at opacity 0.38 **and spoke**: `identify — trace two walks; the seam is the way you sew them — this specimen resolves no traceable form`. ⚠ the second branch's wording names the fact only — final copy flagged to the designer.

### 1.3 · THE SWEEP — every chip's `enabled` vs its `reason`, all N

| chip / rows | enabled predicate | reason coverage | verdict |
|---|---|---|---|
| fold | `foldReason === null` | `foldReason` itself | **total by construction** (one producer) |
| thicken | `thickenReason === null` | `thickenReason` itself | total by construction |
| **identifySew** | was `selected && targetFor(selected)` | was one branch of two | **THE GAP — cured** (now the by-construction idiom) |
| explore | `exploreEligible !== null` | the same predicate's one branch | total |
| chord (menu) | three named refusal branches | every `enabled:false` return carries its reason | total |
| op-group rows | per-op `canApply(context)` | `getDisabledReason(context)` — the gate's own word | total by construction |
| op groups, null target | `!shape` | `'Select a form first.'` — always speaks | total but **WRONG-SENTENCED on a `folded:` selection** (TO THE MOTHERSHIP 2) |
| aperture gate (glue/leave-bounded/subdivide), zoo, shelf doors | refusal-in-place or ungated | the refusal renders where the button would be | total / nothing to silence |

No other chip has more conjuncts than reason branches. The idiom the comment held is now held by construction at the one place it wasn't.

### 1.4 · §4 honored

`collapse`'s unreproduced observation was **not chased** — no probe, no spend, awaiting Arman's route.

## 2 · WHAT I RAN (the five witnesses)

```
1  working tree clean at the tip (the two clean-tree census witnesses —
   the-folded-edge, the-ink — went red mid-flight on the uncommitted work
   and green again the moment it committed; their pin is `git diff HEAD`
   emptiness over src, working as designed)
2  freeze manifest: no frozen file touched this build (argumentReadingModel /
   ManuscriptView / the witnesses — NOT_FROZEN, verified)
3  no re-seal owed (no frozen union in this mandate)
4  suite at the FINAL head: 112 files @ exactly 1 fail = diagnose-dual-inspection
   (accepted baseline). npx tsc -b → exit 0. diagnose-argument-card gains the
   §1b COUNT FORM leg (her table pinned from the rule + the live torus row's
   rootOwnNames = four nulls); the app-leg fold pin recut to the ruled string
   (`unnamed ← two unnamed roots`) — recut from the rule, the app-leg itself
   not re-run this build (its surface was driven by hand at the eye instead)
5  THE READING — §1 above: both count cases live on the ring; the folded
   body's silent-grey case fired and SPOKE; the sweep read from the code with
   the chip states re-verified at the screen during the drive
```

## 3 · WHAT I CHANGED

- **`src/manuscript/argumentReadingModel.ts`** (`e0c20d9`): `mergedRootsPhrase` (exported, the ruled grammar); `rootOwnNames` on every row (`rootPlainOf` — label / honest tail / null); relations carry `[]`.
- **`src/manuscript/ManuscriptView.tsx`** (`e0c20d9`): the ring's merged line composes through `mergedRootsPhrase` (the handle out of the set line); `identifySewReason` derived first, `enabled: reason === null` (§3's cure).
- **`scripts/diagnose-argument-card.cjs`** (`e0c20d9`): the §1b count-form leg.
- **`scripts/app-leg/deficit_app_driver.py` + `diagnose-deficit-app.cjs`** (`e0c20d9`): the fold-case merged-line pin recut to the ruled count form.
- **`.handoff/THE_BUILD.md`** (`a00fd98`): the mandate record.

## 4 · WHAT I COULD NOT REACH / DID NOT TOUCH

- **The other two term positions** — held byte-exact per the ruling's boundary; their readings reported (TO THE MOTHERSHIP 1) for her term-spec.
- **The op-groups' null-shape sentence** on `folded:` selections — reported, not cut (scope).
- **The app-leg drive** — its pins recut from the rule; the leg not re-run (the same surface was driven by hand; disclosed).
- **The folded-body card** — selecting a folded body raises NO card at all (only the caption and the aperture's wall notice speak for it); noticed while driving §3, named here for the record — whether the folded verdict deserves an ON SELECT reading is a meaning question I did not open.
- **§4 / §5** — untouched as ordered.
