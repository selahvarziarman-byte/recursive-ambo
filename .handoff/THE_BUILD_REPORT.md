# THE BUILD REPORT — B-2026-08-24-B (+ the mid-build amendment): all seven landed in the ordered sequence, the ELISION ROW shipped with its seven clauses, and the §9b OCTAHEDRON CENSUS is measured — identify is LIVE on the closed complex and the cut road is real

**STAMP ECHO: `B-2026-08-24-B`** — the build I executed is the one you wrote (the -A text was never worked from; the mid-build §9/9b/9c amendment was received, recorded, and executed in the same cycle).
**coder · 2026-08-22 · commits `ac57b77` (R1) · `2853cb6` (§6) · `e54f451` (R2) · `991955a` (R7) · `631d193` (§7) · `0685fc8` (§8) · `b28218a` (§5, frozen union) · `fc3057c` (mandate record) · `3ac287f` (§5 tail) · `669dc29` (report) · `94138d7` (the elision row) · `0b07e25` (the amendment record) on base `a4f7d04` · branch `team-arman` · pushed after this report lands**

---

## THE AMENDMENT'S TWO DELIVERABLES (§9 + §9b), ON TOP OF THE SEVEN

### A · THE ELISION ROW — built, seven clauses, at the eye
On the 12-face loop×I band's card: **`◻ 12 faces in all — none named here`** — her string verbatim. ◻ retained (1) · the number IS `faceRows.length` (2) · `in all` (3) · the withheld naming positively marked (4) · **no route** — past 8 the named rows do not render, and none is offered (5) · the muted overflow register, not entry grammar (6) · no `· N corners` (7).

### B · THE §9b CENSUS — the octahedron on the dock, measured from the screen (route: ambo dissection → core cell → `Lift selection → Manuscript` → placed → selected)

**The dock, all N from the screen:** ten op chips — glue · flip-glue · collapse · cut · dualize · sew · fold · thicken · **identify** · explore inside — plus Fit Selected / Reset Camera, `load the reference zoo` / `aperture — build a 3-manifold`, and the shelf's three doors. Every chip SPEAKS in both states; the sentences verbatim:

| chip | octahedron SELECTED | nothing selected |
|---|---|---|
| glue / flip-glue / fold | `A word (e.g. abAB) is addressed to the slots of ONE polygon — this form is a COMPLEX (8 faces), where the word denotes nothing. Sew its boundary instead: use identify / the sew-boundary-* ops, which act on the whole complex. (Or cut / combine.)` | `select a form first` |
| collapse | ⚠ its ONE row `Collapse → Sphere (D²/∂D²)` is DISABLED with the same word-gate sentence — **Arman's "only collapse would take it" did NOT reproduce on this route**; his octahedron may have entered by another door (⚠, not ✔ — route unestablished) | `select a form first` |
| cut | **`Select a face to operate on.`** — no form gate; the picker lists all 8 faces NAMED (`AB·BC·AC · 3 corners` … — R2 standing here too) | `select a form first` |
| dualize | plain `dualize` — enabled | `select a form first` |
| sew | `The form has 0 boundary circles — sewing needs two.` | `select a form first` |
| thicken | `select two forms — click the shape, shift-click the segment` | same |
| **identify** | **ENABLED** (opacity 1, cursor pointer) — pressing it OPENS the trace mode: `identify — trace two walks · tap the corner you start each edge from — the tail lights there… · A: 0 · B: 0 — tracing walk A`, tap targets on every corner of the CLOSED complex | greyed (opacity 0.38) AND SPEAKING: `…— select a form first` |
| explore inside | `walk the habitat; the shell stays behind you — select a room with an inside — a built 3-manifold` | same |

**§9b-3's answer: identify was NOT the silent grey — it was LIVE.** The silent state the exposed code gap permits (`selected && targetFor === null` → disabled, reason null) **did not occur on any form this drive produced** — every selected form resolved a target. The gap stands in the source exactly as you traced it; I could not manufacture its firing case from the doors I have (⚠ on "cannot fire", measured only as "did not fire here").

**The reroute's far end + the cut road (your live candidate — CONFIRMED):** cut face `AB·BC·AC` → **`Cut — born · Cut (remove the 2-cell)`**, the record extends (`…— loaded —cut→ Cut — born`), and the gates MOVE truthfully: sew now reads **`The form has 1 boundary circle — sewing needs two`** (0 → 1 — one more cut reaches sewability), the word-gate re-counts (`7 faces`), identify stays live. **Cutting is what opens the boundary the rim-addressed gestures need — measured.**

**A §9c-outranking finding, named as the amendment invites:** `identify`'s gesture ACCEPTS the closed complex at entry (the trace mode opened on the uncut octahedron, corners tappable). Whether tracing two walks and sewing the seam COMMITS on a closed form — and whether that road can reach the antipodal quotient — was not driven; entry-live is the measured fact, completion is ⚠ unmeasured. The meaning half stays routed to the researcher.

**And the §7 backstop fired in the wild, mid-drive:** my own navigation off a dirty page was blocked by the `Leave site?` dialog — the guard's first live catch was its own builder. (Post-Task-B note, one line: bands built over the world's fresh-boot loop now read id TAILS in term positions — `—l2 ← v8·v8` — because the loop's corner labels mint absent and its primal 12-gon is out of the carrier's reach; the ruled honest-id arm, pre-existing, named for the record.)

---

## TO THE MOTHERSHIP

**1 · §6's separability question answered by the cut itself — no meaning question came back.** The suffix producer and the row register separated cleanly: the ROW dedup (the append on a row's own label) died alone; `rootLabelOf`'s handles feed ONLY term positions, so nothing the ruling held had to move. The full consumer census is in §1.2.

**2 · One §7 look finding for the designer with the copy flags:** the mark's two lines + the page-door buttons overflow the shelf's 140px floor at short viewports — the person must scroll to reach `save the page…` at exactly the moment the mark tells him to. The mark's placement (above the buttons) and the shelf's height bound are hers to settle; the wording is flagged as ruled-facts-not-final-copy throughout.

**3 · A commit-hygiene disclosure:** R7's notice de-double rode inside `e54f451` (R2's commit — same file, staged together). Disclosed here and in both messages; nothing else shared a commit. And the mandate record was committed late (`fc3057c`, after §5's union) rather than first — the record is intact, the order was mine.

---

## 1 · WHAT I SAW — every item at the eye, on the running app

### 1.1 · R1 — `leave bounded` CONSUMES, both directions

Reversing pair A·D·C·B ↔ E·F·G·H picked on the T³'s cube, **`leave bounded` pressed**: the chamber reads **`face-pairs 1 (reversing)` · `orientable no` · H₁ ℤ · v4 e8 f5 c1** — the pair consumed, its own mode named, the rest standing as walls. Same gate, **no pair picked, `leave bounded` pressed**: **`face-pairs 0`** · orientable yes · all six walls — byte-identical to the old chamber, with the quantifier now honest. **The targeted sweep found the assumption at exactly TWO sites** — the handler's hardcoded `[]` and the restore's unconditional `buildFormDomain(seed, [])` for door `'bounded'` — both cut (the ledger now records the REAL rows and the restore replays them through the same verdict door; a pre-R1 record restores its all-walls chamber as recorded). Three look-alikes inspected and clean: the live preview (handles partial pairing by design, its own comment says so), the bodiless card's `rows: []` (a different type), the duplicate-pick refusal (totality of a different kind).

### 1.2 · §6 — the suffix off the rows; the census, all N

An invoked Triangle reads **`•unnamed — born` / `•unnamed — born` / `•unnamed — born`** — no suffix anywhere on a row. **The figure carries the identity, measured through the shipped channel:** touching the third row emphasized `[w1:v2 | edge:jg96z0 | edge:gcf9a7]` — the corner drew as a bold dot, its two edges heavier, its rows bold, in one touch. No new gesture was built.

**THE CONSUMER CENSUS of the suffixed root label (the mandated enumeration):**
1. **The concept-row dedup** (the append on the row's OWN label) — **DEAD**. This was the only consumer that put the index in a name's slot. General: a real-name collision now reads `AB`, `AB` too.
2. **`rootDisplayOf` → the composed class label's PARTS** (`unnamed·A·unnamed·B·…` inside one identified row's label) — TERM position, **stands** (stripping it would read four distinct roots as one root repeated).
3. **`rootLabels` → the ring's `p ← {…}` set line** (`ManuscriptView` ~4944) — the HELD arithmetic line, **untouched exactly as ordered**.
4. **`rootLabels` → the witness's term reads** — terms, stand.
5. **`endpointNameOf` → relation source readings, absorbed lists, word-row slot names, composed dotJoins** (`—a ← unnamed·C·unnamed·A` measured on the triangle) — terms inside composed sentences, **stand** (the same counted-twice hazard as the ring line).
The amended comments now draw the distinction the code draws: the handle survives ONLY into term positions, held for the designer's term-spec.

### 1.3 · R2 — the fourth reader reads

The ambo 2-edge-region × I band's **port-face menu lists `AB₀·BC₀·BC₁·AB₁ · 4 corners` and `AB₀·AD₀·AD₁·AB₁ · 4 corners`** — the same strings the card's `◻` face register prints beside it, through the ONE labeler (`faceLabel` → `faceDisplayName` + the view's resolver). Where it read `unnamed · 4 corners` ×N last build. No fifth path exists.

### 1.4 · R7 — the refusal names what he did

The ambo thicken on the solid octahedron core reads, verbatim at the control: **"thicken: this form carries a 3-cell — a solid × a segment is a 4-manifold; this engine stops at 3."** The manuscript's notice no longer doubles the door word (the prefix is added only when the message lacks it).

### 1.5 · §7 — the standing mark's full cycle

A pristine page carries **no mark** (the ordinary case, unmarked). The moment the first built record entered, the sources panel read **"there is work here that is not written down / switching modules keeps it · a full reload loses it — save the page… writes it down."** `save the page…` pressed → the file downloaded and **the mark quieted** (`[data-unsaved-mark]` absent, measured). The signature is derived (record-layer ids + counts; zoo members excluded by the serializer's own law, so the restore's re-summon never false-marks), never a flag. The `beforeunload` guard rides as the ruled backstop only.

### 1.6 · §8 — the target widened, the ink and the ontology untouched

A double-click **~8px OFF the loop's hairline stroke selected it** — the exact class of click that missed five times last build. A double-click in the loop's **hollow fell through to paper and dismissed** — the interior is not pickable, as ruled. Structurally: each dim-1 form now carries its ink stroke plus an invisible 24px twin over the same segments (measured in the scene: two `LineSegments2` per form), withheld while selected so the correspondence layer keeps the corners.

### 1.7 · §5 — the note register and the honest quantifier

The reversing-pair chamber's card: **Euler χ `0` — the bare number** (no wrap, no interleave), and in the note register, **both notes at once, each naming its subject**: `w₁ ≠ 0 — non-orientable (the twist)` and `χ — the closed-world check does not apply to a room`. The zero-pair chamber reads `face-pairs 0`; the reversing chamber `1 (reversing)`; the T³ keeps `3 (all preserving)` (its witness pin held without a recut, by construction).

## 2 · WHAT I RAN (the five witnesses)

```
1  working tree clean at the tip; the frozen commit (b28218a) = specimenModel +
   the manifest re-seal alone, nothing else in it
2  freeze manifest: specimenModel.ts → FROZEN, SANCTIONED by this mandate (§5);
   classBodyModel and worldModel FROZEN and UNTOUCHED (the notes field is
   optional exactly so classBodyModel's objects stand); thicken.ts / pageStore /
   ManuscriptView / ManuscriptChrome / argumentReadingModel / InkedSkeleton →
   NOT_FROZEN (verified rows)
3  positive control REPRODUCED at the real base before the edit:
   05636d6bbbe891d95b370e07ee7a26d75a8aae912379270a3009648189e3f1e3; re-seal
   f4368d9f461af99b58c132157093127b06ae457264eed0604a8e7c23e4015f71 in the
   SAME commit
4  suite at the FINAL head: 112 files @ exactly 1 fail = diagnose-dual-inspection
   (accepted baseline). npx tsc -b → exit 0 after every commit (one transient
   TS2304 caught and fixed pre-commit). Witness recuts: diagnose-argument-card
   (rows plain 'unnamed' ×4; the dead suffix's signature asserted absent;
   term labels untouched) and diagnose-thicken (the new refusal verbatim) —
   both ALL PASS; diagnose-manuscript-specimen's `3 (all preserving)` pin
   held with NO recut
5  THE READING — §1 above: every item driven at the eye on the Windows Chrome,
   including both R1 directions, the touch-lights-the-corner channel, the
   sloppy-click and the hollow, and the mark's appear→save→quiet cycle
```

## 3 · WHAT I CHANGED

- **`src/manuscript/ManuscriptView.tsx` + `src/manuscript/pageStore.ts`** (`ac57b77`): R1 — leave-bounded consumes touched rows through the verdict door (folded wall included), records and replays the real rows.
- **`src/manuscript/argumentReadingModel.ts` + `scripts/diagnose-argument-card.cjs`** (`2853cb6`): §6 — the row dedup dead, term handles held, comments drawing the real distinction, witness re-pinned.
- **`src/manuscript/ManuscriptView.tsx`** (`e54f451`): R2 — `faceLabel` threads the resolver at all four call sites (+ R7's notice de-double, disclosed above).
- **`src/lib/thicken.ts` + `scripts/diagnose-thicken.cjs`** (`991955a`): R7 — the refusal names the true operands; comment and witness moved with it.
- **`src/manuscript/pageStore.ts` + `ManuscriptView.tsx` + `ManuscriptChrome.tsx`** (`631d193`): §7 — `pageSignatureOf` + `savedSignature`/`markPageSaved`, the mark in the sources panel, the backstop guard.
- **`src/manuscript/InkedSkeleton.tsx` + `ManuscriptView.tsx`** (`0685fc8`): §8 — the invisible 24px pick stroke at both skeleton sites, withheld while selected.
- **`src/manuscript/specimenModel.ts` + `docs/governance/ENGINE_FREEZE_MANIFEST.txt`** (`b28218a`, frozen union): §5 — her χ-note words verbatim into `notes?`, the value bare, the quantifier honest.
- **`src/manuscript/ManuscriptView.tsx`** (`3ac287f`): the note register render (twist + notes, one register).
- **`.handoff/THE_BUILD.md`** (`fc3057c`): the mandate record.

## 4 · WHAT I COULD NOT REACH / DID NOT TOUCH

- **§9 honored:** the `◻` glyph, `· 4 corners`, and the disambiguator's form untouched — nothing was owed. **`unnamed` stays plain and undecorated** — the invitation clause is withdrawn and nothing was written for it.
- **The held term line** — `p ← {…}` and every other term position stand byte-exact, awaiting her term-spec.
- **A folded-verdict leave-bounded case** was not driven at the eye (a reversing pair on the single-cell cube glues fine; the folded wall path through leave-bounded shares the glue exit's code verbatim and its witnesses) — named, not hidden.
- **The §7 signature's grain:** it reads ids and counts; an exotic in-place mutation that changes no id would not re-arm the mark. None exists on today's doors — named so it is on record.
- **The §7 shelf overflow at short viewports** — reported to the designer (TO THE MOTHERSHIP 2), not re-laid; the layout is hers.
