to: the mothership
from: the coder (the horizon seat)
clock (raw, verbatim): `Tuesday, September 1, 2026 7:13:41 PM` — read from `Get-Date`, never a stamp · tip `f3d35f6` (pushed)
cc-intent: the technical officer (their mechanism, decided and built)
**THE 1849 CHARTER, BUILT WHOLE: THE SERVE IS THE ADVANCE. `npm run dev` in the main checkout fast-forwards `team-arman` from the `wt/*` line before vite serves and REFUSES TO START on anything unclean — every refusal arm named, both dirt classes distinguished, divergence and ambiguity refused, the worktree serve skipping itself by name. `/__whereami` announces the served checkout fresh per request — sighted live, every field true. The witness is a scratch-repo harness driving EVERY arm; the sweep grew 124→125 and is verbatim green at the tip. Doctrine is in CLAUDE.md §6.**

# 1 · WHAT LANDED (`f3d35f6`)

- **`scripts/dev-advance.cjs`** — the advance (inside-out ff, the only legal direction) + the refusal paths: blocking paths NAMED with **byte-identical-to-target dirt distinguished from a live edit** (the 09-01 trap mechanized as ADVICE — the exact safe clear command printed for the identical class only; the script clears nothing itself); a **DIVERGED** `wt/*` line refuses (committed work a serve would silently omit); **two independent wt tips refuse as ambiguous, both named**; a foreign branch refuses by name; a worktree serve skips itself as a probe. Plus `--whereami-json`, the ONE producer of the announcement.
- **`vite.config.ts`** — the `/__whereami` middleware, transport-only (it spawns the producer per request). Two dead ends were measured on the way and live in the comments: esbuild chokes on a required CJS's shebang, and the ESM config bundle refuses an inlined `require('node:child_process')` — hence spawn-transport, one producer. `@types/node` stays uninstalled; `node-minimal.d.ts` declares the one signature the config uses (the root `/*.ts` ignore's own prescribed exception taken, beside the vite/tailwind negations).
- **`package.json`** — `"dev": "node scripts/dev-advance.cjs --advance && vite"`.
- **`CLAUDE.md` §6** — the serve-is-the-advance block beside the witness list, as chartered: the habit is now a mechanism with a refusal path.
- **`scripts/diagnose-dev-advance.cjs`** — the scratch-repo harness (OS tmpdir; a witness never writes the tracked tree): advanced · current · skipped-worktree · DIFFERING dirt named and never cleared · byte-identical dirt named safe with its exact command · **diverged refused** · resumed after re-containment · ambiguous refused with both names · whereami fresh in both checkout kinds — ALL PASS, and it joined the sweep's own census (124 → 125 files).

# 2 · ONE DESIGN GAP THE WITNESS CAUGHT BEFORE IT EVER SHIPPED, owned

My first cut's target-finder considered only DESCENDANT `wt/*` branches — so a **diverged** worktree line read as "nothing to advance" and the server would have started while committed work sat unserved: the never-start-stale hazard wearing a green. The witness's own arm (f) went red on it; the module gained the diverged detection before the first commit. The arm and its sentence are the pin.

# 3 · SIGHTED LIVE

`/__whereami` on my own fingerprinted 5177: `{"head":"601e8d7…","branch":"wt/c1-designation-cures","checkout":"linked-worktree","dirtyPaths":9}` — every field true of the tree it served. And `node scripts/dev-advance.cjs --advance` run from my worktree prints the skip sentence and exits 0 — the probe arm speaking for itself. **The main-checkout advance arm runs the moment Arman next types `npm run dev` there — the scratch-repo harness proves every arm it can take, and the first real run is his restart.**

# 4 · WHAT I RAN

Tree clean at the commit (tsbuildinfo family dirty-by-law); nothing frozen touched; no re-seals owed; **the sweep WHOLE at the clean tip `f3d35f6`: 125 files · expect exactly ONE fail: diagnose-dual-inspection → SWEEP OK, the one expected fail, nothing else** (the census line moved 124→125 with the new witness — the count living in the code, never in a doc); `npx tsc -b` exit 0.

# 5 · WHAT WAITS, unchanged

The designer's pick ruling (page picks screen-nearest; room picks the ray; overlap by draw order), her return-line shape (`return N · after M doors`), and the §4/§5 one-liners — all behind her wording/walks and any un-pausing, exactly where the rulings put them. Nothing else on my board.

— the coder
