to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-23 18:09 +0330` (machine — `date`) · the chain `6fe02bb → a44a44f → 934a938 → the record commit`, this report the record commit's own letter
re: **THE LOCK removed first (0 bytes, mtime 16:21:49, no git process, `git status` clean after — disclosed). THE ONE MEASUREMENT answered by the researcher's test on the build's own positions and PINNED: a shared face is ONE vertex order written twice, that order is the HOST cell's walk on 14 of 14 shared faces (gen 1 and gen 2), the residue's walk is held by no record — it is the reverse, derivable from the record with no positions. `STAMP C-7h` LANDED (`a44a44f`) — the designer's third cut, items 0–12: `≡` the person's act and only that (the names derived from seed SEGMENTS in the resolver, composition printed once, the corners in brackets), the composed identity stated once per site with the least mark (a hollow ring, the solid's grey, no words), the down-words at the up-words' distance and the rows opening so no foot block crosses a midpoint (32 of 32 owned at the eye), `nothing returned` once, ONE refusal grammar (`refused`, `(the act just made)`, `nothing glued` retired), the stone's sentence, the dependency box in two sentences, the card at a born vertex saying its space, no face named by its id; the tripwire pinned. `STAMP C-9` LANDED (`934a938`) — THE BORN FACE: the engine in a new NOT_FROZEN file reproducing the researcher's deterministic seals to the digit (a1 · a2 · a3 0 of 42; 9 of 42; 234 · 91 · 6; 6 of 42 with T 7 at the reference's grain; 683 = 354 · 210 · 119; 354 of 354; 42 of 42 alike), the surface at the site — the solid quiet, the extension marked and attributed (`+ r3 returns as Φ3 — through your pair r3 ↦ Φ3 at ABAC` at the eye), Und's two hands once per edge, an interior face read as its host's walk with its cells found by vertex set. U1 CARRIED at this record commit: 21 cited rulings and 33 cited instruments tracked with their parents un-ignored; one cited ruling too long for this tree, reported. The eye leg 65 → 77 → 81, three landed runs. Sweeps `139 … SWEEP OK` and `140 … SWEEP OK`.** ECHO §124 · §125 · the lock · the measurement · `STAMP C-7h` · `STAMP C-9` · the U1 rider.

## TO THE MOTHERSHIP — fourteen lines up

1. **The lock.** `.git/index.lock`, 0 bytes, mtime `2026-09-23 16:21:49`, no `git` process on this machine (tasklist read before the act); removed at 16:48 by `rm`; `git status` ran clean after, and every commit since (three) went through. The researcher's diagnosis stood; nothing else in `.git` was touched.
2. **THE ONE MEASUREMENT (your 1535 §3 with the researcher's test, 1642 §1) — the answer, pinned in the born-room witness on the build's OWN positions and records.** Where does the build hold each cell's induced walk of a face it shares? **Nowhere as a per-cell field:** `Cell` (src/types/geometry.ts:290–312) carries no orientation, and a shared face is TWO `Face` records with ONE cyclic order — the core's ring face (src/lib/ambo.ts:556, `midpointRingForVertex`) and the residue's base (src/lib/ambo.ts:709, the SAME `midpointRingForVertex`), the parent-cell copies verbatim (src/lib/ambo.ts:508). By the test — `n = (v₂ − v₁) × (v₃ − v₁)`, the order is the cell's iff `n · (face centroid − cell centroid) > 0` — **that one order is the HOST cell's own walk on every shared face: 4 of 4 at gen 1 (the core octahedron's), 10 of 10 at gen 2 (6 the core cuboctahedron's against the residue square pyramids, 4 the parent octahedron's against the gen-1 residue tetrahedra); the residue's walk is held by NO record** — it is the reverse, derivable from the record itself at zero cost (reverse the order), or from positions by the test (two centroids and a cross product per cell-face pair; every vertex has a position). **The researcher's control reproduced on THEIR regular tetrahedron (`AC·AB·AD` the corner cell's +1, the core's −1) — and their inference inverts on the build's positions:** the build's seed tetrahedron is `A (1,1,1) · B (−1,−1,1) · C (−1,1,−1) · D (1,−1,−1)`, the other handedness, so here the same literal `AC·AB·AD` is the CORE's walk and the record holds the host's, not the corner cell's. The construction says why: `midpointRingForVertex` walks the seed's ordered neighbour ring (`orderedNeighborsByVertex`, induced by the seed's own outward faces), whose normal points toward the seed corner — out of the core, into the residue. The witness pins 14 of 14 and the control's ±1, so a change of construction turns it red.
3. **C-7h item 1 — the mechanism, and one file outside the boundary list, touched additively.** `≡` is decided by the RECORD (`Composed` against `born`), so the names are derived in the RESOLVER: every role and word carries its NAME as seed SEGMENTS (`Resolved.roleSegs` · `wordSegs` — one per seed the class holds, in the order the person's acts joined them); a pair the solid composed MERGES its two chains on the shared seed (printed once — `Φ1 ≡ F1 ≡ r0`, never `Φ1 ≡ F1 ≡ F1 ≡ r0`; `r2 ≡ F1` on the leg's path where the designer read `r0 ≡ F1 ≡ F1`), a pair the person made joins them. The glue takes the names through an ADDITIVE hook — `gluedSpace(A, B, M, naming?)`, src/lib/midpointGlue.ts (NOT_FROZEN row 217), its own default untouched — and the resolver keeps the amalgam it named (`ResolvedEdge.glued`), which the surface now draws instead of re-gluing: one derivation, never two readers agreeing. midpointGlue.ts was not in your boundary list; the alternative — re-deriving segments in the surface — would have put a second reader of the record beside the resolver. Said. **The corners in brackets:** her rule (2) wears every segment's corner when a spelling repeats WITHIN the chain (`sustains [C] ≡ sustains [A] ≡ descends-from [B]` — at the eye); a LONE name wears its corner only against another lone name spelled alike under another seed (`disjoins [A]` — the glue's old alike rule, the CORNER now where the glue wrote the SIDE; at gen 2 `[A]` had meant "side A = AC"). A chain segment against a lone word spelled alike is NOT bracketed (the old behaviour kept; nothing moves at gen 1 — the midpoint witness's 58 unchanged). A doubled seed (the lawful gen-3 doubling — two classes holding it) is told apart by the parent it came through (`(through P_A)`); the mold's own words stay plain. In the unfolded columns a tuple in both parents BY COMPOSITION (every term composed, its word composed or the mold's) carries no glyph and no amber — the solid's grey; at ABAC 68 such tuples, 0 glyphs, 0 of the person's cross-fold marks before the born pair.
4. **C-7h item 2 — the least mark, a DEFAULT for her eye.** The sentence once per site — `corner A's 14 roles and 11 words stand on both sides as one — composed, not yours (their points hollow) · the born room: …` — and on every composed point, in the unfolding and in the own column, ONE glyph: a HOLLOW ring and the solid's grey, no words; composed tuples in the own column the grey and no origin word; the own column's caption no longer says `composed` (it read `· composed (in both because the solid made it so)` — a second `composed` on the surface, found by the witness's count, cut). The word `composed` once per site: measured 1 under node and at the eye.
5. **C-7h items 3–4 — the geometry, measured at the eye.** The down block FIRST, its first line 11.5 px below the row line (mirror of the up block's last at 3.5 above), the loops' block beneath it led by its rings; the rows OPEN so no block crosses the midpoint — a row's extent on a side is at least twice the depth of its last line there, less the neighbour's own half-row, on the half-row grid (a row with one line a side keeps the base pitch; the manufactured 8-word point's pitch 75 → 120). At AB at the eye: 32 blocks, the nearest row its own 32 of 32, the centre distance to the own row on one-line blocks up 8 px · down 7 px · loops 18.3 px, the tightest margin 14 px (F6, up). Her Φ1 block now sits inside its own half.
6. **C-7h items 6–9 — the one grammar, with two defaults of mine.** Every refusal box: the head `not taken`; a form refusal `not taken — <the store's clause>`; a REGISTER refusal `not taken — F1 ↦ Φ9: the two records contradict under it` (the tail mine — the conflicts follow as `in conflict: …`); the local hand `here, on A–B: withdraw this attempt`; a prior act's hand `here, on A–B: withdraw F5 ↦ Φ7`; distant hands `at ABAC (one generation up): withdraw … first`; the refused line's fold label `· not taken — see below the drawing` (the first leg run found `· refused —` still there; cured). The stone `Φ2 ↦ r1 would make c1 and c3 one: two roles of corner C, which the corner keeps apart`. The dependency box in two sentences — `not taken — F1 ↦ Φ3 would make Φ3 one with F1.` / `your pair at ABAC, one generation up, needs Φ3 as its own role: r3 ↦ Φ3.` — `BrokenBornAct.why` is now the collision CLAUSE (`needs … as its own role` · `would then make … one: …` · `would then contradict itself: …`); item 9's tautology reads `needs c2 as its own role — under this act it would be the solid's, composed` (two names: `needs Φ3 apart from F1 — under this act the solid would make them one, composed`) — wording mine, her eye.
7. **C-7h items 10–11.** The card at a born vertex: ONE row `Space · derived from A and B — 20 roles · 22 words · 52 tuples` (the resolver, every read), never a Cast row — the cast rows mount under a SEED vertex's cast alone; an old loaded cast on a born vertex: one line `a loaded cast — not read: a midpoint's space is derived from its parents`, no rows (ruled from the description; unreachable lawfully, not sighted). Faces: `getFaceDisplayLabel` in Panels.tsx, SelectedVertexRelations.tsx and VertexPacketEditor.tsx composes from the corners by D14 through `faceDisplayName` (the one composer's wrapper; the name slot's lawful absence word where it yields nothing — `unnamed`, the register's own); a face the shape no longer holds is SAID (`a face this shape no longer holds`), never addressed; `formatFaceSummary`'s `; face:…` dropped. **Measured, the unchartered row:** `getCellFaceRows` is at Panels.tsx:3613 (your 3427 is an older line); it feeds the Cell Faces list of the cell inspector — its name line already D14-composed, its id on the demoted mono ADDRESS sub-line titled `reference — the face's address` (STAMP C-1 item 4's grading). That sub-line stays: it is a reference position, not a name — 4 such sub-lines on the page at AB, printed by the leg; `face face:…` 0 at AB and at C.
8. **C-7h item 12 — the tripwire, and a third kind the pin allows.** Over 300 towers, 3,300 born vertices at gen 1–4: `corner:carried` 600 · `medial:anchored` 1,800 · `by: content` 0 — and `seed:none` 900, the gen-1 midpoints on SEED edges (a seed edge composes nothing; the record is the J). The pin allows exactly those three; a fourth or a `content` turns it red. `stoneOn`'s docstring now says the shared seed is the null case only.
9. **C-9 — the engine's home, and the guard's grain.** The born face lives in a NEW file, src/lib/bornFace.ts (NOT_FROZEN row 220, classified in the landing commit): it imports the resolver AND the gen-0 face, which import each other's neighbour (`spaceOf` imports `edgeBetween`) — inside either it would close a cycle. It reproduces the researcher's deterministic seals to the digit (line 3 of the re:). **One grain differs and is said:** the reference counts a corner-local refusal at "the tuple met later" — 7 at T; the engine names EVERY contradicted pair within a collapsed class (the researcher's own b3 lesson: naming the first-met tuple shifts as born acts arrive though nothing un-contradicts) — 11 pairs on the same 6 triples, every one inherited, each seed pair once though T's tuples live at both M_AB and M_BC (the signature by seed tags, the word by its tag — a glued space names one word `sustains [B]` here and `sustains` there). The witness ports the reference's grain as the second implementation and pins 7; the engine's 11 is printed. The random-draw seals hold by shape on my generator (monotone 0 failures; every added route through a born pair, 3,713 routes; refusals only grow, 748 draws add one, all attributed; the interior's two walks differ in 110 of 1,680) — the reference's 315 and 92 are its RNG's, quoted.
10. **C-9 — the surface, four defaults for the designer.** (i) The solid part reads `the solid's ground: N returned to itself · M elsewhere · K did not return` once per corner in the solid's grey; (ii) the news `+ r3 returns as Φ3 — through your pair r3 ↦ Φ3 at ABAC` in amber, `nothing added by a pair of yours yet` before one; (iii) Und's two hands ONCE per edge of the face, not per corner — `here, on AC–AB: a pair of yours · or at AC, on A–C: an act on the edge it descends from` — the first cut printed them per corner (nine lines on one block, three of them identical) and the witness's first run showed it; (iv) an interior face's head `the face AB·AD·AC, between the parent octahedron and the residue tetrahedron at A — walked as the parent octahedron's, AB → AD → AC → AB` with the line `walked as the residue tetrahedron at A's, the reverse, it reads alike — one reading, both cells'`, or two blocks named by their cells when they differ. **The cells are found by VERTEX SET** — each cell holds its OWN record of a shared face (line 2), so a face id names one cell's copy; the first cut used the id and read one cell. The corner cell's face carries no block. **Not built:** an inherited refusal's hands — it says `inherited from the seed face — the solid's own, its hands at the seed edges' midpoints` in words; the buttons stand where the acts are, at gen 0.
11. **C-9 at the eye — and one placement for the designer.** The leg's born pair `r3 ↦ Φ3` at ABAC closed a route: the medial face's block read `+ r3 returns as Φ3 — through your pair r3 ↦ Φ3 at ABAC` at both viewports — the extension marked and attributed at the eye, not only under node. The medial block sits inside the panel's visible box at scroll 0; the interior face's block is below it (the leg prints `inPanel: false` for it) — the sources' order is the host's face list. Hers to place.
12. **U1 — carried at this record commit, by the rule as you ruled it, with its sweep and its numbers.** A citation matched as the TAIL of a filename — its basename, its bare stem (≥ 12 chars), or a truncated head (≥ 12 chars ending at `_`/`-`) followed by an ellipsis, since the ADRs cite `RULING_THE_FORK_…`; a generic name (`README.md`) only with its directory. Over the tracked tree: **21 cited RULING files (240,630 bytes) and 33 cited instruments (288,557 bytes; 7 of them RESULTS files, dated events tracked BECAUSE cited — by ADR 0030, ADR 0031, the claims ledger, the face witness), the instruments' import closure adding none** (every import is already cited). The researcher counted 13 rulings and 19 instruments: the difference is the matching — stems cited without `.md` (`RESEARCHER_RULING_ROUTE_B_LINEAGE_CARRIAGE`), truncated heads, and the RESULTS files. The seven stems: six exist under `.handoff/RESEARCHER_RULING_…` and are cited (now tracked); `SEAL_REMOTE_EXPOSURE` is tracked in docs/governance. **One cited ruling is TOO LONG for this tree and is left ignored, reported:** `.handoff/RULING_THE-INSIDE-MIDPOINT-TRACE-REFUSAL_on-the-honesty-line_…md` — 235 characters, 266 with the repo root; git refused the add (`Filename too long`; `core.longpaths` unset on this machine, and a tracked path over 260 would break every Windows clone without it). It is the researcher's durable rulings file (§12–§15 appended this week). A rename or a `core.longpaths` ruling is yours and Arman's; the longest tracked path today is 99. `.gitignore` gains a block: each parent directory un-ignored and its children re-ignored before the file itself is negated (`.handoff/*` does not descend, as the file's own comment warns), 54 negations; the sweep script is mine and re-runs when a tracked file cites a new one.
13. **W1 — carried AS FOUND, hashes with their instruments, none read for content:** the claims ledger `blob c01b4079` = `sha256-LF eee74b05` → `blob c597962c` = `sha256-LF 5306a111` (59 lines added, 0 removed; the tail closes) · the front board `blob 7f68fa2b` = `sha256-LF 3eb9111d` → `blob 6bc1be5c` = `sha256-LF 986b1da0` (12/8; closes) · the connection-layer plan `blob fb2d8af6` = `sha256-LF e6f9169e` → `blob 938960a6` = `sha256-LF 6074eb1f` (3/1; closes) · ADR 0031 `blob 34e03e62` = `sha256-LF 6c884a6b` → `blob 5ef9a361` = `sha256-LF ea164e5d` (4/1; closes — your rider 4 exactly). The sovereign ledger, the designer-seat handoff and the horizon plan unchanged. **⚠ FOUND MODIFIED AND NOT ON YOUR LIST — LEFT UNCARRIED, SAID:** `docs/adr/0030-the-concept-connection-transport-of-meaning-through-walks.md`, 7 lines added at line 32 in one hunk (`blob b95d8f16` = `sha256-LF b5da0f48` → `blob 66cb9c3f` = `sha256-LF a6cea23f`, mtime 17:30:31 — after your 1642; the researcher's owed 0030 §2 rider, by its clock), its tail closing on a finished sentence; not read for content. Your W1 named four files; this is a fifth. It stays in the working tree; say if it rides the next commit and I carry it as found.
14. **Not sighted by a person:** the designer's eye on every C-7h surface (the hollow ring, the one grammar, the chains with corners, the Space row) and on the born face's block; Arman's on the born room after C-7h; the old-loaded-cast line (unreachable lawfully).

## 1 · What I SAW

- **The drive family**, `scripts/app-leg/diagnose-the-concept-layer-eye.cjs`, three landed runs this cycle, each whole: at C-7h `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS` (77 clauses — 65 → 77, six new arms per viewport; served `6fe02bb`, dirty — the C-7h tree; the leg ran twice on that stamp: the first found `· refused — see below the drawing` and the caption's `composed`, both cured); at C-9 `ALL PASS` (81 — two new arms per viewport; served `a44a44f`, dirty). At the eye: the one grammar at the act (`not taken — F1 ↦ Φ9: the two records contradict under it`, one hand `here, on A–B: withdraw this attempt`, the retired words nowhere); foot ownership 32 of 32 (line 5); `nothing returned` once at B and at C; at ABAC 0 glyphed words, 0 person's marks, 68 composed tuples in the grey, no label joined to itself, `r2 ≡ F1`, `sustains [C] ≡ sustains [A] ≡ descends-from [B]`, `composed` once, 28 hollow points with no origin words; the dependency in two sentences with its two hands, the far hand taken and the act then taken; the card at AB `derived from A and B — 20 roles · 22 words · 52 tuples`, no Cast row, `face face:…` 0 at AB and at C; the born faces at ABAC — `AB·BC·AC` one cell walked in the face's own direction, `AB·AD·AC` two cells walked as the parent octahedron's with its other walk alike, the ground once per corner, the hands once per edge, and after the born pair `+ r3 returns as Φ3 — through your pair r3 ↦ Φ3 at ABAC`. The AB drawing 938 px in the 1025 px panel at 1689 × 897, unchanged.
- **The induced walk**, printed by the born-room witness: gen 1 — `AC·AB·AD = AC·AB·AD held by core:octahedron + residue:tetrahedron: the core's walk` and the three others alike; gen 2 — 10 shared faces, one cyclic order 10, the host's walk 10, the residue's held 0; the researcher's control `AC·AB·AD the corner cell's 1, the core's −1`.
- **The born face under node** at ABAC on the lawful path (the born-face witness): before a born pair `nothing added by a pair of yours yet` on every corner; the first free pair tried, `Φ3 ↦ r3`, closed a route — `+ r6 returns as r3 — through your pair Φ3 ↦ r3 at ABAC`.

## 2 · What I RAN

- **`npx tsc -b`**: exit 0 at the C-7h tree and at the C-9 tree.
- **The witnesses at the C-9 tree:** `DIAGNOSE-THE-BORN-FACE: ALL PASS` (13, NEW) · `DIAGNOSE-THE-BORN-ROOM: ALL PASS` (34 — at C-8c 31; the naming clause, the tripwire, the induced walk added; the sentence, the marks, the grammar and the mount re-pinned) · `DIAGNOSE-THE-MIDPOINT: ALL PASS` (58; the grammar; seven store reaches) · `DIAGNOSE-THE-INSIDE: ALL PASS` (33; ownership by construction over Flow, T, Φ and the wide point — 0 lines crossing, the tightest margin 7 px; the loops' blocks beneath the down blocks) · `DIAGNOSE-THE-FACE: ALL PASS` (24; `nothing returned`) · `DIAGNOSE-THE-CAST-LOADER: ALL PASS` (47; the card's two mounts) · `DIAGNOSE-THE-CONCEPT-TYPE: ALL PASS` (11) · `DIAGNOSE-THE-AMBO-GESTURE-TRUTH: ALL PASS` (17) · `diagnose-ambo` exit 0 · `diagnose-workspace-persistence` exit 0 · `DIAGNOSE-THE-J-REGISTER: ALL PASS` (61) · `diagnose-engine-freeze` exit 0 (19; the new lib file intent-staged and classified, row 220).
- **The born-face witness's own lines:** `the 42 triples: {"medial read":36,"interior read":42,"corner-cell read":42,"a1 ok":42,"a2 ok":42,"a3 ok":42,"b-only moved":9,"glued":234,"dom med":91,"|M_AB|":774,"fix med":6,"und":683,"und@M_AB→M_BC":354,"d3 free":354,"d3 breaks":354,"und@M_BC→M_CA":210,"und@M_CA→M_AB":119,"ref flow":0,"ref T":7,"ref phi":0,"interior alike":42,"a3more triples":9,"medial refused":6,"medial refused triples":6,"refusals@AB":7,"inherited":11,"refusals@BC":4,"ref refusing triples":6}` · `this witness's draws: {"draws":1680,"born pairs drawn":5053,"news":3713,"interior news":191,"e1 differ":110,"draws adding a refusal":748}`.
- **The sweep at `a44a44f` (C-7h), whole, read by eye in its own call:** `139 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (exit 0; 138 PASS and the one accepted fail; the born room 47.9 s, the concept-type 8.8 s, the freeze 11.4 s, the loader 2.7 s, the midpoint 2.1 s, the face 1.5 s, the inside 1.5 s; the j-register PASS at 66.9 s inside its declared ceiling; wall 89.8 s).
- **The sweep at `934a938` (C-9), whole, read by eye in its own call:** `140 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (exit 0; 139 PASS and the one accepted fail — the census 140, the born-face witness in the sweep by its own declaration at 11.7 s; the born room 38.7 s, the concept-type 5.7 s, the freeze 7.8 s, the loader 0.9 s, the midpoint 1.7 s, the face 1.7 s, the inside 1.4 s; the j-register PASS at 50.6 s inside its declared ceiling; wall 65.3 s).
- **The U1 sweep** (`u1-rider.py`, mine): 146 candidates (ruling files under `.handoff`, instruments under `.handoff/instruments`, outside mail and archives) · 21 rulings and 33 instruments cited · 1 too long, excluded and reported · `git add` of the 54 exit 0 after the negations.
- **The gate:** `node scripts/hooks/check-coder-inbox.cjs --mark-reviewed --expect 3` in its own call before each stamp and before the record commit (the three letters read whole); `--expect 0` after archiving.
- **Git:** the sim-diff empty at each stamp (`git diff --stat HEAD -- src scripts docs/governance` printed nothing); pushed fast-forward after each landing (`6fe02bb..a44a44f`, `a44a44f..934a938`); no force, no rewrite, no clean; the lock removed by hand, disclosed above.

## 3 · What I CHANGED

**C-7h (`a44a44f`):** `src/lib/spaceOf.ts` (NOT_FROZEN row 219) — `NameSeg`, `Resolved.roleSegs` · `wordSegs`, `namingOf` (the segment rule), `ResolvedEdge.glued`, `stoneClause` · `stoneWords(shape, st, pair)`, the dependency's `why` as clauses, the docstring. `src/lib/midpointGlue.ts` (row 217) — the additive `GluedNaming` hook. `src/store/geometryStore.ts` — the stone's sentence with its pair. `src/components/CastInsideDiagram.tsx` — `solid` marks (the hollow ring, the grey), the down block first, the rows opening. `src/components/MidpointSurface.tsx` — the tuple glyph rule, the composed mark and sentence, the own column from the resolver, `nothing returned`, the one grammar, the two-sentence dependency box, the fold label, the caption. `src/components/Panels.tsx` (CRLF) — `SpaceCardRow`, the cast rows under a seed alone, `getFaceDisplayLabel` and `formatFaceSummary` by D14. `src/components/SelectedVertexRelations.tsx` · `VertexPacketEditor.tsx` — `getFaceDisplayLabel` by D14, the missing face said. The witnesses: the midpoint, the loader, the born room, the inside, the face; the leg and its driver (six arms).

**C-9 (`934a938`):** `src/lib/bornFace.ts` (NEW, row 220) — the engine. `src/lib/spaceOf.ts` — `recordOn` exported. `src/components/MidpointSurface.tsx` — `BornFaceRecord` · `BornFaceBlock`, `ProjectionSource.faceId`. `docs/governance/ENGINE_FREEZE_MANIFEST.txt` — row 220. `scripts/diagnose-the-born-face.cjs` (NEW). The born-room and midpoint witnesses (two pins); the leg and its driver (two arms).

**The record commit:** `.gitignore` (the U1 block), the 54 cited files tracked, `THE_BUILD.md` (the three letters verbatim), `THE_BUILD_REPORT.md` (this letter), the four riders as found.

**Not changed:** nothing frozen (ids.ts, geometry.ts, cornerCycleName.ts untouched); `core.longpaths` untouched.

## 4 · What I could not reach

- **The too-long ruling** (line 12): cited, left ignored, reported — a name or a config ruling, not mine.
- **An inherited refusal's hands at a born face** (line 10): said in words, not offered as buttons there.
- **A person's eye** on any of it (line 14).
