to: the mothership (sixth)
from: the coder (the order seat)
clock: `2026-09-24 13:31 +0330` (machine — `date`) · the chain `5c9ab76 → 8fad706 → the record commit`, this report the record commit's own letter
re: **`STAMP C-10` LANDED (`8fad706`) — THE LIFT CARRIES, item 0 measured first and proceeded from (race rule 1). THE FINDING: the lift's file ALREADY carries the source universe WHOLE as its one ancestor — GAP2C's chain walks from the lifted shape's parent pointer (the source) and stops at the first direct-readable link, which the source itself is (gen 0 · 1 · 2 · 3 measured) — so the seed casts, the person's J records and the lineage ride verbatim, and the committed loader keys the record under the form's own prefix, so a lifted vertex's id names the same vertex in the record. On the lifted shape ALONE the resolver returns nothing for every born vertex whose parents lie outside the region (3 of 4 on the corner cell · 6 of 6 on the core · 5 of 5 on the gen-2 residue · 12 of 12 on the gen-2 core — a FALSE absence); on the carried record every lifted vertex resolves the SAME space as in the Ambo (52 of 52 over eight regions) and every surviving loop reads byte-equal (51 of 51 — the falsifier). ⇒ THE CARRY EXISTS BY CONSTRUCTION: `snapshot.ts` untouched, `subComplexLift.ts` untouched, no frozen file; what was missing was the READER and the MARK — a react-free model (`liftedConceptModel.ts`, NOT_FROZEN) reading the record through the ONE resolver at every read, a form born on the page by an act reading NO record (no transport, none minted), a form that carried none saying so, a corner the record cannot resolve naming the seed with no cast; and the card section (`LiftedConceptSection.tsx`, NOT_FROZEN) reusing the Ambo's OWN inside panel (inline) and face blocks (hands as WORDS — the acts are the Ambo's at the sites named). Subdivision composes on the record exactly (15 = the solid's 14 + the born pair at gen 2; the person's 3 at gen 1). The no-cast control a true absence on every row. The grain law measured: the residue at A lifted at gen 2 carries 7 corners. The witness 31 ALL PASS; the leg 81 → 87 ALL PASS (four runs; the fourth whole); sweep RED at 8fad706 (the concept-type witness — the model read `data.cast`), cured at 3e3629e where `141 … SWEEP OK — the one expected fail, nothing else`; tsc 0. U1's second wave APPLIED at the record commit (§126.2 — 1 ruling 6,636 B + 6 instruments 49,528 B tracked, the .gitignore block regenerated); W1 carried AS FOUND with the sovereign ledger and ADR 0030 now among the riders.** ECHO §126 (Δ90 the race · `C-7h` and `C-9` ratified · U1 ruled · W1) · `STAMP C-10` (item 0 printed, then proceeded).

## TO THE MOTHERSHIP — thirteen lines up

1. **Item 0's answer changes what C-10 is.** The mothership's item 1 asked me to carry "whatever (0) finds missing"; (0) found NOTHING missing from the file. `liftSelectionToManuscript` hands `serializeSnapshot` the whole workspace population (src/store/geometryStore.ts:558), the GAP2C chain pushes the lifted shape's parent — the source — before testing it, and `directComplexOf` reads every Ambo shape on the path (gen 0 · 1 · 2 · 3), so the chain stops there: `file.ancestors = [the source universe]`, byte-equal to the shape the store holds (measured: `J(ancestors[0]) === J(source)` on eight regions, files of 35–179 KB), holding every lifted vertex by id, with no derived space anywhere in the bytes (no `roleContent`/`wordContent`/`roleSegs`/`glued` key). The loader (`deserializeSnapshot`) namespaces the form and the ancestor under the same `<source>:` prefix (GAP2C's own rule), so `spaceOf(record, liftedVertexId)` is well-formed by construction. The shelf keeps the ancestor (`loaded.ancestors`), the page store records it (`shelfAncestors`), the view joins it to the lookup — all pre-existing. **I added nothing to the lift or the snapshot layer; the carry is pinned as a rule in the witness (§1), and every later region the store can lift rides the same chain.**
2. **The reader reads the RECORD, never the lifted shape — the measured false absence is why.** On the lifted shape by itself the resolver gives NOTHING for a born vertex whose parents lie outside the region (39 of 39 such vertices across the eight regions), and a card that read the shape alone would have printed an absence that is false — the vertex HOLDS a space, the region just does not hold its parents. The model's rule: the record a form reads is the carried universe holding its vertices BY ID; a form born on the page by an act (`opId` set) reads NO record whatever ids its birth kept (your item 2 — a cell born after the lift has no transport, and I mint none even where an id survived a quotient); a form that carried no record (invoked on the page, or an old file) says so. **A design default of mine:** the form-born rule is by `opId` (every op-born `WrittenForm` carries one; a loaded lift carries `null`) — the mechanism the type already holds, never a lineage walk.
3. **The hands on a lifted face are WORDS, not acts.** `FaceRecord`/`BornFaceRecord` (now exported) take `here: Edge['id'] | null` and `hands: 'act' | 'words'`; on the card `here` is null (no site is local to the page — every hand reads `at ABAC, on AC–AB: a pair of yours · or at AC, on A–C: …`, the midpoints named from the record) and no withdraw button mounts (0 measured; the Ambo's own mounts unchanged, hands as acts by default). Reason: the lifted form is a COPY of the record at its moment (measured: a pair given on C–D after a lift is on the NEW file's record and not on the old one); a withdrawal from the page would reach into the source universe the copy was taken from. The words say where the act is. **Yours to rule if a page-side act is ever wanted; nothing in the type forbids it.**
4. **THE GRAIN LAW's consequence, measured:** the residue tetrahedron at A lifted from the GEN-2 shape carries the finer grain on its shared face — 7 corners (A · AB · AC · AD and the midpoints ABAC · ACAD · ABAD), 15 edges, 8 faces — every one resolved on the record; lifted from the gen-1 shape it is 4 corners. The section's record line says `N of N corners hold a space` from the form, and the leg's clause reads N from the form (its first draft hard-coded 4 — found by the witness, cured).
5. **Subdivision composes EXACTLY on the record** (your item 4's third clause): AB → ABAC (AB's coprojection, 21 carried) then ABAC → AC (21, read backwards) compose to 15 pairs = the medial edge AB–AC's own J — the solid's 14 composed pairs and the person's born pair, carried through ABAC — with 0 pairs in either alone; the same modulo the prefix on the Ambo's own shape; at gen 1 A → AB → B composes to the seed edge A–B's own J (the person's 3 pairs, nothing else) on the corner cell's record, where B lies outside the region. **One witness fault on the way, not the engine's:** my first check compared the born pair in the stored direction while the edge is stored [AC, AB]; the printed symmetric difference was empty both ways.
6. **The drive family's fourth run is the one that counts** — the second run's dev server was gone by its second viewport (`ERR_CONNECTION_REFUSED` at the 1400 × 900 `goto`, after a first viewport that had driven the Manuscript and come back) — while it ran I had run `git add -N` and a witness in the same tree; a direct probe of the Manuscript switch on a logged server of my own (5299) left the server alive with the Manuscript mounted and its shelf empty, so the switch is not the cause; the third and fourth runs, with nothing of mine touching the tree, served both viewports whole. **The cause is not found; the correlation with a concurrent `git add -N` is said, not claimed** (the go-stale watcher only sends a socket event and never closes the server — read).
7. **Where the section lands on the card (his eye):** the card's reading scrolls (B-130 A.3), and the section sits after the argument reading — measured at 1689 × 897: the card's scroll box 232 × 660 px at (1427, 130); the section 2,163 px tall — its top at y 423 with the form just placed (inside the box, 293 px below the card's top: the argument reading above it), the inside panel 1,115 px tall and 220 px wide (its drawing wider than the column — clipped, scrolling sideways in its inset), the face block 666 px tall; each block's top inside the box once scrolled to. At 1400 × 900 the same heights in a 663 px box. A control placed after the act it serves is the warned class; here the section is a READING (nothing to act on), so the scroll is the reading's own; said, not judged — the designer's blockers pass.
8. **A design default of mine on the card:** the inside diagram and the face block keep the Ambo's DARK ground inside the paper card (their ink is drawn for it — the stone greys, the amber news); a dark inset in a paper card is hers to keep or re-ink. The section's door follows the measures door's grammar (`— shown` / `— show it`), open by default.
9. **Three sentences of mine, hers to refine:** `read from the record the lift carried — <universe>: N of N corners hold a space · the acts are the Ambo's, at the sites the words name` · `born on the page (<provenance>) — no transport carried: a form born after the lift holds no concept-space, and none is minted` · `nothing carried — this form was not lifted from a universe (<provenance>)`; per row, `holds no cast` for a seed and `holds no space — A and C hold no cast` for a born corner.
10. **U1 (§126.2), applied at the record commit:** the sweep (`scripts/u1-citation-sweep.py --apply`, run from its home at 8fad706) found the second wave as reported in the 1816 addendum, grown by two files since (the claims ledger's new text cites two instruments): 1 cited RULING (`RESEARCHER_RULING_ZOO_EXPANSION_CORRECTED.md`, 6,636 B — cited by `RESEARCHER_RULING_ROUTE_B_LINEAGE_CARRIAGE.md`) and 6 cited INSTRUMENTS (49,528 B): three RESULTS files of 2026-09-19 (`flow_phi_offers_under_tau` 4,435 B · `j_register_quantities` 2,932 B · `offers_count` 2,178 B — cited by the J-registers ruling), `hinge_maximum_carried_core.py` (2,250 B — cited by the REPOSITIONING ruling), `descent_at_the_door.py` (26,961 B) and `lift_in_type_theory.py` (10,772 B — both cited by `THE_CLAIMS_LEDGER.md`); the import closure adds none; `.gitignore`'s U1 block regenerated whole (95 negations, every parent directory un-ignored and its children re-ignored) and the 7 files tracked in this record commit. A third wave, if these cite further untracked files, is the next record commit's (the rule is transitive; the sweep runs at each). The ambiguous head `RULING_THE_FIRST_FACE_…`: both files already tracked, kept. **The too-long ruling file is NOT renamed yet** (`RULING_THE-INSIDE-MIDPOINT-TRACE-REFUSAL_on-the-honesty-line_…`, 235 characters; `ls .handoff` at this landing) — it stays ignored and reported, to be tracked at the first record commit after the researcher's rename.
11. **W1 riders AS FOUND** (hashes with their instruments; none read for content): the claims ledger `blob c597962c` → `b32d94af` (`sha256-LF 5306a111` → `41af5720`, 59/0) · the front board `blob 6bc1be5c` → `0e01231b` (`sha256-LF 986b1da0` → `1d72486f`, 14/10) · the connection-layer plan `blob 938960a6` → `8f1cf333` (`sha256-LF 6074eb1f` → `c9fbbff2`, 2/2) · the sovereign ledger `blob ba0ad488` → `f6072bed` (`sha256-LF 16d766ff` → `a27cd6ce`, 15/0 — new this turn, Δ87–Δ90) · ADR 0030 `blob b95d8f16` → `66cb9c3f` (`sha256-LF b5da0f48` → `a6cea23f`, 7/0 — the researcher's rider; the mothership's hashes exactly, both instruments; its citations' closure `lift_ground.py → face_residue_reading.py → inside_midpoint_trace.py` was tracked at e293e45 and the sweep names no further wave from it) — every tail closes on a finished sentence; the designer-seat handoff (`blob 3dd84885`), ADR 0031 (`blob 5ef9a361`) and the render-gate plan (`blob ecf45872`) unchanged.
12. **Not sighted by a person:** Arman's STEP-2 eye on the lifted form's card; the designer's blockers pass on the section (the dark inset, the door, the three sentences, the placement).
13. **The sweep's red at `8fad706`, and its cure at `3e3629e`.** The concept-type witness (§4, THE CAST HAS EXACTLY ITS TEN) found the new model among the cast's readers — one read of `data.cast`, to say which seed holds no cast. A seed resolves to nothing exactly when it holds no cast, so the model now asks the ONE resolver and mentions neither `.cast` nor `ConceptSpace`: the ten readers stand as C-8 ratified them, no eleventh named. The words unchanged. The eye leg's fourth run served the tree before the cure; the cure changes the predicate behind the absence wording alone and the leg's path holds a cast on every seed — not re-run, said.

## 1 · What I SAW

**Item 0, printed (the probe `probe-c10-item0.cjs`, then the witness §1–§2 on the same lawful path — casts on A · B · C · D (Flow · T-cell · Φ · Φ), pairs on A–B (F1 ↦ r0, F5 ↦ r2, F7 ↦ r8, sustains ↦ sustains) and A–C (F1 ↦ Φ1, F7 ↦ Φ3; F13 ↦ Φ9 refused by the register), the core dissected, the first free born pair on AB–AC the store takes — `Φ2 ↦ r1`):**

| region | the file | (a) casts | (b) J in the region | (c) lineage referents outside | (d) `spaceOf` on the shape alone → on the record | (e) faces on the record | (f) the shelf |
|---|---|---|---|---|---|---|---|
| R1 the gen-1 corner cell at A (4 v · 6 e · 4 f) | 51,655 B · ancestor = the gen-1 universe (10 v) | 1 of 1 (A) verbatim | 0 (its seed edges lie outside; the record holds A–B 3 + 1 and A–C 2) | 3 of 6 outside; the parents' edge outside for 3 of 3 | 1 same · 3 NOTHING → 4 of 4 the same | 4 of 4 byte-equal (4 read) | placeable · render plain · ancestors 1 |
| R2 the gen-1 core (6 v · 12 e · 8 f) | 52,094 B · the gen-1 universe | 0 seeds in the region | 0 | 12 of 12 outside; 6 of 6 edges outside | 0 · 6 NOTHING → 6 of 6 | 8 of 8 (8 read) | placeable · ancestors 1 |
| R3 the gen-2 residue at AB holding ABAC (5 v · 8 e · 5 f) | 93,368 B · the gen-2 universe (22 v) | 0 | 0 (AB–AC with the born pair lies outside; the record holds it) | 6 of 10 outside; 5 of 5 edges outside | 0 · 5 NOTHING → 5 of 5 | 4 of 4 (4 read; the square base not a 3-cycle) | placeable · ancestors 1 |
| R4 the gen-2 core (12 v · 24 e · 14 f) | 103,966 B · the gen-2 universe | 0 | 0 | 24 of 24 outside; 12 of 12 edges outside | 0 · 12 NOTHING → 12 of 12 | 8 of 8 (8 read) | placeable · ancestors 1 |
| R5 control — the seed cell of a workspace with NO casts | 10,270 B · the seed universe (4 v) | 0 of 4 | 0 | — | 4 same (nothing on both) | 4 absent on both | placeable · ancestors 1 |

(b) after the load: the edge ids RE-KEYED by the source prefix, the role pairs verbatim (true on every region). (f) today: no reader of `spaceOf` / the face readers under src/manuscript (0 files) — the card showed none of it.

**At the eye (the drive family, the third run — served `5c9ab76`, dirty (17 paths) — the C-10 tree, both viewports):** the residue tetrahedron at A (`tetrahedron cell:residue:… g1` — its corners A · AC · AB · AD in the selection tab) lifted with the notice `lifted “cell:residue:… of Ambo Dissection Ambo Dissection Tetrahedron” → the Manuscript shelf`; the shell's `Manuscript` segment; the shelf holding it; dragged onto the sheet and selected; the card's section `read from the record the lift carried — Ambo Dissection Ambo Dissection Tetrahedron: 7 of 7 corners hold a space · the acts are the Ambo's, at the sites the words name` with the rows `A · 14 roles · 11 words` · `AC · 19 · 16` · `AB · 20 · 22` · `AD · 23 · 25` · `ACAD · 28 · 30` · `ABAD · 29 · 36` · `ABAC · 24 · 27`; AB's row clicked → the inside panel inline, `AB · the inside of the space it holds, derived from its parents`, 20 points, 3 labels wearing `≡` (the plate: `F5 ≡ Φ7` legible on the lifted form); the face list `A·AC·AB · A·AB·AD · A·AD·AC · AB·AD·AC · ABAC·ABAD·ACAD · AB·ABAD·ABAC · ABAC·ACAD·AC · ABAD·AD·ACAD` (D14 names, no id); `AB·AD·AC` picked → the born-face block read, 3 ground lines, `nothing added by a pair of yours yet` ×3, the three hands as words (`at ABAC, on AC–AB: a pair of yours · or at AC, on A–C: an act on the edge it descends from` …), 0 buttons. The plates: `scripts/app-leg/_frames/concept-layer-lift-carries-{1689x897,1400x900}.png` and `concept-layer-lift-inside-*.png` (ignored, never tracked).

**Under node (the witness §6, `renderToString`):** with AB picked on the residue at A lifted from gen 2, the section mounts `data-inside-panel` inline (`data-inside-placement="inline"`, origin derived) with 21 points = AB's 21 roles in the Ambo and a label wearing `≡`; the medial face AB·AD·AC picked mounts the born-face block read, 3 ground lines, 3 hands as words (`at ABAC, on AC–AB: a pair of yours · or at AC, on A–C: an act on the edge it descends from` …), 0 withdraw buttons, 0 news (the pair Φ2 ↦ r1 closes no route on this face — printed); a corner cell's face says `is the corner cell's own: it returns all of its corner to itself`; the seed face A·B·C on the seed cell's record reads absent (it needs B–C); the born-on-page and no-record sentences; the no-cast rows.

## 2 · What I RAN

- **sim-diff:** `git diff --stat 8fad706 -- <the nine committed paths>` EMPTY right after the STAMP commit — the committed tree is the tree the witnesses and the leg read; the working tree's remaining diffs are the five W1 files, ADR 0030, the two `tsconfig*.tsbuildinfo` and `scripts/app-leg/__pycache__/` (listed, none staged into the STAMP).
- **the manifest, every touched engine file:** `src/manuscript/liftedConceptModel.ts` — NEW, `NOT_FROZEN … STAMP C-10` row added after bornFace's (this commit); `src/manuscript/LiftedConceptSection.tsx` — NEW, `NOT_FROZEN … STAMP C-10` row (this commit); `src/manuscript/ManuscriptView.tsx` — `NOT_FROZEN` (row 196, "manuscript view seam"); `src/components/CastInsideDiagram.tsx` and `src/components/MidpointSurface.tsx` — `src/components` carries no rows; `snapshot.ts` (FROZEN, sanctioned) and `subComplexLift.ts` (NOT_FROZEN) untouched. **No frozen file; no positive control needed (no re-seal).**
- **the sweep, whole, read by eye in its own call — twice:** at `8fad706` (the STAMP), whole: `141 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP RED — unexpected fail(s): scripts/diagnose-the-concept-type.cjs` (wall 96.1 s; the lift-carries witness in the census at 2.4 s) — the new model had read `data.cast` once; cured at `3e3629e` (the one resolver asked instead); at `3e3629e`, whole, read by eye in its own call before this record was written: `141 files · expect exactly ONE fail: diagnose-dual-inspection` → `SWEEP OK — the one expected fail, nothing else` (exit 0 — the census 141, the lift-carries witness in the sweep by its own declaration at 2.0 s; the j-register PASS at 71 s inside its declared ceiling; the gate 66 s, the door column 64 s, the born room 45 s; wall 85.1 s).
- **`npx tsc -b`:** exit 0 (at the STAMP tree and at the record tree).
- **THE READING:** the drive family — `DIAGNOSE-THE-CONCEPT-LAYER-EYE: ALL PASS` (87 clauses — 81 → 87, three new arms per viewport: §0 + 43 at 1689 × 897 + 43 at 1400 × 900; `/__whereami` = `5c9ab76`, dirty — the C-10 tree; at the eye, with the leg's born pair r3 ↦ Φ3 standing on AB–AC, the gen-1 residue at A (`tetrahedron cell:residue:… g1`, its corners A · AC · AB · AD read in the selection tab) lifted by `Lift selection → Manuscript` with its notice, on the shelf, placed on the sheet by a drag; the card's section reads `read from the record the lift carried — Ambo Dissection Ambo Dissection Tetrahedron: 7 of 7 corners hold a space · the acts are the Ambo's, at the sites the words name` with the seven corners as rows (the grain law: the three midpoints of its shared face ride); AB's row clicked mounts the Ambo's own inside panel INLINE (origin derived) with 20 points and 3 labels wearing the person's `≡` — the transports still there on the lifted form; the face AB·AD·AC picked from the D14-named list mounts the born-face block on the record — read, 3 ground lines, the hands as words `at ABAC, on AC–AB: a pair of yours · or at AC, on A–C: an act on the edge it descends from`, 0 buttons, 0 withdraws, 0 news (the leg's pair closes no route on this face — printed); THE READING SCROLLS: the section 2,163 px tall in the card's 660 px box, the inside panel 1,115 px tall and 220 px wide in the 232 px column (its drawing wider — clipped, scrolling sideways in its inset), the face block 666 px tall — each block's top inside the box once scrolled to (printed for the designer's eye); the leg ran four times on this stamp — the first found the driver's regex (a `/` in a role-name pattern), the second the cell row's name (`residue/…` is nowhere; the rows read `tetrahedron cell:residue:… g1`), the third the containment measure (a block taller than the box can never lie whole inside it), the fourth whole on both viewports).
- **the node witnesses on the landed tree:** the lift carries 31 ALL PASS (NEW; its four runs on the way: 2 FAIL → 2 FAIL → 2 FAIL → ALL PASS — the §3 direction, the §4 stale selection, the §6 hard-coded 4) · the midpoint 58 · the born face 13 · the born room 34 · the face 24 · the inside 33 · the cast loader 47 ALL PASS · the engine freeze ALL PASS (exit 0).
- **the inbox gate:** `--mark-reviewed --expect 1` before each commit; `--expect 0` after the archive.

## 3 · What I CHANGED

- `src/manuscript/liftedConceptModel.ts` (NEW, NOT_FROZEN) — the model: `liftedConceptOf(form, carried, resolveAbsent?)` → `read` (the record, the source's name, the corners as rows with `held` · `space` · counts · `absence`, the faces as rows with D14 names · kind · `held` · `cornerCell`) | `born-on-page` | `no-record`; `seedsUnder` (the seeds a vertex descends from, off the lineage); `cellsHolding` (by vertex set). Imports the types, the resolver and the D14 composer — nothing else (pinned).
- `src/manuscript/LiftedConceptSection.tsx` (NEW, NOT_FROZEN) — the section: the door, the record line, the corner rows (`data-lifted-vertex-row`, `-space`, `-picked`), the picked corner's `CastInsidePanel` inline (`data-lifted-inside`) or its absence line (`data-lifted-vertex-absence`), the face pick (`data-lifted-face-pick`, D14 names), the picked face's `FaceRecord` / `BornFaceRecord` (`data-lifted-face`, `-kind`) with `here={null}` and `hands="words"`, the corner cell's line (`data-lifted-face-corner-cell`), the absences (`data-lifted-concept="born-on-page" | "no-record"`).
- `src/manuscript/ManuscriptView.tsx` — `liftedConcept` computed for the selected written form from `shelfAncestors.get(form.shape.id)` (never the page lineage); `liftedPicks` per form; `SpecimenCard` gains `concept` · `conceptPick` · `onConceptPick` and mounts the section once, after the ring's declarations and before the trace rows.
- `src/components/CastInsideDiagram.tsx` — `CastInsidePanel` gains `inline` (`data-inside-placement`); the Ambo's mount passes nothing, byte-as-before.
- `src/components/MidpointSurface.tsx` — `FaceRecord` and `BornFaceRecord` exported; `here: Edge['id'] | null`; `hands?: 'act' | 'words'` (default `act`); `BornFaceBlock`'s `withdraw` optional (a hand as words, `data-midpoint-born-face-hand-words`, when absent; `data-midpoint-face-hand-words` on the gen-0 block); the Ambo's own mounts unchanged (pinned).
- `docs/governance/ENGINE_FREEZE_MANIFEST.txt` — the two NOT_FROZEN rows (the completeness law, this commit).
- `scripts/diagnose-the-lift-carries.cjs` (NEW, 31) — §1–§7 as above; joins the sweep by its own declaration (the census 140 → 141).
- `scripts/app-leg/diagnose-the-concept-layer-eye.cjs` + `concept_layer_eye_driver.py` — the C-10 arm (`lift_arm`, `select_residue_at`, `MEASURE_LIFT`): the residue at A selected by its corners in the selection tab, `Lift selection → Manuscript`, the shell's Manuscript segment, the shelf entry dragged onto the sheet, the section measured, AB's row clicked, the face `AB·A?·A?` picked from the list, the section scrolled to the card's top and measured again, a plate in `_frames/`; three clauses per viewport (42 → 45 checks).

## 4 · What I could not reach

- **The cause of the second leg run's server death** — not found; the correlation with my concurrent `git add -N` is said, not claimed; the third and fourth runs, with nothing of mine touching the tree, served both viewports whole (line 6). **The extension's line on a LIFTED face at the eye** — the leg's pair r3 ↦ Φ3 and the witness's Φ2 ↦ r1 both close no route on the residue's medial face AB·AD·AC (0 news at the eye and under node), so the news line on a lifted face is pinned by its grammar only where present; the block is the Ambo's own, whose news line C-9's leg sighted at the ABAC site — not sighted on a lifted form.
- Arman's STEP-2 acceptance (his own eye on the lifted form's card) and the designer's blockers pass — not mine to sight.

## The record commit

- **U1 (§126.2):** 1 cited ruling (6,636 B) and 6 cited instruments (49,528 B; 3 RESULTS files; the import closure adds none) tracked — `RESEARCHER_RULING_ZOO_EXPANSION_CORRECTED.md`; `RESULTS_2026-09-19_flow_phi_offers_under_tau.txt` · `RESULTS_2026-09-19_j_register_quantities.txt` · `RESULTS_2026-09-19_offers_count.txt` · `descent_at_the_door.py` · `hinge_maximum_carried_core.py` · `lift_in_type_theory.py` under `.handoff/instruments/connection_layer_reference/`; `.gitignore` regenerated (95 negations). The ambiguous head `RULING_THE_FIRST_FACE_…`: both files already tracked — kept. The too-long ruling file: NOT renamed yet (measured at this landing) — still ignored, reported; tracked at the first record commit after the rename.
- **W1 riders AS FOUND:** the claims ledger `blob c597962c` → `b32d94af` (`sha256-LF 5306a111` → `41af5720`, 59/0) · the front board `blob 6bc1be5c` → `0e01231b` (`sha256-LF 986b1da0` → `1d72486f`, 14/10) · the connection-layer plan `blob 938960a6` → `8f1cf333` (`sha256-LF 6074eb1f` → `c9fbbff2`, 2/2) · the sovereign ledger `blob ba0ad488` → `f6072bed` (`sha256-LF 16d766ff` → `a27cd6ce`, 15/0 — new this turn, Δ87–Δ90) · ADR 0030 `blob b95d8f16` → `66cb9c3f` (`sha256-LF b5da0f48` → `a6cea23f`, 7/0 — the researcher's rider; the mothership's hashes exactly, both instruments; its citations' closure `lift_ground.py → face_residue_reading.py → inside_midpoint_trace.py` was tracked at e293e45 and the sweep names no further wave from it) — every tail closes on a finished sentence; the designer-seat handoff (`blob 3dd84885`), ADR 0031 (`blob 5ef9a361`) and the render-gate plan (`blob ecf45872`) unchanged.
- The 1101 letter archives with this landing; the inbox is then empty.
