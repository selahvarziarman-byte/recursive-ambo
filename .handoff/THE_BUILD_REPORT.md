# THE BUILD REPORT — B-105: the fact contradiction reconciled to ONE TAP (both seats measured truly), the fold's pick moved ONTO THE DRAWN POLYGON, the window stopped narrating and gained the chiral inhabitant with its LAW-24 control at the eye, the four ruled cuts and both frozen spends landed, the substrate map regenerated — and three of the honesty quartet's premises were stale at HEAD

**STAMP ECHO: `B-105`** — with the mid-build markers **`W` · `W1` · `W2` · `W3`** echoed, each consumed as its own inbox file.
**coder · commits `951b7f4` (ADR §7 as-is) · `0573375` (the ruled cuts) · `85436b2` + `a2bf8df` (the two frozen spends, each edit+re-seal alone) · `e788ed3` (the tap's event mechanics + the witnesses) · `cad8b23` (the spent record) · `4a239bd` (the mandate record) · report follows · on base `f06eb84` · branch `team-arman` · pushed after this report lands**

---

## TO THE MOTHERSHIP

**1 · W3 §2 RECONCILED — both seats measured truly; the states differ by ONE TAP, and the tap is MY OWN LETTER'S NEXT STEP.** At HEAD, immediately after the folded glue, all three stand (measured in the DOM this drive): the wall verbatim · `built 3-manifold 1 — folded` on the shelf (the label is drei `<Html>` = page text) · the `subdivide — resolve the fold; the gate reads again` button. Then **one double-click on the folded body — step 6 of my R5 letter — produces her exact three negatives**: (a) the volume-change effect (`ManuscriptView:3171-3178`) clears `apertureFoldedRows` → the door DIES; (b) that effect does NOT clear the notice → **the wall stands doorless**; (c) the selected body's shelf label hides (`FormLabel` `hidden={selected===id}`) and no card rises (the known no-card frame) → `/built 3-manifold/` leaves the DOM. Her instrument was sound; my report was sound; my letter walked her off the cliff between them. **The doorless wall is REACHABLE IN-APP with no HMR — her §3 finding made mechanical.**

**2 · W3 §3, the measurement (the sentence is hers):** the subdivide door EXISTS (`ManuscriptChrome:796-818`), reachable ONLY inside the open aperture panel, ONLY while `apertureFoldedRows` is armed — from the folded exit press until the aperture volume changes (including selecting the folded body itself), the panel toggles, any later exit fires, or a page restore. **From the folded body's own frame there is NO door on screen**; its one live door is explore. And the wall's promise outlives the door because nothing clears the notice when the rows die. Both of her cure branches are live: the act exists (branch a — "say WHERE": the aperture panel, before pointing anywhere else) — the wording is hers.

**3 · Three of A2's four premises were STALE at HEAD — the retired BACKLOG's rows outlived their cures (measured, no cut needed):** (i) the chord seal file is TRACKED since `7477729` and already carries the DEAD header; (ii) the manifest's `surfaceRefinement` row already names BOTH ratifiers (`diagnose-the-rim.cjs and diagnose-the-exit.cjs`, manifest `:181`); (iii) `diagnose-the-subdivision`'s `headBlobOf` already pins `01da8ed` (`:86`, with the old `HEAD:` scar documented at `:84`). Only (iv) was live — the folded witness's widening `[\s\S]{0,900}` view-pins — recut STRUCTURALLY (`e788ed3`): brace-walked `if (verdict.folded)` blocks, count pinned at exactly TWO, the clauses asserted inside each block; vacuous pass impossible.

**4 · W3 §4(b)'s conditional answered — HER EXTRA WORDS DO NOT FIRE:** the dim-1 loop's shut reasons, measured verbatim: the word family says "this form is a COMPLEX (0 faces), where the word denotes nothing"; cut "Select a face"; dual a non-manifold-edge refusal; sew "0 boundary circles — sewing needs two"; thicken "0 free end(s)". **None is companion-shaped — every reason names the form's OWN lack**, so `nothing on its own` stays unwritten. AND: her empty-set sentence is EYE-UNREACHABLE at this tip — the loop now takes `identify` (cycle-identify opened for 1-complexes since her letter), the arc `fold · identify`; no page form composes the empty total today. The sentence stands at the one producer, witness-pinned (the R2-empty-menu shape: the moment a form with zero doors ships, the total already speaks).

**5 · B2's coincident case has no reachable construction today:** a 1–2-step edge-identify search over the band found NO shape with junction edges AND a pinch vertex. The discriminator is witnessed at the flag's both poles (the girderable seam pinch-free · the two-component link pinch-true at the committed `decomposeLink`) + source-asserted on the union; the misclassification it prevents is engine-unreachable until a richer identify lands. Named, not stretched.

**6 · A4's FLIP first-measure (LAW 26, drive only):** the null control is clean — the flat T³ at rest shows copies to depth 6, zero mirrored. The FOLDED room (the reconciliation drive's own body): most directions are walls ("the manifold ends here"); through the ONE glued corridor, copies of the plaque and coil show TILTED by the front↔bottom fold transport — **but the coil's handedness is ILLEGIBLE at the resting distance; the mirrored return does not yet READ at the eye.** Reported; nothing built.

**7 · W2's shrink measurement:** the {6,6} window DOES show the shrink — central cell → six neighbours → a dense fringe of tiny cells hugging the dashed rim, stopping short of it; the LOD floor does not erase the crowding (the fringe is drawn; 714 dropped are beyond it). Honest edge: {6,6}'s rate compresses the cascade into ~2 visible rings — a {4,5} would show a slower, more gradual crowd. The designer rules what to do with that.

**8 · W1 §B's census:** the page RP²'s acquired quotient = **1 four-gon cell · 2 edges · 2 vertices · q = 2 at each** — the dihedron-quotient presentation, as you suspected. A hemicube-presented RP² is NOT person-reachable today (the word ops act on ONE-face rims; the aperture on 3-cells; identify merges corners, not faces). Measured; nothing ruled. (Per W3 §5 no swap was needed — the dihedron carries the doubled inhabitant perfectly well, and did, at the eye.)

**9 · My R5 letter's `d+1` key was WRONG — the MAP STRING is the invariant:** this drive's menu carries the fold map (`A→B · B→A · F→D · E→C — reversing`) at key `d+3`. The designer followed the STRING (correctly); anyone following my KEY would pick a different map. The candidate keys are enumeration-order artifacts; letters to other seats must cite the map string, never the key.

**10 · One deviation from W3 §6's order, serving its intent:** the substrate map (A1) was regenerated LAST, at the tip — a map regenerated mid-build would have been stale the moment the next cut landed.

---

## 1 · WHAT I SAW — the acceptance drives, at the eye (Windows Chrome; my server on 5174 — a node process from 13:10 holds 5173, likely Arman's own session, untouched)

- **§2/§3 (the reconciliation):** the full route (T³ → aperture → front `A·B·F·E` × bottom `A·D·C·B` → the `A→B · B→A` reversing map → glue): wall + folded shelf body + subdivide door ALL present; then ONE double-click on the folded body → wall standing, door gone, caption gone — her screen, photographed. R3(a)'s `{N}` moved 6→4 live again in passing.
- **§1 (the fold TAP):** hexagon invoked → fold → the panel opens WITH NO EDGE LIST and the legend rides the figure ("…the way it runs is how the EDGES meet · one hue to a pair"). Taps ON THE DRAWN EDGES paired (0,1) · (2,3) · (4,5); the figure showed each pair in ITS OWN HUE, solid, ticks at the run starts, the →⇄ flip visible as the partner's tick jumping ends; the panel rows (`a·a →⇄ · b·b →⇄ · c·c →⇄`) wear the same hues — a person can say out loud which two edges they just paired, by hue, on the drawing. Preview χ −1 · w₁ 1 · orientable no → commit → **"3 cross-caps — born"** → explore → **the Poincaré window ARRIVES** (the route's tail confirmed).
- **§4d (the window stops narrating):** the {6,6} window shows the disk — the phenomenon (rings crowding to the dashed horizon) — under the NAME `hyperbolic — the Poincaré disk` and the LOD line, NOTHING ELSE. The card carries the record: `deck-tiling {6,6}` · `cells at a vertex 6`.
- **§5 (window 4 + the binding control):** the zoo RP² → `spherical — a stereographic plate` — **the chiral coil in the central cell AND its computed antipodal image in the partner (faint, wound the OTHER way)** — the same mark, two places, no prose; card record `deck-tiling {4,2}` · `cells at a vertex 2` · `descent −I ∈ Sym ∧ free — 1 cell pair`. **The control:** the ambo-lifted cube (seed cube → lift → shelf → placed) → `{4,3}` plate with the coil ONCE, no descent row. Two places ⟺ descended; one ⟺ not — both halves stood at the eye.
- **§4a (roles):** the born forms' cards read `—a⁻¹ ← unnamed·unnamed unnamed·unnamed` — the handles gone from every roles position; the concept-set rows keep their counted/handled forms (her held scope untouched).
- **§4c:** the identify chip on the zoo torus speaks "this single-face quotient identifies through the committed word ops (glue / flip-glue on the face)" — the pointer clause gone from the page.
- **B1:** the playground inspector's b₁ row reads **`0 (ℤ/2)`** on the cert branch (the square); the level-1 branch keeps its unringed field-independent form.
- **A4 + W2 + §B** — see TO THE MOTHERSHIP 6–8.

## 2 · WHAT I RAN (the five witnesses)

```
1  git diff HEAD --name-only -- src → EMPTY at the tip (tsbuildinfo dirty by law).
2  freeze manifest: TWO sanctioned spends this build, each Arman-worded in-
   terminal via the twice-landed flow — complexIdentification (85436b2) and
   writtenFormModel's third (a2bf8df) — each edit + re-seal ALONE in its own
   commit; every other frozen file consumed only.
3  re-seals RECOMPUTED (tr -d '\r' | sha256sum) with POSITIVE CONTROLS: the
   old hashes 8957235d… and 0b270df1… each reproduced from HEAD bytes before
   the edit; new rows 0af14869… and 5d91c2df….
4  suite at the FINAL head: 113 files @ exactly 1 fail = diagnose-dual-
   inspection (the accepted baseline) — see the run stamp below. New legs:
   deck-tiling [j] (inhabitant once/twice · far-side · the turning signs
   −2.31/+2.08 · in-one-cell · absence absent · narration-gone + the card
   record, source-asserted) · cycle-identify B2(i–iii) · argument-card roles
   recut (the independent recomputation drops the handle; distinctness moved
   to slot INDICES) · manuscript-operate §4(b) (the ruled sentence + every
   shut door says why, measured on the loop) · folded-edge [f] structural.
   npx tsc -b → exit 0.
5  THE READING — §1 above: every cut driven at the person's eye in the
   Windows Chrome. One stale 0-byte index.lock removed (mtime Aug 24 19:36,
   no live git — the standing protocol, disclosed).
```

## 3 · WHAT I CHANGED

- **`docs/adr/0025…md`** (`951b7f4`): the researcher's §7/§7.1/§7.2 amendment, committed byte-as-is.
- **`src/manuscript/ManuscriptView.tsx`** (`0573375`, `e788ed3`): `FoldTapOverlay` (taps + marks + legend + the `__manuscriptFold` seam) · the pick-layer stand-down during fold · pointerdown tap (the click-chain theft, measured) · the roles/entry/affordance comment sweeps · `deckRecord` on the card · the §4c clause cut.
- **`src/manuscript/ManuscriptChrome.tsx`** (`0573375`): the fold panel's edge list OUT; pair rows de-addressed, hue-matched to the figure.
- **`src/manuscript/argumentReadingModel.ts`** (`0573375`): `endpointNameOf` → bare base names in roles positions; the held-census comment updated.
- **`src/manuscript/affordanceLine.ts`** (`0573375`): `EMPTY_AFFORDANCE_SENTENCE` (hers verbatim, one producer); the composer's type now `string` — total including zero.
- **`src/manuscript/deckTilingModel.ts` + `DeckTilingWindow.tsx`** (`0573375`): the inhabitant (computed on the sphere, antipodal double iff descent) · the window names-and-shows only; ring mark, count caption, arithmetic, rim/pole/descent prose all OUT (the LOD honesty line stays — the drawing's own limit, not the phenomenon; flagged for her eye).
- **`src/components/FieldForShapeOverlay.tsx` · `PlaygroundInvariantsPanel.tsx`** (`0573375`): the certified b₁ figures name their ring; level-1 audited field-independent (no mark on the ordinary). The refusal strings comparing two certified values (classBodyModel/standardBodies) left unringed — they compare like with like.
- **FROZEN** (`85436b2`, `a2bf8df`): `complexIdentification` carries `pinchVertexIds`; `writtenFormModel` routes the girdered arm on it (the endpoint-proxy retired) — each with its re-seal, alone.
- **Witnesses** (`0573375`, `e788ed3`): as §2.4.
- **`CLAUDE.md`** (`cad8b23`): the spent record. **`.handoff/THE_BUILD.md`** (`4a239bd`): the mandate record.
- **Off-repo:** `.handoff/THE_SUBSTRATE_MAP.md` REGENERATED WHOLE at the tip (A1) — every anchor re-verified this session or marked CARRIED with its spot-check; the ambo/lift strata's full re-walk named as owed. (The map stays untracked by the standing `.handoff/` convention — say the word and it gets a gitignore exception like THE_SEAT_MAP.) The four W markers + the designer's copy archived to `inbox/coder/_archive/`.

## 4 · WHAT I COULD NOT REACH / DID NOT TOUCH

- **The doorless wall's CURE** — the fact is reconciled and mechanical (TO THE MOTHERSHIP 1–2); the sentence and the door's lifetime are design calls. I did not touch the notice-clearing, the wall's wording, or the frame (W1 §B / W3 §5's pinch-composition stand with the designer).
- **Window 4's motion clause** — her 1942 §2 asked that moving one image move the other; her 1945 over-rule + ADR §7.2 bound rung 2 to the still. The still double is what shipped; the inhabited register is rung 3+.
- **The FLIP at the eye** — measured NOT legible at rest (TO THE MOTHERSHIP 6); the cure (distance, framing, or the walk) is unruled.
- **The empty-total sentence at the eye** — composer-standing, page-unreachable today (TO THE MOTHERSHIP 4).
- **§3's ledger** — untouched: Arman's walk (rung 2's kill is still his eye — the four windows now answer W2's own ruling), the designer's lanes, the researcher's FYI, sweep R-2.
