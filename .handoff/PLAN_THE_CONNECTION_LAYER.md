# PLAN — THE CONNECTION LAYER *(Station C · the campaign's declared end)*

**Scoped by Arman, 2026-09-03 (Δ67): `a+c` — the Ambo end AND the walk end, built C-FIRST, in three steps.**
**AMENDED 2026-09-04 13:27 (§72) — SUPERSEDED. AMENDED 2026-09-05 19:24 +0330 (§73): Arman ruled P0 — CONCEPTS ARE SPACES (Δ71). The coordinate picture is RETIRED; Step 1 is BLOCKED on four definitional questions; Step 3's §72 rewrite is REVERSED (route-disagreement is LIVE again).**
**AMENDED 2026-09-08 10:17 (§74): THE FOOTING — corners are CAST concept-spaces; an edge is the AMALGAM over a GIVEN J; a face is the loop's MONODROMY. ~~FIRST BUILD CHARTERED: `C-4AMALGAM`~~ WITHDRAWN (§75: a port with no person).**
**⛔ AMENDED 2026-09-08 10:42 +0330 (Δ73, §76): THE LAYER IS DEFINITIONAL BY ARMAN'S WORD — no build until the third corner is cast, one face's monodromy is computed and UNDERSTOOD, and the understanding is written as a THEOREM that becomes the engine's ground. Only then is the packet's new nature — and every build on it — priced.**
**✅ AMENDED 2026-09-10 (§77; ⚠ clock unavailable — shell down): Δ73 STEPS (1)–(4) DONE — the T cell (`E1`) is the third corner; the first face computed BY HAND on Flow·Φ·T; understood under the REFRAME on Arman's re-anchor; the theorem written and formalized. STEP (5) MET: `ADR 0031 — the concept layer is a COSHEAF OF PARTIAL SYMMETRIES` RATIFIED with ONE kill-condition (its two unrun instruments; a disagreement reopens §4's clause). BRANCH (i) ruled by Arman: no centroid-minting dissection, no face nameable — gluing lives on 2-cells and the 3-cell as READINGS. ⛔ STRUCK from §74 below: the C-4 numbers (a FABRICATION — a forward-only J pushed two of Φ's self-relations into Flow); "passages are directed"; "cell-inconsistency is content" (Bianchi is automatic; the only discipline of `J` is commitment). STEP (6) — the packet's PRICING — is NEXT, sequenced not chartered, gated on the runs.**
**Definitions: `ADR 0030` (ratified, claims §66). Refinement law: `ADR 0028`. Registers: `ADR 0029`. This file holds the ORDER and the ACCEPTANCES; 0030 holds the meaning.**

> ### ⇒ **THE ONE PICTURE, and the reason the order is forced:**
> **You name the moments in AMBO · you assign transformations to Ambo's EDGES · you LIFT a region into the Manuscript · you GLUE it into a room · the transformations RIDE ALONG · you WALK inside and find what the room does to the ideas you gave it.**
> ✔ **Why C-first is not a preference: the packet editor — the ONLY place a person writes a concept — mounts in the Ambo's inspector alone** (re-measured at `d4d80fa`: `VertexPacketEditorContent` mounts only in `Panels.tsx`; `src/manuscript/` reads packets and never writes them). ⇒ **Concepts are GIVEN in Ambo and reach the Manuscript by LIFT. A doors-first build would carry stand-in content and be rebuilt when the real content arrived.**
> ★ **And the passage is already doctrine: `ADR 0028`'s *a connection refines by the LIFT LAW — forced along the skeleton, free at BORN cells* was written for exactly this crossing. a+c is not two features; it is the chain the doctrine already describes.**

---

## STEP 1 — THE AMBO END: assign a transport, read a triangle *(the researcher first, then the designer, then the coder)*
**What a person does:** picks an edge of the dissected solid between two concept-carrying vertices and says what carrying a concept along it DOES; then walks a closed triangle of edges **with their eye** and reads what came back.
**The acceptance, in the person's terms** *(re-cut §74 for the footing)*: **C-4 (the edge):** *"Flow and Φ sit on two corners. I said which of their roles are ONE. The edge between them shows me what it holds — the roles that are both, Flow's own, Φ's own — and I can see every relation survived."* · **C-5 (the face):** *"I followed three edges round. For each role the page says: it came back the same, it came back as something else, or it did not come back — and it says by what."* ⛔ **Empty-core guarded: *nothing came back* is never printed as *nothing changed*.**
**Sequence inside the step:** (1) **researcher** — the definitional charter: what a transport may BE at this grain (the fiber group's first form; the identity/absence distinction; what a triangle's residue IS and how it is stated) · (2) **designer** — the surface: how a transport is assigned, how an unassigned edge shows its TRUE ABSENCE, how a residue reads · (3) **coder** — the build, with a witness.
⛔ **Boundaries:** the op-set does not bend (a connection is a FIELD, never an identification) · the transport is a traced GIVEN act (never fabricated; the trivial connection is a CHOICE) · the layer transports the given register, never writes it.
> ### ⛔⛔ **RETIRED 2026-09-05 19:24 +0330 (§73) — the box below described the COORDINATE picture (a concept = a name carrying six amounts; a passage = one number). ARMAN RULED P0 (Δ71, verbatim *"space"*): CONCEPTS ARE SPACES, INSTANCES ARE THEIR POINTS, THE SIX QUALITIES ARE SIX OPERATIONS BETWEEN SPACES.** ⇒ **"ONE NUMBER PER PASSAGE" IS DEAD. Nothing is drawn to that slot.**
> **WHAT DIES:** coordinates/amounts · the mid-walk reading · the graded interpolant and the "decreasing series" (Δ69 §6 retired) · SIX-DISTINCT §1's diagonal model (its §2–3 re-derived for automorphisms) · "a passage's quality is its eigenvector" · add/multiply. ⚠ **CONDITIONAL:** the three regimes + the spectrum unification survive ONLY if concept-spaces are ruled LINEAR.
> **WHAT SURVIVES:** six distinct OPERATIONS · holonomy at a basepoint · **ROUTE-DISAGREEMENT (Step 3's reading — intact, and cleaner)** · Bianchi · the eigen-frame for LOOPS · the invariant closure as a loop object · the boundary stone (if invertibility is kept) · the registers.
> ### ✅ **THE BLOCK CLEARED (2026-09-08 10:17 +0330, §74) — and it cleared BY CONSTRUCTION, not by ruling: Arman had two corners CAST as finite relational structures in his other project (Flow: 14 roles · 13 types · 49 relations; Φ: 9 roles · 14 types · 22 relations), via the researcher's MOLD, by researchers who know nothing of this engine.** ⇒ **THE FOOTING OF STEP 1, as it now stands:**
> - **A CORNER is a CAST concept-space** — roles (points ARE their relational positions; members optional), arity-1 TYPES (a type is a relation), typed DIRECTED relations, axioms (checked at every amalgam; a failed antecedent = a dissolved bound, marked), MARKS (weights, dynamics — carried, never read). **Cast OUTSIDE the engine; the engine TAKES it.** ⛔ *A cast can REFUTE the model and never ESTABLISH it* (Arman's catch, Δ72).
> - **AN EDGE is the AMALGAM (pushout) of its two corners over a GIVEN J** — *which roles across the two concepts are ONE*, chosen from computed candidate cores, possibly EMPTY (= "beside"). **The passage is FORCED by the geometry and POPULATED by the amalgam** (Arman's reframe). ★ **THE BORN MIDPOINT OF AN EDGE IS THE AMALGAM OF ITS PARENTS** — 0028's COMPOSED clause made exact; Δ52's placeholder ANSWERED. "Did not carry" is abolished as a negative: those roles are the midpoint's content. **The boundary stone is INTACT** (each half-edge a total injection; walking back is exact on the image, undefined off it). ⛔ **CORRECTED §77: `J` is a PARTIAL ISOMORPHISM on the shared signature — agreement BOTH WAYS (reflection), no known conflict, every pair marked by its SUPPORT, symmetric, ONE per edge, six in all (~~"passages are directed; six edges → twelve"~~ struck). Under this, FABRICATE-NEITHER holds by construction; under the forward-only scoring of 09-06 it did NOT (two of Φ's self-relations were pushed into Flow's own vocabulary — the numbers once sealed here were a fabrication).**
> - **A FACE is the MONODROMY of the loop's colimit** — per role THREE outcomes (returned unchanged · returned changed · did not return); ⛔ **the EMPTY CORE is a named vacuity class** (*nothing returned ≠ nothing moved* — a build printing the first as the second repeats the telescoping error). ✅ **§77 / `ADR 0031`: the residue is a property of a CLOSED WALK (`h ∈ I(R_v)`, read `Fix · Mov · Und`); a face is the smallest loop, NOT a generating one; the 3-cell's reading is the total colimit; faces and the cell are READINGS — never walked into, never named (branch (i)).**
> - ⛔ **J IS GIVEN, NEVER MACHINE-POSITED** (the name-slot law at the fiber grain — the hermeneutic office's discipline concern, answered: a residue means exactly as far as J is the person's commitment) · ~~cell-inconsistency between six J's is CONTENT~~ ⛔ **CORRECTED §77: for partial `J`'s the cell's boundary word composes to a partial identity for ANY six (Bianchi is AUTOMATIC and disciplines nothing; 0 / 20 000); six identifications can NEVER be inconsistent — the ONLY discipline of `J` is the person's commitment.**
> - **THE OP-SET BOUNDARY HOLDS VERBATIM:** J identifies ROLES in fibers; the op-set identifies CELLS of the base; the tetra keeps four vertices and six edges. **The layer is a DIAGRAM of concept-spaces over the ambo's incidence — a field on the complex, never an identification of it.**
> ### ⇒ **THE FOUR QUESTIONS, as they closed:** (a) roles · (b) a finite relational structure — neither a bare set nor a vector space, so the name-poverty does not arise · (c) named · (d) the cast + J. ⚠ *(a)–(c) closed by construction; the researcher confirms in their vocabulary (asked).* **The invertibility fork is CLOSED by the stone standing.**
> ### ⛔ **THE ORDER OF STEP 1 — RULED BY ARMAN (Δ73), and it is DEFINITIONAL FIRST, BUILD LAST:**
> 1. ✅ **THE THIRD CORNER is cast** — the T cell (`E1`, 09-09), the restarted project's first pour against MOLD v4 (ten roles · six types · eleven instances, three declared negative · axioms-with-warrant · *any arity*). *(Not U — the outside project restarted; Flow and Φ stand.)*
> 2. ✅ **ONE FACE's MONODROMY is computed — BY HAND** on Flow · Φ · T (⚠ `first_face_and_tower.py` written, UNRUN — the shell): under the weight-favoured triple `h = (F1→F7→F8→⊥)(F12→F5→⊥)(F13→⊥)` — three Flow roles return CHANGED, two do not return, none unchanged; the loop glues Flow's transport cascade; cone-deficit 3; under a rival triple `1_{F7}`; **the MEET over all admissible triples is `0` — structure fixes nothing about this face; all its meaning is the person's.** Two refusals are where structure bit (a declared negative; member-status).
> 3. ✅ **UNDERSTOOD** — under the REFRAME, on Arman's re-anchor (*"the picture is closer now"*): tied candidates = GAUGE · the person's `J` = the lawful symmetry-breaker · the residue = COVARIANT meaning with the `J`'s as record · the meet = the INVARIANT stratum. *The engine makes a person's identifications canonical (amalgams), computable (monodromy), legible (three outcomes in the casts' words), and refuses him where they collide with what a cast declares.*
> 4. ✅ **THE THEOREM written** (T1–T7 + the packet corollary) and **FORMALIZED**: a connection valued in PARTIAL SYMMETRIES; the classical connection = the total sub-case.
> 5. ✅ **THE ENGINE'S GROUND — `ADR 0031` RATIFIED (§77); its ONE kill-condition FIRED at one clause of §4 (T–Flow's top weight is 5, not 4 — the hand search never tried `F13` as the anchor) and was DISCHARGED the same day by TWO independent implementations (a JS port in the in-app browser; the Python natively on Arman's machine — identical). No invariant, definition, or sentence of §§1–3, 5–6 changed. STEP (5) IS MET WITHOUT CONDITION (§78).** Branch (i) ruled: no centroid-minting dissection; no face nameable; gluing on 2-cells and the 3-cell as READINGS. ⚠ *The ADR is UNTRACKED at `1b24e7f`; it rides the pricing's record commit.*
> 6. ⇒ **CHARTERED `2026-09-10 13:17` — `STAMP C-6PRICE` (coder): the packet's PRICING, a MEASUREMENT and no build.** Five questions, each answered with file:line · manifest row · count · STOP-or-not: the corner's packet (its data type's home — ✔ `src/lib/packets.ts` is FROZEN and UNSANCTIONED, a STOP by construction; persistence in `snapshot.ts`; does the LIFT carry it whole) · the cast's shape as a typed INPUT and its frozen union + closure bill · how a cast ARRIVES (a loader priced against MOLD v4 §7, not built) · the EDGE's slot (a per-edge input `J` keyed to a RECORD identity; six, never more) · the derived producers (one line). **The frozen union the report names is what Arman sanctions; then the designer's corner surface; then the build.** ⛔ **No build inside the pricing.**
> ✅ **PRICED (`2a0c950`, §79): the frozen union is ONE FILE — `src/types/geometry.ts` (`cast?` on `VertexDataPacket` :88 · `identification?` on `Edge` :131; ~45 lines + one re-seal). Persistence, lift, editor: FREE (the packet rides whole by spread; the lift deep-clones vertices whole; the editor's patch is `Partial`). Loader: ~80 non-frozen lines against MOLD v4 §5's schema, its §7 promises as five refusals; ⚠ no cast exists as JSON yet. Edge key `edge.id`, a RECORD across a hop; six `J`'s; sub-edges compose from the midpoint's `sourceEdgeId` (§3.5 is the substrate's shape). Producers: `conceptLayer.ts`, NOT_FROZEN while no frozen file imports it — *types flow down, producers stay above.* **MEANING RULED: additive — the corner HOLDS a cast; the label stays the name-slot.** ★★ **§97 (09-19 13:32): SURFACES 1 AND 2 RULED; `C-6c` CHARTERED — the layer's first person-facing build.** The ground the surfaces stand on, ratified: **the RANGE of a cast is the TYPE (`ConceptSpace` — a bare relational structure; MOLD v4 is one PROFILE over it); a thin cast is FIRST-CLASS; the device checks what is true of a STRUCTURE (CLOSURE · ARITY · CONSISTENCY), never of a PRACTICE; it NEVER GRADES — a clean load is not a clean cast; MOLD v4 §7's five are PROMISES discharged by behaviour, the surface's acceptance test.** Two refusals only: not-a-cast; a contradictory record (refused BY NAME, at the cast — the tuple/cast scope one line with the researcher). The card: three states · one count across both homes · the TERM-ORDER count (`r · 3 tuples · none reversed`) · address-not-name · `UNKNOWN` shown, omission silent. ⚠ A second frozen spend is priced first inside `C-6c`: the SUBJECT-MATTER field (what a concept is OF — load-bearing for identity across isomorphic casts; never the `label`). ⇒ **6b's surface 3 (the `J` register) is the designer's next; `C-6d` charters on it. Then `C-5`, the face.**
✅ **§98 (09-19 14:43, `aab7598`): 6c (ii)–(v) LANDED `5f8fde1` — THE LAYER'S FIRST PERSON-FACING BUILD: a corner TAKES a cast (`src/lib/castLoader.ts`, NOT_FROZEN; the profile mapped onto the type; CLOSURE · ARITY · CONSISTENCY; two refusals by name; take-and-mark; never grades; the five promises by behaviour; load state PER CORNER) and its card READS it (three states; both-homes count; the term-order count; address-not-name; `warrant carried, never read` ONLY when held). Four corners sighted at the coder's eye (⚠); row 90's positive control reproduced at the mothership's (✔). ✅ **6c (i) — the SUBJECT-MATTER field `subject?: string` on `ConceptSpace` — PRICED, STOPPED, and SANCTIONED (Δ79, 14:55, *"yes sanctioned"*): lands ALONE (edit + re-seal, one commit), then its two unfrozen riders; READ never CHECKED.** ✅ THE RESEARCHER'S LINE (cast-level refusal NAMED AT EVERY tuple; identical repeats are redundancy; consistency spans BOTH homes) ratified and measured against the loader: three deltas ⇒ **`C-6c (vi)` (coder, live)**. ✅ **6b SURFACE 3 — THE `J` REGISTER — RULED and RATIFIED §1–§4:** the em-dash NO (by count); the four states, the middle two never collapse (`nothing identified` + given-mark ≠ not yet acted); candidates offered weight-ordered, pairs marked by SUPPORT, exposure never as disagreement, **two counts never a ratio**; ties ORDINARY, explained by `|Aut|`, never ranked among themselves; the take given-marked and withdrawable; a FIAT pair visibly distinct. Her §5 (the Layer-3 panel's twice-printing) held with her and PRICED by the coder. **6b is RULED on all three surfaces.** ★★ **§125 (09-23 16:41): THE DESIGNER DROVE BOTH CUTS — ruled whole: *`≡` is the person's act and only that* (nine composed words wore it); the composed identity once per site; ONE refusal grammar; the card's `Space` row at a born vertex; the born face's surface (solid quiet, born routes marked and attributed; an interior face's two walks). Rider 4 in 0031 (§3.8 the face at gen ≥ 1). **THE LIFT'S GROUND RATIFIED — the lift CARRIES** (step 2 below). U1: a tracked file may not cite an untracked one. ⇒ **NEXT: `C-7h` → `C-9` (the born face) · DESCENT at the concept layer (the researcher) · Arman's two lift questions.** ✅ **§124 (09-23 15:35, `6fe02bb`): THE TOWER BY CONSTRUCTION — `C-8b` (the corner carry IS the coprojection; the medial meet ANCHORED on the one shared parent, roles and words) and `C-8c` (THE STONE AT THE ACT: a born pair that would make two roles of one corner one is refused by name; a pair dissolving a doubling taken) RATIFIED; gen ≤ 3 byte-identical; the gen-4 station its born vertex's own space; `R` capped at one copy per lineage; the stone agreeing with an independent predicate on every candidate pair. ⇒ **NEXT unchanged: the designer's drive, then the BORN FACE's surface → `C-9`; the researcher's rider 4, then the LIFT's ground.** ★★ **§123 (09-23 14:12): THE FACE AT GEN ≥ 1 HAS ITS GROUND — the researcher's five definitions RATIFIED and RUN at the mothership's hand: three kinds of born face (medial · interior, between two cells · corner-cell); born rooms empty, each is DETERMINED by gen 0 (the medial loop is the route `B → C → A` on a vertex holding both — `h_A` transported on the glued part, and it also moves `B`-only roles: 9 in 9 of 42 — not `h`); **the NEWS is the born pairs'** (every added route runs through one); refusability INHERITED (6 of 42), growing only through born pairs, a refusal named by PAIRS of tuples; `Und` addressed to the born face's edge, descending from exactly one seed edge, both hands reachable, the device never choosing; ONE NEW CASE — an interior face has two walks, one per cell, each with its own D14 name. **THE ANCHORED MEET RATIFIED** — gen-4 compounding was an artifact of the content meet (15,415 spurious splits, 0 anchored; one copy per lineage): the coprojections witness identity first, content decides only what they leave open ⇒ `C-8b`. ⛔ **THE STONE UNENFORCED AT THE ACT: at gen 3 a born pair can pool a corner and the store takes it (2,710 of 3,000); invariant 3 is ratified law ⇒ `C-8c`.** ⇒ **NEXT: `C-8b` → `C-8c` (the coder) · the designer's live drive, then the BORN FACE's surface · then `C-9` builds the face at gen ≥ 1; then the LIFT; then the WALK.** ★★★ **§122 (09-23 13:29, `b3f6c01`): THE BORN ROOM IS IN THE ENGINE, REACHED LAWFULLY — `C-7g` `609293e` (the designer's second cut) and `C-8` `82512e1` RATIFIED: ONE resolver `spaceOf`, re-derived at every read and stored nowhere; the `J` by the edge's KIND (seed: the record · corner: the carry · medial: the MEET by seed content ∪ the born pairs); the meet never pools (0 of 3,000; the parent's rule 986); the loader a WHITELIST of the seed (Δ86); the composed identity marked, never a pair; a born pair TAKEN at the eye; a later act that would break it REFUSED by name with two hands — controlled both ways at the mothership's hand (1,626 / 1,626). **Δ85's (a) is MADE.** Item 5 ruled: an act's generation is where it lives in the tower, never when it was made — no frozen spend. ⛔ **One defect past the seal: the corner edge carries by CONTENT, not by the gluing's coprojection — a station over a DOUBLED vertex (gen 4) is its born vertex's own space in 82 of 3,000; latent, walked by nobody; the researcher's model was right ⇒ `C-8b` (the carry by construction). ⭑ And gen 4 COMPOUNDS the lawful doubling (a seed role in up to 4 roles; pooling still 0) — a question for the researcher, owed before any walk reaches gen 4.** ⇒ **NEXT: THE FACE AT GEN ≥ 1 — the researcher's ground first** (what a born face reads, and what in it is NEWS: the expectation disclosed — its solid part is determined by gen 0, its news is the born pairs'); **then the designer's surface, the coder's build, Arman's acceptance at his hand; then the LIFT carries; then the WALK carries.** ✅ **§121 (09-23 11:10): `C-8`'s RESOLVER RULE CORRECTED BEFORE THE BUILD — `spaceOf` by the edge's KIND (seed record · carried injection · the MEET ∪ born pairs), seed content carried down, derived never stored; the parent-only rule POOLED the stone in 960 of 3,000 and was withdrawn by its own author nine minutes after it was sent. ADR 0031 §3.5 RESTORED in the tree (rider 2). The face block cured without leading with its verdict (ADR 0024); D14 makes the face's direction a fact to state, not a choice; `composed` is a fourth origin, never a pair. ⇒ `C-7g` → `C-8`.** ⛔ **§120/Δ85/Δ86 (09-23 10:47): THE BORN ROOM RULED — (a), in the form that restores ADR 0028's CARRIED · COMPOSED · BORN (0031 §3.5 had silently erased BORN): corner edges carry, medial edges compose their shared corner, the born room beyond it is the person's; the order of acts is dependency, refused by name. CAST LOADING IS ONLY FOR THE SEED (Δ86). ⛔ THE CHAIN IS BROKEN AT ITS SECOND ACT: item 0 reached the born room only by loading files onto midpoints; on the lawful path the next midpoint receives nothing (it reads `data.cast`; composed spaces are drawn only in place). ⇒ `C-8` — `spaceOf` re-derived and never stored; the loader a whitelist of the seed; the composed identity shown; the dependency refusal; the drive family on the lawful path only. **Station C step 1 stays CLOSED at gen 1 (the edge, the chain's first link, the face at gen 0); the chain's SECOND link is `C-8`.** ★★★ **§119 (09-23 10:08, `7d8d021`): THE FACE IS IN THE ENGINE — `C-5` at gen 0: the monodromy of one face (Δ73's precondition for everything) READ AT A PERSON'S HAND, refusable (four things, three hands), the direction stated, `Und` = *did not return* with its address, the face's core derived. Δ73's chain is complete at gen 0. ⛔ ITEM 0: a pair between two BORN corners is reachable today — 0031 §3.5 violable by a person ⇒ **the ruling is ARMAN's (does he act at every generation, the order of his acts part of what he made?), with the researcher's technical line on the cost owed beside it.** `C-7g` (the designer's second cut) chartered. ⇒ **NEXT: Arman's word on §3.5 and his acceptance of the face; then THE LIFT carries (with `CONTEXT.md`'s proposition collision and the 0030 §2 rider), then THE WALK carries.** ★★★ **§118 (09-22 17:05, `2859a06`): STATION C — STEP 1, THE AMBO END: **CLOSED.** `C-7e` (the carry, and it MIRRORS — 12 of 78 pairs reverse at gen 2→3, so a blind copy by pair would have carried an identification the person never gave) + `C-7f` (the designer's eight). The person-gate by Δ84, the chain by the carry, driven at both viewports: **a mapped midpoint, dissected again, selected as a corner, draws its glued space through one code path — Arman's *every midpoint becomes a parent* is TRUE at gen 2.** ⇒ **`C-5` — THE FACE — CHARTERED (gen 0, the seed's four faces): the composite through the corner (orientation read at read time, not stored); `Fix · Mov · Und` with `Und` = *did not return* and its first-break address, never *the person has not said*; the direction walked STATED (the reading is orientation-relative); the domain the face's core at A with ambient `R_A`; ⛔ THE COLIMIT ATTEMPTED NEVER ASSUMED — the face is REFUSABLE (6 of 42 at base T, a lower bound; only a cast that declares a NEGATIVE can refuse a face), the refusal naming four things and offering three hands.** ⛔ One open branch: a FRESH pair above gen 0 makes two acts into two spaces (2,862 of 3,000) while §3.5 forbids it and the store has no guard — **the coder measures reachability; the mothership's expected ruling (§3.5 AMENDED, the order of acts carried by the TRACE) is disclosed to both seats before it is made.** ⇒ **After the face: the LIFT carries, then the WALK carries.** ★★ **§117 (09-22 15:29): TWO SEATS CLOSE THEIR BOARDS — the researcher's four ratified (the witness is a parent whose RECORD IS DEFINED; the caster key FOREIGN; **§8(b) RUN 0/20,000 with a live positive control** — an identity, so the ground is a theorem and the news is `Fix(h_A)`; the frame RELOCATES as a companion with a new law about naming what is not in the midpoint; the associator is a RHYME, not an ancestor) and the designer's eight (the word to the arc's FOOT because apexes cluster; the empty map bare; the re-made attempt attributed; three origins in WORDS; the card a GRID mismatch; the source WHOLE or WORDS never a thumbnail; **39% of text under 4.5:1 including a tab and a button**; *a value may not be dimmer than its label*). ⇒ `C-7f` chartered. ⛔ **The face waits on TWO definitions that my ledger recorded and my letters never sent** (`Und` on a PARTIAL edge record; the residue's domain) — sent. ⭑ **And the carry makes the researcher's gen-2 question live: is the colimit ASSOCIATIVE ACROSS GENERATIONS? If not, the order of a person's own acts changes the space they build.** ★★★ **§116/Δ84 (09-22 14:55): *"accepted. pay the price"* — THE ACT IS ACCEPTED WHOLE at Arman's own hand (both halves; Δ67's sentence met; the ROLE-half caveat discharged) and THE CARRY IS SANCTIONED. `C-7e` chartered (the identification copied onto the re-derived edge of the same PAIR; the τ-drafts re-keyed by pair; the witness clause flipped; the chain's closing clause TRUE at gen 2, measured). ⇒ **Step 1's Ambo end: the person-gate is DISCHARGED; the station closes at the carry's landing — one cut away.** Then `C-5` — THE FACE (held on four researcher definitions: the witness count · caster keys alike · what `Und` means on a PARTIAL edge record · whether the residue reads on A's roles or the core's). ★★ **§115 (09-22 14:41, `c96fd46`): `C-7c` + `C-7d` LANDED — the word half ABOVE the drawing and inside the person's visible box (107 px below the fold before, 0 px of chips visible; one sentence names both halves); THE MIDPOINT DRAWS ITS OWN SPACE through the one presentation (Arman's third finding, built); the sources carry HIS acts on `A–C`/`B–C` (the decoration gone; the June ordering spine on the surface in words); the covering's mechanism found inside the SVG and cured with no guessed z-index; **the concept layer has a DRIVE FAMILY leg that measures the visible box before it touches a hook** (§114.2's hole closed by construction). ⛔⛔ **A DATA-LOSS DEFECT CONFIRMED ON BOTH ARMS at its mint (`edge:${hash(shapeId|pair)}`): every gen-1 act is invisible at gen 2 — PRICED (~8 unfrozen lines in `ambo.ts`, +~10 for the drafts) and it is THE CONDITION of *every midpoint becomes a parent*; the chain's closing clause is one code path today and true at gen 2 only with the carry. WITH ARMAN, beside the word half of his acceptance.** ⇒ then `C-5` — THE FACE, whose first line is a new ~40-line composition module (composition does not exist in `midpointGlue`). ⛔ **§114/Δ83 (09-22 13:05): HALF THE ACT WAS UNREACHABLE — only role-to-role pointing could be found; the word control exists and sits below two tall columns and a whole projection diagram in a scrolling panel. §113.1's acceptance AMENDED to the ROLE half alone (he never reached the seal's glued state, and called the projected sources decorative with the word half of the clue absent — still right). ★★ THE LAW'S SEVENTH FIRING WITH ITS INSTRUMENT NAMED: **a witness that locates a control by its data attribute proves EXISTENCE, never FINDABILITY** — every `data-*` hook a witness drives is a hole in the eye-test that size; a *person does X* acceptance may not close on a hook query; the canvas overlay is a person-facing reading and the drive family's trigger reaches it. ⇒ `C-7d` item 0 = the word control's reachability, ahead of everything. ★★ **§113/Δ82 (09-22 12:54): THE WALK TAKEN — the edge's MECHANISM accepted at Arman's own hand (*"the rest are alright"*), the STATION still open on three riders: the projected sources are DECORATIVE (ruled — their work is the comparison made of the person's OWN acts on `A–C` and `B–C`; the composition and residue stay the FACE's; the June ordering spine appeared at his eye), a MAPPED MIDPOINT MUST BE DRAWN AS ONE SPACE (*because every midpoint becomes a parent* — `glue` already holds the amalgam; one presentation serves corner and midpoint alike, which is the CHAIN's own requirement), and the UI (a covering defect + readability + the card's aspect). ⇒ `C-7d` chartered; **Step 1's Ambo end closes when the midpoint is drawable as a space — not before.** ★★★ **§112 (09-22 12:10, `9b45a3a`): STEP 1's AMBO END IS BUILT — `C-7pre` `436a182` · `C-7a` `9929721` (the inside DRAWN on the canvas, faithful and recoverable) · `C-7b` `7490026` (the MIDPOINT GLUED BY HAND: the two faces unfolded with BOTH opposite vertices whole; pointing role-to-role and word-to-word; the refusal offering every withdrawal; the pushout's seal `20 roles (14+9−3) · 22 words · 52 tuples (4 both)`; the trace a partition with per-parent denominators). Both witnesses pass at the mothership's hand; ADR 0031 carried clean. ⇒ **THE PERSON-ACCEPTANCE IS REACHABLE AT THE MIDPOINT AB — Arman's own hand, with `M3F` riding on it.** Open beside it: `C-7c` (a possible data-loss defect — does a second dissection keep the identifications? — and the retired panel's unmount), two researcher definitions, four designer calls at the eye. ⇒ **NEXT: `C-5` — THE FACE, its ground written (0031 §8(b)); then the lift carries (with `CONTEXT.md`'s proposition-collision), then the walk carries.** ⭑ **§111 (09-22 10:36) — THE ORIENTATION LAYER READ WHOLE; three things enter this plan from the June record: (1) THE ORDERING SPINE (`M5c`): *a site's prompt is answerable only once its J/A/B neighbours are named* ⇒ **a midpoint's surface is answerable only once its parents hold casts, and at gen ≥ 2 the parents are themselves midpoints — the ambo's generations impose an ORDER on the person's acts. Station C's own dependency, stated; `C-7b` builds gen-1 only.** (2) THE LIFT'S COLLISION: `CONTEXT.md`'s ratified table makes a VERTEX a PROPOSITION in the playground, while here a vertex carries a concept-space whose ROLES are its points — step 2 (the lift carries) must either collapse the space into one proposition or refine the playground's grain; **routed to the researcher with the 0030 §2 rider, before we get there.** (3) `M7` THE BRIDGE — *answered packets liftable as a named subcomplex → the topological module* — is step 2 under its older name, OPEN-DESIGN. Also: `M3F`, the June acceptance test *does the prompt help the human name better than geometry alone?*, declined once and still open — for Arman when `C-7b` is at his hand. And the midpoint's STRUCTURAL reader already exists (`generalSitePacketPresenterV0`, complement of `AB` = `{C,D}`, tested at every generation) — the coder consumes its TRACE.** ★★ **§109–§110 (09-22 10:28): THE GRAPH IS THE OFFICE'S INDEX — `graphify-out/` showed the honesty line was ruled in JUNE (`CAL-2`) and BUILT (`trisonizedMidwifeReadingV0.ts`), the GROUND PLAN carries the opposite-vertex principle verbatim, and the archived Fano associator is §8(b)'s ancestor (its carrier claim withdrawn by Station I). Δ81 *"both!"* — `CAL-1` (one combined projection) SUPERSEDED; BOTH vertices shown, with the fence: ONE map, ONE core, ONE trace. ADR 0031 amended (7 hunks, verified). `C-7a` (the INSIDE as an arc diagram over the caster's order, on the CANVAS) and `C-7b` (the MIDPOINT unfolded about the shared edge; pointing; the two-handed refusal; the pushout; `both` alone marked; per-parent denominators) CHARTERED behind `C-7pre`. ⇒ **then `C-5` — THE FACE, where §8(b)'s route-map becomes the residue; then the lift carries; then the walk carries.** ✅ **§108 (09-22 10:00): THE RE-CUT'S GROUND DELIVERED — the INSIDE = the incidence presentation (faithful, not Gaifman; nothing for the unrecorded); the MIDPOINT = the pushout over `(J, τ)` = over the core, the opposite vertices shaping nothing (invariant 9); the TRACE = the origin partition, the residual `A ⊖ K`; the refusal at the act, named at the tuple; 0031 amended (§1.3/§3.6/§5, invariant 9, +§8 Legibility: clueing at the edge, gluing only at the face — the midwife's formal shadow IS the face's residue). The designer: only arity 2 is an arrow; the midpoint's surface = the two faces UNFOLDED about the edge; the residual label is the parent→child relation; the caster's order everywhere; the diagram on the CANVAS. A fabricated refusal (`has × unrecorded`) found and corrected. ⇒ `C-7pre` (the refusal) → `C-7a` (the inside on the canvas, on the designer's form) → `C-7b` (the midpoint unfolded, on her gestures) → `C-5` on the midpoints. ⛔⛔ **§107/Δ80 (09-21 12:43): RE-ANCHORED — THE HONESTY CATCH. The register's offering was a DRIFT (agreement counts offered as reasons; identifications computed from the two parents alone; a g0 surface with no consumer) — retired as a surface; its arithmetic kept as an instrument and the refusal check. THE HONESTY LINE: the device states the FORM, shows the RECORD, REFUSES by name, shows the TRACE — never candidates, never a ranking, never a same-name proposal. THE SITE: the born MIDPOINT after the ambo, the parents side by side with BOTH opposite vertices as projection sources (the oldest meaning system — the Trisonized Midwife — read at last: the child is made legible by the opposite vertex, never a parent-blend). ⇒ **STEP 1 RE-CUT: (i) the INSIDE of a corner's space as a DIAGRAM · (ii) the ambo bears midpoints whose space is the AMALGAM over the person's role-map AND word-map — unglued until given · (iii) the midpoint's surface: point role to role, word to word; the device refuses contradiction by name, glues, shows the trace · (iv) `C-5` on the midpoints. Order: researcher (definitions) → designer (three surfaces) → coder → Arman's acceptance at a midpoint.** The persistence sanction PARKED. ★★★ **§106 (09-21 11:16, `10ccb00`): `C-6d (γ)` LANDED `6b642bb` — THE REGISTER IS READY FOR ARMAN'S HAND: τ as the person's (the seal read directly; the device's by-name pairs a marked PROPOSAL, confirmable); the mold's `member_status` by name (T × Φ refused by name); `read as directed` on the act's line and the card; the witness 53/53 at the mothership's hand. THE PERSISTENCE PRICE with Arman ((a) one frozen line + re-seal — one home; (b) a draft in the workspace export — two homes by discipline; recommended (a)). ⇒ **STEP 1's EDGE CLOSES ON ARMAN'S TWO WORDS: his acceptance at his own hand (give τ, take a `J` on Flow–Φ) and the sanction. Then `C-5`, the FACE.** ✅ **§105 (01:46): THE REGISTER'S DEFINITIONS CLOSED — τ REPLACES (by-name sharing is the device's PROPOSED default, marked as such; the MOLD's `member_status` shared by definition; caster types only by τ); `member_status` canonical; one name one home stands (the range ruling amended). Its copy MEASURED at the designer's eye (the tie sentence per group; no empty `types:` line; exposure by side; both panels kept; her 09-04 finding withdrawn). The trap's catch cured a third way: `— read as directed` beside the evidence, `· read as directed` on the act's line. ⇒ `C-6d (γ)` chartered (definitions + copy). **ARMAN'S ACCEPTANCE AFTER (γ).** Then the τ-persistence sanction; then `C-5`.** ★★★ **§104 (09-20 00:33, `b383df8`): `C-6d (β)` LANDED `2a193b6` — THE `J` REGISTER IS IN THE ENGINE: the layer's first GIVEN surface, every one of the designer's states sighted at the coder's eye on the real pair (Flow × Φ: τ given, 18 offers in 16 readings, the top taken, a fiat pair with its conflict by name, `none`, withdrawal); the witness 48/48 at the mothership's hand. `(α)` re-pinned (`41e2735`; a coder maximality error caught by the seal). Open: three definitional lines with the researcher (τ adds/replaces; by-name as a device premise; the unary key's spelling) + the two-homes contradiction; the designer's first eye (six readings; the witness panel's fate); **τ before a take must persist — ruled an input; priced by the coder; a possible third `geometry.ts` spend for Arman AFTER his acceptance.** ⇒ **STEP 1's EDGE closes on ARMAN'S OWN ACT: he gives τ and takes a `J` on Flow–Φ at his hand. Then `C-5`, the FACE.** ★★ **§103 (23:10): `C-6d (β)` CHARTERED — THE `J` REGISTER, the layer's first GIVEN surface (the designer's riders 1–3 ratified: all offers by orbit within weight, ALL OR NONE, a FIFTH state `not computed`; τ above the offers as the stated premise, withdrawable and marked; the tie sentence leads with the reading; `roles`+`types` written, `support`/`fiat` by nothing; the seal at the eye on Flow × Φ under τ₃/τ₄, the T cell, the triangles). The person's acceptance: Arman gives τ and takes a `J` on Flow–Φ at his own hand. Open beside it: the researcher's line on a contradiction between two of its ratified texts (the two-homes clause — loader refusal stays as built); the designer's sighting of the term-order row in the selection tab and her ruling on whether the trap's catch is MISPLACED one tab from the load. ⇒ **After `(β)`: `C-5`, the FACE — follow a loop, read `Fix · Mov · Und` at the triangle, empty-core guarded — with Flow · Φ · T all fixtures; then the lift carries; then the walk carries (the 0030 §2 rider from the researcher then).** ✅ **§102 (20:30): WEIGHT FOLLOWS SUPPORT — (b) ruled on principle (weight = arity ≥ 2; a unary key refuses and prints its own count, never orders); the seal renumbered (`relational weight 11 · types: 10 of 10 agree`); the wild seal EXACT under named τs — τ₃ top 4 at size 3, τ₄ top 5 at size 5 (the 09-08 size-4 was a sub-core; the 'tie' only across τs; the sampler retired); `|Aut(Flow)| = 2`; a τ-transcription divergence between the two implementations caught at the mothership's hand before it could fire the kill rule — one τ per seal line, the reference's maps verbatim. ⇒ `(α)` re-pinned by marker; **`(β)` is gated on the designer's ONE letter alone (riders 1–3; giving τ is the register's first act at the real pair); the researcher at rest.** Then `C-5`.** ✅ **§101 (18:35, `cff2e37`): `C-6e` and `(vii)` LANDED — 6c is CLOSED; the register runs on the REAL casts (Flow 14/34 · Φ 9/22 as fixtures); the 09-08 wild numbers reproduced relations-only. ★★ THE SAME HOLE FOUND TWICE: a value every role shares backs none — SUPPORT corrected to arity ≥ 2; `t-cell × t-cell` has TEN offers (reproduced); the designer's scale rider DISSOLVED — the register's number is ORBITS. Open: the researcher's line on a constant unary key as a tie-breaker in WEIGHT (7 vs 8 at Flow × Φ) and the wild seal under the corrected clause; the designer's one letter (riders 1–3). ⇒ `(β)` charters on those two; then `C-5`, the face — Flow · Φ · T all exist as fixtures now.** ✅ **§100 (17:31, `d06080a`): 6c (i) LANDED ALONE `d9b3c0c` — the second `geometry.ts` spend CLEAN (both hashes at the mothership's hand); its riders `d7e28d7` (`cast.subject`; the `Of` row only when held); (vi) `ba09810` — the loader exactly as the researcher ruled; `C-6d (α)` `a162aa9` — THE `J` REGISTER'S ARITHMETIC AS A SECOND IMPLEMENTATION (`src/lib/jRegister.ts`, NOT_FROZEN, pure; tuple-driven against the researcher's `10!`; every number the seal's; RUN by the mothership 16/16); the census `32d7eeb`. ⇒ **6c is CLOSED but for two small stamps: `C-6e` (the Layer-3 cure before (β) + the card's not-taken clause) and `(vii)` (Flow and Φ as fixtures; the wild clause; the budget at the real pair; the offer count printed). `C-6d (β)` — the register's SURFACE — waits on the designer's three riders and the researcher's offer count. Then `C-5`, the face — its fixtures (Flow · Φ · T) will exist by then.** ~~★ **§99 (16:21): THE PIN IS IN AND REPRODUCED at my hand (96 s; identical) — six definitions on the type (candidate · weight/support · exposure/unrecorded · `Aut` and gauge orbits · the empty core's two shapes); the numbers sealed with falsifiers held; δ3 amended on the researcher's own correction (a NAME in both homes is the contradiction); the malformed item CARRIED beside its mark. ⇒ `C-6d` SPLIT: (α) the quantities as a NOT_FROZEN module + witness — the SECOND IMPLEMENTATION — chartered to the coder, last in its queue; (β) the register's surface waits on three designer riders (the offering at SCALE; the empty core's (a) inviting τ; the tie sentence printing orbits). The researcher's line came back WITHDRAWING "full-size" (§99.4): an OFFER is a maximal conflict-free candidate whose EVERY pair is SUPPORTED — size follows from support; (α)'s clause 2 replaced; the scale number sent to the designer died with the definition — the count of DISTINCT OFFERS is the researcher's one open number.**~~ ~~⇒ **`C-6d` (the register's BUILD) is GATED on the researcher's PIN — candidate · support/weight · exposure · unrecorded · `|Aut|` · the empty core DEFINED ON THE TYPE with NUMBERS on the landed fixtures by an instrument (file:line), vacuity-checked — because a seal carries its definition's own falsifier and none of the register's quantities is yet a definition on the frozen type.**~~ Then `C-5`, the face.**
✅ **§92–§96 (09-17 → 09-19): ITEM (0) of 6b DONE — the Ambo tells the truth about its gestures and states them in its own line (`6b378ab`); the line's scope rule (*a label that exhausts its act needs no clause; one that leaves its grain or refusals unsaid does*) pre-decides `load cast…` INTO the line. ★ THE `J` SITING RULED before surface 3: `J` is GIVEN in the layer's own edge register — `Layer 3 Witness` generalised WITNESS → GIVEN (mounted before the composition, `Panels.tsx:923`) — never in the composition's edge row (3.42 screens down, last, farther with width — queued as the LIFT's route). The record placement (`identification?` on the Edge type) is where the datum lives; the register is where a person gives it. ⇒ SURFACE 1 (the corner takes a cast) is the designer's now, unblocked on every side; surface 2 the card; surface 3 in the register.**
⛔ **Δ78 (2026-09-17 10:20): THE SEMANTIC LAYER IS THE FRONT AGAIN — the camera lane re-parked by Arman's no-drift clause (§91). Step 6b (`C-6SURF`) was chartered 09-10 and NOT BEGUN while the keyboard was built; it is the designer's ONLY item now. Order from here: 6b (her three surfaces) → 6c (coder: the loader + the surfaces she rules) → `C-5` (the face: follow a loop, read `Fix · Mov · Und` at the triangle, empty-core guarded) → the lift carries it → the walk carries it (the two readings re-derived for partial symmetries; the 0030 §2 rider owed by the researcher then; T³ vs SW the free control — now walkable by keyboard).**
✅ **SANCTIONED (a) — Arman, Δ75, `2026-09-10 14:11`.** ✅ **6a LANDED `7bd9cbc` (§82) — the TYPE alone; the re-seal reproduces at the mothership's hand from raw bytes with its positive control; the witness `fa2d0c1` is the compiler, with a LAW-24 negative that really fails. THE CONNECTION LAYER HAS ITS FIRST BYTES IN THE ENGINE — exactly `ADR 0031` §5 and nothing more; nothing reads or writes the fields yet.** ⇒ ~~**6a `C-6TYPE` (coder) LIVE: the TYPE alone in its frozen commit**~~ · **6b `C-6SURF` (designer) LIVE, concurrent: the gesture and where it is STATED · the card's reading · the edge's `J` choice (`C-3EDGE` resumed)** · **6c (coder, after 6a+6b): the loader against MOLD v4 §5 with §7's five promises as five refusals, first input a hand transcription of the T cell; the surface she rules.** ⛔ *6a carries no surface, loader, producer, or written `J`; 6c carries no design decision of its own.***
> ⛔ **NO LAYER BUILD BEFORE (5).** ~~C-4 the edge now~~ (withdrawn, §75). *The two-cast amalgam stays a computed fact in the researcher's instruments until the theorem says what an edge IS in the engine.* ★ His own sense of the ground: *"getting closer to intuitionistic type theory almost."*
>
> <details><summary>the four questions as they stood while blocked (superseded)</summary>
>
> **(was:) STEP 1 IS BLOCKED ON FOUR DEFINITIONAL QUESTIONS, in blocking order:**
> **(a) WHAT POPULATES A CONCEPT-SPACE?** *Sky is a space — of what?* Four names, four empty spaces; a function has nothing to carry until they have points. **BLOCKS EVERYTHING — with Arman now.** (Candidates, none ruled: hand-given instances · the packet workbench read as points · dissection's born concepts read as instances.)
> **(b) WHAT KIND OF SPACE?** Bare set ⇒ passages permute instances ⇒ cycle-type residues — *the C-2AMBO poverty returns one level down*. Linear ⇒ spectra. **P0 relocates the poverty question; it does not escape it.**
> **(c) ARE INSTANCES NAMED?** Named ⇒ no gauge ⇒ all six passages content. Anonymous ⇒ gauge ⇒ `6 = 3+3`.
> **(d) WHAT IS GIVEN PER PASSAGE?** A function between populated spaces — answerable only after (a)–(c).
> ⚠ **THE SECOND SOVEREIGN FORK, open beside (a): ARE PASSAGES INVERTIBLE?** INVERTIBLE ⇒ the standard gauge-invariant bundle (holonomy fully live; the boundary stone STANDS; no new types — born concepts are new INSTANCES). NON-INVERTIBLE ⇒ born concepts can be NEW TYPES (pushouts) and the stone must be RE-OPENED (a non-invertible passage is by ruling a pooling). **The researcher recommends INVERTIBLE; the word is Arman's.**
> ⚠ **Δ52's born-midpoint reading RETURNS as an open question** (no interpolant remains to replace the placeholder).
> </details>

<details><summary>the retired coordinate-picture box, kept for the record</summary>

> ### ⇒ **THE FIBER IS SETTLED (Δ70, and the frame reaches the surface): ONE FORM, THREE REGIMES — permutation (the degenerate case, what remains when the fiber is stripped to names) · rotation (norm-preserving) · GRADED (real positive eigenvalues — Arman's transcendence series).** The classification unifies in the SPECTRUM: each eigenvalue `λ = r·e^{iθ}` carries an ANGLE (the metric cone's coordinate) and a GAIN (his degree). ⇒ ⛔ **AN EDGE'S SLOT HOLDS A TRANSPORT — the first build merely DISPLAYS the degenerate regime. Drawn as *"assign a permutation"* it gets rebuilt when degrees arrive; drawn as *"assign a transport, shown here as a relabeling"* it GENERALISES.**
> ⛔ **And on an ABELIAN fiber the six edge-values MUST BE GIVEN INDEPENDENTLY** — derivation from endpoint coordinates telescopes to zero and makes the first acceptance vacuous. ⛔ **The quality space is ITS OWN space and MUST NOT be the drawing's** (keying meaning to positions is the trap family's next row — and, combined with derivation, it IS the telescoping from the other side).
</details>

## STEP 2 — THE LIFT CARRIES IT *(the doctrine's own clause, tested)*
**What a person does:** lifts a region they have given transports to, and finds the transports still there on the lifted form.
**The acceptance:** ***what the substrate holds, the lift CARRIES — and where it cannot, it MARKS*** (the lift/identity/grain law at the connection's grain). **Subdivision composes; a born cell's transport is a TRUE ABSENCE — the person's to give, never minted.**
⚠ **This step is 0028's anomaly-matching acceptance made concrete; its falsifier: a surviving loop whose residue changed with no act.**
✅ **GROUND (§125.4, 2026-09-23 — the researcher's `lift_ground.py`, run at the mothership's hand): THE LIFT CARRIES — neither COLLAPSE nor REFINE.** The lifted vertex stays one 0-cell and carries its concept-space whole (0030 §1.1: *a vertex CARRIES a concept; it is not one*). **Collapse erases** — on the first face 33 distinct residues become 1 and T's 6 refusals 0; kept with its record, T contradicts itself and is refused as a cast. **Refine pools** — roles as 0-cells turn every `J` into a gluing of the base (0030 §0's opposite; the op-set boundary). `CONTEXT.md`'s table keeps its base rows and gains FIBRE rows — structure the researcher's (the space carried · a role as its point · a known tuple · an edge's partial isomorphism · a loop's residue), **reading words Arman's (the wall)**; ⚠ **one reading only he can license: a lifted vertex as ONE proposition, its roles as that proposition's PARTS.** ⇒ **OWED before any lift build: DESCENT at the concept layer** — do the lifted edges' `J`s DETERMINE a door's transport? (0028 §1.4: descends iff equivariant; where it fails, the obstruction is CONTENT).

## STEP 3 — THE WALK CARRIES IT *(the (a) end, on the surface Station B built)*
**What a person does:** glues a room from lifted, transport-bearing material and walks it carrying a concept.

> ### ✅ **§72's AMENDMENT REVERSED (2026-09-05 19:24 +0330, §73): the "abelian ⇒ route-disagreement is EMPTY" finding was a fact about the RETIRED diagonal model. Under P0 with invertible passages the round trip is an AUTOMORPHISM of sky-space and route-disagreement is a COMPARISON in mortals-space of two arrivals — LIVE, and the researcher calls the acceptance *"intact and cleaner."* ⇒ Step 3's acceptance returns to 0030 §2's two readings, re-derived for automorphisms when (a)–(d) close.** ⚠ *My own law, taken (§73): a computed fact about a MODEL inherits the model's provisionality — an amendment to an acceptance on such a fact must carry the model's name and die with it. §72's did not name it loudly enough; kept below for the record.*
>
> <details><summary>§72's amendment as written (superseded)</summary>
>
> **(was:) ACCEPTANCE AMENDED (2026-09-04, the sixth, on the researcher's computed finding):**
> **Arman ruled the fiber SIX DISTINCT (Δ70) ⇒ each passage moves its own quality and leaves the other five untouched ⇒ THE CONNECTION IS DIAGONAL, HENCE ABELIAN.** ✔ Computed: `a·b·a⁻¹·b⁻¹` returns the cargo EXACTLY unchanged (`max|K−I| = 0.00e+00`); the transport depends only on the TALLY. ⇒ ⛔ **`ADR 0030` §2's SECOND reading — cancelled-and-NOT-HOME ⇒ route-disagreement — HAS NO INSTANCES on this fiber. It is not wrong; it is EMPTY.**
> ⇒ ***An acceptance written on an empty reading is unfalsifiable in the OTHER direction: the cargo comes home always, and a person could not tell a correct build from a broken one.***

⭑ **§125.4 (2026-09-23) — THE 0030 §2 RIDER UNDER PARTIAL SYMMETRIES, ratified:** the cargo arrives THREE ways (`Fix · Mov · Und`); **at home `Mov` is never a spur** (0 of 779 null words moved), **`Und` is the loop's residue OR a spur** — told apart by the REDUCED word; **not home, PRESENCE is route-dependent — even on a DIAGONAL connection** (1,175 of 4,000; the total control 0), so the reading 0030 called empty on a diagonal is live here. ⇒ **when Step 3 charters, its acceptance gains the PRESENCE reading and its surface must tell a spur from a residue.** *(Recorded now, charted then.)*
**THE ACCEPTANCE AS IT NOW STANDS — the two things Step 3 can honestly show:**
1. ✅ **CURVATURE (0030 §2's FIRST reading), which is live and non-vacuous:** cancelled-and-HOME with a CHANGED cargo — the walker went round an edge and came back holding something different. **This is the semantic cone at the person's feet and it is what the diagonal fiber buys.**
2. ✅ **THE CONTRAST, and it is worth having as an acceptance in its own right:** the deck group is non-abelian (geometry) while the connection is abelian (meaning) ⇒ ***"the room moved you; your ideas came back intact."*** **A true, readable sentence about a real difference between the two structures — written as ITSELF, never as a failed route-disagreement.**
⚠ **The control is free and structural, as ever: T³ against Seifert–Weber.**
⇒ ✔ **THE ROUTE-DISAGREEMENT READING IS NOT LOST — IT IS PRICED: one MIXING passage (a transport touching two qualities) makes the cargo route-dependent at once** (computed: `max|K−I| = 0.0518`). **Diagonal buys the CONE; mixing buys the COMMUTATOR.** ⛔ **Whether he ever gives a mixing passage is ARMAN'S QUESTION, and it is due when Step 3 charters — not now.**
> </details>

⇒ ★ **Also placed (the researcher, 1616): even on the OLD picture a NON-ABELIAN connection existed on the bare K₄ from ONE declared rule (the S₄ transposition acting on edge-labels — holonomy round S·E·M = `(E M)`), so "mixing is a later gift" was already over-priced. Under P0 the question is moot in that form and returns inside (d).**

---

## THE STANDING RULES OF THIS PLAN
- ⛔⛔ **EVERY ACCEPTANCE IN THIS LAYER IS CHECKED FOR ARITHMETIC VACUITY BEFORE IT CHARTERS.** *(Bought twice in one hour, 2026-09-04: a DERIVED abelian transport telescopes — every triangle reads "nothing moved" as a certainty, not a measurement (step 1's acceptance) · a DIAGONAL connection commutes — the cargo comes home always (step 3's).)* ⇒ ***In this layer the ARITHMETIC can empty an acceptance in a way the PROSE cannot show: a test that cannot fail reads exactly like a test that passes.*** **Name the fiber, then ask what the algebra forces, then write the sentence.**
- ⛔⛔ **A RATIO IS NEVER A REASON TO BUILD** *(bought §75: five days at zero commits frightened the mothership into chartering a port with no person; Arman caught it in one line).* ⇒ **A definitional phase declared by the sovereign has the RECORD as its substrate — theorems and rulings — and its silence on commits is the phase, not a failure. The record-to-substrate ratio is read against the DECLARED phase, never against a calendar.**
- ⛔⛔ **A COMPUTED FACT ABOUT A MODEL INHERITS THE MODEL'S PROVISIONALITY.** *(Bought at §72→§73: "the connection is abelian" was exact — for the diagonal model, which was ONE READING of Arman's gift; the acceptance rewritten on it had to be reversed a day later when he ruled P0.)* ⇒ **An acceptance amended on a model-fact CARRIES THE MODEL'S NAME in its first line and is written to RETIRE with it.**
- ⛔ **Each step's acceptance is a PERSON'S sentence, and each step is usable alone.** A step that only makes the next step possible has not been built.
- ⛔ **Nothing here re-scopes the op-set, and nothing here writes the GIVEN register.**
- ⚠ **The fiber's generality stays open** (the trivial and `±1` cases exist today — instance-zero is `connectionWaveInstrumentV0`, ADR 0013). The compass document's S₄-on-four-moments is the natural first rich case; **it is not chartered by this plan.**
- **Kill-condition:** this plan dies by Arman's word, or when step 3's acceptance is met at his own eye.
