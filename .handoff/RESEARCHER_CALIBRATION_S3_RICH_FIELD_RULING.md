# RESEARCHER RULING — the rich field the render must carry (S3 / the seating)

**To:** Mothership (ratify by re-derivation) + Engineer (the sampler re-charter) · **From:** Researcher — candidate, session `e7fa7f71` · **Date:** 2026-07-01 · **Self-contained.**
**Rests on:** `.handoff/RESEARCHER_FIELD_LAYER_MAP.md` (the three field vintages). **Answers:** `.handoff/RELAY_ENGINEER_TO_RESEARCHER_RICH_FIELD_DEFINITION.md`.

## §0 The ruling in one breath
The render's rich field is built on **layer 3 only** — the live spectral column (`L_U` / `SF` / `ψ`) **plus the committed topological loci** (`H₁` loops, `Σ=PD(φ)`, the `w₁` disclination). Layer 1 (scalar interference — moratorium) and layer 2 (octonionic carrier — observable field *not possessed*) are stale/killed and **not wired to the render; never sample them.** The charter's **"routes/gates" are layer-1 vocabulary** — not render-law. And the honest crux: the render looks minimal partly because it samples only `n=R(α)·n₀`, **and partly because the form it renders is minimal** — the witness form has `b₁=1` (one loop, one disclination). The one clean law-level per-point enrichment is the **`L_U` eigenmodes** (needs a small eigenvector-emission derivation); a *plurality* of features (many loops/vortices, `SF` flowing) is a **richer-form** question, not a denser sampler.

## §1 What the rich field IS (each feature: committed origin · spatial type · status)
| feature | committed origin | spatial type | status |
|---|---|---|---|
| director line `n(p)` | `directorFieldV0` (`n=R(α)·n₀`) | per-point line (headless) | **LIVE — sampled now** |
| `w₁` **disclination** (½ half-vortex) | `Σ=PD(φ)` / holonomy (`windingSign`) | **line defect**: core axis, `Σ`-cut locus, strength ½ | **LIVE** (sampled as `α`; core+cut can be foregrounded) |
| essential **`H₁` loop(s)** | `globalW1.basisCycles` | embedded cycle(s) | **LIVE** — but `b₁=1` on this form (ONE loop) |
| connection `U_e` | `witnessBridge.edgeSigns` | per-edge sign on the `X_K` cycle | **LIVE** (the one −1 seam edge) |
| `L_U` **spectrum** | `connectionWaveInstrumentV0.spectrumReadout` | **global scalars** (not per-point) | **LIVE readout**; per-point form = eigenmodes → **NEEDS DERIVATION** |
| second arrow `SF` | `spectralFlowV0` | **per-BIRTH integer** (genealogy-indexed) | **LIVE** but not per-point (needs a birth-path) |
| connection-wave `ψ` | `connectionWaveInstrumentV0.evolveProbe` | per-site complex field → per-point | **INSTRUMENT** (declared knobs; audit deferred, ADR 0013) — render only if labelled |

## §2 What the render should sample per point `p` (the spec — all derive-only from the committed bridge)
Today: `n(p)=R(α)·n₀` — one line. To *be* the rich field on layer 3, expose per `p`:
1. **`n(p)`** — the connection-modulated director line (keep).
2. **`α(p)` + disclination proximity** — the twist magnitude + distance to the core line (`ConnectionSample.alpha`; committed).
3. **loop / `Σ`-cut proximity** — whether `p` lies on/near the committed `H₁` loop or the `Σ` cut (committed loci) — foregrounds the essential loop and the disclination cut as the phenomenologically real structure.
4. **`topGap(p) = λ₁−λ₂`** of the interpolated Q (`directorAt`; committed) — render as **order/coherence**, NOT a feature: its zeros on `n₀` are interpolation artifacts ("geometric junk"), not derived defects. Label honestly.
5. **(needs derivation) low `L_U` eigenmode amplitude `ψ_k(p)`** — interpolated per-site eigenvector; the genuine spatial spectral texture (nodal structure). Requires eigenvector emission first (see §3).

## §3 Derivable NOW vs needs-derivation vs instrument vs richer-form vs stale
- **NOW (committed, law-level, derive-only):** director line; disclination (½ + core + `Σ`-cut + the linked `H₁` loop); connection `U_e`; spectrum as a readout; `topGap` as order. Honest caveat: this is **one loop + one disclination** — the minimal form's whole content.
- **NEEDS DERIVATION (law-level, small ADDITIVE step):** the **`L_U` eigenmode emission** — the committed N×N Jacobi (`symmetricEigenvalues`) returns eigen*values only*; emit the eigen*vectors* (accumulate `V`), exactly the additive discipline that built `SF`. This is the one clean path to a rich **per-point volumetric spectral field**. Seal-before-build applies (seal the expected eigen-structure on a chosen test form before the blind build).
- **INSTRUMENT (committed, declared-only):** `ψ` connection-wave. A labelled overlay at most; never law; its cross-office audit is still deferred (ADR 0013).
- **RICHER FORM needed (NOT a sampler change):** many loops / many disclinations / `SF` actually *flowing* require rendering a **richer form or a birth-path genealogy** (multi-cell / the ADR-0012 E2 seam), not a denser sampler on the single-loop witness form. This is the deepest honest answer to "why minimal."
- **STALE / KILLED — never render:** "routes/gates/regions" (layer 1, moratorium, decorative), the octonionic carrier/emission field (layer 2, observable field *not possessed*). Rendering either = reviving a killed object.

## §4 Canonical example — SEALED, derived from the committed engine (reproduce to ratify)
Render form via `buildKnownSeamRenderState()` + the frustrated `X_K` `L_U`:
- `perCycleW1 = [1]`; `windingSign = −1`; `[Σ] = [1]`; `sigmaChainEdges = ["RM:0:0","RM:0:1"]`; `U_e = [1,1,1,−1,1,1]`; **`b₁ = 1`** (committed `diagnose-s4-frame-witness` §5.1).
- 6 site directors: `ab +x`, `ac +y`, `ad +z`, `bd −y`, `cd −x`, `bc −z`; seam = `bd–cd`; **core axis `n_bd × n_cd = [0,0,−1] = −ẑ`**.
- `L_U` spectrum `{2−√3, 2−√3, 2, 2, 2+√3, 2+√3}`; **min band `λ = 2−√3`, multiplicity 2**.
- **The pinned rich feature (the eigenmode):** min-band per-site `|ψ|² = [⅓,⅓,⅓,⅓,⅓,⅓]` — **UNIFORM.** → On the minimal symmetric form the spectral eigenmode carries **no spatial loci**; a rich per-point spectral field requires a **richer/irregular form**, not a denser sampler here. (This is the derived, reproducible number the ruling turns on.)
Reproduce: `buildKnownSeamRenderState()` for the invariants; `signedLaplacian(cycleGraph(6), [1,1,1,−1,1,1])` eigen-decomposed for the spectrum + uniform min-band intensity.

## §5 Routing (what happens on this ruling)
- **Engineer:** re-charter the sampler (`directorFieldV0` / the bridge exposure) to carry the committed layer-3 enrichment (§2 items 1–4, derive-only); and — on a **derivation authorization** — build the `L_U` **eigenvector emission** (additive, seal-before-build) so item 5 becomes real. Never wire layers 1/2.
- **Mothership:** foreground which of these derived features to render (ADR 0017 phenomenology — felt, not marked); own the "richer-form" question (§3) — the plurality the sovereign wants (routes/gates/many vortices) is a form/genealogy call, and "routes/gates" as named are stale layer-1 vocabulary to retire or convert to a where-test (ADR 0014).
- **Nothing un-derived reaches the screen.** The disclination locus is a free representative (ADR 0017 Am.1); center it for legibility, never pin correctness to it.

## §6 Cadence
S3 filed. Mothership ratifies by re-derivation (§4 reproduces exactly). This completes S0–S3.
