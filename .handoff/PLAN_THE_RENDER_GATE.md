# THE RENDER GATE — SUB-PLAN

**Held by: mothership. REGENERATED (rewrite, not append) 2026-08-20 by the FIFTH at HEAD `1e09f36`** — the prior copy (08-12, `834826f`) predated the multi-cell wall coming down, the cone arc's acceptance, the whole D1–D16 legibility detour, and the Sovereign's 08-20 ruling that moved the front. Its full text lives in this file's git history. ✔ marks = verified at or since `5bac4f5` by my own hand or a named seat's; ⚠ = inferred, not run.

## ★★ THE FRAME (ratified with Arman 2026-08-07; unchanged)
The render gate is the **EXPLORE-WINDOW feature**. The class-body SHELL stays the operable handle (it need not be faithful); **faithfulness lives in the WINDOW** — walk-inside for 3-manifolds, deck-tiling for surfaces. Rungs: **1** the E³ window · **2** the surface deck-tiling · **3** the curved 3-manifold transports · **4** the arbitrary-symmetry orbit. **Build doctrine:** fat autonomous builds (TARGET · INVARIANTS · ACCEPTANCE; one app-path check; no per-step ratification). **Standing render laws:** LAW 23 countable captions · the horizon doctrine (address, never draw the lie) · every witness exercises the APP PATH · the five gating laws (`docs/design/CONTEXT.md:65`).

## ★ THE TILING CANON (Arman VERBATIM, 2026-08-12; unchanged, governs rung 2)
*"the tiling standard is a canon… because the tiling method is a proven technic for hyperbolic ones, first we should observe it for our own hyperbolic rendering, and derive from it a method for the others, and in the case no such method was found, we should either approximate a tiling convention or move to another standardized representation."*
⇒ Rung 2's METHOD validates **HYPERBOLIC FIRST**; spherical/euclidean are **DERIVED** from that observation; the **fallback clause** is on record. "Spherical-first" is superseded as sequencing.

## ⛔ THE GATE'S OWN LAW — **THE GATE = RUNGS × THE CARRY CHAIN**
Every defect of this arc has been a broken **arrow**, not bad math: positions → owned corner angle → dihedral → cone angle → verification → the person's pairing → the deck → the walk's transport → the caption's word → the person's eye. ⇒ **No rung closes on a headless green. Acceptance is a person-driven traversal by a seat that did not build it. A witness must DECLARE which arrows it supplies.** (`.handoff/THE_CARRY_CHAIN_the-shape-of-the-render-gate.md`.)

---

# THE LIVE STATE — REGENERATED at `665bc71`, **2026-08-22** (clock-read, not stamp-derived) · **AMENDED at `dcc68a0`, 2026-08-23 (clock-read) by the SIXTH mothership** — seated and ratified this day (sovereign ledger S9); ranked list updated below at the ratification of `B-2026-08-26-A` + the dispatch of `B-101`
*(on Arman's order: **"state where we are in our plans and update them."** This file's CONTENT stood at `1e09f36` while the arc moved on.)*

> ### ⛔ **DATE RETRACTION — read this before citing anything below.**
> **My first version of this section was dated `2026-08-26` and priced every gap in days. Those dates were FABRICATED: I took "today" from the BUILD STAMP (`B-2026-08-26-A`) instead of reading a clock.** **The clock says `2026-08-22`; every commit in this arc is authored `2026-08-22`.**
> ⛔ **`CLAUDE.md` carries the exact guard I broke: *"⚠ Cite SHAs, never typed dates."***
> **WITHDRAWN:** *"Rung 2 has been free for five days"* (**one day** — the walk closed 08-21) · *"the ledger sat 11 days stale"* (**7**) · *"08-22→08-26 was spent on the detour"* (**there was no such window; three complete build cycles landed TODAY**). ⇒ ***I invented a tempo failure. The structural findings below stand; the durations and the drift verdict do not.***

## ⛔ **RUNG 2 IS UNBLOCKED AND UNOPENED**
**This file says: *"RUNG 2 — the surface deck-tiling: HELD behind the walk."*** ⇒ **THE WALK CLOSED 2026-08-21 AT ARMAN'S OWN HAND — yesterday. The hold condition has expired and nothing has opened it.** **That is the single largest fact this regeneration recovers, and it is the answer to *"where are we."*** ⚠ **One day is not drift; it is a normal lag. What would MAKE it drift is nobody writing the re-aim down** — which is, verbatim, the failure this file already records from the August detour.
> ★★ **THE GUARD, event-based not calendar-based: this file and `THE_SOVEREIGN_LEDGER.md` are re-read AT EVERY FRONT CHANGE and at every ratification that closes a plan item.** ★ **The mechanism it defends against: *a defect report always outranks a plan item in felt urgency — it has a victim and a next action; a plan item has neither.***

## ✅ CLOSED SINCE THE LAST REGENERATION — items this file still lists as OPEN or PARKED
| item | this file said | truth at `665bc71` |
|---|---|---|
| **(c) THE WALK** | the front | ✅ **CLOSED 08-21, his own hand** — *"i did walk it. all is good."* |
| **PERSISTENCE + #37** | ⛔ IN FLIGHT, blocked on #37 | ✅ **BOTH LANDED** (store + versioned file, double-hop; #37's cross-hop re-rooting) — **and he aimed it himself: *"my decision is persistence. proceed."*** |
| **D16** | PARKED | ✅ **LANDED** — leaders read `AB₀ · AB₁ · AD₀ · AD₁`; `·A`/`·B` nowhere |
| **THE SCREEN CENSUS** | PARKED, coupled to D16 | ✅ **RAN**, SHA-pinned, ranked, exclusions declared |
| **THE FACE REGISTER** | PARKED | ✅ **LANDED on BOTH surfaces** — Ambo CELL FACES lead `AC·CD·BC`; the card gained `◻ … · n corners`. **The 08-19 finding is closed.** |
| **the born-name / catalogue question** | PARKED | ✅ **RULED + LANDED** — `primitiveCatalogue:39` stopped; **the four naming cases complete: CARRIED · COMPOSED · GIVEN · BORN(=`unnamed`)** |
| **the terminal cut · the frozen unions** | PARKED / sanctioned | ✅ **spent: `multiform`, `worldModel`, `primitiveCatalogue`, `specimenModel`** (each edit + re-seal in one commit, positive control reproduced) |
| **`depthWeightRatio 9` never re-gated on ECHO** | ⚠ open, hers | ✅ **RE-GATED and STANDS** (designer; instrument validated first) |
| **the card union · R1 leave-bounded · R2 · R7 · R8 · R9** | *(post-dates this file)* | ✅ all landed |

## ⛔ OPEN, AND RANKED — the true state of the board *(amended at `dcc68a0`)*
0. ✅✅ **RUNG 2 — THE SURFACE DECK-TILING: CLOSED AT ARMAN'S OWN HAND, 2026-08-25 (Δ19).** His words entire: ***"walk is done, all seems well."*** ⚠ **His hedge preserved, not upgraded** (rung 1 read *"all is good"*); **no per-window observations were reported, so nothing is recorded about what he saw beyond that sentence — do not cite it as a clause-by-clause verification.** ★ **How it closed is the record worth keeping: the FIRST walk rejected it, and that rejection bought ring-and-count's death, the fold's move onto the drawing, and the doorless wall made mechanical. A rung that closes on the second walk closed honestly.**
⇒ ✅✅ **Δ20's SEQUENCE IS TWO-THIRDS SPENT AND HE HAS RE-AIMED THE TAIL (Δ22).** ✅ **R1 CLOSED** (`74be04c`/`026d499` — the icosahedron appears). ✅ **B.4's FIRST BUILD LANDED** (`B-109`/`7878bcf` — checker + realizer + Seifert-Weber, Poincaré, both lenses, certified by the committed tower; euclidean control fails, curved passes at 0.0e+0 rad). ⛔ **BUT FROM B.4 A PERSON SEES NOTHING** — `apertureModel:26-27`'s refusal still stands and nothing routes to the transport. ⇒ **Arman, Δ22 verbatim: *"1) yes i want it next, 2) pay the price"*** — **(1) ROUTE THE APERTURE THROUGH THE TRANSPORT, ahead of P5 · (2) the word-materialized union PAID (one frozen file, `snapshot.ts`).** ★ **He bought the ROUTE the moment existence was sealed — *the mark is not the deliverable, the route is*, ruled without being told.** ⛔ **The routing's CHARTER waits on ONE researcher question: 0026 DEFERRED the inhabited curved interior with a trigger, and the trigger has fired — a deferral's trigger firing is a licence to ASK what was deferred, never to build from the deferral note.** **THE ORDER NOW: `B-110` (the camera — P5's ratified prerequisite) → the routing (`B-111`, on the researcher's answer) → P5 + UNDO.**
> ### ⇒ ✅✅ **Δ20's SEQUENCE IS SPENT AND P5 IS THE FRONT — CHARTERED `B-116` at `9581aa1`.** **R1 ✅ · B.4's build ✅ · the ROUTING ✅ landed whole (`B-111`…`B-115`: the transport carries a model · the plate and the walk window read ONE geometry, plane agreement `0.00e+0` over 12 faces · the crowding READS in the window · the label STAYS · the plate's route ANSWERED — the old fly was `startsWith('w:')`, written forms only, and never framed a plate).** ⇒ **P5's two prerequisites are both closed and its spec is whole on both sides** (the designer's form · the researcher's meaning · his answer to her §5 and M.6). ⛔ **RUNG 3 remains behind B.4's `§8` hand-off; the HORIZON layer is still the declared LAST layer and still unaimed.**
> ### ⛔⛔ **AND THE FRONT MOVED WITHOUT PASSING THIS DESK — Arman routed TWO ASKS DIRECTLY TO THE DESIGNER while the coder was building, and she has specced both.** *(He is the router; this is not a breach. It is a note that my board was stale about the front for a cycle, and I found out by reading her letter rather than by asking.)* ⇒ **THE NEW ITEMS, both specced, NEITHER buildable yet:**
> ### ✅✅ **THE DRAG IS CHARTERED — Δ24, *"build the darg!"*** (`MARKER D1`). ⛔ **BEHIND `B-119` §5.2, not ahead of it: the record must stop aliasing live state BEFORE the first thing that rewrites `home` exists.** **ORDER: the roof → the copy → the drag.** ⚠ **Arman's walk of P5 remains owed; he routed the build ahead of it and that is his call to make.**
> - **DRAGGING — the person's hand on `home`.** ✔ **Nothing new is stored, drawn or saved: `WrittenPageEntry.home` exists, the stemma is drawn BETWEEN homes, and `home` is serialized. WHAT IS MISSING IS A GESTURE** — *the mark is not the deliverable, the route is*, in its purest instance yet. ★★★ **D.6 is why it is not a convenience feature: the stemma follows the homes, so ARRANGING THE PAGE IS ARRANGING THE ARGUMENT.** ✅ **D.7 answered by the researcher's Q5 — moving is NOT an act; undo skips it.** ⛔ **BLOCKED on a JOIN DEFECT the sixth caught between her own two specs: CAN HE DRAG A MEMORIAL?** (a memorial is a stemma ENDPOINT per her §B.3; homes are draggable per her §A; neither clause ranges over the other.) **Coder measuring whether a memorial's site is the same `home` field.**
> ### ⛔⛔⛔ **FRONT RE-AIMED AGAIN — AND THE TARGET IS NOT THE BAND AND NOT THE STEMMA. IT IS *WHAT IS AN INSTANCE AND WHAT IS A FORM*.**
> **The designer counted rather than argued: `document.body.innerText` ⇒ `Square — invoked` × 12 — SIX in the stemma, SIX in the record, one screen.** ⇒ ⛔ ***"The stemma does not draw one square with six edges. It draws six squares with one edge each."*** **BOTH registers duplicate the parent; retiring the band takes twelve to six and changes nothing about branching.** ★★★ ***A cure that inherits the disease it was called for*** — **and she ran that count on the argument she was about to win.**
> ⇒ **THE REAL QUESTION, composed from two seats' halves:** **CHILD end (coder) — two glue→Torus births give TWO page bodies, ONE record line, ONE DAG edge, the SAME shape id, obeying ADR 0027 §2. PARENT end (designer) — six page bodies, SIX record nodes.** ⚠ **Hypothesis (the sixth's, UNVERIFIED): `stemmaLines` draws from `homeOfShapeId.get(edge.parent)`, keyed by shape id ⇒ six diamonds means six ids ⇒ SIX FORMS, and invocation is individuated by ACT while everything else is by RESULT.** ⇒ ⛔ **CHARTERED: coder MEASURES (`B-121` §2) · researcher RULES (does §2's grain extend to invocation) · designer DRAWS once ruled — she refused to spec before the measurement and was right.**
> ⇒ ★★★ **WHY IT IS THE FRONT AND NOT A DEFECT: *"There are SIX squares on his screen. Which one is THE square?"*** **Arman's model is *from 1 square all its unique children* — liveness, exhaustion and revival are properties of ONE parent, and the page cannot point at one.** ⛔ **THE BAND STAYS until this is settled.**
> ### ⛔⛔ **(SUPERSEDED, KEPT FOR THE RECORD) FRONT RE-ORDERED LAST PASS: THE STEMMA IS A PREREQUISITE OF PARENT-LIFE, NOT A PARALLEL ITEM.** **The designer drove the band on a square with six children and it printed `Square — invoked` SIX TIMES — one parent, six children, as six unrelated births.** ⇒ ***A DAG flattened into a sentence loses its branching, and branching is the whole of a genealogy.*** ⛔ **Her consequence: *a form that cannot express one-parent-many-children cannot show a parent's remaining life, cannot show exhaustion, and cannot show a revival.*** ⇒ **Arman's death-as-exhaustion model has no surface that can carry its reading until the stemma does.** ★ **Same shape as the camera being P5's prerequisite.**
> - **THE STEMMA / THE BAND.** ⛔ **TWO GENEALOGIES EXIST — the drawn STEMMA and the band's running sentence: one fact, two renderings, and only the drawn one is form-specific, which is Arman's complaint.** ✅ **Cure: put the OPERATION on the edge, retire the band.** ⚠ **PRICE UNVERIFIED — she flagged that she has not checked whether `genesis.reducedEdges` carries the op; the coder is measuring before anyone prices it.** ★★★ **AND THE FINDING OF THE DAY, hers: retiring the band would DROP the lineage of any child whose parent has left — so THE MEMORIAL IS THE ENDPOINT that keeps the stemma drawable.** ⇒ **P5's memorial is not only *what left*; it is what keeps the genealogy DRAWABLE, and M.6 is now load-bearing twice.**
> - **PARENT LIFE / DEATH-AS-EXHAUSTION** — ✅ **RULED durable by the researcher (two registers; liveness DERIVED never stored; three senses; moving is not an act) and ADR CHARTERED this pass.** **Her design half (L.1–L.5) assumes his model: the affordance line IS the liveness display · two kinds of grey must not look alike · REVIVAL must be marked · two deaths, two words, two marks.**
> ⚠ **TWO THINGS SIT WITH THE DESIGNER, and they are ONE REGION OF SCREEN:** the plate's legibility *(one number pending — the threshold sweep at the SUMMONED size, `fracH 0.455`, since the coder's composition used only their table's default-camera row)* **and** the label's new placement over that same plate, surfaced by R2's own cure. **Ruled in sequence they fight; ruled together they are one question about what that patch is for.**
*(the aim that produced this, kept:)* ✅✅ **THE FRONT WAS AIMED (Δ20, verbatim): *"the front is R1 and then b.4 (rung 3 behind it) and the p5 (with 'undo' added as well, not just delete)."*** ⇒ **THE SEQUENCE: (1) R1 → (2) B.4 with RUNG 3 behind it → (3) P5 + UNDO.** ⚠ **He WIDENED P5 himself** — the row read *"remove a form"*; **undo is a second gesture and the larger question** (what removal MEANS where forms beget forms; whether undo erases a trace or is itself a traced act). ⛔ **The horizon layer was not aimed — it remains the declared LAST layer.** **Lanes now: researcher = R1's definitional half (commissioned, parallel) · coder = `B-106` Phase B, then `B-107` = R1's enumeration + build · designer = the reading residue, then P5+UNDO's form ahead of its turn.**
*(the nominee computation that produced this aim, kept as the record:)* ⛔ **THE NOMINEES ARE COMPUTED FROM THIS FILE + `PLAN_THE_LAST_PHASE.md` + `THE_CLAIMS_LEDGER.md`'s live rows — not proposed from memory** *(Arman's own order: "reach into the plans and compute what the real front nominees are." My first fork named two and MISSED the Sovereign's own declared last layer, the person-reach hole, and rung 3's prerequisite — enumerate from the record, never from a constant.)*

> ### ⛔⛔ **THE FIVE NOMINEES BELOW ARE 08-23's AND FOUR ARE SPENT — DISPOSITIONED 2026-08-27 (clock-read) on Arman's question *"where are we regarding our plans?"***
> **1 B.4 ✅ BUILT** (`7878bcf`; ADR 0026; the routing `B-109`…`B-115`) · **2 R1 ✅ CLOSED** (`74be04c`/`026d499`) · **4 P5 ✅ BUILT AND DRIVEN** (`B-116`; ⛔ unwalked by him) · **5 THE READING RESIDUE — still running in her lane** · ⛔ **3 THE HORIZON — THE ONLY UNSPENT NOMINEE, AND IT IS THE DECLARED LAST LAYER.**
> ### ⇒ ★★★ **AND THE FINDING THAT MATTERS MORE THAN THE TALLY: WE HAVE BEEN BUILDING THE HORIZON FOR TWO DAYS WITHOUT ANYONE NAMING IT.**
> **This row's own words are *"the module is GENERATIVE — forms beget forms — and this layer is that made LEGIBLE."*** ⇒ **The stemma · the operation on the edge · P5's memorial · the record/acts two registers · ADR 0027 (death-is-exhaustion) · the drag (*arranging the page IS arranging the argument*) · the band's flattening against the stemma's fan — THAT IS THIS ROW.** ⚠ **It arrived because Arman routed the dragging and band asks directly, and they turned out to BE the horizon.**
> ⛔ **THE COST OF NOT NAMING IT: the row still reads *"needs a DEEP arc, not a surfacing pass"* and *"nothing gates it — but it is the largest"*, while it has been running as a stream of small mandates for two days. A deep arc built as a queue of tickets is the shape this campaign has been warned about.** ⇒ **Not a correction of the work — a correction of the BOARD.**
> ⚠ **AND THE OTHER HALF, from `PLAN_THE_LAST_PHASE.md`'s through-line: its ACTIVE clause is *can they INHABIT it* — rungs 3–4. B.4 (rung 3's declared head) landed; RUNG 3 IS BUILDABLE AND UNCHARTERED. Two fronts are live and only one has been being worked.**

| # | NOMINEE | written where | what it really is | gated by | blast if deferred |
|---|---|---|---|---|---|
| **1** | **B.4 — THE NON-CUBE DOMAIN CONSTRUCTOR** *(rung 3's true head)* | last-phase §1 rungs 3–4 · sweep §CARRIED "ARC B residuals" | ⛔ **RUNG 3 IS NOT A FRONT — IT IS A CHAIN, AND B.4 IS ITS HEAD.** ARC B's own note: **B.2 (H³) PROVABLY NEEDS B.4** — the cube reaches H³ zero times by census; and B.1 is not *"add a spherical transport"* but **RE-REALIZE THE FORM IN S³** — *the engine is EUCLIDEAN and every form it builds is a Euclidean cone-manifold.* **Two independent axes, only one ever named: DOMAIN SHAPE (B.4) vs AMBIENT REALIZATION.** Customers named: the LENS · the DODECAHEDRON | nothing — it is the head | rung 3 and rung 4 both wait; **and the inhabited register (0025 §7.2), the FLIP's route, and B.3's curved deck-fit all sit behind it** |
| **2** | **R1 — THE ICOSAHEDRON** | ranked item 2 · ledger R1/R3/DUAL · §3 gates | the diagonalized cuboctahedron is not a real icosahedron (12 unrelaxed positions; `t=1/φ` named, not sealed) | its own re-derivation ENUMERATION (§3) — the price IS the work | **it gates TWO ledger rows: R3 (the n=5 fan gate — the regular gate would refuse our own demonstration) and DUAL (the 4π seal may only close post-R1). It sits under every pyritohedral thing the window draws** |
| **3** | **THE HORIZON — the semantic / genealogy layer** | last-phase §4 (**Sovereign's own, 2026-07-28: "the LAST layer"**) | *the module is GENERATIVE — forms beget forms — and this layer is that made LEGIBLE.* ⚠ **Needs a DEEP arc, not a surfacing pass.** ★ **Its inventory is already bought** (the naming doctrine · positive presence · the packet's slot · the argument reading) and **Δ18 just sharpened its first question: a BORN entity's name must be something he READS, not a token we print** | nothing — but it is the largest | it is the declared LAST layer; deferring it defers the finish line itself |
| **4** | **P5 — THE PERSON CANNOT REMOVE A FORM** | ledger P5 (LIVE, *"observation, uncartered"*) | no delete gesture exists; right-click gives operations only. **A page he can only add to** | nothing — smallest of the five | every drive accumulates debris; it blocked a mothership drive once already |
| **5** | **THE READING RESIDUE** | ranked 6 · ledger L1/L2/L3 · `B-106` Phase B | the folded body's card (**now UNBLOCKED — the fact it waited on is reconciled**) · the aperture's third reader · the `·` punctuation · the doorless wall's sentence + lifetime | the designer's lanes; **partly IN FLIGHT already** | ⚠ **NOT a front — it is running. Naming it the front would be servicing the inbox and calling it a plan** |
⚠ **Also live and deliberately NOT nominated** (each real, none front-sized): S5 vacuous-green · S7 deficit gate · S8 seal census · P1/P2/P3 person-reach tails · A3's stamp-drift residue · the believed-cured band-vs-twist check · D.2 the beautification pass (his own 07-03 grouped order) · the expansion set.
**In flight meanwhile: `B-106` Phase A + Phase B (gate lifted, marker `V1`).**
*(the state his re-walk closed, kept for the record:)* **RUNG 2 REBUILT TO THE NEW READING — AWAITING ARMAN'S RE-WALK, which is the rung's only kill.** `B-105` RATIFIED at `63216d5`: the windows now NAME and SHOW (captions out; `{p,q}`, the count and the descent check demoted to the card's record rows), **the descent's mark is the chiral inhabitant's doubled image with its LAW-24 control standing at the eye on both halves** (twice on RP², once on the lifted cube, mirror-wound by the math), **the fold's pick is a TAP on the drawn polygon** (hues, ticks-and-runs, P.5's direction; `e0…e5` off his surface) — so the route to the hyperbolic window is aimable and was driven end to end. **The two-seat fact contradiction reconciled: both measured truly; the states differ by ONE double-click, and the doorless wall is now mechanical and named.** ⚠ **Carried, measured, not defects: the FLIP does not read at rest (null control clean) · {6,6} compresses the shrink to ~2 rings · the empty-total sentence is page-unreachable · the hemicube RP² is not person-reachable.** **KILL: his acceptance at his own eye — with every caption covered, he tells the three geometries apart (ADR 0025 §7's re-cut clause).**
*(the prior state, kept — what his first walk rejected:)* ⛔⛔ **RUNG 2 WALKED AND NOT ACCEPTED — ZERO windows accepted (Δ17 + Δ18).** ⛔ **RETRACTED, MINE: this line read *"two of four windows stand at his eye"* on my reading of *"2,3) there"* — he corrected it: *"i just meant i see the design. did not mean that the design is good."* **A presence-report is not a verdict; I wrote one into the record as the other.** ⛔⛔ **AND THE RUNG'S MARK IS WITHDRAWN (Δ18): ring-and-count is dead** — the ring is drawn by us, the count computed by us, and the person performs nothing. **RULED: the mark is WHAT THE CELLS DO** — repeat unchanged (euclidean) · crowd and shrink toward a rim that never arrives (hyperbolic) · close up and come back (spherical); **`{p,q}` and the count are RECORD, never the world's mark.** **The acceptance question follows the person's chair: with the captions covered, can he tell the three apart?** ⚠ **AND THE REGISTER IS OPEN WITH THE DESIGNER — did rung 2 re-buy Amendment 7's scar (`docs/design/adr/0004`: *"the aperture is a PLACE, not a PICTURE"*)? Two of the three phenomena may be MOTION facts a still cannot carry.** ✅ **THE READING IS RE-RULED AND THE DOCTRINE FOLLOWED IT: ADR 0025 §7 (researcher-amended, mothership-ratified) — the mark is the PHENOMENON SHOWN; `{p,q}` and the count DEMOTE TO THE RECORD.** The three still-readable marks: *the same cell forever* · *cells crowd and shrink toward a rim they never reach* · **spherical = FINITE, the tiling RUNS OUT** (⚠ not *"comes back"* — that is motion). **§7.1: the descent's mark is an INHABITANT'S DOUBLED IMAGE** — a CHIRAL figure in one cell, its antipodal image visible in the partner — with the LAW-24 control *(non-descended shows it ONCE)*; the descent sentence and the `−I ∈ Sym` check demote to the record. **§7.2: the METHOD is register-independent — the still suffices for rung 2; the INHABITED experience (*comes back*, *the rim never arrives*) is a LATER RUNG, with rung 1's ratified `back where you started · N doors` banked as its port.** ⇒ **The designer WITHDREW "rung 2 is a PLACE" on reading §7.2; her mixed-register refusal stands stronger (if one is a plate, all three are).** ⛔ **The window may NAME what it is; it may not NARRATE the phenomenon — surviving words are the designer's.** ⛔ **The hyperbolic window is UNCONFIRMED** — the fold that leads there cannot be aimed: **the fold card labels his edges `e0, e1, …`** (F.4, the picker we never swept — the coder's new front, ruling `W1`). ⛔ **The RP² window FAILED at his eye** — *"a circle with the pinch on it marked, with some prose? i don't understand"*: each part true (honest symbol, checked descent, faithful drawing), **the composition unintelligible — there was nothing to COUNT.** ⚠ **The prior question is MINE + the designer's, not a cut:** ADR 0025 §3 works RP² as the HEMICUBE (3 squares, 270° — something to ring); the page's arrives as the dihedron. Letter out; the coder measures the true census + hemicube reachability, rules nothing. ⛔ **AND MY OWN ACCEPTANCE QUESTION WAS THE FOURTH FINDING** — written in the office's vocabulary, asking him to confirm our doctrine's satisfaction. **Standing form now: *what did you DO, and what did you SEE.*** **KILL unchanged: his acceptance.**
*(the build's own state, for the record:)* **`B-104` RATIFIED AT `f06eb84` — the windows stand; the ROUTE to one and the LEGIBILITY of another do not.** `B-104` RATIFIED at `f06eb84`: the later-chapter door retires at its seam; four windows drove at the coder's eye (person-built {6,6} Poincaré · torus {4,4} plane · lifted-cube {4,3} stereographic · RP² {4,2} with the descent CHECKED and the identification ARISING); riders R1–R5 landed (her fork-(ii) sentence wired at the emptiness · her two lost strings live with moving {N} · the route letter in her inbox); R6 severed → `B-105` A1. **Honest edges on record, not defects:** the worked {4,5} lives in the witness, the page's hyperbolic case is {6,6} · the strong far-side case awaits an octahedron-class surface · R2's eye case awaits an incongruent-equal-corner volume. **IN FLIGHT BESIDE THE WALK: `B-105`** — Phase A no-src (substrate map A1 · the honesty quartet A2 · the catch locate A3 · the FLIP first-measure A4), **Phase B src-gated on his walk verdict** (b₁ ring print · the pinch discriminator). **KILL: rung 2 closes at Arman's acceptance; `B-105` closes at its report's ratification.** *(prior kills fired in turn: `B-101`/`B-102`/`B-103`/`B-104`.)*
1. ⛔⛔ **RUNG 2 — ADR 0025 RATIFIED; THE BUILD IS QUEUED BEHIND `B-101`.** `docs/adr/0025-the-surface-deck-tiling-…md` drafted by the researcher and **ratified against Δ6 clause-by-clause by the sixth, 2026-08-23** (letter: `inbox/researcher/2026-08-23_1922_mothership_ADR-0025-RATIFIED-…md` — the Δ6 "observe FOR our own rendering" interpretation is named there and rides the queue for Arman's one-word veto). Method: hyperbolic-anchored reflect-across-edge generator, one `{p,q}` + one `cosh R`, sphere/plane derived; descent = `σ ∈ Sym ∧ free` (unifies with the op-set's deferred σ-macro — trigger recorded in the claims ledger). ⛔⛔ **HARD GATE (Arman, in-session 2026-08-23): NOTHING FURTHER ON RUNG 2 IS RATIFIED OR CHARTERED — the `B-104` charter included — until he EXPLICITLY rules the Δ6 interpretation below. The earlier "silence = it governs" is REVOKED on his word; an explicit ruling is required. What stands already: ADR 0025 alone, ratified WITH the interpretation flagged in its own Status line — if he vetoes, its clause (1) re-opens and the researcher re-grounds.** **The engine build is `B-104`, after `B-103` AND after that ruling (the O22 sequencing — its three unknowns already carry NUMBERS from `B-102` §1.4: depth-8 {4,5} generates in one frame · {p,q} derives at named lines · far-side rides the ink stack's own idiom, kill-probe run).** **KILL: the rung-2 build ratified at Arman's eye (his acceptance: curvature read off a countable vertex; RP²'s identification seen arising).** *(prior states — commissioned 2026-08-23, kill = ADR ratified, FIRED; measurements chartered `B-102`, LANDED.)*
2. ⛔ **R1 — the diagonalized cuboctahedron is NOT a real icosahedron** (12 unrelaxed positions). ⚠ **A substrate-truth debt under everything the window draws for pyritohedral seeds.** Carried since before the walk; **the cone arc's closure does not touch it.** **KILL: the gated re-derivation enumeration + `acos`-all-20 (claims ledger §3).**
3. ✅ **E6's CAP re-gate — CLOSED, driven by the DESIGNER at her own hand, `2026-08-22`.** `mid = 2 · small = 4 · tiny = 5` stand; **`mid = 4` shown ARITHMETICALLY UNSATISFIABLE at `level = 6`**; every legal combination renders identically while the out-of-band extremes are dramatic; controls passed on both dials; **every plate carries its `settle`.** ⛔ **NO DRIVE IS OWED BY ARMAN. None was owed after 09:20.**
> ⛔ **STRUCK — this line previously read *"the trigger FIRED and has not been served… the designer asks for ONE drive from Arman"*, and it stood for a day AFTER her two closing letters had arrived and been archived.** ⇒ ★★ **Her diagnosis, taken: *my letters are DATED EVENTS; this plan is a STANDING ASSERTION; a letter cannot amend an assertion, and nothing in our convention made it.*** **We swept five registers of the app's strings this week and never swept our own board.**
> ⇒ ✅ **CURE, applied to every open item below: A STANDING ASSERTION MUST NAME ITS KILL-CONDITION.** ✅ **And the reciprocal convention, hers and accepted: a letter that closes a board item names WHICH LINE IN WHICH FILE it kills, by path and number.**
4. ✅ **Δ11's presentation — CLOSED (kill fired: `B-103` ratified at `1148093`).** The computed line ticks the dock chip-for-chip and MOVES with a pick; the refusals stop; her bound and tooltip verbatim; **the designer verified line and bound at her own independent eye.** Residue in her lane only: the empty-open-set word · the identify-entry sentence.
5. ✅ **The `unnamed` card contradiction — CORE CLOSED (same kill):** the label falls to the guard and AGREES with the ring at the eye; the `conceptId` silent-drop defused. Residue: the third position measured ROLE-BEARING — **her case, grammar in her hands.**
6. ⚠ **The folded verdict body: EXTENT MEASURED** — the census at `dcc68a0` (coder §1.1): exactly ONE illegible kind of twelve; the op-row misreport is CURED (`b08848d` + `5bc1301`, control held). **What remains is the CARD's content — a meaning + copy question, the mothership's and the designer's. Not chartered; no build.** **KILL: a ruled card content chartered and landed.**
7. ✅ **Δ12 — CLOSED at the record (kill fired this sitting):** the researcher's wedge ruling, found archived-unprocessed and processed — bare vertex-merge refusal TOTAL (link = two circles); kind-1 intents get the tooltip translation (`B-103` 2d); kind-2 (the bare wedge sum) out of scope by business — the module builds the inhabitable. Claims row C3 holds the full ruling.
8. ⚠ **`collapse`'s non-reproduction** — an observation of his we cannot reproduce. **Open on Arman: which route.**
9. ✅ **The `state persists` rider — DEAD, doubly (kill fired at `B-101` §4):** no chrome carries the phrase (F.0e had already STRUCK the hint — `AppShell.tsx:127-128` is the striking's comment; the rider's premise was stale in V3 and inherited by the sixth unread — owned), and the behavior measured at the eye matches the RATIFIED doors model (the plan's own persistence acceptance was *save → reload → load*; the app's §7 wording states it). **Residual, NEW SCOPE, Arman's word only: automatic page persistence across reload.**

## 📦 THE BACKLOG SWEEP — `BACKLOG.md` RETIRED on Arman's approval (2026-08-23, condition honored: swept whole first)
**The plan is now the ONE queue.** Disposition of everything that was still alive in that file; anything not listed here was verifiably closed in place (ARC R · ARC 0 · THE UNION · THE RECUT · C.1 · THE PERSON'S HANDS · N3/graveyard via O13's 62-witness retirement · R5's `SUBDIVIDE_PATH` deletion) or died with the FIELD (O20: C.2 · C.4 · C.5 · the graveyard's remainder — parked in git with the field's claims).

**REVIVED — at risk of evaporating, now on this board:**
- **R-1 THE FLIP's expired hold.** *"Held for the shader — needs the observer to WALK a meridian; a still cannot carry it"* (backlog §HELD). **The walk exists since rung 1 closed.** The FLIP is the fold's mark (Amendment 11's second of three); its person-route through the moving window on a FOLDED form with a chiral reference is undriven. LAW 26 (noise floor) binds the claim. **Owner: me (charter when rung-2's front clears); KILL: the mirrored return read at an eye on a folded form.**
- **R-2 `deserializeSnapshot` NULLS acquisition on save→load** (backlog §FROZEN-FIX, found at `ab077ef`): bridged downstream (save→load→combine works); **the PROPER fix — the frozen `snapshot.ts` edit + re-seal — is a named owed ticket** and persistence is now load-bearing (Δ10 doors). **Owner: coder, next `snapshot.ts` sanction; KILL: the frozen edit landed, bridge retired.**
- **R-3 THE PINCH DISCRIMINATOR — its ride-along trigger FIRED and was missed:** route bodiless iff `∃v: decomposeLink(v).pinch` (researcher-blessed 07-30) was to ride "the next `writtenFormModel` touch" — **`b08848d` WAS that touch.** The coincident case would girder a lying body. **Owner: coder, next `writtenFormModel` union OR a fixture; KILL: the discriminator on the pinch flag, witnessed.**
- **R-4 `b₁` PRINTS WITHOUT ITS RING** (backlog §b₁, researcher-ruled): cards print `b₁` today; the engine's is `b₁(ℤ/2)` — *a viewer cannot count a coefficient ring*. Cure = rename, never renumber (one line). Field-independent half only. **Owner: designer wording + coder; small; KILL: the ring printed.**
- **R-5 the `ManuscriptView:1085` bare catch** still (⚠ unverified at HEAD) swallows `surfaceRefinement:371`'s internal-consistency alarm — the one message that would help is the eaten one. **Owner: coder, a LOCATE first; KILL: the alarm speaks or the catch names what it caught.**
- **R-6 INSTRUMENT-HONESTY QUARTET (mechanical, one light run):** the untracked chord hash file presenting a DEAD seal as a live charter (mark dead or delete) · the manifest's `surfaceRefinement` NOT_FROZEN line naming only one of its two ratifiers · `diagnose-the-subdivision`'s `headBlobOf` self-comparison (pin to `01da8ed` — a moving baseline is not a baseline) · the folded witness's VIEW-PIN regex at its limit (a pin that will eventually match nothing and PASS). **Owner: coder; rides any light build.**

**CARRIED BY NAME (parked, conscious):** the expansion set (A.2 · A.3(has a caller) · C6 · C3-stranding · F3/F4 · Q4 · slot-resolved lineage · P-IMMERSE §7 · D.1 · **D.2 the beautification pass (Arman's own grouped order, 07-03)** · D.3) · HATCH-PERIOD normalisation (Arman-ruled low) · the interaction-level witness clause (folds into the coder's witness practice — one line rides the next person-gesture mandate) · chiConsistent closed-world + the fossil-date/stale-message rule-aparts · ARC B residuals (rung 3 re-opens them; **B.4's residual needs a re-measure against `e93adab`'s multi-cell landing**) · `RichFieldOverlay.tsx` orphan-or-consumed (one locate).
**BELIEVED CURED, one cheap check owed:** identify's band-vs-twist aimability — the a2c2fe7 gesture refinement + the trace preview's per-pair counterfactuals look like candidate-2 landed; **acceptance question for the next drive: can a person AIM cylinder vs Möbius on a square, and does the preview say which they will get?**
**MINE:** geometry-type as a first-class tower invariant — ruled, never written into ADR 0019; verify 0019's state at my next docs pass. **AND: `BACKLOG.md`'s last line is TORN mid-sentence** (`"Routed 2026-07-"` — the acquisition-chain routing; the file's own concurrent-edit disease) — the acquisition residue is carried by the claims ledger's P1 row, so nothing is lost with the tear.

## ⚠ RUNG 2's PARKED INPUTS — carried forward unchanged, still good
The S²/RP² fixture (octahedron cover) · the far-side-shows-through constraint · `instruments/tiling_reference/` (all three geometries; two traps encoded — `cosh R = cot(π/p)·cot(π/q)`, dedup tolerance scaled to `(1−|c|²)`) · **the RP²-DESCENT CHECK — the engine must CHECK `−I ∈ Sym(tiling)`, never assume (cube ✔ octa ✔ tetra ✘)** · the vertex-deficit trichotomy (3/4/5 cells → 270/360/450).
★ **And it has just gained a live connection:** the researcher's ruling (⚠ cited here with a stamp-drifted "08-24" date — the sixth's sweep; the ruling stands, SHAs arbitrate) that **RP² from the octahedron is a fundamental-domain macro (`cut` → `fold` with `abab`)** is the same descent question this rung already encodes. **`−I ∈ Sym` is exactly the free-involution condition.** *(Handed to the researcher in the rung-2 commission.)*

---

# *(the state as it stood at `1e09f36` — retained below; read the table above first)*

# THE LIVE STATE at `1e09f36`

## ✅✅ `(c)` THE WALK — **CLOSED 2026-08-21, ACCEPTED AT THE SOVEREIGN'S OWN HAND**

**Arman: *"i did walk it. all is good."*** ⇒ **Rung 1 is complete.** The fan chamber reads, on his screen:
```
Euclidean cone-manifold · cone edges (measured): 1 × 300° · the manifold ends here; the orbit recurs only through the glued corridors
back where you started · 1 door · the room came back turned
```
✔ **The winding route** (`fdb92ca`, `bd5829d`) + **W.7's two-line comparison** (`2398c08`) + **interior transport** (`4c0d94e`). ★ **The mechanism that made it true: the stored embedding SMEARS the fan's 300° to 360° with the distortion inside cells, so no per-wall transform on it could be honest — the walk-room is DEVELOPED FROM THE RECORDS instead** (the base fan unrolled at its true wedge angles, the cycle-closing wall a seam-portal pair carrying the 60°). ⇒ ***Record-not-reading, applied to geometry.*** ⛔ **And `apertureModel:1334`'s precondition is now a WRITTEN GUARD — only genuinely flat products keep the union path, byte-identically.**
✔ **Blind identification closed with it** (`61ea3e6` · `959e3f6` · `9e39a13`): the live skeleton takes the stage while the door is open, faces are named by their corners, pair marks are **traced edge cycles whose rotation IS the printed name's rotation**, pending is unconfusable with decided, the legend says what the marks mean, and the forced-wall parity line tells him the shape of the possible before he spends the act.
⇒ **33 commits pushed to `origin/team-arman` 2026-08-21 — the campaign is no longer on one disk.**

## ⛔ THE FRONT IS NOW OPEN — the Sovereign aims it
**Candidates, in the order I would recommend:** **PERSISTENCE** (his standing ask; #37 priced and the gate; ★ **and its value just rose sharply — the fan chamber is the first thing he has built that he would want back tomorrow**) · **RUNG 2, the surface deck-tiling** (held behind the cone arc's front, which has now settled — the TILING CANON governs its method) · the **legibility parked set** · the **designer's board**.

## *(historical — the front as it stood 2026-08-20)*

Rung 1's last leg and the reason this gate exists. **Everything below it in this file is either closed or parked.**

### ✅ THE GROUNDING IS IN (researcher, 2026-08-20) — **arrow-7 is ALIVE at portals and DIES at one comment**
✔ `ExploreWindow.tsx:553-558` applies each portal's `face.g` to the eye **and all three axes**, increments `seam.doors`, folds `det3of` into `frameHanded`. ⇒ **the window is NOT a flat-chart instrument; Amendment 10's cone WAS this mechanism** (a single-cell domain whose circuit is all boundary doors). **My "single flat chart" suspicion is REFUTED.**
⛔ **But `apertureModel.ts:1331-1342` strips every face owned by TWO cells as *interior*** — so on the door-3 **5-cell fan the pillar circuit's crossings are not in `cellSurface.faces` at all**; `transportWalk` never crosses them; **the 60° never accumulates and the cone renders FLAT.**
★ **The diagnosis: the cut conflated NOT-A-WALL with NOT-A-TRANSITION.** An interior face is stripped as though it were the identity, when on a cone it is a **chart transition carrying its prism's angle.**
⛔★★ **And the line is defended by a comment that NAMES ITS OWN DOMAIN** — `:1334-1335` *"on an embedded product the charts coincide, so interior pairings fit identity — geometrically true."* **True, and applied UNCONDITIONALLY.** ⇒ ***A comment that states its precondition is a guard that was never written*** — and its correctness is what stops anyone auditing it. **Sweep for the shape: "on a …", "for …", "assuming …".**
**Nine-arrow map:** the window supplies **6** (`deckOf(surfacePairingsOf)`, `:1344`) · **7 at BOUNDARY only** · **9** · the frame/door/handedness carry (LAW 22). It does **not** supply **7 at interior** — exactly where a multi-cell cone's holonomy lives. Arrows 1–5 and 8 are upstream/other readers.
✔ **NOT a missing capability (LAW 24 honoured): the window CAN show a cone.** The fix is to transport interior chart-transitions with the SAME `face.g` deck mechanism — ⚠ **without walling the walk-region** (*not-a-wall AND not-nothing*; a transition the eye passes THROUGH). ⛔ **No price on that fix until the positive control renders.**

### ⛔ THE FIRST BUILD — **THE WINDING ROUTE, and it IS the owed positive control**
The designer's **W.1–W.5 and the three return strings are RATIFIED** (`back where you started · 2 doors · the room came back turned`); the detector's shape is settled (position-return, remembered start frame, no accumulated `W`, fire on first return, `turned` gated on det +1). ***It has only ever waited on a coder lane.***
> ⇒ **One build, two payloads.** On **Amendment 10's single-cell cone** it must read **`2 doors` + `the room came back turned`** — **the positive control at a person's eye.** On the **door-3 5-cell fan** it must read **`back where you started` with no turn** — **the interior-transport gap made VISIBLE rather than argued.** ★ **A falsifier and a diagnosis in one cut, with no capture rig: the person reads the answer in three ratified strings.**

**⛔ THE ACCEPTANCE PROBLEM, binding on every mandate in this arc:** *no seat instrument can drive the walk (no press-and-hold).* ⇒ **The Sovereign is the ONLY instrument that can accept it.** ⇒ (1) every build produces something **he** can walk on his own route in one sitting, never a fixture that proves transport to a test; (2) **the seats owe FRAME-CAPTURE before any walk claim is made at all** — without it the claim is unfalsifiable, and *a seal whose route only a test can walk is a seal on the substrate, not the product.*

**⛔ THE MARK IS ALREADY RULED — do not re-derive it and do not build toward a sensation the canon forbids.** ADR 0004: **LAW 20 — a deck transformation is invisible from inside; you can never see your own transformation.** **LAW 22 — handedness is state the OBSERVER CARRIES.** Amendment 11's three marks: **THE WINDING** — the room comes home **EARLY, COUNTED IN DOORS, never a felt rotation** · **THE FLIP** — mirrored · **THE MIRROR-CLUSTER** — copies return left-handed. **LAW 24 — a negative result requires a positive control. LAW 26 — a visibility claim names its noise floor.**
⚠ **`docs/design/adr/` IS IN THE WAKE ORDER.** Its absence cost a week of re-deriving arrow 7.

⚠ **`(c)` does NOT wait on `(b)`.** The prior plan sequenced the walk behind the picker; that dependency was panel polish, not transport.

---

## RUNG 1 — the E³ window

### ✅ PART A — T³ legibility: **CLOSED**, ratified at two attitudes (`3c4ff8d`); dials `f68c91c`; dial-axis `834826f` (LOD ladder on the ECHO axis; the true content horizon is the fade `exp(−echo/2.4)`, extinct ≈ echo 6–7).

### THE CONE ARC — **three of four legs CLOSED since the last regeneration**
- **(0) REACHABILITY ✅** — a sound cube-derived cone room is reachable by clicks alone. E3 closed.
- **(a) THE NON-CUBE CONE FORM ✅ CLOSED.** The multi-cell wall is **down**: `e93adab` landed the frozen union + `readSeedCells`; **the fan×I cone room builds.** ✔ **Accepted at my own eye:** the window caption reads **`cone edges (measured): 1 × 300°`** while the cube reads **`(k×90° heuristic)`** — *both states positively marked.* The sealed acceptance (300° vs 450° ON SCREEN) is **met**.
- **(d) THE METRIC VISIBLE ✅ in substance.** ✔ At my own eye 08-19: the argument card derives the stance **through the map** and speaks the verdict as a consequence — `EH : 60 ⊕ 60 ⊕ 90 ⊕ 45 ⊕ 45 = 300` · `EH +60° — a cone` · `○ open · local-cone` · `Euler χ 1 (certified)`. **The sealed conformal investment is no longer dark.**
- **(b) THE PICKER — pass 1 ratified-as-amended; pass 2 PARKED** (the corner-correspondence mark; menu-pictures; preview-on-change with a REFUSAL state; the three turn-tag strings are FINAL). Parked with the legibility set — **not a walk dependency.**
- **(c) THE ORBIT — ⛔ THE FRONT.** See above.

### ⛔ STILL OWED IN RUNG 1 — **R1**
**The diagonalized cuboctahedron is NOT a real icosahedron** (the 12 positions are unrelaxed). Gated on a re-derivation enumeration. ⚠ **This is a substrate-truth debt under everything the window draws for pyritohedral seeds — carry it; do not let the cone arc's closure imply it.**

## ✅ E6 — THE ENCLOSURE: **CLOSED at `ac3984b`, ratified at the mothership's own eye 2026-08-13**
Built: smooth-rod recede graded on rank · `depthWeightRatio` on **ECHO** not world-travel · the one-sided depth-break contour · the LOD echo trio reachable within the cap. ✔ Accepted on my own drive (Station A rebuilt, both attitudes): *"the rods are quiet AND the hierarchy is back… the phantom-quadrilateral gestalt broken."* ✔ The mechanism-probe **ran** and the designer's one-sided reading won. ✔ The echo trio is **`lodMid 3 · lodSmall 4 · lodTiny 5` — PINNED, not tuned**: `mid < small < tiny ≤ level−1 = 5` makes it the maximal legal ladder, two of three cap-forced.
⛔ **RE-OPEN TRIGGER FIRED by the walk becoming the front** — and **the live item is the CAP, not the cure.** Her eye asked for `mid = 4`, which the cap forbids; **she gated the whole ladder standing still**, and the reading gets stronger the moment anyone walks (a deep corridor is where ranks 3–5 become the whole picture). ⇒ **Do NOT price the cap before the walk exists; DO re-open it the moment it does.** ★ *A ladder gated standing still is a reading whose domain was never entered.*
⚠ **Open, small, hers, unblocked:** `depthWeightRatio 9` has never been re-gated on the ECHO axis — the old saturation result was measured on world-travel and does not transfer.

## RUNG 2 — the surface deck-tiling: **HELD** behind the walk
Method order per the TILING CANON. Parked inputs unchanged: the S²/RP² fixture (octahedron cover) · the far-side-shows-through constraint · `instruments/tiling_reference/` (all three geometries; two traps encoded — `cosh R = cot(π/p)·cot(π/q)` and dedup tolerance scaled to `(1−|c|²)`) · **the RP²-DESCENT CHECK — the engine must CHECK `−I ∈ Sym(tiling)`, never assume (cube ✔ octa ✔ tetra ✘)** · the vertex-deficit trichotomy (3/4/5 cells → 270/360/450). The researcher drafts the tiling ADR when this opens.

## RUNGS 3–4 — queued, unchanged (curved transports · the arbitrary orbit).

---

## ⛔ THE LEGIBILITY DETOUR — named, bounded, and PARKED *(2026-08-13 → 08-20)*

**What it was:** a week aimed entirely by the Sovereign's own use — *"no un-asked-for room," "the faces are code names," "when empty comes back everything is lost."* **In this plan's vocabulary it is §3 HORIZON groundwork, opened while the plan said the horizon was closed.** The work was right; **the failure was that nobody wrote the re-aim down**, so the walk starved for a week behind a queue that had swallowed the plan.

**What it BOUGHT (all landed, all at HEAD):** D1 the first end-to-end carry · D2 the pairing door · D5→R2 stamped angles replaced by an acos import (`47cd5a0`) · D6α the vacuous seal replaced by a lift-coherence guard that can fail (`ebe8a51`) · D8+D9+D10 (`dfbd6a3`) · D13 error boundaries + D14 the ratified corner-label name (`6cf7e8c`) · **D12-b the thickened room speaks its carried names** (`5bac4f5`, accepted at my eye: `AE₀·EH₀·EF₀ … AD₀·AE₀·AE₁·AD₁`) · the `multiform` union (`1e09f36`).

⛔ **IN FLIGHT — PERSISTENCE + #37, un-parked 2026-08-20.** *(Sovereign: the walk being the front is not abandonment.)* ★ The walk's first move is a **researcher read**, so parking the coder's lane freed nothing. Persistence is ✔ priced (record-not-reading, ~5 durable fields, A+B with the record defined once), its two owed decisions ✔ ruled from the Sovereign's own words, ⛔ blocked only on **#37 — unpriced, and the thing that makes a saved page re-loadable TWICE.** ✔ `snapshot.ts` v1 + `workspacePersistence.ts` prove the machinery on the ambo side; ⛔ **nothing saves the manuscript page.** Acceptance: *fan room → save → **reload the browser** → load → the room is there and still reads `cone edges (measured): 1 × 300°`.*

**PARKED, with the standing sanction ALIVE and unexpired:** the four frozen unions (3 `materializeOperation` · 4 `complexIdentification` · 6 `surfaceImmersion` · 13 `standardBodies`) · the terminal cut (BOTH readers' `=== key` clauses in one commit) · **D16** (the card takes the door's resolver ENTIRE — reach *and* the level mark) · **the SCREEN CENSUS** ⚠ *coupled to D16, not to the calendar: producer-stops do not unify surfaces, so the disagreements survive; the census runs first at a pinned SHA whenever D16 opens* · the **FACE REGISTER** (compose from corners; `Face.data?` held for the semantic layer) · the **born-name/catalogue question** (`primitiveCatalogue:39` — the INVOKE palette is the front door, not a backwater) · the `D1` pair (15)+(16), lineage before name.

**FINISHING, not parked** (union-free, independent, no surface unified): **group A** (`closeEdgeIntoCircle:173`) · **group C** (`surfaceRefinement:307/664/909`) · the `patchLift` one-line rider.

⚠ **One rider I am carrying regardless:** the chrome reads **"state persists"** and a reload wipes the person's work. **Taking three words off the screen is not persistence and is not a union** — it rides the first free cut unless told otherwise.

---

## ⛔ AUTHORIZATION + ROUTING — **REWRITTEN 2026-08-21: the chain is `Arman → mothership → coder`. THE ENGINEER SEAT IS DISSOLVED.**
**The coder builds, measures, and runs the PRE-COMMIT WITNESS LIST themselves:** ① `git diff --stat <sim> HEAD` empty · ② the freeze manifest line printed beside every touched file · ③ re-seals **recomputed with a positive control** · ④ suite 112 @ 1 + tsc 0 at the real HEAD · ⑤ ⛔ **THE READING — what the person SEES, in the frame they are in when they see it.** *A green suite closes nothing a person can see; a COUNT of the marks is not a SIGHTING of them.*
**Mothership holds meaning, the record, and the no.** **Researcher + designer are CONSULTED BY QUESTION, not routed through** (their standing rulings need no re-issue). A frozen file outside the sanctioned seven stops the coder — ask, never spend.
★ **Why:** the engineer seat could not run the app, **and a seat that cannot measure produces prose** — every false premise of 08-20 dissolved on one measurement, while the STOP, the `pending` type, the sensitivity-buying recut and the parent-transform catch all came from the coder. **Smallest measurement first, every time.**
**Arman's eye remains the branch-1 instrument, and it has caught something every day it has been applied.**

## MAP TO `PLAN_THE_LAST_PHASE.md`
§1 = this plan (the active gate; the walk is its front) · §2 the tail unchanged · §3 THE HORIZON — ⚠ **partially opened by the legibility detour and now re-closed; what it bought is listed above so the layer does not start from zero when it properly opens.**
