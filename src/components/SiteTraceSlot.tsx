// Reserved slot for the per-site structural TRACE (the compositional meat). Intentionally
// empty: the trace is bridge-provenance, derived later under a separate sovereign-gated
// mandate, and is kept OUT of the naming face by construction. This reserves its place.
export function SiteTraceSlot() {
  return <p className="text-sm text-stone-500">Structural trace — reserved. Not yet derived.</p>;
}
