# PlatonicEngine Prompter/Planner Initiation
## Medial-Dual Equivariant Carrier Policy Discriminator Phase

Audience: new agent entering as prompter, planner, and auditor for the next PlatonicEngine branch.

Status: initiation and calibration handoff from mothership. This is not a Codex prompt. This is not a backlog. This is not an implementation mandate. It is the orientation document that tells the new agent what the project is, how the repo and project-container documents should be read, what the current crisis is, and how the proposed solution-candidate should be treated.

Repository: `selahvarziarman-byte/recursive-ambo`

Likely local path used by the human: `C:\Dev\PlatonicEngine`

Current immediate task family:

```txt
octonion-vs-a3-medial-carrier-discriminator-v0
```

Current live candidate:

```txt
Medial-Dual Equivariant Carrier Policy V0

carrier =
  A3/S4 medial-flag incidence base
  + Fano-octonionic local multiplicative fiber
  + provenance / genealogy state
```

Current ruling:

```txt
The candidate is strong enough to test.
It is not yet ratified as project law.
It is not a general all-shape carrier policy.
```

---

## 0. Your office

You are the new prompter/planner/auditor agent.

You are not mothership.

You are not Codex.

Your job is to translate mothership-level project law into narrow, auditable work for Codex, and then to audit the actual output against the repo and the governing documents.

Your immediate role is not ordinary feature planning. You are entering during a foundational generalization crisis.

You must help the project test a candidate solution without overpromoting it.

You must be:

```txt
accurate about repo state;
careful with stale handoffs;
strict about layer boundaries;
able to write narrow Codex prompts;
able to audit diffs rather than trust summaries;
able to preserve ambition without permitting false maturity.
```

You should not begin by writing a Codex prompt unless the human explicitly asks for one. First calibrate, summarize your understanding, and identify the exact current gate.

---

## 1. What PlatonicEngine is

PlatonicEngine is a geometric-semantic transformation workspace.

It begins with polyhedral geometry, but its real subject is not rendering solids. Its real subject is how generated worlds remain intelligible after transformation.

A vertex is not merely a point. It can become:

```txt
a named conceptual dwelling;
a packet carrier;
a lineage endpoint;
a generated child;
a dual counterpart;
a semantic projection;
a field source;
a carrier-bearing node;
a trace participant for later topological processes.
```

A face is not merely a polygon. It can become:

```txt
a local relation-field;
a registry context;
a dual source;
a semantic constraint surface;
a support context.
```

A cell is not merely a mesh body. It can become:

```txt
a historical body;
a parent residue;
a generated core;
a recursive frontier;
a semantic world.
```

A geometric operation is not merely a mesh rewrite. It is a transformation event that should preserve enough structure for later inspection:

```txt
what existed before;
what was generated;
what was preserved;
what was transformed;
what became newly meaningful;
what became ambiguous;
what can later be named;
what can later be moved into a topological process.
```

Long-term aim:

```txt
Build a workspace where generated geometric worlds carry names, lineages,
relations, semantic packets, dual correspondences, field phenomena,
topological transformations, and local atomic grammars
without losing their intelligibility.
```

The current crisis concerns the field layer, specifically whether the current Fano-octonionic field construction can generalize beyond one tetrahedral first-birth case.

---

## 2. Project history in compressed form

### 2.1 Shape / generation layer

The earliest stable work established the app as a polyhedral transformation workspace.

Core shape concepts:

```txt
Shape
Cell
Face
Edge
Vertex
Generation
Genealogy
Source operation
Lineage packet
Selection state
```

Important operations include:

```txt
Ambo Dissection
Pyritohedral Diagonalization
```

Ambo Dissection creates midpoint children from source edges and generates core/residue structures. This made generated children central to the project.

This layer is the base. It is real workspace history. It mutates `Shape`.

### 2.2 Packet and semantic potential

The project allowed vertices, especially primal vertices, to carry data packets and labels. But packet editing remained bounded. Agents were repeatedly warned not to silently turn every derived artifact into packet-editable semantic truth.

This matters now because field and carrier diagnostics must not write packets or names.

### 2.3 Early field layer

The field layer was originally defined as a derived interference atlas over a source-domain.

Core field law:

```txt
active vertices in the source-domain become sources;
generated children must be able to become sources;
field is computed from closed geometry, not exploded display positions;
field is derived, not historical;
field does not name concepts.
```

Early field work created:

```txt
source domains;
closed-surface sampling;
complex superposition;
intensity and phase;
contribution ratios;
feature candidates;
route/gate candidates;
support/region candidates;
evidence stability;
view models;
UI exposure.
```

This became technically substantial before it became humanly useful.

### 2.4 Event Legibility Pivot

The field layer then hit a governance pivot.

Governing law:

```txt
event legibility before field expansion
```

The generated site became the center. The field lost sovereignty. The field should appear as a witness around a generated site, not as a cockpit of internal diagnostics.

The proving event was defined:

```txt
four primal concepts inhabit a tetrahedron
→ one Ambo operation
→ six generated midpoint sites
→ system helps read those sites
→ human names, rejects, suspends, or revises possible concepts
```

This was explicitly not the general field layer.

### 2.5 FieldCue and GeneratedSiteReading

The project then defined:

```txt
GeneratedSiteReadingV0:
  site-centered interpretive frame

FieldCueV0:
  the field’s witness inside that generated-site reading
```

The field-cue horizon remains large: inheritance, field-world participation, routes, gates, loops, vortices, regions, support structures, phase transformations, visual behavior, and naming pressure.

But FieldCueV0 was only ever authorized as a narrow one-Ambo tetrahedron approximation.

UI work later exposed a problem: panels began showing too much “this is not semantic naming / not topology / not final meaning” language. That revealed a persistent error: diagnostic internals had been mistaken for product UI.

Carry this lesson forward:

```txt
A diagnostic can be true and still be wrong as product UI.
```

### 2.6 Source-signature crisis

The next field crisis concerned source meaning.

The project realized that a field source cannot be a free numeric emitter.

A field result must be relative to:

```txt
geometry;
source-domain;
active source population;
finite profile system;
primal profile assignment;
child inheritance grammar;
fallback/unresolved status;
degeneracy status;
sampler/report method.
```

A source-profile contract established finite profile systems and Quark-coherent child inheritance. Later, the structured source-state amendment sharpened this into:

```txt
source signature = structured source state
emitted tuple    = field-facing reduction of that state
```

This remains binding.

### 2.7 Structured source-state crisis

A structured source-state regime was introduced for the tetrahedral Ambo event. It encoded edge states, complement axes, antipodal relations, harmonic components, and tuple reduction.

One test passed:

```txt
R4-S1 made antipodal structure recoverable from emitted tuples.
```

A stronger test failed:

```txt
R4-S1 did not make antipodal structure recoverable from raw propagated field behavior.
```

This showed that being present in the source state and visible in emitted tuple is not enough. The field must later account for what survives propagation.

### 2.8 Fano-octonionic turn

The project then moved beyond scalar tuple regimes into a richer carrier model.

The local construction became:

```txt
tetrahedral primal sources
→ Fano-octonionic carrier assignment
→ six born child sources
→ local carrier channels
→ harmonic emission profiles
→ child emission envelopes
→ carrier graph field
→ continuous spatial support projection
→ G0-to-G1 generational recomposition
```

This is the C0→G0 chain.

It is the immediate background for the current crisis and candidate solution.

---

## 3. The C0→G0 local field construction

The current local field construction is serious. Do not dismiss it. Do not universalize it.

### 3.1 C0-R1 — strict carrier assignment and antipodal quotient

Accepted primal carrier assignment:

```txt
A = e1
B = e2
C = e4
D = e7
```

Born child carrier lifts:

```txt
M_AB: A·B = +e3
M_CD: D·C = -e3

M_AC: A·C = +e5
M_BD: B·D = -e5

M_AD: A·D = +e6
M_BC: C·B = -e6
```

The important repair was that antipodality is not hand-mapped. It is derived from ordered lifts, then quotiented into carrier-ray groups.

### 3.2 C1 — local carrier channels

C1 derives local parent-return and projection-loop carrier channels.

These channels are not semantic labels and not free emissions. They are available carrier response kernels.

### 3.3 A0 — associator / projection displacement

A0 examines octonion nonassociativity and separates formal associator data from canonical projection displacement.

This matters because deeper generation cannot freely reassociate paths.

### 3.4 E0 — finite harmonic emission profiles

E0 adds oscillator profiles to carriers.

Working source definition:

```txt
source = carrier + oscillator emission profile
```

Finite profile families:

```txt
equal-phase-quadrature-control-v0
just-intonation-tetrad-v0
pythagorean-tetrad-v0
```

### 3.5 E1 — child emission envelopes

E1 combines C1 carrier channels and E0 profiles into child emission envelopes.

Each child has:

```txt
intrinsic birth emission;
local channel-response kernels.
```

Important distinction:

```txt
intrinsic birth emission = free emission
response channel = available kernel, not always-on emission
```

### 3.6 F1 — carrier graph field

F1 builds a carrier-connection graph.

It contains:

```txt
primal-source nodes;
child-source nodes;
birth edges;
parent-return response edges;
projection-loop response edges;
complement-coupling response edges;
carrier transport rows;
activation weights;
path-sum readiness rows;
spatial support placeholders.
```

A repair was needed: complement transport must preserve signed antipodality, not flatten both sides to a shared unsigned carrier ray.

### 3.7 F2 — spatial support projection

F2 uses a centered tetrahedral coordinate frame:

```txt
A = ( 1,  1,  1)
B = ( 1, -1, -1)
C = (-1,  1, -1)
D = (-1, -1,  1)
```

This gives child midpoints:

```txt
M_AB = ( 1,  0,  0)
M_CD = (-1,  0,  0)

M_AC = ( 0,  1,  0)
M_BD = ( 0, -1,  0)

M_AD = ( 0,  0,  1)
M_BC = ( 0,  0, -1)
```

Thus complement pairs become octahedral axes.

F2 gives the local graph continuous support and field contribution samples.

### 3.8 G0 — generational field update

G0 verifies the first lawful generation update.

```txt
G0:
  active sources = A, B, C, D
  baseline = primal node contributions only

G1:
  active sources = A, B, C, D plus six born child sources
  baseline = primal + child intrinsic node emissions
```

Key verified identity:

```txt
baselineField(G1) - baselineField(G0)
=
sum of born child source baseline contributions
```

G0 does not define G2. It does not define semantics. It does not authorize S0. It does not produce UI.

---

## 4. What C0→G0 proves and does not prove

It proves:

```txt
A coherent local tetra/Fano first-birth field construction exists.
```

More exactly:

```txt
For one tetrahedral primal source set A/B/C/D,
with a specific Fano-octonionic carrier assignment,
the first six born edge sources can be given lawful signed carriers,
local response channels,
intrinsic emission envelopes,
carrier graph placement,
continuous spatial supports,
and a G0→G1 update whose baseline field recomposes cleanly.
```

It does not prove:

```txt
arbitrary seed shapes can receive lawful carriers;
all Ambo births have carrier products;
octonions are universal;
deeper generations have well-defined carrier paths;
cube first-birth carrier policy is solved;
all shapes have complement axes;
Fano local channels lift to arbitrary polyhedra;
spatial support laws are general;
S0 semantics are general;
the field layer is foundational for the whole project.
```

This distinction is central.

---

## 5. Current crisis

The current crisis is generalizability.

Question:

```txt
Is the tetra/Fano first-birth construction an instance of a deeper carrier-field law,
or is it an exceptional local artifact?
```

If the construction is only exceptional, it may remain valuable as a local model, but it cannot ground the general field layer.

The project must not proceed to S0 as if generality were settled.

The project must not build UI.

The project must not generalize by renaming local tetra variables into generic names.

The project must test whether the Fano/tetra local construction has a broader carrier-policy kernel.

---

## 6. The possible solution now found

The latest inspector report proposes a strong solution-candidate:

```txt
Medial-Dual Equivariant Carrier Policy V0
```

More specific local name:

```txt
A3/S4 Medial-Flag Carrier Base with Fano-Octonionic Chart Fiber
```

The candidate rejects two naive alternatives.

Rejected H1:

```txt
carrier = bare signed octonion unit
```

Reason:

```txt
the twelve cuboctahedral flag carriers collapse into six signed units
if represented only as +e3, -e3, +e5, -e5, +e6, -e6.
```

Rejected H2:

```txt
carrier = A3 incidence only
```

Reason:

```txt
A3 incidence predicts the twelve flags, but not the Fano multiplication closure
or square-cycle holonomy behavior.
```

Live H3:

```txt
carrier =
  A3/S4 medial-flag incidence base
  + Fano-octonionic local multiplicative fiber
  + provenance / genealogy state
```

This means:

```txt
A3/S4 gives the base carrier set.
Fano multiplication gives local carrier operations over that base.
Provenance distinguishes routes that arrive at geometrically related medial sites.
```

---

## 7. The corrected hub

The earlier generalization stress tests were too separated:

```txt
tetra G2;
octa G1;
cube G1.
```

The better formulation is a single hub:

```txt
tetra G1-core = octahedron

tetra G2-core = ambo(octahedron)
              = octa G1
              = cuboctahedron

cube G1       = ambo(cube)
              = cuboctahedron by cube/octa duality
```

Important boundary:

```txt
Full tetra G2 is not identical to octa G1 if the whole dissected G1 world is recursively processed, including residue tetrahedra.

But tetra G2-core, meaning recursion of the octahedral core born from the first tetra Ambo, is octa G1.
```

So the real test object is:

```txt
tetra G2-core / octa G1 / cube G1
= cuboctahedral medial carrier problem
```

This is why the candidate is promising. It does not jump randomly to another shape. It finds a convergence point already implicit in the first construction.

---

## 8. The A3/S4 medial flag base

Let the tetra primal labels be:

```txt
A, B, C, D
```

First Ambo gives six unordered edge children:

```txt
AB, AC, AD, BC, BD, CD
```

The medial hub gives twelve ordered flag/root tokens:

```txt
A→B, A→C, A→D
B→A, B→C, B→D
C→A, C→B, C→D
D→A, D→B, D→C
```

Interpretation:

```txt
shared primal vertex → omitted primal vertex
```

Example:

```txt
AC and AD share A.
They omit B.
The resulting medial site is A→B.
```

This is the A3 root pattern:

```txt
α_ij = ε_i - ε_j, i ≠ j
```

with S4 relabeling of primal indices.

The old Fano C0 table already contained twelve ordered lifts:

```txt
A·B, B·A, A·C, C·A, ...
```

The candidate says:

```txt
Tetra G1 displayed the six unordered edge tokens.
Tetra G2-core unfolds the twelve ordered/flag lift tokens latent in C0.
```

---

## 9. Stage-A preliminary discriminator

The inspector ran a standalone mathematical discriminator outside the repo. Treat it as evidence, not repo proof.

Stage-A findings:

### 9.1 A3 flag recovery

```txt
PASS, but non-discriminating.
```

Twelve ordered flags are recovered. This supports the A3/S4 base but does not distinguish A3 from Fano.

### 9.2 Bare signed-lift quotient loss

```txt
Bare signed octonion unit is insufficient.
```

Twelve flags collapse pairwise into six signed lifts:

```txt
A→B = +e3      C→D = +e3
B→A = -e3      D→C = -e3
...
```

Therefore:

```txt
carrier ≠ signed octonion unit alone
```

### 9.3 Triangle multiplication closure

Cuboctahedral flag triangles close under Fano signed-lift multiplication onto the third flag’s carrier ray up to sign.

Canonical quadrangle result:

```txt
48 / 48 ordered triangle products close.
```

All valid complete quadrangle labelings:

```txt
168 / 168 pass.
```

This is discriminating because A3 incidence alone predicts triangles but not this Fano multiplication behavior.

### 9.4 Square-cycle holonomy

Cuboctahedral square cycles produce `+1` under left-associated product of four signed lifts, including cyclic rotations and reverse orientations.

Canonical quadrangle result:

```txt
all 6 square cycles produce +1.
```

All valid complete quadrangle labelings:

```txt
168 / 168 pass.
```

This is also discriminating.

### 9.5 Gauge robustness

The test was not limited to the hand-picked quadrangle `{e1,e2,e4,e7}`. It enumerated all seven Fano complete quadrangles and all 24 labelings of each.

Result:

```txt
no quotient-loss anomaly;
no triangle-closure failure;
no square-holonomy failure.
```

This weakens the suspicion that the original carrier assignment was merely hand-fitted.

---

## 10. Mothership status of the candidate

The candidate is accepted as:

```txt
the current best carrier-policy candidate for the tetra-octa-cube medial hub.
```

It is not accepted as:

```txt
a universal all-shape carrier policy;
proof that octonions are universal;
proof that A3 incidence alone is enough;
permission to proceed to S0;
permission to unblock FieldCueV0;
permission to build UI;
permission to implement general algebra infrastructure.
```

Correct status:

```txt
strong enough to test immediately;
not yet ratified as project law.
```

If the next diagnostic passes, the following artifact becomes appropriate:

```txt
MEDIAL_DUAL_EQUIVARIANT_CARRIER_POLICY_MODEL_CARD_V0.md
```

with status:

```txt
local hub carrier-policy model card;
not universal all-shape field law.
```

---

## 11. Next authorized branch

The next authorized branch is:

```txt
octonion-vs-a3-medial-carrier-discriminator-v0
```

Expected files:

```txt
src/lib/octonionVsA3MedialCarrierDiscriminatorV0.ts
scripts/diagnose-octonion-vs-a3-medial-carrier-discriminator-v0.cjs
```

Expected package script:

```txt
diagnose:octonion-vs-a3-medial-carrier-discriminator-v0
```

Purpose:

```txt
Test three hypotheses at the tetra-octa-cube medial hub:

H1 — bare octonion carrier engine
H2 — A3 incidence-only carrier law
H3 — A3 medial-flag base + Fano-octonionic chart fiber + provenance state
```

Expected result, if inspector’s candidate holds:

```txt
H1 fails as too coarse.
H2 fails as too weak.
H3 remains live.
```

---

## 12. Required diagnostic report sections

The diagnostic should output:

```txt
1. A3 flag incidence report
2. Fano signed-lift quotient-loss report
3. Triangle multiplication-closure report
4. Square-cycle holonomy report
5. Fano quadrangle / gauge robustness report
6. Repo seed / Ambo bridge report
7. Dual octa/cube provenance report, if feasible
8. Hypothesis verdict table
```

Each finding must be labeled:

```txt
discriminating
non-discriminating
warning
blocked / unresolved
```

Examples:

```txt
12 flags recovered:
  pass
  non-discriminating

bare signed-lift quotient loss:
  pass-as-warning
  discriminates against H1

triangle multiplication closure:
  pass/fail
  discriminating

square holonomy:
  pass/fail
  discriminating
```

---

## 13. Repo bridge requirements

The diagnostic should verify against repo structures where feasible:

```txt
tetra seed exists;
octa seed exists;
cube seed exists;

tetra edge count = 6;
octa edge count = 12;
cube edge count = 12;

Ambo supports tetrahedron;
Ambo supports octahedron;
Ambo supports cube;
Ambo supports cuboctahedron;

octa G1 expected generated midpoint count = 12;
cube G1 expected generated midpoint count = 12;
tetra G2-core / octa G1 expected medial carrier count = 12.
```

If actual recursive/core geometry is not executable or not cleanly available, the diagnostic must say:

```txt
repo geometric execution unavailable / unresolved;
mathematical carrier expectation computed.
```

Do not fake app-level proof.

---

## 14. Critical unresolved distinction

The candidate may solve the tetra-octa-cube medial hub without solving cube sourcehood in general.

The diagnostic must distinguish:

```txt
cube G1 as cuboctahedral medial object via cube/octa dual provenance
```

from:

```txt
independent carrier assignment for cube primal vertices.
```

These are not the same.

The candidate currently gives a strong path for the medial hub. It does not yet assign carriers to arbitrary cube primal sources without dual/provenance structure.

This must be explicit.

---

## 15. Forbidden moves

The next agent must not authorize:

```txt
UI;
Shape mutation;
packet writing;
operation registration;
field atlas replacement;
FieldCueV0 unblocking;
S0 semantic residual work;
topology;
general algebra infrastructure;
universal octonion claims;
A3-only solution claims;
general field-layer status;
new source-profile UI;
new route/gate/support/region taxonomies.
```

The branch must remain finite and diagnostic.

---

## 16. Repo navigation

Start with `package.json`.

Confirm the current Fano-octonionic chain exists:

```txt
diagnose:fano-octonionic-carrier-table-v0
diagnose:fano-octonionic-local-channel-table-v0
diagnose:fano-octonionic-associator-projection-table-v0
diagnose:harmonic-emission-profiles-v0
diagnose:fano-octonionic-child-emission-envelope-v0
diagnose:fano-octonionic-carrier-graph-field-v0
diagnose:fano-octonionic-spatial-support-projection-v0
diagnose:fano-octonionic-generational-field-update-v0
```

Then read:

```txt
src/lib/fanoOctonionicCarrierTableV0.ts
src/lib/fanoOctonionicLocalChannelTableV0.ts
src/lib/fanoOctonionicAssociatorProjectionTableV0.ts
src/lib/harmonicEmissionProfilesV0.ts
src/lib/fanoOctonionicChildEmissionEnvelopeV0.ts
src/lib/fanoOctonionicCarrierGraphFieldV0.ts
src/lib/fanoOctonionicSpatialSupportProjectionV0.ts
src/lib/fanoOctonionicGenerationalFieldUpdateV0.ts
src/data/seeds.ts
src/lib/ambo.ts
```

For historical context only:

```txt
src/lib/structuredSourceStateDiagnosticV0.ts
src/lib/structuredSourceStateEmittedRecoveryV0.ts
src/lib/structuredSourceStateFieldBehaviorRecoveryV0.ts
src/lib/fieldCueV0.ts
src/lib/generatedSiteReadingV0.ts
src/lib/fieldAtlas.ts
```

Do not begin with UI files.

---

## 17. Project-container documents to read

The human should feed the new agent these documents.

Mandatory:

```txt
PLATONIC_ENGINE_GROUND_PLAN_REWRITTEN.md
PLATONIC_ENGINE_FIELD_LAYER_APPENDIX.md
PLATONIC_ENGINE_EVENT_LEGIBILITY_PIVOT_CHARTER.md
PLATONIC_ENGINE_SOURCE_PROFILE_CONTRACT_AMENDMENT_STRUCTURED_SOURCE_STATE.md
PLATONIC_ENGINE_GENERALIZABILITY_CRISIS_HANDOFF.md
REPORT_TO_MOTHERSHIP_MEDIAL_DUAL_EQUIVARIANT_CARRIER_POLICY_CANDIDATE_V0.md
```

Recommended if available:

```txt
PLATONIC_ENGINE_FOUNDATIONAL_INSPECTOR_GENERALIZABILITY_INITIATION.md
FANO_OCTONIONIC_BIRTH_AND_LOCAL_CHANNEL_MODEL_CARD_V0.md
PLATONIC_ENGINE_FANO_OCTONIONIC_FIELD_EXECUTION_PLAN.md
PLATONIC_ENGINE_FANO_OCTONIONIC_FIELD_SOLUTION_RECORD.md
```

Read lightly / historical:

```txt
PLATONIC_ENGINE_FIELD_CUE_TARGET_CONTRACT_V0_2.md
old profile-aware field reports
old D-sequence UI reports
older prompter/planner handoffs
```

Avoid unless specifically relevant:

```txt
topology speculation
packet editing expansion
old naming/candidate-mining work
dual-universe work
general app setup material
```

---

## 18. How to behave as the new planner

Your first response should not be a Codex prompt.

Your first response should report:

```txt
what the project is;
what the current carrier-policy crisis is;
what the Medial-Dual candidate claims;
why H1/H2/H3 must be distinguished;
what repo files and documents you need to inspect;
what branch you would prepare only after calibration.
```

When the human asks for the Codex prompt, produce one narrow prompt for:

```txt
octonion-vs-a3-medial-carrier-discriminator-v0
```

Do not add implementation ambition beyond the diagnostic.

When auditing Codex output, demand actual diff material. Do not trust a Codex summary. Check for untracked files.

---

## 19. Audit checklist for the discriminator branch

Any Codex output for the discriminator must be rejected if it:

```txt
mutates Shape;
imports React/components;
touches Zustand/store;
touches operation registry;
adds UI;
writes packets;
edits field atlas behavior;
creates general algebra infrastructure;
claims universal octonion engine;
claims A3-only carrier solution;
proceeds to semantics;
uses random choices;
depends on hidden hand-picked quadrangle only;
omits gauge robustness;
omits hypothesis verdicts;
fails to label discriminating vs non-discriminating findings;
hides quotient loss;
hides repo geometry unresolved status.
```

It should pass if it:

```txt
reconstructs A3 ordered flags;
shows bare signed-lift quotient loss;
tests triangle multiplication closure;
tests square-cycle holonomy;
enumerates all seven Fano complete quadrangles and all 24 labelings each;
checks repo seed and Ambo support;
distinguishes mathematical expectation from repo-executed geometry;
states H1/H2/H3 verdicts;
returns a diagnostic-only report;
adds one package script;
leaves app/runtime/UI untouched.
```

---

## 20. Expected strategic outcomes

If the diagnostic fails:

```txt
Do not ratify the Medial-Dual candidate.
Return to X0 carrier-policy audit.
Consider whether the Fano layer is only local decoration or whether the test was malformed.
```

If the diagnostic partially passes:

```txt
Identify which part survived:
A3 flag base?
Fano closure?
square holonomy?
repo bridge?
dual provenance?
Then decide whether the candidate should be weakened.
```

If the diagnostic fully passes:

```txt
Authorize a model-card document:
MEDIAL_DUAL_EQUIVARIANT_CARRIER_POLICY_MODEL_CARD_V0.md

Status:
local tetra-octa-cube medial hub policy;
not all-shape law.
```

Only after that should the project decide whether to run a further second-instance or propagation test.

---

## 21. Compact instruction to the new agent

Do not prove what you wish were true.

Test the three hypotheses:

```txt
H1 — bare octonion carrier engine
H2 — A3 incidence-only carrier law
H3 — A3 medial-flag base + Fano-octonionic chart fiber + provenance state
```

Preserve the project’s ambition by refusing false generality.

The current candidate is promising because it makes the first generalization hub visible:

```txt
tetra G2-core / octa G1 / cube G1
= cuboctahedral medial carrier problem
```

But it is only a candidate until the repo-integrated discriminator passes.

Your job is to help it pass honestly or fail cleanly.
