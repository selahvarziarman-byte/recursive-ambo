# PlatonicEngine Vital Note to the Prompter/Planner
## Source-Signature Proving Regime After the Inspector Report

Audience: the Prompter/Planner/Auditor lieutenant who ran into the source-signature blockage during FieldCueV0 / GeneratedSiteReadingV0 work.

Status: mothership handoff and ruling. This is not a Codex prompt. It is not a task list to execute blindly. It is a strategic-technical calibration note for planning the next corrective implementation sequence.

Repository: `selahvarziarman-byte/recursive-ambo`

Current priority: source-signature proving regime repair.

---

## 0. Mothership ruling

The blockage you encountered was real.

The project did not merely run into a presentation problem, a UI problem, or a missing copy layer. The blockage exposed a substrate failure in the source-signature regime that FieldCueV0 and GeneratedSiteReadingV0 depend on.

The profile-aware pipeline exists structurally:

```txt
finite primal profile system
→ primal profile assignment
→ Quark channel construction
→ child source derivation
→ degeneracy/fallback reporting
→ profile-aware field atlas
→ FieldCueV0
→ GeneratedSiteReadingV0
```

But structural existence is not success.

The active uniform-circle fixture is too weak to act as the proving fixture. It keeps amplitude, wave number, and attenuation invariant, varies only phase, and can produce fallback or collapsed child signatures under the one-Ambo tetrahedron event. That means the pipeline can display numbers without those numbers being earned by the proving event.

The inspector has now produced a serious candidate solution: **Pythagorean Tetrachord Quark Regime v0**.

Mothership decision:

```txt
Approve the inspector's regime as the first source-signature proving candidate.
Do not treat it as final source-profile metaphysics.
Do not treat it as the general field layer.
Do not resume FieldCueV0 / GeneratedSiteReadingV0 UI refinement until the proving regime passes audit.
```

The active planning problem is now:

```txt
How do we implement and audit a finite, non-arbitrary, event-bound source-profile regime strong enough that the six first-Ambo child sites inherit real, field-ready, traceable signatures?
```

The answer must preserve the project’s ambition. This is not a hygiene maneuver meant to make the project safe, small, or timid. The field-cue horizon remains vast: child-source inheritance, field-world participation, routes, gates, regions, loops, vortices, phase transformations, visual/interference behavior, and naming pressure. But none of that can bear fruit if the child source does not first have an earned signature.

No field-cue can be stronger than the source-signature regime that lets the generated site speak into the field.

---

## 1. What changed since the blockage

Before the blockage, the project had reached this mistaken practical assumption:

```txt
The field source regime is now profile-aware;
therefore FieldCueV0 can safely build on it.
```

That assumption is false.

The corrected assumption is:

```txt
The field source regime is structurally profile-aware,
but its active proving fixture has failed.
```

The uniform-circle fixture should now be reclassified:

```txt
uniform-circle scalar-invariant fixture = bad control
not proving fixture
not product source-signature regime
not human-legibility basis
```

This fixture remains useful. It shows why mere profile-awareness is insufficient. It demonstrates the failure mode: invariant scalar dimensions, over-symmetric phase, fallback risk, and misleading emitted tuple displays.

The new candidate is:

```txt
Pythagorean Tetrachord Quark Regime v0
```

This regime is approved as a **proving candidate**, not as final doctrine.

---

## 2. Why the crisis matters in the total project

The Ground Plan says PlatonicEngine is a geometric-semantic transformation workspace whose real subject is not polyhedron rendering, but how generated worlds remain intelligible after transformation. A vertex may be a named site, child, packet carrier, semantic projection, or trace participant; an operation is not just a mesh rewrite but an event that should preserve what was generated, transformed, and newly meaningful.

The Field Layer Appendix says the field is a derived dynamic source-population interference atlas. Generated vertices, including Ambo children, must become sources. The field must eventually help read reinforcement, cancellation, gates, routes, regions, and constraints, but it does not directly name concepts.

The Event Legibility Pivot says the current profile-aware field stack is event-bound, not the general field layer. The first proving event is:

```txt
four primal concepts inhabit a tetrahedron
→ one Ambo operation generates six child sites
→ the system helps the human read those sites
→ the human attempts to name what can dwell there
```

The Field-Cue Target Contract says the field-cue is the field’s witness inside the generated-site reading. It must eventually answer:

```txt
What did this generated child inherit as a field source?
How does that child-source participate in the surrounding field-world?
```

This means the source-signature regime is no longer background plumbing. It is the first point where a primal conceptual constellation becomes field-active. If the source signatures are constants, collapses, or arbitrary settings, then the field-cue becomes theater.

The immediate law is therefore:

```txt
No generated-site reading is trustworthy until its field witness has an earned source signature.
```

---

## 3. What the original source-profile contract still governs

The Field Source Profile and Quark-Coherent Child Inheritance Contract remains binding.

It established these laws:

```txt
A field source is not a free numeric emitter.
Primal vertices receive profiles from a finite generated profile system.
The human assigns primal vertices to generated profile slots.
Generated midpoint children inherit through Quark-coherent grammar.
The child must preserve parent edge, complement edge, antipodal child, Quark channels, ratio constants, derivation record, and degeneracy diagnostics.
The numeric emission tuple is only the reduced result of structured derivation.
```

Do not violate these laws.

The inspector’s proposal does not replace the source contract. It specializes and repairs it for the proving event.

The original contract allowed a simple uniform-circle v0 profile system. That was acceptable as a diagnostic scaffold. It is no longer acceptable as the proving fixture.

The new correction is:

```txt
The authored object is still the profile system, not arbitrary per-source tuples.
But the profile system must now be strong enough to differentiate the proving event.
```

This distinction is the whole matter. Do not return to free parameters. Do not return to random palettes. Do not hand-tune A, B, C, D until the children look interesting. Tune the finite system and audit its consequences.

---

## 4. Accepted core of the inspector solution

The approved proving candidate is:

```txt
Pythagorean Tetrachord Quark Regime v0
```

Its primal slots are generated from:

```txt
r0 = 1/1
r1 = 9/8
r2 = 81/64
r3 = 4/3
```

These are not concept names. They are harmonic source slots.

The human assigns the primal vertices to these generated slots:

```txt
A → one generated slot
B → one generated slot
C → one generated slot
D → one generated slot
```

The assignment is human-authored. The slots are system-generated.

The regime’s v0 emission law is:

```txt
amplitude:
  A_j = baseAmplitude

waveNumber:
  k_j = baseWaveNumber × r_j

phase:
  φ_j = phaseOrigin + 2π log2(r_j)

attenuation:
  δ_j = baseAttenuation
```

This means v0 differentiates only two axes:

```txt
active differentiating axes:
  waveNumber
  phase

neutral axes in v0:
  amplitude
  attenuation
```

The neutral axes must be marked as neutral wherever they appear in FieldCueV0 or source-signature reports. Do not let the UI imply that amplitude means “strength” or attenuation means “reach” in v0. Those meanings are not yet earned.

---

## 5. Log-ratio inheritance is not optional under this regime

The inspector’s strongest contribution is not merely the use of Pythagorean ratios. The strongest contribution is the log-ratio derivation law.

Intervals are multiplicative. Therefore interval-like quantities must be handled in log space:

```txt
ratio r
→ x = log(r)
→ weighted derivation in x
→ emitted ratio exp(x)
```

If implementation averages raw wave numbers arithmetically, it destroys the musical and structural rationale of the regime.

For a Quark channel with parent term `P60` and projection term `Q30`:

```txt
parent weight = √3
projection weight = 1

x_channel(P,Q)
=
(√3 × x_P + x_Q) / (√3 + 1)

k_channel = baseWaveNumber × exp(x_channel)
```

Phase still uses the existing weighted circular mean:

```txt
φ_channel = arg(√3 exp(iφ_P) + exp(iφ_Q))
```

For tetrahedral child `M_AB | (C,D)`, the four channels remain:

```txt
A/C
B/C
A/D
B/D
```

The log-wave-number merge gives:

```txt
x_M_AB
=
[ √3(x_A + x_B) + (x_C + x_D) ]
/
[ 2(√3 + 1) ]
```

Equivalently:

```txt
r_M_AB
=
[ (r_A r_B)^√3 × (r_C r_D) ] ^ [ 1 / (2(√3 + 1)) ]
```

This is the key structural result:

```txt
The child interval is a Quark-weighted geometric mean of:
  parent-edge product
  complement-edge product
```

This is why the child tuple is no longer a flat average of A, B, C, D. The parent edge and complement edge remain legible inside the numeric result.

Planning consequence:

```txt
This is not merely a new fixture.
It is a source-derivation regime variant.
```

Therefore it must be explicitly versioned.

---

## 6. Required versioning correction

Do not hide this under the old grammar ID if the old grammar implies arithmetic scalar means for wave number.

Introduce explicit regime / policy / grammar identification sufficient to distinguish:

```txt
uniform-circle arithmetic-scalar diagnostic fixture
```

from:

```txt
Pythagorean tetrachord log-wave-number proving regime
```

Recommended naming direction:

```txt
sourceProfileSystemId:
  pythagorean-tetrachord-primal-profile-system-v0

childInheritanceGrammarId:
  tetrahedral-quark-log-wave-number-inheritance-v0

sourcePolicyId:
  pythagorean-tetrachord-quark-proving-policy-v0

provingRegimeId:
  pythagorean-tetrachord-quark-regime-v0
```

The exact identifiers can be adjusted to repo style, but the distinction is mandatory.

A diagnostic report must be able to say:

```txt
This child wave number was derived by log-ratio / geometric mean inheritance.
```

not merely:

```txt
This child wave number was derived by weighted scalar mean.
```

The old arithmetic behavior may remain as diagnostic history or bad control. It must not silently masquerade as the new proving regime.

---

## 7. Log-ratio should be provenance, not a fifth emission parameter

Mothership decision on one inspector question:

```txt
Do not add logRatio as a new public emission parameter replacing the canonical tuple.
```

The emitted field source still supplies:

```txt
amplitude
waveNumber
phase
attenuation
```

But the source-profile and derivation records must preserve provenance:

```txt
ratio r
logRatio x = log(r)
baseWaveNumber
emitted waveNumber = baseWaveNumber × r
channel log-ratio derivation
child log-ratio merge
child emitted waveNumber
```

So `logRatio` is an internal/provenance axis of the proving regime, not a separate physical field parameter. This keeps compatibility with the existing field atlas while preserving the mathematical truth of the harmonic derivation.

A report that only shows `waveNumber` without ratio/log-ratio provenance is insufficient.

---

## 8. Pair-sum uniqueness must become a first-class audit

The inspector’s pair-sum audit is approved.

For a child such as `M_AB`, let:

```txt
S_AB = x_A + x_B
T = x_A + x_B + x_C + x_D
```

Then:

```txt
x_M_AB
=
[ T + (√3 - 1)S_AB ]
/
[ 2(√3 + 1) ]
```

Since `T` is fixed for the active assignment, differentiation on the child log-wave-number axis depends on the six parent-edge pair sums.

Therefore the audit law is:

```txt
All six parent-edge pair sums must be distinct.
```

For the tetrachord ratios, the six pair products are:

```txt
r0 r1 = 9/8
r0 r2 = 81/64
r0 r3 = 4/3
r1 r2 = 729/512
r1 r3 = 3/2
r2 r3 = 27/16
```

These are distinct.

Planning consequence:

```txt
The source-signature audit must expose pair-sum uniqueness directly.
```

Do not bury this inside generic degeneracy output. It is the mathematical proof that the proposed regime does what the failed fixture could not do: differentiate the six child signatures on a scalar axis.

But do not overclaim. Pair-sum uniqueness proves non-collapse of one source-signature axis. It does not prove mature field-world usefulness. That must be tested later through FieldCueV0 / GeneratedSiteReadingV0 legibility.

---

## 9. Base wave number is still unresolved and must be calibrated

The inspector’s proposal defines:

```txt
k_j = baseWaveNumber × r_j
```

This solves relative harmonic differentiation. It does not by itself solve geometric scale.

Wave number is meaningful only relative to the source-domain scale. If `baseWaveNumber` is inherited from the old fixture by habit, then the project has merely replaced one hidden constant with another.

This is the main layer the inspector could not fully see from their local task.

Mothership requirement:

```txt
The proving regime must include base-wave-number calibration provenance.
```

The planner must not let this become a new research swamp. The solution does not need final physics. It needs a declared v0 law or an explicit calibration audit.

Acceptable planning direction:

```txt
baseWaveNumber should be tied to a declared reference length of the proving event.
```

Possible reference choices include:

```txt
seed tetrahedron edge length;
mean active primal source spacing;
mean active source spacing after one Ambo;
closed-surface characteristic length.
```

Do not choose by visual taste. Do not choose by copying `Math.PI`. Do not choose by searching for pretty route/gate patterns.

A clean v0 may use a simple reference-wavelength law such as:

```txt
baseWaveNumber = 2π / L_ref
```

where `L_ref` is explicitly declared and computed from the proving event geometry.

The exact `L_ref` decision belongs to the planner/human implementation conversation. The non-negotiable requirement is that the diagnostic report says:

```txt
baseWaveNumberCalibrationStatus
baseWaveNumber
referenceLengthKind
referenceLengthValue
calibrationRationale
```

If calibration remains provisional, mark it provisional. Do not hide it.

---

## 10. Amplitude and attenuation are neutral axes in v0

The inspector recommends keeping amplitude and attenuation constant in v0. Mothership accepts this.

But neutrality must be visible.

Do not let generated-site readings display amplitude and attenuation as if they carry source meaning. In v0:

```txt
amplitude = neutral constant
attenuation = neutral constant
waveNumber = active harmonic differentiation axis
phase = active harmonic differentiation axis
```

This is not a weakness if stated honestly. It is a correct refusal to invent metaphysics of strength and reach before a law has been earned.

Future variants may explore amplitude and attenuation laws. Do not include them in the first proving fixture unless the human explicitly reopens the source-signature research problem.

---

## 11. Human assignment provenance is not optional

The profile system generates harmonic slots.

The human assigns concepts/vertices to those slots.

This assignment is not a small implementation detail. It is the first act by which the primal conceptual tetrahedron becomes field-active.

Every proving report and FieldCueV0 generated under this regime must preserve assignment provenance:

```txt
which vertex was assigned to which harmonic slot;
which ratio that slot carries;
which emitted primal tuple follows;
whether the assignment is default, human-authored, provisional, or test fixture;
which child signatures depend on that assignment.
```

The system must not say:

```txt
A means 1/1.
```

It must say:

```txt
In this active setup, vertex A is assigned to harmonic slot 1/1.
```

This distinction protects the human’s future freedom to test alternate assignments without rewriting ontology.

---

## 12. The old uniform fixture remains as bad control

Do not delete the failed fixture.

Reclassify it:

```txt
uniform-circle scalar-invariant fixture = bad control fixture
```

Its purpose is now:

```txt
show scalar invariance;
show phase-symmetry risk;
show fallback/collapse risk;
show why profile-aware does not mean proving-regime solved;
serve regression comparison;
prevent false improvement claims.
```

The proving audit should compare at least:

```txt
uniform-circle control
Pythagorean tetrachord proving regime
```

If the new regime does not outperform the bad control on the required source-signature gates, it is not accepted.

---

## 13. Two-stage acceptance gate

Do not collapse technical source-signature success into human legibility success.

There are now two gates.

### Gate 1: Source-Signature Proving Audit

This gate asks:

```txt
Does the regime produce field-ready, non-invariant, traceable child source signatures for the one-Ambo tetrahedron proving event?
```

Minimum pass conditions:

```txt
four primal profiles generated from named finite system;
human/profile assignment recorded;
waveNumber differentiated by harmonic ratio;
phase differentiated without full-circle symmetry;
all six child signatures field-ready;
zero fallback for the proving event;
zero unresolved child signatures;
no undefined circular phase merge;
child log-wave-number signatures non-identical;
pair-sum uniqueness passes;
every child signature traces through parent edge, complement edge, projection vertices, Quark channels, merge law, and antipodal relation;
baseWaveNumber calibration provenance present;
amplitude and attenuation marked neutral;
uniform control comparison included.
```

Passing Gate 1 means:

```txt
The source-signature substrate is provisionally repaired.
```

It does not mean:

```txt
The field-cue has borne fruit.
```

### Gate 2: Generated-Site Legibility Audit

This gate asks:

```txt
Does FieldCueV0 / GeneratedSiteReadingV0 become more legible after switching from the bad control to the proving regime?
```

Minimum pass conditions:

```txt
one FieldCueV0 per generated midpoint child;
source signature shown as earned inheritance, not fixture residue;
provenance visible enough that displayed numbers are not mysterious;
field participation status remains honest;
candidate field-feature refs are not inflated into mature routes/gates/regions;
naming pressure questions become sharper than before;
unsupported, weak, saturated, or sensitive field participation is visible;
no topology, packet writing, or automatic naming is introduced.
```

Passing Gate 2 means:

```txt
The field witness can resume as part of generated-site reading.
```

Only after Gate 2 should UI refinement resume.

---

## 14. Relation to FieldCueV0

FieldCueV0 remains authorized, but it is currently downstream of a failed source-signature regime.

Current classification:

```txt
FieldCueV0 model direction: aligned
FieldCueV0 current trustworthiness: blocked by source signatures
GeneratedSiteReadingV0 model direction: aligned
GeneratedSiteReadingV0 current trustworthiness: blocked by source signatures
UI over these layers: provisional and potentially misleading
```

The Field-Cue Target Contract remains alive. Do not weaken it.

The mature field-cue still demands:

```txt
child-source inheritance;
field-world participation;
routes, gates, loops, vortices, regions, phase transformations where warranted;
visual/interference behavior;
naming pressure without naming conclusion.
```

But the active work now returns to the first axis:

```txt
What field signature did this generated child inherit?
```

The second axis cannot be trusted until the first one is no longer a fixture artifact.

---

## 15. Relation to route/gate/region/loop/vortex ambitions

Do not read this note as a retreat from the human’s desired field-cue horizon.

The human wants the field-cue to eventually show how a child-source’s waves are carried, blocked, bent, echoed, routed, gated, looped, regionally absorbed, or transformed. That remains valid.

The current restriction is not:

```txt
routes and gates are dangerous words.
```

The restriction is:

```txt
routes, gates, regions, loops, vortices, and phase transformations cannot become meaningful field-cue witnesses if source signatures are unearned.
```

No new feature family is permitted in the proving-regime repair branch. Existing candidate machinery may be used later to test whether the new source regime improves FieldCueV0. But the next branch must not add new route/gate/support/region taxonomies, visual animation, or field-feature maturation.

The field must eventually become rich. It must first stop speaking from a false throat.

---

## 16. Relation to topology

Topology remains outside this correction.

Do not use topology as an escape from the source-signature problem.

Do not make topological supports necessary before FieldCueV0.

Do not export proving-regime outputs into topology.

The only topological relevance here is distant and negative:

```txt
A future topological unit must not inherit unnamed or falsely signed material.
```

That is not a branch. It is a reason to repair source signatures now.

---

## 17. Symbolic research boundaries

The inspector explored neighboring constellations. The exclusions are part of the solution.

Keep out of v0 active tuple law:

```txt
Keplerian shell scaling;
Tetractys/Decad tuple assignment;
Kingmaker / 90-45-45 / √2;
H3 / golden / pentagonal atoms;
Trison / nonadic feedback.
```

These are not rejected as future symbolic material. They are rejected as first proving-regime tuple laws.

Specific notes:

```txt
Keplerian tetra→octa √3 shell relation:
  may be recorded as optional provenance or research note;
  must not scale waveNumber or attenuation in v0;
  risk = double-counting geometry.

Tetractys/Decad:
  useful closure audit: 4 primal + 6 generated = 10 sites;
  not source tuple assignment law.

Kingmaker / √2:
  belongs to square/axis/diagonal regimes;
  not tetrahedral Quark v0.

H3 / golden:
  belongs to icosahedral/dodecahedral or pentagonal closure problems;
  not one-Ambo tetrahedron v0.

Trison / nonadic:
  possible future residual/fold grammar;
  not parental source-profile tuning.
```

This is discipline, not timidity. A first proof must know what it refuses.

---

## 18. Reading order for the planner

Do not re-read the whole project indiscriminately.

Read deeply:

```txt
1. Extraordinary Notice: Source-Signature Regime Failure and Required Divergence
   Purpose: understand the actual blockage and why UI refinement must pause.

2. Inspector Report: Source-Signature Proving Regime
   Purpose: understand the Pythagorean Tetrachord Quark Regime v0 proposal.

3. Field Source Profile and Quark-Coherent Child Inheritance Contract v0.2
   Purpose: preserve the original source contract: no arbitrary emitters, finite profile systems, Quark child inheritance, edge/complement/antipodal structure.

4. Event Legibility Pivot Charter
   Purpose: remember why this correction serves event legibility, not infrastructure accumulation.

5. Field-Cue Target Contract v0.2
   Purpose: remember the field-cue horizon and why source-signature repair is not a retreat from it.
```

Read lightly:

```txt
Ground Plan:
  use for layer boundaries, not for near-term tasks.

Field Layer Appendix:
  use for canonical field definition and source-domain discipline.

Old field handoffs:
  use for history and repo map only.
```

Avoid as near-term planning authority:

```txt
old next-branch recommendations;
old UI exposure plans;
old topology horizon notes;
any handoff that predates the Event Legibility Pivot;
any report that treats evidence stability or candidate count as field success.
```

The repo remains factual authority. The documents govern strategic permission.

---

## 19. Repo navigation for this correction

Before writing any branch plan, inspect the actual repo and report the current factual state. The project has repeatedly advanced faster than handoffs.

Likely files involved:

```txt
src/lib/fieldSourceProfiles.ts
src/lib/fieldSourceChildContexts.ts
src/lib/fieldSourceQuarkChannels.ts
src/lib/fieldSourceChildDerivations.ts
src/lib/fieldSourceChildDegeneracy.ts
src/lib/fieldSourceProfileAwarePolicy.ts
src/lib/fieldSourceProfileAwareAtlasAdapter.ts
src/lib/fieldSourceProfileAwareAtlasExecution.ts
src/lib/fieldSourceProfileAwareShapePositionResolver.ts
src/lib/fieldSourceProfileAwareShapeResolvedSurfaceAtlas.ts
src/lib/fieldSourceProfileAwareFeatureReport.ts
src/lib/fieldSourceProfileAwareRouteGateCandidates.ts
src/lib/fieldSourceProfileAwareSupportRegionCandidates.ts
src/lib/fieldSourceProfileAwareEvidenceStability.ts
```

Also inspect any newer files if present:

```txt
FieldCueV0 builders / reports
GeneratedSiteReadingV0 builders / reports
SourceSignatureContractAuditV0 files
Field Mode UI / generated-site reading UI files
package.json diagnostic scripts
```

Do not assume exact file names from this note. Verify.

The audit must not mutate `Shape`, write packets, register operations, or alter historical geometry behavior.

---

## 20. Planning sequence now authorized

This is a planning sequence, not a Codex prompt.

### Step 0: Verify current repo state

Before planning implementation, verify:

```txt
which FieldCueV0 / GeneratedSiteReadingV0 files exist;
which source-signature audit files exist;
whether the uniform fixture is currently hard-coded as default;
whether waveNumber derivation currently uses arithmetic scalar means;
whether fallback children exist under the current fixture;
which diagnostics already expose source-signature failure.
```

### Step 1: Define the proving regime as named, finite, and event-bound

Add or plan a named profile system for:

```txt
Pythagorean tetrachord ratios:
  1/1, 9/8, 81/64, 4/3
```

This should be generated from a system definition, not hand-authored tuples.

### Step 2: Introduce log-wave-number derivation as a versioned regime

Do not silently change all scalar means. This is not a global replacement of field math.

Only the interval-like waveNumber axis in the Pythagorean proving regime requires log-ratio / geometric derivation.

Amplitude and attenuation stay neutral constants in v0.

Phase uses circular mean and must be audited.

### Step 3: Add base-wave-number calibration provenance

Do not leave `baseWaveNumber` as a ghost constant.

At minimum, reports must expose:

```txt
reference length kind;
reference length value;
base wave number;
calibration rationale;
calibration status.
```

### Step 4: Extend source-signature audit

The audit must compare:

```txt
uniform-circle bad control
Pythagorean tetrachord proving regime
```

It must report:

```txt
structuralContractStatus
provingFixtureUsefulnessStatus
provingEventSignatureStatus
humanLegibilityStatus
pairSumUniquenessStatus
baseWaveNumberCalibrationStatus
activeDifferentiatingAxes
neutralAxes
fallbackCount
unresolvedCount
phaseCancellationStatus
misleadingSignatureUiRisk
```

### Step 5: Gate before switching downstream readers

Only after source-signature audit passes may FieldCueV0 / GeneratedSiteReadingV0 proving-event builders switch from the failed uniform control to the proving regime.

This switch must preserve provenance:

```txt
regime id;
profile system id;
assignment id;
child inheritance grammar id;
ratio/log-ratio derivation;
base wave-number calibration;
neutral-axis status;
bad-control comparison.
```

### Step 6: Re-evaluate generated-site legibility

After the switch, rerun FieldCueV0 / GeneratedSiteReadingV0 diagnostics.

The question is no longer:

```txt
Did the code produce a cue?
```

The question is:

```txt
Does the cue now show earned inheritance that helps the human read the site better than before?
```

Only then resume UI refinement.

---

## 21. Forbidden moves

Do not proceed with:

```txt
FieldCueV0 UI polish before source-signature audit passes;
GeneratedSiteReadingV0 product claims before source-signature audit passes;
arbitrary source sliders;
random tuple palettes;
manual hand-fitting of A/B/C/D to desired child readings;
hiding fallback as a valid proving-event state;
calling neutral amplitude/attenuation meaningful;
using arithmetic waveNumber means under the tetrachord regime;
copying baseWaveNumber = π without calibration rationale;
claiming pair-sum uniqueness proves field-world usefulness;
adding routes/gates/regions/loops/vortices machinery;
visual animation;
topology;
packet writing;
semantic auto-naming;
Shape mutation;
operation registration;
calling the tetrachord regime the final source metaphysics;
calling it the general field layer.
```

---

## 22. What success looks like

The immediate success state is:

```txt
The project can produce four primal source signatures and six child source signatures under a named, finite, non-arbitrary proving regime.
```

A successful source-signature proving report should be able to say:

```txt
The old uniform fixture fails as control.
The tetrachord proving regime passes pair-sum uniqueness.
All six child signatures are field-ready.
No fallback is used.
No unresolved child signature exists.
No phase circular-mean cancellation occurs.
WaveNumber is differentiated by harmonic ratio and log inheritance.
Amplitude and attenuation are neutral by design.
Base wave-number calibration is declared.
Every displayed number has provenance.
```

The next success state is:

```txt
FieldCueV0 and GeneratedSiteReadingV0 use those earned signatures to make each generated child site more legible to the human.
```

The final near-term success state remains:

```txt
A human can place four primal concepts on a tetrahedron;
after one Ambo, inspect six generated child sites;
see what each child inherited as a field source;
see what field participation is available or unavailable;
and attempt naming with more support than geometry alone.
```

---

## 23. Mothership compact for the planner

The inspector gave us the first plausible answer to the source-signature crisis.

Do not trivialize it into “use musical ratios.”

The real answer is:

```txt
finite harmonic profile system
+ human assignment to generated slots
+ log-ratio inheritance
+ Quark √3 parent/projection weighting
+ pair-sum uniqueness audit
+ base-wave-number calibration
+ neutral-axis honesty
+ bad-control comparison
+ generated-site legibility test
```

This is the source-signature proving regime.

The field-cue horizon remains mad and large. It must eventually speak of inherited pressure, field-world participation, routes, gates, loops, vortices, regions, phase transformations, visual interference, and naming pressure.

But the next branch is not there.

The next branch is the throat of the field.

Make the child source sing lawfully first.

Then let the field witness speak.
