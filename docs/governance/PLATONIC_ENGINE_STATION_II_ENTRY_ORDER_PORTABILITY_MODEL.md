# PlatonicEngine Station II Entry Order
## Field Portability Model

Audience: the prompter/planner/auditor (lieutenant).

Status: mothership authorization for Station II entry. Not an implementation prompt — the lieutenant plans the runs. Binding within the midterm campaign plan; where this order is silent, the campaign plan rules.

SEQUENCING AMENDMENT (2026-06-11, same day, before any Station II work began):
the human revised Decision D1 from deferred to immediate execution. This order
now TAKES EFFECT ONLY AFTER the D1 lift branch is complete and ratified (see
PLATONIC_ENGINE_D1_LIFT_ENTRY_ORDER_HUB_LAYER_SOURCE_STATE_CAPSULE.md).
Deliverable 3 (the D1 rider / slot-specification input) is struck — the lift
will already exist; in its place, Station II consumes the post-lift survival
re-audit results as input to the portability table.

Issued by: mothership, 2026-06-11.

Repo state anchor at issuance:

```txt
branch: Claude-child
5625d10 Add Station I medial-carrier source-state survival audit diagnostic
Station I: CLOSED and ratified
Decision D1: (a) scoped schema-lift, executed immediately after Station II
```

## 1. Station II question

```txt
What exactly would a second instance have to reuse?
```

Classify the entire C0→G0 stack, object by object, into the campaign-plan statuses:

```txt
portable-field-architecture
shape-policy-required
carrier-policy-required
spatial-support-policy-required
instance-specific
blocked-until-second-instance
unsafe-to-universalize-before-proof
```

This is the X0 audit of the crisis handoff, finally executed, now informed by the Station I survival matrix.

## 2. Deliverables (exit criteria)

```txt
1. The portability table over the C0→G0 artifacts
   (C0-R1 carrier table, C1 channels, A0 associator/displacement,
    E0 profiles, E1 envelopes, F1 graph field, F2 spatial supports,
    G0 generational update — at the object level, not the file level).

2. Formal statement with explicit status for BOTH hard problems:
   cube primal sourcehood (status formally DUE here);
   deeper-generation bracketing (preliminary statement; full status due
   Station III, but Decision D2 needs at least a candidate framing).

3. D1 rider — lift design input:
   for each Station-I-orphaned object, record what a hub-layer source-state
   capsule must provide for that object to instantiate (slot specification
   only; NO schema work, NO source-state edits in Station II).

4. Decision D2 recommendation with reasoning:
   choice of second instance to maximize stress on the carrier policy.
   Default expectation: octa first-birth. Cube only via dual provenance
   until sourcehood is solved. Tetra G2 only with a candidate bracketing
   policy. D2 is decided by mothership/human; the lieutenant recommends.

5. Station II closing memo drafted for mothership ratification.
```

## 3. Constraints

```txt
Diagnostic/audit-only station. No mutation of C0→G0 modules, no
source-state edits (the D1 lift is a SEPARATE branch after this station),
no FieldCueV0, no UI, frozen list unchanged.

Classification must be derived where derivable (consume existing report
builders; locator/extractor discipline as in Station I) and declared as
judgment where judgment is unavoidable — judgment rows must be marked as
such, never disguised as computation.

Branch budget: 1–4 runs plus the memo. A fifth run is the drift signature —
stop and escalate.

Mock-solution and count-vs-structure tests apply to every audit.
Fake abstraction (renaming tetra structures to generic names without
re-derivation) voids any pass it produces.
```

## 4. Sequencing after Station II

```txt
Station II closes (ratified)
→ D1 lift branch: hub-layer source-state capsule v0, designed against
  the portability table + D1 rider slot specs (fresh authorization)
→ Station III entry (conditional on lift completion), instance per D2.
```

Report per the initiation's protocol after every run. Plan check standard applies: "Station II, which run, what closes it" — one sentence, always available.
