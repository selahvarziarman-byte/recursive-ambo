# PlatonicEngine Station IV Entry Order
## Propagation / Field-Activity Survival, and the FieldCueV0 Proof Surface (Decision D4)

Audience: the prompter/planner/auditor (lieutenant).

Status: mothership authorization for Station IV entry. Not an implementation prompt — the lieutenant plans the runs and submits each plan for mothership audit before any implementation prompt to Claude Code. Where this order is silent, the campaign plan and the ratified amendments rule.

Issued by: mothership, 2026-06-11.

Repo identity (per the binding Repo-Identity Gate Protocol):

```txt
Canonical repo:   C:\Dev\202cl\PlatonicEngine202   (native Windows is sole authority)
Canonical branch: Claude-child
Entry anchor:     513b1a2 Ratify Station III Bench 2 closing memo
Decoy warning:    C:\Dev\PlatonicEngine is NOT the campaign tree.
```

The gate (path + branch + HEAD, all three) is the mandatory preamble of every implementation prompt and every report in this station. Stop conditions apply. Sealed predictions are filed to mothership BEFORE each run (Station III lesson, now law).

---

## 1. Why this station is decisive

Everything the campaign has proven stops at the boundary of source state:

```txt
PROVEN (Stations I-III, D1 lift):
  - the medial-dual carrier policy is source-state-real AS STRUCTURE at the
    hub layer (D1 lift: fiberSurvives=true in the hub capsule);
  - the policy GENERALIZES: at root-level (R12) resolution, octa first-birth
    reproduces the hub's order-2 and order-3 invariants in both registers,
    surviving the carrier->space projection, gauge-invariant (D3 confirmed);
  - the "no bare units" law is general, not tetra-local.

NOT PROVEN (this station's burden):
  - that ANY of this structure survives into field-facing behavior;
  - that any of it is witnessable from propagated field data;
  - that it improves the human act of reading generated sites — the
    project's original promise (Event Legibility Pivot).
```

The campaign's standing caveat, attached at the D1 lift and repeated at D3, comes due here:

```txt
"source-state-real AS STRUCTURE only; tuple-projection-lost; nothing
field-active; field-activity is Station IV territory."
```

And the oldest open wound in the project is on this station's docket. Gate C.4 (2026-06-09) proved that structure can exist upstream and be destroyed by propagation: R4-S1 passed emitted-source recovery and FAILED raw field-behavior recovery; the C.4D residual was ambiguous; Gate C.5 resolved it only by introducing the structural projection. The hard-problems registry has carried "survival through propagation — status due at Station IV" ever since. This station IS that status.

If the fiber survives into honest field-facing witness and improves reading: Verdict-A direction holds into Station V. If it survives only as source-state structure that no field projection can witness: that is Verdict-B material — recorded, not papered over. Both are successful station outcomes. The only failure is an unresolved middle or a dishonest pass.

## 2. Station structure — two sub-stations, strictly ordered

```txt
IV-A  Propagation / field-activity survival audit   (diagnostic-only)
        -> IV-A closing memo ratified by mothership
IV-B  FieldCueV0 / GeneratedSiteReadingV0 proof surface  (consumption adaptation)
        -> Decision D4 (the human-fruit test), escalated to mothership + human
```

IV-B may not begin until IV-A's memo is ratified. IV-B's design depends on IV-A's outcome — which relations are field-witnessable and under which statuses determines what FieldCueV0 may honestly say.

Budget: each sub-station is plan-mode-first, then 1–3 implementation runs plus its memo. A fourth implementation run in either sub-station is the drift signature: stop, escalate.

## 3. Sub-station IV-A — propagation / field-activity survival

### 3.1 The question

```txt
For each fiber relation now source-state-real at the hub layer
(signed Fano lift, triangle closure, square holonomy, ordered flag
identity, orientation sign, carrier ray / antipodal axis), and for the
provenance routes:

  Which Gate C.5 visibility status does it attain in field-facing
  projections — DERIVED from propagated or projected field data,
  not asserted from source state?
```

The full C.5 status vocabulary is the required classification axis:

```txt
raw-visible
propagation-transformed
depropagation-recoverable
structural-channel-visible
source-state-only
tuple-projection-lost
misleading-if-read-as-raw-field
unsupported
```

Every relation gets a derived status. "Field-active" may be claimed for a relation ONLY if it is witnessed from field-facing data under a declared, pre-registered basis — never because the source state contains it.

### 3.2 The substrates (read-only inputs; the plan chooses and justifies)

```txt
Accepted field-facing stack (tetra hub route):
  F1 carrier graph field        (fanoOctonionicCarrierGraphFieldV0)
  F2 spatial support projection (fanoOctonionicSpatialSupportProjectionV0)
  G0 generational field update  (fanoOctonionicGenerationalFieldUpdateV0)

Legacy scalar substrate (read-only; historical witness):
  fieldAtlas / fieldSampler and the multi-projection consumption path
  (structuredSourceStateMultiProjectionStructuralChannelV0,
   fieldCueV0MultiProjectionConsumption)

Source-state regions:
  tetra-G1 capsule (FROZEN), hub-layer capsule (extend-only),
  v0/v1 survival audits (FROZEN as evidence)
```

The IV-A plan must declare which substrate(s) carry the propagation test and why, and must state the projection basis explicitly (the C.5 lesson: visibility claims are basis-relative; "structural-channel-visible under declared basis X" is honest, "visible" is not).

Scope control: the propagation test runs on the TETRA HUB route first, where the accepted F1/F2/G0 stack exists. An octa-route field stack does NOT exist and is NOT to be invented inside IV-A. The octa route's field-level status is recorded as `blocked-until-octa-field-stack` unless the lieutenant shows a bounded, honest way to read it through existing substrates — escalate before building anything new for it.

### 3.3 The blind-recovery discipline (non-negotiable)

This station faces the exact failure mode Gate C.4 was built to catch. The strongest honesty machinery the project owns is reused:

```txt
- anonymized sources: the recovery side receives field-facing data with
  source identities blinded;
- controls: structured controls AND strict all-control baselines (the
  C.4 / C.4D pattern); the fiber must OUTPERFORM controls, not merely
  score;
- pre-registered pass/fail boundaries: what recovery rate, against which
  controls, under which basis, counts as which status — sealed and filed
  to mothership BEFORE the run;
- no target-matching: no upstream "reference law" used as pass criterion;
  raw values read against blind controls (the Bench-2 pattern);
- recompute-not-echo: field-facing values recomputed from the substrate's
  atoms; consuming a source-state report to "find" structure in the field
  is the mock-solution failure of this station;
- the F1-complement and orientation-sign precedents apply: a count that
  passes while sign/orientation structure is flattened is a failure.
```

### 3.4 IV-A exit criteria

```txt
1. Every fiber relation + provenance route classified with a derived
   C.5 visibility status, under a declared basis, with blind-recovery
   evidence where field-activity or recoverability is claimed.

2. The hard-problems registry entry "survival through propagation"
   receives its formal status — whatever the answer.

3. Honest negative handling: if no fiber relation exceeds
   source-state-only, that is the finding. It is NOT failure of IV-A.
   It reshapes IV-B (readings may cite source-state structure with
   honest statuses) and feeds the final verdict as Verdict-B material
   for the fiber's field role.

4. IV-A closing memo drafted for mothership ratification, including
   the anomaly ledger pattern (any unexpected measurement listed and
   derived or carried as unexplained — never silently dropped).
```

## 4. Sub-station IV-B — the FieldCueV0 proof surface and Decision D4

### 4.1 The blockade lifts here, and only here

FieldCueV0 and GeneratedSiteReadingV0 have been blocked as trusted witnesses since 2026-06-09. This order authorizes their adaptation — consumption changes only:

```txt
ALLOWED:
  - FieldCueV0 consumes the IV-A-classified, generalized source-state /
    field-facing truth (hub capsule content with its derived statuses);
  - GeneratedSiteReadingV0 inherits the revised FieldCueV0 honestly;
  - existing mounted panels and preview scripts updated to DISPLAY the
    new consumption (existing surfaces only);
  - every displayed relation carries its C.5 status visibly — a reading
    may cite source-state-only structure, but it must SAY so.

NOT ALLOWED:
  - new UI surfaces, panels, or visual families (UI is Station V);
  - any claim of field-activity for a relation IV-A did not so classify;
  - tuple-as-signature regressions (the sovereignty law stands:
    source signature = structured source state; emitted tuple = reduction);
  - automatic naming, packet writing, semantic truth claims (S0 remains
    blocked; naming authority is the human's, at Gate-H level, later).
```

### 4.2 Decision D4 — the human-fruit test

The campaign plan's words: the human, not a diagnostic, judges. Operationally:

```txt
The proving event (Event Legibility Pivot): tetrahedron -> one Ambo ->
six generated midpoints. The human reads the generated sites through the
revised GeneratedSiteReadingV0 (preview scripts / existing panels) and
judges against the pre-campaign scalar readings:

  (a) do the readings make the six sites MORE legible — easier to read,
      distinguish, and (eventually) name?
  (b) is the fiber structure (axes, opposition, closure, provenance)
      presented honestly, statuses visible, nothing overclaimed?
  (c) is anything in the reading misleading-if-read-as-raw-field?

D4 outcomes (escalated; decided by human + mothership):
  improves, honest        -> Verdict-A direction; proceed to Station V
  generalizable but does
  not improve reading     -> Verdict-B consideration; recorded, not hidden
  misleading in any part  -> stop; repair or demote before any Station V
```

The lieutenant prepares the D4 sitting (what to run, what to compare, what to look at) but does not vote. Mothership verifies the comparison is fair: the pre-campaign baseline must be the real historical reading, not a strawman.

## 5. Hard problems at this station

```txt
survival through propagation   -> FORMAL STATUS DUE at IV-A exit (this is the station).
cube primal sourcehood         -> travels as BOUNDARY, not blocker. Any cube-route
                                  content in any field projection must preserve the
                                  dual-provenance-only wording. No 8-vs-7 work here.
deeper-generation bracketing   -> the §11 convention stands (left-association of
                                  record). Station IV touches only first-birth
                                  content (G0->G1). ANY contact with deeper
                                  generations must open with a bracketing-
                                  sensitivity test — or be escalated and deferred.
```

## 6. Standing constraints (frozen list, station-specific form)

```txt
- tetra-G1 capsule, v0 audit, v1 audit, Bench 2: FROZEN as ratified evidence;
- hub capsule: extend-only if IV-A needs projection slots — prefer a NEW
  projection module CONSUMING the capsule over any capsule edit; capsule
  edits require mothership pre-authorization;
- no Shape mutation, no packet writing, no operation registration;
- no topology; no S0; no new route/gate/support/region families;
- no field atlas replacement (read-only consumption permitted);
- no general harmonic universe, no multi-shape field expansion beyond
  what D3 proved at carrier/source-state level;
- no universal-law language: field-facing claims are basis-relative and
  scoped to the tested instance(s);
- exact-path staging; no commit before audit; native gate before every
  instruction (the Protocol).
```

## 7. Process and reporting

```txt
- IV-A Run 1 = plan only: substrate choice + projection basis + blind-recovery
  design + sealed predictions -> mothership audit before implementation.
- Expected-outcome matrices: held by the lieutenant as withheld audit
  baselines; never given to the implementer (teaching-to-the-test
  discipline, unchanged).
- Per-run reporting: station/sub-station/run, gate output, diff scope,
  verbatim diagnostic output, audit verdict, distance to exit.
- Mid-station checkpoint: IV-A memo ratified before any IV-B work.
- Mothership verifies independently at each memo (re-run, source
  spot-checks, scope) — as at every prior closure.
```

## 8. What Station IV must NOT become

```txt
- not a wholesale rerun of the Gate C ladder — reuse its discipline, not
  its backlog;
- not a field-feature renaissance (routes/gates/regions stay frozen);
- not an octa field stack construction project (escalate if needed);
- not a UI sprint (Station V exists);
- not S0 by the back door: structure may be DISPLAYED with statuses;
  meaning is not assigned, names are not written.
```

## 9. Sequencing after Station IV

```txt
IV-A memo ratified -> IV-B authorized -> D4 sitting -> D4 decided
  D4 = improves, honest  -> Station V entry order (3D field-map target
                            contract; contract first, render second)
  D4 = does not improve  -> verdict deliberation (B direction) with
                            evidence; campaign plan section 1 governs
  D4 = misleading        -> repair under fresh authorization, or demote
```

Plan check standard: "Station IV-A (or IV-B), which run, what closes it" — one sentence, always available.

The station's character, inherited from every serious agent before it:

```txt
Do not preserve the field layer out of loyalty. Try to break it.
If it survives propagation, specify exactly how it is witnessed.
If it does not, say exactly where it dies.
```
