# Cascade Driver Campaign — Mothership Closeout (ratified 2026-06-22)

**Verdict: CLOSED.** The cascade driver — ADR 0004's systemic-trace fixpoint closure, the module's deepest layer — is built, sealed-before-code, and verified on the real engine by independent mothership audit (clean-mount re-run + reading the seal's checks, not the report).

## Verified, not taken on report

- **Seal-before-code** — git order: the acceptance seal `ad11299` (SHA-256 `029f1d99` of the off-repo plaintext) precedes every driver-code commit (`cb4d3fd` … `6fb55a0`). Hash-integrity of the plaintext rests on the seat process; the commit order confirms it was sealed before the build.
- **The seal has teeth** (the collapse-gap lesson, applied): the 77 cascade checks *encode* the clauses, not pattern-only — μ strictly down on real counts (18→10, 7→5) with no cell created; **confluence via reversed-seed → identical partition** (a real order-independence test); w₁ from the 2-colouring with the **sign-blindness discriminator** (flip vs control: same μ, opposite w₁); ∂ᵀ removal forcing the coface upward; the heterogeneous merge annotated-and-continues.
- **Live on a healed mount, mothership run:** cascade 77 · level2-zoo 86 · registry 117 · ledger 104 · loop 31 · cut 19 — all ALL PASS; `npm run build` clean.
- **The `collapseFace` fix (`6e7cf83`) closed the bug and the ratification gap:** `collapseFace` reports S² (χ=2, manifold); all six surface seals now test χ (level2-zoo 77→86). A surface seal now tests the surface.

## What this means

The deepest frontier is closed. The module runs the full loop on the engine's own connected material — operation → systemic cascade (fixpoint) → faithful/oriented/honest trace — derive-only, certifiers single-sourced (faithfulness law + orientation certifier reused read-only). The working module's core is real.

## Standing flags (accepted, honest)

1. **Removal-honesty (∂ᵀ with lineage) not exercised** — the seal didn't require it; optional future slice. (Connects to `collapseFace`'s edge-collapse not yet logged as a faithfulness-tracked removal.)
2. **`collapseFace` ↔ `runCollapseCascade` agree topologically** (the sanctioned fallback: separate functions + agreement seal χ=2 / V1·E0·F1), not literal single-source — consistent with the fix charter. Literal single-source (one boundary-quotient primitive both call) is a clean optional follow-up.
3. **Scope is single/two-face by design** — the seal's scope is the claim's scope; larger complexes untested, no claim made.
4. **Mount torn on uncommitted audits, healed post-commit** — handled correctly (real-FS Read + hand-trace + native arbiter; honest "couldn't re-run myself" each time). Native runs remain the authority.
5. **Two untracked engineer charters** (W1-orientation, collapseFace-fix) — TRACK with the trail (Arman's native commit).

## Seat process — held

Cross-office audit on every diff (Read of the real artifact + hand-trace + independent arbiter run); nothing implemented by the engineer; every commit fired natively on an exact path scope (never `git add -A`, given the CRLF phantom wall); the acceptance target sealed as a hash before driver code existed; predictions off the working diff; the gate never waved through on a torn-mount excuse. The seat held under real adversity — credit.

## Optional next targets (none owed)

(a) removal-honesty ∂ᵀ · (b) literal single-source of the collapse primitive · (c) the driver on a larger complex. The sovereign's call whether and which. The open theoretical tail (the `multiset = lineage` injectivity proof, verified to 62 leaves) stands as before, unaffected.
