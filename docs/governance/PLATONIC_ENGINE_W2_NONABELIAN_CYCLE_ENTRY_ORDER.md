# PlatonicEngine — W-2 Non-Abelian Cycle: Entry Order (lean)

Audience: researcher (abducts), lieutenant (builds), Arman (sovereign).
Status: mothership authorization. **Cycle 1 of charter N=3.** Issued 2026-06-14, branch `team-arman`. Sovereign chose the non-abelian shot over closing.

This is deliberately one page. The seal thread is **closed** (`PLATONIC_ENGINE_W2C_MOTHERSHIP_RULING_SEAL_REMOTE_EXPOSURE.md`); the discipline below is background, not a tribunal.

## What this follows from

```txt
W-1 (ratified): the walk-primitive regime is LEGITIMATE. Its PRIZE = a gauge-equivariant
  signed holonomy on the 280 off-Q (octonionic-sector) loops — DEFINED exactly where the
  old unit policy W_0 was only sign-ambiguous. This is the one quantity that makes W > W_0.

W-2 abelian (terminal verdict, mothership): FIELD-INACTIVE. No label-free ABELIAN field
  carries the prize — GF(2)/Z2 abelian-representability theorem; Z2 is the abelian ceiling.
```

## The question (this cycle)

```txt
Can a field whose holonomy is valued in a NON-ABELIAN (or NON-ASSOCIATIVE) structure carry
the 280-loop prize that the abelian field provably cannot — recovered by a LABEL-BLIND
observer, beating controls INCLUDING the bare-geometry control, with no label leakage?
And does it PREFER one of the three W-1 bracketing branches (Hole #2: agree 192/280,
diverge 88), closing that residual to a unique selection?
```

## Bounded frame (finite-hole law — the one binding constraint)

```txt
- Holonomy carrier G: a NAMED, enumerated set. The set deliberately SPANS the two
  hypotheses so the cycle also tells us WHICH property does the work:
      non-abelian but associative:   Q8 (quaternion group),  2O (binary octahedral)
      non-associative:               the unit-octonion MOUFANG loop (order 16)
  Researcher SELECTS and JUSTIFIES one (or a named addition, with reason). No "some
  non-abelian group, somehow" — that is forbidden motivated design.

- Observable: an automorphism-gauge-INVARIANT CLASS FUNCTION (character / trace) of the
  loop holonomy in G. Gauge = the established legitimate one (the 168 = PSL(2,7) Fano
  automorphisms + sign-lifts, Gate 0). Relational / loop-borne — NEVER one scalar per
  source (the IV-A death). Label-free.

- The discriminator that EARNS the cycle: the observable must reproduce the W-1 prize
  sign-pattern in the off-Q sector where Z2 cannot, AND beat the bare-geometry control.
  If it collapses to the abelian case, or only matches bare geometry, that is SHAPE, not
  field -> FIELD-INACTIVE, recorded.
```

## Minimal discipline (cheap, background)

```txt
relational not scalar · Addition-C bare-geometry control · blind-in-code ·
sealed VALUE-predictions off-repo, one shot · .gitignore already guards seal hygiene.
That is the whole list. No custody tribunal (see the seal ruling §6 downgrade).
```

## Flow and verdict

```txt
researcher -> W-2.A(non-abelian) field-reduction model card: the chosen G + justification,
  the class-function observable, the recovery map Rec(O), the bare-geometry control, the
  branch-selection test, sealed values
-> mothership ONE-PASS audit (relational? anti-staple? bare-geometry present? real
   discriminator distinguishing it from the abelian case?)  [fast; no ceremony]
-> lieutenant blind diagnostic (Riders A/B; g>=0.90 sign score)
-> close -> mothership terminal verdict:

  FIELD-OBSERVABLE  -> the project's FIRST field, scoped to the anchored lineage. Charter
                       advances over a real object.
  FIELD-INACTIVE    -> the boundary hardens; the W-gate CLOSES on the named absence
                       (legitimacy possessed, field absent for this algebra family).

NO GRINDING: a charter cycle is spent only on a principled candidate carrying a real
discriminator. This non-abelian/non-associative shot is THE principled remaining option.
If the bounded G-set fails, we CLOSE — we do not invent cycles 2-3 to consume budget.
```

Next: the researcher produces the model card. Nothing builds before the one-pass mothership audit. The research is moving again.
