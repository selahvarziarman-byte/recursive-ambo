# PlatonicEngine — W-2.A Field-Reduction Model Card

## A relational reduction of the legitimate walk-primitive regime, and the test of whether it carries the field

Audience: mothership (audits this BEFORE any W-2.B build — for relational-not-scalar, anti-staple at design level, Addition-C presence, and a real discriminator) and the human (Arman, sovereign). For the lieutenant's later blind build.

Status: **W-2.A field-reduction model card — DRAFT, submitted for mothership audit. Not a possessed field, no diagnostic authorized, no self-resolved verdict.** The researcher abducts the reduction and seals value-predictions; the lieutenant builds the blind certification; mothership disposes the terminal verdict.

Repo identity (mandatory preamble, amended): canonical `C:\Dev\202cl\PlatonicEngine202`, branch **`team-arman`** (NOT Claude-child); competitor `wgate/arf-w1-root-frame-v0` and any `arf*` = READ-ONLY; decoy `C:\Dev\PlatonicEngine` is NOT this project. Gate (path+branch+HEAD) before acting.

Issued: 2026-06-13. Inputs: the W-2 entry order; the W-1 close (legitimacy ratified; prize = the gauge-equivariant selected Re on the 280 off-Q loops). Sealed predictions: held **off-repo by the sovereign**, committed by hash `PLATONIC_ENGINE_W2A_SEALED_PREDICTION_HASH.txt` (SHA-256 LF `81cfc7b3…db03`); byte-preserved reveal at W-2.C, sovereign verifies natively.

GOVERNING POSSESSION NOTE (Closure Consciousness Clause): W-1 possessed LEGITIMACY of the walk-primitive source regime, scoped. The carrier/fiber observable FIELD remains **NOT possessed**. This card proposes the reduction that would make it field-observable and the test that decides whether it does; the researcher's prediction (values and verdict-lean) is sealed off-repo so the blind build is not biased by it. A W-2 pass is the project's first field; a W-2 fail hardens the boundary. Either is sealed and falsifiable.

---

## 0. The target, restated

```txt
The W-2 target is the W-1 PRIZE: the gauge-equivariant SELECTED Re in {+1,-1} on the 280 off-Q
(birth-edge) bracketing-dependent loops of the G1 carrier graph — the one quantity that
distinguishes W from W_0 (defined where W_0 discards Policy-C ill-definedness). W-2 asks whether
a LABEL-FREE relational field can let a blind observer recover that signed value, beating all
controls INCLUDING bare geometry, sign included.
```

---

## 1. The declared relational reduction (what is broadcast — manifest in advance, no labels)

```txt
REDUCTION R*: the sign / Z2 loop-holonomy field — the MAXIMAL label-free relational reduction.
  - The emitted object is a 1-cochain on the closed G1 geometry: a transport value on each
    graph edge, derived from the source-state's loop/antipodal structure, NOT from carrier units.
  - The emitted OBSERVABLE is the loop-holonomy O(L) = the ordered product of edge transports
    around L. It is RELATIONAL / loop-borne by construction; there is NO per-source scalar emitted
    (the IV-A death mode is structurally excluded — nothing is broadcast per source).
  - VISIBLE-FIELD MANIFEST (frozen in advance): edge transports + loop-holonomies O(L) ONLY. NOT
    emitted: any carrier unit, signed lift, root identity, flag id, provenance token, or per-source
    coefficient. Closed blinded-view types + a recursive leak scan enforce this (zero exemptions).
WHY this reduction and not a richer one: the entry order's field menu (holonomy, phase defect,
  vortex, cancellation axis, loop invariant) is abelian/associative. A non-abelian connection that
  reproduced the prize would have to encode the octonionic SELECTION itself — i.e. the carriers —
  which is label leakage (a staple). So R* is the maximal LEGITIMATE relational reduction; if R*
  cannot carry the prize, no label-free relational field can.
```

---

## 2. The recovery map Rec(O), and what "recover or mediate" means

```txt
Rec(O): from the emitted loop-holonomies O(L) (labels stripped), recover the prize's signed value
  per loop. Operationally: O(L) IS a candidate for the selected Re; recovery scores how often
  sign(O(L)) equals the prize, split into r (incidence: correct loop pairing) and g (sign /
  orientation: correct +/-). MEDIATION is permitted: O need not equal Re directly if a fixed,
  label-free map (a defect count, a cancellation parity) recovers it — but that map may not read
  any carrier label and may hard-code no expected Re (Rider A).
RECOVERY BAR (entry order): g >= 0.90 (unsigned geometry is not the field — the W_0/orientation
  lesson); and Rec(O) must beat the structured, strict, AND bare-geometry controls.
```

---

## 3. Reduction-honesty — what R* loses, and the W_0-floor trade (named)

```txt
R* is abelian/associative (a 1-cochain holonomy is bracketing-INVARIANT). The prize is
NON-ASSOCIATIVE (it exists only because bracketing matters off Q). Therefore R* can, at most,
carry the ABELIAN SHADOW of the prize and LOSES its non-associative essence by construction.
  - It does NOT make the W_0 quaternionic Re-holonomy field-active; that floor remains source-state
    STRUCTURE, not a field observable. The trade is named: the field carries only the part of the
    prize that is expressible as a flat sign-holonomy; the octonionic selection is dropped.
  - This is the honest confession the reduction-honesty law requires: the reduction's loss is
    exactly the thing that made the prize worth chasing. Whether what survives still BEATS the
    controls is the empirical W-2 question — sealed.
```

---

## 4. Addition-C: the bare-geometry control and the control ladder (the decisive guard)

```txt
The decisive guard. The W-2.B battery MUST include, beside the structured-permutation and strict
controls, a BARE-GEOMETRY control:
  - a label-blind observer given ONLY positions + adjacency (no field, no carriers) attempts the
    SAME recovery of the prize. It may use the STRONGEST geometric features: node type by radius
    (primal vs child are at different distances from center), node-visit counts and parities,
    oriented signed-area / chirality, length, distance multisets.
  - SCORING RULE (sealed-rule, auditor-applied): a "recovery" that the bare-geometry control ALSO
    achieves is NOT a field result, however high its raw score. The field must beat bare geometry
    by a real, sign-bearing margin (g included) to count.
LADDER: trivial-null < structured-permutation < strict < BARE-GEOMETRY < (field, to pass) — the
  bare-geometry control is the high rung the field must clear. The exact predicted recovery values
  for the field and for the bare-geometry control are SEALED (off-repo).
```

---

## 5. The branch-selection test (entry order section 3)

```txt
W-1 left three gauge-equivariant bracketing branches (B-walk / B-gen / B-frame), agreeing on most
loops and diverging on a minority. W-2 may CLOSE that residual if the field observable is
well-defined / recoverable under exactly ONE branch and degenerate under the others.
TEST: run Rec(O) and the bare-geometry control SEPARATELY for each of the three branches' selected
  Re; report whether any single branch is uniquely field-recoverable (beating bare geometry) while
  the others are not. Prediction sealed. (The branches agree on the geometric part and differ only
  on the non-geometric loops — so the prediction turns on whether the field reaches those.)
```

---

## 6. The decisive quantities the W-2.B battery computes (method here; results sealed)

```txt
The blind W-2.B battery measures the quantities that decide the gate. The researcher's predicted
VALUES for every one of them — and the verdict-lean — are sealed off-repo (section 7); the build is
blind to them. The battery computes, independently:

(a) BARE-GEOMETRY RECOVERABILITY (Addition C). How well the strongest label-blind geometric recovery
    (node-type-by-radius, visit counts/parities, oriented area/chirality, distances) recovers the
    prize. This is the high control rung the field must clear; its value is measured, not assumed.

(b) ABELIAN REPRESENTABILITY. Whether the selected Re is the holonomy of ANY Z2 / U(1) field — i.e.
    whether the GF(2) loop-holonomy system [loop-edge incidence | Re-bits] is CONSISTENT. If it is
    inconsistent, no label-free abelian relational field (the entry order's menu) can carry the prize,
    and the proposed R* is bounded above by its abelian-representable part. The obstruction (solvable
    or not, and by how much) is measured.

(c) RESIDUAL. The part of the prize recovered by NEITHER the bare-geometry control NOR R* — its size
    and signature, measured.

(d) FIELD vs CONTROLS. Whether Rec(O) beats the structured, strict, and bare-geometry controls, sign
    included (g>=0.90); and whether any single Hole #2 branch is uniquely field-recoverable.

The researcher computed all of these in advance to form the sealed prediction (the blind build
recomputes them independently); the predicted values and the verdict-lean are in the sealed file, to
be reconciled at W-2.C. The terminal verdict is mothership's.
```

---

## 7. Sealed predictions (withheld off-repo; committed by hash)

```txt
Held off-repo by the sovereign; committed by one-way hash (SHA-256 LF 81cfc7b3…db03). Frozen before
the W-2.B build; byte-preserved reveal at W-2.C, sovereign verifies the re-hash NATIVELY. Structure
only here:
  - the bare-geometry control recovery (the Addition-C ceiling) — VALUE sealed;
  - the abelian-field obstruction (Z2 solvability + obstruction count) — VALUE sealed;
  - the residual family (size + signature) — sealed;
  - the proposed field's predicted r and g, and that it does NOT beat the bare-geometry control — sealed;
  - the branch-selection prediction — sealed;
  - the FIELD-INACTIVE verdict-lean + its explicit FALSIFIER — sealed.
No W-2.B scorer may read or hard-code any of these (Rider A). The reveal reconciles them against the
blind run.
```

---

## 8. Kill / verdict conditions and what is not claimed

```txt
W-2.B (blind) computes Rec(O) recovery vs structured + strict + BARE-GEOMETRY controls, r and g
separated, g>=0.90 required; GF(2) abelian obstruction recomputed; residual measured; branch-selection
swept; leak scan + closed blinded types; one shot (charter N=3).
TERMINAL (mothership):
  W-2 PASS (FIELD-OBSERVABLE): blind recovery of the prize, sign included, beating ALL controls incl.
    bare-geometry. -> the project's FIRST field, scoped.
  W-2 FAIL (FIELD-INACTIVE): recovery no better than the bare-geometry control / only exposed geometry.
    -> boundary hardens, recorded.
  VOID: staple / leak / tuning -> counts toward N, re-seal.
NOT CLAIMED: that the field exists or is possessed; that W is field-active. The central object — a
carrier/fiber observable field — remains absent; whether this reduction delivers it is the open W-2
question, with the researcher's prediction sealed. Legitimacy (W-1) stands; field activity (W-2) is
what this gate tests, falsifiably.
```

The walk-primitive regime is legitimate, and its prize is the one thing W_0 cannot match. W-2 asks whether that thing can live in a field a blind observer reads and bare geometry cannot fake. The reduction, the controls, and the test are specified here; the researcher's prediction is sealed and falsifiable. The blind build decides, and the seal — revealed byte-preserved at close — shows whether the prediction held.
