# PlatonicEngine — Higher-Form (Associator) Model Card: Mothership One-Pass Audit

Audience: researcher (seal-holder), lieutenant (blind build), Arman (sovereign).
Status: mothership one-pass audit of the Higher-Form Model Card (researcher, 2026-06-14). **Verdict: ACCEPT-WITH-REQUIRED-SHARPENINGS (conditional pass).** The card is in the right dimension and respects every discipline; the blind build is authorized ONLY after the §3 sharpenings are folded in and the seal is confirmed or re-committed (§4). Branch `team-arman`. Issued 2026-06-14.

## 1. What the card gets right (credited)

```txt
Channel theorem respected (it is NOT a loop holonomy); the object is the right-dimensional one
(associator = 3-cochain on triads / 3-cells); a real destructive carrier-strip test (card §2); the
frame + triangulation invariance sweep (§3); Addition-C lifted to the 2-3-complex (§4); sealed
off-repo predictions with native byte-preserved reveal (§6); clean terminal verdicts, no grinding (§7).
This is a sound card. The conditions below make the central MEASUREMENT non-vacuous and the OBJECT
well-defined — they are not a rejection.
```

## 2. The four audit checks

```txt
(a) DERIVED-NOT-INSERTED:     PASS, conditional on S1, S2.
(b) NON-TRIVIALITY REAL:      PASS, conditional on S3, S4, S5 — the spine; a vacuous verdict in
                              EITHER direction would hide exactly here.
(c) ADDITION-C PRESENT:       PASS, conditional on S6.
(d) CLEAN VERDICT BOTH WAYS:  PASS, conditional on S7.
```

## 3. Required sharpenings (binding, before build)

The heart of this audit is ANTI-VACUITY. On a 3-ball two vacuities lurk and must both be excluded:

```txt
VACUOUS PASS:  "non-associating cells exist" — always true (that is the whole premise). A count of
               anti-associating cells is NOT a field.
VACUOUS FAIL:  "absolute H3 = 0 on a ball" — always true. The absolute class is not the observable.
The real, contentful invariant is the RELATIVE class / the boundary flux. The sharpenings pin it.
```

```txt
S1 — OBJECT. Define O as a VERIFIED Z2 3-COCYCLE derived from the WALK-carriers. Canonical form:
  the associator [g1,g2,g3] of the three COMPOSABLE EDGE-carriers (the W-1 walk-steps) along each
  ORIENTED 3-simplex — not "the cell's carriers" (a tetra has 4 vertices; an associator takes 3
  ordered, composable arguments). This makes O (i) genuinely derived from W-1's walk structure
  (edges ARE the walks), (ii) a rigorous 3-cocycle via the associator's ALTERNATING property,
  (iii) orientation-resolved. A different derived construction is allowed only if it equally meets
  (i)-(iii).

S2 — CLOSEDNESS. The build VERIFIES delta-O = 0 (O is a cocycle) BEFORE computing any class. A
  non-closed O is routed to S7, never silently classified.

S3 — RELATIVE FRAMING (anti-vacuity #1). Non-triviality = the RELATIVE class in H3(complex, boundary;
  Z2) / the boundary Z2 flux — NOT the absolute class. State and compute it relatively. (H3(ball,
  boundary; Z2) = Z2, so a non-trivial class is POSSIBLE, not forced — the test is genuinely live.)

S4 — VERDICT DRIVER (anti-vacuity #2). The verdict is driven ONLY by that relative class / boundary
  flux being non-zero AND invariant. The raw COUNT of non-associating cells (card §3.i) is a
  DIAGNOSTIC ONLY and may never, by itself, support HIGHER-FORM-OBSERVABLE.

S5 — GAUGE COMPLETENESS. The invariance sweep covers the 168 Fano labelings, >=2 triangulations, AND
  the orientation / vertex-ordering convention (or a proof, via the alternating property, that the
  relative class is ordering-independent). Dependence on ANY of these = TRIVIAL.

S6 — CONTROL PIPELINE IDENTITY. The bare-geometry-AND-topology control runs the IDENTICAL relative-
  class pipeline (same orientation, same delta, same rel-boundary flux), differing ONLY in carrier
  input — so a class the control also produces is exposed combinatorics, caught as such.

S7 — THIRD OUTCOME. If delta-O != 0 / the class is ill-defined, classify HIGHER-FORM-TRIVIAL-WITH-
  REASON ("the associator does not form a gauge-invariant cocycle on the legitimate complex -> no
  higher-form observable") — a hardened-boundary close, DISTINCT from VOID (staple / leak / tuning).
```

## 4. Seal disposition

```txt
The seal f3d518f7 hashed predictions for the card AS WRITTEN. If S1 / S3 change the observable so the
sealed VALUES would differ, RE-SEAL natively before the build (sovereign re-hashes the off-repo
plaintext; byte-preserved reveal). This is pre-build WELL-DEFINEDNESS, not a tuning cycle — NO charter
cycle charged (same logic as the W-2.B remediation ruling). The researcher confirms whether the
existing seal already corresponds to the edge-carrier / relative-class construction; if yes, no re-seal.
```

## 5. Disposition

```txt
ACCEPT-WITH-REQUIRED-SHARPENINGS. Build authorized AFTER S1-S7 are folded in AND the seal is
confirmed or re-committed. Lieutenant builds blind (Riders A/B; raw values only; declares no verdict);
auditor derives per-criterion status; mothership disposes the terminal verdict. With the relative
framing in place the gate is genuinely live BOTH ways — a non-trivial class is possible but not forced,
and the sharpenings stop a fake verdict in either direction.
```

Next: the researcher folds S1-S7 and confirms or re-commits the seal; then the lieutenant's blind build. The spine of this gate is one finite, gauge-swept, RELATIVE cohomological measurement — and the sharpenings are what keep "we found an associator" from masquerading as "we found a field."
