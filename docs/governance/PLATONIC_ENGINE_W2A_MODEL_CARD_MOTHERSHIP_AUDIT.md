# PlatonicEngine — W-2.A Field-Reduction Model Card: Mothership Audit

Audience: the researcher, the lieutenant, the human (Arman, sovereign).

Status: mothership audit of the W-2.A model card, performed BEFORE any W-2.B build per the W-2 entry order. Verdict: **AUDITED-PASS — W-2.B blind build AUTHORIZED, subject to five binding conditions (§3).** Branch `team-arman`. Issued 2026-06-13.

Possession (Closure Consciousness Clause): legitimacy (W-1) stands; the field remains NOT possessed. This audit authorizes the TEST, not a field.

## 1. The four required checks — all PASS

```txt
RELATIONAL-NOT-SCALAR: PASS. The emitted object is a 1-cochain; the observable is the loop-
  holonomy O(L). Nothing is broadcast per source. The IV-A death mode (one scalar per source)
  is structurally excluded, not merely avoided. Correct.

ANTI-STAPLE AT DESIGN LEVEL: PASS, with Condition C1. The visible-field manifest emits only edge
  transports + loop-holonomies; no carrier unit/lift/root/flag/provenance token; leak scan +
  closed blinded types, zero exemptions. The one residual staple risk is at the TRANSPORT-
  DERIVATION level (the transports are "derived from the source-state's loop/antipodal structure,
  not carrier units") — that claim must be enforced by a destructive test (C1), not trusted.

ADDITION-C PRESENT: PASS, with Condition C3. The bare-geometry control is the decisive high rung,
  with strong features named (radius node-type, visit parities, oriented area/chirality, distance
  multisets) and the sealed scoring rule that a recovery bare geometry also achieves is not a
  field result. The fairness of the comparison requires symmetric information (C3).

A REAL DISCRIMINATOR: PASS. The GF(2) abelian-representability test ([loop-edge incidence | Re-bits]
  consistency) is decidable and blind-computable; the prize-vs-bare-geometry margin is measured.
  The discriminator can produce BOTH outcomes — subject to Condition C2 (the sealed NEGATIVE lean
  must have a genuine falsifier).
```

## 2. The deep ruling: R* is the MAXIMAL legitimate label-free reduction (verified)

```txt
The researcher's §1 argument ("no richer label-free reduction exists") is SOUND, and mothership
records the verification so it cannot be reopened as "you skipped a richer field":

  - The prize's essence is BRACKETING-DEPENDENCE (it exists only because the word leaves Q and
    octonion multiplication is non-associative). A connection valued in any ASSOCIATIVE group
    (Z2, U(1), unit quaternions, SU(2), any Lie/finite associative group) has BRACKETING-INVARIANT
    holonomy — it cannot carry bracketing-dependence at all.
  - To carry bracketing-dependence, the transport must be NON-ASSOCIATIVE — i.e. the octonion/
    Moufang carrier structure itself. Emitting that as a field IS emitting the carriers = label
    leakage = a staple. Forbidden.
  - The SELECTED Re (after the walk law picks a branch) is Z2-valued (a sign per loop). Any field
    carrying a Z2-valued target factors through its Z2 image; a richer associative group buys
    nothing. So Z2 is the CEILING for a label-free relational field on this target.
  => R* (the Z2 sign-holonomy field) is not a motivated-weak choice; it is the principled maximum.
     If R* cannot carry the prize, no legitimate label-free relational field can. This is exactly
     the right thing to test, and its failure (if it fails) is a real boundary, not a strawman.
```

## 3. Five binding conditions on the W-2.B build and the W-2.C close

```txt
C1 — TRANSPORT-DERIVATION DESTRUCTIVE TEST (anti-staple at definition level). The build must
   include a destructive test: strip/scramble the carriers and recompute the edge transports.
   The transports MUST be UNCHANGED (they are derived from label-free loop/antipodal structure).
   If the transports change when carriers are removed, they were carrier-derived — a staple at
   the definition level — and the run VOIDS. (This is the W-gate's standing "fails if defining
   facts removed" discipline, applied to the reduction itself.)

C2 — THE SEALED NEGATIVE MUST HAVE A GENUINE FALSIFIER (the symmetric-discipline guard). The
   researcher's sealed verdict-lean is FIELD-INACTIVE. A predicted NEGATIVE is where motivated
   PESSIMISM hides, exactly as a predicted positive once hid motivated optimism (the W_0 false
   positive). Therefore: the sealed falsifier must be a CONCRETE, MEASURED condition that flips
   the verdict to PASS — i.e. (GF(2) system CONSISTENT) AND (field g >= 0.90) AND (field beats
   bare-geometry by the sealed margin). At W-2.C, mothership verifies the falsifier was REAL:
   that there existed a measurable outcome of the blind run that would have produced PASS. A
   negative with no real path to positive is unfalsifiable pessimism and VOIDS.

C3 — SYMMETRIC INFORMATION (fair comparison). The field recovery Rec(O) and the bare-geometry
   control must use the IDENTICAL recovery procedure and the IDENTICAL supervision regime (both
   label-blind; both unsupervised, or both supervised-with-held-out-CV — declared and sealed).
   The ONLY difference permitted is the INPUT (field observables vs bare positions/adjacency). A
   bare-geometry control allowed to fit harder than the field would rig the comparison toward
   FAIL — the mirror of a weak control rigging toward PASS. Both rungs, same ladder.

C4 — MAXIMALITY ON RECORD. The §2 verification (Z2 is the ceiling for a sign-valued label-free
   target) is recorded in the W-2.C close, so the negative (if it lands) cannot later be dismissed
   as "you only tested the weakest field." The researcher adds it to the card.

C5 — STANDARD INHERITANCE (unchanged, enforced). Seals off-repo; byte-preserved reveal; sovereign
   re-hashes NATIVELY at W-2.C (the W-1 §3b lesson — both chronology AND byte-match this time);
   blind-in-code; Riders A/B; leak scan zero exemptions; one shot, counts toward charter N=3;
   all work on team-arman, arf* read-only; gate before every action.
```

## 4. Note for the record — the discipline tested in the harder direction

```txt
This is the first gate where the researcher predicts a NEGATIVE. The campaign's machinery was
built to catch motivated optimism (sealed values, blind controls). C2 and C3 extend it to catch
motivated pessimism: a genuine falsifier and a fair (symmetric) comparison. The honesty of a
predicted "no field exists" is established the same way as a predicted "field exists" — by a
sealed, blind, falsifiable test whose negative could have come out positive. Mothership notes the
model card already reaches for this (it seals "the falsifier"); C2 makes its genuineness an
audit requirement, not a courtesy.
```

## 5. Disposition

```txt
W-2.A model card: AUDITED-PASS. R* is the maximal legitimate label-free relational reduction;
relational-not-scalar, anti-staple, Addition-C, and a real discriminator are all satisfied.
W-2.B blind build: AUTHORIZED under C1-C5. The lieutenant builds blind to the sealed values;
mothership disposes the terminal verdict at W-2.C after the seal reveal and native re-hash.
The field is not possessed; W-2 tests, falsifiably, whether this reduction delivers it.
```

The reduction is the right one — the strongest a label-free field is allowed to be — and the test can fail honestly or pass honestly. The one thing mothership will not accept is a negative that was never allowed to become a positive. Seal it so it could have, run it blind, and let the reveal show which way the prize fell.
