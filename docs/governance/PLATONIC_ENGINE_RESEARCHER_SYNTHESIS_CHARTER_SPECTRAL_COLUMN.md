# Charter — Researcher Synthesis: the Spectral Column on the Topological Spine (Merge Phase 1)

**From:** Mothership (4th seating) · **To:** Researcher · **cc:** Sovereign, Engineer (awaits Phase 2), TO · **Date:** 2026-06-26

Per the authorized merge plan (`PLATONIC_ENGINE_MERGE_PLAN_TOPOLOGICAL_SPINE.md`), this is **Phase 1 — the gating first move.** Ground the unified reading on OUR substrate before any build or branch merge. The field/spectral line (Blicero/arf `t28g`) is now ours; treat its T28-G result as a **hypothesis to ground on our objects**, not a settled import (it is verified v0 on *their* Platonic seeds, unsealed even by their own protocol).

## Mandate
Rule whether and how the spectral column — complement-centered incidence kernel + the −γ response + the skew-adjoint energy-exchange — attaches to our topological spine, grounded on our committed complexes. Ground-before-rule; pin numbers; name nothing you haven't computed on our objects; route build to the engineer and the commit to Arman.

## S1 — Spectral on OUR objects
Compute `Kσ`, `γ = 2(d+λ)/(d(n−2))`, the `ζ = −γ q` response, and the skew-adjoint exchange on **our** complexes — the surface zoo (cylinder/torus/Möbius/Klein/RP²/sphere) and the five-necessity assembled complexes — not just the tetra/cube/octa seeds. Rule:
- **(a) Reproduce** — does the law compute cleanly on our cell-complexes?
- **(b) Discriminate** — does the spectrum separate forms our `w₁/χ/homology` do *not* (the way necessity-5 split Klein `[0,1]` from RP² `[1]`), or is it redundant with our invariants?
- **(c) Cohere** — does it agree with our invariants where they overlap (orientability, genus)? Any contradiction is a finding, not a rounding error.

## S2 — Settle the open truths (seal the rulings)
- **(T1)** Does the spectral law **escape our sealed IV-A observable-blinding negative?** Mothership read: yes — `γ` is a *structural* invariant (functions of `d,n,λ`), not an observable broadcast recoverable under blinding. Confirm or refute against the IV-A criterion; this decides whether the column is even admissible after IV-A.
- **(T2)** Is the skew-adjoint "exchange" **genuinely dynamical, or a relabeled static identity?** (The loophole the Watch and I both flagged.) Show it evolves non-trivially / carries information a static read does not.
- **(T3)** Is **cube = tetra²** (`γ`, `μ`) real **product** structure (cube graph `Q3 = K2□K2□K2`) or numeric coincidence? If real, it likely informs **Enabler 3 (product)** — pin the relation.

## S3 — Unified domain model (the build surface)
Define how the spectral column attaches to the topological spine: **one assembled complex, two readings.** Specify the spectral/dynamics data model alongside the existing topological layer; the **seam** where it attaches (the same incidence/assembly the topological reading already uses — `η = Aᵀq` is shared); and the meaning question — whether the energy-exchange dynamics is sanctioned as a **second arrow** in the picture (alongside genealogical depth, ADR 0009). Pin a canonical worked example with numbers for the engineer.

## Output & routing
A **researcher ruling + a unified-reading spec** (the build surface). Then: **Mothership ratifies the unified picture** (CONTEXT.md + a new ADR) on your ruling → **Engineer** builds Phase 2 only after → commit is **Arman's** (native). Hold the researcher boundary: rule truth/definitions; do not build, and do not rule scope (mothership's). Seal expected values before asserting them.

## Note on parallelism
**Enabler 1 (multi-form load-and-assemble) runs in parallel as the understudy engineer's initiation cycle — topological payload only.** Its spectral operator-assembly (their §10) attaches at E1's seam *after* this synthesis lands, so this charter does **not** block E1; it gates only the spectral attachment and Phase 2.
