# Read-Back Charter — the Generative Playground re-conception

**From:** mothership · **To:** engineer + researcher · **Status:** scope ratified by the mothership; circulating for read-back. **Commit is Arman's native call (uncommitted).**

**Success test:** all three seats recognize the **same module** in the *artifact* — `docs/CONTEXT.md` (redrafted) + `docs/adr/0008` (generative playground) + `0009` (genealogy the standing object) + `0010` (standalone multi-universe) + `0011` (the reading is the mirror; five necessities). Read **these**, not the grill transcript.

## The core (mothership-ratified scope)

The module is a **generative playground** — forms beget forms. The reductive transformer we built is the **unary sub-case** (metabolism). Load-bearing facts, ratified:
- **No new operation family.** Birth = the *built* boundary-identification applied to a disjoint union of two forms, + un-quarantined **product** (the one named-but-**unbuilt** member; build pressure-gated).
- **The committed pull-back IS the genealogy**, read forwards (child → parents). No new structure — a re-reading.
- **Population consumptive, record monotone.** The cascade's "μ-monotone, never creates" still holds for the live population; the genealogy grows. Generativity is in the *record*.
- **ADRs 0001–0007 re-situate** inside the unary sub-case; zoo/cut/cascade **re-read, not rebuilt**.
- **The wall holds:** meaning-words stay on the reading side; no operation's definition touches them.

## Engineer — read-back against the committed code

- Does the built work (zoo / cut / cascade) genuinely re-read as the **unary (metabolism) sub-case** — same machinery at arity 1 — with at most minor **disjoint-union plumbing** for arity ≥ 2, and **no rebuild** of the operation set?
- Is **"no new operation family"** true at the substrate: birth = the built glue applied between two forms' boundaries; **product** the one named-but-unbuilt member?
- Is the **one structural extension** — namespace each primal by `(source, primalKey)` — minimal and sound against `lineage.ts` (which already treats a source-less vertex as its own primal)?
- Confirm the four **built** necessities are the committed pieces: (1) descent = the pull-back · (2) orientation = the signed pull-back · (3) incidence = the registry · (4) invariants = topology-signature / `GlueCoh`.

## Researcher — read-back + the open question

- **Settle the 5th necessity** (ADR 0011, open): is "the whole assembled complex with its signed loops" satisfied by **one** signed loop (a single non-orientable witness) or the **whole loop-structure** (all essential cycles — the cycle space / H₁)? Ground it; don't assert. It decides whether the reading needs one contradiction-witness or the complete contradictory-structure.
- Confirm the **genealogy / lineage** framing against the lineage law: the pull-back-as-parent-pointer, and lineage **carried-not-minted** for a born form (it inherits its parents' lineage, unlike an ambo midpoint which mints fresh). Sound against the multiset / injectivity foundation?
- Confirm **co-location ≠ identity across universes**: namespacing (universe-1's "A" ≠ universe-2's "A") is the right support, and **sealed-except-by-explicit-glue** holds.

## Report

Send back what lands and what doesn't — **dissent is the point.** If the artifact doesn't read as the same module for either of you, that is the finding. When both read-backs are in (and the 5th-necessity question ruled), the picture is the shared reference and the next build target falls out of it.
