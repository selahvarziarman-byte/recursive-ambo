# W-Gate — W-2 Entry Order
## Field-observability of the legitimate walk-primitive source regime

Audience: the prompter/planner/auditor (lieutenant) and the researcher (theorist). For the human (Arman, sovereign).

Status: mothership authorization for W-2 entry, on the terminal ratification of W-1 (W-PASS-LEGITIMATE, scoped). Not an implementation prompt. The researcher abducts the field-reduction law and seals predictions; the lieutenant builds the blind diagnostic; mothership audits and disposes the terminal verdict. Where silent, the Abductive Research Gate ruling and the Carrier-Borne Field charter govern.

Issued: 2026-06-13. Branch `Claude-child`. Anchor: W-1 terminal verdict `PLATONIC_ENGINE_W1_TERMINAL_VERDICT_MOTHERSHIP.md`.

Repo identity (mandatory preamble): canonical `C:\Dev\202cl\PlatonicEngine202`, branch `Claude-child`; decoy `C:\Dev\PlatonicEngine` is NOT this project; gate (path+branch+HEAD) before every instruction.

GOVERNING POSSESSION NOTE (Closure Consciousness Clause): W-1 possessed LEGITIMACY of the walk-primitive source regime, scoped. The carrier/fiber observable FIELD remains NOT possessed. W-2 is the gate that seeks it. A W-2 pass is the project's first field; a W-2 fail hardens the boundary and is a first-class result.

---

## 1. The W-2 question

```txt
Does a declared RELATIONAL reduction of W's walk-frame admit a field law whose emitted
observable lets a LABEL-BLIND observer RECOVER OR MEDIATE the bracketing-selection holonomy
(the W-1 prize: the gauge-equivariant signed value on the 280 off-Q loops), beating blind
controls, WITHOUT label leakage AND without being recoverable from bare geometry/topology?

And, if so: does that field observable PREFER one of the three gauge-equivariant Hole #2
branches (B-walk / B-gen / B-frame), closing the W-1 residual to a unique selection?
```

The W-1 prize is the target precisely because it is the one quantity that distinguishes W
from W_0 (defined-where-W_0-discards, in the octonionic sector). If the field cannot carry
even that, the inversion buys legitimacy but no field — an honest, bounded outcome.

## 2. The binding design constraints (inherited, non-negotiable)

```txt
RELATIONAL FROM THE START (W-1 §2 synthesis, ratified): the emitted observable must be
  relational / loop-borne (holonomy, phase defect, vortex, cancellation axis, loop
  invariant) — NEVER one complex scalar per source. Per-source reduction is the PROVEN
  cause of the IV-A death; a W-2 law that reduces to one-scalar-per-source dies identically
  and must not be proposed.

ADDITION C — THE EXPOSED-GEOMETRY GUARD (binding): the observable must beat controls by NOT
  being recoverable from bare positions/topology alone. IV-A's lone partial reach (unsigned
  antipodal pairing from positions) was an EXPOSED-GEOMETRY channel — controls equalled the
  real value, so it was not a propagation signal. The W-2 controls MUST include a
  bare-geometry control: a label-blind observer given only positions/adjacency must NOT
  recover the prize. If they can, the observable tests the shape, not the field.

ANTI-STAPLE + RIDER A: no carrier LABEL (signed lift, unit, root identity, flag id,
  provenance token) in the emitted observable set; the reconstruction scorer hard-codes no
  expected holonomy values. Observable-only blinding: closed blinded-view types + recursive
  leak scan, zero exemptions; leak hits void cells by code.

RIDER B — DESIGN/CERTIFICATION SEPARATION: the field-reduction law is FROZEN and
  hash-committed before the certification run; sealed predictions (values) filed; one shot;
  tuning inside a certification voids it and counts toward the N=3 charter cycle budget.

SEAL DISCIPLINE, REFINED (W-1 §3b lesson): sealed plaintext held off-repo; the canonical
  verification is Proof B (chronology: seal commit precedes the blind diagnostic commit) plus
  blind-in-code; AND the reveal MUST be byte-preserved so the revealed copy re-hashes to the
  commitment, with the sovereign confirming that match NATIVELY at reveal. Both, this time.
```

## 3. The branch-selection sub-question (the residual W-1 carried)

```txt
W-1 left three gauge-equivariant bracketing branches (agree 192/280, diverge 88). W-2 may
CLOSE this residual if the field observable demonstrably prefers one branch — i.e., the
relational observable is well-defined / recoverable under exactly one of B-walk/B-gen/B-frame
and degenerate under the others. That is the GOOD outcome: the field selects the bracketing
the source regime could not. It is NOT required for a W-2 pass on the recovery question, but
it is the prize-of-the-prize and must be tested, sealed, and reported either way.
```

## 4. Phases and deliverables

```txt
W-2.A  RESEARCHER — the field-reduction model card (theory, no code):
  - the declared RELATIONAL reduction from W's walk-frame to an emitted field observable
    (what is broadcast; the visible-field manifest IN ADVANCE; no labels);
  - the recovery map Rec(O) and what "recover or mediate the prize" means operationally;
  - reduction-honesty: what the reduction LOSES, stated; it must at least preserve the
    quaternionic Re-holonomy floor (W_0 floor) or name the trade;
  - the bare-geometry control design (Addition C) and the label-blind control ladder;
  - the branch-selection test design (§3);
  - sealed predictions (VALUES) for every W-2 diagnostic quantity, hash-committed before
    the run, plaintext off-repo, byte-preserved reveal.
  -> submitted to mothership; audited for relational-not-scalar, anti-staple at design level,
     Addition-C presence, and a real discriminator BEFORE any build.

W-2.B  LIEUTENANT — the blind certification diagnostic:
  - built blind to the sealed values (blind-in-code), under Riders A/B;
  - computes Rec(O) recovery vs structured + strict controls + the bare-geometry control;
  - scores r (incidence) and g (sign/orientation) SEPARATELY; g>=0.90 required (unsigned
    geometry is not the field, the W_0/orientation lesson);
  - leak scan + closed blinded types mandatory;
  - one-shot certification; raw values only; status derivation is the auditor's.

W-2.C  CLOSE — seal reveal + reconciliation (researcher) -> auditor per-criterion verdict
  -> mothership terminal verdict: FIELD-OBSERVABLE (the prize recovers, beats all controls
     incl. bare-geometry, label-blind, sign included) -> the project's FIRST field, scoped;
     or FIELD-INACTIVE (recovers no better than controls, or only via exposed geometry) ->
     boundary hardens, recorded; or VOID (staple/leak/tuning) -> counts toward N, re-seal.
```

## 5. Kill / verdict criteria

```txt
- W-2 PASS (FIELD-OBSERVABLE): blind recovery of the prize, sign included, beating
  structured + strict + BARE-GEOMETRY controls, label-stripped. -> first field possessed,
  scoped to the anchored lineage; charter advances toward F-III/F-IV over a real object.
- W-2 FAIL (FIELD-INACTIVE for this reduction): the legitimate source regime has no field
  image under the proposed relational reduction. A real finding. The charter's N=3 cycle
  budget allows redesign->re-certification; exhaustion fires the charter kill criterion.
- The bare-geometry control is the decisive guard: a "recovery" that the bare-geometry
  control also achieves is NOT a field result, however high its raw score.
```

## 6. Constraints and budget

```txt
- D1/D3, Gate 0, W-1 are load-bearing and READ-ONLY; defects escalate, never silent fixes.
- Frozen (inherited): no S0, no topology, no packet writing, no Shape mutation, no operation
  registration, no new route/gate/region families, no UI, no universal-law language.
- Legitimacy-before-activity is satisfied (W-1 ratified); W-2 is the activity gate.
- Budget: researcher model card (plan) + lieutenant 1-3 runs + close; certification cycles
  counted under the charter N=3. A 4th implementation run is the drift signature -> escalate.
```

## 7. Disposition

```txt
W-2 OPEN. Researcher prepares the W-2.A field-reduction model card (relational reduction +
recovery map + Addition-C bare-geometry control + branch-selection test + sealed value-
predictions), submitted to mothership before any build. The lieutenant does not build, and
the researcher does not self-authorize, before mothership audits the model card.
```

The source regime is legitimate; the field is still absent. W-2 asks the only question left that matters: can the one quantity that makes W more than W_0 — the defined holonomy in the octonionic sector — be made to live in a field that a blind observer can read, and that bare geometry cannot fake. If yes, the project has its first field. If no, it has its honest boundary. Either is an ending; drift is not.
