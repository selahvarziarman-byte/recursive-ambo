# PlatonicEngine — Higher-Form (Associator) Model Card

## The prize read where it lives: the octonion associator as a 3-cocycle on the Ambo triads

Audience: mothership (ONE-PASS audit BEFORE any build — derived-not-inserted? non-triviality test real? Addition-C present? clean verdict both ways?) and the human (Arman, sovereign). For the lieutenant's later blind build.

Status: **higher-form model card — DRAFT, submitted for mothership one-pass audit. Not a possessed field, no build authorized, no self-resolved verdict.** The researcher abducts the observable and seals value-predictions; the lieutenant builds blind; mothership disposes the terminal verdict.

Repo identity (mandatory preamble): canonical `C:\Dev\202cl\PlatonicEngine202`, branch **`team-arman`**; competitor `wgate/arf-w1-root-frame-v0` and any `arf*` = READ-ONLY; decoy `C:\Dev\PlatonicEngine` = not this project. Gate before any action.

Issued: 2026-06-14. Inputs: the Higher-Form Gate entry order; the W-1 legitimate structure (carriers + the Ambo cell complex); the W-2 channel theorem (non-associativity cannot be a loop holonomy). Sealed predictions: held **off-repo** (gitignored), committed by hash `PLATONIC_ENGINE_HIGHERFORM_SEALED_PREDICTION_HASH.txt` (SHA-256 LF `f3d518f7…b3bc`); byte-preserved reveal at close.

GOVERNING POSSESSION NOTE (Closure Consciousness Clause): W-1 possessed LEGITIMACY of the walk-primitive source regime, scoped. The carrier/fiber observable FIELD remains **NOT possessed**. This card proposes the observable in the dimension where the prize actually lives (the associator is a function of triples) and the test of whether it survives as a field; the researcher's prediction (values and verdict-lean) is sealed off-repo so the blind build is not biased.

---

## 0. The target, re-aimed

```txt
The W-1 prize is a BRACKETING-DEPENDENT sign: it exists only because off Q, (ab)c != a(bc). That
is an ASSOCIATOR -- a function of TRIPLES, a 3-cocycle. W-2 proved (channel theorem) it cannot be a
loop holonomy: a loop is 1-D and 1-D transport is associative. So we read it where it lives: on the
TRIADS / the 2-and-3-cells of the legitimate Ambo complex, as a higher-form (gerbe / 3-cocycle)
observable. The substrate is not invented -- it is the order-3 / A2 / triadic structure already
proven legitimate (Bench-2) and the Ambo dissection's own cells.
```

---

## 1. The observable (relational, higher-form, label-free)

```txt
OBSERVABLE O: the octonion ASSOCIATOR 3-cochain on the cells of the Ambo dissection, and its
gauge-invariant Z2 COHOMOLOGY CLASS.
  - On each ORIENTED 3-simplex, O is the associator [g1,g2,g3] of the three COMPOSABLE EDGE-carriers
    -- the W-1 walk-steps g_i = c_i^{-1} c_{i+1} along the simplex (edges ARE the walks), NOT the four
    vertex-carriers (S1). The edge-steps telescope, so O is the pullback of the octonion 3-cocycle:
    a rigorous, orientation-resolved Z2 3-cocycle.
  - The emitted/measured quantity is the RELATIVE CLASS in H3(complex, boundary; Z2) -- the boundary
    Z2 flux -- NOT the absolute class (vacuously 0 on a ball) and NOT the raw count of non-associating
    cells (vacuously non-zero). H3(ball, boundary; Z2) = Z2, so a non-trivial class is POSSIBLE, not
    forced -- the test is genuinely live (S3).
  - It is RELATIONAL and higher-form by construction (a function of triples / on 2-3-cells), not a
    per-source scalar (the IV-A death mode is structurally excluded) and not a loop holonomy (the
    W-2 channel theorem is respected). NO carrier label is emitted: the manifest is the cell-incidence
    + the Z2 class only. Closed blinded-view types + recursive leak scan, zero exemptions.
```

---

## 2. Derivation from W-1 structure (derived, not inserted) — kill criterion (a)

```txt
- The carriers are W-1's (primal {e1,e2,e4,e7}, children = their products); the 3-cells are the REAL
  Ambo dissection's residue + core cells (createSeedShape + applyAmboDissection). Nothing is invented.
- The associator IS the very non-associativity that produced the W-1 prize's bracketing-dependence --
  the same off-Q phenomenon, now read on triples instead of loops. It is consumed, not authored.
- DESTRUCTIVE TEST (must fire): strip/replace the carriers with an associative (Q-confined or abelian)
  assignment -> the associator pattern must VANISH (all cells associate). If a non-trivial associator
  survives a carrier strip, it was inserted, not derived -> VOID. (The W-gate "fails if defining facts
  removed" discipline, at the 3-cell level.)
```

---

## 3. The non-triviality / frame-invariance test — kill criterion (b), the spine

```txt
The CENTRAL test (the physics caveat made into discipline). The blind build computes, with NO expected
value hard-coded:
  (0)  CLOSEDNESS FIRST (S2): verify O is a genuine cocycle (delta-O = 0; the edge-steps telescope ->
       pullback of the octonion 3-cocycle). A non-closed / ill-defined O is routed to the third outcome
       (S7), never silently classified.
  (i)  the RELATIVE CLASS in H3(complex, boundary; Z2) / the boundary Z2 flux, and whether O is a
       COBOUNDARY (= delta of a face 2-cochain / a B-field). Coboundary / even boundary-flux == TRIVIAL
       (Dirac-trivializable). THIS relative class is the SOLE verdict driver (S4).
  (ii) the raw COUNT of non-associating cells is a DIAGNOSTIC ONLY -- it may NEVER, by itself, support
       HIGHER-FORM-OBSERVABLE (anti-vacuity, S4).
  (iii) GAUGE COMPLETENESS (S5): recompute the relative class across the 7x24=168 Fano frames, >=2
       triangulations of the core, AND the vertex-ordering / orientation convention. Dependence on ANY
       of these = TRIVIAL.
NON-TRIVIAL (passes b) iff: O is a verified cocycle, its RELATIVE class is NOT a coboundary, AND that
  class is invariant under gauge + triangulation + ordering. TRIVIAL iff coboundary / even boundary-flux
  / dependent on any convention. Predicted values sealed (section 6).
```

---

## 4. Addition-C lifted: the bare-geometry-AND-topology control — kill criterion (c)

```txt
If (and only if) the class is non-trivial, it must be LABEL-BLIND recoverable while a bare-geometry-
AND-topology control CANNOT recover it:
  - the control gives a label-blind observer ONLY positions + faces + cell-adjacency (the 2-3-complex)
    and NO carriers, using the IDENTICAL recovery procedure and supervision regime as the field
    (C3 symmetric-information discipline, inherited): only the INPUT differs.
  - a class the bare-geometry+topology control also recovers is exposed combinatorics, not a field.
The field must beat that control, frame-invariant, label-stripped.
```

---

## 5. Branch-selection test

```txt
Does the associator class SELECT one of the three W-1 gauge-equivariant bracketing branches
(B-walk/B-gen/B-frame; the Hole #2 residual, agree 192/280, diverge 88)? Run the class computation
per branch and report whether exactly one branch yields a well-defined / non-trivial class while the
others are degenerate. Prediction sealed. (The associator sign is a property of the carriers; whether
it is branch-sensitive at all is itself the test.)
```

---

## 6. Sealed predictions (withheld off-repo; committed by hash)

```txt
Held off-repo by the sovereign (gitignored *_SEALED_PREDICTIONS.txt); committed by one-way hash
(SHA-256 LF f3d518f7...b3bc). Frozen before the build; byte-preserved reveal at close, sovereign
re-hashes NATIVELY. Structure only here; the values + verdict-lean are sealed:
  - the per-cell associator (which cells non-associate; the count) -- VALUE sealed;
  - the total Z2 flux and whether it is a coboundary (the non-triviality disposition) -- sealed;
  - the frame-invariance across the 168 and triangulation-independence -- sealed;
  - the control + branch-selection predictions -- sealed;
  - the verdict-lean + its explicit FALSIFIER -- sealed.
No blind scorer may read or hard-code any of these (Rider A).
```

---

## 7. Kill / verdict conditions (auditor + mothership; not the builder)

```txt
The blind build computes-and-reports raw values only; declares NO verdict.
  HIGHER-FORM-OBSERVABLE: a non-trivial, gauge/frame-invariant, triangulation-independent, label-blind
    associator class that beats the bare-geometry-AND-topology control -> the project's FIRST genuine
    field, in the dimension where octonions live. Charter advances over a real object.
  HIGHER-FORM-TRIVIAL: the associator is real in the source but the realized class is a coboundary /
    Dirac-trivialized / frame-dependent -> non-associativity is NOT a clean field observable in any
    reachable dimension. The W-gate CLOSES on a hardened boundary, with the cohomological reason NAMED.
  VOID: staple / leak / tuning -> re-seal (counts toward N=3).
NO GRINDING (entry order): this is THE principled re-target. A trivial outcome closes the gate; it does
not authorize another channel hunt.
```

---

## 8. Scope on record (for either verdict)

```txt
This is the dimension where the associator natively lives: a 3-ary object read on triples / 3-cells,
not a 1-D loop. The substrate (the Ambo cells, the order-3 / A2 triadic structure) is already legitimate
(W-1, Bench-2). So the test is not "the weakest channel": it is the home channel. If the realized class
is non-trivial here, it is a real field; if it trivializes here, the boundary is hardened to its limit --
non-associativity is real in the source and provably not a clean, gauge-invariant field observable in the
dimension built to host it. Either is a first-class, terminal result. Which one holds is sealed and
decided by the blind build, not asserted here.
```

The prize is an associator; W-2 proved it cannot be a loop. This card reads it on the triads where a 3-cocycle lives, derives it from the legitimate structure, and stakes the whole question on one finite, gauge-swept, frame-invariant cohomological measurement — non-trivial class, or coboundary. The reduction is the right-dimensional one (section 8); the prediction is sealed and falsifiable; the blind build decides, and the seal, revealed byte-preserved at close, shows which way the associator fell.

---

## 9. Audit sharpenings S1–S7 folded; seal confirmed (post one-pass audit, 2026-06-14)

```txt
S1 OBJECT (section 1, sharpened): O = the associator of the three composable EDGE-carriers (W-1
   walk-steps g_i = c_i^-1 c_{i+1}) along each oriented 3-simplex -- a rigorous, orientation-resolved
   Z2 3-cocycle; NOT the four vertex-carriers.
S2 CLOSEDNESS: verified first (section 3.0). The edge-steps telescope -> O is the pullback of the
   octonion 3-cocycle; on a 3-complex (no 4-cells) well-definedness IS the triangulation+ordering
   invariance of S5. A non-cocycle routes to S7.
S3 RELATIVE FRAMING: the observable is the RELATIVE class in H3(complex, boundary; Z2) / boundary flux
   -- not the absolute class (vacuously 0), not the count (vacuously non-zero).
S4 VERDICT DRIVER: ONLY the relative class drives the verdict; the cell count is diagnostic-only.
S5 GAUGE COMPLETENESS: invariance over the 168 Fano frames + >=2 triangulations + vertex-ordering /
   orientation convention; dependence on any = TRIVIAL.
S6 CONTROL PIPELINE IDENTITY (section 4): the bare-geometry-AND-topology control runs the IDENTICAL
   relative-class pipeline (same orientation, same delta, same rel-boundary flux), differing ONLY in
   carrier input; a class the control also produces is exposed combinatorics.
S7 THIRD OUTCOME (section 7): if delta-O != 0 / the class is ill-defined -> HIGHER-FORM-TRIVIAL-WITH-
   REASON ("the associator does not form a gauge-invariant cocycle on the legitimate complex"),
   DISTINCT from VOID (staple / leak / tuning).

SEAL CONFIRMATION (audit section 4): the researcher re-verified the observable under the sharpened S1
   (edge-carrier) and S3 (relative class), ordering-resolved, across the 168 frames x 2 triangulations
   x 3 vertex-orderings. The sealed VALUES are UNCHANGED (the original computation already used the
   edge-ratio walk-steps and the rel-boundary-flux sum). The existing seal SHA-256 LF f3d518f7...b3bc
   therefore already corresponds to the edge-carrier / relative-class construction; NO re-seal is
   required and no charter cycle is charged.
```
