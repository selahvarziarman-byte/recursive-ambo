# Carrier-Borne Field Campaign — Station F-I Entry Order
## Emission law design (model card), opened on the §10.3 fork — quaternionic caveat governing

Audience: the prompter/planner/auditor (lieutenant).

Status: lieutenant-drafted, mothership-RATIFIED AND ISSUED 2026-06-13, with the fork sequencing set by the sovereign and two mothership sharpenings (§10). Not an implementation prompt. Where this order is silent, the Carrier-Borne Field charter rules; where the charter is silent, the predecessor's ratified law (Closure Consciousness Clause, anti-staple + Riders A/B, Repo-Identity Gate Protocol) rules.

Drafted: 2026-06-13 (lieutenant). Issued: 2026-06-13 (mothership). Branch `Claude-child`. Anchors: Gate 0 ratified PASS-quaternionic (closing memo §10). Governing docs: the charter (§3 Station F-I), the Gate-0 closing memo §5 (caveat), §6 (yellow flag), §10.1 (structural cause + octonionic locus), §10.3 (the fork).

Repo identity (mandatory preamble of every prompt and report):

```txt
Canonical repo:   C:\Dev\202cl\PlatonicEngine202   (native Windows is sole authority)
Canonical branch: Claude-child
Decoy warning:    C:\Dev\PlatonicEngine is NOT the campaign tree.
Gate: path + branch + HEAD, all three, before any instruction.
```

GOVERNING CONSTRAINT (binding on every F-I document, verbatim):

```txt
The Gate-0 pass is QUATERNIONIC. The hub link variables lie in an associative quaternion
subalgebra Q; the holonomy is well-defined BY ASSOCIATIVITY, not by taming non-associativity.
Octonion non-associativity is DORMANT for the valid observable; where it appears it appears
only as ill-definedness (Policy C) and as non-invariance under the illegitimate conjugation
gauge -- never as a richer well-defined observable. Gate 0 PASS means a QUATERNIONIC observable
exists; it does NOT mean the field problem is solved, and it gives the field NO octonion-non-
associativity support from the hub holonomy.

YELLOW FLAG (Gate-0 memo §6): the strong octonion-field thesis has no support from the hub
holonomy. Every F-I document must say so wherever it reaches for octonionic content.
```

## 1. The F-I question (charter §3)

```txt
What does the field emit?
F-I designs, model-card-first, a carrier-borne EMISSION LAW over the D1/D3 source state:
genuine field quantities (multi-component / algebra-valued amplitude, polarization, frame),
maturity level 2 (fiber-valued emitted field, source-lookup forbidden), level 3 (carrier-
dynamic transport / connection field) as the declared horizon. F-I is DESIGN ONLY: no field
substrate (F-II), no certification (F-III), NO field-activity claim of any kind.
```

## 2. The §10.3 fork — the two tracks

The structural fact from Gate-0 §10.1 fixes where each track lives: child lifts are products of two primal-quadrangle units and land on the complement Fano line {e3,e5,e6} = the quaternion subalgebra Q; hub-internal holonomy words can never leave Q. Genuinely octonionic content exists only in words that LEAVE Q — loops mixing primal-quadrangle units with child-line units, i.e. loops traversing birth edges of the carrier graph alongside hub-internal edges.

```txt
TRACK (a) QUATERNIONIC-FIRST -- broadcast the validated observable.
  Object: Re of the Q-holonomy (Gate-0-validated: triangles & squares +1, hexagons -1).
  Status: buildable on a proven object; modest; carries no octonion content.

TRACK (b) OCTONIONIC EXTENSION -- a Gate-0-grade validity contract for the mixed/birth-edge
  loop classes (where Q is left and non-associativity is LIVE).
  Work: the entire Gate-0 gauntlet on these loop classes BEFORE any broadcast design --
    WELL-DEFINED (bracketing: which mixed classes are 2-generated and saved by Artin/Moufang
      diassociativity vs genuinely bracketing-dependent -- COMPUTED per class, never assumed),
    GAUGE-INVARIANT (under the ruled automorphism gauge, global + site-local),
    INFORMATIVE (strong controls; reality non-generic; mock-scramble; Rider A).
  Status: a NEW validity gate, not a broadcast. Its failure is K4-adjacent evidence against
    the strong octonion thesis; its pass is the campaign's actual prize.
```

## 3. The forbidden line

```txt
- (b) is Gate-0-grade and MUST precede ANY broadcast design on mixed-loop observables.
- FORBIDDEN (§10.3): building a broadcast on UNVALIDATED mixed-loop observables.
- No F-I document may describe the inheritance as a working field needing improvement; it is a
  validated QUATERNIONIC observable plus an UNVALIDATED octonionic sector (Closure clause §5).
```

## 4. Fork sequencing — SET (sovereign, 2026-06-13)

```txt
RULING: (b) FIRST; (a) DEFERRED, not canceled.

The mixed/birth-edge validity gauntlet runs first as the decisive, cheap,
Gate-0-grade test of the campaign's real thesis. The emission-law model card
is then designed ONCE, knowing whether it must carry one sector or two —
no quaternionic-only law that gets reworked if (b) validates. (a) resumes
either way: as the sole carried sector if (b) fails, or alongside the
validated octonionic sector if (b) passes.
```

## 5. F-I deliverables (re-sequenced under the ruling)

```txt
PHASE 1 — the (b) validity contract (Gate-0-grade):
  1. Mixed loop-class inventory on the carrier graph (birth edges + hub-internal
     edges + the C1 channel families), with link variables DERIVED per §10.2
     sharpening — never freely chosen.
  2. Per-class 2-generation analysis (computed); bracketing DP over all
     parenthesizations for non-saved classes; the ruled automorphism gauge,
     global + site-local; informativeness controls; Rider A throughout.
  3. Sealed predictions (values, thresholds, total decision rule) filed to
     mothership and hash-committed BEFORE the run.
  4. Auditor-derived verdict per class: validated / ill-defined / uninformative /
     gauge-variant — the octonionic sector's possession register.

PHASE 2 — the emission-law model card (designed once, after Phase 1):
  5. The emitted OBSERVABLE SET stated in advance (visible-field manifest);
     NO carrier label in the emitted set (anti-staple at design time).
  6. Reduction honesty: what the tuple reduction LOSES, stated explicitly.
  7. Carriage-in-principle of Re(Q-holonomy) (track a), plus the validated
     mixed-loop observables IF AND ONLY IF Phase 1 validated them.
  8. Every element marked [carries-validated-observable] /
     [requires-(b)-validation] / [forbidden-until-validated].
```

## 6. Discipline / constraints

```txt
- Plan-mode-first; expected outcomes withheld from the implementer; Repo-Identity
  Gate heads every prompt and report.
- F-I is DESIGN + the Phase-1 validity diagnostic: NO field substrate, NO
  certification, NO field-activity claim, NO broadcast on unvalidated loops.
- Consume D1/D3 + Gate-0 results READ-ONLY; defects ESCALATE, never silent fixes.
- Frozen (inherited): no S0, no topology, no packet writing, no Shape mutation,
  no operation registration, no new route/gate/support/region families, no UI,
  no universal-law language.
- Budget: plan + 1-4 runs + memo; a breach is the drift signature -> stop, escalate.
```

## 7. Exit criteria

```txt
1. Phase 1 verdict per mixed loop class, sealed-rule-derived, ratified.
2. The emission-law model card RATIFIED (designed once, post-Phase-1), with
   manifest-in-advance, reduction-loss honesty, carriage-in-principle, and
   validation-status marking complete.
3. The governing caveat and yellow flag present verbatim wherever octonionic
   content is touched.
4. F-I closing memo drafted for mothership ratification.
```

## 8. Sequencing after F-I

```txt
F-I ratified -> F-II field substrate build (validated sector(s) only)
            -> F-III blind certification gauntlet (Riders A/B; N=3 cycles)
            -> F-IV human fruit (vs the predecessor's archived D4 baseline).
```

## 9. Ratification

```txt
Lieutenant: F-I entry prepared under the §10.3 fork; recommendation (b)-first submitted.
Mothership: [X] entry RATIFIED AND ISSUED with sharpenings (§10);
            [X] fork sequencing SET: (b) first, (a) deferred (sovereign, 2026-06-13).
Human (Arman): fork confirmed; commits this entry to docs/governance/ on Claude-child.
Next: the lieutenant prepares the F-I Run-1 plan — the Phase-1 (b) validity-contract
  design with sealed predictions — under this authorization, never self-initiated.
```

## 10. Mothership sharpenings (binding)

```txt
10.1 TWO-GENERATION COMPUTED, NEVER ASSUMED (elevating the draft's own note to law):
  the per-class bracketing status (saved-by-theorem vs bracketing-dependent) is a
  COMPUTED deliverable with the same exactness as Gate 0's DP — all
  parenthesizations for any class not proven 2-generated.

10.2 LINK VARIABLES DERIVED, NEVER CHOSEN: the transport on every non-hub edge
  (birth, parent-return, projection-loop, complement-coupling) must be DERIVED
  from the existing C1 channel rows / F1 transport tables, consumed read-only.
  A freely chosen link variable is the anti-staple violation at the definition
  level — free choice could tune the mixed sector toward well-definedness. If
  the existing rows underdetermine a needed transport, that is an ESCALATION
  (a genuine gap in the carrier theory), not a design freedom.

10.3 DESIGN-ONCE RATIONALE (recorded): the (b)-first ruling exists so the
  emission law is designed exactly once, against a known sector inventory.
  Any pressure to start broadcast design before Phase 1 closes is the drift
  signature of this station.
```

The hub gave us a real observable and told us, precisely, where it stops: at the edge of Q. F-I walks to that edge — the birth edges — and asks Gate 0's three questions again where the answer is not already known. Nothing octonionic is carried until it is validated.
