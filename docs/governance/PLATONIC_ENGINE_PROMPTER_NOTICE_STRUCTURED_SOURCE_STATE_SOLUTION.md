# PlatonicEngine Notice to the Prompter/Planner
## Structured Source-State Solution to the Source-Signature Blockage

Audience: the Prompter/Planner/Auditor lieutenant working on PlatonicEngine’s FieldCueV0, GeneratedSiteReadingV0, and source-signature proving sequence.

Status: mothership ruling and operational notice. This is not a Codex prompt. It is not a UI plan. It is not a topology plan. It is not a permission slip for general algebra infrastructure. It is the corrected solution-frame for the source-signature blockage that halted field-cue / generated-site legibility work.

Repository: `selahvarziarman-byte/recursive-ambo`

Current priority: **Structured Source-State Diagnostic v0**.

Supersedes: the previous mothership vital note that approved the Pythagorean Tetrachord Quark Regime v0 as the first source-signature proving candidate. That regime is no longer the main solution. It remains valuable as a harmonic scalar baseline and diagnostic control.

---

## 0. Executive ruling

The problem is now solved at the level needed for planning.

The solution is not another scalar profile fixture.

The solution is not simply a better tuple.

The solution is not an antipodal tag added after derivation.

The solution is:

```txt
Structured Source-State and Antipodal-Covariant Inheritance Regime v0
```

In mothership language:

```txt
source signature ≠ emitted tuple

source signature = structured source state
emitted tuple    = field-facing projection / reduction of that state
```

The field atlas may still consume the old scalar tuple:

```txt
amplitude
waveNumber
phase
attenuation
```

But those four numbers are no longer allowed to be treated as the whole source signature. They are only the field-facing projection of a richer state.

The active task is therefore:

```txt
Build a finite algebraic source-state capsule for the one-Ambo tetrahedron event,
then reduce it honestly to the existing emitted tuple,
then report what survives, what is reduced, and what is lost.
```

This is not caution for its own sake. This is the project refusing to let generated structure disappear into weak numbers.

---

## 1. Why the previous solution was incomplete

The previous proposed correction, the Pythagorean Tetrachord Quark Regime v0, was a serious improvement over the uniform-circle fixture.

It correctly introduced:

```txt
finite harmonic slots;
log-ratio handling;
non-arbitrary source-profile generation;
Quark-weighted child inheritance;
pair-sum auditability;
all-six-child scalar differentiation in principle.
```

But it still treated the source signature mainly as scalar harmonic inheritance.

For child `M_AB` with complement `CD`, the tetrachord/log-ratio child rule gives a scalar result of the form:

```txt
x_AB = [ √3(x_A + x_B) + (x_C + x_D) ] / [ 2(√3 + 1) ]
```

For the antipode:

```txt
x_CD = [ √3(x_C + x_D) + (x_A + x_B) ] / [ 2(√3 + 1) ]
```

Their contrast is compressed by:

```txt
(√3 - 1) / (2(√3 + 1)) ≈ 0.134
```

That means the scalar regime can make the six children non-identical while still muting the stronger generated structure: three antipodal axes in the octahedral child core.

The scalar regime says, in effect:

```txt
these children differ a little in frequency.
```

But the generated geometry says:

```txt
these six children form three complement / antipodal pairs:
AB ↔ CD
AC ↔ BD
AD ↔ BC
```

That is a different order of fact.

The previous regime improved the tuple. It did not repair the source-signature kernel.

---

## 2. The deeper error: tuple as signature

The previous stack had slid into this hidden equation:

```txt
source signature = amplitude + waveNumber + phase + attenuation
```

That equation is now rejected.

The tuple is necessary because the current field atlas computes complex superposition from scalar parameters. But the tuple is not the whole source.

The original source-profile contract already implied this distinction when it said that the child must retain parent edge, complement edge, antipodal child, Quark channels, ratio constants, derivation record, and degeneracy diagnostics, and that the numeric tuple is only the reduced result of structured derivation.

The new ruling makes that implication explicit:

```txt
A source signature is a representation-bearing structured state.
The emitted tuple is a lossy projection into the current field sampler.
```

If the tuple cannot express a structural fact, the diagnostic must say so.

A field-cue built on an unexamined tuple is not yet trustworthy.

---

## 3. The new solution

The solution is a finite, event-bound, algebraic source-state capsule.

Use this name unless the human later chooses another:

```txt
Tetrahedral-Ambo Source-State Algebra v0
```

or, as regime name:

```txt
Structured Source-State and Antipodal-Covariant Inheritance Regime v0
```

This is not a general algebra engine.

This is not merely metadata.

It is a closed algebraic account of the current proving event:

```txt
four primal vertices
one Ambo operation
six generated midpoint children
three complement / antipodal axes
one source-domain event
one tuple reduction into the existing field atlas
```

The source-state capsule must be algebraic in law and finite in scope.

That is the balance:

```txt
full algebraic seriousness now;
full general algebra machinery later.
```

---

## 4. What the capsule must contain

The capsule must make the tetrahedral Ambo event native inside source-signature space.

At minimum it should contain the following conceptual structures.

### 4.1 Primal vertex states

```txt
A
B
C
D
```

These are source-domain roles, not philosophical names and not harmonic slots by themselves.

### 4.2 Generated edge / bivector states

```txt
AB
AC
AD
BC
BD
CD
```

These correspond to the six Ambo midpoint children:

```txt
M_AB
M_AC
M_AD
M_BC
M_BD
M_CD
```

The natural conceptual model is edge-state / bivector-like:

```txt
AB ≈ A ∧ B
AC ≈ A ∧ C
...
```

Implementation v0 does not need a full exterior-algebra library. But it must not reduce these states to labels attached after scalar derivation.

### 4.3 Complement / Hodge-like involution

The complement operation must be internal:

```txt
c(AB) = CD
c(AC) = BD
c(AD) = BC
```

Equivalent notation may use a Hodge-like star:

```txt
*(A ∧ B) = C ∧ D
*(A ∧ C) = B ∧ D
*(A ∧ D) = B ∧ C
```

up to declared orientation convention.

The crucial requirement is:

```txt
complementarity acts inside source-state space.
```

Not in UI labels. Not in after-the-fact metadata. Inside the state.

### 4.4 Orientation / polarity convention

The capsule must declare whether orientation is active and how it is represented.

Possible v0 convention:

```txt
AB = -BA
```

or a conservative unsigned convention plus a declared polarity action under complement.

Do not let orientation be implicit. If it is not active, say so. If it is active, audit it.

### 4.5 Incidence and face relations

Antipodality was the visible missing feature. It may not be the only one.

The capsule must preserve enough local structure to audit:

```txt
edge incidence;
face incidence;
source-edge relation;
complement-edge relation;
projection-source relation;
axis-pair membership;
source-edge vs complement-edge tension;
projection asymmetry;
children equivalent only after tuple projection.
```

This is what prevents patchwork.

The human noticed antipodality. The algebraic capsule must be able to reveal what the human did not yet know to ask for.

### 4.6 Tetrahedral relabeling covariance

The capsule must not depend on accidental slot order.

Relabeling vertices should relabel source states and derivations coherently.

This does not require a full group-theory engine. It requires an explicit finite covariance audit for the current event.

---

## 5. Harmonic component: keep the tetrachord, but demote it

The Pythagorean tetrachord is not discarded.

It becomes the first harmonic scalar baseline inside the structured source state.

Current hierarchy:

```txt
uniform-circle fixture:
  bad control

Pythagorean tetrachord scalar regime:
  harmonic scalar baseline / diagnostic control

Structured source-state regime:
  active source-signature kernel candidate
```

The harmonic component may remain:

```txt
HarmonicSourceState = log-ratio-v0
```

with example ratios:

```txt
1/1, 9/8, 81/64, 4/3
```

But the source state must not be identical with that harmonic scalar.

The future harmonic horizon may eventually involve note-states, spectral packets, chordal states, finite harmonic envelopes, or richer musical structures. Do not build those now. But do not design the v0 type so narrowly that they cannot later replace the scalar log-ratio component.

The immediate law:

```txt
harmonic scalar is one component of source state;
it is not the whole source state.
```

---

## 6. Child derivation under the new regime

The Quark inheritance grammar remains important.

Do not throw it away.

But Quark derivation must now operate around structured states, not merely scalar tuples.

For a child such as `M_AB`, the child state must carry:

```txt
source edge = AB
parent endpoint states = A, B
projection states = C, D
complement edge state = CD
antipodal state = M_CD / edge CD
Quark channel records = A/C, B/C, A/D, B/D
harmonic inherited state
structural edge state
reduction record
```

The correct division is:

```txt
common-mode harmonic inheritance
+
differential structural polarity
```

Do not interpret small scalar spread as automatic failure if structural polarity becomes field-active elsewhere.

Do not interpret rich derivation metadata as success if the tuple projection loses everything and fails to say so.

---

## 7. Tuple reduction: the current atlas remains unchanged

The field atlas remains unchanged for the next corrective branch.

The current atlas expects emitted parameters:

```txt
amplitude
waveNumber
phase
attenuation
```

The new source-state regime must reduce structured states into those parameters deterministically.

The first reduction posture should be:

```txt
waveNumber:
  derived primarily from harmonic component

phase:
  derived from harmonic phase plus structural polarity projection

amplitude:
  neutral in v0, unless a state-norm law is explicitly justified

attenuation:
  neutral / conservative in v0, unless a reach-locality law is explicitly justified
```

The strongest immediate carrier of structural polarity is phase/sign.

But it must be derived from the structural state, not appended manually.

For example, a declared phase projection may satisfy:

```txt
π_phase(σ(q_child)) = π_phase(q_child) + π
```

or the corresponding relation under the chosen complement / polarity involution.

The planner must not prompt Codex to “add π to antipodal children” as a patch. The prompt must require that the phase relation follows from the source-state reduction law.

---

## 8. Reduction honesty is mandatory

Every emitted tuple under this regime must carry a reduction report.

The reduction report must say:

```txt
what structure was preserved in field-active form;
what structure was reduced or compressed;
what structure remained derivation-only / metadata-only;
what structure was lost by tuple projection;
whether the tuple is too narrow for the state.
```

Expected statuses may include:

```txt
field-active-structure-preserved
partial-structure-loss
tuple-too-narrow
```

This is not bureaucracy. This is how the project prevents false field-cues.

A generated-site reading may eventually say:

```txt
This child’s harmonic inheritance is field-active in waveNumber.
Its antipodal polarity is field-active in phase.
Its face-incidence structure is retained in source-state provenance but not active in the emitted tuple.
```

That is the kind of truth the human needs.

---

## 9. The next authorized branch

The next branch should be planned as:

```txt
Structured Source-State Diagnostic v0
```

Do not call it FieldCueV0 work.

Do not call it UI work.

Do not call it topology.

Do not call it general algebra.

It is a pure diagnostic source-kernel branch.

### Authorized scope

```txt
one named/profile-assigned tetrahedron;
one Ambo event;
four primal structured states;
six child structured states;
finite tetrahedral-Ambo source-state capsule;
harmonic log-ratio component, using tetrachord as baseline/control;
edge/bivector-like child states;
complement / antipodal involution;
incidence and projection relations where needed for audit;
structured Quark derivation records;
deterministic tuple reduction;
reduction honesty report;
antipodal covariance audit;
structural covariance / relabeling audit;
comparison against uniform-circle bad control and tetrachord scalar baseline.
```

### Forbidden scope

```txt
UI;
persistence;
field atlas replacement;
multi-component field engine;
full exterior-algebra infrastructure;
Clifford algebra engine;
general harmonic universe;
source-profile editing UI;
Shape mutation;
packet writing;
operation registration;
route/gate/support/region expansion;
semantic naming;
topology;
recursive-generation generalization;
multi-shape generalization.
```

These prohibitions are not ambition-killers.

They protect the ambition from dispersing.

The dream is large: a field cue that can eventually show inherited pressure, movement, interference, routes, gates, loops, regions, vortices, phase transformation, and naming pressure.

The current proof is narrower: can the generated child’s own structure become source-active without being flattened into a scalar tuple?

---

## 10. Required audits

A Codex branch is not acceptable unless the planner can audit these points.

### 10.1 Finite-system audit

```txt
No arbitrary tuple palette.
No free per-source sliders.
Primal harmonic profiles come from a named finite system.
Human assignment to slots is recorded as setup/provenance.
```

### 10.2 Algebraic capsule audit

```txt
A, B, C, D exist as primal source states.
AB, AC, AD, BC, BD, CD exist as generated edge states.
Each child state knows its endpoints.
Each child state knows its complement edge.
Each child state knows its antipodal child.
Orientation/polarity convention is declared.
Incidence/projection relations are available for diagnostics.
```

### 10.3 Antipodal covariance audit

For all three pairs:

```txt
AB ↔ CD
AC ↔ BD
AD ↔ BC
```

the diagnostic must prove a declared internal relation:

```txt
D(c(edge)) = σ(D(edge))
```

or equivalent under the chosen finite source-state operation.

### 10.4 Structural covariance audit

A relabeling of the tetrahedron must not break the source-state logic.

At v0 this can be finite and diagnostic. It does not require a universal group-action engine.

### 10.5 Tuple-reduction audit

For every primal and child state:

```txt
emitted tuple is finite;
emitted tuple is derived from state;
reduction reports preserved structure;
reduction reports reduced structure;
reduction reports lost / metadata-only structure;
neutral axes are marked neutral;
tuple is not described as full source signature.
```

### 10.6 Unknown-feature retention audit

The diagnostic must support asking:

```txt
Which structural distinctions vanish during tuple reduction?
Which children are distinct in source-state space but equivalent or near-equivalent after tuple projection?
Which features are field-active?
Which remain retained but not field-active?
```

This is the anti-patch audit. It exists because antipodality may not be the only feature the scalar regime missed.

### 10.7 Baseline comparison audit

The diagnostic must compare:

```txt
uniform-circle fixture:
  bad control

Pythagorean tetrachord scalar regime:
  harmonic scalar baseline

structured source-state regime:
  active source-kernel candidate
```

The comparison must show not only that children differ numerically, but what structural facts the new regime preserves or activates that the scalar baseline mutes.

---

## 11. Prioritized planning sequence

The planner should prioritize the next work in this order.

### Priority 1: Amend the source-profile contract

The contract must now explicitly state:

```txt
source signature is structured state;
emitted tuple is field-facing reduction;
reduction loss must be reported.
```

A separate amendment document has been prepared for the project container.

Do not plan new implementation work as if the old tuple-centered contract were sufficient.

### Priority 2: Inspect the repo for tuple/signature conflation

Before writing any prompt, inspect the existing source-profile stack and identify where the repo currently treats `derivedParameters`, `emissionTuple`, or similar scalar objects as if they were the source signature itself.

Likely areas:

```txt
src/lib/fieldSourceProfiles.ts
src/lib/fieldSourceChildContexts.ts
src/lib/fieldSourceQuarkChannels.ts
src/lib/fieldSourceChildDerivations.ts
src/lib/fieldSourceChildDegeneracy.ts
src/lib/fieldSourceProfileAwarePolicy.ts
src/lib/fieldSourceProfileAwareAtlasAdapter.ts
src/lib/fieldSourceProfileAwareAtlasExecution.ts
src/lib/fieldSourceProfileAwareShapeResolvedSurfaceAtlas.ts
FieldCueV0 / GeneratedSiteReadingV0 files if already present
```

Use the repo as factual authority. This notice is strategic authority.

### Priority 3: Plan the pure source-state diagnostic

The first branch must be diagnostic-only. It should create structured source states and tuple reductions for the one-Ambo tetrahedron event.

It must not touch UI.

It must not change the field atlas.

It must not build field features.

### Priority 4: Compare against controls

Do not delete old regimes.

They are needed as witnesses:

```txt
uniform-circle bad control shows the old failure;
tetrachord scalar baseline shows why better scalar differentiation is still insufficient;
structured source state shows whether the kernel is repaired.
```

### Priority 5: Only after source-state audit passes, reconnect to FieldCueV0

FieldCueV0 and GeneratedSiteReadingV0 should not be trusted until the source-state diagnostic passes.

After passing, the field-cue can begin to say:

```txt
what the child inherited harmonically;
what structural polarity it carries;
which structural facts survived tuple projection;
which were lost;
which field behaviors followed.
```

That is the bridge back to the human naming problem.

---

## 12. How to read project documents for this task

Use this hierarchy.

### 12.1 Read as current law

```txt
PLATONIC_ENGINE_EVENT_LEGIBILITY_PIVOT_CHARTER.md
PLATONIC_ENGINE_FIELD_CUE_TARGET_CONTRACT_V0_2.md
Structured Source-State amendment to the source-profile contract
this notice
```

### 12.2 Read as original foundation

```txt
PLATONIC_ENGINE_GROUND_PLAN_REWRITTEN.md
PLATONIC_ENGINE_FIELD_LAYER_APPENDIX.md
```

The Ground Plan supplies the large invariant: generated worlds must remain intelligible after transformation.

The Field Layer Appendix supplies the field invariant: generated vertices join the source population and the field is a derived interference atlas, not a decorative overlay or naming machine.

### 12.3 Read as local constitutional source, now amended

```txt
Field Source Profile and Quark-Coherent Child Inheritance Contract v0.2
```

Keep its core laws:

```txt
no free numeric emitters;
finite profile systems;
human assignment to generated slots;
Quark-coherent child derivation;
edge-complement and antipodal structure retained;
derivation records and degeneracy diagnostics preserved.
```

But amend its tuple language:

```txt
emission tuple is not the whole source signature.
```

### 12.4 Read as diagnostic history / controls

```txt
Extraordinary Notice: Source-Signature Regime Failure
Inspector Report: Source-Signature Proving Regime
Mothership Vital Note: Source-Signature Proving Regime
```

These explain why uniform-circle failed and why tetrachord helped but did not finish the problem.

### 12.5 Read lightly or avoid for this branch

Do not spend time on topology documents, UI branch reports, route/gate/support expansion reports, or general field visualization plans unless they are needed to verify a dependency. This branch is source-state kernel work.

---

## 13. Branch permission test

A proposed next branch is permitted only if it satisfies the primary condition:

```txt
It builds or audits the structured source-state kernel for the one-Ambo tetrahedron proving event.
```

It must also satisfy all negative conditions:

```txt
no UI;
no topology;
no packet writes;
no Shape mutation;
no field atlas replacement;
no new route/gate/support/region machinery;
no mature field-feature claims;
no general algebra engine;
no general harmonic universe;
no source-profile sliders;
no generalization beyond one-Ambo tetrahedron;
no treating emitted tuple as full source signature.
```

If the branch does not help source-state truth, it is not the next branch.

---

## 14. What success looks like

Success is not “the tuple numbers look interesting.”

Success is:

```txt
The project can derive four primal structured source states and six generated child structured source states.
The six child states live in a finite tetrahedral-Ambo algebraic capsule.
Complement / antipodal relations are internal operations, not tags.
The child derivation is covariant with the tetrahedral Ambo structure.
The tuple reduction is deterministic and honest.
The diagnostic says which structures become field-active and which do not.
The uniform-circle and scalar tetrachord regimes remain available as controls.
FieldCueV0 can later consume this without lying about source meaning.
```

The decisive improvement is not prettier math.

The decisive improvement is that the generated world’s own structure begins to speak into the field.

---

## 15. Final instruction to the Prompter/Planner

The blockage is resolved conceptually.

Do not return to UI polishing.

Do not return to scalar tuple repair.

Do not build a vast algebra machine.

Do not shrink the ambition into safety language.

The correct next move is exact and strong:

```txt
Plan the Structured Source-State Diagnostic v0 branch.
Make source signatures representation-bearing.
Keep the event finite.
Reduce to the current tuple honestly.
Audit what survives and what is lost.
Then, and only then, reconnect to FieldCueV0.
```

This is not hygiene as blockage.

This is the project acquiring the vessel strong enough to carry its madness.
