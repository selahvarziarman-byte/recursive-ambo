# PlatonicEngine — Corrected Reading: Supersession Chain, the Topological Destination, and the Two-Campaign Remainder

Supersedes the prior flat-read map. Mothership finding, 2026-06-14, branch `team-arman`. This corrects three errors the sovereign named: (1) I read the docs as a flat pile instead of a supersession chain; (2) I under-described the topological module — the actual destination; (3) I mis-framed the legibility "halt." Every claim is cited to a repo file; ignorance is reported where the repo is silent.

---

## 1. The docs are a SUPERSESSION CHAIN, not a flat pile

Precedence is stated outright in the Lieutenant Initiation §4 / §4.7 (`docs/archive/PLATONIC_ENGINE_PROMPTER_LIEUTENANT_INITIATION.md`):

```txt
Repo            = what IS            (factual authority for implemented state)
Ground Plan     = WHY               (identity + layer distinctions; a HORIZON, not a backlog)
Field Appendix  = what the field SHOULD MEAN   (read with restraint; NOT an expansion mandate)
Source-profile/Quark contract = how source emission is constrained (event-bound)
EVENT LEGIBILITY PIVOT CHARTER = what is ALLOWED NOW   (current strategic law)
Field Cue Target Contract = sharpens what the field must answer to, under the pivot
Human / mothership = decides when the law changes
```

The hinge is the **Event Legibility Pivot Charter (2026-06-07)**. It explicitly supersedes stale "expand the field" recommendations (Charter §14; Lieutenant §4.2) and did three reframing things: the field LOSES SOVEREIGNTY (becomes a witness), the GENERATED SITE becomes the center, TOPOLOGY is deferred as a horizon constraint. So the whole field-generalizability -> carrier -> W-gate arc was NOT the spine — it was the sanctioned "prove generality / generality audit" sub-quest the charter itself flagged (§8). My prior report treated the campaign docs as the main line; the charter had already subordinated them. That was the flat-reading error.

---

## 2. The topological module IS the destination (the part I under-described)

The repo names it in escalating fullness:

```txt
- Ground Plan §4.5 "Semantic-topological workspace": "an independent process space"
  answering "Where do names live after topological transformation, and what semantic
  conditions govern that habitation?" Binding law: "Topological co-location is NOT
  semantic identity" -> support vs identified vs fused vs constrained vs conflicted vs
  rejected. §6.3: import named marks WITH PROVENANCE; separate supports from identity;
  semantic fibers explicit; trace events; quotient != fusion; don't mutate source shape.

- Foundational Inspector (archive) gives the fullest destination line. The pipeline
  (place concepts -> generate -> read lineage/field/birth-law -> name what dwells ->
  repeat) "Eventually produce material for a TOPOLOGICAL LIFE-OF-CONCEPTS WORKSPACE.
  That is the ambition. The present crisis concerns whether the field layer can CARRY
  that ambition."

- THE INHERITANCE LINK (the connective tissue I missed):
  Prompter Vital Note §16: "A future topological unit must NOT inherit UNNAMED or
  FALSELY SIGNED material. That is not a branch. It is a reason to repair source
  signatures NOW."
  Field Cue Contract §18: "The field-cue must preserve provenance and ambiguity so that
  future topology is NOT FED LIES."

- ENTRY RULE (anti-monster): topology begins ONLY when named generated material raises a
  concrete topological question (Charter §9; Lieutenant §10) — from pressure, never desire.
```

So the destination is a **semantic-topological "life-of-concepts" process space**: where named conceptual material, after recursive generation, inhabits topological supports and is identified / fused / quotiented / conflicted / rejected under topological operations — with provenance, semantic-fiber, and trace discipline, governed by the cardinal rule co-location != identity. The field was meant to be the bridge that "carries that ambition" into this space. Since the field-as-carrier is absent, the bridge must instead be the clean, named, honestly-signed source-state material the minor campaign produces.

**Code state (ground-truthed):** the module is UNBUILT. What exists is a cell-topology SIGNATURE / CLASSIFIER (`src/lib/topologySignature.ts`: `CellTopologySignature`, `GenericAmboReadinessStatus`, `GenericAmboPreview`) and a UI "topology filter" tab (`WorkspaceTopologyContent` in `Panels.tsx`) — structural fingerprinting to gate generic-Ambo readiness, NOT the semantic process space. A related dual / semantic-dual strand exists in vision only (Ground Plan §3.3 Semantic Dual Universe: pyritohedral-icosahedron -> dodecahedron; the `pyritohedralDiagonalization` op; the Jitterbug cuboctahedron<->icosahedron<->octahedron<->tetrahedron and Bench-3 order-5 icosahedral, both marked FORWARD).

---

## 3. Honest ignorance (reported, as instructed)

There is **NO dedicated topological-module design document** in the repo. The destination is specified only as (a) vision fragments (Ground Plan §4.5/§6.3; Foundational Inspector "life-of-concepts workspace") and (b) a recurring discipline plus a single hard inheritance constraint ("don't feed topology lies / unnamed or falsely-signed material"). There is no data model for supports / fibers / traces, no operation set, no quotient-semantics specification — and this is **BY DESIGN**: the anti-monster rule forbade designing topology before the legibility pressure exists. I searched every `.md` in the repo and the topology-relevant `src`. If a richer treatment exists, it is not in this repo as of 2026-06-14. If you hold it off-repo or in mind, I want it — I will not invent it.

---

## 4. The two-campaign remainder (your framing, corrected onto the evidence)

The proving-event / legibility was **HALTED BY DESIGN** — "event legibility before field expansion" (Charter §2; Lieutenant §2) plus the Gate ladder in the Current-State map — so generality could be settled first. Not merely "unfinished." We DISPROVED the field's generality (the carrier-borne observable is absent), but generality WAS found elsewhere and is useful for legibility anyway: the legitimate walk-primitive source-state regime (W-1), the channel theorem, and the 60° / A3 / vector-equilibrium / medial-hub invariants (Bench-1/2, D3; gauge-invariant).

```txt
MINOR CAMPAIGN (next) — complete the NON-TOPOLOGICAL module.
  Make the proving event (named tetrahedron -> one Ambo -> generated sites) genuinely
  legible and nameable. The honest source-state reader (src/lib/honestSourceStateReadingV0.ts)
  is the field-free foundation already in code. Fold in the found-generality; repair
  source-signatures so material is honestly signed; settle the generality audit (Charter §8);
  CLOSE the human-fruit test (old D4). GOAL: clean, named, provenance-bearing material READY
  to hand to the topological unit — exactly the Vital-Note-§16 / Field-Cue-§18 readiness rule.

MAJOR CAMPAIGN (after) — the TOPOLOGICAL module.
  The semantic-topological / life-of-concepts workspace (Ground Plan §4.5), entered ONLY when
  the minor campaign's named material raises a concrete topological question (anti-monster
  entry rule), inheriting the clean material and enforcing co-location != identity.
```

---

## 5. One-line corrected answer

The field layer was a witness, demoted by the Pivot Charter; its carrier-generality is absent, but generality was found elsewhere and feeds legibility. The real destination — which I under-described — is the topological "life-of-concepts" workspace, specified in the repo only as vision + discipline + the inheritance rule ("don't feed topology lies"), deliberately never designed. The remainder is two campaigns: minor = complete the non-topological module into clean, named, honestly-signed material; major = the topological module that inherits it.
