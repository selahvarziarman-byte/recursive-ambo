# PlatonicEngine — W-0 Candidate-W Model Card

## W = the walk-primitive source regime: a source is a basepoint carrying a local walk frame; the algebraic carrier is a derived shadow

Audience: mothership (auditing authority — completeness, hole-boundedness, and presence of a real discriminator) and the human (Arman, sovereign). For the lieutenant's later W-1 build.

Status: **candidate-W model card — DRAFT, submitted for mothership audit. Not a possessed result, not a field law, no diagnostic authorized.** Per the W-gate ruling, this is the W-0 abduction artifact: the nine components, every open slot a single named bounded hole with an enumerated branch set, the §6 anchors hash-committed as the FLOOR check, and one hash-committed CONSTRUCTIVE discriminating prediction (Addition A) as the PRIZE check. Mothership audits this before any W-1 diagnostic is authorized to the lieutenant. Legitimacy (W-1) precedes field activity (W-2).

Repo identity (mandatory preamble): canonical `C:\Dev\202cl\PlatonicEngine202`, branch `Claude-child`. Decoy `C:\Dev\PlatonicEngine` is NOT this project.

Issued: 2026-06-13. By: researcher, under the Source-Regime Reset, the W-gate ruling, and the Mothership Acknowledgment (all 2026-06-13). Sealed predictions: held **off-repo by the sovereign** (Rider A — the W-1 implementer must not read them); committed here only by one-way hash in `..._SEALED_PREDICTION_HASH.txt` (SHA-256 LF `28d6b0d6…48d8`).

---

## 0. Standing, and the W_0 floor (possession language)

This card proposes a candidate W. It claims nothing the diagnostics have not earned. Under the Closure Consciousness Clause: W is **not** a possessed field law; "legitimate source regime" (the W-1 question) and "field-observable regime" (the W-2 question) are distinct possessions, kept separate throughout. The central object — a carrier/fiber observable field — remains **not possessed**.

W_0 (source = octonion unit; child = product of parent units; emission = reduced scalar) is the **floor**: proven source-state-real *as structure*, generalizing across the tetra–octa–cube medial hub at root-level resolution, with a *quaternionic* well-defined holonomy observable, and proven **field-inactive** under the accepted emission stack. W must clear that floor (reproduce its legitimacy) and earn ground above it (predict something W_0 cannot), or its honest verdict is W-FAIL.

---

## 1. The candidate in one paragraph

The Ambo birth hands a child exactly two things: a position and an **unordered** parent pair. W_0 added a *state* (a unit) as primitive and read relations off as products. **W inverts the primacy**: the primitive is the source's **walk frame** — its directed outgoing relations to lineage-neighbors, carrying the intrinsic 60° angular geometry — and the algebraic carrier (a unit) is the *derived shadow* of that frame, recovered as the angular-composition of the walks that bore it. On the hub-internal (Q-confined) sector this shadow coincides exactly with W_0's Fano product, so every ratified result is preserved (no regression below the floor). Off that sector — on mixed loops that traverse birth edges and **leave Q** — the walk frame carries content W_0 cannot: the genealogical bracketing of the walk makes the loop's holonomy bracketing-dependent, and the walk selects a definite value where W_0 sees only ill-definedness to discard. That difference is the prize, and it is sealed.

---

## 2. The nine components

Each component is tagged **[SPECIFIED]** (fixed in shape), **[BOUNDED HOLE]** (a single named slot with an enumerated branch set, §3), or **[DEFERRED→W-2]**.

**(1) Source ontology — [SPECIFIED].** A source is a basepoint `b` equipped with a **local walk frame** `Φ(b)`: the set of directed outgoing relations (walks) from `b` to its lineage-neighbors, each tagged with the operation and parent(s) that created it, together with the intrinsic angular metric of the frame (pairwise walk angles). A source is **not** a unit; its unit, if any, is the shadow of `Φ(b)`. At G0 the four tetra vertices carry frames of three walks each, meeting at 60° (the equilateral faces).

**(2) Walk ontology — [SPECIFIED].** A walk `b→c` is a directed relation seated in `Φ(b)` at a definite angle within the relevant A₂ subsystem plane. Walks compose by **angular advance** along the subsystem's great circle. The carrier shadow of a single walk `b→c` is the unit transition `u(b)→u(c)`; the carrier shadow of a composed walk is the corresponding product, **bracketed by the order of composition** (this bracketing is what (4) and Hole #2 govern).

**(3) Reverse / return law — [BOUNDED HOLE #1].** How the reverse walk `c→b` relates to `b→c`. Branch set in §3.1. The reverse law is *not assumed* to be sign-negation; its sign behaviour is a consequence of (4), and the campaign's evidence (orientation-sign was reconstructed lexicographically and was the weakest cell) is consumed, not re-guessed.

**(4) Angular product law — [SPECIFIED on-lineage; off-lineage extension = BOUNDED HOLE #2].** Composition of two walks = advance by the subtended angle in the A₂ plane. **On-lineage constraint (specified):** restricted to hub-internal (Q-confined) edges, the angular composition's algebraic shadow must equal the Fano product `e_p1·e_p2`. This makes consistency with the Bench-2 60°/vector-equilibrium result automatic, and it is what holds W at the floor. **Off-lineage extension (Hole #2):** how the law brackets walks across birth edges (where the word leaves Q and bracketing becomes physical) is the one genuinely open law; branch set in §3.2. Per mothership's Correction 1, on-lineage coincidence with Fano is *necessary but not sufficient* — sufficiency requires (4) to be **richer than Fano exactly off Q**, which is where the prize lives.

**(5) Child-inheritance law — [SPECIFIED], incorporating Addition B.** A child's walk frame is generated from the two parent frames meeting at the birth edge; the child's derived carrier is the angular-composition shadow `= e_p1·e_p2`. The octonion product reappears as a *consequence* of walk-composition, never a primitive. **Addition B (midpoint→flag map as a derived fact):** the identification of each midpoint-child (geometric, born on an octa edge) with its ordered A₃ root `εᵢ−εⱼ` / hub flag `i→j` must be **produced** by this inheritance law (from the directed walk that bore the child), and must be **gauge-equivariant**. It may not be hand-supplied by coincidence of count. This is a legitimacy criterion (§4) and a candidate discriminator site.

**(6) Legitimacy criteria — [SPECIFIED].** Enumerated in §4, each paired with the destructive test that must fail if W's defining facts are removed. In shape: correct parentage; walks close under the operation; angles preserved (60°, edge = circumradius); **antipodality derived** as child-level root negation (the `j→i` child is the reverse-walk of the `i→j` child); **loop-closure derived** (triangle/square +1; A₂-hexagon −1 forced by `((−a)(−b))(−c) = −((ab)c)`); correct counts under the declared population policy; **equivariance** under S₄ on the base and the 168 Fano gauge on the shadow; **no arbitrary carrier** (the carrier is forced by walk-composition, not chosen); and the **midpoint→flag map derived** (Addition B).

**(7) Field propagation law — [DEFERRED→W-2], with a binding guard (Addition C).** Not designed here. Recorded now and binding so it cannot drift: the field observable must be **relational / loop-borne** (holonomy, phase defect, vortex, cancellation axis), never one scalar per source — per-source reduction is the proven cause of the IV-A field-death. **Addition C guard:** the W-2 observable must beat controls by being **not recoverable from bare geometry/topology alone**. IV-A's lone partial reach (the unsigned antipodal *pairing* from positions) was an exposed-geometry channel (controls = real), not a propagation signal; any relational quantity a correctly-drawn cuboctahedron would also exhibit tests the shape, not the field. The observable must *require* W's carried walk content, blind-recoverable, label-stripped.

**(8) Observable / recovery law — [DEFERRED→W-2].** Blind recovery, label-stripped, beating controls; mediated recovery permitted; form not predetermined. The natural W-2 candidate, flagged not decided: the **bracketing-selection observable** of §5 (the off-Q holonomy branch the walk frame selects), which is relational by construction and — by §5's contrast with the exposed channels — is *not* readable from bare geometry.

**(9) Reduction-honesty law — [SPECIFIED].** Any declared reduction from walk frame to what the field sees must confess its loss explicitly and must clear the W_0 floor (it must at least preserve the quaternionic Re-holonomy Gate 0 validated). The reduction that would re-collapse the frame to one scalar per source is named **forbidden** (it reproduces the IV-A death by construction).

---

## 3. The two bounded holes (enumerated branch sets — the entire open budget)

Exactly two substantive holes. Each is a single named slot with a finite, independently-testable branch set; the rest of W is fixed while a hole's branches are swept. No third hole, hidden or otherwise; the W-2 components are deferred by the gate's own ordering, not held open as design escape.

### 3.1 Hole #1 — reverse / return law

```txt
branch set (exactly one will be selected by W-1):
  R-neg   : c->b = sign-negation of b->c (carrier shadow: u_c->u_b = -(u_b->u_c)).
  R-ret   : c->b = a distinct "return" relation with its own status (not a mere sign),
            carrying a provenance tag; antipodality must still be derived, not the input.
  R-anti  : c->b = the walk at the antipodal angle in the frame; its sign is INHERITED
            from the angular product law (4), not posited.
default lean: R-anti (sign derived from (4)); pre-tested branch available: the octa
  second-instance already derives edge direction GEOMETRICALLY (stella-octangula
  positive-face-class boundary; lexicographicSortingUsed:false) and passed Bench-2 —
  W-1 evaluates that geometric orientation as the concrete R-anti instance.
each branch independently finite-testable against F1 (antipodality) and the destructive test D1.
```

### 3.2 Hole #2 — off-lineage angular extension (the bracketing-selection rule)

```txt
the law that brackets walk-composition across birth edges, where the word leaves Q and
bracketing is physical (on-lineage it is fixed = Fano, §2(4)).
branch set (exactly one will be selected by W-1):
  B-gen   : genealogical bracketing — a traversed child enters as its bracketed
            (parent.parent) sub-expression; the loop associates by the birth tree.
  B-walk  : walk-order bracketing — the walk "continues" (right-association along the
            path b->c->d => b.(c.d)), the child read as a walk-in-progress not a finished unit.
  B-frame : frame-angular bracketing — the bracket is fixed by the A2-plane angular order
            of the consecutive walks (the same rule that orders the hexagon roots).
each branch selects a definite holonomy branch on the sealed off-Q loop (§5); W-1 sweeps the
three and reports which (if any) is gauge-equivariant. If none selects gauge-equivariantly,
the prize fails and W collapses toward the quaternionic floor.
```

---

## 4. Legitimacy criteria ↔ destructive tests

Each criterion is paired with the diagnostic that must **fail** when W's defining fact is removed (the ontology-level mock-solution test). A criterion that survives deletion of its own premise is decorative and void.

```txt
criterion                                   destructive test (must FAIL if the fact is removed)
L1 correct parentage (child from its edge)  scramble createdBy.sourceVertexIds -> inheritance breaks
L2 walks close under the operation          drop one walk from a frame -> loop-closure (L5) fails
L3 angles preserved (60deg, edge=radius)    collapse to bare-unit (R6) resolution -> the metric must
                                            BREAK to 90deg/sqrt2 (the ratified Bench-2 R6 fact); if
                                            60deg survives the collapse it was inserted, not derived
L4 antipodality derived (root negation)     remove the reverse law (Hole #1) -> F1 6/6 must collapse
L5 loop-closure derived (+1 / -1)           remove directedness -> hexagon -1 (F3) must collapse to +1
L6 counts correct under population policy    switch current-core <-> cumulative -> counts must change
                                            lawfully, not silently agree
L7 equivariance (S4 base; 168 shadow)       apply a non-automorphism (arbitrary conjugation) ->
                                            verdicts must break (only the legitimate gauge preserves)
L8 no arbitrary carrier                      perturb one carrier off its walk-shadow value -> a
                                            legitimacy check must fire (carrier is forced, not free)
L9 midpoint->flag map derived (Addition B)   hand-supply the map -> equivariance (F6) on the map fails
```

**Population policy (declared).** Primary G2 test object = **current-core** (the 12 cuboctahedron children = the A₃ roots). **Historical-cumulative** (4 + 6 + 12) is carried as a bounded variant for the L6 count test and the equivariance/embedding check. The dependence of G2 legitimacy on whether G1 sources persist is itself a finite test, not a free parameter.

---

## 5. Sealed predictions (withheld off-repo; committed by hash; FLOOR and PRIZE)

The frozen predictions are **held off-repo by the sovereign** and committed here only by one-way hash (`PLATONIC_ENGINE_W_GATE_W0_SEALED_PREDICTION_HASH.txt`; SHA-256 LF `28d6b0d6…48d8`). The plaintext is deliberately NOT in this repo so the W-1 implementer cannot read the predicted values and tune a scorer to them (Rider A); it is revealed and re-hashed at W-1 close. Only the STRUCTURE appears below.

### 5.1 FLOOR check (W reproduces; W and W_0 agree) — structure

W reproduces the ratified hub invariants on the anchored lineage: the Bench-2 order-2 (antipodality) and order-3 (triadic A₂ closure) signatures, the 60°/vector-equilibrium metric, the Gate-0 quaternionic Re-holonomy, the 168-Fano-gauge invariance, and — restated as a value — the Gate-0 confinement (every Q-confined hub loop is bracketing-invariant). The committed figures are the public R12/Gate-0 values; sealing them only fixes that W commits in advance rather than a scorer matching them.

### 5.2 PRIZE check (the discriminator, Addition A) — structure

The prize is a single mixed loop that traverses BIRTH edges and LEAVES Q. Structural claim (all values sealed): on the hub sector W and W_0 agree (bracketing-invariant); on the sealed off-Q loop the holonomy is bracketing-DEPENDENT, and W's walk frame SELECTS a definite branch by a genealogy-derived, gauge-equivariant rule (Hole #2), whereas W_0 — carrying no genealogy — sees only ill-definedness and discards it (its Policy-C verdict). W therefore emits a real holonomy value on that loop which W_0 structurally cannot. The specific loop, its link word, the per-bracketing values, the cardinalities, and the S₄-robustness counts are in the sealed file. **Kill:** if the sealed loop is bracketing-invariant → W-FAIL (floor only); if bracketing-dependent but no gauge-equivariant rule selects the branch → W-FAIL (decorative inversion = W_0 in costume).

The discriminator is **continuous with ratified work**: it is precisely the *negation* of the Gate-0 confinement, off Q. Gate 0 proved the hub is bracketing-invariant (associative, quaternionic); W stakes its non-triviality on the claim that one edge out — across a birth edge — the bracketing becomes a *selected, physical* observable rather than discarded noise. If it does not, W earns nothing beyond the floor, and that is a real, publishable W-FAIL.

---

## 6. The W vs W_0 ledger (what differs, and why it is not regression)

```txt
                       W_0 (unit-primitive)            W (walk-primitive)
primitive              source state = a unit           source = basepoint + walk frame
carrier                primitive                        DERIVED shadow of walk-composition
on-lineage (in Q)      Fano product                     SAME (coincides) -> floor preserved
reverse law            negation (assumed; was fragile)  Hole #1, sign DERIVED from (4)
midpoint->flag map     by coincidence of count          DERIVED (Addition B), gauge-equivariant
off-Q mixed loops      ill-defined -> discarded (Pol.C)  bracketing PHYSICAL -> selected observable
sealed off-Q loop      single value, ambiguity tossed   a definite branch selected [values sealed]
field observable       one scalar/source -> IV-A death   relational/loop-borne (Addition C) [W-2]
```

W is not W_0 renamed: on Q they agree by construction (no regression), and the *only* places they differ — the reverse law's derivation, the midpoint→flag derivation, and the off-Q bracketing selection — are exactly the places where W carries directed-walk content that W_0 lacks, and each is guarded by a destructive test (L4, L9, D1/D2) that fails if that content is stripped. The risk that W is "W_0 in costume" is not waved away; it is made the explicit kill condition P5/D2.

---

## 7. Budget, verdict conditions, and what is not claimed

```txt
OPEN HOLES: exactly two (Hole #1 reverse/return; Hole #2 off-lineage bracketing), each a
  finite enumerated branch set. W-2 components deferred by the ordering law, not held open.
W-1 VERDICTS this card enables:
  W-PASS-LEGITIMATE : floor (F1-F7) reproduced AND prize (P1-P6) passed (a gauge-equivariant
                      branch selects the off-Q observable) AND every L-criterion's destructive
                      test fires. -> authorizes the W-2 field-observability sub-question.
  W-LOCAL-ONLY      : legitimate tetra->octa, fails or needs controlled extension octa->cubocta.
  W-FAIL            : prize false (sealed loop bracketing-invariant) or decorative (no gauge-
                      equivariant selection) or a destructive test fails to fire. A real finding: the
                      walk inversion buys nothing beyond the quaternionic floor.
NOT CLAIMED: that W is a field law; that the field is possessed; that legitimacy is proven.
  This card is an abduction to be tested. The field — the central object — remains absent.
```

The hub gave a real observable and named where it stops: the edge of the quaternion subalgebra. This W stakes its entire claim to novelty on one step past that edge — across a birth edge, where W_0 discards the ambiguity and W proposes to read it. The sealed prize makes that stake falsifiable in a single finite diagnostic. I have tried to break it (the destructive tests are built in, and the kill conditions are explicit); the rest is for the W-1 run to decide.
