# PlatonicEngine Carrier-Borne Field Campaign — Charter
## IN FORCE — finalized 2026-06-11 upon ratified predecessor closure and the sovereign's start confirmation

Audience: the human (Arman, sovereign), mothership, the prompter/planner/auditor, and every future agent seated into the successor campaign.

Status: BINDING campaign charter. Finalization conditions met 2026-06-11: (1) predecessor closing verdict ratified (`PLATONIC_ENGINE_CAMPAIGN_CLOSING_MEMO_FIELD_GENERALIZABILITY.md`); (2) F-IV baseline recorded as the archived dual-reading artifact pair + legibility annex, with the contamination caveat (D4-note declined); (3) sovereign start confirmation given; (4) Gate 0 entry order issued (`PLATONIC_ENGINE_CBF_GATE0_ENTRY_ORDER_OBSERVABLE_BEFORE_BROADCAST.md`). The filename retains "DRAFT" for history; this status line governs.

Drafted: 2026-06-11.

Inheritance (binding premises, not reopened):

```txt
- composite verdict of the predecessor campaign: a generalizable source-state
  carrier law (D1 lift, D3) that is FIELD-INACTIVE under the accepted
  F0/F1/F2/G0 emission stack (IV-A, ratified NEGATIVE, law-relative);
- IV-A anomaly-ledger row 01 as charter: the accepted emission broadcasts one
  complex coefficient per contribution; the fiber's defining observables are
  never emitted;
- the corrected target (Bench-2, internal proof): raw carrier labels are
  GAUGE; only gauge-invariant relations (closures, holonomies, invariant
  pairings) are candidate physical observables;
- the anti-staple / earned-field-activity discipline + Riders A and B
  (New-Field Ruling, Ruling 2) — binding on every station below;
- the Repo-Identity Gate Protocol; sealed predictions filed to mothership
  and hash-committed BEFORE every run; observable-only blinding equipment
  (closed blinded-view types + recursive leak scan) mandatory in every
  certification;
- D1/D3 carrier mathematics is consumed, never re-derived; defects found at
  connection level ESCALATE (Ruling 5 boundary);
- the deeper-generation bracketing problem and A0 associator residues are
  RELOCATED into this campaign's Gate 0 — named, not laundered.
```

---

## 1. The campaign question

Opening premise, binding per the Closure Consciousness Clause:

```txt
This campaign exists because the project possesses NO field law.
Its predecessor proved a hub-scoped source-state carrier policy and
proved that the accepted emission stack cannot carry it. The successor
seeks the FIRST lawful carrier-borne field object — it improves nothing,
because there is nothing of the required kind to improve. Success is not
assumed; Gate 0 and the kill criteria exist because the absence may be
the final answer for this algebra family.
```

The campaign question:

```txt
Can an emission/propagation law be built over the proven carrier source
state whose emitted field observables are rich enough that a LABEL-BLIND
observer reconstructs the gauge-INVARIANT carrier relations — including
sign — from the field alone, beating blind controls?
```

## 2. Destination — verdicts (all three are successful endings)

```txt
Verdict F-A — carrier-borne field:
  a frozen, hash-committed emission law passes one-shot blind certification
  (invariant relations recovered from genuine field observables, sign
  included, beating structured + strict controls), and the certified field
  demonstrably improves generated-site reading beyond the D4 baseline.

Verdict F-B — bounded emission family:
  the observable exists (Gate 0 passes) but no emission law within the
  budgeted cycles carries it through blind certification; the field-
  inactivity boundary hardens, scoped to this architecture family.

Verdict F-C — no observable:
  Gate 0 fails — no well-defined gauge-invariant holonomy exists for the
  octonionic/Moufang connection on the medial hub; the field-activity
  question closes for this algebra family before any field is built.

The only failure is the unresolved middle, or a certification corrupted
by staple, target-matching, or tuning — which voids and counts.
```

## 3. Stations

### Gate 0 — Observable before broadcast

```txt
QUESTION: does a well-defined, gauge-invariant holonomy exist on the medial
hub under the octonionic (Moufang) carrier algebra?

WORK: model card + finite diagnostic. Two admissible resolutions:
  (i)  bracketing-INVARIANT loop classes proven by theorem — first lead:
       Moufang diassociativity (any two elements generate an associative
       subloop); loop classes whose link variables are 2-generated may have
       bracketing independence by structure, not convention;
  (ii) an explicit bracketing POLICY whose gauge-invariance is proven across
       the full 7x24 labeling orbit and disclosed as policy.

EXIT: the campaign's observable is defined, gauge-invariant, and computable
on the hub — or kill criterion K1 fires and the campaign ends at Verdict F-C.

Constraints: pure mathematics + finite diagnostic; no emission design, no
field substrate, no UI. Sealed predictions apply even here.
```

### Station F-I — Emission law design (model card)

```txt
QUESTION: what does the field emit?

WORK: model-card-first design of a carrier-borne emission law over the D1/D3
source state — genuine field quantities (multi-component / algebra-valued
amplitude, polarization, frame), maturity level 2 (fiber-valued emitted
field; source-lookup forbidden) with level 3 (carrier-dynamic transport /
connection field) as the declared horizon. The law must state explicitly:
  - its emitted observable set (the visible-field manifest, in advance);
  - what its tuple reduction loses (reduction honesty, inherited law);
  - how Gate 0's invariant observables are carried in principle.
ANTI-STAPLE check at design time: no carrier LABEL (signed lift, unit, root
identity, flag id, provenance token) appears in the emitted observable set.

EXIT: model card ratified; design runs counted from here forward.
```

### Station F-II — Field substrate build

```txt
WORK: the F1/F2/G0 analogs for the new law — graph field table, spatial
support projection, generational update — as pure tables + diagnostics
(model before UI; the predecessor's architecture pattern is the portable
part and is reused deliberately).

EXIT: substrate diagnostics pass on well-formedness; the generational
identity (delta = born contributions) re-proven under the new law;
NO field-activity claim yet — that is certification's alone.
```

### Station F-III — Blind certification gauntlet

```txt
WORK: one-shot sealed certifications under Riders A and B:
  - emission law FROZEN and hash-committed; sealed predictions (values,
    thresholds, decision rule — total, no gaps) filed to mothership;
  - observable-only blinding: closed blinded-view types, recursive leak
    scan, zero exemptions; leak hits void cells by code;
  - reconstruction scored against structured + strict-all controls;
    r (incidence) and g (sign/orientation) scored separately;
    g >= 0.90 required — unsigned geometry is not the fiber;
  - no target-matching in the reconstructor (Rider A);
  - tuning inside a certification voids it and counts (Rider B).

CYCLE BUDGET: N = 3 design->certification cycles (charter-fixed here,
before the first cycle; the sovereign may lower but not raise N after
cycle 1 has run). Exhaustion fires kill criterion K2 -> Verdict F-B.

EXIT: one certification passes -> the new field is FIELD-ACTIVE for the
certified relations, scoped to the tested instance(s); or K2.
```

### Station F-IV — Human fruit

```txt
WORK: readings consume the CERTIFIED field-active relations (statuses
visible; uncertified content stays source-state-only and says so).
The human sitting compares legibility of the proving event against the
D4 baseline from the predecessor campaign — that baseline is why
Option A ran first.

EXIT / Decision: improves beyond the D4 baseline -> Verdict F-A direction;
no improvement -> the certified field is recorded as physics without
human fruit, and the verdict deliberation weighs it honestly.
```

### Forward ladder (recorded, not authorized)

```txt
- octa-route field stack under the new law (lifts the predecessor's
  blocked-until-octa-field-stack honestly);
- Bench-3 / order-5 (icosahedral, golden-ratio) — ladder-record only;
- 3D field-map rendering of a CERTIFIED field — only after F-IV, under
  a display contract (the predecessor's Station V genre).
```

## 4. Kill criteria (binding)

```txt
K1  Gate 0 failure: no well-defined gauge-invariant holonomy -> Verdict F-C.
K2  certification exhaustion: N=3 cycles fail blind certification -> F-B.
K3  staple detection: any certification voided by leak-scan hits, label
    emission, target-matching, or in-run tuning is disclosed, counts
    toward N, and triggers a mothership process review before any next cycle.
K4  the honest negative: if the richest defensible emission within scope
    cannot carry the invariants, the boundary is final for this family;
    no scope widening to escape K4 without a sovereign-level new charter.
```

## 5. Roles, discipline, budget

```txt
Command chain unchanged: sovereign -> mothership -> lieutenant -> Claude Code.
Repo-Identity Gate heads every prompt and report. Plan-mode-first per
station; expected outcomes withheld from the implementer; per-station
budget 1-4 runs + memo (certification cycles counted separately under N);
a budget breach is the drift signature -> stop, escalate. Every station
closes with a ratified memo. This charter is amended in docs/governance,
never silently contradicted.

Frozen throughout (inherited): no S0, no topology workspace, no packet
writing, no Shape mutation, no operation registration, no new
route/gate/support/region families, no UI before the forward ladder's
display contract, no universal-law language — every claim scoped and
basis-relative.
```

## 6. Finalization conditions

```txt
This charter takes force when:
  1. the predecessor campaign's closing verdict is ratified (D4 demoted to
     an optional non-blocking note per the D4-Demotion Ruling);
  2. the F-IV baseline is recorded — either the D4-note (if held) or the
     archived artifact pair (baseline + revised reading + annex) with the
     contamination caveat;
  3. the sovereign confirms campaign start;
  4. mothership issues the Gate 0 entry order.
Until then it binds nothing and authorizes nothing.
```
