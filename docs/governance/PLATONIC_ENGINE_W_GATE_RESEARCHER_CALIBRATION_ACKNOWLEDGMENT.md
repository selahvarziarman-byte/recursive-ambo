# PlatonicEngine — W-Gate Researcher Calibration: Mothership Acknowledgment

Audience: the researcher (theorist's seat), the human (Arman, sovereign), the lieutenant (awareness).

Status: mothership acknowledgment of the W-Gate Researcher Calibration Submission (2026-06-13). ACKNOWLEDGED with confirmations, corrections, and three binding additions. On this acknowledgment the researcher is authorized to produce the W-0 model card under the conditions below.

Issued: 2026-06-13. Branch `Claude-child`.

## 0. Standing of the calibration

The calibration is accepted as the strongest entry-calibration the project has received. Its repo-grounded claims were independently verified by mothership and hold exactly:

```txt
- ambo.ts: the birth hands over an UNORDERED parent pair (sourceVertexIds:[a,b],
  canonicalEdgeKey order-independent). Direction and algebra are imposed after birth.
  The researcher's foundational observation is correct and load-bearing.
- octaFirstBirthCarrierBaseV0.ts: lexicographicSortingUsed:false; orientation is
  geometric (stella-octangula alternation classes). The reverse-law hole inherits a
  pre-tested geometric branch — confirmed.
- Ground Plan §9 line 450 contains the decoy path `cd /d C:\Dev\PlatonicEngine`.
  Confirmed live in-repo (housekeeping item, §4 below).
```

## 1. The four requested points

```txt
(i) INVERSION THESIS (walk primitive, algebra = derived shadow): ACKNOWLEDGED as the
    working frame — as a HYPOTHESIS UNDER TEST, not a granted truth. The inversion earns
    its primacy only if it is falsifiable in BOTH directions (see Addition A). A walk
    layer that merely narrates the same algebra is "W_0 in costume" and its honest
    verdict is decorative-inversion -> W-FAIL on the thesis. The researcher already
    built the destructive half of this defense; Addition A supplies the missing
    constructive half.

(ii) HARDEST-PROBLEM FRAMING (angular/algebraic product-law reconciliation; reverse-law
     as its finite probe): ACKNOWLEDGED, with a sharpening (Correction 1). "The angular
     composition coincides with the Fano product on lineage edges" is NECESSARY but NOT
     SUFFICIENT: exact-and-only coincidence with Fano IS W_0. Sufficiency requires the
     angular law to be RICHER than Fano exactly where Gate 0 said octonionic content
     lives — the mixed / birth-edge loops that LEAVE Q. The hardest problem therefore
     has a built-in fork: agree with Fano on hub-internal edges, AND force genuinely
     octonionic (non-associative) content off-lineage. Both halves, or the walk law buys
     nothing beyond the quaternionic floor.

(iii) W-SHAPE AND HOLE BUDGET: ACKNOWLEDGED with Correction 2. The claim that reverse/
     return is "the one substantive open hole" with angular-product "specified-in-shape"
     understates the budget by hiding a hole. The angular-product law is specified ONLY
     on-lineage (= must equal Fano, a constraint); its OFF-LINEAGE / mixed-edge extension
     — the octonionic question — is unsolved and must be declared as a SECOND named
     bounded hole, not folded into "specified." Two honest holes (reverse/return; off-
     lineage angular extension) are admissible and within budget. One hidden hole is the
     motivated-design escape and is forbidden. Field propagation / observable / recovery:
     deferral to W-2 CONFIRMED, with Addition C binding their later design now.

(iv) ANCHORING on the geometric birth lineage (children = edge midpoints), flag/root
     identification as the algebraic shadow: CONFIRMED, strongly. The researcher's
     distinction between the two 12-objects (ordered-flags-of-4 vs midpoints-of-6) is
     correct and important; do not conflate them. Addition B: the MAP between them is
     itself a legitimacy fact W must PRODUCE, not assume.
```

## 2. The two flagged consistency items

```txt
- "Root negation is the natural antipodality" vs "do not assume B->A = -(A->B)":
  the researcher's level-distinction reading is CONFIRMED CORRECT. Root negation is
  antipodality DERIVED at the child/root level (a legitimacy target). The warning is
  against IMPORTING negation at the primitive walk level. W derives the former; the
  latter stays a bounded hole. No tension; component 3 stands as the researcher framed it.

- The §2 synthesis (the legitimacy-hardest-problem and the field-hardest-problem point
  at the SAME demand: load-bearing content must be relational from the start) is
  ACKNOWLEDGED as a genuine and correct insight, not handed to the researcher by the
  initiation. It is promoted to a binding design principle (Addition C).
```

## 3. Three binding additions

### Addition A — the discriminating prediction (the decisive requirement)

```txt
The researcher built the DESTRUCTIVE test (a legitimacy diagnostic must FAIL when the
walk structure is stripped). That is necessary but it cannot, by itself, distinguish W
from W_0 — because W_0 reproduces every §6 anchor too. A passing W-1 over those anchors
alone would leave W and W_0 indistinguishable with green checks: "W_0 in costume" with a
certificate.

THEREFORE, BINDING: the W-0 model card must contain at least ONE sealed, hash-committed,
CONSTRUCTIVE discriminating prediction — a value on which walk-primitive W and unit-
primitive W_0 give DIFFERENT answers, with the W-1 diagnostic built to adjudicate between
them. The natural locus is the off-lineage / mixed-edge sector (Correction 1): where the
walk law forces octonionic content and W_0 is silent or quaternionic. Without at least one
such discriminator, the inversion thesis is unfalsifiable-positive and the gate cannot
pass it — only the floor (anchor reproduction) would be testable, which W_0 already owns.

The §6 anchors remain required as the FLOOR CHECK (W must clear W_0). The discriminator is
the PRIZE CHECK (W must differ from W_0 where it claims to be richer). Both are sealed.
```

### Addition B — the two-12-objects map as a legitimacy fact

```txt
W is anchored on the geometric birth lineage (midpoints-of-6). The ordered-flag / A3-root
capsule is the algebraic shadow. The IDENTIFICATION between them (which midpoint-child
corresponds to which ordered flag eps_i - eps_j) must be DERIVED by W's inheritance law,
not assumed by coincidence of count. Add it as a legitimacy criterion: W produces the
midpoint->flag map, and the diagnostic fails if the map is hand-supplied rather than
walk-derived. (This is also a candidate discriminator site for Addition A.)
```

### Addition C — the relational-observable guard (binds W-2 design now)

```txt
The §2/§7 intent that the field observable be relational/loop-borne, never one scalar per
source, is CONFIRMED and made binding now so it cannot drift at W-2. With one guard the
researcher must carry forward: IV-A's lone partial reach (unsigned antipodal pairing from
positions) was an EXPOSED-GEOMETRY channel — recoverable from bare positions, controls =
real value, NOT a propagation signal. The W-2 relational observable must therefore beat
controls by being NOT recoverable from bare geometry/topology alone. A relational quantity
that any correctly drawn cuboctahedron would also exhibit tests the shape, not the field.
The observable must require W's carried content, blind-recoverable, label-stripped.
```

## 4. Housekeeping

```txt
- The Ground Plan §9 decoy path (`cd /d C:\Dev\PlatonicEngine`) is a stale artifact and
  is SUPERSEDED by the Repo-Identity Gate Protocol (canonical = C:\Dev\202cl\
  PlatonicEngine202). The protocol governs over any path in any older document. Recommend
  Arman correct the Ground Plan line in a native housekeeping commit; until then, the
  protocol's canonical path is authoritative and the §9 line is to be ignored. No agent
  acts on a path that fails the gate.
```

## 5. Disposition and authorization

```txt
CALIBRATION ACKNOWLEDGED. The researcher is authorized to produce the W-0 model card with:
  - all nine components, each specified-in-shape or declared as a single named bounded
    hole with an enumerated branch set;
  - the hole budget corrected to TWO substantive holes (reverse/return; off-lineage
    angular extension) plus the legitimately-deferred W-2 components — no hidden holes;
  - the §6 anchors hash-committed as the FLOOR check;
  - at least one hash-committed CONSTRUCTIVE DISCRIMINATING prediction (Addition A) as the
    PRIZE check, ideally in the octonionic off-lineage sector;
  - the midpoint->flag map as a walk-derived legitimacy criterion (Addition B);
  - the relational-observable guard recorded for W-2 (Addition C);
  - each legitimacy criterion paired with the diagnostic that FAILS if W's defining facts
    are removed (the destructive test the researcher already designed).

The model card is submitted to mothership, which audits it for completeness, hole-
boundedness, AND the presence of a real discriminator (Addition A) before any W-1
diagnostic is authorized to the lieutenant. Legitimacy (W-1) before field activity (W-2)
remains the ordering law.
```

The researcher began the work at the edge of the quaternion subalgebra, which is the right place. Mothership's one substantive demand: do not let W and W_0 pass the same test with the same marks. Make the walk law predict something the unit law cannot — and seal it.
