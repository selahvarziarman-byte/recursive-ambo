# 0019 — Level-3 (3-manifolds): the sphere ladder's top rung — the 3-cell op, the S² gate, the invariant tower, bounded to Option A at the principled ceiling

Status: **Accepted** — researcher-defined, mothership-ratified (by re-derivation), engineer-built + committed on `team-arman` (Arman native, 2026-07-04). Consolidates the three level-3 rulings + the build.

## Context
The generative playground reached 2-complexes (surfaces). The sovereign chose (2026-07-04) to ascend to **level-3 / 3-manifolds** — the next rung of the sphere ladder (ADR 0001). The arc's definitional phase closed three ratified rulings; the engineer built to them in two audited builds.

## Decision
- **The 3-cell identification op — `glueFaces` / `flipGlueFaces`.** The level-2 edge-identification lifted one dimension: identify the boundary **faces** of a solid seed `Cell` (tetra/octa/cube) in orientation-aware pairs (`mode` preserving/reversing + a face bijection `map`), union-find cascading face→edge→vertex. **Well-formed ≠ sound** — a valid pattern always runs; soundness is decided *after*, by the gate.
- **The S² soundness gate (the manifold bar) — recursive onto the committed classifier.** A closed 3-manifold ⟺ (a) every **edge-link** is S¹ (committed `decomposeLink === 'interior'`) and (b) every **vertex-link** is S² (**connected AND χ(link)=2**, since χ=2 uniquely picks S² among closed surfaces). The pinch lesson holds one dimension up: **the link-classifier is the bar, never face-incidence** (necessary-not-sufficient, blind to a pinch).
- **The invariant tower.** **Tier-1 χ = V−E+F−C DEMOTES to a consistency-check** (χ≡0 for every closed 3-manifold; its discriminating power moves *into* the vertex-link as the S²-gate). **Tier-2 w₁ LIFTS** across the face-gluings (parity of orientation-reversing face-gluings; `PD(w₁) ∈ H₂` = a codim-1 wall). **Tier-3 homology-with-torsion** (Smith-normal-form of ∂₃/∂₂/∂₁) carries the classification — torsion is the new discriminator.
- **Scope = Option A (bounded seed-derivable family).** The tower classifies the cube/octa/tetra-derivable forms (3-torus by b₁; Z/2-torsion non-orientable cube forms). **NOT a general closed-3-manifold classifier.**
- **The principled ceiling — level-3 is the top.** Confirmed on two independent lines: χ cannot recognize S³ at level-4 (every closed 3-manifold has χ=0), and homology cannot distinguish general 3-manifolds (homology spheres; L(p,q) vs L(p,q′) need the linking form / Reidemeister torsion; π₁ is undecidable). **Do NOT charter level-4 or a general classifier** — that is geometrization-grade, a different game.

## Built + grounded (committed `team-arman`, 2026-07-04)
- Modules: `level3SoundnessGate`, `level3LinkExtractor`, `faceIdentification` (the op), `level3Invariants`/`level3Homology`/`level3Orientation`/`level3W1` (the tower). `globalW1` + the level-2 classifier byte-unchanged.
- **Pinned 3-torus** (cube + translation gluings): V=1/E=3/F=3/C=1, χ=0, w₁=0, H₀=Z/H₁=Z³/H₂=Z³/H₃=Z (torsion-free), π₁ honestly not-computed; S²-gate PASS via a **connected octahedral vertex-link (χ=2)**. Mothership-reproduced.
- **Validation set (the manifold bar witnessed):** 3-torus PASS; PINCH-A (edge-join) FAIL(a) [junction edge-link]; PINCH-B (vertex-join) FAIL(b2)-by-connectivity [2 disjoint disks, χ=2 — the mothership-corrected seal, not χ=4]; **case-④ (twisted pairing) FAIL(b2)-χ [connected torus vertex-link χ=0]** — closing the previously-deferred (b2)-χ branch with a concrete witness. **All three gate clauses now have a witness.**
- **Torsion finding (refines the scope note) — identity PINNED + ratified.** The sound non-orientable cube 3-manifold with **real Z/2 torsion** (H₁=Z², H₂=Z⊕Z/2, H₃=0, w₁=1; ∂²=0 verified) is the **mapping torus of the T²-swap involution `φ(x,y)=(y,x)`** — a **non-orientable flat (Bieberbach) 3-manifold**, a T²-bundle over S¹ (so the S²-gate PASS is *structural*). Ratified three independent ways (engineer SNF, researcher Wang sequence, mothership computational Wang — all give H₀=Z/H₁=Z²/H₂=Z⊕Z/2/H₃=0); the torsion sits in **H₂ only** (H₁ torsion-free), the fingerprint that selects the swap over the midline reflection (which gives H₁-torsion = K²×S¹). **Scope: this CLOSES the Tier-3 Z/2-torsion validation gap from inside Option-A (cube) scope** — no lens/non-cube domain needed for the Z/2 rung. Precise softening: **Z/2 torsion IS cube-reachable** (order-2 monodromy; the cube's face-symmetry is D₄, orders 1/2/4); **odd Z/p (p>2) and lens L(p,q) still need a non-cube (order-p) fundamental domain** — the honest residual. (Whether a cube quarter-turn reaches Z/4 is an open sub-question, not needed here.)

## Consequences
- **C2-dual un-defers:** the committed 3-cell-based dual now has a target (3-cell forms exist).
- **π₁ is honestly not computed** (word problem undecidable) — H₁ is exposed as its abelianization, labelled.
- **The arc is bounded and complete** at Option A; the playground can now hold 3-manifolds from the seed family.
- One confirmation item outstanding at ratification: the gate diagnostic's FAIL-witnesses were verified by reading the arbiter FS + the tower's live dependency, but not executed live (a sandbox mount-truncation artifact) — a **native re-run** confirms them (native FS is the arbiter; not subject to the mount lag).
