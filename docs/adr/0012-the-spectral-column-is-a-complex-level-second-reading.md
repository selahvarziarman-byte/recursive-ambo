# 0012 — The spectral column is a complex-level second *reading* on the shared incidence substrate, not a second *arrow*

Ratifies the researcher's Merge Phase-1 *opening* ruling (`RESEARCHER_SYNTHESIS_CHARTER_SPECTRAL_COLUMN`), mothership-verified 2026-06-26. The merge folds the field/spectral line (arf `t28g`: complement-centered incidence kernel `Kσ`, response `γ`, skew-adjoint exchange) into the topological module as a second **reading**. This ADR records what is grounded and firm; what is held; and the open grounding target. It is the *opening* of the spectral workstream — firm at the single-regular-cell level, **not yet sealed over our forms**.

## Decisions (grounded; mothership-verified)

1. **Shared substrate, no new scaffold.** The spectral column reads the *same* incidence matrix `A` the topological reading already uses (`η = Aᵀq`; `AAᵀ = dI + G`). It attaches as a second reading of the one assembled complex — confirmed at the linear-algebra level.
2. **It reads the COMPLEX, never the FORM.** `γ` is **not** a form-invariant. Sealed counterexample: tetra, octa, cube are all S² (χ=2, orientable, genus 0) yet `γ = 2/3, 1/2, 4/9`. **`γ` / the spectral data must never enter `CONTEXT.md` as a form-invariant.** Coherence with our topological invariants holds only at the fixed-complex level.
3. **The honest datum is the eigenvalue SET, not a single `γ`.** Mothership-verified on the cube: the response operator `R = DσAσᵀ` has centered spectrum `{−4/9 ×3, −2/9 ×3, 0 ×2}` (trace −2.0, trace² 0.7407 — confirmed by my own run; maps to adjacency eigenvalues λ=1→−4/9, λ=−1→−2/9, λ=−3→0). The sealed `γ = −4/9` is only the *coordinate-mode top* eigenvalue; a general centered source mixes the two nonzero values. The single number under-reports the operator.
4. **NOT a second arrow (T2 ruled).** The skew-adjoint "field exchange" `J = [[0,−Dσ†],[Dσ,0]]` conserves energy *by construction* — true of every skew generator, hence content-free — and the only observables of `e^{tJ}` are the singular values of `Dσ` (a static SVD). It evolves but carries nothing a static spectral read does not. The spectral column is a second **reading** (static spectral data of the complex); **genealogical depth (ADR 0009) remains the only arrow.**
5. **Admissible after IV-A (T1, preliminary).** `γ` and the spectrum are structural invariants of the complex's own incidence/adjacency — not a broadcast observable recoverable under blinding — so on the admissible side of the sealed IV-A negative. *Confirm against the exact IV-A criterion before final seal.*
6. **The assembly discipline coincides with ours.** Their multi-cell context bundle — children `η` *global* (= our pull-back / global edge), contexts `ζσ,v` *local* per cell-vertex incidence, and a disagreement witness `δσ,τ,v` **reported, never auto-averaged** — *is* our co-location ≠ identity / per-incidence registry / B-twin non-merge, one column over. Both columns read one assembled complex per-incidence and refuse to auto-identify across shared supports.

## Held / open (the next grounding target)

- **Multi-cell over OUR forms is unsealed.** `Kσ`/`γ` is well-posed only on **regular single cells** (the Platonic seeds). Our surface-zoo and five-necessity assembled complexes are **irregular and multi-cell**, where `d` and "the" `λ` are not single-valued. The multi-cell spectral-assembly law (their Theory-2, **unbuilt even on their side**) must be grounded on our objects before the spectral column is sealed over our forms. **This lands on the Enabler-2 (genealogy / per-incidence registry) seam — the merge's deepest convergence.**
- **`γ` does NOT inform Enabler-3 (product).** `cube = tetra²` is a numeric coincidence (octa, also S², breaks it; `(d+λ)` is not `□`-multiplicative; tetra is not a `□`-factor of the cube). A product spectral law, if wanted, is the `□`-spectrum composition `spec(G□H)=spec(G)+spec(H)` — not `γ`.

## Status

Ratified as the **opening** of the spectral workstream. Decisions 1–6 are firm and canonical (the second-reading attachment + the guards). The spectral column is **not** sealed over our forms until the multi-cell grounding (E2 seam) lands. `CONTEXT.md` gains the spectral column as a complex-level second reading when that grounding closes; until then this ADR is the record and the `γ`-is-not-a-form-invariant guard is binding.

---

## Addendum (2026-06-26) — the second arrow refined: spectral flow of the genealogy (candidate, gated)

Decision 4 ("not a second arrow", T2) is **refined, not reversed.** It holds for **frozen-complex** dynamics (the skew-adjoint exchange is hollow — its frequencies are the static singular values of `D`; confirmed). The researcher has since grounded a **non-frozen** candidate the frozen analysis could not see: the **spectral flow of the genealogy** — read the spectral operators *along* the genealogical growth (ADR 0009's one arrow) and the eigenvalues flow.

**Grounded (mothership-verified, independent run).** A vertex birthing into K4 (`s:0→1`) flows the Laplacian spectrum `{0,0,4,4,4} → {0,3,5,5,5}`: a zero mode lifts (Fiedler `0→3`), net crossing of `E_ref=1` is `1`, and the **path does not return** (`s=0 ≠ s=1`). So the flow is **derived** (`s` = the birth, no free parameter), **irreversible**, and **non-hollow** (a path invariant, not recoverable from any single frozen generation). Confirmed to the researcher's exact values.

**`w₁` is its Z/2 shadow** (sound structural correspondence). The flip-glue sign `±1` is the holonomy of the orientation line `= w₁ ∈ Z/2`; made continuous (`±1 → e^{iθ}`) it is the orientation-line case of the flow, with `θ=π ↔ w₁=1`. The committed static `w₁` (ADR 0001 / signed pull-back / the non-orientable zoo) is the shadow; the flow is its dynamical lift. (Exact `SF mod 2 = w₁`: a flagged follow-on computation, not yet sealed.)

**This marries the two columns at the root:** genealogical depth (the topological arrow) *is* the time; the spectral column read along it *is* the field; same operators — frozen → S1 (static complex-level reading), flowed → the arrow. The "field" is not bolted on; it is the spectralization of our own genealogy.

**GATE (binding).** Existence is grounded; **legibility is not.** The dynamics is **NOT sanctioned** as a second arrow — not into `CONTEXT.md`, no full ADR, no engineer build — until the researcher's **§7 legibility test** passes: that the birth pattern/order leaves a spectral-flow signature that **changes a generated-site reading** (the two-tetrahedra-sharing-a-face test, two birth orders; falsified if `SF` is order-blind, vertex-count-trivial, or touches no site reading). Existence-grounded ≠ legibility-grounded; sanction fires only on §7 — the line between a grounded prize and a pretty number.
