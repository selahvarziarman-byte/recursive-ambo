diff --git a/src/components/Panels.tsx b/src/components/Panels.tsx
index d8e4a05..73f800f 100644
--- a/src/components/Panels.tsx
+++ b/src/components/Panels.tsx
@@ -1,6 +1,6 @@
 import { useEffect, useMemo, useState } from 'react';
 import { seedRegistry } from '../data/seeds';
-import { defaultOperation } from '../operations/registry';
+import { registeredOperations } from '../operations/registry';
 import { formatVec3 } from '../lib/shape';
 import { useGeometryStore } from '../store/geometryStore';
 import type { Cell, CellKind, JsonValue, Shape, VertexDataPacket } from '../types/geometry';
@@ -45,26 +45,34 @@ export function OperationControls() {
   const resetViewLayout = useGeometryStore((state) => state.resetViewLayout);
   const shape = useCurrentShape();
   const selectedCell = findCell(shape, selectedCellId);
-  const operation = defaultOperation;
   const operationContext = { shape, selectedCellId, selectedCell };
-  const canApply = operation.canApply(operationContext);
   const cellCounts = countCellsByKind(shape);
-  const operationStatus =
-    operation.getStatusMessage?.(operationContext) ??
-    operation.getDisabledReason(operationContext) ??
-    operation.description;
 
   return (
     <Panel title="Operation Controls">
-      <button
-        type="button"
-        onClick={() => applyOperationToSelection(operation.id)}
-        disabled={!canApply}
-        className="h-10 w-full rounded border border-amber-500/70 bg-amber-400 px-3 text-sm font-semibold text-stone-950 transition hover:bg-amber-300 focus:outline-none focus:ring-2 focus:ring-amber-200 disabled:cursor-not-allowed disabled:border-stone-700 disabled:bg-stone-800 disabled:text-stone-500"
-      >
-        Apply {operation.label}
-      </button>
-      <p className="mt-3 text-sm leading-5 text-stone-400">{operationStatus}</p>
+      <div className="grid gap-3">
+        {registeredOperations.map((operation) => {
+          const canApply = operation.canApply(operationContext);
+          const operationStatus =
+            operation.getStatusMessage?.(operationContext) ??
+            operation.getDisabledReason(operationContext) ??
+            operation.description;
+
+          return (
+            <div key={operation.id} className="grid gap-2">
+              <button
+                type="button"
+                onClick={() => applyOperationToSelection(operation.id)}
+                disabled={!canApply}
+                className="h-10 w-full rounded border border-amber-500/70 bg-amber-400 px-3 text-sm font-semibold text-stone-950 transition hover:bg-amber-300 focus:outline-none focus:ring-2 focus:ring-amber-200 disabled:cursor-not-allowed disabled:border-stone-700 disabled:bg-stone-800 disabled:text-stone-500"
+              >
+                Apply {operation.label}
+              </button>
+              <p className="text-sm leading-5 text-stone-400">{operationStatus}</p>
+            </div>
+          );
+        })}
+      </div>
       <button
         type="button"
         onClick={resetWorkspace}
@@ -187,6 +195,10 @@ export function ObjectInspector() {
             <dd className="break-all font-mono text-xs text-stone-300">
               {selectedCell.parentCellId ?? 'none'}
             </dd>
+            <dt className="text-stone-500">Dual counterpart</dt>
+            <dd className="break-all font-mono text-xs text-stone-300">
+              {selectedCell.dualCounterpartCellId ?? selectedCell.dualSourceCellId ?? 'none'}
+            </dd>
             <dt className="text-stone-500">Source op</dt>
             <dd className="text-stone-200">{selectedCell.sourceOperation}</dd>
           </dl>
@@ -417,6 +429,10 @@ function findCell(shape: Shape, cellId: string | null): Cell | null {
 }
 
 function describeCellTopology(cell: Cell): string {
+  if (cell.topology) {
+    return cell.topology;
+  }
+
   if (cell.kind === 'seed') {
     return 'tetrahedron';
   }
@@ -451,6 +467,7 @@ function countCellsByKind(shape: Shape): Record<CellKind, number> {
       parent: 0,
       core: 0,
       residue: 0,
+      dual: 0,
     },
   );
 }
@@ -461,6 +478,7 @@ function formatCellCounts(counts: Record<CellKind, number>): string {
     ['parent', counts.parent],
     ['core', counts.core],
     ['residue', counts.residue],
+    ['dual', counts.dual],
   ];
 
   return parts
diff --git a/src/components/Workspace3D.tsx b/src/components/Workspace3D.tsx
index 5b93acc..1615749 100644
--- a/src/components/Workspace3D.tsx
+++ b/src/components/Workspace3D.tsx
@@ -334,6 +334,23 @@ function computeCellDisplayOffsets(shape: Shape, explodeAmount: number): Map<str
 
     visiting.add(cell.id);
 
+    if (cell.relationToParent === 'dual-counterpart') {
+      const sourceCell = getDualCounterpartAnchor(cell, cellById);
+      const sourceOffset = sourceCell ? offsetForCell(sourceCell) : ([0, 0, 0] as Vec3);
+      const sourceCentroid = sourceCell ? centroids.get(sourceCell.id) : null;
+      const cellCentroidValue = centroids.get(cell.id) ?? ([0, 0, 0] as Vec3);
+      const direction = normalizeVec3(
+        sourceCentroid ? subtractVec3(cellCentroidValue, sourceCentroid) : [0, 0, 0],
+        fallbackDirection(`${cell.id}:dual-counterpart`),
+      );
+      const offset = addVec3(sourceOffset, scaleVec3(direction, explodeDistance * 0.5));
+
+      offsets.set(cell.id, offset);
+      visiting.delete(cell.id);
+
+      return offset;
+    }
+
     const parentCell = cell.parentCellId ? cellById.get(cell.parentCellId) : null;
     const parentOffset = parentCell ? offsetForCell(parentCell) : ([0, 0, 0] as Vec3);
     const parentCentroid = parentCell ? centroids.get(parentCell.id) : null;
@@ -354,6 +371,16 @@ function computeCellDisplayOffsets(shape: Shape, explodeAmount: number): Map<str
   return offsets;
 }
 
+function getDualCounterpartAnchor(cell: Cell, cellById: Map<string, Cell>): Cell | null {
+  const anchorId = cell.dualSourceCellId ?? cell.dualCounterpartCellId ?? cell.parentCellId;
+
+  if (!anchorId || anchorId === cell.id) {
+    return null;
+  }
+
+  return cellById.get(anchorId) ?? null;
+}
+
 function cellCentroid(shape: Shape, cell: Cell): Vec3 {
   const positions = cell.vertexIds
     .map((vertexId) => shape.vertices[vertexId]?.position)
diff --git a/src/operations/registry.ts b/src/operations/registry.ts
index b14fbc6..f4703ba 100644
--- a/src/operations/registry.ts
+++ b/src/operations/registry.ts
@@ -1,7 +1,8 @@
 import { amboDissectionOperation } from './amboDissectionOperation';
+import { dualOperation } from './dualOperation';
 import type { GeometryOperation } from './types';
 
-export const registeredOperations: GeometryOperation[] = [amboDissectionOperation];
+export const registeredOperations: GeometryOperation[] = [amboDissectionOperation, dualOperation];
 
 export const operationRegistry: Record<string, GeometryOperation> = Object.fromEntries(
   registeredOperations.map((operation) => [operation.id, operation]),
diff --git a/src/operations/types.ts b/src/operations/types.ts
index 3387120..3a71157 100644
--- a/src/operations/types.ts
+++ b/src/operations/types.ts
@@ -1,4 +1,4 @@
-import type { Cell, CellId, CellKind, Shape } from '../types/geometry';
+import type { Cell, CellId, CellKind, Shape, VertexId } from '../types/geometry';
 
 export interface OperationContext {
   shape: Shape;
@@ -11,6 +11,14 @@ export interface OperationTargetMetadata {
   topology: string;
 }
 
+export interface OperationExecutionResult {
+  shape: Shape;
+  selectedCellId?: CellId | null;
+  selectedVertexId?: VertexId | null;
+}
+
+export type OperationResult = Shape | OperationExecutionResult;
+
 export interface GeometryOperation {
   id: string;
   label: string;
@@ -19,5 +27,5 @@ export interface GeometryOperation {
   canApply: (context: OperationContext) => boolean;
   getDisabledReason: (context: OperationContext) => string | null;
   getStatusMessage?: (context: OperationContext) => string;
-  execute: (context: OperationContext) => Shape;
+  execute: (context: OperationContext) => OperationResult;
 }
diff --git a/src/store/geometryStore.ts b/src/store/geometryStore.ts
index 7e09ce2..6c3dee3 100644
--- a/src/store/geometryStore.ts
+++ b/src/store/geometryStore.ts
@@ -1,6 +1,7 @@
 import { create } from 'zustand';
 import { createSeedShape } from '../data/seeds';
 import { getOperation } from '../operations/registry';
+import type { OperationExecutionResult, OperationResult } from '../operations/types';
 import type { CellId, SeedKey, Shape, ShapeId, VertexDataPacket, VertexId } from '../types/geometry';
 
 interface CellVisibility {
@@ -118,7 +119,8 @@ export const useGeometryStore = create<GeometryState>((set, get) => ({
       return;
     }
 
-    const nextShape = operation.execute(context);
+    const result = normalizeOperationResult(operation.execute(context));
+    const nextShape = result.shape;
     const nextShapeOrder = shapeOrder.includes(nextShape.id)
       ? shapeOrder
       : [...shapeOrder, nextShape.id];
@@ -130,8 +132,8 @@ export const useGeometryStore = create<GeometryState>((set, get) => ({
       },
       shapeOrder: nextShapeOrder,
       currentShapeId: nextShape.id,
-      selectedCellId: null,
-      selectedVertexId: null,
+      selectedCellId: result.selectedCellId ?? null,
+      selectedVertexId: result.selectedVertexId ?? null,
     });
   },
   applyAmboDissectionToCurrent: () => {
@@ -211,3 +213,11 @@ export const useGeometryStore = create<GeometryState>((set, get) => ({
     });
   },
 }));
+
+function normalizeOperationResult(result: OperationResult): OperationExecutionResult {
+  if ('shape' in result) {
+    return result;
+  }
+
+  return { shape: result };
+}
diff --git a/src/types/geometry.ts b/src/types/geometry.ts
index 0ae6334..b6bd4cd 100644
--- a/src/types/geometry.ts
+++ b/src/types/geometry.ts
@@ -7,8 +7,16 @@ export type FaceId = string;
 export type EdgeId = string;
 export type SeedKey = string;
 
-export type OperationKind = 'seed' | 'ambo' | 'ambo-dissection';
-export type CellKind = 'seed' | 'parent' | 'core' | 'residue';
+export type OperationKind = 'seed' | 'ambo' | 'ambo-dissection' | 'dual';
+export type CellKind = 'seed' | 'parent' | 'core' | 'residue' | 'dual';
+export type CellTopology =
+  | 'tetrahedron'
+  | 'octahedron'
+  | 'cube'
+  | 'cuboctahedron'
+  | 'square-pyramid'
+  | 'unknown';
+export type CellParentRelation = 'dissection-child' | 'dual-counterpart' | 'snapshot';
 
 export type JsonValue =
   | string
@@ -77,6 +85,7 @@ export type FaceRole =
   | 'ambo-face-from-vertex'
   | 'dissection-core-face'
   | 'dissection-residue-face'
+  | 'dual-face'
   | 'parent-cell-face';
 
 export interface Face {
@@ -102,6 +111,8 @@ export interface ShapeGenealogy {
 export interface Cell {
   id: CellId;
   kind: CellKind;
+  topology?: CellTopology;
+  relationToParent?: CellParentRelation;
   generationDepth: number;
   parentCellId: CellId | null;
   sourceOperation: OperationKind;
@@ -110,6 +121,8 @@ export interface Cell {
   sourceVertexIds: VertexId[];
   sourceEdgeIds: EdgeId[];
   preservedVertexId?: VertexId;
+  dualCounterpartCellId?: CellId;
+  dualSourceCellId?: CellId;
   data?: PacketData;
   lineage?: PacketLineage;
 }
diff --git a/tsconfig.tsbuildinfo b/tsconfig.tsbuildinfo
index 53b05eb..4ecd5fa 100644
--- a/tsconfig.tsbuildinfo
+++ b/tsconfig.tsbuildinfo
@@ -1 +1 @@
-{"root":["./src/app.tsx","./src/main.tsx","./src/vite-env.d.ts","./src/components/panel.tsx","./src/components/panels.tsx","./src/components/workspace3d.tsx","./src/data/seeds.ts","./src/lib/ambo.ts","./src/lib/ids.ts","./src/lib/packets.ts","./src/lib/shape.ts","./src/operations/ambodissectionoperation.ts","./src/operations/registry.ts","./src/operations/types.ts","./src/store/geometrystore.ts","./src/types/geometry.ts"],"version":"5.9.3"}
\ No newline at end of file
+{"root":["./src/app.tsx","./src/main.tsx","./src/vite-env.d.ts","./src/components/panel.tsx","./src/components/panels.tsx","./src/components/workspace3d.tsx","./src/data/seeds.ts","./src/lib/ambo.ts","./src/lib/dual.ts","./src/lib/ids.ts","./src/lib/packets.ts","./src/lib/shape.ts","./src/operations/ambodissectionoperation.ts","./src/operations/dualoperation.ts","./src/operations/registry.ts","./src/operations/types.ts","./src/store/geometrystore.ts","./src/types/geometry.ts"],"version":"5.9.3"}
\ No newline at end of file
