node_modules
dist
.vite
storybook-static
.env
.env.local
*.log
*_SEALED_PREDICTIONS.txt
*_SEALED_PREDICTIONS.txt

# off-repo handoff channel — mandates/reports between engineer and coder; never committed
.handoff/
